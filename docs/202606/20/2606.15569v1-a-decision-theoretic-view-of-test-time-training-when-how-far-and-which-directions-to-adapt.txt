Title: A Decision-Theoretic View of Test-Time Training: When, How Far, and Which Directions to Adapt

URL Source: https://arxiv.org/pdf/2606.15569v1

Published Time: Tue, 16 Jun 2026 01:14:28 GMT

Number of Pages: 39

Markdown Content:
# A Decision-Theoretic View of Test-Time Training: When, How Far, and Which Directions to Adapt 

Tomoya Wakayama 1

## Abstract 

Test-time training (TTT) adapts a pretrained model to each prompt via parameter updates, im-proving accuracy under pretraining-to-test distri-bution shifts. Yet, its performance often suffers from instability and sensitivity to hyperparame-ters such as update steps and subspace. We ex-plain this behavior through a decision-theoretic lens, treating TTT as implicit Bayesian inference in the kernel regime. Under a Gaussian process benchmark, we show that TTT reduces prediction error when updates are spectrally matched to the prompt’s signal-to-noise ratio and aligned with query-relevant eigen-directions. This perspec-tive underpins the following results: (1) we show when fixed update steps and subspaces fail un-der distribution shifts, motivating adaptive strate-gies; (2) we prove that selecting update steps via prompt evidence admits a PAC-Bayes guarantee against overfitting; and (3) we characterize the Bayes-optimal update subspace under a linear-Gaussian correction model, yielding a scoring rule for selecting Transformer blocks and heads. Our theory helps explain the empirical instability of TTT, taking a step toward principled guidance for when, how far, and which directions to adapt. 

## 1. Introduction 

Modern foundation models increasingly serve as general-purpose predictors, answering diverse user queries. Yet real deployments frequently deviate from pretraining con-ditions, in which a fixed (no-update) inference procedure can become brittle. This has kindled interest in test-time training (TTT), which updates model parameters using only the prompt to adapt to a specific test task. Following the 

> 1

RIKEN Center for Advanced Intelligence Project (AIP), Tokyo, Japan. Correspondence to: Tomoya Wakayama <to-moya.wakayama@riken.jp >.

Proceedings of the 43 rd International Conference on Machine Learning , Seoul, South Korea. PMLR 306, 2026. Copyright 2026 by the author(s). 

seminal work by Sun et al. (2020), a growing body of work has shown that TTT improves robustness and domain gen-eralization in vision (Wang et al., 2021; 2022; Yuan et al., 2023), extends to high-stakes settings such as medical image segmentation (Valanarasu et al., 2024), and adapts end-to-end automatic speech recognition models to non-stationary acoustic conditions (Kim et al., 2023; Lin et al., 2024). More recently, analogous ideas have also been explored for large language models (Hardt & Sun, 2024; H ¨ubotter et al., 2025a). While these empirical improvements are noteworthy, TTT often exhibits instability in practice. TTT’s performance is sensitive to optimization hyperparameters (e.g., update spaces and step count), and more steps can negatively impact performance (Zhao et al., 2023). This sensitivity is exac-erbated by non-stationary shifts and small-batch streaming (Boudiaf et al., 2022; Niu et al., 2022), and analogous issues have been reported for language models (Aky ¨urek et al., 2025). While recent works seek more stable updates and safer parameter choices (Alfarra et al., 2025; Sahoo et al., 2025), we still lack principled guidance for TTT. Prior theories (Gozeten et al., 2025; Kuwataka & Suzuki, 2025) have started to characterize when TTT can improve in-context learning (ICL, i.e., no parameter updates) in specific regimes (e.g., linearized transformers or structured task mod-els). Complementary to the ICL-centered analyses, H ¨ubotter et al. (2025b) propose a mechanistic explanation for why TTT can help at scale even on nominally in-distribution data. Still, general principles guiding the choice of optimization hyperparameters remain elusive. Our work focuses on the following questions: (i) when TTT reduces the predictive risk relative to ICL , (ii) how the number of test-time up-date steps affects performance , and (iii) which parameter subspace is effective for updating .In a kernel (linearized-quadratic) regime, where TTT typ-ically operates, we present a Bayes-risk perspective by in-terpreting TTT as implicit Bayesian inference . Here, the update steps and the update subspace act as hyperparameters of a prompt-induced prior. Our claims are as follows. 

(C1) Under a Gaussian process benchmark that couples prompt and query, we derive a conditional risk identity showing that query predictions benefit from both filter 1

> arXiv:2606.15569v1 [cs.LG] 14 Jun 2026 A Decision-Theoretic View of Test-Time Training

match (in eigenvalue space) and eigen-alignment with the prompt-query coupling vector (Thm. 5.2). We further show that fixed steps and fixed update subspaces can fail (Propositions 6.1–6.3). 

(C2) We show that selecting the number of updates by prompt evidence provides a PAC-Bayes guarantee and mitigates overfitting to the prompt (Thm. 7.2). 

(C3) Under a linear-Gaussian correction prior, we char-acterize the Bayes-optimal update subspace via an information-capture matrix, yielding an attention block/head scoring rule for prediction (Thm. 8.2); see App. E.6 for a regret bound. Together, these results give a principled starting point for analyzing few-step, restricted-update TTT. We do not aim to give a complete theory of fully nonlinear TTT. Rather, we use a local Bayesian lens to identify design principles for TTT: how far to update, and which directions to update. The Transformer specializations in Secs. 3.3 and 8.2 make this link to block/head-level TTT explicit. 

1.1. Related Work Test-time training. TTT is widely used for domain gener-alization and test-time adaptation (Sun et al., 2020; Niu et al., 2022). Recent work studies parameter-efficient or specialization-oriented updates for foundation models (H ¨ubotter et al., 2025b). Theoretically, Gozeten et al. (2025) relate TTT to ICL in linearized transformer settings, and Kuwataka & Suzuki (2025) show multi-step TTT can en-hance ICL on single-index tasks. We cast TTT as Bayesian inference in a kernel regime, analyzing a risk identity and PAC-Bayes theory for steps and optimal update subspaces. 

Bayesian view of test-time behavior. ICL is often viewed as amortized Bayesian inference learned through pretraining (Reuter et al., 2025; Mittal et al., 2025). Be-cause its inference mapping is fixed, correcting for prior mismatch (Wakayama & Suzuki, 2025) or the amortiza-tion gap (Margossian & Blei, 2024) at test time remains challenging. Unlike fixed in-context inference, we view TTT as prompt-induced Bayesian inference, revealing how evidence-tuned steps and subspaces correct prior mismatch. 

## 2. Setup: tasks, prompts, and risk 

In TTT and ICL, we consider a distribution over tasks: at test time, a latent task is sampled, and then the prompt and query are generated from the corresponding task-specific data distribution. This section introduces a mixture (hierar-chical) regression setting as in Wakayama & Suzuki (2025) and formalizes an analytical framework. 

Mixture task model and test-time information Let α

denote a latent task variable drawn from a meta-distribution 

π over tasks. Conditioned on α, data pairs (X, Y ) follow a task-specific distribution Pα over X × Y ⊂ Rdx × R.At test time, we observe an i.i.d. labeled prompt Pn := 

{(Xi, Y i)}ni=1 , followed by a query input X and target Y ,all generated from the same task 

α ∼ π, (Xi, Y i) | α i.i.d.

∼ Pα , (X, Y ) | α ∼ P α. (1) At test time, only (Pn, X ) is observed, while α is not. Throughout, we assume each labeled example corresponds to a single token position. 

Bayes gap. We use the squared loss ℓ(u, v ) = ( u − v)2,following Gozeten et al. (2025); Kuwataka & Suzuki (2025). To evaluate a measurable map M : ( Pn, X ) 7 → bY ∈ R,we adopt the Bayes risk (see Murphy, 2022, §5.3.1.2), i.e., the expected squared prediction error R(M ) := 

E M (Pn, X ) − Y 2, where the expectation is over the joint distribution in (1). In this setting, the minimizer of the Bayes risk among all square-integrable measurable maps of (Pn, X ) is the con-ditional mean MBayes (Pn, X ) := E[Y | Pn, X ]. See Ap-pendix C for a review of Bayes risk and the Bayes predictor. 

Proposition 2.1 (Orthogonal Bayes-risk decomposition) .

Assume E[M (Pn, X )2] < ∞ and E[Y 2] < ∞. Then 

R(M ) = E

h M (Pn, X ) − MBayes (Pn, X )2i| {z }   

> =: G(M)(Bayes gap)

+ E

h MBayes (Pn, X ) − Y 2i| {z }     

> =: V=E[Var( Y|Pn,X )] (posterior variance)

. (2) Here, V is the irreducible uncertainty about Y after observ-ing (Pn, X ) and is independent of predictor M . In contrast, the Bayes gap G(M ) is the excess risk relative to MBayes ,measuring how accurately M approximates MBayes as a map of (Pn, X ). Thus, any improvement of TTT must come from reducing G.

Correction view. To analyze the adaptation process, con-sider the setting where we correct a base predictor B (e.g., ICL). We write any adapted rule as M = B + C with cor-rection C. Let ∆B (Pn, X ) := E[Y | Pn, X ] − B(Pn, X )

be the Bayes-optimal correction. Under squared loss, the Bayes gap of M simplifies to correction gap G(M ) = 

E{C(Pn, X ) − ∆B (Pn, X )}2. Thus, improving over B

amounts to learning a prompt-dependent correction. As the Bayes correction under the general model is typically intractable, in Secs. 5–8, we analyze the Bayes gap relative to a tractable benchmark Bayes correction. 2A Decision-Theoretic View of Test-Time Training 

## 3. A Local Linearized Model for TTT 

This section builds a tractable local model for few-step TTT. We do not assume that practical TTT is globally lin-ear. Instead, we use the first-order model as a controlled approximation to the nonlinear update path. This approxi-mation is appropriate when the update subspace is restricted and the number of test-time steps is modest; Appendix F.1 gives a finite-step deviation bound between the nonlinear and linearized prompt corrections. Within this model, TTT becomes a linear inverse problem in the update parameters. Since test-time supervision is available only through the labeled prompt, we use leave-one-out residuals as noisy observations of the desired correction at prompt points. 

Residual correction induced by a base predictor. Let TTT output MTTT (Pn, x ) = B(Pn, x ) + C(Pn, x ). We form leave-one-out residuals r ∈ Rn with ri := yi −

B(P (−i) 

> n

, x i), where P (−i) 

> n

:= Pn \ { (xi, y i)}. Define the (prompt-point) Bayes-optimal correction by f⋆,i := E[Yi |

P (−i) 

> n

, x i] − B(P (−i) 

> n

, x i). Then, E[ri − f⋆,i | P (−i) 

> n

, x i] = 0, so r is an unbiased noisy observation of f⋆. We use r to avoid label leakage: the target yi is not used in computing either B(P (−i) 

> n

, x i) or the Jacobian feature, defined later. 

3.1. Restricted update subspace and Jacobian features 

Let w ∈ Rp denote the full parameter vector of a model and w0 its pretrained value. Let fw(x; Pn) ∈ R denote the model prediction at query input x given prompt Pn

under parameters w. Note that B(Pn, x ) = fw0 (x; Pn).Since practical TTT typically updates only a small subset of parameters or a low-rank reparameterization (Wang et al., 2021; Niu et al., 2022; H ¨ubotter et al., 2025a;b), we consider the following setting. 

Assumption 3.1 (Restricted update subspace) . There exists a d(< p )-dimensional linear subspace U ⊂ Rp such that test-time updates satisfy w ∈ w0 + U.Only the subspace U matters, not a particular basis, that is, any full-column-rank eA with range( eA) = U can be replaced, without loss of generality, by an orthonormal basis. Let A ∈ Rp×d be such a basis ( A⊤A = Id), so that any update can be written as w(θ) = w0 + Aθ . We denote the orthogonal projector by ΠU := AA ⊤.Since TTT uses few gradient steps within a low-dimensional update subspace, a first-order expansion in θ is a natural local surrogate (Jacot et al., 2018; Lee et al., 2019; Chizat et al., 2019). We refer to this few-step approximation as a local linear (kernel/NTK) regime. 

Assumption 3.2 (Linearized surrogate model) .

We analyze TTT through the linearized surrogate 

fθ (x; Pn) := fw0 (x; Pn) + ϕ(x; Pn)⊤θ, with Jacobian features ϕ(x; Pn) := ∇θ fw(θ)(x; Pn) θ=0 .Note that the approximation fθ (x; Pn) ≈ fw0 (x; Pn) + 

ϕ(x; Pn)⊤θ is valid in the few-step regime, as in common practice (Aky ¨urek et al., 2025; Sun et al., 2020). See Ap-pendix F.1 for validation of the assumption. Stack the Jacobian features into the matrix Φ := (ϕ(x1; P (−1)  

> n

), . . . , ϕ (xn; P (−n) 

> n

)) ⊤ ∈ Rn×d. Under As-sumption 3.2, the vector of prediction shifts on the prompt is approximately Φθ. Hence, fitting the residuals r reduces to a linear inverse problem in θ.

3.2. Quadratic prompt loss and the prompt-induced geometry K(Pn)

Based on the linearization, we use the quadratic prompt objective L(θ) := 12σ2 ∥r − Φθ∥22. For fixed σ2, minimizing 

L is maximum likelihood estimation in the Gaussian regres-sion model r = Φ θ⋆ + ε with ε ∼ N (0 , σ 2In), where θ⋆ is the (unknown) optimal update parameter that best explains the residuals under the linearization and σ2 is an effective noise level that accounts for label noise and linearization er-rors. In Sec. 4, we study the few-step optimization dynamics of L.In this kernel regime, the optimization dynamics on this objective are governed by the prompt-space Gram structure of the features, captured by K(Pn) := ΦΦ ⊤ ∈ Rn×n. We refer to K as the prompt-induced geometry , as it determines how updates couple the n prompt residuals during optimiza-tion. Crucially, this geometry depends on both the prompt instance Pn and the update mechanism A (e.g., which at-tention heads are adaptable at test time). This dependence distinguishes TTT from classical early stopping with a fixed geometry, where the Gram structure is predetermined. To relate the prompt geometry K = ΦΦ ⊤ to the full parameter space and the chosen update subspace, col-lect the n supervised prompt outputs as sw(Pn) =(s1(w), . . . , s n(w)) ∈ Rn with si(w) := fw(xi; P (−i) 

> n

)

and targets y ∈ Rn; then r = y − sw0 (Pn). Let Jw := 

∇wsw(Pn) w=w0

∈ Rn×p. Since w(θ) = w0 + Aθ , the Jacobian in update parameters is ∇θ sw(θ)(Pn) θ=0 = JwA,i.e., Φ = JwA, and thus K = ΦΦ ⊤ = ( JwA)( JwA)⊤ =

JwΠU J⊤ 

> w

.

3.3. Specializing the Prompt Kernel to Transformers 

We specialize K to Transformer attention blocks (Vaswani et al., 2017) under a local frozen-feature regime. Specifi-cally, we evaluate all forward activations (including atten-tion weights) and upstream gradients at w0 and treat them as constants (i.e., we ignore their dependence on the test-time updates), in the spirit of Chizat et al. (2019); Fu et al. (2023). See Appendix H.1 for detailed discussion. Let h ∈ {1, . . . , H } index attention heads. We in-3A Decision-Theoretic View of Test-Time Training 

dex the n supervised prompt positions (tokens) by k ∈{1, . . . , n } and define the head output at token k by 

o(h) 

> k

:= W (h) 

> O

u(h) 

> k

, u (h) 

> k

:= P 

> s≤k

α(h) 

> ks

W (h) 

> V

zs, where zs

is the residual stream and α(h) 

> ks

are attention weights (eval-uated at w0). Under tokenwise supervision ( i ≡ k), let 

g(h) 

> k

:= ∇o(h)

> k

sk(w) w0

be the upstream gradient. Define m(h) 

> k

:= P 

> s≤k

α(h) 

> ks

zs and β(h) 

> k

:= ( W (h) 

> O

)⊤g(h) 

> k

,

so u(h) 

> k

= W (h) 

> V

m(h) 

> k

. Then the Jacobian feature w.r.t. W (h)

> V

is m(h) 

> k

⊗ β(h) 

> k

and K(h,V ) 

> kk ′

= ( m(h)⊤ 

> k

m(h) 

> k′

)( β(h)⊤ 

> k

β(h) 

> k′

).Stack Z = [ z⊤ 

> 1

; . . . ; z⊤ 

> n

], A(h) 

> ks

= α(h) 

> ks

, M (h) := A(h)Z,and B(h) = [ β(h)⊤ 

> 1

; . . . ; β(h)⊤ 

> n

]. Then 

K(h,V ) =  M (h)M (h)⊤ ⊙  B(h)B(h)⊤, (3) and if g(h) 

> k

≡ g(h) (hence β(h) 

> k

≡ β(h)), 

K(h,V ) = ∥β(h)∥22 A(h)(ZZ ⊤)A(h)⊤. (4) 

## 4. Few-step TTT as implicit Bayesian inference 

This section shows that the resulting update is a spectral filter and admits an exact auxiliary Bayesian interpretation. 

Gradient descent dynamics. To analyze the update dynam-ics under the local linear–quadratic surrogate of Sec. 3, fix a prompt Pn and consider gradient descent on the quadratic prompt loss L(θ) starting from θ0 = 0 . With a scaled step size ρ := η/σ 2, the parameter update is 

θt+1 = θt − η∇L(θt) = θt + ρ Φ⊤(r − Φθt). (5) Multiplying (5) by Φ yields the dynamics of the in-prompt prediction correction ˆft := Φ θt as ˆft+1 = ˆft + ρK (r − ˆft),

where K := ΦΦ ⊤. This reveals that TTT operates on r

solely through K(Pn).

Spectral filtering. In this setup, the standard early-stopping identity (Yao et al., 2007; Bauer et al., 2007) gives 

ˆfT = GT r, where GT := In − (In − ρK )T . If K =

U diag( λ1, . . . , λ n) U ⊤ denotes an eigen-decomposition with λi ≥ 0, then 

ˆfT = U diag  gT (λ1), . . . , g T (λn) U ⊤r, (6) where gT (λ) := 1 −(1 −ρλ )T . Set 0 < ρ < 1/λ max (K) to ensure gT (λ) ∈ [0 , 1) for all λ ∈ [0 , λ max (K)] and T < ∞.Under the same linearized model, the T -step update also induces a query-point correction of the form ˆhT (x) := 

ϕ(x; Pn)⊤θT = k⊤ 

> x

qT (K) r, where kx := Φ ϕ(x; Pn) ∈

Rn and qT (λ) := {1 − (1 − ρλ )T }/λ for λ > 0 (with 

qT (0) := ρT ). Thus, to understand when TTT helps, it suffices to compare the induced filter qT with the Bayes-optimal filter over the spectrum of K, with spectral weights scaled by the alignment of kx with the eigenvectors of K

(Sec. 5). 

Bayesian view. Recalling that the quadratic prompt ob-jective corresponds to a Gaussian observation model with noise level σ2 as in Sec. 3.2, we next show that the smoother 

r 7 → ˆfT admits an exact representation as a Gaussian poste-rior mean. This interprets the algorithm-induced map GT ,and we do not assume the true residuals are generated from this prior. This view treats T as a hyperparameter and selects it based on evidence (Sec. 7). 

Proposition 4.1 (TTT equals a Gaussian posterior mean) .

Assume 0 < ρ < 1/λ max (K). There exists a Gaussian prior f ∼ N (0 , σ 2GT (In − GT )−1) such that, under the auxiliary observation model r = f + ε, ε ∼ N (0 , σ 2In),the posterior mean satisfies E[f | r] = ˆfT .

The result suggests that TTT corresponds to Bayesian infer-ence under a prompt-induced Gaussian prior, in which the 

prior shape (eigenvectors) and the prior strength (eigen-values via T ) vary across prompts. In particular, changing which attention blocks/heads are updated changes the eigen-modes of K, i.e., it changes the shape of the implicit prior. Detailed derivations and discussions regarding extensions to other loss functions are provided in the Appendix E.2. 

## 5. When does TTT help? Bayes-gap identities 

Sec. 4 shows that few-step TTT applies a spectral filter to the prompt residuals through K(Pn). For prediction, however, our interest lies in the induced query-point correction: under the local linear model (Sec. 3), ˆhq (x) = k⊤ 

> x

q(K)r with 

kx := Φ ϕ(x; Pn). See Prop. E.1 for the derivation. To keep the presentation simple and make the Bayes-optimal query correction explicit, we adopt the following Gaussian process benchmark (Rasmussen & Williams, 2006). Below, 

fBayes (x) denotes the posterior mean under a benchmark (a Bayes-optimal correction). 

Assumption 5.1 (Gaussian benchmark) . Fix prompt inputs 

(x1, . . . , x n) and a query input x. Let K ⪰ 0 be the prompt geometry and let the prompt–query covariance vector kx and the query variance κx satisfy 

 K kx 

> k⊤
> xκx



⪰ 0. Assume latent functions (f⋆, f x) are Gaussian with mean 0 and covariance 

τ 2

 K kx 

> k⊤
> xκx



, and we observe prompt residuals r = f⋆ + ε

with ε ∼ N (0 , σ 2In) independent. Write λ⋆ := σ2/τ 2.We do not claim real TTT residuals follow this model; rather, it provides an interpretable baseline for quantifying filter mismatch and motivates extensions beyond Gaussianity. See Appendix F.2 and F.3 for extensions. 

Theorem 5.2 (Conditional Bayes-gap identity) . Grant As-sumption 5.1 and let K = U diag( λi) U ⊤ be an eigen-

4A Decision-Theoretic View of Test-Time Training 

decomposition with eigenvectors {ui}ni=1 . The Bayes-optimal estimator (posterior mean) of the query correction is 

fBayes (x) = k⊤ 

> x

(K + λ⋆In)−1r. Moreover, for any kernel-linear estimator ˆhq (x) = k⊤ 

> x

q(K) r with q⋆(λ) := ( λ +

λ⋆)−1, the Bayes gap satisfies E{ˆhq (x) − fBayes (x)}2 |

K, k x, κ x

 = Pni=1 ai(x) q(λi) − q⋆(λi) 2, where the mode weights are ai(x) := ( τ 2λi + σ2) ( u⊤ 

> i

kx)2.

Thm. 5.2 suggests that achieving a small Bayes gap at the query requires both: (i) filter match , meaning that qT (λi)

is close to q⋆(λi) = ( λi + λ⋆)−1 on the modes with large weight ai(x); and (ii) eigen-alignment , since only modes with non-negligible (u⊤ 

> i

kx)2 contribute through ai(x). This perspective parallels spectral analyses of generalization on kernel regression (Bordelon et al., 2020). Importantly, sim-ply fitting the model to the prompt is not sufficient. 

Specialization to TTT and ICL (Corollary E.5). TTT uses 

q = qT , while naive ICL uses q ≡ 0. Thus, conditioned on (K, k x, κ x), TTT improves over ICL in terms of the Bayes gap if and only if Pni=1 ai(x) qT (λi) − q⋆(λi) 2 <Pni=1 ai(x) q⋆(λi)2.

Takeaway (C1-1): When TTT helps. 

TTT improves upon ICL via 1. Optimal Steps : the step count tunes shrinkage (so that qT matches q⋆), avoiding underfitting/overfitting; 2. Geometric Alignment : the update subspace contains directions learned from the prompt that are also aligned with the query prediction. 

5.1. Geometry and Dynamics of Transformer TTT Prompt geometry as token coupling. In a Transformer, the prompt geometry K = ΦΦ ⊤ is an n × n token–token Gram matrix: Kkk ′ = ⟨∇ θ sk, ∇θ sk′ ⟩ measures the extent to which a small update (within the chosen update sub-space) changes the outputs at tokens k and k′ in similar directions. For value-matrix updates in head h, K(h,V ) =(M (h)M (h)⊤) ⊙ (B(h)B(h)⊤) (Eq. (3) ), so a token pair is emphasized only when it is both similar in attention-mixed representations M (h) and aligned in upstream gradient di-rections B(h). In the global-upstream regime, this simpli-fies to K(h,V ) = ∥β(h)∥22 A(h)(ZZ ⊤)A(h)⊤, an attention-sandwiched token-similarity matrix (symmetric PSD even for causal A(h)). Since head/block contributions sum to 

K = P 

> b∈B

K(b), choosing which blocks/heads to update determines which eigen-directions in token-space are avail-able for test-time correction .

Filtering dynamics and query coupling. Few-step TTT applies the polynomial filter ˆfT = PT −1 

> t=0

ρK (I − ρK )tr,which can be viewed as diffusion/smoothing of residual information over this attention-induced token geometry; the horizon T sets the diffusion time and thus the amount of shrinkage across eigenmodes. Finally, Thm. 5.2 implies that query-point improvement comes only from eigen-directions that also align with the prompt-query vector kx (weights 

(u⊤ 

> i

kx)2), so head/block choices matter both through the spectrum of K and through its coupling to the query .

## 6. Necessity of adapting the update steps and directions 

The identity in Theorem 5.2 identifies two requirements for query improvement: the update filter must match the prompt signal-to-noise structure, and the updated eigendirections must align with the query. In this section, we show that fixing the number of update steps T or the update subspace 

A as in standard optimization can fail. We then argue for the necessity of prompt-dependent step selection and subspace design. Recall from Sec. 4 that, for a fixed prompt Pn, few-step TTT on the quadratic prompt loss produces an in-prompt correc-tion ˆfT = gT (K) r with filter gT (λ) = 1 − (1 − ρλ )T .In this section, we consider the broader class of prompt-only spectral estimators ˆfg := g(K) r for measurable 

g : [0 , ∞) → R with g(0) = 0 , and denote by S(Pn) = range( K(Pn)) the representable subspace in prompt space. We outline the three propositions below. For the formal statements, see Appendix E.4. 

Proposition 6.1 (Necessity of adapting T under prior shift; informal) . Assume K is a rank-m projector with {0, 1}-eigenvalues. Under Assumption 5.1, the Bayes-optimal esti-mator is fBayes = 11+ λ⋆ ΠS r, which varies with the test-time SNR 1 

> λ⋆

. Hence, no fixed horizon T can be Bayes-optimal across two different priors. 

Connection to phenomenon. Prop. 6.1 underpins empir-ical reports that TTT is sensitive to the number of update steps (Aky ¨urek et al., 2025; Zhao et al., 2023) and that, in particular under test-time condition shifts across scenarios, fixing T can lead to catastrophic failure (Boudiaf et al., 2022). 

Proposition 6.2 (∞-steps TTT can be sub-optimal; infor-mal) . Assume the linearized quadratic prompt loss and 

0 < ρ < 2/λ max (K). GD initialized at zero satisfies 

ˆfT → ΠS r as T → ∞ . If dim( S) ≥ 3 (with known Gaus-sian noise level), there exists a shrinkage rule within S that reduces its squared-error risk. 

Connection to phenomenon. Prior experiments have reported that increasing the number of test-time adapta-tion steps can eventually hurt performance due to overfit-ting (Zhao et al., 2023; Alfarra et al., 2025). Prop. 6.2 provides a principled explanation of this. 

Proposition 6.3 (Irreducible error of subspace-mismatch; 5A Decision-Theoretic View of Test-Time Training 

informal) . For any target f⋆ ∈ Rn, the TTT estimator ˆfgT

satisfies inf T ≥0 ∥ ˆfgT − f⋆∥22 ≥ ∥ ΠS⊥ f⋆∥22.

In the global-upstream regime (4) , the representable prompt-space directions satisfy range( K(h,V )) = range( A(h)Z);see Lem. H.3 in the Appendix. 

Connection to phenomenon. Prop. 6.3 formalizes a rep-resentational bottleneck, that is, regardless of hyperparam-eters, the correction ˆfg lies in S, so components outside 

S are unavoidable. Consistent with this view, Zhang et al. (2025) discuss gains from scaling test-time state capacity (expanding S in our context). However, simply expanding the update space is not sufficient. Choi et al. (2022) report settings where full-parameter updates can degrade perfor-mance, motivating careful design of the update mechanism 

A (and hence the induced S). 

Takeaway (C1-2): why T and A must adapt 

A fixed T can be Bayes-suboptimal under test-time prior shift; taking T → ∞ can overfit; and any fixed representable subspace induces an irreducible mismatch floor. In addition, Appendix G explains how the spectrum of 

K(Pn) governs when a fixed T can (or cannot) match Bayes shrinkage across modes via the gradient flow and random matrix theory (RMT). 

## 7. Per-prompt update steps via PAC-Bayes 

The first failure mode above concerns how far to adapt. Building on the evidence (marginal likelihood) frame-work (MacKay, 1992), we interpret T as a hyperparameter in the implicit Bayesian formulation of Section 4 and ana-lyze it via PAC-Bayes (Catoni, 2007; Alquier et al., 2024). 

Evidence family induced by TTT. Fix a prompt-induced geometry K ⪰ 0 and ρ ∈ (0 , 1/λ max (K)) . Motivated by Prop. 4.1, we consider the associated auxiliary evidence family in which r is distributed according to the Gaussian marginal induced by the implicit prior: 

r | (K, T ) ∼ N (0 , ΣT ), ΣT = σ2(I − ρK )−T .

This induces a data-dependent score for T , given by the normalized negative log-evidence (ignoring T -independent constants) ℓT (r) := 12n

 log det(Σ T ) + r⊤Σ−1 

> T

r . We will use these scores to form a posterior over T via a Gibbs construction introduced below. To analyze the statistical validity of selecting T via this objective, we formalize the data generating process as follows. 

Assumption 7.1 (Reference model for residuals) . Condi-tional on K, the true residual vector is Gaussian: r | K ∼N (0 , Σ⋆), Σ⋆ ≻ 0.

Under this reference model, define the normalized popu-lation cross-entropy risk as L(T | K) := E[ℓT (r) | K].Since Σ⋆ is unknown, we use the observed prompt residu-als r to evaluate ℓT (r) across candidates and convert these scores into the Gibbs posterior over T introduced next. 

Gibbs posterior over update steps. Fix a finite candi-date set T ⊂ N0 and a prior π0 on T with full support. For β > 0, define the Gibbs posterior νβ (T | r) ∝

π0(T ) exp {− βn ℓ T (r)}. This posterior can be used either to select a point estimate TMAP ∈ arg max T νβ (T | r) or to perform model averaging (mixture-of-filters (Dalalyan & Tsybakov, 2008)) ¯G := ET ∼νβ [gT (K)] . When π0 is uniform, TMAP coincides with evidence minimization (Ger-main et al., 2016), defined as TMAP ∈ arg min T ∈T ℓT (r).Formally, this Gibbs posterior is defined as a Radon– Nikodym tilt of π0; see Remark E.10. 

Theorem 7.2 (PAC-Bayes guarantee for selecting T ). Con-sider a point mass posterior ν = δT and uniform prior π0

on T . Grant Assumption 7.1 and let T be finite. Define 

AT := Σ 1/2 

> ⋆

Σ−1 

> T

Σ1/2 

> ⋆

⪰ 0 and ¯v := sup T ∈T 1 

> n

tr( A2 

> T

).Then, for any δ ∈ (0 , 1) , with probability at least 1 − δ over 

r | K, choosing β = 2 p{log |T | + log(1 /δ )}/n ¯v yields 

L(TMAP | K) ≤ ℓTMAP (r) + 

s

¯v log |T | + log(1 /δ )

n .

Thm. 7.2 controls the population negative log-evidence of the prompt-residual model. It is not an end-to-end theorem that the evidence-minimizing horizon is always query-risk optimal. The link to prediction is through the shared fil-ter: Sec. 5 shows that the query correction uses the same 

qT (K), weighted by the prompt–query coupling kx, while Sec. 8 handles query-dependent update directions. Thus, evidence selects the shrinkage level within the TTT filter family; query-aware subspace selection addresses the re-maining query dependence. Under the GP reference model, App. E.5.3 further relates evidence closeness to a normal-ized Bayes gap. 

Takeaway (C2): per-prompt T selection 

Selecting T by prompt evidence enjoys a PAC-Bayes bound for the out-of-sample negative log-evidence, so tuning T on the prompt does not overfit the prompt residuals. 

## 8. Bayes-optimal subspace selection 

Evidence selection chooses how far to move within a fixed prompt geometry; it does not by itself choose which ge-ometry is most useful for the query. We next turn from the update horizon to the update subspace. The update 6A Decision-Theoretic View of Test-Time Training 

subspace should capture query-aware prompt signal since 

ˆhT (x) = k⊤ 

> x

qT (K) r (Sec. 4) and improvements come pri-marily from eigenmodes aligned with kx (Thm. 5.2). We formalize this test-time update subspace as a Bayesian de-sign variable, in the spirit of Attia et al. (2018). 

Assumption 8.1 (Gaussian benchmark of subspace) . There exists a latent parameter correction u⋆ ∈ Rp such that 

u⋆ ∼ N (0 , Σ) , r = Jwu⋆ + ε, ε ∼ N (0 , σ 2In), and 

u⋆ ⊥ ε, where Σ ≻ 0 is task-to-task variability and 

Jw := ∇wsw(Pn)|w=w0 ∈ Rn×p.This assumption is the subspace version of Assump-tion 5.1 (see Lem. E.20). For a query input x, denote the linearized query correction by fx := j⊤ 

> x

u⋆, where 

jx := ∇wfw0 (x; Pn) ∈ Rp.

8.1. Bayes-optimal update subspace 

To focus on query prediction, we assess an estimator bu(r)

by the mean squared error of the linearized query correction 

E{j⊤ 

> X

(bu − u⋆)}2, where the expectation is over the test-time query X (conditioned on the prompt, unless stated otherwise). Define Q := EjX j⊤

> X

 ⪰ 0. Then E[{j⊤ 

> X

(bu −

u⋆)}2] = E[∥bu − u⋆∥2

> Q

] with ∥v∥2 

> Q

:= v⊤Qv .To align the subspace constraint with the query metric, we reparameterize via v := Q1/2u (restricted to range( Q) if 

Q is singular), so that ∥u∥2 

> Q

= ∥v∥22. We choose an update subspace of dimension budget kbudget by selecting a rank-

kbudget Euclidean projector eΠ(= eΠ⊤ = eΠ2) acting on v,and restrict estimators to bv(r) ∈ range( eΠ) .Let ¯u(r) := E[u⋆ | r] denote the Bayes estimator un-der the linear-Gaussian model, and set ¯v(r) := Q1/2 ¯u(r).The key quantity for subspace design is how much recov-erable signal the prompt provides about u⋆, captured by the covariance of the posterior mean: I := Var(¯ u(r)) = ΣJ⊤ 

> w

(JwΣJ⊤ 

> w

+ σ2In)−1JwΣ. Its query-whitened version 

IQ := Var(¯ v(r)) = Q1/2IQ1/2 will determine the Bayes-optimal rank-kbudget update subspace. 

Theorem 8.2 (Optimal rank-kbudget update subspace) . Un-der Assumption 8.1, define the query-weighted Bayes risk 

R(eΠ) := inf 

> bv(·)∈range( eΠ)

E

h

∥bv(r) − Q1/2u⋆∥22

i

= tr( QΣ) − tr( eΠIQ).

Then, any minimizer over rank-kbudget projectors eΠ is the projector onto the top-kbudget eigenspace of IQ.

To realize the designed subspace in the original coordinates 

u, map back from v = Q1/2u. If Q ≻ 0, the correspond-ing u-space subspace is Q−1/2range( eΠ) . If Q is singular, interpret Q−1/2 as the pseudoinverse on range( Q).By the law of total variance, the Q-weighted total variability of the correction Var( Q1/2u⋆) decomposes into IQ (recov-erable from the prompt) and EQ1/2Var( u⋆ | r) Q1/2 (ir-reducible). Hence, tr( eΠIQ) is the query-aligned posterior-mean variability recovered by the update subspace. This is large when the subspace both (i) contains directions whose posterior means are identifiable from the prompt and (ii) aligns with query sensitivities (via Q), consistent with the eigen-alignment findings of Thm. 5.2. A global (population-level) update mechanism can be obtained by averaging IQ

across prompts and applying a plug-in top-eigenspace es-timator. Details and a martingale-type regret bound are deferred to Appendix E.6. 

8.2. Block/head selection for Transformer Transformer partitioning. To obtain a practical approxi-mation to Thm. 8.2, we restrict the update projector to be aligned with a Transformer parameter partition (motivated by previous analyses showing that attention-head contribu-tions are so uneven that many heads are prunable (Michel et al., 2019; Voita et al., 2019)). Specifically, we parti-tion parameters into disjoint blocks (e.g., by layer/head-/matrix type), writing the corresponding Jacobian block columns by Jw = [ J(b)]b∈B all , and restrict updates to a block-diagonal projector Π = diag  Π(b)

> b∈B all

, which in-cludes block on/off selection ( Π(b) ∈ { 0, I pb }). For block-diagonal Π, let Ibb denote the diagonal block of 

I for block b. If we specify Q by a single query x (i.e., 

Q = jxj⊤ 

> x

) and perform on/off selection, then the risk reduction attributable to block b is proportional to the score 

j(b)⊤ 

> x

Ibb j(b) 

> x

(QUERY -A WARE ),

where j(b) 

> x

is the restriction of jx to block b. We refer to ranking blocks by this score as Q UERY -A WARE selection. Under the isotropic correction prior Σ = Ip, we have 

I = J⊤ 

> w

(Kfull + σ2In)−1Jw with Kfull := JwJ⊤ 

> w

(the prompt geometry under full parameter updates). Let 

K(b)full := J(b)J(b)⊤. Then the query-marginal block score becomes 

tr  Ibb 

 = tr 



K(b)full (Kfull + σ2In)−1

. (TRACE -T OP K)

Selecting the top-kbudget blocks by this score corresponds to the T RACE -T OP K baseline in Sec.9. In contrast, for a specific query x, the Q UERY -A WARE score can be written as k(b)⊤ 

> x

(Kfull + σ2In)−1k(b) 

> x

, where k(b) 

> x

:= J(b)j(b) 

> x

is the prompt-query coupling induced by block b. The factor 

(Kfull + σ2In)−1 downweights directions already explained by other blocks (or dominated by noise), making the score explicitly redundancy-aware and query-aligned. For on/off selection Π(b) ∈ { 0, I pb } under a block/param-eter budget, we rank blocks by an appropriate score (e.g., 7A Decision-Theoretic View of Test-Time Training 

QUERY -A WARE : j(b)⊤ 

> x

Ibb j(b) 

> x

when Q = jxj⊤ 

> x

), optionally normalized by pb. For a global update mechanism, use the query-averaged Q = EX [jX j⊤ 

> X

].The exact query-specific Q UERY -A WARE score requires one backward pass through the query output to obtain jx. This overhead is additive rather than per-block: a single query backward pass gives jx for all blocks, after which the block-specific vectors j(b) 

> x

are obtained by slicing. The remaining inverses and trace scores are in prompt space (dimension 

n) and can be implemented with Jacobian–vector products without materializing Jw. When per-query latency is unde-sirable, the query-averaged choice Q = EX [jX j⊤ 

> X

] gives an amortized global design that removes the per-query back-ward pass while remaining the Bayes-optimal solution for the averaged query objective; see Appendix H.4 for details. 

Takeaway (C3): optimal update directions 

For TTT prediction, the Bayes-optimal rank-kbudget up-date subspace is the top-kbudget eigenspace of IQ. For Transformer, this yields query-aware scores for atten-tion block/head selection. 

## 9. Experiments 

The experiments are performed for mechanism-level checks of the theory rather than a comprehensive benchmark study. Experiment 1 tests whether evidence selects prompt-dependent horizons, and Experiment 2 tests whether query-aligned directions improve query prediction more reliably than prompt-fit directions. 

Experimental setup. We use a digit-shift task with 

distilgpt2 (Wolf et al., 2020). We generate 2,000 tasks, each sampled from one of two settings: clean (pnoise = 0 .0) and noisy (pnoise = 0 .55 ). For each task, we sample a shift s ∼ Unif(0 , . . . , 9) , draw digits 

x ∼ Unif(0 , . . . , 9) , and set labels y = ( x + s) mod 10 .Each test instance contains n = 10 prompt pairs (x, y ) and an independent query digit x from the same task. Prompt labels are corrupted independently with probability pnoise by replacing y with a uniformly random digit in {0, . . . , 9}.We update only attention Value weights at the (layer, head) level via gradient descent on leave-one-out prompt residuals. Although the model outputs logits over digits, we treat this as a regression problem: labels are normalized to ˜y := y/ 9

and the model’s prediction is taken as the expected digit 

ˆy = P9 

> j=0
> j
> 9

softmax( ℓ)j . Full details are in Appendix I. 

(Exp1) Per-prompt T selection. To demonstrate claim 

(C2) , we compare ICL (no update), TTT with a fixed T

(T = 8 ), and evidence-based per-prompt selection ( TMAP )1.                            

> 1We implement two evidence variants: a fixed-σplug-in and Method MSE ( ±SE) Mean T
> TTT (evidence, MLE-σ)0.0719 ±0.0029 11.200 TTT (evidence, fixed-σ)0.0722 ±0.0030 9.793 TTT ( T=8 )0.0734 ±0.0030 –ICL ( T=0 )0.0747 ±0.0031 –
> Table 1. Evidence-based TTT reduces risk (Exp1). Comparison of MSE over 2,000 tasks. While fixed TTT yields marginal gains, adaptive TMAP (MLE-σ) achieves the lowest risk, consistent with Claims (C1) -(C2) .

We report averaged MSE across 2,000 tasks with SE denot-ing the standard error of the mean across tasks. Table 1 shows that evidence-based T selection improves over both ICL and fixed-T TTT. Although the effect sizes are modest, the gains are statistically significant under a task-matched paired analysis. For each task, we com-pute the paired improvement as the baseline query MSE minus the method’s query MSE, so that positive values indicate lower error. The best fixed horizon T =8 im-proves over ICL with a 95% bootstrap confidence inter-val [0 .000648 , 0.001796] and paired permutation p-value 

p = 5 .0 × 10 −5. Both evidence-based variants further im-prove over this fixed-T baseline: fixed-σ evidence gives CI [0 .000545 , 0.002894] with p = 0 .00325 , and MLE-σ

evidence gives CI [0 .000605 , 0.003079] with p = 0 .00250 .Figure 1 shows the empirical cumulative distribution func-tion (ECDF) of the selected TMAP under the fixed-σ

evidence method, revealing substantial variation across prompts rather than a single globally optimal T . More-over, TMAP responds to prompt signal-to-noise: in the noisy regime, evidence tends to select smaller T s to mitigate over-fitting, whereas, in the clean regime, it favors larger T to maximize adaptation benefits. 

(Exp2) Alignment-aware block selection. We next in-vestigate update-subspace design under a fixed budget of kbudget blocks. We compare Q UERY -A WARE , T RACE -TOP K, T RACE -B OTTOM K, and random selection. Consistent with Claim (C3) , Figure 1 illustrates that the QUERY -A WARE selection consistently outperforms random selection. Notably, the T RACE -T OP K baseline, which pri-oritizes dominant prompt features without considering the query, often fits the prompt well but fails to generalize to the query. This confirms that effective adaptation requires not just capturing prompt variance, but capturing variance aligned with the query, as predicted by (C1) .

## 10. Discussion 

We have presented a unified Bayes-risk framework for TTT, interpreting it as implicit Bayesian inference. We found  

> an MLE-σvariant; details in Appendix.

8A Decision-Theoretic View of Test-Time Training                  

> !#
> 
> 
>  
> 
> 
>     
> 
> "!
> $
> 
> 
> 
> 
> % $!  $
> 
> "!
> $
> 
> 
> 
> % $!  $
> Noisy

(a) 𝑇 Selection by evidence (b) Query prediction (c) In -prompt fit 

> Clean
> Trace -BottomK
> Trace -BottomK
> Trace -TopK
> Trace -TopK
> Query -Aware
> Query -Aware

Figure 1. (a) Evidence adaptively selects T (Exp1). ECDF of the selected TMAP . The optimal T is prompt-dependent, so adapting T is preferable to using a fixed T (Claims (C1) –(C2) ). See Table 1 for predictive performance. (b) Query gains depend on alignment (Exp2). 

Query-MSE improvement relative to random selection ( ∆ > 0 is better). Q UERY -A WARE consistently outperforms T RACE -T OP K(Claims (C1) , (C3) ). (c) Prompt fit is a different objective (Exp2). The same ∆MSE evaluated using prompt-fit MSE. T RACE -T OP Ktends to improve prompt fit, but this does not necessarily generalize to query improvement in (b). Error bars indicate standard error of the mean across tasks. 

that predictive improvements depend critically on filter match and eigen-alignment (C1) , explaining why fixed hy-perparameters often fail in practice. This Bayesian view also motivates evaluating T through prompt evidence and yields a PAC-Bayes non-overfitting guarantee (C2) . Finally, the Bayes-optimal update subspace is characterized by an information-capture matrix, leading to query-aligned block-/head criteria for Transformers (C3) . Experiments support these implications, highlighting the roles of adaptive regular-ization and query-aligned update geometry. More broadly, as the kernel regime analysis (such as NTK) informed neu-ral network engineering, we believe this work represents an important step toward making TTT more reliable in practice. 

Novelty boundary. The implicit-Bayesian view is a con-ceptual lens rather than our sole technical contribution. Our novelty lies in the TTT-specific consequences of this lens: query-level Bayes-gap identities, PAC-Bayes step selection, and Bayes-optimal update-subspace design. 

What is expected to transfer. We expect two lessons to be useful beyond the exact Gaussian-quadratic model. First, the update horizon should depend on the prompt, because prompts can have different signal-to-noise levels and differ-ent overfitting risks. Second, update directions should be chosen with the query in mind, because fitting the prompt alone need not improve the query. Our Transformer deriva-tions in Secs. 3.3 and 8.2 show how these ideas can be ex-pressed as attention block/head geometry and query-aware scores. 

Scope and future directions. Our analysis is local. It uses restricted update parameters, a first-order approximation to the model, and a locally quadratic prompt objective. It is therefore best viewed as a theory of few-step TTT around a fixed pretrained model, rather than as a complete theory of arbitrary test-time adaptation. Appendix F.1 gives a finite-step deviation bound, but the present theory does not yet cover large updates, changing representations along the adaptation path, full-parameter TTT, or the optimization dynamics of self-supervised TTT losses. Several extensions are natural. First, one could move from a fixed local model to a trajectory-level analysis, where the tangent features, kernel, and prompt–query coupling are allowed to evolve during adaptation. Second, one could ex-tend the Bayesian view to non-squared and self-supervised losses, such as entropy minimization, masked prediction, consistency losses, or pseudo-label training. This would require modeling pseudo-residuals, local curvature, and the bias introduced by noisy self-supervision. Third, one could develop scalable approximations to query-aware sub-space selection, for example through amortized, low-rank, or query-averaged scores for choosing Transformer blocks, heads, or adapters. Finally, broader experiments on lan-guage, vision, and tabular tasks are needed to test which parts of the local Bayesian picture transfer to realistic non-linear TTT. 

## Acknowledgments 

We thank the anonymous reviewers for their constructive comments. TW was partially supported by JSPS KAKENHI (26K21188), JST ACT-X (JPMJAX23CS) and RIKEN In-centive Research Project. 

## Impact Statement 

This paper presents work whose goal is to advance the field of Machine Learning. There are many potential societal consequences of our work, none of which we feel must be specifically highlighted here. 

## References 

Aky ¨urek, E., Damani, M., Zweiger, A., Qiu, L., Guo, H., Pari, J., Kim, Y., and Andreas, J. The surprising effective-9A Decision-Theoretic View of Test-Time Training 

ness of test-time training for few-shot learning. In Forty-second International Conference on Machine Learning ,2025. URL https://openreview.net/forum? id=asgBo3FNdg .Alfarra, M., Correia, A., Ghanem, B., and Louizos, C. Test-time adaptation with source based auxiliary tasks. Trans-actions on Machine Learning Research , 2025. ISSN 2835-8856. URL https://openreview.net/ forum?id=XWAXcxNg4n .Alquier, P. et al. User-friendly introduction to PAC-Bayes bounds. Foundations and Trends® in Machine Learning ,17(2):174–303, 2024. Attia, A., Alexanderian, A., and Saibaba, A. K. Goal-oriented optimal design of experiments for large-scale bayesian linear inverse problems. Inverse Problems , 34 (9):095009, 2018. doi: 10.1088/1361-6420/aad210. Bai, Z. and Silverstein, J. W. Spectral Analysis of Large Dimensional Random Matrices . Springer, New York, second edition, 2010. doi: 10.1007/978-1-4419-0661-8. Bauer, F., Rosasco, L., and Pereverzev, S. On regularization algorithms in learning theory. Journal of Complexity , 23 (1):52–72, February 2007. doi: 10.1016/j.jco.2006.07. 001. Bishop, C. M. Pattern Recognition and Machine Learning .Information Science and Statistics. Springer New York, 2006. ISBN 978-0-387-31073-2. Bordelon, B., Canatar, A., and Pehlevan, C. Spectrum depen-dent learning curves in kernel regression and wide neural networks. In Proceedings of the 37th International Con-ference on Machine Learning , volume 119 of Proceedings of Machine Learning Research , pp. 1024–1034. PMLR, 13–18 Jul 2020. URL https://proceedings.mlr. press/v119/bordelon20a.html .Boudiaf, M., Mueller, R., Ayed, I. B., and Bertinetto, L. Parameter-free online test-time adaptation. In Proceed-ings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pp. 8344–8353, June 2022. Catoni, O. Pac-bayesian supervised classification: The thermodynamics of statistical learning, 2007. URL 

https://arxiv.org/abs/0712.0248 .Chizat, L., Oyallon, E., and Bach, F. On lazy training in differentiable programming. In Advances in Neural Information Processing Systems , volume 32, 2019. Choi, S., Yang, S., Choi, S., and Yun, S. Improving test-time adaptation via shift-agnostic weight regularization and nearest source prototypes. In Computer Vision – ECCV 2022 , pp. 440–458, Cham, 2022. Springer Nature Switzerland. Dalalyan, A. S. and Tsybakov, A. B. Aggregation by ex-ponential weighting, sharp PAC-Bayesian bounds and sparsity. Machine Learning , 72(1):39–61, 2008. doi: 10.1007/s10994-008-5051-0. Engl, H. W., Hanke, M., and Neubauer, A. Regulariza-tion of Inverse Problems . Mathematics and Its Applica-tions. Springer Dordrecht, 2000. URL https://link. springer.com/book/9780792341574 .Fu, H., Guo, T., Bai, Y., and Mei, S. What can a Single Attention Layer Learn? A Study Through the Random Features Lens. In Thirty-seventh Conference on Neural Information Processing Systems , 2023. URL https: //openreview.net/forum?id=wX8GuzDSJR .Germain, P., Bach, F., Lacoste, A., and Lacoste-Julien, S. Pac-bayesian theory meets bayesian inference. In Advances in Neural Information Pro-cessing Systems , volume 29. Curran Associates, Inc., 2016. URL https://proceedings.neurips. cc/paper_files/paper/2016/file/ 84d2004bf28a2095230e8e14993d398d-Paper. pdf .Gozeten, H. A., Ildiz, M. E., Zhang, X., Soltanolkotabi, M., Mondelli, M., and Oymak, S. Test-time training provably improves transformers as in-context learners. In Proceedings of the 42nd International Conference on Machine Learning , volume 267 of Proceedings of Machine Learning Research , pp. 20266–20295. PMLR, 2025. URL https://proceedings.mlr.press/ v267/gozeten25a.html .Hardt, M. and Sun, Y. Test-time training on near-est neighbors for large language models. In Interna-tional Conference on Learning Representations (ICLR) ,2024. URL https://openreview.net/forum? id=CNL2bku4ra .H ¨ubotter, J., Bongni, S., Hakimi, I., and Krause, A. Ef-ficiently learning at test-time: Active fine-tuning of LLMs. In The Thirteenth International Conference on Learning Representations , 2025a. URL https: //openreview.net/forum?id=NS1G1Uhny3 .H ¨ubotter, J., Wolf, P., Shevchenko, A., J ¨uni, D., Krause, A., and Kur, G. Specialization after generalization: Towards understanding test-time training in foundation models. 

arXiv preprint arXiv:2509.24510 , 2025b. Jacot, A., Gabriel, F., and Hongler, C. Neural tan-gent kernel: Convergence and generalization in neural networks. In Advances in Neural In-formation Processing Systems (NeurIPS) , 2018. 10 A Decision-Theoretic View of Test-Time Training 

URL https://proceedings.neurips. cc/paper_files/paper/2018/file/ 5a4be1fa34e62bb8a6ec6b91d2462f5a-Paper. pdf .James, W. and Stein, C. Estimation with quadratic loss. In Proceedings of the Fourth Berkeley Sym-posium on Mathematical Statistics and Probability ,1961. URL https://digitalassets.lib. berkeley.edu/math/ucb/text/math_s4_ v1_article-07.pdf .Kim, C., Park, J., Shim, H., and Yang, E. SGEM: Test-Time Adaptation for Automatic Speech Recognition via Sequential-Level Generalized Entropy Minimization. In Proceedings of the Annual Conference of the Inter-national Speech Communication Association (INTER-SPEECH) , pp. 3367–3371, Dublin, Ireland, 2023. doi: 10.21437/Interspeech.2023-1282. Kuwataka, K. and Suzuki, T. Test time training enhances in-context learning of nonlinear functions. arXiv preprint arXiv:2509.25741 , 2025. Lee, J., Xiao, L., Schoenholz, S. S., Bahri, Y., Novak, R., Sohl-Dickstein, J., and Pennington, J. Wide neu-ral networks of any depth evolve as linear models un-der gradient descent. In Advances in Neural Informa-tion Processing Systems (NeurIPS) , 2019. URL https: //arxiv.org/abs/1902.06720 .Lehmann, E. L. and Casella, G. Theory of Point Estimation .Springer Texts in Statistics. Springer, New York, NY, second edition, 1998. doi: 10.1007/b98854. Lin, G.-T., Huang, W.-P., and Lee, H.-y. Continual test-time adaptation for end-to-end speech recogni-tion on noisy speech. In Proceedings of the 2024 Conference on Empirical Methods in Natural Lan-guage Processing (EMNLP) , pp. 20003–20015, Mi-ami, Florida, USA, 2024. Association for Computa-tional Linguistics. doi: 10.18653/v1/2024.emnlp-main. 1116. URL https://aclanthology.org/2024. emnlp-main.1116/ .MacKay, D. J. C. The evidence framework applied to classi-fication networks. Neural Computation , 4(5):720–736, 09 1992. ISSN 0899-7667. doi: 10.1162/neco.1992.4.5.720. URL https://doi.org/10.1162/neco.1992. 4.5.720 .Mar ˇcenko, V. A. and Pastur, L. A. Distribution of eigen-values for some sets of random matrices. Mathemat-ics of the USSR-Sbornik , 1(4):457–483, 1967. doi: 10.1070/SM1967v001n04ABEH001994. Margossian, C. and Blei, D. Amortized variational infer-ence: When and why? In The 40th Conference on Un-certainty in Artificial Intelligence , 2024. URL https: //openreview.net/forum?id=mCVYIsnctr .Michel, P., Levy, O., and Neubig, G. Are sixteen heads re-ally better than one? In Advances in Neural Information Processing Systems , volume 32. Curran Associates, Inc., 2019. URL https://proceedings.neurips. cc/paper_files/paper/2019/file/ 2c601ad9d2ff9bc8b282670cdd54f69f-Paper. pdf .Mittal, S., Bracher, N. L., Lajoie, G., Jaini, P., and Brubaker, M. Amortized in-context bayesian posterior estimation. 

arXiv preprint arXiv:2502.06601 , 2025. Murphy, K. P. Probabilistic Machine Learning: An intro-duction . MIT Press, 2022. URL http://probml. github.io/book1 .Murphy, S. A. and van der Vaart, A. W. On profile likelihood. 

Journal of the American Statistical Association , 95(450): 449–465, June 2000. ISSN 0162-1459. doi: 10.1080/ 01621459.2000.10474219. URL https://doi.org/ 10.1080/01621459.2000.10474219 .Niu, S., Wu, J., Zhang, Y., Chen, Y., Zheng, S., Zhao, P., and Tan, M. Efficient test-time model adaptation without forgetting. In Proceedings of the 39th International Con-ference on Machine Learning , volume 162 of Proceedings of Machine Learning Research , pp. 16888–16905. PMLR, 17–23 Jul 2022. URL https://proceedings.mlr. press/v162/niu22a.html .Radford, A., Wu, J., Child, R., Luan, D., Amodei, D., and Sutskever, I. Language Models Are Un-supervised Multitask Learners. Technical report, OpenAI, 2019. URL https://cdn.openai. com/better-language-models/language_ models_are_unsupervised_multitask_ learners.pdf .Rasmussen, C. E. and Williams, C. K. I. Gaussian Processes for Machine Learning . MIT Press, 2006. URL https: //gaussianprocess.org/gpml/ .Reuter, A., Rudner, T. G. J., Fortuin, V., and R ¨ugamer, D. Can transformers learn full Bayesian inference in con-text? In Proceedings of the 42nd International Confer-ence on Machine Learning , volume 267 of Proceedings of Machine Learning Research , pp. 51531–51582. PMLR, 2025. URL https://proceedings.mlr.press/ v267/reuter25a.html .Robert, C. P. The Bayesian Choice: From Decision-Theoretic Foundations to Computational Implementation .11 A Decision-Theoretic View of Test-Time Training 

Springer Texts in Statistics. Springer, second edition, 2007. doi: 10.1007/0-387-71599-1. Sahoo, S., ElAraby, M., Ngnawe, J., Pequignot, Y., Precioso, F., and Gagn ´e, C. A layer selection approach to test time adaptation. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 39, pp. 20237–20245, 2025. doi: 10.1609/aaai.v39i19.34229. URL https: //doi.org/10.1609/aaai.v39i19.34229 .Sanh, V., Debut, L., Chaumond, J., and Wolf, T. Distilbert, a distilled version of bert: smaller, faster, cheaper and lighter. In Proceedings of the 5th Workshop on Energy Efficient Machine Learning and Cognitive Computing () co-located with NeurIPS 2019 , 2019. Shao, P. Y.-S. and Strawderman, W. E. Improving on the James-Stein Positive-Part Estimator. The Annals of Statistics , 22(3):1517 – 1538, 1994. doi: 10.1214/aos/ 1176325640. URL https://doi.org/10.1214/ aos/1176325640 .Socher, R., Perelygin, A., Wu, J., Chuang, J., Manning, C. D., Ng, A., and Potts, C. Recursive deep models for semantic compositionality over a sentiment treebank. In Yarowsky, D., Baldwin, T., Korhonen, A., Livescu, K., and Bethard, S. (eds.), Proceedings of the 2013 Confer-ence on Empirical Methods in Natural Language Process-ing , pp. 1631–1642, Seattle, Washington, USA, October 2013. Association for Computational Linguistics. URL 

https://aclanthology.org/D13-1170/ .Sun, Y., Wang, X., Liu, Z., Miller, J., Efros, A., and Hardt, M. Test-time training with self-supervision for generalization under distribution shifts. In Pro-ceedings of the 37th International Conference on Ma-chine Learning , volume 119 of Proceedings of Machine Learning Research , pp. 9229–9248. PMLR, 13–18 Jul 2020. URL https://proceedings.mlr.press/ v119/sun20b.html .Tropp, J. A. User-friendly tail bounds for sums of random matrices. Foundations of Computational Mathematics ,pp. 389–434, 2012. doi: 10.1007/s10208-011-9099-z. URL https://link.springer.com/article/ 10.1007/s10208-011-9099-z .Valanarasu, J. M. J., Guo, P., VS, V., and Patel, V. M. On-the-fly test-time adaptation for medical image segmentation. In Medical Imaging with Deep Learning , volume 227 of 

Proceedings of Machine Learning Research , pp. 586–598. PMLR, Jul 2024. URL https://proceedings. mlr.press/v227/valanarasu24a.html .Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., Kaiser, L. u., and Polosukhin, I. Atten-tion is All you Need. In Advances in Neural Information Processing Systems , volume 30. Curran Associates, Inc., 2017. URL https://proceedings.neurips. cc/paper_files/paper/2017/file/ 3f5ee243547dee91fbd053c1c4a845aa-Paper. pdf .Vershynin, R. High-Dimensional Probability: An Introduc-tion with Applications in Data Science . Cambridge Series in Statistical and Probabilistic Mathematics. Cambridge University Press, 2018. Voita, E., Talbot, D., Moiseev, F., Sennrich, R., and Titov, I. Analyzing multi-head self-attention: Specialized heads do the heavy lifting, the rest can be pruned. In 

Proceedings of the 57th Annual Meeting of the Asso-ciation for Computational Linguistics , pp. 5797–5808, Florence, Italy, July 2019. Association for Computa-tional Linguistics. doi: 10.18653/v1/P19-1580. URL 

https://aclanthology.org/P19-1580/ .Wakayama, T. and Suzuki, T. In-context learning is provably Bayesian inference: a generalization theory for meta-learning. arXiv preprint arXiv:2510.10981 , 2025. Wang, D., Shelhamer, E., Liu, S., Olshausen, B., and Darrell, T. Tent: Fully test-time adaptation by entropy minimiza-tion. In International Conference on Learning Represen-tations , 2021. URL https://openreview.net/ forum?id=uXl3bZLkr3c .Wang, Q., Fink, O., Van Gool, L., and Dai, D. Continual test– time domain adaptation. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pp. 7201–7211, June 2022. Wolf, T., Debut, L., Sanh, V., Chaumond, J., Delangue, C., Moi, A., Cistac, P., Rault, T., Louf, R., Funtow-icz, M., Davison, J., Shleifer, S., von Platen, P., Ma, C., Jernite, Y., Plu, J., Xu, C., Le Scao, T., Gugger, S., Drame, M., Lhoest, Q., and Rush, A. Transform-ers: State-of-the-Art Natural Language Processing. In 

Proceedings of the 2020 Conference on Empirical Meth-ods in Natural Language Processing: System Demon-strations , pp. 38–45, Online, October 2020. Association for Computational Linguistics. doi: 10.18653/v1/2020. emnlp-demos.6. URL https://aclanthology. org/2020.emnlp-demos.6/ .Yao, Y., Rosasco, L., and Caponnetto, A. On early stop-ping in gradient descent learning. Constructive Approxi-mation , 2007. URL https://doi.org/10.1007/ s00365-006-0663-2 .Yuan, L., Xie, B., and Li, S. Robust test-time adaptation in dynamic scenarios. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition ,pp. 15922–15932, 2023. 12 A Decision-Theoretic View of Test-Time Training 

Zhang, T., Bi, S., Hong, Y., Zhang, K., Luan, F., Yang, S., Sunkavalli, K., Freeman, W. T., and Tan, H. Test-time training done right. arXiv preprint arXiv:2505.23884 ,2025. Zhao, H., Liu, Y., Alahi, A., and Lin, T. On pit-falls of test-time adaptation. In Proceedings of the 40th International Conference on Machine Learn-ing , volume 202 of Proceedings of Machine Learn-ing Research , pp. 42058–42080. PMLR, 23–29 Jul 2023. URL https://proceedings.mlr.press/ v202/zhao23d.html .13 A Decision-Theoretic View of Test-Time Training 

## A. Roadmap 

This appendix provides full proofs and additional analyses supporting the main claims. We begin with a self-contained primer on the relevant decision-theoretic background. We then derive the Bayes risk identities and other theorems stated in the main text. Finally, we describe the experimental setup to facilitate reproducibility. • Notation and background. We list the notation used throughout (App. B), provide a brief primer on decision theory (App. C), and relate our setting to classical early stopping (App. D). • Proofs for the main results. We provide proofs for the decision-theoretic setup (App. E.1), the spectral-filter and implicit-prior equivalence and out-of-sample (query) forms (App. E.2), and Bayes-gap identities under GP benchmarks including the query-coupled form used in the main text (App. E.3). We provide rigorous versions of the three necessity propositions (App. E.4) and full PAC-Bayes details for evidence-based per-prompt horizon selection (App. E.5). Finally, we prove the Bayes-optimal subspace characterization, and include global-design and plug-in regret bounds (App. E.6). • Validity and robustness of the local model. We bound finite-step deviations from linearization (App. F.1), extend filter identities beyond Gaussianity via second-moment arguments (App. F.2), and discuss anisotropic priors and preconditioning (App. F.3). • Interpretability: gradient flow and random-matrix theory. We provide a gradient-flow interpretation of the update horizon, derive deterministic oracle curves via random-matrix theory, and connect spectral properties to horizon selection (App. G). • Details specific to Transformers and stability. We derive Transformer Jacobian and geometry formulas (App. H.1), include a simple step-size stability bound (App. H.2), and characterize representable prompt sub-spaces for value updates (App. H.3). • Experimental details. We describe task generation, op-timization and evidence procedures, and the evaluation protocol (App. I). • Supplementary real-data check. We include a small SST-2 experiment with distilgpt2 , using 10 labeled prompt examples and one held-out query per task, to test the same two mechanisms—evidence-based horizon selection and query-aware head selection—outside the synthetic digit-shift setting (App. J). 

## B. Notation list 

This section collects notation for clarity. 

(Sec. 2) 

α: Latent task variable, α ∼ π.

π: Meta-distribution over tasks. 

Pα: Task-conditional data distribution on X × Y .

(Xi, Y i): Labeled prompt samples, i.i.d. given α.

(X, Y ): Query input/target random pair drawn from Pα.

(x, y ): A realized value of (X, Y ).

Pn: Labeled prompt of length n: Pn = {(Xi, Y i)}ni=1 .

n: Number of supervised prompt positions (residual terms), i.e., r ∈ Rn and K ∈ Rn×n. In our simplified setting, each labeled example corresponds to one supervised token position, so n also equals the number of labeled examples. 

dx: Input dimension X ⊂ Rdx .

M : Prediction rule mapping (Pn, X ) 7 → bY .

R(M ): Bayes risk E[( M (Pn, X ) − Y )2].

MBayes : Bayes predictor MBayes (Pn, X ) = E[Y |

Pn, X ].

G(M ): Bayes gap E[( M − MBayes )2].

V: Posterior variance E[Var( Y | Pn, X )] .

B: Base predictor (typically the no-update ICL predictor). 

C: Prompt-dependent correction rule, with M = B + C.

∆B : Bayes correction relative to B: ∆B (Pn, X ) =

MBayes (Pn, X ) − B(Pn, X ).

(Sec. 3) 

w, w 0: Model parameters and their pretrained value. 

p: Total number of parameters w ∈ Rp.

A: Orthonormal basis of the update subspace, A ∈ Rp×d

with A⊤A = Id.

d: Update-subspace dimension. 

U: Update subspace U = range( A).

ΠU : Orthogonal projector onto U, ΠU = AA ⊤.

θ: Update parameters, w(θ) = w0 + Aθ .

fw(x; Pn): Prediction at input x given prompt Pn under parameters w.

fθ (x; Pn): Linearized surrogate model. 

P (−i) 

> n

: Prompt with the ith example removed. 14 A Decision-Theoretic View of Test-Time Training 

r: Prompt residual vector: ri = yi − B(P (−i) 

> n

, x i).

ϕ(x; Pn): Jacobian feature ϕ(x; Pn) = ∇θ fθ (x; Pn)|θ=0 .

Φ: Prompt feature matrix in Rn×d, with rows 

ϕ(xi; P (−i) 

> n

)⊤.

L(θ): Quadratic prompt loss L(θ) = (2 σ2)−1∥r − Φθ∥22.

θ⋆: Latent update parameter signal in the local regression model r = Φ θ⋆ + ε.

ε: Noise vector in the local regression model, ε ∼N (0 , σ 2In).

σ2: Effective noise or temperature parameter. 

K: Prompt-induced geometry (kernel) K = K(Pn) = ΦΦ ⊤ ∈ Rn×n.

sw(Pn): Vector of prompt supervision outputs: sw(Pn) = (s1(w), . . . , s n(w)) .

Jw: Jacobian of prompt outputs Jw = ∇wsw(Pn)|w=w0 ∈

Rn×p.

Kfull : Full (unrestricted) kernel: Kfull = JwJ⊤ 

> w

.

S: Representable prompt subspace: S = range( K).

k: Token index / supervised-position index, k ∈{1, . . . , n }.

H: Number of attention heads. 

h: Attention head index. 

α(h) 

> ks

: Attention weight from token s to token k in head h.

zs: Residual-stream representation of token s.

Z: Stack of residual-stream vectors: Z = [ z⊤ 

> 1

; . . . ; z⊤ 

> n

].

W (h) 

> V

, W (h) 

> O

: Value and output matrices of head h.

g(h) 

> k

: Upstream gradient into head-h output at token kg(h) 

> k

:= ∇o(h)

> k

sk(w)|w0 .

m(h) 

> k

: Attention-mixed representation m(h) 

> k

=

P 

> s≤k

α(h) 

> ks

zs.

M (h): Stack of attention-mixed representations: M (h) := 

A(h)Z.

β(h) 

> k

: Projected gradient β(h) 

> k

= ( W (h) 

> O

)⊤g(h) 

> k

.

⊙: Hadamard (elementwise) product. 

B(h): Stack of projected gradients B(h) =[β(h)⊤ 

> 1

; . . . ; β(h)⊤ 

> n

].

(Sec. 4) 

t: GD iteration index ( t = 0 , 1, 2, . . . ). 

T : Update horizon (number of steps). 

η: GD step size (in parameter/update space). 

ρ: Normalized step size: ρ = η/σ 2.

θt: GD iterate, initialized at θ0 = 0 .

ˆft: Prompt-space correction: ˆft = Φ θt.

GT : TTT smoother: GT = In − (In − ρK )T .

gT (λ): GD spectral filter: gT (λ) = 1 − (1 − ρλ )T .

ˆfT : T -step prompt correction: ˆfT = GT r.

U, {λi}: Eigen-decomposition of K: K =

U diag( λ1, . . . , λ n)U ⊤.

ui: Eigenvector of K corresponding to the eigenvalue λi

(i.e., the i-th column of U ). 

g(K): Functional calculus of g; namely, g(K) =

U diag( g(λi)) U ⊤.

A ⪰ 0, A ⪯ B: For symmetric matrices, A ⪰ 0 means that A is positive semidefinite, and A ⪯ B means that 

B − A is positive semidefinite. 

λmax (A): The largest eigenvalue of a symmetric matrix A.

∥A∥op : The operator norm of A (equal to the largest singu-lar value; for symmetric A, it equals max i |λi(A)|). 

A†: The Moore–Penrose pseudoinverse. 

log det ∗(A): For a positive semidefinite matrix A with eigenvalues {μi}, log det ∗(A) := P 

> i:μi>0

log μi.

(Sec. 5) 

f⋆: Latent true prompt-level correction. 

τ 2: Prior variance / signal strength. 

λ⋆: test-time noise-to-signal ratio λ⋆ = σ2/τ 2.

g⋆(λ): Bayes-optimal shrinkage: g⋆(λ) = λ/ (λ + λ⋆).

ˆhT (x): Query-point correction induced by T -step TTT: 

ˆhT (x) = ϕ(x; Pn)⊤θT = k⊤ 

> x

qT (K) r.

fBayes : Bayes posterior mean. 

kx: Prompt–query kernel vector: kx = Φ ϕ(x; Pn) ∈ Rn.

qT (λ): Query-side TTT filter: qT (λ) = 1−(1 −ρλ )T 

> λ

for 

λ > 0 (and qT (0) = ρT ). 15 A Decision-Theoretic View of Test-Time Training 

q⋆(λ): Bayes-optimal resolvent: q⋆(λ) = ( λ + λ⋆)−1.

κx: Query-point kernel scalar in the GP benchmark, so that  K kx 

> k⊤
> xκx



⪰ 0 and Var( fx | K, k x, κ x) = τ 2κx.

α(h) 

> ks

: Attention weight (head h) from token s to token k.

A(h): Attention matrix with entries A(h) 

> ks

= α(h) 

> ks

.

(Sec. 7) 

T : Finite candidate set of horizons ( ⊂ N0). 

π0: Prior distribution over T .

ΣT : Evidence covariance: ΣT = σ2(I − ρK )−T .

ℓT (r): Normalized negative log-evidence for horizon T .

L(T | K): Population cross-entropy risk: L(T | K) = 

E[ℓT (r) | K].

νβ (T | r): Gibbs posterior: νβ (T | r) ∝

π0(T ) exp {− βn ℓ T (r)}.

β: Gibbs temperature parameter in the PAC-Bayes / evi-dence selection procedure. 

Σ⋆: True/reference covariance of r conditional on K in Assumption 7.1. 

AT : Information matrix: AT := Σ 1/2 

> ⋆

Σ−1 

> T

Σ1/2 

> ⋆

⪰ 0.

vT : Variance proxy: vT := 1 

> n

tr( A2 

> T

).

¯v: Max variance proxy: ¯v := sup T ∈T vT .

TMAP : Evidence/MAP-selected horizon: TMAP ∈

arg min T ∈T ℓT (r) (uniform π0). 

(Sec. 8) 

u⋆: Latent parameter correction, u⋆ ∼ N (0 , Σ) .

Σ: Prior covariance of parameter corrections. 

ε: Observation noise, ε ∼ N (0 , σ 2In).

¯u(r): Posterior mean: ¯u(r) = E[u⋆ | r].

Π: Rank-d orthogonal projector defining an update sub-space. 

buΠ(r): Constrained Bayes estimator: buΠ(r) = Π ¯ u(r).

I: Information-capture matrix: I = Var(¯ u(r)) =ΣJ⊤ 

> w

(JwΣJ⊤ 

> w

+ σ2In)−1JwΣ.

jX : Query Jacobian: jX = ∇wfw0 (X; Pn) ∈ Rp.

jx: Query Jacobian at a specific query input x: jx := 

∇wfw0 (x; Pn) ∈ Rp.

Q: Query sensitivity (semi)metric: Q := E[ jX j⊤ 

> X

] ⪰ 0

(conditioning on the prompt if desired). 

v: Q-whitened parameter coordinates: v := Q1/2u (re-stricted to range( Q) if Q is singular). 

eΠ: Rank-kbudget Euclidean orthogonal projector acting on v-space (defines the update subspace in the Q-whitened coordinates). 

kbudget : Rank / budget of the update subspace (number of adaptable directions/blocks). 

IQ: Query-whitened information-capture matrix: IQ := Var(¯ v(r)) = Q1/2IQ1/2 ⪰ 0.

## C. Primer on Decision Theory and Bayesian Estimation 

To render our theoretical framework self-contained and ac-cessible to readers less familiar with statistical decision theory (Lehmann & Casella, 1998; Robert, 2007; Mur-phy, 2022), we briefly review predictive risk, Bayes risk under our mixture task model, and the Bayes posterior (risk-minimizing) estimator. Throughout this primer, we use the squared loss and assume square-integrability (e.g., 

E[Y 2] < ∞) so that conditional expectations under squared loss are well-defined. We highlight the minimal assump-tions needed for each identity and indicate where they are relaxed in later appendices. 

C.1. Risk and Bayes-optimal prediction for a fixed distribution 

Let (Ω , F, P) be a probability space and let (X, Y ) : Ω →X × R be a random pair with joint law P. A (deterministic) predictor is a measurable map M : X → R, and the induced prediction is the random variable bY := M (X).

Predictive risk. Assume E[M (X)2] < ∞. The (squared-loss) risk (Bishop, 2006; Murphy, 2022) of M under P

is 

RP (M ) := E(M (X) − Y )2.

Even the best possible predictor incurs nonzero risk when 

Y is noisy given X.

Bayes predictor under squared loss. Any minimizer of 

RP (M ) over measurable M with E[M (X)2] < ∞ is given (uniquely PX -a.s.) by the conditional expectation: 

MBayes ,P (X) := E[Y | X].

Equivalently, any two minimizers agree PX -almost surely. 16 A Decision-Theoretic View of Test-Time Training 

C.2. Bayes risk under the mixture task model 

In the context of TTT and ICL, we do not face a single static distribution. Instead, we face a distribution of tasks (e.g., a mixture of different functions or domains (Wakayama & Suzuki, 2025)). As in (1) , let α ∼ π and, conditional on α, let (Xi, Y i)ni=1 

and (X, Y ) be conditionally i.i.d. with law Pα. Let Pn := 

{(Xi, Y i)}ni=1 and let the available information be σ-field 

σ(Pn, X ). A predictor is any σ(Pn, X )-measurable map: 

M : ( Pn, X ) 7 → bY ∈ R,

with E[ bY 2] < ∞.

Bayes risk (Robert, 2007; Murphy, 2022). Assume 

E[M (Pn, X )2] < ∞. We evaluate M by the task-averaged (Bayes) risk 

R(M ) := Eα∼π,P n∼P ⊗nα ,(X,Y )∼P α

(M (Pn, X ) − Y )2,

where the expectation is taken with respect to the joint dis-tribution of (α, P n, X, Y ) induced by the mixture model in (1) . Here, P⊗nα denotes n-fold product (tensor) measure of 

Pα.

Bayes predictor under the mixture model. Under squared loss, any minimizer of R(M ) over measurable maps M (Pn, X ) (with finite second moment) is given (uniquely a.s.) by the conditional expectation 

MBayes (Pn, X ) := E[Y | Pn, X ].

Bayes-risk decomposition (Prop. 2.1). For any predictor 

M with finite second moment, the Bayes risk decomposes as 

R(M ) = G(M ) (Bayes gap) + V (posterior variance) .

• The posterior variance (V) represents the inherent uncertainty in the targets Y that cannot be resolved even with infinite compute and perfect knowledge of the prior given σ(Pn, X ). It is constant with respect to the model M .• The Bayes Gap (G) measures how closely the model 

M approximates the optimal Bayes predictor. In other words, this is the excess risk to the Bayes predictor. To improve performance via TTT, we must reduce the Bayes Gap. This means our adapted model must essentially per-form a better approximation of the conditional expectation 

E[Y | Pn, X ] than the base model. 

Existence and regular conditional distributions Let 

G ⊆ F be a sub-σ-field and let Y ∈ L1(P). A condi-tional expectation E[Y | G ] is any G-measurable random variable Z satisfying R 

> A

Z d P = R 

> A

Y d P for all A ∈ G . Ex-istence follows from the Radon–Nikodym theorem and Z

is unique P-a.s.; throughout we fix a G-measurable version when writing E[Y | Pn, X ].In our setting (Pn, X, Y ) take values in standard Borel spaces, so there exists a regular conditional distribution 

P(dy | Pn, X ) and, whenever Y is integrable, E[Y |

Pn, X ] = R y P(dy | Pn, X ) holds σ(Pn, X )-a.s. 

## D. Relation to classical optimization theory 

Classical early stopping and iterative regularization analyzes gradient descent on a fixed quadratic problem, yielding the spectral filter form ˆfT = gT (K)r with gT (λ) = 1 −

(1 − ρλ )T Yao et al. (e.g., 2007); Engl et al. (e.g., 2000). We reuse this identity, but our TTT setting differs in three critical ways. 

(1) The geometry K is not fixed. Under our local lin-earization, the prompt-space geometry is K(Pn) = ΦΦ ⊤ =

JwΠU J⊤ 

> w

. Unlike classical analyses where K (or the de-sign) is exogenous, here K varies across prompts and changes with the update mechanism ΠU (which blocks/-heads/directions are adaptable). Thus, TTT behavior is inherently prompt- and mechanism-dependent. 

(2) The target is query prediction, not only in-sample fitting. Beyond the prompt correction ˆfT , prediction relies on the query-point correction ˆhT (x) = k⊤ 

> x

qT (K)r with 

kx = Φ ϕ(x; Pn). Hence, success depends not only on filter choice (via qT ) but also on prompt–query coupling: only eigenmodes aligned with kx affect the query (Thm. 5.2). 

(3) T and update directions are on-the-fly objects. In TTT, a fixed T can be Bayes-suboptimal under test-time prior shift, and T → ∞ can overfit (Sec. 6), motivating per-prompt T selection (Sec. 7). Moreover, no choice of T can overcome subspace mismatch: corrections lie in S(Pn) = range( K(Pn)) , so update-direction design is essential. Together, these distinctions explain why step sensitivity and update-direction choices are intrinsic to TTT, rather than secondary hyperparameter details. 

## E. Proofs (by main-text order) 

E.1. Proofs for Sec. 2 

This appendix provides proofs for the decision-theoretic statements in Sec. 2. Throughout, expectations are with re-spect to the joint distribution induced by the mixture model 17 A Decision-Theoretic View of Test-Time Training 

(1). E.1.1. P ROOF OF PROP . 2.1 

Proof. Let MBayes (Pn, X ) := E[Y | Pn, X ], and ξ := 

Y − MBayes (Pn, X ). Then E[ξ | Pn, X ] = 0 and Y =

MBayes (Pn, X ) + ξ. For any predictor M (Pn, X ) with 

E[M (Pn, X )2] < ∞,

 M (Pn, X ) − Y 2

=  M (Pn, X ) − MBayes (Pn, X ) − ξ2

=  M (Pn, X ) − MBayes (Pn, X )2 + ξ2

− 2ξ M (Pn, X ) − MBayes (Pn, X ).

Taking expectation and using the tower property, 

Eξ M − MBayes 

 = EEξ M − MBayes 

 | Pn, X  

= E  M − MBayes 

 E[ξ | Pn, X ]

= 0 .

Therefore, 

R(M ) = E

h M − MBayes 

2i

+ Eξ2 .

Finally, E[ξ2] = E[Var( Y | Pn, X )] by definition of condi-tional variance, and it does not depend on M . This yields (2). 

E.2. Proofs and additional details for Sec. 4 

This appendix derives the spectral-filter representation of TTT and proves the equivalence to Gaussian posterior means stated in Sec. 4. Before proceeding, several points should be noted. 

Beyond squared loss. Beyond squared loss, an analogous one-step update can be written by replacing the residual r

with the scaled pseudo-residual ˜r := −σ2∇sℓ(sw0 (Pn), y )

evaluated at θ = 0 . Extending the exact multi-step spectral-filter identities to general losses requires additional local cur-vature approximations (e.g., Gauss–Newton/Hessian freez-ing), which we leave as future work. 

Well-posedness of the prior. Since gT (0) = 0, the operator GT = gT (K) vanishes on null( K) and hence 

ˆfT ∈ range( K) for all T . The implicit prior covariance CT

is supported on range( K). It has eigenvalue 0 on null( K)

and positive eigenvalues only on range( K).E.2.1. D ERIVATION OF (6) 

Proof. Recall the GD update in update parameters (Sec. 4): 

θt+1 = θt + ρ Φ⊤(r − Φθt), ρ = η/σ 2, θ0 = 0 .

Define ˆft := Φ θt ∈ Rn. Multiplying both sides by Φ gives the prompt-space recursion 

ˆft+1 = ˆft + ρ ΦΦ ⊤(r − Φθt) = ˆft + ρK (r − ˆft).

Rearranging yields 

r − ˆft+1 = ( In − ρK )( r − ˆft).

Since ˆf0 = 0 , we have r − ˆf0 = r, and iterating gives 

r − ˆfT = ( In − ρK )T r, hence ˆfT =  In − (In − ρK )T r.Diagonalizing K = U diag( λi)U ⊤ implies 

GT = U diag  1 − (1 − ρλ 1)T , . . . , 1 − (1 − ρλ n)T  U ⊤

= U diag  gT (λ1), . . . , g T (λn) U ⊤,

where gT (λ) = 1 − (1 − ρλ )T , which yields the statement. Finally, if 0 < ρ < 2/λ max (K), then all eigenvalues of In−

ρK lie in (−1, 1] , and if moreover 0 < ρ < 1/λ max (K),then 0 ≤ 1 − ρλ i < 1 for all i, so gT (λi) ∈ [0 , 1) for all finite T .E.2.2. F ULL PROOF OF PROP . 4.1 

Proof. Recall 0 < ρ < 1/λ max (K). Let K =

U diag( λ1, . . . , λ n)U ⊤. Then 

GT = U diag  gT (λ1), . . . , g T (λn) U ⊤,

0 ≤ gT (λi) < 1 for all i.Define the implicit prior covariance CT :

CT := σ2 GT (In − GT )−1.

In the eigenbasis of K (equivalently of GT ), the matrix 

(In − GT )−1 is diagonal with entries 

(1 − gT (λi)) −1 =

( 11−gT (λi) , λi > 0,

1, λi = 0 ,

but since gT (0) = 0, the product gT (0)(1 −

gT (0)) −1 = 0. Hence CT has the form of CT =

U diag( cT, 1, . . . , c T,n )U ⊤, where 

cT,i =



σ2 gT (λi)1 − gT (λi) , λi > 0,

0, λi = 0 .

Now consider the Gaussian latent-variable model r = f + ε

with f ∼ N (0 , C T ) and ε ∼ N (0 , σ 2In) independent. Working in the eigenbasis of CT , we have coordinate-wise for ˜r := U ⊤r, ˜f := U ⊤f , and ˜ε := U ⊤ε:

˜ri = ˜fi + ˜ εi, ˜fi ∼ N (0 , c T,i ), ˜εi ∼ N (0 , σ 2).

18 A Decision-Theoretic View of Test-Time Training 

For each coordinate, the posterior mean is given by the scalar Gaussian conditioning formula (Rasmussen & Williams, 2006; Bishop, 2006): 

E[ ˜fi | ˜ri] = cT,i 

cT,i + σ2 ˜ri.

If λi > 0, then by the definition of cT,i ,

cT,i 

cT,i + σ2 = σ2 gT (λi)1−gT (λi)

σ2 gT (λi)1−gT (λi) + σ2 = gT (λi).

If λi = 0 , then cT,i = 0 and also gT (0) = 0 , so the identity still holds. Therefore, 

E[ ˜fi | ˜ri] = gT (λi)˜ ri for all i, 

and transforming back gives 

E[f | r] = U diag  gT (λ1), . . . , g T (λn)U ⊤r = ˆfT ,

where the last equality uses (6). E.2.3. O UT -OF -SAMPLE (QUERY ) FORM OF T -STEP GD 

> UNDER THE PROMPT GEOMETRY

We record the kernel-linear form of the T -step prediction at a query input x under the linearized model (Assumption 3.2). This form is used by the predictive Bayes-gap identity in Appendix E.3.2. 

Proposition E.1 (Out-of-sample form of TTT) . Work under Assumptions 3.1–3.2 and the quadratic prompt loss. Let 

K = ΦΦ ⊤ ∈ Rn×n and let r ∈ Rn be the prompt residual vector. For a query input x, define the prompt–query kernel vector 

kx := Φ ϕ(x; Pn) ∈ Rn,

so (kx)i = ⟨ϕ(xi; P (−i) 

> n

), ϕ (x; Pn)⟩. Let {θt}t≥0 be the GD iterates (5) with θ0 = 0 , and define the query correction 

ˆhT (x) := ϕ(x; Pn)⊤θT . Then for every T ≥ 1,

ˆhT (x) = k⊤ 

> x

qT (K) r, qT (K) := ρ 

> T−1

X

> t=0

(In −ρK )t, (7) 

where ρ = η/σ 2. Equivalently, if 

K = U diag( λ1, . . . , λ n)U ⊤, then ˆhT (x) =Pni=1 (u⊤ 

> i

kx) qT (λi) ( u⊤ 

> i

r), where 

qT (λ) = 



1 − (1 − ρλ )T

λ , λ > 0,ρT, λ = 0 .

(8) 

Moreover, on the prompt points one recovers ˆfT = gT (K)r

as ˆfT = Kq T (K)r.Proof. Unrolling the GD recursion (5) gives 

θT = ρ 

> T−1

X

> t=0

(Id − ρΦ⊤Φ) t Φ⊤r. 

Therefore, 

ˆhT (x) = ϕ(x)⊤θT = ρ 

> T−1

X

> t=0

ϕ(x)⊤(Id − ρΦ⊤Φ) t Φ⊤r. 

Using the identity (In − ρK )Φ = Φ( Id − ρΦ⊤Φ) and induction, one has (In − ρK )tΦ = Φ( Id − ρΦ⊤Φ) t. Hence 

ϕ(x)⊤(Id − ρΦ⊤Φ) t Φ⊤r =  Φ( Id − ρΦ⊤Φ) tϕ(x)⊤r

=  (In − ρK )tkx

⊤r

= k⊤ 

> x

(In − ρK )tr. 

Substituting back yields (7) . Diagonalizing K gives the scalar form (8) . Finally, for λ > 0 we have λq T (λ) = 1 −

(1 − ρλ )T = gT (λ), and gT (0) = 0 , so Kq T (K) = gT (K)

on range( K), implying ˆfT = Kq T (K)r on the prompt points. 

E.3. Proofs and additional details for Sec. 5 

This appendix proves the spectral Bayes-gap identity (Thm. E.4 (prompt values)) and records its query-point ana-logue under a standard GP extension. E.3.1. P ROMPT -VALUE (IN -SAMPLE ) IDENTITIES :BAYES -OPTIMAL SHRINKAGE AND FILTER -MISMATCH GAP 

This subsection identifies the Bayes-optimal predictor and shows that the Bayes gap of any spectral estimator is a weighted squared filter mismatch. Fix a prompt Pn and the associated geometry K ⪰ 0. Recall the prompt residuals r ∈ Rn from Sec. 3. We model the (unknown) latent prompt-level correction as a random vector 

f⋆ ∈ Rn and adopt the following benchmark prior. 

Assumption E.2 (Aligned GP benchmark on prompt values) .

Conditioned on K, the latent prompt-level correction and observed residuals satisfy f⋆ | K ∼ N (0 , τ 2K), r = f⋆ +

ε, ε ∼ N (0 , σ 2In), and f⋆ ⊥ ε, for some τ 2 > 0 and effective noise level σ2 > 0.The key structural assumption is alignment: Cov( f⋆ | K)

shares eigenvectors with K (here Cov( f⋆ | K) = τ 2K). This is what makes the Bayes-optimal estimator a spectral shrinkage rule on K and enables the exact filter-mismatch identities below. Within this benchmark, we model test-time prior shift as changes in the effective SNR τ 2/σ 2 (equivalently λ⋆), which shifts the Bayes-optimal shrinkage g⋆.19 A Decision-Theoretic View of Test-Time Training 

Proposition E.3 (Bayes-optimal estimator on prompt values) . Under Assumption E.2 with 

K = U diag( λ1, . . . , λ n)U ⊤, conditioned on (K, r ), the Bayes-optimal estimator of f⋆ under squared loss is the pos-terior mean fBayes = U diag  g⋆(λ1), . . . , g ⋆(λn) U ⊤r,where g⋆(λ) := λ/ (λ + λ⋆).

The Bayes-optimal estimator is a spectral shrinkage rule: each eigenmode with eigenvalue λ is scaled by g⋆(λ) ∈

[0 , 1] , where λ⋆ encodes the test-time noise-to-signal ratio. This provides a precise target for adaptation: a procedure helps when it moves its spectral shrinkage closer to g⋆ on the eigenvalues of K(Pn).We next quantify the Bayes gap of any spectral estimator under the GP benchmark. Let K = U diag( λ1, . . . , λ n)U ⊤. Given a measur-able map g : [0 , ∞) → R with g(0) = 0, define the spectral estimator ˆfg := g(K)r, where g(K) := 

U diag  g(λ1), . . . , g (λn) U ⊤.

Theorem E.4 (Bayes risk and Bayes gap in the spectral domain) . Assume Assumption E.2 and condition on K =

U diag( λi)U ⊤. Let ˆfg = g(K)r be any spectral estimator. Then 

E

h

∥ ˆfg − f⋆∥22 K

i

=

> n

X

> i=1

τ 2λi(1 − g(λi)) 2 + σ2g(λi)2 .

(9) 

Moreover, the unique minimizer over g(λi) is g⋆(λi) = 

λi/(λi +λ⋆), and the excess Bayes risk (equivalently, Bayes gap to fBayes ) satisfies the exact identity 

E

h

∥ ˆfg − f⋆∥22 K

i

− E∥fBayes − f⋆∥22 K

=

> n

X

> i=1

(τ 2λi + σ2)  g(λi) − g⋆(λi)2. (10) 

Proof. Work under Assumption E.2 and condition on K =

U diag( λ1, . . . , λ n)U ⊤. Let ˜f⋆ := U ⊤f⋆, ˜ε := U ⊤ε, and 

˜r := U ⊤r. Then, coordinate-wise for i = 1 , . . . , n ,

˜f⋆,i ∼ N (0 , τ 2λi), ˜εi ∼ N (0 , σ 2), ˜ri = ˜f⋆,i + ˜ εi,

and ( ˜f⋆,i , ˜εi) are independent across i.For a spectral estimator ˆfg = g(K)r, we have in the same eigenbasis 

eˆf g,i := ( U ⊤ ˆfg )i = g(λi)˜ ri.

Hence 

eˆf g,i − ˜f⋆,i =  g(λi) − 1 ˜f⋆,i + g(λi)˜ εi.

By independence and zero means, 

E



(eˆf g,i − ˜f⋆,i )2 K



= τ 2λi(1 − g(λi)) 2 + σ2g(λi)2.

Summing over i yields (9). To obtain the excess-risk identity, fix λ ≥ 0 and define the scalar risk 

rλ(g) := τ 2λ(1 − g)2 + σ2g2.

This is a strictly convex quadratic in g with derivative 

ddg rλ(g) = −2τ 2λ(1 −g)+2 σ2g = 2  (τ 2λ+σ2)g−τ 2λ,

so the unique minimizer is 

g⋆(λ) = τ 2λτ 2λ + σ2 = λλ + λ⋆

, λ⋆ = σ2/τ 2.

Completing the square gives 

rλ(g) − rλ(g⋆) = ( τ 2λ + σ2) ( g − g⋆)2.

Applying this identity with λ = λi and summing over i

yields (10). Thm. E.4 shows that, conditional on K(Pn), each prompt-only algorithm corresponds to a spectral shrinkage rule g,and its suboptimality is the weighted squared distance to the Bayes-optimal rule g⋆ on the spectrum of K(Pn). We interpret prior mismatch as this filter-level distance. We now compare the two procedures of interest within the prompt-level correction model: • Naive ICL (no update). No correction is fit on the prompt: ˆfICL = 0 , equivalently gICL (λ) ≡ 0.• T -step TTT. TTT yields ˆfT = gT (K)r with gT (λ) = 1 − (1 − ρλ )T (Eq. (6)). 

Corollary E.5 (Conditional Bayes gaps for ICL and TTT) . Under Assumption E.2 and conditioned on K =

U diag( λi)U ⊤,

E

h

∥ ˆfICL − fBayes ∥22 K

i

= Pni=1 (τ 2λi + σ2)g⋆(λi)2, and 

E

h

∥ ˆfT − fBayes ∥22 K

i

= Pni=1 (τ 2λi + σ2) gT (λi) −

g⋆(λi)2. In particular, within this benchmark, T -step TTT improves over naive ICL (in Bayes gap on prompt values) if and only if the weighted squared filter mismatch decreases: 

> n

X

> i=1

(τ 2λi+σ2) gT (λi)−g⋆(λi)2 <

> n

X

> i=1

(τ 2λi+σ2)g⋆(λi)2.

E.3.2. P ROOF OF THM . 5.2 

Proof. Condition on (K, k x, κ x) and work under Assump-tion 5.1. By construction, (f⋆, f x) is jointly Gaussian with mean 0, and Cov 

f⋆

fx



= τ 2

 K kx

k⊤ 

> x

κx



, r = f⋆ + ε, 

20 A Decision-Theoretic View of Test-Time Training 

ε ∼ N (0 , σ 2In), and ε ⊥ (f⋆, f x). Hence 

Cov( r) = τ 2K + σ2In = τ 2(K + λ⋆In),

Cov( fx, r ) = τ 2k⊤ 

> x

, λ⋆ := σ2/τ 2.

Gaussian conditioning yields the Bayes (posterior-mean) estimator 

fBayes (x) = E[fx | r, K, k x, κ x]= Cov( fx, r ) Cov( r)−1r

= k⊤ 

> x

(K + λ⋆In)−1r. 

Let ˆhq (x) = k⊤ 

> x

q(K) r be any kernel-linear estimator and define ∆q(K) := q(K)−q⋆(K) with q⋆(λ) := ( λ+λ⋆)−1.Then ˆhq (x) − fBayes (x) = k⊤ 

> x

∆q(K) r, and conditioning on (K, k x, κ x),

E

h

{ˆhq (x) − fBayes (x)}2 | K, k x, κ x

i

= k⊤ 

> x

∆q(K) Cov( r) ∆ q(K) kx

= k⊤ 

> x

∆q(K) ( τ 2K + σ2In) ∆ q(K) kx.

Diagonalize K = U diag( λ1, . . . , λ n) U ⊤ with columns 

{ui}ni=1 . Since q(K) and q⋆(K) are functions of K, they share eigenvectors with K, so expanding in this basis gives 

E

h

{ˆhq (x) − fBayes (x)}2 | K, k x, κ x

i

=

> n

X

> i=1

(τ 2λi + σ2) ( u⊤ 

> i

kx)2 {q(λi) − q⋆(λi)}2,

which matches Thm. 5.2 with ai(x) := (τ 2λi +

σ2)( u⊤ 

> i

kx)2.

E.4. Formal statements and proofs for Sec. 6 

E.4.1. P RIOR -SHIFT SEPARATION FOR FIXED SPECTRAL BASELINES 

Here, we use the term prior shift to denote test-time distri-bution shift that changes the task prior (equivalently, the effective SNR parameter λ⋆ = σ2/τ 2) in the GP bench-mark, thereby altering the Bayes-optimal shrinkage level. 

Proposition E.6 (Prior-shift separation on a projector ker-nel) . Work under Assumption 5.1 and focus on the prompt la-tent correction f⋆ ∈ Rn observed through r = f⋆ +ε, where (marginally) f⋆ | K ∼ N (0 , τ 2K) and ε ∼ N (0 , σ 2In) is independent. Assume K ∈ Rn×n is an orthogonal projector of rank m (so K2 = K and tr( K) = m), and recall the SNR parameter λ = σ2/τ 2 > 0. Then, the Bayes estimator of f⋆ given (K, r ) is 

f (λ)Bayes = 11 + λ Kr. 

Now fix any spectral estimator ˆf0 = g0(K)r with g0(0) = 0 ,and write a := g0(1) (so ˆf0 = aKr ). Assume a is fixed (i.e., it does not depend on λ). Then for any two distinct prior parameters λ1̸ = λ2, letting 

sj := 11 + λj

, τ 2 

> j

:= σ2

λj

(j ∈ { 1, 2}),

one has the separation bound 

max  

> j∈{ 1,2}

Eλj

h ˆf0 − f (λj )Bayes  

> 22

K

i

≥ m min 

> j∈{ 1,2}

(τ 2 

> j

+ σ2)

 |s1 − s2|

2

2

,

where Eλj denotes expectation under the model with prior parameter λj .Proof. Under the stated marginal prompt model, 

Cov( f⋆ | K) = τ 2K, Cov( r | K) = τ 2K + σ2In.

By standard Gaussian conditioning for r = f⋆ + ε,

f (λ)Bayes = E[f⋆ | r, K ]= τ 2K(τ 2K + σ2In)−1r

= K(K + λI n)−1r, 

where λ = σ2/τ 2. Since K is a projector, it has only eigenvalues 0 and 1, hence K(K +λI n)−1 = 11+ λ K, which yields f (λ)Bayes = 11+ λ Kr .Diagonalize K = U diag( Im, 0) U ⊤. Because g0(0) = 0 ,the functional calculus gives 

g0(K) = U diag( g0(1) Im, 0) U ⊤ = aK, a := g0(1) ,

so ˆf0 = g0(K)r = aKr .Fix j ∈ { 1, 2}. Under prior λj (equivalently τ 2 

> j

= σ2/λ j ), we have f (λj )Bayes = sj Kr , and sj := 11+ λj . Therefore, ˆf0 −

f (λj )Bayes = ( a−sj )Kr ⇒ ˆf0 −f (λj )Bayes  

> 22

= ( a−sj )2∥Kr ∥22.Taking conditional expectation given K (under prior j), 

Eλj

h ˆf0 − f (λj )Bayes  

> 22

K

i

= ( a − sj )2 Eλj [∥Kr ∥22 | K].

Since Eλj [r | K] = 0 and Cov λj (r | K) = τ 2 

> j

K + σ2In,

Eλj [∥Kr ∥22 | K] = tr  Cov λj (Kr | K)

= tr  K(τ 2 

> j

K + σ2In)K

= ( τ 2 

> j

+ σ2)tr( K)= m(τ 2 

> j

+ σ2).

21 A Decision-Theoretic View of Test-Time Training 

Hence, for each j ∈ { 1, 2},

Eλj

h ˆf0 − f (λj )Bayes  

> 22

K

i

= m(τ 2 

> j

+ σ2) ( a − sj )2. (11) Because s1̸ = s2, for any scalar a at least one of the dis-tances |a − s1| or |a − s2| is at least 12 |s1 − s2| (e.g., by considering the midpoint (s1 + s2)/2). Thus 

max 

> j∈{ 1,2}

(a − sj )2 ≥

 |s1 − s2|

2

2

.

Combine this with (11) and lower bound (τ 2 

> j

+ σ2) by 

min j∈{ 1,2}(τ 2 

> j

+ σ2) to obtain the stated separation bound. The final claim follows because a single fixed a cannot equal both s1 and s2.E.4.2. F ULL LEAST -SQUARES FITTING CAN BE DOMINATED 

The following proposition is a corollary of the Stein phe-nomenon (James & Stein, 1961; Shao & Strawderman, 1994). 

Proposition E.7 (Inadmissibility of projection on a

p-dimensional subspace) . Let r = f⋆ + ε with ε ∼N (0 , σ 2In) (known σ2), and assume f⋆ ∈ S where S ⊆ Rn

is a fixed subspace of dimension dst := dim( S) ≥ 3. Let 

ΠS be the orthogonal projector onto S. Then the full-fit estimator ˆf∞ := Π S r is inadmissible under squared loss 

∥ ˆf −f⋆∥22; namely, it is dominated by the (subspace) James– Stein shrinkage estimator 

ˆfJS =



0, ∥ΠS r∥2 = 0 ,



1 − (dst − 2) σ2

∥ΠS r∥22



ΠS r, otherwise .

Equivalently, for all f⋆ ∈ S ,

Ef⋆ ∥ ˆfJS − f⋆∥22 ≤ Ef⋆ ∥ ˆf∞ − f⋆∥22,

with strict inequality for some f⋆ ∈ S .Remark E.8 . Although Prop. E.7 is stated under f⋆ ∈ S , the conclusion can be read as a statement about improving the estimation of the in-subspace component ΠS f⋆ from ΠS r.For general f⋆ ∈ Rn, the orthogonal component contributes a fixed additive error by the Pythagorean decomposition (Prop. E.9). E.4.3. A DAPTING UPDATE STEPS CANNOT OVERCOME SUBSPACE MISMATCH 

Let K ⪰ 0 be symmetric and define the representable sub-space S := range( K) ⊆ Rn. Let ΠS and ΠS⊥ be Eu-clidean orthogonal projectors. 

Proposition E.9 (Irreducible error outside range( K)). For any target vector f⋆ ∈ Rn and any estimate ˆf ∈ S ,

∥ ˆf − f⋆∥22 = ∥ ˆf − ΠS f⋆∥22 + ∥ΠS⊥ f⋆∥22.

In particular, ∥ ˆf − f⋆∥22 ≥ ∥ ΠS⊥ f⋆∥22 and inf ˆf ∈S ∥ ˆf −

f⋆∥22 = ∥ΠS⊥ f⋆∥22.Moreover, any spectral estimator ˆfg = g(K)r with g(0) = 0 satisfies ˆfg ∈ S . In particular, TTT for any T lies in S.Proof. Decompose f⋆ = Π S f⋆ + Π S⊥ f⋆. If ˆf ∈ S , then 

ˆf − ΠS f⋆ ∈ S , while ΠS⊥ f⋆ ∈ S ⊥, so the cross term vanishes: 

∥ ˆf − f⋆∥22 = ∥ ˆf − ΠS f⋆∥22 + ∥ΠS⊥ f⋆∥22.

The lower bound and the infimum follow immediately. For the final claim, diagonalize K =

U diag( λ1, . . . , λ n) U ⊤. If g(0) = 0, then 

g(K) = U diag( g(λi)) U ⊤ annihilates ker( K) and therefore maps Rn into range( K) = S. Hence 

ˆfg = g(K)r ∈ S .

E.5. PAC-Bayes details for Sec. 7 

This appendix proves the normalized PAC-Bayes general-ization bound stated in Sec. 7. Throughout, we condition on the prompt-induced geometry K ⪰ 0 and treat all matrices as deterministic given K.

Remark E.10 (Gibbs posteriors as Radon–Nikodym deriva-tives; measurability and conditional PAC-Bayes) . We use a Gibbs construction to select (or average over) the update horizon T . This can be formulated cleanly in measure-theoretic terms, which clarifies measurability and why our PAC-Bayes guarantees are naturally stated conditionally on the prompt geometry K.

RN definition (general form). Let (H, A) be a hypoth-esis space (here H = T ) and let π0 be a prior probability measure on H. Given data D (here D = ( r, K ) or just 

r when K is fixed) and a measurable empirical score/loss 

ℓ : H × D → R, define the normalizer 

Zβ (D) := 

Z

> H

exp {− βn ℓ (h, D )} π0(dh ),

assumed finite and strictly positive. The Gibbs posterior is the probability measure νβ (· | D) on H defined by the Radon–Nikodym derivative 

dν β (· | D)

dπ 0

(h) = exp {− βn ℓ (h, D )}

Zβ (D) .

In our setting with finite T , this reduces to the familiar normalized weights νβ (T | r) ∝ π0(T ) exp {− βnℓ T (r)}.22 A Decision-Theoretic View of Test-Time Training 

Measurability (posterior as a kernel). If ℓ(h, D ) is mea-surable in (h, D ), then Zβ (D) is measurable in D and the map D 7 → νβ (· | D) is a measurable probability kernel from the data space to (H, A). Consequently, posterior expectations (model averaging) and measurable selectors such as an argmin/argmax (e.g., TMAP with a deterministic tie-breaking rule) are measurable functions of the observed data. 

Why the PAC-Bayes statement is naturally conditional on K. In Sec. 7 and App. E.5, we condition on the prompt-induced geometry K and analyze the randomness of r | K.This is not merely a convenience: the Gibbs posterior νβ (· | 

r, K ) is defined as an RN tilt of π0 given the realized data, so all PAC-Bayes change-of-measure steps (e.g., Donsker– Varadhan / exponential-moment arguments) can be applied 

pathwise in K to the conditional law of r | K. The resulting bounds therefore hold with high probability over r | K for each fixed K. If desired, one can then integrate over the distribution of K to obtain unconditional statements. 

Nullspace contributions. If K is singular, then on 

null( K) we have ΣT = σ2I for all T , so both log det(Σ T )

and r⊤Σ−1 

> T

r contribute only T -independent constants from 

null( K). Equivalently, after ignoring T -independent con-stants, all determinants/inverses may be interpreted on 

range( K).Let T be a finite set of candidates of T and let π0 be a prior on T with full support. Write ∆( T ) for the simplex of distributions over T .

Lemma E.11 (PAC-Bayes with exponential-moment) . Let 

{XT (r)}T ∈T be real-valued random variables and fix β > 

0. Assume there exist deterministic (given K) numbers 

ψT (β) such that 

Eexp  βX T (r) K ≤ exp {ψT (β)} ∀T ∈ T .

Then for any δ ∈ (0 , 1) , with probability at least 1 − δ over 

r | K, simultaneously for all ν ∈ ∆( T ),

ET ∼ν [XT (r)] ≤ 1

β

n

KL( ν∥π0)+log 1

> δ

o

+ 1

β ET ∼ν

ψT (β).

This is a standard PAC-Bayes result based on Donsker-Varadhan’s variational formula. See Lem. 2.2 and the proof of Thm. 2.1 in Alquier et al. (2024). 

Lemma E.12 (Exact MGF and a trace bound) . Under As-sumption 7.1, for every T ∈ T and β > 0,

E



exp 

 β

2

 tr( AT ) − Z⊤AT Z 

= exp 

 β

2 tr( AT )



det( I + βA T )−1/2, Z ∼ N (0 , I n).

(12) 

Moreover, 

log E



exp 

 β

2

 tr( AT ) − Z⊤AT Z 

≤ β2

4 tr( A2 

> T

).

(13) 

Proof. Diagonalize AT = U diag( ai)U ⊤ with ai ≥ 0 and set eZ = U ⊤Z ∼ N (0 , I ). Then Z⊤AT Z = P 

> i

ai eZ2

> i

and E[exp( − β 

> 2

Z⊤AT Z)] = Q

> i

(1 + βa i)−1/2 = det( I +

βA T )−1/2, which gives (12) . For (13) , use log(1 + u) ≥

u − u2/2 for u ≥ 0 to obtain log det( I + βA T ) ≥

βtr( AT ) − β2 

> 2

tr( A2 

> T

), and substitute into log of (12). E.5.1. P ROOF OF THE NORMALIZED PAC-B AYES BOUND 

Lemma E.13 (PAC-Bayes bound for update steps) . Grant Assumption 7.1 and let T be finite. Fix a prior π0 on T

with full support and β > 0. Then for any δ ∈ (0 , 1) , with probability at least 1 − δ over r | K, simultaneously for all 

ν ∈ ∆( T ),

ET ∼ν

L(T | K)

≤ ET ∼ν

ℓT (r) + KL( ν∥π0) + log(1 /δ )

βn + β

4 ¯v. (14) 

Proof. Write r = Σ 1/2 

> ⋆

Z with Z ∼ N (0 , I n). For fixed 

T , the log-determinant term log det(Σ T ) is deterministic, hence 

L(T | K) − ℓT (r) = 12n

n

tr(Σ −1 

> T

Σ⋆) − r⊤Σ−1 

> T

r

o

= 12n

n

tr( AT ) − Z⊤AT Z

o

.

Define 

XT (r) := n L(T | K)−ℓT (r) = 12

 tr( AT )−Z⊤AT Z.

By Lem. E.12, 

log E[exp( βX T (r)) | K] ≤ β2

4 tr( A2 

> T

)

⇒ E[exp( βX T (r)) | K] ≤ exp 

 β2

4 tr( A2 

> T

)



.

Apply Lem. E.11 with certificate ψT (β) = β2 

> 4

tr( A2 

> T

) to obtain, with probability ≥ 1 − δ,

ET ∼ν [XT (r)] 

≤ KL( ν∥π0) + log(1 /δ )

β + 1

β ET ∼ν

 β2

4 tr( A2 

> T

)



.

Divide by n and use vT = tr( A2 

> T

)/n to conclude (14) .23 A Decision-Theoretic View of Test-Time Training 

E.5.2. O PTIMIZING THE TEMPERATURE β (RATE FORM )

Corollary E.14 (Rate form via β-optimization) . On the event of Lem. E.13, for any ν ∈ ∆( T ),

ET ∼ν

L(T | K)

≤ ET ∼ν

ℓT (r) +

s

¯v KL( ν∥π0) + log(1 /δ )

n .(15) 

Proof. Starting from the last bound in (14) , set A := KL( ν∥π0) + log(1 /δ ) and minimize f (β) = A/ (βn ) + (β/ 4)¯ v over β > 0. The minimizer is β⋆ = 2 pA/ (n¯v),yielding f (β⋆) = p¯v A/n , which proves (15). If π0 is uniform on T and ν = δT , then KL( δT ∥π0) = log |T | and Cor. E.14 gives 

L(T | K) ≤ ℓT (r) + 

s

¯v log |T | + log(1 /δ )

n .

E.5.3. F ROM EVIDENCE MINIMIZATION TO VANISHING NORMALIZED BAYES GAP 

This subsection formalizes how evidence-based T selection controls (and, in well-specified regimes, drives to zero) the normalized Bayes gap under the GP benchmark of Assump-tion E.2. We adopt the notation from Sec. 7 under the GP bench-mark (Assumption E.2). Let Σ⋆ := τ 2K + σ2In be the marginal residual covariance under the prior. We define the oracle baseline risk Ltrue (K) and the normalized Bayes gap 

Gap n(T | K) as follows: 

Ltrue (K) := 12n

n

log det(Σ ⋆) + n

o

,

Gap n(T | K) := 1

n E

h

∥ ˆfT − fBayes ∥22 K

i

,

where ˆfT is the T -step TTT estimator and fBayes is the Bayes posterior mean. E.5.4. A TRANSFER INEQUALITY : KL CONTROLS THE 

BAYES GAP 

Lemma E.15 (A scalar quadratic lower bound) . For every 

t > 0,

t − 1 − log t ≥ (t − 1) 2

2 max {t, 1} . (16) 

Proof. If 0 < t ≤ 1, define h(t) := t − 1 − log t − (t−1) 2 

> 2

.Then h(1) = h′(1) = 0 and h′′ (t) = t−2 − 1 ≥ 0, so 

h(t) ≥ 0 on (0 , 1] . If t ≥ 1, define h(t) := t − 1 − log t − 

> (t−1) 2
> 2t

. Then h(1) = 0 and 

h′(t) = 1 − 1

t − 12



1 − 1

t2



= (t − 1) 2

2t2 ≥ 0,

so h(t) ≥ 0 for t ≥ 1. This yields (16). 

Lemma E.16 (Evidence-to-Bayes-gap transfer) . Under As-sumption E.2 and conditioned on K, for every T ∈ N0,

E

h

∥ ˆfT − fBayes ∥22 K

i

≤ 4σ2 KL( N (0 , Σ⋆) ∥ N (0 , ΣT )) .

(17) 

Moreover, 

KL( N (0 , Σ⋆) ∥ N (0 , ΣT )) = n



L(T | K) − L true (K)



.

(18) 

Proof. Condition on K = U diag( λi)U ⊤. In the K-eigenbasis, the Bayes shrinkage is g⋆(λ) = λ/ (λ + λ⋆)

and the T -step GD shrinkage is gT (λ) = 1 − (1 − ρλ )T .Since r | K ∼ N (0 , Σ⋆) with Σ⋆ = τ 2K + σ2I, the mode-wise Bayes gap is 

E

h

∥ ˆfT − fBayes ∥22 K

i

=

> n

X

> i=1

(τ 2λi+σ2) gT (λi)−g⋆(λi)2.

Define the dimensionless eigenvalues m⋆,i := τ 2λi+σ2 

> σ2

=1 + λi 

> λ⋆

≥ 1, m T,i := eig(Σ T )i 

> σ2

= (1 − ρλ i)−T ≥ 1, and 

ri := m⋆,i  

> mT ,i

> 0. Then g⋆(λi) = 1 − 1/m ⋆,i and gT (λi) = 1 − 1/m T,i , hence gT (λi) − g⋆(λi) = m−1 

> ⋆,i

− m−1 

> T,i

=(ri − 1) /m ⋆,i and therefore 

(τ 2λi + σ2) gT (λi) − g⋆(λi)2 = σ2 (ri − 1) 2

m⋆,i 

.

Since mT,i ≥ 1, one has ri = m⋆,i /m T,i ≤ m⋆,i , and also 

m⋆,i ≥ 1, so m⋆,i ≥ max {ri, 1}. Hence 

(τ 2λi + σ2) gT (λi) − g⋆(λi)2 ≤ σ2 (ri − 1) 2

max {ri, 1} . (19) On the other hand, since Σ⋆ and ΣT commute (both are maps of K), the Gaussian KL decomposes mode-wise: 

KL( N (0 , Σ⋆) ∥ N (0 , ΣT )) = 12

> n

X

> i=1



ri − 1 − log ri



.

Applying Lem. E.15 to each ri yields (ri−1) 2 

> max {ri,1}

≤ 2( ri −

1 − log ri), and combining with (19) gives 

(τ 2λi + σ2) gT (λi) − g⋆(λi)2 ≤ 2σ2(ri − 1 − log ri)= 4 σ2 12 (ri − 1 − log ri).

Summing over i proves (17). Finally, for zero-mean Gaussians, 

KL( N (0 , Σ⋆) ∥ N (0 , ΣT )) = 12

n

log det(Σ T ) − log det(Σ ⋆) + tr(Σ −1 

> T

Σ⋆) − n

o

.

Using the definitions of L(T | K) and Ltrue (K) yields (18). 24 A Decision-Theoretic View of Test-Time Training 

E.5.5. A PAC-B AYES HIGH -PROBABILITY BOUND FOR THE EB/MAP T

Let T ⊂ N0 be a finite candidate set and define the EB/MAP 

T bT ∈ arg min   

> T∈T

ℓT (r). (20) 

Lemma E.17 (High-probability Bayes-gap bound for evi-dence minimization) . Let T be finite. For any δ ∈ (0 , 1) ,with probability at least 1 − δ over r | K, Gap n( bT | K) ≤

4σ2



min T ∈T ℓT (r) − L true (K) + 

q ¯v(log |T | +log(1 /δ )) 

> n



.

Proof. On the 1 − δ event of Thm. 7.2 (point se-lection, uniform prior), L(T | K) ≤ ℓT (r) + p¯v(log |T | + log(1 /δ )) /n holds for all T ∈ T . Us-ing (18) yields, for all T ∈ T on the same event, 

KL( N (0 , Σ⋆) ∥ N (0 , ΣT )) ≤ n



ℓT (r) − L true (K) + 

q ¯v(log |T | +log(1 /δ )) 

> n



. Apply Thm. E.16 and divide by n,then use ℓ bT (r) = min T ∈T ℓT (r) by definition of bT .E.5.6. A SYMPTOTIC CONSEQUENCE : VANISHING NORMALIZED BAYES GAP 

Lemma E.18 (One-sided concentration of ℓT around 

L(T | K)). Fix T ∈ N0. Let AT = Σ 1/2 

> ⋆

Σ−1 

> T

Σ1/2 

> ⋆

and 

vT = 1 

> n

tr( A2 

> T

). Then for any δ ∈ (0 , 1) , with probability at least 1 − δ over r | K,

ℓT (r) ≤ L (T | K) + 

r vT log(1 /δ )

n + ∥AT ∥op log(1 /δ )

n .

(21) 

Proof. Write r = Σ 1/2 

> ⋆

Z with Z ∼ N (0 , I n). Then 

ℓT (r) − L (T | K) = 12n



Z⊤AT Z − tr( AT )



.

Diagonalize AT = U diag( ai)U ⊤ with ai ≥ 0

and eZ = U ⊤Z ∼ N (0 , I ). Then Z⊤AT Z =

P 

> i

ai eZ2 

> i

. A standard Chernoff bound for Gaussian quadratic forms (Vershynin, 2018) gives: for any t > 0,

P



Z⊤AT Z − tr( AT ) ≥ 2ptr( A2 

> T

) t + 2 ∥AT ∥op t



≤

e−t. Setting t = log(1 /δ ) and dividing by 2n yields (21) using tr( A2 

> T

) = nv T .

Theorem E.19 (Vanishing normalized Bayes gap) . Let 

{T n}n≥1 be a sequence of finite candidate sets and let bTn

be the EB selector (20) . Assume: 

(A1) (Approximation / well-specification.) There exists a (possibly n-dependent) comparator T ⋆n ∈ T n such that 

αn := 1

n KL  N (0 , Σ⋆) ∥ N (0 , ΣT ⋆n ) → 0.

(In particular, αn ≡ 0 in the well-specified case Σ⋆ =ΣT ⋆n .) 

(A2) (Complexity.) ¯vn := sup T ∈T n vT = O(1) and 

log |T n| = o(n).

(A3) (Boundedness.) ∥AT ⋆n ∥op = O(1) .Then, for any fixed δ ∈ (0 , 1) , with probability at least 

1 − 2δ (over r | K), 

Gap n( bTn | K)

≤ 4σ2

αn +

r ¯vn(log |T n| + log(1 /δ )) 

n

+

r vT ⋆n log(1 /δ )

n + ∥AT ⋆n ∥op log(1 /δ )

n



, (22) 

and in particular Gap n( bTn | K) → 0 in probability (and along the above high-probability event). Proof. Apply Lem. E.17 with confidence δ to get, with prob-ability ≥ 1−δ, Gap n( bTn | K) ≤ 4σ2



ℓT ⋆n (r)−L true (K)+ 

q ¯vn(log |T n|+log(1 /δ )) 

> n



, since ℓ bTn (r) ≤ ℓT ⋆n (r). Next, ap-ply Lem. E.18 to T = T ⋆n with confidence δ to obtain, with probability ≥ 1 − δ,

ℓT ⋆n (r) ≤ L (T ⋆n | K) + 

q vT ⋆n log(1 /δ ) 

> n

+ ∥AT ⋆n ∥op log(1 /δ ) 

> n

.

Intersect the two events (probability ≥ 1 − 2δ) and combine with L(T ⋆n | K) − L true (K) = αn by (18) . This yields (22) . The stated convergence follows from assumptions (i)–(iii). 

Summary: Evidence tuning does more than “avoid overfit” 

• KL controls Bayes gap: evidence closeness ⇒ pre-diction gap shrinks (App. E.5.3). • EB/MAP comes with guarantees: it yields high-probability Bayes-gap bounds, not just heuristics. 

E.6. Bayes-optimal update subspaces: proofs and extensions 

This appendix provides proofs and extensions for Sec. 8. We work under the linear-Gaussian correction model (As-sumption 8.1). Note that Assumption 8.1 implies Assumption 5.1. 

Lemma E.20 (Assumption 8.1 induces the Gaussian benchmark) . Under Assumption 8.1, condition on (Jw, j x)

and define f⋆ := Jwu⋆ ∈ Rn and fx := j⊤ 

> x

u⋆ ∈

R. Then (f⋆, f x) is jointly Gaussian with Cov( f⋆) =

JwΣJ⊤ 

> w

, Cov( f⋆, f x) = JwΣjx, and Var( fx) = j⊤ 

> x

Σjx.

25 A Decision-Theoretic View of Test-Time Training 

In particular, taking Σ = Π U yields the benchmark in As-sumption 5.1 with K = JwΠU J⊤ 

> w

and kx = JwΠU jx (and 

κx = j⊤ 

> x

ΠU jx). 

Remark on singular Q (whitened coordinates). When the query metric Q ⪰ 0 is singular (e.g., Q = jxj⊤ 

> x

in the QUERY -A WARE specialization), we interpret the whitening map v := Q1/2u as mapping into range( Q) and restrict all v-space objects (including any projector eΠ) to range( Q)

(equivalently, quotient out null( Q)). When mapping back to u, we understand Q−1/2 as the Moore–Penrose pseudoin-verse on range( Q) (cf. the implementation note in Sec. 8). 

Lemma E.21 (Posterior mean and covariance) . Under Assumption 8.1, the posterior is Gaussian u⋆ | r ∼N (¯ u, Σpost ) with ¯u = E[u⋆ | r] = ΣJ⊤ 

> w

(JwΣJ⊤ 

> w

+

σ2In)−1r, and Σpost = Σ −ΣJ⊤ 

> w

(JwΣJ⊤ 

> w

+σ2In)−1JwΣ.Proof. This is the standard Gaussian conditioning formula for linear observations (see Bishop, 2006, §2.3). 

Lemma E.22 (Constrained Bayes estimator equals projec-tion of the posterior mean) . Fix an orthogonal projector Π

of rank d in Rp. Among all measurable estimators bu = bu(r)

satisfying bu(r) ∈ range(Π) almost surely, the unique mini-mizer of E[∥bu − u⋆∥22 | r] is 

buΠ(r) = Π ¯ u(r) = Π E[u⋆ | r].

Remark. Lemma E.22 is purely Euclidean and applies ver-batim to the whitened coordinates v = Q1/2u: replacing 

(u⋆, ¯u, Π) with (v⋆, ¯v, eΠ) yields the constrained estimator 

bveΠ(r) = eΠ ¯ v(r).

Proof. Condition on r and decompose u⋆ = Π u⋆ + ( I −

Π) u⋆ into orthogonal components. For any bu ∈ range(Π) ,

∥bu−u⋆∥22 = ∥bu−Πu⋆∥22 +∥(I −Π) u⋆∥22. The second term is independent of bu, so the conditional risk is minimized by bu = E[Π u⋆ | r] = Π¯ u. Uniqueness follows from strict convexity. E.6.1. P ROOF OF THM . 8.2 

Proof. Work in the Q-whitened coordinates v := Q1/2u (re-stricted to range( Q) if Q is singular). Define v⋆ := Q1/2u⋆,

¯v(r) := Q1/2 ¯u(r), and IQ := Var(¯ v(r)) = Q1/2IQ1/2 ⪰

0. Fix a rank-kbudget Euclidean projector eΠ acting on v-space and restrict estimators to bv(r) ∈ range( eΠ) almost surely. By Lem. E.22 (applied to (v⋆, ¯v)), the unique Bayes-optimal constrained estimator is bveΠ(r) = eΠ ¯ v(r).Write v⋆ = ¯ v + z with z := v⋆ − ¯v. Then E[z | r] = 0 and 

Var( v⋆) = Var(¯ v)+ E[Var( v⋆ | r)] = IQ +E[Var( v⋆ | r)] .Moreover, ¯v and z are uncorrelated (and jointly Gaussian), so the cross term vanishes: 

R(eΠ) = E∥bveΠ(r) − v⋆∥22

 = E∥eΠ¯ v − (¯ v + z)∥22



= E∥(I − eΠ)¯ v∥22

 + E∥z∥22



= tr  (I − eΠ) IQ

 + tr  E[Var( v⋆ | r)] 

= tr(Var( v⋆)) − tr( eΠIQ)= tr( Q1/2ΣQ1/2) − tr( eΠIQ)= tr( QΣ) − tr( eΠIQ).

Therefore, minimizing R(eΠ) over rank-kbudget projec-tors is equivalent to maximizing tr( eΠIQ). Let IQ =Ppi=1 λiviv⊤ 

> i

with λ1 ≥ · · · ≥ λp ≥ 0. The maximum of tr( eΠIQ) over rank-kbudget projectors is achieved by tak-ing eΠ to project onto span {v1, . . . , v kbudget }.Let J := Jw(Pn) denote the prompt Jacobian (random via random prompts). In this subsection, we take the query met-ric Q ⪰ 0 to be global across prompts (e.g., Q = E[jX j⊤ 

> X

]

under the test-time query distribution). 2 Define I(J) := ΣJ⊤(JΣJ⊤+σ2In)−1JΣ, IQ(J) := Q1/2I(J)Q1/2, and 

IQ, glob := E[IQ(J)] , where the expectation is over the prompt distribution (equivalently over J). 

Corollary E.23 (Global optimal subspace) . Any rank-kbudget 

projector eΠ⋆ onto the top-kbudget eigenspace of IQ, glob max-imizes tr( eΠ IQ, glob ) and hence minimizes E[RJ,Q (eΠ)] over rank-kbudget projectors, where RJ,Q (eΠ) is the conditional risk in Thm. 8.2. Proof. By Thm. 8.2 applied conditionally on (J, Q ),

RJ,Q (eΠ) = tr( QΣ) − tr( eΠ IQ(J)) . Taking expecta-tion over random prompts (equivalently over J) yields 

E[RJ,Q (eΠ)] = E[tr( QΣ)] − tr( eΠ IQ, glob ). Thus minimiz-ing E[RJ,Q (eΠ)] is equivalent to maximizing tr( eΠ IQ, glob ),achieved by the top-kbudget eigenspace of IQ, glob .E.6.2. F INITE -SAMPLE PLUG -IN SUBSPACE SELECTION AND A REGRET BOUND 

Suppose we estimate IQ, glob from m prompts, yielding 

IQ(J1), . . . , IQ(Jm), and define 

bIQ := 1

m

> m

X

> j=1

IQ(Jj ).

Let beΠ be the rank-kbudget projector onto the top-kbudget 

eigenspace of bIQ, and let eΠ⋆ be the analogous projector for IQ, glob .

> 2

If one instead lets Q depend on the prompt, the same deriva-tion holds conditionally, but mapping back to an u-space update subspace involves Q−1/2(Pn) and can become prompt-dependent. 

26 A Decision-Theoretic View of Test-Time Training 

Assumption E.24 (Matrix-martingale concentration con-ditions for bIQ). Let Fj := σ(J1, . . . , J j ) and define the centered increments Xj := IQ(Jj ) − I Q, glob ∈ RdQ×dQ .Assume: 

(A1) (Martingale-difference mean.) E[Xj | F j−1] = 0 a.s. for all j. (In particular, this holds when Jj are i.i.d.) 

(A2) (Uniform boundedness.) There exists R > 0 such that 

∥Xj ∥op ≤ R a.s. for all j.

(A3) (Predictable quadratic variation bound.) Let Vm := Pmj=1 E[X2 

> j

| F j−1]. Assume ∥Vm∥op ≤ ν a.s. for some ν > 0.

Theorem E.25 (Subspace selection regret via matrix Bern-stein/Freedman) . Under Assump. E.24, for any δ ∈ (0 , 1) ,with probability at least 1 − δ over the m prompts used to form bIQ,

bIQ − I Q, glob op ≤ εm,δ , (23) 

where εm,δ := 1 

> m

(

q

2ν log 2dQ 

> δ

+ 2R 

> 3

log 2dQ 

> δ

). Conse-quently, on the same event, 

E[RJ,Q (beΠ)] − E[RJ,Q (eΠ⋆)] ≤ 2kbudget εm,δ . (24) 

Proof. Let Sm := Pmj=1 Xj = m(bIQ − I Q, glob ). Under Assump. E.24 (A1)–(A3), the self-adjoint matrix Freedman inequality (Tropp, 2012) states that for all t ≥ 0,

P(∥Sm∥op ≥ t) ≤ 2dQ · exp − t2

2ν + 2Rt 

> 3

!

.

Setting t =

q

2ν log 2dQ 

> δ

+ 2R 

> 3

log 2dQ 

> δ

yields P(∥Sm∥op ≥

t) ≤ δ. Dividing by m gives (23). On the event ∥bIQ − I Q, glob ∥op ≤ εm,δ , write bIQ =

IQ, glob + E with ∥E∥op ≤ εm,δ . Since beΠ maximizes 

tr( eΠ bIQ) over rank-kbudget projectors, 

tr( beΠ bIQ) ≥ tr( eΠ⋆ bIQ).

Expanding bIQ = IQ, glob + E gives 

tr( eΠ⋆ IQ, glob ) − tr( beΠ IQ, glob ) ≤ tr( eΠ⋆E) − tr( beΠE).

For any rank-kbudget projector eΠ, |tr( eΠE)| ≤ kbudget ∥E∥op ,so the RHS is at most 2kbudget εm,δ . Finally, by the global-risk identity E[RJ,Q (eΠ)] = const − tr( eΠ IQ, glob ) (cf. Cor. E.23), this implies (24). 

Remark E.26 (i.i.d. prompts) . If J1, . . . , J m are i.i.d., then (A1) holds. One may take ν = m E[X21 ] op and (23) becomes 

bIQ−I Q, glob op ≤

s

2 E[X21 ] op log 2dQ

> δ

m + 2R

3m log 2dQ

δ .

Summary: Subspace design can be learned globally 

• Global mechanism: average IQ over prompts, take top eigenspace (App. E.6). • Finite-sample stability: plug-in top-eigenspace has a concentration and regret bound. 

## F. Validity & Robustness 

F.1. Finite-step validity of the local linearization 

In the main text, starting in Sec. 3, we suppose the locally linearized (kernel/NTK) regime. This appendix analyzes how accurately the linearized model represents the actual nonlinear gradient updates used in prompt-only TTT. We show that this linear model is a reliable approximation in Theorem F.5. As long as the local conditions are smooth and the step size is moderate, the difference between the true nonlinear updates and the linear version remains small for a few steps ( T ). F.1.1. N ONLINEAR PROMPT DYNAMICS AND LINEARIZED DYNAMICS 

Given a prompt Pn, we define sw(Pn) ∈ Rn as the vector of predictions produced by the model at weights w for the n

supervision points. Let w0 be pretrained weights and r := 

y − sw0 (Pn) ∈ Rn the base residuals. We restrict test-time updates to a d-dimensional subspace via w(θ) = w0 + Aθ 

(Assumption 3.1). Define the in-prompt prediction shift s(θ) := sw(θ)(Pn) −

sw0 (Pn) ∈ Rn with s(0) = 0 . Consider the squared prompt loss 

L(θ) := 12σ2 ∥r − s(θ)∥22. (25) Let the Jacobian in update parameters be J(θ) := ∇θ s(θ) ∈

Rn×d and the fixed Jacobian be Φ := J(0) ∈ Rn×d.

Nonlinear TTT (true GD). Gradient descent with step size η > 0 on (25) is 

θt+1 = θt − η∇L(θt) = θt + ρ J (θt)⊤ r − s(θt), (26) where θ0 = 0 and ρ := η/σ 2. Define the induced prompt-space correction and residual ft := s(θt) ∈ Rn, and et := 

r − ft = r − s(θt) ∈ Rn.

Linearized TTT (fixed Jacobian). The linearized model replaces s(θ) by Φθ. The corresponding GD iterates satisfy 

θlin  

> t+1

= θlin  

> t

+ ρ Φ⊤ r − Φθlin 

> t

, θlin 0 = 0 , (27) with prompt-space correction f lin  

> t

:= Φ θlin  

> t

∈ Rn and resid-ual elin  

> t

:= r − f lin  

> t

. Let the (prompt-dependent) restricted 27 A Decision-Theoretic View of Test-Time Training 

kernel at initialization be K0 := ΦΦ ⊤ ⪰ 0. Then, f lin 

> t

satisfies the closed recursion 

f lin  

> t+1

= f lin  

> t

+ ρK 0 (r − f lin  

> t

) = f lin  

> t

+ ρK 0 elin  

> t

, (28) and hence f lin  

> T

=  In − (In − ρK 0)T r.F.1.2. F INITE -STEP DEVIATION BOUND 

We impose standard regularity assumptions in a neighbor-hood of θ = 0 .

Assumption F.1 (Bounded and Lipschitz Jacobian) . There exist R > 0 and constants B, L > 0 such that for all θ, θ ′ ∈

Rd with ∥θ∥2 ≤ R and ∥θ′∥2 ≤ R,

∥J(θ)∥op ≤ B, (29) 

∥J(θ) − J(θ′)∥op ≤ L ∥θ − θ′∥2. (30) Assumption F.1 implies a second-order Taylor remainder bound. 

Lemma F.2 (Second-order remainder bound) . Under As-sumption F.1, for any θ, u ∈ Rd such that ∥θ∥2 ≤ R and 

∥θ + u∥2 ≤ R, ∥s(θ + u) − s(θ) − J(θ)u∥2 ≤ L 

> 2

∥u∥22

holds. Proof. By the integral form of Taylor’s theorem, s(θ + u) −

s(θ) = R 10 J(θ+tu ) u dt . Hence, s(θ+u)−s(θ)−J(θ)u =

R 10 (J(θ+tu )−J(θ)) u dt , and (30) yields ∥s(θ+u)−s(θ)−

J(θ)u∥2 ≤ R 10 Lt ∥u∥22 dt = L 

> 2

∥u∥22.Suppose the step-size regime 

0 < ρ ≤ 1

B2 , (31) which ensures ∥In − ρK t∥op ≤ 1 for all t as long as 

∥J(θt)∥op ≤ B, where Kt := J(θt)J(θt)⊤.

Assumption F.3 (Iterate stability) . For a given T ∈ N,the nonlinear GD iterates (26) satisfy ∥θt∥2 ≤ R for all 

t = 0 , 1, . . . , T .Define the time-varying kernel and the linearized kernel 

Kt := J(θt)J(θt)⊤ ⪰ 0, and K0 := J(0) J(0) ⊤ = ΦΦ ⊤.We first derive a function-space recursion for the nonlinear dynamics. 

Lemma F.4 (Nonlinear prompt-space recursion) . Under Assumption F.1, if ∥θt∥2 ≤ R and ∥θt+1 ∥2 ≤ R, then 

ft+1 = ft + ρK tet + ξt, where the remainder term ξt ∈ Rn

satisfies ∥ξt∥2 ≤ L 

> 2

∥θt+1 − θt∥22.Proof. Let ∆θt := θt+1 − θt = ρJ (θt)⊤et by (26) . Apply Lem. F.2 with θ = θt and u = ∆ θt:

ft+1 − ft = s(θt + ∆ θt) − s(θt) = J(θt)∆ θt + ξt,

where ∥ξt∥2 ≤ L 

> 2

∥∆θt∥22. Since J(θt)∆ θt =

ρJ (θt)J(θt)⊤et = ρK tet, we obtain the statement. We now bound the deviation fT − f lin  

> T

.

Theorem F.5 (Finite-step deviation between nonlinear and linearized prompt corrections) . Fix a T ∈ N. Assume As-sumptions F.1 and F.3, and the step-size condition (31) . Let 

fT = s(θT ) be the nonlinear prompt correction after T

steps of (26) , and let f lin  

> T

= Φ θlin  

> T

be the linearized prompt correction after T steps of (27) . Define the residual scale inside the stability ball ER := sup ∥θ∥2≤R ∥r − s(θ)∥2.

Then 

∥fT − f lin  

> T

∥2 ≤ 32 L B 2 ρ2 T 2  max {ER, ∥r∥2}2

≤ 32 L B 2 ρ2 T 2 E2

> R

. (32) 

In particular, for fixed local regularity constants (B, L ) and residual scale ER, the discrepancy is O (ρT )2.Proof. Define the discrepancy δt := ft − f lin  

> t

∈ Rn. By Lem. F.4 and (28), 

δt+1 =  ft + ρK tet + ξt

 −  f lin  

> t

+ ρK 0elin 

> t



= δt + ρK t(r − ft) − ρK 0(r − f lin  

> t

) + ξt

= δt + ρK t(elin  

> t

− δt) − ρK 0elin  

> t

+ ξt

= ( In − ρK t)δt + ρ(Kt − K0)elin  

> t

+ ξt.

Taking norms and using ∥In − ρK t∥op ≤ 1 (from (31) and 

∥J(θt)∥op ≤ B), 

∥δt+1 ∥2 ≤ ∥ δt∥2 + ρ∥Kt − K0∥op ∥elin  

> t

∥2 + ∥ξt∥2. (33) Under (31) , 0 ⪯ In − ρK 0 ⪯ In, so elin  

> t

= ( In − ρK 0)tr

implies ∥elin  

> t

∥2 ≤ ∥ r∥2 for all t.Write Jt := J(θt). Then, Kt − K0 = JtJ⊤ 

> t

− J0J⊤ 

> 0

=(Jt − J0)J⊤ 

> t

+ J0(Jt − J0)⊤, so ∥Kt − K0∥op ≤ ∥ Jt −

J0∥op ∥Jt∥op + ∥J0∥op ∥Jt − J0∥op ≤ 2B ∥Jt − J0∥op . By (30) , ∥Jt − J0∥op ≤ L∥θt∥2. Moreover, from (26) and (29) ,we have 

∥θt+1 − θt∥2 = ρ∥J(θt)⊤et∥2

≤ ρ ∥J(θt)∥op ∥et∥2

≤ ρ B E R,

since ∥et∥2 = ∥r − s(θt)∥2 ≤ ER for ∥θt∥2 ≤ R. Thus 

∥θt∥2 ≤ t ρBE R, and hence 

∥Kt − K0∥op ≤ 2BL ∥θt∥2 ≤ 2LB 2 ρ t E R. (34) From Lem. F.4 and the bound on ∥θt+1 − θt∥2,

∥ξt∥2 ≤ L

2 (ρBE R)2 = L

2 B2ρ2E2

> R

. (35) Plug (34), ∥elin  

> t

∥2 ≤ ∥ r∥2, and (35) into (33): 

∥δt+1 ∥2 ≤ ∥ δt∥2 + ρ(2 LB 2ρtE R)∥r∥2 + L

2 B2ρ2E2

> R

.

28 A Decision-Theoretic View of Test-Time Training 

Let E := max {ER, ∥r∥2}. Then ∥r∥2 ≤ E and ER ≤ E,so 

∥δt+1 ∥2 ≤ ∥ δt∥2 + 2 LB 2ρ2 t E 2 + L

2 B2ρ2E2.

Since δ0 = 0 , summing from t = 0 to T − 1 gives 

∥δT ∥2 ≤ 2LB 2ρ2E2 

> T−1

X

> t=0

t + L

2 B2ρ2E2 

> T−1

X

> t=0

1= LB 2ρ2E2T (T − 1) + L

2 B2ρ2E2T

≤ 32 LB 2ρ2E2T 2,

which is (32). 

Corollary F.6 (Kernel drift along nonlinear TTT) . Under the assumptions of Thm. F.5, for every t ≤ T ,

∥Kt − K0∥op ≤ 2LB 2 ρ t E R.

Deviation bound for query predictions Thm. F.5 con-trols discrepancies on the prompt supervision values. Asimilar bound can be derived for query-point predictions under additional smoothness assumptions. Concretely, sup-pose the query prediction shift 

∆( x; θ) := fw(θ)(x; Pn) − fw0 (x; Pn)

is twice differentiable in θ on the same ball {∥ θ∥2 ≤ R},with Jacobian ∇θ ∆( x; θ) bounded and Lipschitz uniformly in θ. Then, one can repeat the argument above to show that the gap between the nonlinear query correction ∆( x; θT )

and its linearized form ϕ(x; Pn)⊤θlin  

> T

is O(( ρT )2). We omit the algebraic details to avoid repetition: the proof follows the same contraction, kernel-drift, Taylor-remainder decomposition as in Thm. F.5, merely replacing the prompt map with the query map. 

F.2. Generalizing Filter Mismatch to Non-Gaussian Priors 

In Sec. 5, we analyzed the Bayes gap assuming Gaussianity. This appendix discusses the robustness of the mode-wise MSE decomposition and the squared mismatch identity. The statements require only independence and finite second moments. 

Proposition F.7 (Mode-wise MSE and squared mismatch without Gaussianity) . Fix K ⪰ 0 and suppose that r =

f⋆ + ε, E[f⋆] = 0 , Cov( f⋆) = C⋆ E[ε] = 0 , Cov( ε) = 

σ2In, f ⋆ ⊥ ε. Let S be any symmetric matrix such that 

SK = KS . Then, there exists an orthogonal matrix U

such that K and S are simultaneously diagonalized as K =

U diag( λ1, . . . , λ n) U ⊤, S = U diag( s1, . . . , s n) U ⊤. De-fine ci := u⊤ 

> i

C⋆ui = Var( u⊤ 

> i

f⋆) ≥ 0, where ui is the ith column of U . Then, the MSE of ˆfS := Sr satisfies 

E

h

∥ ˆfS − f⋆∥22 K, S 

i

=

> n

X

> i=1

n

ci(1 − si)2 + σ2s2

> i

o

. (36) 

Moreover, for each coordinate i, the unique minimizer over 

si ∈ R is s⋆i = ci 

> ci+σ2

and the excess risk admits the exact squared-mismatch identity 

E

h

∥ ˆfS − f⋆∥22 K, S 

i

− E

h

∥ ˆfS⋆ − f⋆∥22 K, S 

i

=

> n

X

> i=1

(ci + σ2) ( si − s⋆i )2, (37) 

where S⋆ := U diag( s⋆

> 1

, . . . , s ⋆n) U ⊤.

This proposition suggests that, relying only on second-moment conditions (no Gaussianity), the estimation error decomposes into independent mode-wise shrinkage prob-lems in the eigenbasis of K. The optimal shrinkage is given by s⋆i = ci 

> ci+σ2

, and the excess risk admits an exact identity as the weighted sum of squared mismatches (si − s⋆i )2.

Proof. Let ˜r := U ⊤r, ˜f := U ⊤f⋆, and ˜ε := U ⊤ε. Since U

is orthogonal, ∥ ˆfS −f⋆∥22 = ∥eˆf − ˜f ∥22, where eˆf = diag( si)˜ r

and ˜r = ˜f + ˜ ε. Thus, 

∥eˆf − ˜f ∥22 =

> n

X

> i=1

(si ˜ri − ˜fi)2 =

> n

X

> i=1

 (si − 1) ˜fi + si ˜εi

2.

Taking expectations and using f⋆ ⊥ ε (hence E[ ˜fi ˜εi] = 0 ), 

E[ ˜f 2 

> i

] = ci, and E[˜ ε2 

> i

] = σ2 yields (36). For fixed i, the scalar risk is a strictly convex quadratic 

ci(1 − si)2 + σ2s2 

> i

, whose unique minimizer is s⋆i = ci 

> ci+σ2

.Completing the square gives ci(1 − s)2 + σ2s2 = ci(1 −

s⋆)2 + σ2(s⋆)2 + ( ci + σ2)( s − s⋆)2, which implies (37) after summing over i.

Remark F.8 (Connection to the GP benchmark in Sec. 5) .

If C⋆ = τ 2K for some τ 2 > 0 (covariance alignment), then ci = τ 2λi and s⋆i = λi/(λi + λ⋆) with λ⋆ = σ2/τ 2,recovering the shrinkage g⋆(λ) used in the main text. If, in addition, (f⋆, ε ) are jointly Gaussian, then ˆfS⋆ coincides with the Bayes posterior mean for that GP model. 

F.3. Extension to anisotropic priors 

This subsection extends the GP benchmark by relaxing the aligned covariance assumption (covariance proportional to 

K) in Assumption 5.1, allowing anisotropic task variability in the update parameters. Specifically, we show that ap-propriate preconditioning yields Bayes-consistent filtering under the induced anisotropic geometry. 29 A Decision-Theoretic View of Test-Time Training 

Assumption F.9 (Anisotropic Gaussian prior in update pa-rameters) . Let Φ ∈ Rn×d be the prompt feature matrix and consider the linearized residual model r = Φ θ⋆ + ε, ε ∼N (0 , σ 2In). Assume an anisotropic prior on the latent up-date parameter signal θ⋆ ∼ N (0 , τ 2Σ) , with Σ ∈ Rd×d and 

λ⋆ := σ2/τ 2.

Proposition F.10 (Bayes-optimal predictor on prompt val-ues under anisotropic priors) . Under Assumption F.9, the latent prompt-level signal f⋆ := Φθ⋆ satisfies f⋆ ∼N (0 , τ 2 eK), eK := ΦΣΦ ⊤, and the Bayes-optimal estima-tor of f⋆ given r under squared loss is 

fBayes = E[f⋆ | r] = eK( eK + λ⋆In)−1r. (38) 

Proof. Since θ⋆ ∼ N (0 , τ 2Σ) and f⋆ = Φ θ⋆ is linear in 

θ⋆, we have f⋆ ∼ N (0 , τ 2ΦΣΦ ⊤) = N (0 , τ 2 eK). With 

r = f⋆ + ε and ε ∼ N (0 , σ 2In), Gaussian conditioning yields the posterior mean (38). 

Theorem F.11 (Preconditioned GD induces spectral filtering under eK). Under Assumption F.9, consider preconditioned GD in θ-space 

θt+1 = θt + ρ Σ Φ ⊤(r − Φθt), (39) 

where θ0 = 0 and ρ := η/σ 2. Let ˆft := Φ θt. Then, 

ˆft+1 = ˆft + ρ eK (r − ˆft) holds, where eK := ΦΣΦ ⊤ and hence, for any T ≥ 0, ˆfT = eGT r holds, where eGT := 

In − (In − ρ eK)T .

Consequently, all spectral-filter and Bayes-gap results for TTT apply verbatim after replacing K by eK: TTT selects an implicit prior under the geometry induced by eK, and the Bayes-optimal shrinkage on prompt values is eK( eK +

λ⋆In)−1.

Proof. Multiply (39) by Φ to get 

ˆft+1 = Φ θt+1 = Φ θt + ρΦΣΦ ⊤(r − Φθt)= ˆft + ρ eK(r − ˆft).

Solving the linear recursion concludes the proof. 

Remark F.12 (Interpretation: whitening and Bayes-consis-tency) . Under the anisotropic prior, the Bayes-optimal pre-dictor depends on eK = ΦΣΦ ⊤ rather than K = ΦΦ ⊤.Preconditioning by Σ reshapes the effective geometry so that the induced GD filter family can be viewed as Bayes-consistent shrinkage after whitening the update parameter prior. 

Summary: robustness of the spectral theory 

• Even non-Gaussian: excess risk is still a squared spectral shrinkage mismatch under 2nd moments (App. F.2). • Anisotropic tasks: preconditioning replaces K by 

eK = ΦΣΦ ⊤ (App. F.3). 

## G. Gradient flow and random-matrix theory view of the update horizon T

This appendix explains how the update horizon is governed by the spectrum of the prompt geometry K(Pn). We recast discrete T -step updates via the continuous diffusion time 

t ≈ ρT , under which the estimator becomes an explicit spectral filter gt(λ) = 1 − e−tλ , making “how far to adapt” a one-parameter shrinkage rule over eigenmodes. We then use this view to define an oracle (risk-optimal) horizon and to motivate deterministic proxies from random-matrix con-centration, clarifying why the best T is inherently prompt-and subspace-dependent. 

G.1. Gradient flow as the canonical horizon parameter 

Fix a prompt of length n. Let K ∈ Rn×n be the prompt geometry, and r ∈ Rn be the prompt residual vector. Write an eigendecomposition K = U diag( λ1, . . . , λ n)U ⊤ with 

λi ≥ 0.

Proposition G.1 (Gradient flow solution and spectral filter) .

Consider the gradient-flow ODE ˙f (t) = K(r − f (t)) with 

f (0) = 0 . Then 

f (t) = ( I − e−tK )r = U diag  gt(λ1), . . . , g t(λn) U ⊤r, 

where gt(λ) := 1 − e−tλ .

Proof. Let e(t) := r − f (t). Then ˙e(t) = −Ke (t) and 

e(0) = r, so e(t) = e−tK r, hence f (t) = r − e(t). Diago-nalization yields the scalar filter. Each eigenmode ui is learned at rate λi, so large-λ modes fit quickly and small-λ modes fit slowly. Note that our discrete GD recursion is ft+1 = ft + ρK (r −

ft). The gradient flow is written in the rescaled time that absorbs ρ, and thus the canonical horizon parameter is the diffusion time t ≃ ρT .Recall the discrete T -step GD filter (main text) 

gT (λ) = 1 − (1 − ρλ )T , 0 < ρ < 1/λ max (K).

Lemma G.2 (Discrete GD ≈ gradient flow) . Fix λ ≥ 0 and 

30 A Decision-Theoretic View of Test-Time Training 

assume ρλ < 1. Let t := ρT . Then 

0 ≤ gT (λ)−gt(λ) = e−tλ −(1 −ρλ )T ≤ T (ρλ )2

2(1 − ρλ ) e−tλ .

Proof. Let x = ρλ ∈ [0 , 1) . Since 1 − x ≤ e−x, (1 −

x)T ≤ e−xT , giving gT ≥ gt. Also log(1 − x) = −x −P 

> k≥2

xk/k ≥ − x − x2/(2(1 − x)) , hence (1 − x)T ≥

e−xT exp {− T x 2/(2(1 − x)) }. Rearranging and using 1 −

e−y ≤ y concludes the proof. 

G.2. Bayes gap for a fixed prompt: oracle diffusion time and mixture 

We evaluate horizons under the aligned GP benchmark (used throughout the main text for filter-match analysis). 

Assumption G.3 (Aligned GP benchmark) . Conditioned on 

K, f⋆ ∼ N (0 , τ 2K) and r = f⋆ + ε with ε ∼ N (0 , σ 2In)

independent. Define λ⋆ := σ2/τ 2 and g⋆(λ) := λ/ (λ +

λ⋆).

Proposition G.4 (Mode-wise risk and mismatch identity (recall)) . Under Assump. G.3 and conditioned on K =

U diag( λi)U ⊤, for any measurable g with g(0) = 0 , the spectral estimator ˆfg = g(K)r satisfies 

E

h

∥ ˆfg − f⋆∥22 | K

i

=

> n

X

> i=1

h

τ 2λi(1 − g(λi)) 2 + σ2g(λi)2i

.

Moreover, g⋆(λ) = λ/ (λ + λ⋆) uniquely minimizes the bracketed term, and the excess Bayes risk is 

E

h

∥ ˆfg − f⋆∥22 | K

i

− E∥g⋆(K)r − f⋆∥22 | K

=

> n

X

> i=1

ai

 g(λi) − g⋆(λi)2,

where ai := τ 2λi + σ2.

Specializing to gradient flow ˆf (t) = gt(K)r with gt(λ) = 1 − e−tλ gives a one-parameter family of risks. Define the oracle time 

t⋆(K) ∈ arg min  

> t≥0

E∥gt(K)r − f⋆∥22 | K .

G.2.1. M ODE -WISE IDEAL TIME 

For a single eigenvalue λ > 0, matching gt(λ) = g⋆(λ)

yields an explicit ideal time. 

Lemma G.5 (Mode-wise Bayes-matching time) . For λ > 0,the unique t satisfying gt(λ) = g⋆(λ) is 

t⋆λ = 1

λ log 



1 + λλ⋆



. (40) 

Proof. gt(λ) = 1 − e−tλ equals g⋆(λ) = λ/ (λ + λ⋆) iff 

e−tλ = λ⋆/(λ + λ⋆), i.e. (40). 

Why a single time is a compromise. The Bayes-matching time t⋆λ depends on λ, so unless the spectrum is nearly de-generate, there is no single t that simultaneously matches 

gt(λi) = g⋆(λi) for all modes. Consequently, any fixed horizon must trade off over-fitting fast modes (large λ)against under-fitting slow modes (small λ). This motivates selecting a representative eigenvalue (e.g. μ(K)) or using a time-mixture. A simple near-oracle proxy matches at a representative eigenvalue. Define the weighted mean 

μ(K) := 

Pni=1 aiλi

Pni=1 ai

, ai = τ 2λi + σ2. (41) Set the match-at-μ time 

tμ := 1

μ(K) log 



1 + μ(K)

λ⋆



⇒ gtμ (μ(K)) = g⋆(μ(K)) .

(42) 

Proposition G.6 (Bayes as an exponential mixture of diffu-sion times) . For λ⋆ > 0 and all λ ≥ 0,

g⋆(λ) = 

Z ∞

> 0

λ⋆e−λ⋆s  1 − e−sλ  ds. 

The same identity holds at the matrix level g⋆(K) =R ∞ 

> 0

λ⋆e−λ⋆s(I − e−sK )ds .Proof. Compute the scalar integral: R ∞ 

> 0

λ⋆e−λ⋆sds = 1 

and R ∞ 

> 0

λ⋆e−(λ⋆+λ)sds = λ⋆/(λ⋆ + λ). Subtract to obtain 

g⋆(λ). Apply spectral calculus for the matrix form. 

Interpretation (random stopping time). Let S ∼

Exp( λ⋆) be an exponential random time. Then, Prop. G.6 is equivalent to g⋆(K)r = E[( I − e−SK )r]. In words, the Bayes shrinkage equals the expected gradient-flow predictor evaluated at a random diffusion time. This highlights why broad spectra are problematic for a single deterministic t

and Bayes effectively averages over a range of times. G.2.2. A CONTINUOUS -TIME SPECTRAL CONCENTRATION BOUND 

Define the mismatch dt(λ) := gt(λ) − g⋆(λ).

Proposition G.7 (Spectral concentration of mismatch (gradient flow)) . For any t ≥ 0, Pni=1 ai dt(λi)2 ≤

2

 Pni=1 ai



dt(μ)2 +2 



t+ 1

> λ⋆

2 Pni=1 ai(λi −μ)2, where 

ai = τ 2λi + σ2 and μ =   

> P
> iaiλi
> P
> iai

.Proof. g′

> t

(λ) = te −tλ ≤ t so gt is t-Lipschitz. Also 

g′

> ⋆

(λ) = λ⋆ 

> (λ+λ⋆)2

≤ 1/λ ⋆. Hence dt is (t+1 /λ ⋆)-Lipschitz. For each i, |dt(λi)| ≤ | dt(μ)| + ( t + 1 /λ ⋆)|λi − μ|. Square and sum with weights ai to obtain the inequality. 31 A Decision-Theoretic View of Test-Time Training 

Interpretation (horizon instability). The risk identity decomposes the error into two meaningful components: 

dt(μ)2 represents the mismatch at a representative eigen-value, while P 

> i

ai(λi − μ)2 penalizes spectral spread via weighted variance. Notably, the Lipschitz factor (t + 1 /λ ⋆)

grows linearly with t, suggesting that longer diffusion times heighten the procedure’s sensitivity to spectral heterogeneity. This scaling relationship provides a principled explanation for horizon instability, particularly why performance de-grades when the prompt geometry K possesses a broad spectrum. 

Summary: Why T is prompt-dependent (diffusion intuition) 

• Instability comes from spectral heterogeneity: 

wide spectra make a single t overfit fast modes and underfit slow ones, so performance becomes sensitive to T .• Bayes behaves like mixing times: the Bayes shrink-age equals a mixture over stopping times. 

G.3. RMT: deterministic oracle curves from asymptotic spectra 

Define the per-token risk under gradient flow 

Rin ,n (t | K) := 1

n

> n

X

> i=1

h

τ 2λie−2tλ i + σ2(1 − e−tλ i )2i

.

Spectral Concentration and Horizon Selection. Al-though the prompt kernel K is random, in high dimen-sions, its spectrum is highly predictable. Specifically, av-erages over eigenvalues 1

> n

Pni=1 h(λi(K)) fluctuate little and converge to integrals under a limiting law (e.g., the Marchenko–Pastur distribution). Since our normalized risk can be expressed as such an eigenvalue-average for each t

(i.e., Rin ,n (t | K) = 1 

> n

tr ψt(K) for an appropriate function 

ψt), the entire risk curve t 7 → R in ,n (t | K) and its mini-mizer admit deterministic approximations. This provides a rigorous foundation for using simple spectral moments, such as μ(K), as stable and robust horizon predictors. 

Assumption G.8 (Wishart kernel model) . Let Φ = 1√d X ∈

Rn×d with i.i.d. mean-zero, variance-one subgaussian en-tries, and n, d → ∞ with n/d → γ ∈ (0 , ∞). Set 

K = ΦΦ ⊤ = 1 

> d

XX ⊤.

Proposition G.9 (Asymptotic risk under a Marchenko–Pas-tur geometry) . Let Φ ∈ Rn×d have i.i.d. entries Φij ∼N (0 , 1/d ) and set K := ΦΦ ⊤. Assume n, d → ∞

with n/d → γ ∈ (0 , ∞). Under the aligned GP bench-mark (Assump. G.3), consider the gradient-flow estimator 

ˆf (t) = ( I − e−tK )r. Then, almost surely, 

1

n E

h

∥ ˆf (t) − f⋆∥22 K

i

−→ 

Z

ψt(λ) μγ (dλ ),

where ψt(λ) := τ 2λe −2tλ + σ2(1 − e−tλ )2 and μγ is the Marchenko–Pastur law with aspect ratio γ, i.e. μγ (dλ ) = 



1 − 1

> γ



> +

δ0(dλ ) + 

√(λ+−λ)( λ−λ−)2πγλ 1[λ−,λ +](λ) dλ, with 

λ± = (1 ± √γ)2.

Proof. By Prop. G.4 with g = gt(λ) = 1 − e−tλ , condition-ing on K = U diag( λi)U ⊤ gives 

1

n E

h

∥ ˆf (t) − f⋆∥22 K

i

= 1

n

> n

X

> i=1

ψt(λi),

where ψt(λ) = τ 2λe −2tλ + σ2(1 − e−tλ )2. Let μn := 

> 1
> n

Pni=1 δλi be the empirical spectral measure of K. For 

K = ΦΦ ⊤ = (1 /d )XX ⊤ with Xij ∼ N (0 , 1) , the Marchenko-Pastur theorem yields μn ⇒ μγ almost surely as n, d → ∞ with n/d → γ (Mar ˇcenko & Pastur, 1967; Bai & Silverstein, 2010). Since ψt is bounded and continuous on 

[0 , ∞), we have R ψt dμ n → R ψt dμ γ almost surely. G.3.1. M OMENT PROXY : DETERMINISTIC 

“REPRESENTATIVE EIGENVALUE ”The weighted mean eigenvalue μ(K) in (41) admits a de-terministic limit under MP moments. Since m1(γ) := R λ ν γ (dλ ) = 1 and m2(γ) := R λ2 νγ (dλ ) = 1 + γ,

Proposition G.10 (Deterministic equivalent of μ(K)). Un-der Assump. G.8, 

μ(K) a.s.

−−→ μ∞ := τ 2m2(γ) + σ2m1(γ)

τ 2m1(γ) + σ2 = (1 + γ) + λ⋆

1 + λ⋆

.

(43) 

Thus the match-at-μ time concentrates around 

tμ∞ = 1

μ∞

log 



1 + μ∞

λ⋆



.

Proof. Write μ(K) = τ 2 1       

> n
> Pλ2
> i+σ2 1
> n
> Pλi
> τ2 1
> n
> Pλi+σ2

and use the Marchenko-Pastur theorem. 

Concentration replacement (what is random). Under Assump. G.8, K (hence {λi}, μ(K), and Rin ,n ) is random. The limits νγ , μ∞, and R∞ are deterministic. Standard concentration for linear spectral statistics implies μ(K) and 

Rin ,n (t | K) concentrate around these limits (proof sketch omitted; see standard subgaussian random matrix concen-tration). 32 A Decision-Theoretic View of Test-Time Training 

G.4. Transformer head example: head choice ⇒

spectrum ⇒ horizon 

We specialize to Value-matrix updates in Transformer head 

h under the global-upstream regime. With attention matrix 

A(h) (entries α(h) 

> ks

), residual stream stack Z ∈ Rn×dz , and upstream direction β(h), the head-induced prompt kernel is 

K(h,V ) ≈ ∥ β(h)∥22 A(h)(ZZ ⊤)A(h)⊤, K = X

> h∈H

K(h,V ).

Thus head selection H changes the eigenvalues {λi(K)}

and therefore changes the near-oracle time (e.g. tμ in (42) ). G.4.1. T WO REGIMES AND A TOY CALCULATION 

(i) Spiky / low-rank head. Suppose A(h)Z is approxi-mately rank-one: A(h)Z ≈ √κ vu ⊤ with ∥v∥2 = 1 . Then 

K(h,V ) ≈ λsp vv ⊤ with spike 

λsp := ∥β(h)∥22 κ. 

Toy horizon. If K ≈ λsp vv ⊤ exactly, matching gt(λsp ) = 

g⋆(λsp ) yields 

tsp = 1

λsp 

log 



1 + λsp 

λ⋆



, Tsp ≈ tsp /ρ. 

Intuition (spike ⇒ step-sensitivity). A dominant spike makes the effective dynamics essentially one-dimensional and very fast. The residual energy along the spike direction decays on the scale t ∼ 1/λ sp . Hence small changes in T

(or ρ) can move the predictor from under- to over-adaptation quickly, producing sharp performance cliffs. 

(ii) Mixing head / MP-like bulk. If A(h)Z behaves like a high-rank random feature matrix, then K(H) can have an MP-like bulk. Let dH denote the effective feature dimension contributed by selected heads, and γH := n/d H. Then the deterministic proxy (43) suggests μ∞(γH) = (1+ γH)+ λ⋆ 

> 1+ λ⋆

,and tμ∞(γH) = 1 

> μ∞(γH)

log 



1 + μ∞(γH)

> λ⋆



.

Intuition. Adding heads expands the effective dimension 

dH, thereby reducing the aspect ratio γH. This adjust-ment reshapes the spectral distribution of K(H) and, conse-quently, shifts the predicted near-oracle horizon, reflecting a higher adaptation capacity. 

Summary: spectrum couples how far and which heads to adapt 

• Spiky heads: large eigenvalues ⇒ faster diffusion ⇒

smaller near-oracle times. • Mixing heads: bulk spectra ⇒ horizons are well-approximated by deterministic equivalents. Which heads to adapt and how far to adapt are spectrally coupled through the prompt geometry. 

## H. Transformer-specific derivations 

H.1. Technical details for Transformer Jacobian features 

H.1.1. O UTER -PRODUCT JACOBIAN FOR LINEAR LAYERS 

Lemma H.1 (Outer-product Jacobian for linear layers (ex-act at w0)) . Let y = W x with W ∈ Rdout ×din be a linear layer in the computation graph, and let si(w) be any scalar output. Evaluate all forward activations and backpropa-gated gradients at w0, and define ai := x w0 ∈ Rdin and 

bi := ∇y si(w) w0 ∈ Rdout . Then, using the column-major convention for vec( ·),

∇vec( W )si(w) w0

= ai ⊗ bi.

Consequently, the kernel contribution induced by a full up-date of W satisfies 

K(W ) 

> ij

=

D

∇vec( W )si, ∇vec( W )sj

E

= ( a⊤ 

> i

aj ) ( b⊤ 

> i

bj ).

Proof. By the chain rule for y = W x , holding x

fixed when differentiating w.r.t. W , ∇W si(w) w0 =

 ∇y si(w) w0

   x w0

⊤ = bia⊤ 

> i

. Under the column-major convention, vec( ba ⊤) = a ⊗ b, hence ∇vec( W )si(w) w0

=vec( bia⊤ 

> i

) = ai ⊗ bi. Finally, ⟨ai ⊗ bi, aj ⊗ bj ⟩ =(a⊤ 

> i

aj )( b⊤ 

> i

bj ).H.1.2. L OCAL LINEARIZATION REGIME : FIXED FORWARD /BACKWARD FEATURES 

Assumption H.2 (Fixed forward/backward features in the local regime) . When forming Jacobian features for selected parameter blocks and the induced geometry K, we eval-uate all forward activations (e.g. residual streams zs and attention weights α(h) 

> ts

) and all upstream gradients (e.g. gi)at w0 and treat them as constants under the local lineariza-tion. Equivalently, we ignore second-order feedback effects whereby changing one block perturbs intermediate activa-tions/gradients used to define Jacobian features of another block. 33 A Decision-Theoretic View of Test-Time Training 

Remark (why attention can be treated as fixed for 

WV /W O features). For standard attention, α(h) 

> ts

depends on (W (h) 

> Q

, W (h) 

> K

) and the input activations, but not directly on W (h) 

> V

or W (h) 

> O

. Thus, when differentiating w.r.t. W (h)

> V

or W (h) 

> O

at w0, holding α(h) fixed is exact at the layer level; Assumption H.2 only rules out indirect, higher-order changes of α(h) through upstream activation shifts when multiple blocks are updated. Similar “frozen-attention” sim-plifications also appear in theoretical analyses of attention in random-feature regimes (Fu et al., 2023). H.1.3. D ERIVATION : VALUE -MATRIX UPDATES WV

Fix a coordinate i that supervises token k = k(i) and let 

gi := ∇o(h)

> k

si(w) w0 be the upstream gradient into o(h) 

> k

at 

w0. Under Assumption H.2, the first-order dependence on 

W (h) 

> V

is 

δs i =

D

gi, W (h)

> O

X

> s≤k

α(h) 

> ks

δW (h) 

> V

zs

E

+ o(∥δW (h) 

> V

∥).

Define β(h) 

> i

:= (W (h) 

> O

)⊤gi ∈ Rdh , and 

m(h) 

> i

:= P 

> s≤k(i)

α(h)

> k(i)s

zs ∈ Rdmodel . Then 

δs i = ⟨β(h) 

> i

, δW (h) 

> V

m(h) 

> i

⟩ + o(∥δW (h) 

> V

∥), so by Lem. H.1 applied at w0, ∇vec( W (h) 

> V)

si(w) w0

=

m(h) 

> i

⊗ β(h) 

> i

, K(h,V ) 

> ij

= ( m(h)⊤ 

> i

m(h) 

> j

) ( β(h)⊤ 

> i

β(h) 

> j

).

Matrix form and PSD. Under tokenwise supervision ( i ≡

k), stacking m(h) 

> k

and β(h) 

> k

gives the Hadamard form (3) .Since M (h)M (h)⊤ ⪰ 0 and B(h)B(h)⊤ ⪰ 0, the Schur product theorem implies K(h,V ) ⪰ 0. The global-upstream specialization yields the attention-sandwiched Gram (4). H.1.4. D ERIVATION : OUTPUT -MATRIX UPDATES WO

If we update W (h) 

> O

only, then (at w0) δs i =

⟨gi, δW (h) 

> O

u(h)

> k(i)

⟩ + o(∥δW (h) 

> O

∥), so Lem. H.1 yields ∇vec( W (h) 

> O)

si(w) w0

= u(h) 

> k(i)

⊗ gi, K (h,O ) 

> ij

=

 u(h)⊤ 

> k(i)

u(h)

> k(j)

   g⊤ 

> i

gj

. Since u(h) = A(h)V (h) with 

V (h) := ZW (h)⊤ 

> V

, this again exhibits attention-induced structure. H.1.5. D ERIVATION : MLP BLOCK UPDATES 

For an MLP block hk = σ(W1zk) and mlp k = W2hk, up-dating W2 yields Jacobian features hk ⊗gk (Gram of hidden activations). Updating W1 yields features zk ⊗ σ′(W1zk)⊙

(W ⊤ 

> 2

gk), again an outer product (input × backpropagated gate). H.1.6. A DDITIVITY ACROSS UPDATED BLOCKS 

If B is a set of disjoint parameter blocks that are updated, stacking Jacobians blockwise yields Φ = [Φ (b)]b∈B and hence K = ΦΦ ⊤ = P 

> b∈B

K(b).

H.2. A simple stability bound for the step size 

In the global-upstream regime (4) for a head h,

K(h,V ) = ∥β(h)∥22 M (h)M (h)⊤, M (h) = A(h)Z. 

Therefore, λmax (K(h,V )) = ∥β(h)∥22 ∥M (h)∥2op ≤∥β(h)∥22 ∥A (h)∥2op ∥Z∥2op . Since K = P 

> b∈B

K(b) is PSD, 

λmax (K) ≤ P 

> b∈B

λmax (K(b)), so enlarging the updated block set tightens the admissible step-size range 0 < ρ < 

1/λ max (K).

H.3. Representable prompt subspace for value updates Lemma H.3 (Representable prompt subspace for WV up-dates (global-β case)) . Assume tokenwise supervision and a global upstream direction gk ≡ g, so that β(h) 

> k

≡ β(h) with 

∥β(h)∥ > 0 for each head h. For a head h with WV updates, 

K(h,V ) = ∥β(h)∥22M (h)M (h)⊤ where M (h) = A(h)Z.Hence range( K(h,V )) = range( M (h)) = range( A(h)Z).If we update a head set H (and sum their kernel compo-nents), then 

range X

> h∈H

K(h,V )

!

= X

> h∈H

range( M (h))= range 



[ M (h) ]h∈H 



,

where [ M (h) ]h∈H denotes column-wise concatenation. Proof. In the global-β case, K(h,V ) =

∥β(h)∥22M (h)M (h)⊤, hence range( K(h,V )) =range( M (h)M (h)⊤) = range( M (h)). For the sum, note that for symmetric PSD matrices {Sℓ},

null( P 

> ℓ

Sℓ) = ∩ℓnull( Sℓ) because x⊤(P 

> ℓ

Sℓ)x = 0

implies x⊤Sℓx = 0 for every ℓ; by PSD-ness this gives Sℓx = 0. Taking orthogonal complements yields range( P 

> ℓ

Sℓ) = P 

> ℓ

range( Sℓ). Finally, P 

> h∈H

range( M (h)) = range([ M (h)]h∈H ) is stan-dard. 

H.4. Computational cost, memory, and scalable approximations 

This appendix complements Sections 7 and 8 by (i) making the computational overhead explicit (time, memory, and additional forward/backward passes), and (ii) describing practical approximations for large Jacobians. We emphasize that the expressions (JwΣJ⊤ 

> w

+ σ2I)−1 and I can look expensive, but the core linear algebra lives in prompt space 

34 A Decision-Theoretic View of Test-Time Training 

(dimension n) and can be implemented via Jacobian–vector products (JVP/VJP), without materializing Jw ∈ Rn×p.H.4.1. B OOKKEEPING AND PRIMITIVE COSTS 

We use the following dimensions (consistent with the main text): • p: number of model parameters. • n: number of prompt positions / residual terms (Sec. 3). • d: dimension of the update subspace U = range( A)

(Assumption 3.1). • Ball : partition of parameters into blocks (e.g., by lay-er/head/matrix type) used in Sec. 8. To keep the discussion model-agnostic, we express runtime in terms of primitive passes: • Cfwd : cost of one forward pass on the prompt objective (producing the supervision outputs sw(Pn) ∈ Rn and the scalar loss). • Cbwd (S): cost of one backward pass producing gradi-ents restricted to a parameter set S (e.g., the updated parameters, or a single block b). In practice, Cbwd (S)

is typically of the same order as a full backward pass unless specialized kernels or partial-backprop are used. Throughout, the memory footprint at test time is dominated by storing activations needed for a backward pass. Impor-tantly, memory does not scale with the number of adaptation steps T when updates are performed sequentially (activa-tions are freed after each step), but time does scale with 

T .H.4.2. B ASELINE : NO -UPDATE INFERENCE VS . 

> STANDARD

TTT Let T be the number of test-time gradient steps on the prompt loss. A coarse but useful accounting is: • No-update inference (ICL / standard inference): 

one forward pass on the prompt+query, and no back-ward pass. • Standard TTT (few-step adaptation): each step re-quires (at least) one forward pass to form the prompt loss and one backward pass to compute gradients, fol-lowed by a final forward pass for the query prediction. Thus, TTT runtime ≈ T (Cfwd + Cbwd (U)) + Cfwd ,query .

This matches common lightweight TTT baselines (e.g., up-dating only a small parameter subset / LoRA state) where the dominant cost is the backward pass, not the parameter update itself. H.4.3. C ONSTRUCTING THE PROMPT GEOMETRY K 

> WITHOUT FORMING

Jw

Many quantities in Sections 4–7 depend on K = ΦΦ ⊤ =

JwΠU J⊤ 

> w

∈ Rn×n. Crucially, K is an n × n matrix: when 

n is modest (few-shot prompts or a small set of tokens), forming and factorizing K is cheap, and the real bottleneck is accessing Jacobian information. 

Explicit feature construction (Φ). Recall Φ ∈

Rn×d stacks the Jacobian features ϕ(xi; P (−i) 

> n

)⊤ =

∇θ si(w(θ)) |θ=0 . If one explicitly builds Φ, the simplest approach is n reverse-mode calls (one per si) restricted to the updated parameters: cost to build Φ ≈ n · Cbwd (U),

memory ≈ one backward-pass footprint .

Once Φ is available, K = ΦΦ ⊤ costs O(n2d) flops and stores O(n2) numbers. 

Implicit matrix–vector products (recommended for large 

p and/or larger n). Many downstream procedures do not 

require Φ itself, only products of the form Kv or (K +

λI )−1v. These can be computed without forming Jw:

Kv = JwΠU J⊤ 

> w

v. (44) A single Kv can be computed with one VJP and one JVP (or two reverse-mode passes via Pearlmutter’s trick): 1. compute g := J⊤ 

> w

v (one VJP / backward pass), 2. project gU := Π U g (cheap: masking or low-rank projection), 3. compute Kv := JwgU (one JVP / forward-mode, or equivalent). Thus, cost of one Kv ≈ Cbwd (params ) + Cjvp (params ),

where Cjvp depends on the autodiff implementation. This is the standard way to scale kernel methods and Gauss– Newton/Fisher computations in large models. 

Solves with K for larger n. When n is small, one can form K explicitly and use a Cholesky factorization at O(n3)

time. When n is larger, one can solve linear systems with conjugate gradients (CG) using the matvec in (44) . Each CG iteration costs one Kv (plus O(n) vector ops), and the number of iterations depends on the conditioning of K + λI .35 A Decision-Theoretic View of Test-Time Training 

H.4.4. C OST OF PAC-B AYES HORIZON SELECTION 

(S EC . 7) Horizon selection evaluates (for T ∈ T ) the evidence score 

ℓT (r) = 12n

log det(Σ T ) + r⊤Σ−1 

> T

r ,

where ΣT = σ2(I − ρK )−T . Given an eigendecomposition 

K = U diag( λi)U ⊤, we have 

log det(Σ T ) = n log σ2 −

> n

X

> i=1

T log(1 − ρλ i),r⊤Σ−1 

> T

r = 1

σ2

> n

X

> i=1

(1 − ρλ i)T (u⊤ 

> i

r)2,

so evaluating ℓT for all T ∈ T costs O(|T | n) once 

{λi, u ⊤ 

> i

r} are known. Therefore, the total overhead of evidence-based selection is: cost to obtain K| {z }

> dominant (Jacobian access)

+ O(n3)

| {z }   

> eig/Cholesky (cheap if nis small)

+ O(|T | n)

| {z }

> negligible

.

In typical few-shot regimes (small n), the extra prompt-space linear algebra is negligible relative to even a single backward pass. 

Net compute can decrease. A practical point is that hori-zon selection can reduce total test-time compute by choos-ing small T (or T = 0 ) when adaptation is predicted to be unhelpful, potentially offsetting the cost of estimating K.H.4.5. C OST OF BAYES -OPTIMAL SUBSPACE DESIGN 

(S EC . 8) Thm. 8.2 involves the information-capture matrix 

I = Σ J⊤ 

> w

(JwΣJ⊤ 

> w

+ σ2In)−1JwΣ ∈ Rp×p,

whose full formation is infeasible for modern p. In practice, we advocate two implementation regimes. 

(A) Offline global design. In many deployments, the up-date mechanism (which blocks/heads are trainable at test time) is fixed ahead of time . One can estimate a global 

design from a development set of prompts (Cor. E.23) and then keep the selected subspace fixed at test time. In this regime, there is no additional test-time cost beyond the chosen TTT updates. 

(B) Prompt-adaptive scoring via prompt-space formulas. 

When restricting to block on/off selection and (optionally) an isotropic correction prior Σ = Ip, Sec. 8.2 shows that the block score admits a purely prompt-space form: 

sb := tr( Ibb ) = tr 



K(b)full (Kfull + σ2In)−1

, (45) where Kfull = P 

> b

K(b)full and K(b)full = J(b)J(b)⊤. Here the only matrix inverse is (Kfull + σ2In)−1 in Rn×n. Thus, the 

linear algebra is again in prompt space. The remaining challenge is to obtain K(b)full (or its action on vectors) without forming J(b). This can be done with implicit Jacobian products restricted to block b:

K(b)full v = J(b)J(b)⊤v

via one VJP and one JVP restricted to block b.

Hutchinson estimator for large block sets. If scoring every block exactly is expensive, one can approximate (45) with R Hutchinson probes zr ∼ {± 1}n:

sb = Ez

z⊤K(b)full (Kfull + σ2I)−1z ≈ 1

R

> R

X

> r=1

z⊤ 

> r

K(b)full ur ,

where ur := ( Kfull + σ2I)−1zr . When n is small, each ur

can be computed by a direct n × n solve; when n is large, CG with the matvec Kfull v applies. The expensive part per probe is applying K(b)full to ur , which can be done implicitly without storing gradients. 

Top-eigenspace computation without forming I. If one truly requires the top-d eigenspace of I (e.g., within a block for low-rank adaptation), one can use randomized SVD/Lanczos with only I–vector products. A matvec Iv

can be implemented as: 

Iv = Σ J⊤ 

> w

(JwΣJ⊤ 

> w

+ σ2In)−1

| {z }   

> n×nsolve

JwΣv, 

which requires (i) one JVP to compute JwΣv, (ii) one prompt-space solve, and (iii) one VJP to apply J⊤ 

> w

, plus multiplications by Σ (cheap if Σ is diagonal/structured). This yields a scalable route to approximate eigenspaces without explicit p × p matrices. H.4.6. F ORWARD /BACKWARD PASS ACCOUNTING 

(SUMMARY )Table 2 summarizes the dominant pass counts at test time. Here nJ denotes the number of additional Jacobian evalua-tions needed to build or approximate K (typically nJ = n

for explicit Φ, and nJ = O(R) for randomized trace/eigen-solvers). H.4.7. P RACTICAL GUIDANCE AND REGIMES OF TRACTABILITY 

When is the matrix inverse cheap? The inverse that ap-pears in our design objectives, (JwΣJ⊤ 

> w

+ σ2In)−1, is an 

n × n matrix. Thus it is cheap whenever n is modest. If n

is large (e.g., token-level supervision on long prompts), one should not form this inverse explicitly; instead use CG/Lanc-zos with implicit matvecs. 36 A Decision-Theoretic View of Test-Time Training                           

> Method # forward passes # backward/Jacobian passes Remarks No-update inference (ICL) 10baseline inference only Standard TTT ( Tsteps) T+ 1 Tone prompt step = fwd+bwd TTT + horizon selection (ours) T+ 1 T+nJnJto estimate K/ spectrum TTT + offline subspace design (ours) T+ 1 Tno extra test-time overhead
> Table 2. Test-time pass counts (coarse). Horizon selection adds Jacobian access to estimate K, while offline subspace design adds no test-time cost.

Where is the true bottleneck? The dominant cost is ac-cessing Jacobian information, not the n × n linear algebra. This is why we emphasize (i) restricting test-time updates to a small parameter subset, and (ii) using implicit Jacobian products instead of explicit Jacobian materialization. 

Amortization via offline design. The subspace-design step can be performed offline once (global design) and reused at test time, eliminating any additional inference-time overhead while still benefiting from a principled update mechanism. 

Compute–robustness tradeoff. Our framework explic-itly trades compute for robustness: step selection can avoid harmful over-adaptation (and may reduce T ), while sub-space design can restrict updates to identifiable, redundancy-aware directions (reducing the effective dimension of adap-tation). Both tradeoffs are consistent with the empirical observation that “more test-time optimization” can be worse when the prompt signal is weak or misspecified. 

## I. Experimental Details 

I.1. Task and Data Generation 

We evaluate our framework using distilgpt2 on a syn-thetic integer regression task. We use the pretrained GPT-2 family model and, in particular, the distilgpt2 checkpoint released via the Hugging Face Transformers library (Radford et al., 2019; Wolf et al., 2020; Sanh et al., 2019). 

Task Distribution. Each task is defined by a hidden shift 

s ∈ { 0, . . . , 9}. Data pairs (x, y ) are generated such that the clean label is y = ( x + s) (mod 10) . At test time, we sample n = 10 input digits and one query digit from 

0, . . . , 9.

Noise and Templates. We introduce label noise by replac-ing the target y with a uniformly random digit sampled from 

{0, . . . , 9} with probability pnoise . We simulate two regimes: a clean regime where pnoise = 0 .0, and a noisy regime where 

pnoise = 0 .55 . These values create a bimodal distribution over prompt quality, allowing us to test adaptive T selection under varying signal-to-noise ratios. The model sees the data formatted via two templates, se-lected randomly per task: "x->y" (Template 0) or "x:y" 

(Template 1). 

I.2. Model and TTT Optimization Architecture. We use the pretrained distilgpt2 model (6 layers, 12 heads, 768 embedding dim). 

Update Subspace. We restrict optimization to the atten-tion Value projection weights. For a selected block (l, h )

(layer l, head h), we construct a binary mask Ml,h targeting the indices corresponding to the Value matrix in the fused 

c attn linear layer (indices 2 dmodel to 3 dmodel ). This corresponds to the W (h) 

> V

parameters in the notation of Sec-tion 3.3. 

Optimization Objective. We map the digit label y ∈{0, . . . , 9} to ˜y := y/ 9 ∈ [0 , 1] and predict byw(θ)(x; Pn) := 

> 19

P9 

> j=0

j p d, where pd are the softmax probabilities of the digit tokens. For each prompt position i, we use leave-one-out supervision and define the residual ri(θ) := ˜yi − byw(θ)(xi; P (−i) 

> n

). The prompt loss is L(θ) =

> 12σ2

Pni=1 ri(θ)2. In the few-step regime, linearizing byw(θ)

at θ = 0 yields r(θ) ≈ r − Φθ, recovering the quadratic surrogate L(θ) = 12σ2 ∥r − Φθ∥22 as in Section 3.2. Updates are performed via the stochastic gradient descent on the leave-one-out prompt residuals for T steps with step size η = ρσ 2 (equivalently, scaled step size ρ). 

I.3. Evidence-Based T Selection 

We select the horizon T by minimizing the negative log-evidence of the prompt residuals r. We assume the residuals follow a zero-mean Gaussian induced by the TTT filter: 

r ∼ N (0 , ΣT ).Using the eigen-decomposition of the kernel K = U ΛU ⊤

with eigenvalues λi, the covariance is ΣT = σ2(I −ρK )−T .We implement two selection criteria: 

1. Fixed-σ Evidence. We estimate a constant noise variance 

ˆσ2 = 1 

> n

∥r∥2 from the unadapted residuals. We then select 37 A Decision-Theoretic View of Test-Time Training 

T to minimize: 

L(T ) = − Tn

> n

X

> i=1

log(1 − ρλ i) + 1

nˆσ2

> n

X

> i=1

(1 − ρλ i)T (˜ ri)2,

where ˜r = U ⊤r are the rotated residuals. This corresponds to the fixed-σ2 variant discussed in Section 7. 

2. MLE-σ Evidence. We treat σ2 as a free parame-ter to be profiled out (Murphy & van der Vaart, 2000). We minimize the profile log-likelihood: LMLE (T ) =log   1

> n

Pni=1 (1 − ρλ i)T (˜ ri)2 − Tn

Pni=1 log(1 − ρλ i).Both methods select TMAP ∈ arg min T ∈T L(T ) where 

T = {0, 1, 2, . . . , 30 }.

I.4. Subspace Selection Algorithms 

In Experiment 2, we select a subset of heads from the last 4 layers (48 candidate blocks). We vary kbudget ∈{1, 2, 4, 8, 16 , 32 } to assess scaling behavior. 

Trace-TopK. We compute the trace score for each block b

as Tr (K(b)(Ksum + σ2I)−1), where K(b) is the kernel for block b and Ksum is the sum of all candidate kernels. 

Query-Aware. Consistent with Theorem 8.2, which iden-tifies the subspace maximizing posterior variance reduc-tion, we prioritize update directions with significant query-aligned gradients. In practice, this corresponds to iteratively selecting the block b that maximizes the squared magnitude of the predicted correction h2, where h = k⊤ 

> x

QT (Kcurr +

K(b))r. Here, kx is the prompt-query coupling vector, Kcurr 

is the current kernel sum, and QT (λ) = 1−(1 −ρλ )T 

> λ

is the TTT filter response. 

Trace-BottomK. We use the same trace score as T RACE -TOP K, but select the kbudget blocks with the smallest scores 

sb. We include T RACE -B OTTOM K as a negative-control baseline for query-agnostic selection. 

Random (Uniform). As a baseline, we sample kbudget 

blocks uniformly at random without replacement from the candidate set (e.g., the 48 (layer, head) blocks), and update only the selected blocks. 

I.5. Hyperparameters 

We selected the prior scale c via a pilot study on 20 tasks distinct from the test set, sweeping c ∈ { 0.05 , 0.1, 0.2} with fixed T = 8 . For the main experiments, we used a step size scale ρ ≈ c/λ max to ensure stability ( ρ < 1/λ max (K) as required by the monotone-filter regime in Section 4). 

Evaluation Protocol. We evaluate all methods on 2,000 independent test tasks for Experiment 1, and 500 tasks for Experiment 2 (due to the additional computational cost of multiple block configurations). 

## J. Supplementary Real-Data Experiment on SST-2 

We include a small real-data experiment on SST-2 to check whether the two design principles from the main text also appear outside the synthetic digit-shift task. This experiment is intended as supplementary mechanism-level evidence, not as a definitive benchmark. 

Dataset and task construction. SST-2 is a binary sentence-level sentiment classification dataset (Socher et al., 2013). We sample tasks from the validation split. Each task consists of 10 labeled prompt sentences and one held-out query sentence, sampled without replacement within the task. The query label is used only for evaluation. We encode the negative and positive labels as 0 and 1, respec-tively, and use a fixed text-label template: each prompt example is written as Text: {sentence } Label: 

{0/1 }, followed by the query prefix Text: {query }

Label: . To keep the setting close to the squared-loss theory, we cast the binary classification problem as scalar regression on labels in {0, 1} and evaluate query mean-squared error. 

Prediction model. We use distilgpt2 as a causal lan-guage model. The prediction at the query is computed from the next-token distribution restricted to the two single-token label strings for 0 and 1: if p0, p 1 are the restricted softmax probabilities, the scalar prediction is ˆy = p1 (equiv-alently, the expected normalized label). For computational consistency across tasks, input texts are truncated before constructing the prompt, and all model weights are reset to the pretrained checkpoint before each TTT run. 

TTT protocol. We use the same few-step TTT protocol as in the main experiments. Prompt residuals are computed in a leave-one-out manner to avoid label leakage: when forming the residual for a prompt example, its own label is not included in the prompt used by the base predictor. The prompt loss is the squared error between these residuals and the linearized prediction shifts. Adaptation updates only attention Value weights, selected at the attention-head level; for GPT-2’s fused c attn parameter, this corresponds to the Value slice from 2dmodel to 3dmodel . Gradients outside the selected Value-head slices are masked to zero. We use 50 sampled tasks in this supplementary run. 

Kernel and step-size construction. For each task, we compute the prompt kernel and prompt–query coupling from Jacobian features with respect to the candidate Value-head parameters. The effective noise level is estimated by the plug-in value ˆσ2 = ∥r∥22/n from the base leave-one-out residuals. The scaled step size is chosen by the same stable normalization as in the synthetic experiment, 38 A Decision-Theoretic View of Test-Time Training 

ρ = c/λ max (K) with c = 0 .1 and clipped so that ρ < 

1/λ max (K).

Horizon selection. For the update horizon experiment, we compare a fixed-horizon baseline with T = 8 against evidence-based per-prompt selection. The evidence meth-ods search over the same grid as in the synthetic experiment, 

T ∈ { 0, 1, . . . , 30 }, using either a fixed plug-in estimate of σ or the profiled MLE-σ variant. The evidence score is evaluated from the prompt-kernel eigenspectrum and the rotated residuals, so the candidate horizons are not evaluated by rerunning TTT from scratch. 

Subspace selection. For the block/head selection exper-iment, we consider 48 candidate attention heads from the last four layers of distilgpt2 . We evaluate budgets 

kbudget ∈ { 1, 2, 4, 8, 16 , 32 } and compare Q UERY -A WARE ,TRACE -T OP K, T RACE -B OTTOM K, and Random selection. QUERY -A WARE uses the prompt–query coupling to greedily select heads under the fixed horizon T = 8 ; T RACE -T OP Kranks heads by the query-agnostic trace score; T RACE -BOTTOM K is the corresponding negative-control baseline. The Random baseline is averaged over five independent random head selections for each task and budget. As in the main text, subspace results are reported as query-MSE improvement relative to Random, so positive values indicate lower query error than Random. 

Method Query MSE Fixed-T TTT 0.328 Evidence, fixed-σ 0.325 Evidence, MLE-σ 0.324 

Table 3. SST-2 horizon selection. Evidence-based horizon se-lection slightly improves over the fixed-T baseline in this small real-data study. Budget QUERY -A WARE TRACE -T OP K TRACE -B OTTOM K1 +0 .0010 +0 .0005 −0.0001 

2 +0 .0019 +0 .0011 −0.0004 

4 +0 .0031 +0 .0017 −0.0007 

8 +0 .0039 +0 .0031 −0.0022 

16 +0 .0059 +0 .0060 −0.0030 

32 +0 .0029 +0 .0029 −0.0047 

Table 4. SST-2 subspace selection. Mean query-MSE improve-ment relative to Random selection; positive values are better. QUERY -A WARE improves over Random across budgets and is stronger than T RACE -T OP K for small-to-moderate budgets, while TRACE -B OTTOM K consistently hurts. 

Overall, the results show the same qualitative pattern as the synthetic experiment: evidence-based selection gives a modest improvement over a fixed horizon, and query-aware update directions tend to improve query prediction more reliably than query-agnostic negative controls. Because this study uses only 50 tasks and a small distilled language model, we treat it as preliminary support for the proposed mechanisms rather than evidence of broad practical superi-ority. 39