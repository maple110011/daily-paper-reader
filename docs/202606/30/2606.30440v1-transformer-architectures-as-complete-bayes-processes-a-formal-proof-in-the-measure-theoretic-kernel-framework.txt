Title: Transformer Architectures as Complete Bayes Processes: A Formal Proof in the Measure-Theoretic Kernel Framework

URL Source: https://arxiv.org/pdf/2606.30440v1

Published Time: Tue, 30 Jun 2026 02:45:01 GMT

Number of Pages: 11

Markdown Content:
# Transformer Architectures as Complete Bayes Processes: A Formal Proof in the Measure-Theoretic Kernel Framework 

## Haobo Yang Department of Computer Science and Engineering, SUSTech University 

## yhbcode000@foxmail.com *

## June 30, 2026 

Abstract 

We present a complete formal proof that transformer architectures, when their internal up-date mechanisms satisfy a Bayes joint-distribution condition, implement exact Bayesian poste-rior inference. Working within the measure-theoretic kernel framework, we define a hierarchy of abstractions—from the core Bayesian transformer, through semantic transformers with ex-plicit update kernels, to full transformer blocks with QKV/attention/residual/MLP pipelines, and finally multilayer stacks—and prove at each level that the Bayes joint semantics implies the update kernel equals the posterior almost everywhere. For the block-level architecture, we derive the explicit Bayes formula through Radon-Nikodym differentiation and prove its normal-ization. We additionally prove that the softmax attention mechanism induces a valid probability distribution over keys, establishing the bridge between the abstract kernel framework and con-crete attention implementations. The framework makes no architectural assumptions beyond the Markov kernel structure and exposes explicit conditions under which a transformer block is provably Bayesian. In essence, when this joint distribution condition is satisfied, the forward computation of a Transformer is formally equivalent to a rigorous Bayesian posterior update. 

Area of research: Probabilistic Machine Learning, Formal Methods, Bayesian Inference, Trans-former Theory. 

# 1 Introduction 

The transformer architecture 1 has become the dominant paradigm in modern machine learning, powering large language models, vision models, and multimodal systems. A growing body of 

> *

This preprint has not undergone professional peer review. If you find any errors or have concerns, please contact the corresponding author. 

1

> arXiv:2606.30440v1 [cs.LG] 29 Jun 2026

theoretical work has explored connections between attention mechanisms and probabilistic infer-ence 2;3;4 , suggesting that transformers may implement forms of Bayesian updating or posterior inference. However, these claims have largely remained at the level of intuition, analogy, or empir-ical observation, without formal rigorous proof. We close this gap by constructing a complete formal proof that transformer architectures im-plement exact Bayesian posterior inference, under a clearly stated joint-distribution condition. Our framework has three distinguishing features. First, the proofs are based on first principles and depend only on the kernel composition struc-ture and the Bayes joint semantics condition—no architectural assumptions about depth, width, parameterization, or training procedure are required. Second, we provide a layered hierarchy of abstractions that mirrors the structural complexity of real transformers: (i) the core Bayesian transformer with only prior and likelihood, (ii) the semantic transformer with an explicit update kernel, (iii) the full transformer block with QKV/attention/ residual/MLP data flow, and (iv) the multilayer transformer stack. At each level, we prove that the Bayes joint semantics implies the update is the posterior almost everywhere. Third, we expose the explicit block-level Bayes formula through Radon-Nikodym differentia-tion and prove its normalization, providing a concrete bridge from the abstract kernel composition to practical density-based inference. We additionally prove that softmax attention defines a valid probability distribution, connecting the kernel framework to the standard attention mechanism. The central result is that when this joint distribution condition is satisfied, the forward compu-tation of a Transformer is formally equivalent to a rigorous Bayesian posterior update. The paper is organized as follows. Section 2 presents the mathematical framework and defines the kernel-based transformer abstractions. Section 3 states and proves the core theorems at each level of the hierarchy. Section 4 derives the explicit Bayes formula for the block-level architecture. Section 5 proves softmax normalization and establishes the bridge to concrete attention. Section 6 discusses the scope, assumptions, and limitations of the framework. 

# 2 Mathematical Framework 

## 2.1 Probability Kernels and Bayesian Inference 

We work in the category of measurable spaces with Markov kernels as morphisms. All spaces are standard Borel, and all measures are σ-finite. We adopt standard kernel notation: κ : Kernel XY 

is a probability kernel from X to Y , κ ◦μ μ is measure-kernel composition, and κ1 ⊗κ κ2 is the product kernel. 

Definition 2.1 (Bayesian Transformer) . A Bayesian transformer is a tuple (Θ , X, π, ℓ ) where: 

• Θ is the latent/parameter space (standard Borel, nonempty); 

• X is the observation/data space; 

• π is a finite prior measure on Θ;

• ℓ : Kernel Θ X is a finite Markov kernel. Its Bayes method is the posterior kernel: posterior( ℓ, π ) : Kernel XΘ.

2Proposition 2.2 (Bayes Joint Law) . For any Bayesian transformer T = (Θ , X, π, ℓ ), the Bayes method satisfies the joint-distribution identity: 

(ℓ ◦μ π) ⊗κ posterior( ℓ, π ) = ( π ⊗κ ℓ). map swap . (1) This proposition—that the posterior kernel, when composed with the prior, recovers the sym-metrized joint law—is the fundamental identity of Bayesian inference and serves as the anchor for all subsequent transformer-level proofs. 

## 2.2 Semantic Transformers: Adding an Update Kernel 

The core Bayesian transformer defines the posterior abstractly. A semantic transformer equips this structure with an explicit internal update kernel, and the key question becomes: when does this update equal the posterior? 

Definition 2.3 (Semantic Transformer) . A semantic transformer extends a Bayesian transformer 

T = (Θ , X, π, ℓ ) with an internal update kernel u : Kernel XΘ. Its Bayes joint semantics condition is: 

(ℓ ◦μ π) ⊗κ u = ( π ⊗κ ℓ). map swap . (2) The Bayes joint semantics condition states that the transformer’s internal update, when com-posed with the prior and likelihood, reconstructs the correct symmetrized joint law. This is the sole 

structural condition required for all subsequent theorems. 

## 2.3 Transformer Block with QKV/Attention/Residual/MLP 

A practical transformer block has a concrete data-flow pipeline: input embedding → QKV pro-jection → attention → residual/MLP → readout. We formalize this as a composition of Markov kernels. 

Definition 2.4 (QKV State and Attention Trace) . Let Q, K, V be the query, key, and value type parameters. 

• QKVState Q K V := ( Q × K) × V ;

• AttentionTrace X Q K V A := ( X × QKVState Q K V ) × A.

The attention trace stores the input state, the QKV state, and the attention state, enabling the residual/MLP kernel to condition on all intermediate representations. This is the natural kernel-level encoding of the residual connection. 

Definition 2.5 (Block Likelihood) . For type parameters Θ, X, Q, K, V, A, H, O , the block likeli-hood is the kernel composition: 

blockLikelihood ( input : Kernel Θ X)( qkv : Kernel X(QKVState Q K V )) (attention : Kernel( X × QKVState Q K V )A)(residualMLP : Kernel(AttentionTrace X Q K V A )H)(readout : Kernel HO ) : Kernel Θ O

:= readout ◦κ residualMLP ◦κ

(( input ⊗κ prodMkLeft Θ qkv ) ⊗κ prodMkLeft Θ attention ). (3) 3The composition reflects the exact data flow: the input kernel maps latent parameters to input states; the QKV kernel maps input states to query-key-value triples; the attention kernel sees both input and QKV states; the residual/MLP kernel sees the full attention trace; and the readout kernel produces the final output. 

## 2.4 Multilayer Transformer Architecture 

Real transformers stack multiple blocks. We formalize this as a list of hidden-state kernels com-posed in reverse order (first layer first, last layer last). 

Definition 2.6 (Layer Stack) . For hidden-state type H, the layer stack layerStack maps a list of kernels (Kernel HH ) to a single kernel: 

layerStack [] := id , (4) 

layerStack ( ℓ :: ℓs ) := layerStack ℓs ◦κ ℓ. (5) 

The architecture likelihood composes embedding, layer stack, and readout: 

architectureLikelihood emb layers readout := readout ◦κ layerStack layers ◦κ emb . (6) 

## 2.5 Softmax Attention 

The abstract kernel framework treats attention as a black-box Markov kernel. To bridge to concrete implementations, we formally define softmax and prove it produces a valid probability distribution. 

Definition 2.7 (Softmax) . For a nonempty finite index type ι and score vector x : ι → R, the softmax of index i is: 

softmax x i := exp( xi)

P 

> j

exp( xj ). (7) 

# 3 Core Theorems 

## 3.1 Bayes Kernel Level: The Posterior Joint Law 

Theorem 3.1 (Posterior Joint Law) . For any Bayesian transformer T = (Θ , X, π, ℓ ) with finite prior and finite likelihood, 

(ℓ ◦μ π) ⊗κ posterior( ℓ, π ) = ( π ⊗κ ℓ). map swap . (8) 

Proof. This follows from the disintegration theorem for finite kernels, which constructs the poste-rior kernel via disintegration and proves the identity in full measure-theoretic generality. The proof requires only that Θ is standard Borel, π is σ-finite, and ℓ is a Markov kernel. Theorem 3.1 is the foundation: it establishes that the posterior kernel by construction satis-fies the Bayes joint law. Every transformer-level theorem below reduces to this identity via the definition of the Bayes joint semantics condition. 43.2 Semantic Level: Update Equals Posterior 

Theorem 3.2 (Semantic Transformer is Bayes) . Let S = ( T, u ) be a semantic transformer ex-tending Bayesian transformer T with update kernel u. If S satisfies the Bayes joint semantics condition (2), then 

u =a.e. [ℓ◦μπ] posterior( ℓ, π ). (9) 

Proof. The Bayes joint semantics condition states that u satisfies the same joint-law identity as 

posterior( ℓ, π ) (Theorem 3.1). By the uniqueness of the posterior kernel under the Bayes joint law, any kernel satisfying the Bayes joint law equals the posterior almost everywhere with respect to the observation distribution. The result follows by direct application of this lemma. 

Corollary 3.3 (Semantic Bayes) . For any semantic transformer S with Bayes joint semantics, 

S. IsBayes holds. 

The semantic-level theorem establishes the core pattern that propagates through every subse-quent level: Bayes joint semantics =⇒ update equals posterior . This is the central result that we now extend to increasingly concrete architectures. 

## 3.3 Block Level: Complete Bayes Process 

Definition 3.4 (Block Structure) . A transformer block B over (Θ , X, Q, K, V, A, H, O ) consists of a prior π, component kernels (input, qkv, attention, residualMLP, readout), an update kernel u, and finiteness typeclasses. Its likelihood ℓB is defined in (3). 

Definition 3.5 (Block Bayes Joint Semantics) . The block satisfies Bayes joint semantics if: 

(ℓB ◦μ π) ⊗κ u = ( π ⊗κ ℓB ). map swap . (10) 

Theorem 3.6 (Transformer Block is Complete Bayes Process) . Let B be a transformer block with finite prior, finite likelihood, and finite update. If B satisfies Bayes joint semantics, then: 

u =a.e. [ℓB ◦μπ] posterior( ℓB , π ). (11) 

Proof. Apply Theorem 3.2 with ℓ = ℓB , using the block’s Bayes joint semantics as the hypothesis. The block likelihood ℓB is a finite Markov kernel by the finiteness typeclass and the closure of kernel composition under finiteness; all required typeclass instances are available from the block structure. 

Theorem 3.7 (Block-Level Bayes Formula) . Under the additional absolute-continuity condition 

∀μθ, ℓ B (θ) ≪ ℓB ◦μ π, the block update has the explicit density form: 

u(o)( A) = 

Z

> A

dℓ B (θ)

d(ℓB ◦μ π)(o) dπ (θ), (12) 

for almost every observed output o.Proof. By Theorem 3.6, u =a.e. posterior( ℓB , π ). Under the absolute-continuity condition, the posterior admits the explicit Radon-Nikodym form, yielding the density representation. 5Theorem 3.8 (Block Density Normalization) . If the block update u is a Markov kernel, then under the Bayes joint semantics and absolute-continuity conditions, the likelihood density integrates to 1 for almost every observed output: 

∀μo, 

Z

> Θ

dℓ B (θ)

d(ℓB ◦μ π)(o) dπ (θ) = 1 . (13) 

Proof. By Theorem 3.7 evaluated at the full space Θ, and the Markov property u(o)(Θ) = 1 , the integral equals u(o)(Θ) = 1 .Theorem 3.8 is critical: it confirms that the block constructs a normalized posterior distribution, not merely an unnormalized score. This distinguishes the framework from heuristic approaches that interpret attention weights as unnormalized log-probabilities. 

## 3.4 Multilayer Level: Stacked Architecture is Bayes 

Theorem 3.9 (Multilayer Transformer is Bayes) . Let M be a multilayer transformer with embed-ding emb , layer stack layers , readout readout , and update u. If M satisfies the Bayes joint se-mantics condition for the full architecture likelihood ℓM = readout ◦κ layerStack layers ◦κ emb ,then: 

u =a.e. [ℓM ◦μπ] posterior( ℓM , π ). (14) 

Proof. The architecture likelihood ℓM is a composition of finite Markov kernels, hence itself a finite Markov kernel. Apply Theorem 3.2 with ℓ = ℓM and the multilayer Bayes joint semantics as hypothesis. 

Corollary 3.10 (Depth Independence) . The Bayes property holds for any finite depth depth( M ) = 

| layers |. The proof is uniform in the layer count. 

This corollary is noteworthy: the Bayesian guarantee does not degrade with depth, and no additional conditions are required for deeper stacks. The compositional structure of Markov kernels ensures that the Bayes property is preserved under arbitrary finite compositions. 

## 3.5 Softmax Normalization 

Theorem 3.11 (Softmax Sums to One) . For any nonempty finite type ι and score vector x : ι → R,

X

> i:ι

softmax x i = 1 . (15) 

Proof. Let S = P 

> j

exp( xj ). Since exp( z) > 0 for all real z, and the sum is over a nonempty finite set, S > 0. Then: 

X

> i

softmax x i = X

> i

exp( xi)

S = 1

S

X

> i

exp( xi) = SS = 1 . (16) The proof uses standard real analysis: Real.exp pos for positivity, Finset.sum pos for the denominator, and algebraic rewriting. 6Table 1: Summary of Formalized Theorems                      

> Level Theorem Formal Designation
> Kernel Posterior joint law tf bayes joint
> Semantic Update equals posterior tf is bayes
> Block Complete Bayes process tf block complete bayes
> Block Bayes formula (density) tf block bayes formula
> Block Density normalization tf block density norm
> Block Event-level formula tf block formula apply
> Multilayer Stacked Bayes tf multilayer bayes
> Conditional Event-level Bayes tf conditional bayes
> Softmax Sums to one softmax sum eq one
> Softmax Positivity softmax pos

Corollary 3.12 (Softmax Defines a Probability Distribution) . For any nonempty finite ι and x :

ι → R, (softmax x i )i∈ι is a valid probability distribution: softmax x i ≥ 0 for all i, and P 

> i

softmax x i = 1 .Proof. Positivity follows from exp( xi) > 0 and S > 0, hence each term is a ratio of positive numbers. Summation to 1 is Theorem 3.11. This theorem bridges the abstract kernel framework to concrete transformer implementations. The scaled dot-product attention mechanism softmax( QK T /√dk)V uses softmax to produce at-tention weights that form a convex combination of value vectors. Theorem 3.11 establishes that these weights are a valid probability distribution, and therefore the attention output is a convex combination—a property essential for viewing attention as a Markov kernel. 

# 4 Scope, Assumptions, and Limitations 

## 4.1 What the Framework Proves 

The framework establishes a structural theorem: any transformer architecture whose internal up-date satisfies the Bayes joint semantics condition in the sense of Eq. (2) implements Bayesian posterior inference. The proof is constructive in the following sense: given a transformer block with Bayes joint semantics, we can explicitly compute its posterior density via Radon-Nikodym differentiation (Theorem 3.7). 

## 4.2 Explicit Assumptions 

The framework makes the following assumptions, all of which are either structural (about the math-ematical setting) or conditional (“if the Bayes joint semantics holds, then...”): 

A1 . Standard Borel spaces. All type parameters are standard Borel measurable spaces. This is the natural setting for probability kernel theory and holds for all practical parameterizations. 

A2 . Finite measures and kernels. The prior is a finite measure and all component kernels are finite Markov kernels. This excludes improper priors and unbounded kernel measures. 7A3 . Kernel composition closure. The block likelihood is a finite kernel, which follows from finite-kernel closure under composition and product (a structural property of the kernel cate-gory). 

A4 . Countable or countably generated. For the explicit Bayes formula with Radon-Nikodym derivatives, the latent and observation spaces are required to be countably generated. This is a standard technical condition for disintegration. 

A5 . Absolute continuity. For the density formula, the likelihood kernel must be absolutely con-tinuous with respect to the prior-predictive marginal. This is the standard condition for the existence of a Radon-Nikodym derivative. 

A6 . Bayes joint semantics. This is the sole substantive condition : the transformer’s update kernel, when composed with the prior and likelihood, must satisfy the Bayes joint law identity. The framework does not assume this condition holds automatically; it proves what follows if it does. Notably, the framework makes no assumptions about: • The specific parameterization of the attention mechanism (softmax, linear, kernelized); • The depth or width of the transformer; • The training procedure, loss function, or optimization algorithm; • The data distribution or representativeness of training data. 

## 4.3 Scope Boundaries 

The framework establishes a conditional guarantee. It does not :• Prove that any specific trained transformer satisfies the Bayes joint semantics condition—that remains an empirical question; • Provide training guarantees (convergence to the Bayes joint law); • Address the quality of the prior or the appropriateness of the likelihood model; • Extend to loss functions other than those for which the posterior is the Bayes act. These limitations are by design: the framework isolates the structural question (“if the update is Bayes-consistent, is it Bayesian?”) from the empirical question (“is this specific model’s update Bayes-consistent?”). 

## 4.4 Softmax Attention Bridge 

The softmax normalization theorem (Section 3.5) establishes that the attention mechanism pro-duces valid probability weights, which is a necessary condition for attention to be interpretable as a Markov kernel. However, this is not sufficient to prove that a specific attention parameterization satisfies the full Bayes joint semantics. That stronger claim would require proving that the attention kernel, when composed with QKV, residual/MLP, and readout kernels in a specific parameteriza-tion, reconstructs the correct joint law—a substantial open problem. 85 Discussion 

## 5.1 Summary of Results 

We have established a complete formal hierarchy proving that transformer architectures implement Bayesian posterior inference under the Bayes joint semantics condition: 1. Bayes kernel foundation (Theorem 3.1): The posterior kernel satisfies the Bayes joint law by construction. 2. Semantic completeness (Theorem 3.2): Any update kernel satisfying the Bayes joint law equals the posterior a.e. 3. Block completeness (Theorem 3.6): A transformer block with QKV/attention/residual/MLP/ readout pipeline is a complete Bayes process—its update equals the posterior. 4. Explicit Bayes formula (Theorem 3.7): Under absolute continuity, the block’s update has a closed-form density representation via Radon-Nikodym differentiation. 5. Density normalization (Theorem 3.8): The Bayes formula yields a properly normalized poste-rior distribution. 6. Multilayer extension (Theorem 3.9): The Bayes property is preserved under arbitrary finite layer stacking. 7. Softmax normalization (Theorem 3.11): The softmax function produces valid probability dis-tributions, connecting the kernel framework to concrete attention mechanisms. 

## 5.2 Relationship to Existing Work 

The connection between attention and Bayesian inference has been explored from several angles. Garnelo et al. 3 proposed conditional neural processes that implement a form of Bayesian updating. Singh and Anand 2 drew structural analogies between attention and message-passing in probabilistic graphical models. M¨ uller et al. 4 showed that self-attention can approximate posterior inference in Gaussian process models. Our contribution differs from these works in three ways. First, we provide rigorous proofs 

rather than analogies or empirical demonstrations. Second, we work at the level of Markov kernel composition, which abstracts over specific parameterizations while exposing the exact structural conditions needed for the Bayesian property. Third, we provide the explicit Bayes formula for the block-level architecture, not merely an existential claim. 

## 5.3 Open Problems 

Several important directions remain open: • Training to Bayes joint semantics. The framework proves that if the Bayes joint law holds, 

then the transformer is Bayesian. Proving that standard training procedures (empirical risk min-imization, next-token prediction) converge to a kernel satisfying this condition is a substantial open problem. 9• Concrete parameterization proofs. Proving that a specific parameterized attention mecha-nism (e.g., scaled dot-product softmax attention with learned WQ, W K , W V matrices) satisfies the Bayes joint semantics under some conditions on the weight matrices would bridge the gap from structural guarantee to architectural guarantee. • Conjugate priors for transformer blocks. Identifying prior families for which the transformer block’s posterior has a closed-form conjugate structure would enable efficient Bayesian updating in practical transformer implementations. • Beyond squared loss. The framework currently relies on the posterior being the Bayes act for proper scoring rules. Extending to other decision-theoretic loss classes would broaden the applicability. 

# 6 Conclusion 

We have presented a complete formal proof that transformer architectures implement exact Bayesian posterior inference when their internal update kernels satisfy the Bayes joint semantics condition. The proof is organized as a hierarchy of abstractions mirroring the structure of real transformer designs, from the core Bayesian kernel through semantic, block, and multilayer levels. The framework makes minimal assumptions—no architectural constraints, no training proce-dure requirements, no data distribution assumptions—and exposes the Bayes joint semantics as the single load-bearing condition. The explicit Bayes formula for the block-level architecture and the softmax normalization proof bridge the abstract kernel theory to concrete implementations. In summary, when this joint distribution condition is satisfied, the forward computation of a Transformer is formally equivalent to a rigorous Bayesian posterior update. This is a precise, carefully circumscribed claim: under the stated conditions, a transformer block satisfies the Bayes joint law and its update equals the posterior almost everywhere. We believe this level of precision is essential for building a mathematically defensible foundation for the probabilistic semantics of neural architectures. 

# Acknowledgements 

The author thanks colleagues for helpful discussions on the probabilistic foundations of this work. 

# Author contributions 

Haobo Yang developed the mathematical framework, the proofs, and the manuscript. 

# Competing interests 

The author declares no competing interests. 10 Data availability 

The complete mathematical derivations are available in the project repository. 

# References 

[1] A. Vaswani et al., Attention Is All You Need , NeurIPS, 2017. [2] R. Singh, A. Anand, Transformers as Probabilistic Programs , arXiv, 2023. [3] M. Garnelo et al., Conditional Neural Processes , ICML, 2018. [4] S. M¨ uller et al., Attentive Neural Processes , NeurIPS, 2019. [5] L. Le Cam, Asymptotic Methods in Statistical Decision Theory , Springer, 1986. [6] M. J. Schervish, Theory of Statistics , Springer, 1995. 11