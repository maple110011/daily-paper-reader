---
title: Consistency of variational approximations under bounded Kullback--Leibler divergence
title_zh: 有界Kullback--Leibler散度下变分逼近的一致性
authors: "Hien Duy Nguyen, Jacob Westerhout, Thomas Guilmeau, Julyan Arbel"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13230v1"
tags: ["query:bayes-dl"]
score: 9.0
evidence: 有界KL散度下变分近似的一致性——直接相关于深度学习中的变分推理
tldr: 变分近似广泛应用于贝叶斯推断中近似后验分布，但其一致性理论尚不完善。该研究在一般度量空间上证明，若近似分布与目标后验的Kullback-Leibler散度一致有界且目标序列紧致，则近似序列也紧致。进而当目标后验弱收敛到真实参数时，任何KL散度有界的变分序列也一致收敛。还给出了简单的对数矩条件验证该有界性，并应用于光滑广义后验分布。该工作为变分近似的一致性提供了通用理论保证。
source: arxiv
selection_source: fresh_fetch
motivation: 变分方法在贝叶斯推断中广泛使用，但缺乏一致性理论保证，需要研究在KL散度有界条件下变分近似的一致性。
method: 在一般度量空间上，利用KL散度一致有界性和目标序列紧致性推导近似序列的紧致性，并给出对数矩条件验证有界性。
result: 若目标后验弱收敛到Dirac测度，则KL散度有界的变分序列也弱收敛到同一极限，即一致性成立。
conclusion: 建立了变分近似一致性的通用条件，为实际应用提供了理论支撑，并可通过简单矩条件验证。
---

## 摘要
变分方法广泛应用于贝叶斯推断中，当精确计算不可行时，用于近似后验分布。我们研究此类近似何时继承后验一致性。我们的第一个结果表明，在一般度量空间上，从近似测度到目标测度的紧序列的Kullback--Leibler散度的一致有界迫使近似序列是紧的。因此，如果目标后验弱收敛到真实参数处的Dirac质量，那么任何与目标具有有界Kullback--Leibler散度的变分序列也是一致的。我们还给出了简单的对数矩条件来验证这一有界性条件，并针对光滑广义后验分布进行了说明。

## Abstract
Variational methods are widely used to approximate posterior distributions in Bayesian inference when exact computation is infeasible. We study when such approximations inherit posterior consistency. Our first result shows that, on a general metric space, a uniform bound on the Kullback--Leibler divergence from the approximating measures to a tight sequence of target measures forces the approximating sequence to be tight. It follows that if the target posteriors converge weakly to a Dirac mass at the true parameter, then any variational sequence with bounded Kullback--Leibler divergence to the targets is also consistent. We also give simple logarithmic-moment conditions that verify this boundedness condition, and illustrate them for smooth generalised posterior distributions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：变分推断（Variational Inference）是贝叶斯推断中处理复杂后验分布的重要工具，但理论保证（如后验一致性）尚不完善。现有研究通常对模型结构、变分族或优化算法施加严格限制，缺乏一个统一的、仅依赖KL散度有界性的充分条件。
- **核心问题**：在什么条件下，变分近似序列 \(\nu_n\) 能够继承真实后验序列 \(\mu_n\) 的一致性（即弱收敛到真实参数 \(\theta_0\) 处的狄拉克测度）？
- **整体含义**：本文在一般度量空间上建立了一个简洁的理论框架，证明只要近似分布与目标后验的KL散度一致有界，且目标后验是紧的并收敛到狄拉克测度，则变分近似也是一致的。这一结论不依赖于变分族的具体结构、优化是否精确或后验是否分数化，为变分近似的统计可靠性提供了通用理论基石。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用KL散度的Donsker–Varadhan变分界，结合弱收敛与紧性概念，将变分近似的一致性问题转化为KL散度有界性的验证。
- **关键技术细节**：
  - **定理1（KL水平集的紧性传递）**：若 \((\mu_n)\) 是渐近紧的，且对任意序列 \((\epsilon_n)\) 满足 \(\limsup \epsilon_n < \infty\)，则集合 \(S_n = \{\nu : \mathrm{KL}(\nu\|\mu_n) \leq \epsilon_n\}\) 是渐近紧的。证明使用了Donsker–Varadhan界的变形（Lemma 1）和紧性的定义。
  - **定理2（一致性推论）**：若 \(\mu_n \rightsquigarrow \delta_{\theta_0}\) 且 \(\limsup_n \mathrm{KL}(\nu_n\|\mu_n) < \infty\)，则 \(\nu_n \rightsquigarrow \delta_{\theta_0}\)。证明利用定理1得到 \((\nu_n)\) 的紧性，再由Prohorov定理和KL的下半连续性得出结论。
  - **定理3（变分一致性条件）**：若 \(\inf_{\nu \in \mathcal{F}_n} \mathrm{KL}(\nu\|\mu_n)\) 的上极限有限，则任何近似最优的 \(\nu_n\)（即 \(\mathrm{KL}(\nu_n\|\mu_n) \leq m_n + \epsilon_n\) 且 \(\limsup \epsilon_n < \infty\)）都一致收敛。
  - **引理1（Donsker–Varadhan界的变形）**：对任意 \(\nu, \mu\)，有 \(\nu(A) \leq \frac{1}{\delta} \left[ \mathrm{KL}(\nu\|\mu) + \mu(A) e^\delta \right]\)，用于将KL控制与测度集中性联系起来。
  - **验证KL有界性的方法**：通过引入可测双射 \(\gamma_n\) 将KL散度转化为关于单个参考测度 \(\tilde{\nu}\) 的期望差（Proposition 1），然后利用下包络条件（Proposition 2）和光滑性条件（Proposition 3）来检验 \(\tilde{p}_n\) 的下界，最终得到形如 \(\tilde{p}_n(z) \ge c e^{-C\|z\|^2/2}\) 的界，从而保证 \(\mathbb{E}_{\tilde{\nu}} (-\log \tilde{p}_n)_+\) 有界。
- **关键公式**：KL散度的积分表达式 \((1)\)；推前密度的变换 \(\tilde{p}_n(z) = t_n^{-p} p_n(\theta_n + t_n^{-1}z)\)；下包络条件 \(\tilde{p}_n(z) \ge c e^{-C\|z\|^2/2}\)。
- **算法流程**：无特定算法，是一个理论框架。实际应用中，只需验证：
  1. 目标后验 \(\mu_n\) 一致且收敛到狄拉克测度（通常由先验条件保证）。
  2. 变分族 \(\mathcal{F}_n\) 存在序列 \(\nu_n^*\) 满足 \(\mathrm{KL}(\nu_n^*\|\mu_n)\) 有界（可利用Proposition 1-3及Theorem 4给出的条件验证）。
  3. 实际变分逼近 \(\nu_n\) 的KL散度近似最优（即 \(\mathrm{KL}(\nu_n\|\mu_n) \le m_n + \epsilon_n, \limsup \epsilon_n < \infty\)）。

### 3. 实验设计

- **论文类型**：纯理论推导，无任何数值实验或仿真。没有使用任何数据集、benchmark，也没有对比任何方法。
- **示例说明**：文中仅包含理论例子（Example 1-3）用于说明KL有界性条件不是必要的，以及有界性条件的反例。这些例子是概率测度序列的构造，非真实数据实验。

### 4. 资源与算力

- **未提及**：论文未涉及任何计算资源（GPU型号、数量、训练时长等），因为其为纯理论工作，无需算力。

### 5. 实验数量与充分性

- **实验数量**：0。文章无实验部分。
- **充分性**：作为理论论文，其充分性体现在数学证明的严谨性和推理的完整性。论文提供了主要定理（Theorem 1-4）及其证明（见附录），并给出了多个命题和引理作为支撑，逻辑是完整的。但缺乏实证验证是该工作的一种局限。

### 6. 论文的主要结论与发现

- **主要结论**：
  1. （Theorem 1）在一般度量空间上，若目标测度序列 \((\mu_n)\) 渐近紧，且变分测度序列 \((\nu_n)\) 与 \(\mu_n\) 的KL散度一致有界，则 \((\nu_n)\) 也是渐近紧的。
  2. （Theorem 2）进一步，若 \(\mu_n\) 弱收敛到 \(\delta_{\theta_0}\)，则任何KL散度有界的 \(\nu_n\) 也弱收敛到 \(\delta_{\theta_0}\)，即变分近似一致。
  3. （Theorem 3）该结论可直接推广到变分推断场景：只要 \(\inf_{\nu \in \mathcal{F}_n} \mathrm{KL}(\nu\|\mu_n)\) 有上极限，且优化误差有界，则变分近似一致。
  4. （Proposition 1-3, Theorem 4）给出了验证KL有界性的充分条件：通过一个参考测度 \(\tilde{\nu}\)，要求 \(\tilde{p}_n\) 满足下包络（如光滑性、二阶矩条件），可转化为简单的对数矩条件。并在广义线性模型（GLMs）中验证了这些条件。
- **重要发现**：
  - KL散度的有界性足矣，无需假设变分族的结构或模型的具体形式。
  - 验证KL有界性可简化为验证 \(\tilde{\nu}\) 的有限二阶矩和 \(\tilde{p}_n\) 的下界（如二次型下界）。
  - 对光滑广义后验（如GLMs），Theorem 4保证了这种下界存在。

### 7. 优点

- **理论普适性**：工作覆盖了任意度量空间，不限定于欧氏空间或特定模型，是第一个给出变分近似一致性的通用充分条件的工作。
- **简洁性**：证明主要基于KL散度的标准性质和紧性概念，逻辑链条清晰，避免了复杂的先验质量条件或测试条件。
- **易用性**：将验证一致性的工作简化为检查一个对数矩条件，且给出了具体的推导方法（Proposition 2, 3），对实践者有指导意义。
- **对近似最优性的容忍**：允许变分序列仅近似最小化KL散度（引入 \(\epsilon_n\) 有界），更贴近实际算法（如梯度下降不保证精确最优）。
- **反例分析**：通过例子（Example 1-3）澄清了KL有界性条件既非必要也不可放松，帮助理解理论的边界。

### 8. 不足与局限

- **缺乏实验验证**：论文为纯理论工作，未进行任何数值仿真或真实数据实验，无法展示条件在实际算法（如黑盒变分推断）中的可验证性和效果。
- **条件验证的实践难度**：虽然给出了简单的对数矩条件，但要求后验密度 \(\tilde{p}_n\) 满足二次型下界，这在实际中需要后验密度的解析形式或高精度估计，对复杂模型可能不易验证。此外，参考测度 \(\tilde{\nu}\) 的选取和双射 \(\gamma_n\) 的构造需要领域知识。
- **仅讨论弱收敛到狄拉克测度**：未覆盖更一般的后验收敛速率或后验形状（如伯恩斯坦-冯·米塞斯定理）。只处理了“点估计一致性”（posterior consistency），未涉及不确定性量化性质。
- **有限适用场景**：文中例子主要针对光滑广义后验和GLMs，对非光滑损失、分层模型或非参数模型（如高斯过程）的适用性未讨论。
- **优化误差假设**：要求 \(\limsup \epsilon_n < \infty\)，即近似误差有界。实际变分优化算法（如均值场变分贝叶斯）可能导致误差无界，论文未提供控制误差有界的通用方法。

（完）
