---
title: On-Manifold Variational Learning with Heat-Kernel Priors
title_zh: 基于热核先验的流形上变分学习
authors: "Jiarui Xing, Tal Zeevi, Nian Wu, Jian Wang"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18658v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 基于流形锚定的变分学习与热核先验
tldr: 现有深度潜变量模型在高维医疗影像上学习无监督表征时，因使用欧氏平均估计高斯混合先验，导致子群原型偏离数据流形并随子群数量增加而退化。本文提出一种流形锚定变分框架，基于几何感知的EM算法，在热核加权潜图上选取扩散中心性最高的图质心作为原型，确保每个原型位于流形上。Dirichlet能量正则化保持潜空间几何平滑，且每个子群的不确定性分数实现无标签质量评估。在心脏瘢痕和脑MRI基准上，该方法准确率最高，原型最清晰，且在大子群数量下保持稳定，而所有基线均退化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18658v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1233, \"height\": 628, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18658v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18658v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1270, \"height\": 1008, \"label\": \"Figure\"}]"
motivation: 现有方法使用欧氏平均估计原型，导致原型偏离数据流形并随子群增多退化，无法真实反映病理异质性。
method: 提出流形锚定变分框架，采用几何感知EM算法，在热核加权潜图上选取扩散中心性最高的图质心作为原型，并加入Dirichlet能量正则化。
result: 在心脏瘢痕和脑MRI基准上，准确率最高，原型最清晰，且大子群数量下仍稳定，而所有基线均退化。
conclusion: 该框架作为通用几何工具，可扩展至其他潜变量模型，实现无监督学习中的流形保持与高质量表征。
---

## 摘要
无监督地学习医学影像队列的表征，可以在没有专家标签的情况下揭示临床上有意义的原型，而专家标签通常带有噪声且无法捕捉真实的病理异质性。然而，现有的深度潜变量模型通过欧几里得平均来估计高斯混合先验，生成的原型会偏离弯曲的数据流形，并随着子群体数量的增加而退化。我们提出了一种基于几何感知期望最大化（EM）算法的流形锚定变分框架，其M-step选择每个子群体原型为热核加权潜在图上具有最高扩散中心性的图medoid，确保每个原型保持在流形上。Dirichlet能量正则化项强制潜在空间的几何平滑性，每个子群体的不确定性分数则实现了无标签质量评估。\rev{流形锚定EM是一种通用几何工具，它扩展了标准EM，并可直接应用于此设置之外的其他潜变量模型。}在心脏疤痕和脑MRI基准测试中，我们的框架在所有比较方法中达到了最高准确率，产生了迄今为止最清晰的原型，并且在所有基线方法退化的较大子群体数量下保持稳定。

## Abstract
Learning unsupervised representations of medical imaging cohorts can reveal clinically meaningful prototypes without expert labels, which are often noisy and fail to capture true pathological heterogeneity. However, existing deep latent-variable models estimate Gaussian mixture priors via Euclidean averaging, producing prototypes that drift off the curved data manifold and degenerate as the number of sub-populations grows. We propose a manifold-anchored variational framework built on a geometry-aware Expectation-Maximization (EM) algorithm, whose M-step selects each sub-population prototype as the graph medoid with the highest diffusion centrality on a heat-kernel-weighted latent graph, ensuring that every prototype remains on-manifold. A Dirichlet energy regularizer enforces geometric smoothness of the latent space, and a per-sub-population uncertainty score enables label-free quality assessment. \rev{The manifold-anchored EM is a general-purpose geometric tool that extends standard EM and applies readily to other latent-variable models beyond this setting.} On cardiac scar and brain MRI benchmarks, our framework attains the highest accuracy among all compared methods, produces the sharpest prototypes reported to date, and remains stable at large sub-population counts where all baselines degenerate.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：在医学影像分析中，无监督学习子群体表征对于疾病分期、图谱构建和临床试验队列选择至关重要。但临床上诊断标签常不可靠（如心肌瘢痕亚型模糊、脑MR认知评分非直接形态标签），监督方法会继承标签噪声。
- **核心问题**：现有深度潜变量模型（如VAE-GMM、扩散模型）使用欧氏平均估计高斯混合先验的原型，导致原型偏离数据流形（off-manifold），落入低密度区域；随着子群体数量增加，原型退化并产生重叠的混合分量，降低可解释性。
- **论文含义**：提出一个统一变分框架，同时解决无标签表征学习、流形感知原型估计和对具有不同拓扑特征异构群体的生成建模。关键创新在于将谱流形几何（热核、图拉普拉斯）融入概率潜变量模型，并引入通用的**流形锚定EM算法**，可作为标准EM的几何扩展，适用于其他潜变量模型。

### 2. 方法论

#### 核心思想
- 原型不应是欧氏平均，而应是流形上的离散点（图medoid）。构建热核加权最近邻图近似潜空间流形，利用热核亲和度选择每个子群体中扩散中心性最高的样本作为原型，确保原型位于流形上。
- 引入Dirichlet能量正则化项促进潜空间几何平滑。
- 每个子群体生成不确定性分数（像素级标准差和结构级DSSIM），实现无标签质量评估。

#### 关键技术细节
1. **热核亲和图**：对潜变量 \( Z = \{z_i\} \)，边权重 \( W_{ij} = \exp(-\|z_i - z_j\|^2 / 4t) \)，带宽 \( t \) 自适应为mini-batch内距离中位数。
2. **图拉普拉斯与Dirichlet能量**：\( L = D - W \)，\( E(Z) = \mathrm{Tr}(Z^\top L Z) \)，在mini-batch上计算，复杂度 \( O(B^2 d) \)，避免全局 \( O(N^2) \)。
3. **流形锚定EM**（MA-EM）：
   - **E步**：标准软责任 \( \gamma_{ik} \)。
   - **M步**：对每个子群体 \( C_k \)（硬分配），原型 \( \mu_k^* = \arg\max_{z_i \in C_k} \sum_{\ell \in C_k} W_{i\ell} \)（即图medoid），确保 \( \mu_k^* \in C_k \subset \mathcal{M} \)。协方差保留软责任并加 \( \epsilon I \) 确保正定，混合系数为标准平均。
4. **变分框架**：编码器输出高斯后验 \( q_\phi(z|x) \)，解码器重建 \( \hat{x} = D_\theta(z) \)。结构化先验 \( p(z) = \sum_k \pi_k^* \mathcal{N}(z|\mu_k^*, \Sigma_k^*) \) 由MA-EM更新得到。训练目标：
   \[
   \mathcal{J} = -\log p_\theta(x|z) + \beta D_{KL}(q_\phi(z|x) \| \mathcal{N}(\mu_{\hat{k}}^*, \Sigma_{\hat{k}}^*)) + \lambda E(Z)
   \]
   分别对应重建、流形锚定KL正则化、Dirichlet能量平滑。
5. **不确定性量化**：每个样本 \( M=30 \) 次后验采样，计算像素不确定性 \( U_i(p) = \mathrm{Std}_m[\hat{x}_i^{(m)}(p)] \) 和结构不确定性 \( S_i = \frac{1}{M}\sum_m D_{SSIM}(\hat{x}_i^{(m)}, \bar{x}_i) \)，然后聚集到子群体层面。

### 3. 实验设计

#### 数据集
- **MNIST**：60k图像，10类，作为受控基准。
- **Synthetic Cardiac**：5k心脏掩膜（5类：健康、心内膜下、透壁、中壁、心外膜）。
- **OASIS Brain MRI**：185个受试者的T1加权轴位切片（128×128），按受试者划分避免数据泄露。

#### 划分与处理
- 70%/15%/15%训练/验证/测试。

#### 对比方法
- 主要基线：VAE-GMM（变分深度嵌入）、Diffusion-GMM（扩散模型+GMM）。说明：非生成式方法（如DEC、谱聚类）不具备合成原型和不确定性映射的能力，因此不纳入比较。

#### 评估指标
- 有标签（MNIST、Cardiac）：Accuracy (ACC)、Normalized Mutual Information (NMI)、Adjusted Rand Index (ARI)。
- 无标签（OASIS）：Silhouette Score。
- 原型锐度（sharpness）、不确定性图质量、临床关联（诊断分数 vs 子群体排名）。

#### 实现细节
- 优化器：Adam，学习率 1e-3（MNIST/Cardiac）和 1e-4（OASIS），梯度裁剪和L2权重衰减。
- 超参数：\( \beta=0.5, \lambda=1.0 \)（OASIS）；组件数 \( K \) 通过肘部法选择（MNIST=10，Cardiac=5，OASIS=8），与有标签数据真类数一致。
- 不确定性估计：30次蒙特卡洛采样。
- 所有结果取多随机种子平均。

### 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及优化器、学习率等配置。因此无法总结具体算力消耗。
- 但可推断：mini-batch图计算复杂度 \( O(B^2 d) \)，在基准数据集上训练应可在单GPU上完成。

### 5. 实验数量与充分性

- **实验组数**：三个数据集上进行了定量比较（图2a：ACC/NMI/ARI/Silhouette），并展示了原型与不确定性图可视化（图2c/d、图3）。对于OASIS，还分析了临床诊断分数相关性（图2b）。未明确报告消融实验（如去除Dirichlet能量或使用欧氏平均M步），但通过对比两个生成式基线间接体现贡献。
- **充分性评价**：
  - **优点**：覆盖了不同复杂度（简单MNIST、合成心脏、真实脑MR）和有/无标签场景，度量全面。原型锐度定量（数值）和不确定性定性分析合理。结果一致支持论点。
  - **不足**：缺乏与更具竞争力的无监督聚类方法（如DEC、谱聚类、扩散模型+聚类）的比较；无消融实验分离每个组件贡献；临床关联分析仅展示趋势，无统计显著性检验。另外，实验主要在中等规模数据集上，未在大规模（如UK Biobank）上验证。

### 6. 主要结论与发现

- 所提框架在所有三个数据集上均达到**最高准确率**（ACC/NMI/ARI），并在OASIS上获得最佳Silhouette分数。
- 产生的原型（prototypes）视觉上最清晰、锐度最高，基线的原型模糊退化。
- 不确定性图定位在病理相关区域（如心疤痕边界、脑室边缘），而基线不确定性弥散无意义。
- 在较大子群体数量下（如10类MNIST），所提方法稳定，基线退化。
- 临床关联：健康类似子群体对应较低临床评分，疾病类似子群体对应较高评分，表明发现的子群体具有临床意义。

### 7. 优点

- **方法论创新**：将谱图几何（热核、图medoid）与变分推断结合，解决了长期存在的“原型漂移流形”问题。所提流形锚定EM可作为通用模块扩展至其他潜变量模型。
- **技术上**：Dirichlet能量正则化促进几何平滑；不确定性评分提供自动质量标记；mini-batch图计算避免了全局O(N²)代价。
- **实验设计亮点**：
  - 对生成式模型公平比较（相同编码器-解码器骨干）。
  - 同时评估定量聚类指标和原型可视化质量。
  - 在无标签数据上验证临床相关性（诊断分数排名）。
- **实用性**：无标签需求，适用临床专家标注稀缺场景；推理时无需重建图，比扩散模型更高效。

### 8. 不足与局限

- **实验覆盖不足**：
  - 未与更多无监督方法（如DEC、谱聚类、多模态方法）比较。
  - 缺少消融实验（如去掉Dirichlet能量、使用欧氏平均M步）以量化各组件贡献。
  - 未在大规模、高分辨率医学影像数据库（如UK Biobank、ADNI全脑3D）上验证。
- **不确定性评估缺乏定量验证**：未给出校准曲线或与重建误差的相关性。
- **模型局限性**：
  - 组件数 \( K \) 需用肘部法选择，未提供自适应方法。
  - 假设所有子群体协方差同向（式7使用相同结构），未探讨不同形状协方差。
  - 原型选择仅限于当前批次内的样本，可能受批次不平衡影响。
- **偏差风险**：合成心脏数据集为合成数据，可能简化真实病理复杂性；OASIS仅T1加权轴向切片，未探索多模态。

（完）
