Title: Fast approximate Bayesian multidimensional scaling with consistency guarantees

URL Source: https://arxiv.org/pdf/2605.30687v1

Published Time: Mon, 01 Jun 2026 00:29:37 GMT

Number of Pages: 26

Markdown Content:
# Fast approximate Bayesian multidimensional scaling with consistency guarantees 

Ami Sheth amisheth26@g.ucla.edu Department of Biostatistics University of California, Los Angeles 

Aaron Smith smith.aaron.matthew@gmail.com Department of Mathematics and Statistics University of Ottawa 

Andrew J. Holbrook aholbroo@g.ucla.edu Department of Biostatistics University of California, Los Angeles 

## Abstract 

Bayesian multidimensional scaling (BMDS) embeds n objects in a low-dimensional space to approximately preserve an observed dissimilarity matrix. Compared to classic MDS, BMDS is more robust to model misspecification and supports posterior uncertainty quantification and joint estimation within hierarchical models. However, standard BMDS inference is computationally prohibitive, requiring O(n2) operations per MCMC iteration to evaluate the likelihood. We propose Barnes–Hut BMDS (BH-BMDS), which uses a tree-based ap-proximation to the likelihood and a Gibbs sampler that leverages this structure, remaining compatible with hierarchical extensions. BH-BMDS reduces computational complexity to 

O(n log n) while preserving the geometric fidelity of the embedding. We further establish consistency for the stationary measure of BH-BMDS, proving that it concentrates around the true latent configuration even as the total error of the surrogate likelihood diverges. Notably, this consistency holds in the infinite-dimensional limit. We evaluate the approx-imation on datasets with diverse structure, including air traffic networks, arXiv abstracts, MNIST images and neural activity recordings from mouse models of tau pathology. Across all settings, BH-BMDS closely matches BMDS while achieving substantial computational gains, with approximately 10-fold speedups at n = 1 ,000 and 70-fold speedups at n = 10 ,000 .These gains increase with n, demonstrating strong empirical scalability. 

## 1 Introduction 

Dissimilarity data arise in many domains, including psychology, physics and biology, where a goal is to quantify how pairs of complex or high-dimensional objects differ from one another. A common approach is to embed objects into a low-dimensional space, enabling visualization and downstream modeling while preserving the observed dissimilarities. Bayesian multidimensional scaling (BMDS) provides a probabilistic framework for this task, mapping n objects to latent coordinates in a low-dimensional Euclidean space while modeling observed dissimilarities through a likelihood-based formulation. Unlike classical approaches such as PCA (Pearson, 1901; Hotelling, 1933) and MDS (Torgerson, 1952), or more recent approaches such as t-SNE (van der Maaten & Hinton, 2008) and UMAP (McInnes et al., 2018), BMDS enables posterior uncertainty quantification over latent embeddings and supports integration into hierarchical models. These properties have made BMDS a valuable tool in applications such as viral phylogeography (Bedford et al., 2015; Holbrook et al., 2021; Li et al., 2023), incorporation of auxiliary information (Lin & Fong, 2019) and clustering (Oh & Raftery, 2007; Sheth et al., 2026). 1

> arXiv:2605.30687v1 [stat.CO] 29 May 2026

Despite these advantages, BMDS is computationally constrained by the O(n2) cost of likelihood evaluation, which requires summing over all pairwise distances (Equation 2). As n grows, this quadratic scaling becomes prohibitive, restricting BMDS inference to relatively small datasets. Existing approximation methods (Sheth et al., 2026) lack rigorous posterior consistency guarantees for two or more dimensional latent spaces, raising concerns about their practical reliability. To address these limitations, we propose an approximation to BMDS based on the Barnes–Hut algorithm (BH-BMDS). This framework scales as O(n log n), preserves the original Bayesian formulation and guarantees the consistency for the stationary measure under the approximation and its stochastic realizations. 

## 2 Related Work 

Several approaches attempt to mitigate the cost of BMDS. Bedford et al. (2015) adopt a misspecified Gaussian likelihood to bypass the computational bottleneck imposed by non-negativity constraints; however, this assumption places probability mass outside the support of the observed data. Holbrook et al. (2021) leverage multi-core central processing units, vectorization and graphic processing units to significantly reduce runtime but depend on specialized hardware for scalability. Both methods require O(n2) floating point operations. To achieve sub-quadratic scaling, Sheth et al. (2026) induce sparsity in the dissimilarity matrix, achieving O(mn )

complexity, where m ≪ n represents the number of landmarks. Although this approach yields substantial speedups, m must scale with n to maintain accuracy, and its selection introduces an additional tuning parameter. Moreover, they do not provide posterior consistency guarantees for subsampled dissimilarity matrices in dimensions d > 1.Tree-based methods offer an alternative strategy for approximating pairwise interactions. For instance, van der Maaten (2014) use the Barnes–Hut algorithm to accelerate t-SNE by approximating long-range interactions in an n-body system. While this scales as O(n log n), the paper lacks formal accuracy bounds for the approximation error. Noting that the BMDS likelihood admits a similar n-body interpretation, we adapt the Barnes–Hut algorithm to the BMDS setting (BH-BMDS), enabling O(n log n) likelihood approximations within an MCMC framework. We further establish consistency under uniform vanishing pairwise loss error for BH-BMDS, proving the stationary measure concentrates on the true configuration despite errors introduced by the approximation and noisy MCMC updates. 

## 3 Preliminaries 

3.1 Bayesian multidimensional scaling 

Suppose we have high-dimensional observations, e.g., y1, . . . yn ∈ RΩ, and let Dij = D(yi, yj ) denote the dissimilarity between observations. Bayesian multidimensional scaling (BMDS) (Oh & Raftery, 2001) assumes that the observed dissimilarities follow 

Dij = [ rij + σZ ij 

]

> +

, rij = ∥xi − xj ∥ , Zij 

> i.i.d.

∼ N (0 , 1) , (1) independent across pairs and conditioned on the low-dimensional latent locations X = {xi}ni=1 ⊂ Rd. Here, 

[t]+ = max {t, 0} represents truncation at zero, and the BMDS variance σ2 represents the level of noise distorting the latent distances. This yields the log-likelihood 

ℓ(D | X, σ 2) = − N

2 log(2 πσ 2) − ∑

> i<j

[ (Dij − rij )2

2σ2 + log Φ 

( rij 

σ

)]

, (2) where D = {Dij } is the observed symmetric n × n dissimilarity matrix, N = (n

> 2

), and Φ( ·) is the standard normal CDF. Because the likelihood involves a summation over all unordered pairs, its evaluation scales as 

O(n2). Consequently, we seek to reduce this computational burden using tree-based approximation methods, such as Barnes–Hut algorithms, that scale as O(n log n).2x1    

> x2
> x3
> x4x5
> x6
> x7x1
> x2x3
> x4x5x6
> x7

Figure 1: Barnes–Hut structural components. The left figure is the spatial representation of a recursive subdivision of the embedding space into four quadrants while the right figure is the respective quadtree data structure. One builds the quadtree using Algorithm B.1. 

3.2 Barnes–Hut algorithm 

The gravitational n-body problem involves computing the gravitational forces exerted by all pairs of particles on one another. Direct evaluation of these forces requires O(n2) computations which becomes prohibitive for large datasets. To reduce this cost, Barnes & Hut (1986) propose a hierarchical approximation algorithm that estimates the influence of distant particles as a single aggregate interaction. To organize groups of particles efficiently, the Barnes–Hut algorithm uses a tree data structure, e.g., a quadtree T ∈ R2 (Figure 1). A quadtree T is built by recursively subdividing the embedding space into four quadrants based on particle locations (Algorithm B.1). A cell refers to a fixed spatial region, and a node corresponds to a cell in the tree. Each leaf node contains at most one particle, while internal nodes store summary statistics of all particles within the cell as a pseudo-particle. As particles are inserted, these summaries are updated recursively. The quadtree T is constructed in O(n log n) time and enables efficient spatial queries and interaction approximations. To compute interactions, the quadtree T is traversed starting from the root node. At each node k ∈{0, . . . , K } where the total number of nodes K = O(n), we consider its corresponding cell and pseudo-particle. Let wk be the width of this cell, ¯rik the Euclidean distance between the target particle xi and the pseudo-particle’s center of mass, and ρik = wk/¯rik . If ρik < θ , where θ is a user-specified threshold, the pseudo-particle is considered distant and used as an approximation of its constituent particles. Otherwise, we recursively examine the node’s children; interactions with particles in leaf nodes are computed directly. The parameter θ controls the trade-off between accuracy and computational efficiency; larger values of θ

result in fewer tree traversals and faster computation at the cost of reduced accuracy. θ ≥ 0.5 typically yields 

O(n log n), while θ = 0 recovers the exact O(n2) computation. 

## 4 A tree-based framework for Bayesian multidimensional scaling 

4.1 Barnes–Hut Bayesian multidimensional scaling 

Given latent embeddings {xi}ni=1 ⊂ R2, we construct a quadtree T (Algorithm B.1) where each node k

corresponds to a spatial cell Ck in the latent space with index set Ik = {i ∈ { 1, . . . , n } : xi ∈ C k}. While leaf nodes contain single observations, internal nodes store pseudo-particles summarizing the number of points ( Nk = |I k|) and the centers of mass of the high-dimensional ( ¯yk = 1

> Nk

∑ 

> l∈I k

yl) and low-dimensional (¯xk = 1

> Nk

∑ 

> l∈I k

xl) points within Ck. The root node ( k = 0 ) summarizes the entire dataset. Unlike t-SNE (van der Maaten, 2014), BMDS (Equation 2) depends on squared differences between high-dimensional and low-dimensional dissimilarities. To preserve the O(n log n) complexity, we approximate the high-dimensional dissimilarity ¯Dik = ∥yi − ¯yk∥ using the Euclidean distance between the observed point 

yi and the pseudo-particle’s high-dimensional center of mass ¯yk. This choice avoids storing or computing 3all pairwise dissimilarities which would require O(n2) memory or operations, at the cost of introducing approximation error. During likelihood computations, we perform a depth-first traversal of quadtree T for each point xi and compute ¯rik = ∥xi − ¯xk∥, the Euclidean distance between the target point xi and the pseudo-particle’s low-dimensional center of mass ¯xk. This quantity is used within the Barnes–Hut criterion to determine tree traversal. The Barnes–Hut BMDS (BH-BMDS) framework reduces the computational burden of evaluating the BMDS log-likelihood through the use of summary statistics; dissimilarities between a target point and points within a sufficiently distant cell can be represented by their aggregate summary. Because each unordered pairwise interaction is visited twice (one per target point), the normalization term is adjusted by a factor of two. The BH-BMDS log-likelihood is 

˜ℓ(D | X, σ 2, T ) = −N log(2 πσ 2) −

> n

∑

> i=1

∑

> k∈B i

Nk

[ ( ¯Dik − ¯rik )2

2σ2 + log Φ 

( ¯rik 

σ

)] 

,

≡ − N log(2 πσ 2) −

> n

∑

> i=1

˜ℓ(xi | T ), (3) where Bi denotes the set of nodes satisfying the Barnes–Hut acceptance criterion (or leaf nodes) for target point xi, and ˜ℓ(xi | T ) is the pointwise contribution to the BH-BMDS log-likelihood from a Barnes–Hut traversal of xi starting at the root node (Algorithm B.3). The BH-BMDS log-likelihood approximates the full BMDS log-likelihood, and when θ = 0 , it reduces to the exact formulation since Bi contains only leaf nodes. 

4.2 Noisy Barnes–Hut algorithm 

Under the Barnes–Hut approximation, the BMDS log-likelihood becomes a piecewise function of the latent positions due to the discrete tree structure. For a fixed set of points, small changes in the embeddings can alter traversal paths or cell assignments, leading to discontinuous changes in the BH-BMDS log-likelihood. Therefore, we introduce a probabilistic acceptance rule to smooth the transition of accepting pseudo-particles near the threshold θ, mitigating abrupt changes in the Barnes–Hut approximation as ρik varies for small movements in xi.Noisy Barnes–Hut follows the same tree traversal as the deterministic version but relaxes the hard acceptance criterion. Instead of the binary indicator I(ρik < θ ), noisy Barnes–Hut treats this decision probabilistically. A pseudo-particle is accepted if u < p (ρik | θ), where u ∼ Unif (0 , 1) and the acceptance probability is defined by the logistic function 

p(ρ | θ) = 

(

1 + exp 

[

− cθ (θ − ρ)

]) −1

. (4) The parameter θ is a user-specified threshold, while c is a constant controlling the slope of the logistic function. We fix c so that the transition between acceptance and rejection occurs over a narrow band of ρik 

values; specifically, p(ρ | θ) = 0 .8 when (θ − ρ)/θ = 0 .2. In our implementation, this corresponds to c = 6 .93 .As c → ∞ , the probability p(ρ | θ) converges to the indicator function I(ρ < θ ), recovering the deterministic rule. The method preserves the expected computational complexity of O(n log n) with occasional additional node evaluations near the acceptance boundary. By treating Bi as a realization of a random process with controlled variance, the stochastic approach improves mixing in MCMC settings (Section 4.4). However, it introduces approximation noise that must be rigorously accounted for. We address this by proving that the stationary measure of BH-BMDS is consistent (Section 4.3). 

4.3 Consistency for noisy BH-BMDS 

We prove consistency for a class of surrogate-likelihood algorithms, even in the regime where the surrogate is not pointwise accurate. See Section A.1 for an expanded discussion of this theoretical framework. 4Fix dimension d ∈ N and noise level σ > 0. For each n ∈ N, we assume the data are generated from "true" latent positions x⋆

> 1

, . . . , x⋆n

> i.i.d.

∼ Unif([0 , 1] d), and the observed dissimilarities Dij are generated according to Model 1 where rij = r⋆ij ≡ ∥∥x⋆i − x⋆j

∥∥. We define the truncation function TM (u) = min {u, M } for u ≥ 0,and a maximum distance M ≥ √d . We then set the capped dissimilarities and their expected values as 

˜Dij = TM (Dij ), μM (r) = E[TM ([ r + σZ ]+)], r ≥ 0 (5) such that the conditional law of ˜Dij given r⋆ij = r has a piecewise density qM,r (y) (Equation A.2). Since the maximum possible distance within this unit hypercube is bounded by √d, the capped distances are consistent with the underlying data-generation process. For a candidate configuration X = ( x1, . . . , xn) ∈ ([0 , 1] d)n and rij (X) = ∥xi − xj ∥, define the loss (negative log-likelihood) as 

ℓM (y, r ) = − log qM,r (y), y ∈ [0 , M ], r ∈ [0 , √d], (6) and the associated empirical risk as 

Rn(X) = 1

N

∑

> 1≤i<j ≤n

ℓM ( ˜Dij , r ij (X)) . (7) We consider a family of surrogate log-likelihoods indexed by ξ ∈ Ξ, where each ξ induces an MCMC algorithm. Let ξ0 ∈ Ξ represent the deterministic reference algorithm, e.g., standard Barnes–Hut. By defining a probability measure on Ξ, we draw a stochastic realization ξ, yielding the randomized surrogate log-likelihood that drives our noisy MCMC. We denote ̂ ℓ(ξ) 

> ij

(X) as the resulting approximation of ℓM ( ˜Dij , r ij (X)) and define the surrogate risk as ̂

R(ξ) 

> n

(X) = 1

N

∑

> 1≤i<j ≤n

̂

ℓ(ξ) 

> ij

(X). (8) Note that ̂ ℓ(ξ) 

> ij

depends on the entirety of X due to the quadtree construction. Since distances are invariant under Euclidean isometries, e.g., rigid motion, we measure convergence using the orbit distance dG defined in Remark A.1. Specifically, we prove that the mass of the stationary measure outside any fixed dG -ball around the true configuration X⋆ goes to 0 (in probability) as n → ∞ . To achieve this, we assume a standard data-generating process characterized by a random design, independent paired noise and sufficient prior regularity around the true configuration (Assumption A.2). The key assumption is 

uniform vanishing error for the pairwise losses :

Assumption 1 (Small Approximation Error) There exists a deterministic sequence εn ↓ 0 such that, with probability → 1 under the joint law of (X⋆, D), the reference approximation ̂ ℓ(ξ0) satisfies 

sup 

> X∈([0 ,1] d)n

max 

> 1≤i<j ≤n

∣∣∣̂ ℓ(ξ0) 

> ij

(X) − ℓM ( ˜Dij , r ij (X)) 

∣∣∣ ≤ εn. (9) While the difference in the pairwise approximation error must go to 0 as n grows, this assumption allows the difference in the total log-likelihood to diverge to infinity, provided the growth remains sub-quadratic. 

Theorem 1 (Consistency for noisy MCMC) Fix d ≥ 1, σ > 0, M ≥ √d and a parameter space Ξ.Suppose there exists a reference parameter ξ0 ∈ Ξ such that the chain Pξ0 is uniformly ergodic and let ̂ ℓ(ξ0)

> ij

be an approximation satisfying Assumption 1. Let Pξ be a Markov chain with stationary measure ̂ Π(ξ) 

> n

,and define the mixture kernel P = ∫ 

> Ξ

Pξ μ(dξ ) for a probability measure μ on Ξ. Let ˜Πn be an invariant probability measure of P and set κn and ∆n as Equations A.10 and A.11, respectively. If κn∆n

> P

−→ 0, then for every δ > 0,

˜Πn

({X : dG (X, X⋆) > δ }) P

−→ 0. (10) 5Remark 1 Theorem 1 applies directly to the deterministic Barnes–Hut algorithm ( ξ = ξ0, Section 3.2, Algorithm B.3) by choosing a sequence θ = θn → 0. More subtly, the result also extend to the noisy quadtree algorithm (Section 4.2, Algorithm B.4) for sufficiently small noise. Because the target distribution is supported on a compact state space and possesses a density strictly bounded away from both zero and in-finity, utilizing any standard proposal distribution with a similarly bounded density ensures that the reference Metropolis-Hastings chain is uniformly ergodic. Furthermore, the one-step total variation distance ∆n is governed by the logistic slope c in the noisy traversal (Equation 4). By letting c → ∞ , ∆n can be made arbitrarily small, fulfilling the final theoretical assumptions. 

Proof Sketch The full proof, detailed in Appendix A, proceeds in two main stages. First, we establish consistency for the deterministic reference ( ξ0). We demonstrate that the baseline empirical risk uniformly concentrates around the expected "population" risk. Because this population risk is strictly minimized at the true configuration X⋆ (up to isometry), the mass of the stationary measure must concentrate around X⋆

in orbit distance. Second, we apply perturbation arguments to show this consistency is preserved under the noisy quadtree algorithm. 

4.4 Bayesian computation 

Algorithm 1: BH-BMDS via Metropolis-within-Gibbs                                                                    

> Input: y1, . . . , yn∈RΩ
> Output: posterior samples for x1, . . . , xn∈R2
> Initialize X0={x0
> i}ni=1 ⊂R2via dimension reduction (e.g., MDS or PCA); Build quadtree Ton X0;
> for each MCMC iteration do
> Permute indices 1, . . . , n ;
> for i= 1 , . . . , n do
> Compute current ˜ℓ(xi| T )using Algorithm B.4; Propose x′
> i∼N(xi, τ 2
> iI);Update Tlocally using Algorithm B.2; Compute proposed ˜ℓ(x′
> i| T )using Algorithm B.4; Accept/reject via MH criterion (Equation 12);
> if rejected then
> Restore cached node summaries in T;
> else
> Keep updated T;Compute ˜ℓ(D|X, σ 2,T)using Algorithm B.3; Propose 1/σ 2′∼N(0 ,∞)(1 /σ 2( s), τ 2( s)1/σ 2);Compute ˜ℓ(D|X, σ 2′,T)using Algorithm B.3; Accept/reject via MH criterion (Equation 12); Rebuild T

To infer the model parameters, we implement aMetropolis-within-Gibbs (MwG) sampler (Algorithm 1) targeting the posterior distribution 

p(X, σ 2 | T ) ∝ ˜ℓ(D | X, σ 2, T )π(X) π(σ2), (11) where π(·) is the prior specification. The sampler proceeds by sequentially updating each model parameter γ = ( x1, . . . , xn, σ 2) using Metropolis– Hastings (MH) steps within a Gibbs framework. At it-eration s and for a generic parameter γk with proposal 

γ′ 

> k

∼ q(· | γ(s) 

> k

), the acceptance probability is 

α(γ′ 

> k

| γ(s), T ) = min 

{

1, p(γ′

> k

, γ (s) 

> −k

| T ) q(γ(s) 

> k

| γ′

> k

)

p(γ(s) 

> k

, γ (s) 

> −k

| T ) q(γ′ 

> k

| γ(s) 

> k

)

}

, (12) where γ−k denotes all parameters except γk. We propose a candidate latent location from x′ 

> n

∼ N(x(s) 

> n

, τ 2( s) 

> n

I) and a candidate precision from a truncated normal proposal, 

1/σ 2 ′ ∼ N(0 ,∞)(1 /σ 2( s), τ 2( s)1/σ 2 ).To efficiently evaluate the posterior, we implement dif-ferent Barnes–Hut approximations depending on the pa-rameter being updated. Latent locations use Algorithm B.4 which evaluates a pointwise contribution to the BH-BMDS likelihood via a stochastic tree traversal. Noisy Barnes–Hut mitigates discontinuities in the likelihood approximation that arise when small changes in xi cause crossings of the deterministic Barnes–Hut acceptance boundary I(ρik < θ ). By replacing this hard threshold with a probabilistic acceptance rule (Equa-tion 4), the resulting likelihood approximation varies more smoothly as a function of xi which increases the stability of Metropolis-Hastings updates. In contrast, the variance parameter σ2 uses deterministic Barnes– Hut to approximate the full BMDS likelihood (Algorithm B.3). This strategy avoids introducing additional noise into likelihood evaluations, thereby improving numerical stability. Under this MwG sampler, for fixed quadtree T , a single latent location update requires O(log n) expected time, and a full sweep over all locations costs O(n log n). We assume proposals are sufficiently local such that 

xi and x′ 

> i

lie in the same quadtree path, so that only center-of-mass updates are required. While running 6experiments on data (Section 5), we found that this assumption holds true, and within a sweep, points’ cell allocations do not change. At the end of each sweep, the quadtree T is rebuilt using the updated latent locations, which also requires O(n log n) time. The cost of updating σ2 is of the same order as a full sweep. 

## 5 Experiments 

We conduct experiments on different datasets to evaluate the performance of Barnes–Hut BMDS. All algo-rithmic results arise from single-core CPU implementations carried out on a single Linux machine with two 26-core Intel Xeon Gold processors (2.1 GHz). Tree related data structures and likelihood computations are implemented in C while MCMC samplers are implemented in Python . For visualization, we use the ggplot2 

(Wickham, 2016) package in R (R Core Team, 2023). 

5.1 Data 

We describe each of the four datasets that span structured mobility data, text embeddings, image represen-tations and neural activity recordings, allowing us to evaluate BH-BMDS across diverse modalities. We also explain our preprocessing step for defining high-dimensional inputs which are mapped to two-dimensional latent representations for BH-BMDS. Dimensions are chosen to balance information retention and compu-tational efficiency. 

Influenza This dataset consists of air traffic-dervied pairwise "effective distances" between n = 5 ,392 

taxa. The taxa belong to four influenza subtypes: 1,370 , 1,389 , 1,393 and 1,240 observations of H1N1, H3N1, Victoria and Yamagata, respectively. Effective distances inversely measure the probability of traveling between airports, such that shorter distances correspond to more heavily trafficked routes. Compared to geometric distances, effective distances better capture the structure of the global mobility network and are therefore more appropriate for modeling disease spread (Brockmann & Helbing, 2013). Since multiple taxa are associated with the same country, the dataset exhibits strong clustering, with only 66 unique inter-country distances. As preprocessing, we apply classical MDS to the dissimilarity matrix to obtain Euclidean embeddings yi ∈ R6.

ArXiv This dataset consists of n = 10 ,000 abstracts collected from arXiv, each labeled with one of five sub-ject areas: math ( math ), astrophysics ( astro-ph ), optics ( physics.optics ), quantum physics ( quant-ph )and computer science / machine learning ( cs/ml ). Each abstract is embedded into 768-dimensional vector 

wi using SentenceTransformers (Reimers & Gurevych, 2019) under the all-mpnet-base-v2 model (Song et al., 2020) in Python . This large language model captures semantic similarity at the sentence level. We compute the pairwise cosine dissimilarities, e.g., 1 − cos( wi, wj ) = 1 − wi·wj  

> ∥wi∥∥ wj∥

for wi ∈ R768 between embeddings and then apply classical MDS to obtain Euclidean embeddings yi ∈ R50 .

MNIST This dataset is a random subset n = 10 ,000 of grayscale handwritten digit images from the MNIST dataset, where each image has dimension d = 28 × 28 = 784 with pixel intensity in [0 , 1] and labels from 0 to 9. To extract meaningful features, we employ a convolutional autoencoder that maps each image to a 30-dimensional latent representation yi ∈ R30 . The model is trained to minimize pixel-wise reconstruction loss using the Adam optimizer in PyTorch (Paszke et al., 2019). The resulting 30-dimensional embeddings serve as the high-dimensional input for BH-BMDS. 

Neural activity Data come from experiments reported in Zwang et al. (2025). This dataset comprises local field potential (LFP) recordings from 32-channel probes implanted in the CA1 hippocampus and medial entorhinal cortex of healthy and ThyTau22 mice. Recordings were captured over six months during a goal-oriented virtual reality task that lasted 15 minutes. We employ a pre-trained MaGNet GNN (Zhou et al., 2025)–trained to decode behavioral states (stalled vs. moving)–to generate high-dimensional latent embeddings yi ∈ R51 , independently across mouse types and weeks. We use BH-BMDS to project these embeddings into a low-dimensional space to evaluate manifold interpretability and identify progressive signs of dementia. 7Figure 2: Posterior Procrustes-aligned means across MCMC iterations and taxa for only H1N1 ( n = 1 ,370 )and all influenza subtypes ( n = 5 ,392 ) under full Bayesian multidimensional scaling (BMDS) and Barnes– Hut Bayesian multidimensional scaling (BH-BMDS) frameworks. 

Figure 3: Posterior Procrustes-aligned means across MCMC iterations for 1,000 and 10 ,000 embedded ArXiv abstracts under full Bayesian multidimensional scaling (BMDS) and Barnes–Hut Bayesian multidimensional scaling (BH-BMDS) frameworks. Figures C.1 and C.2 show analogous results for the MNIST and neural activity datasets. 

5.2 Experimental setup 

For our simulations, we assume that our posterior distribution (Equation 11) is specified with priors 

x1, . . . , xn

> i.i.d.

∼ N (0, I2) and 1/σ 2 ∼ Gamma (1 , 1) , where 1/σ 2 is the BMDS precision parameter. The proposal variances τ 2( s) 

> i

and τ 2( s) 

> σ−2

are adaptively tuned to maintain target acceptance rates, thereby fa-cilitating efficient exploration of the posterior. Let ˆα denote the empirical acceptance rate over a fixed adaptation window and α the target acceptance rate. Define R = ˆ α/α , truncated to lie in [0 .5, 2] . After each adaptation window, the proposal variances are updated via τ (s+1) = Rτ (s). Adaptation is performed every 50 MCMC iterations. We set θ = 2 for all experiments as it empirically provides a favorable trade-off between accuracy and computational efficiency. 

5.3 Accuracy 

Building on the theoretical guarantees for consistent posterior behavior (Theorem 1), we empirically evaluate BH-BMDS across four datasets. We visualize the posterior Procrustes-aligned mean embeddings using every 50th MCMC iteration. Across all benchmarks (Figures 2–C.2), BH-BMDS closely recovers the clustering structure of the exact BMDS, with shared labels consistently co-located. We observe minor coordinate shrinkage toward the origin, a known effect of Gaussian priors when the likelihood approximation introduces additional variance. Notably, exact BMDS results are only provided for smaller subsets where computation is feasible; for larger n, exact inference is prohibitive. These results demonstrate that BH-BMDS provides a faithful, scalable approximation to the full Bayesian formulation. 

5.4 Computational Efficiency 

We evaluate efficiency using effective sample size (ESS) per hour, computed via the coda (Plummer et al., 2006) package in R. ESS inversely reflects the degree of autocorrelation in MCMC samples, with higher values indicating more efficient exploration of the posterior. We calculate ESS using thinned MCMC samples 8Figure 4: 95% empirical intervals of computational efficiency for H1N1, ArXiv, MNIST and ThyTau22 and Wild-Type cohorts at Week 9 datasets. Computational efficiency is measured as the effective sample size (ESS) per hour, in which ESS is a function of asymptotic auto-correlation. We compare efficiency across three frameworks: 1) BMDS log-likelihood and gradient using HMC (HMC), 2) BMDS log-likelihood using MwG (BMDS) and 3) BH-BMDS log-likelihood using MwG (BH-BMDS, Algorithm 1). ThyTau22 has a lower ESS per hour compared to Wild-Type for similar number of points due to weaker clustering. 

Figure 5: 95% empirical intervals of computational efficiency for influenza, ArXiv and MNIST datasets. Computational efficiency is measured as the effective sample size (ESS) per hour, in which ESS is a function of asymptotic auto-correlation. Since efficiency would be infeasible to compute for such large data under BMDS, we provide a theoretical value. Time at n = 10 ,000 is predicted from a quadratic regression using observed times at n = 100 , 500 , 1,000 , 5,000 while ESS is generously assumed to be perfect. BMDS* equals 4.5, 1.0 and 0.7 for influenza, MNIST and ArXiv, respectively. (retaining every 50th iteration) on a random subset of 1,000 pairwise latent distances to avoid identifiability issues inherent in raw locations. Figure 4 compares the computational efficiency across three inference frameworks: Hamiltonian Monte Carlo (HMC) and MwG for exact BMDS and Algorithm 1 for BH-BMDS and five datasets: H1N1, ArXiv, MNIST and the ThyTau22 and Wild-Type mouse at Week 9. HMC efficiently explores the posterior by leveraging gradient information but incurs additional computational cost due to gradient evaluations (Neal, 2012). In our implementation, HMC jointly samples latent locations under the full BMDS likelihood using 20 leapfrog steps. BMDS and BH-BMDS follow Algorithm 1 with θ = 0 and θ = 2 , respectively. For BH-BMDS, we repeat each experiment four times to account for variability induced by the quadtree-based likelihood approximation. As shown in Figure 4, BH-BMDS achieves approximately 60-fold speedup relative to HMC and 9-fold speedup relative to BMDS. Gains are most pronounced in datasets with distinct clustering; for instance, the ThyTau22 cohort shows slightly lower relative efficiency than Wild-Type due to more diffuse posterior mass (Figure C.2). As exact BMDS is infeasible at scale, we extrapolate its performance to the full datasets, generously assuming idealized sampling efficiency by setting ESS = S for S retained (thinned) samples. For the influenza dataset, we use observed BMDS runtimes; for the ArXiv and MNIST datasets, we estimate BMDS runtime at n = 10 ,000 via a quadratic regression on benchmarked runtimes ( n ∈ { 100 , 500 , 1,000 and 5,000 }). Even with this optimistic assumption, BH-BMDS achieves substantial gains: approximately 27-fold speedup for influenza, 67-fold for MNIST and 93-fold for ArXiv (Figure 5). These gains increase with n, consistent with the improved scaling behavior of the BH-BMDS approximation. 96 Discussion 

We propose a scalable approximation to the BMDS likelihood using Barnes–Hut algorithms (BH-BMDS), reducing computational complexity from O(n2) to O(n log n). By embedding this approximation within a Metropolis-within-Gibbs sampler, our approach enables efficient posterior inference while maintaining a probabilistic framework. Crucially, we use a novel approach (Section A.1) to establish consistency of the stationary measure for BH-BMDS and evaluate the proposed method on datasets with diverse clustering patterns. Several limitations suggest directions for future work. First, our inference scheme relies on coordinate-wise updates of latent locations, which may mix slowly in settings with strong dependence across points. We ex-plored joint updates using HMC, discontinuous HMC and Metropolis–Hastings but encountered difficulties achieving both stable convergence and favorable scaling for larger n. More broadly, these findings high-light the challenges of combining discrete approximation schemes with MCMC methods. Second, the noisy Barnes–Hut approximation (Section 4.2) introduces stochasticity through the traversal criterion. While this improves mixing by mitigating abrupt changes in the approximated likelihood, it also complicates the theoretical scaling behavior and may deviate from ideal O(n log n) performance, although such deviations were not observed in practice. Developing more stable approximation schemes that preserve both accuracy and computational guarantees is an important direction for future work. Finally, as with standard BMDS, low-dimensional embeddings may not capture all relevant structure in complex datasets, particularly when restricted to two dimensions. Extending BH-BMDS to higher-dimensional embeddings or adaptive represen-tations is a promising avenue for future work. 

Acknowledgments 

This work was supported by the NSF (DMS 2152774 and DMS 2236854) and NSERC (RGPIN-2022-03012). We also thank Ryan O’Dell, a PhD student at the University of California, Los Angeles, for implementing MaGNet (Zhou et al., 2025) and providing the high-dimensional embeddings on the neural activity recordings. 

## References 

Pierre Alquier, Nial Friel, Richard Everitt, and Aidan Boland. Noisy monte carlo: convergence of markov chains with approximate transition kernels. Statistics and Computing , 26:29–47, 2016. Ery Arias-Castro, Adel Javanmard, and Bruno Pelletier. Perturbation bounds for Procrustes, classical scaling, and trilateration, with applications to manifold learning. Journal of Machine Learning Research ,21(15):1–37, 2020. Josh Barnes and Piet Hut. A hierarchical o(nlogn) force-calculation algorithm. Nature , 324:446–449, 1986. Trevor Bedford, Steven Riley, Ian G. Barr, Shobha Broor, Mandeep Chadha, Nancy J. Cox, Rodney S. Daniels, C. Palani Gunasekara, Aeron C. Hurt, Anne Kelso, Alexander Klimov, Nicola S. Lewis, Xiyan Li, John W. McCauley, Takato Odagiri, Varsha Potdar, Andrew Rambaut, Yuelong Shu, Eugene Skepner, Derek J. Smith, Marc A. Suchard, Masato Tashiro, Dayan Wang, Xiyan Xu, Philippe Lemey, and Colin A. Russell. Global circulation patterns of seasonal influenza virsuses vary with antigentic drift. Nature , 523: 217–220, 2015. Sebastian Bieringer, Gregor Kasieczka, Maximilian F. Steffen, and Mathias Trabs. The surrogate Gibbs-posterior of a corrected stochastic MALA: Towards uncertainty quantification for neural networks. Journal of Machine Learning Research , 27(1):1–50, 2026. URL https://www.jmlr.org/papers/v27/22-0483. html .Dirk Brockmann and Dirk Helbing. The hidden geometry of complex, network-driven contagion phenomena. 

Science , 342(6164):1337–1342, 2013. Andrew J. Holbrook, Philippe Lemey, Guy Baele, Simon Dellicour, Dirk Brockmann, Andrew Rambaut, and Marc A. Suchard. Massive parallelization boosts big bayesian multidimensional scaling. Journal of Computational and Graphical Statistics , 30(1):11–24, 2021. 10 Roger A. Horn and Charles R. Johnson. Matrix Analysis . Cambridge University Press, 2 edition, 2013. Harold Hotelling. Analysis of a complex of statistical variables into principal components. Journal of Educational Psychology , 24(6):417–441, 1933. YQ Li, M Ghafari, AJ Holbrook, I Boonen, N Amor, S Catalano, JP Webster, YY Li, HT Li, V Vergote, P Maes, YL Chong, A Laudisoit, P Baelo, S Ngoy, SG Mbalitini, GC Gembu, AP Musaba, J Goüy de Bellocq, H Leirs, E Verheyen, OG Pybus, A Katzourakis, AN Alagaili, S Gryseels, YC Li, MA Suchard, M Bletsa, and P Lemey. The evolutionary history of hepaciviruses. bioRxiv : the preprint server for biology , 2023. Lin Lin and Duncan K.H. Fong. Bayesian multidimensional scaling procedure with variable selection. Com-putational Statistics & Data Analysis , 129:1–13, 2019. Leland McInnes, John Healy, Nathaniel Saul, and Lukas Großberger. Umap: Uniform manifold approxima-tion and projection. Journal of Open Source Software , 3(29):861, 2018. F. J. Medina-Aguayo, Anthony Lee, and Gareth O. Roberts. Stability of noisy Metropolis–Hastings. Statistics and Computing , 26:1187–1211, 2016. A. Yu. Mitrophanov. Sensitivity and convergence of uniformly ergodic markov chains. Journal of Applied Probability , 42(4):1003–1014, 2005. Radford M. Neal. MCMC using Hamiltonian dynamics , chapter 5. Campman & Hall, 2012. Man-Suk Oh and Adrian E. Raftery. Bayesian multidimensional scaling and choice of dimension. Journal of the American Statistical Association , 96(455):1031–1044, 2001. Man-Suk Oh and Adrian E. Raftery. Model-based clustering with dissimilarities: A bayesian approach. 

Journal of Computational and Graphical Statistics , 16(3):559–585, 2007. Adam Paszke, Sam Gross, Francisco Massa, Adam Lerer, James Bradbury, Gregory Chanan, Trevor Killeen, Zeming Lin, Natalia Gimelshein, Luca Antiga, Alban Desmaison, Andreas Köpf, Edward Yang, Zach DeVito, Martin Raison, Alykhan Tejani, Sasank Chilamkurthy, Benoit Steiner, Lu Fang, Junjie Bai, and Soumith Chintala. Pytorch: An imperative style, high-performance deep learning library, 2019. URL 

https://arxiv.org/abs/1912.01703 .Karl Pearson. Liii. on lines and planes of closest fit to systems of points in space. The London, Edinburgh, and Dublin Philosophical Magazine and Journal of Science , 2(11):559–572, 1901. Martyn Plummer, Nicky Best, Kate Cowles, and Karen Vines. Coda: Convergence diagnosis and output analysis for mcmc. R News , 6(1):7–11, 2006. URL https://journal.r-project.org/archive/ .R Core Team. R: A Language and Environment for Statistical Computing . R Foundation for Statistical Computing, Vienna, Austria, 2023. URL https://www.R-project.org/ .Riccardo Rastelli, Florian Maire, and Nial Friel. Computationally efficient inference for latent position network models. Electronic Journal of Statistics , 18(1):2531 – 2570, 2024. doi: 10.1214/24-EJS2256. URL 

https://doi.org/10.1214/24-EJS2256 .Nils Reimers and Iryna Gurevych. Sentence-bert: Sentence embeddings using siamese bert-networks. In 

Proceedings of the 2019 Conference on Empirical Methods in Natural Language Processing . Association for Computational Linguistics, 11 2019. URL https://arxiv.org/abs/1908.10084 .Daniel Rudolf and Nikolaus Schweizer. Perturbation theory for markov chains via Wasserstein distance. 

Bernoulli , 24(4A):2610–2639, 2018. Ami Sheth, Aaron Smith, and Andrew J. Holbrook. Sparse bayesian multidimensional scaling(s). Computa-tional Statistics , 2026. 11 Kaitao Song, Xu Tan, Tao Qin, Jianfeng Lu, and Tie-Yan Liu. Mpnet: Masked and permuted pre-training for language understanding. arXiv preprint arXiv:2004.09297 , 2020. Yee Whye Teh, Alexandre H. Thiery, and Sebastian J. Vollmer. Consistency and fluctuations for stochastic gradient langevin dynamics. Journal of Machine Learning Research , 17(7):1–33, 2016. Warren S. Torgerson. Multidimensional scaling: I. theory and method. Psychometrika , 17(4):401–419, 1952. Laurens van der Maaten. Accelerating t-sne using tree-based algorithms. Journal of Machine Learning Research , 15:1–21, 2014. Laurens van der Maaten and Geoffery Hinton. Visualizing data using t-sne. Journal of Machine Learning Research , 9:2579–2605, 2008. Roman Vershynin. High-dimensional probability: An introduction with applications in data science, 2025. Second edition. Prepublication PDF dated Nov. 28, 2025. Hadley Wickham. ggplot2: Elegant Graphics for Data Analysis . Springer-Verlag New York, 2016. ISBN 978-3-319-24277-4. URL https://ggplot2.tidyverse.org .Wenzhuo Zhou, Annie Qu, Keiland W. Cooper, Norbert Fortin, and Babak Shahbaba. A model-agnostic graph neural network for integrating local and global information. Journal of the American Statistical Association , 120(550):1225–1238, 2025. Theodore J Zwang, Jasen Zhang, Rudy Gelb-Bicknell, Nina Wolf, Viktor Chanchykov, Haoyang Zhu, Ergina Agastra, Ava Devine, Andrew J. Holbrook, Rachel E. Bennett, and Bradley T. Hyman. Tau drives cell specific functional isolation of the hippocampal formation. bioRxiv , 2025. doi: 10.1101/2025.08.10.669580. 

## A Consistency of approximate BMDS 

We restate and then prove Theorem 1 from the main document. We begin by recalling some basic notation from Section 4.3. Throughout Appendix A, fix d ≥ 1, σ > 0, and 

M ≥ √d. We use the shorthand N = (n

> 2

), J = I − n−111 ⊤, and 

r⋆ij = ∥∥x⋆i − x⋆j

∥∥ , rij (X) = ∥xi − xj ∥ .

Our overall goal is to infer the latent positions x⋆

> 1

, x⋆

> 2

, . . . , x⋆n ∈ [0 , 1] d, which we write in matrix form as X⋆

given the observed dissimilarity matrix ˜Dij ∈ [0 , M ]. For any matrix X or the actual latent positions X⋆,define the centered configurations Xc = JX and X⋆c = JX ⋆.Relative to the reference measure 

ν = δ0 + λ|(0 ,M ) + δM (A.1) on [0 , M ], the conditional law of ˜Dij given r⋆ij = r has density 

qM,r (y) = 



Φ( −r/σ ), y = 0 ,

1

σ ϕ

( y − rσ

)

, 0 < y < M, 

1 − Φ

( M − rσ

)

, y = M, 

(A.2) where λ is the Lebesgue measure and ϕ, Φ denote the standard normal density and CDF, respectively. 12 We also recall the following notation from Section 4.3: the loss ℓM (y, r ) = − log qM,r (y) (Equation 6), the empirical risk Rn(X) = N −1 ∑ 

> i<j

ℓM ( ˜Dij , r ij (X)) (Equation 7), and for an algorithmic realization ξ, the surrogate risk ̂ R(ξ) 

> n

(X) = N −1 ∑

> i<j

̂ ℓ(ξ) 

> ij

(X) (Equation 8). These yield the exact posterior 

Πn(dX | D) ∝ Π0(dX) exp {− N R n(X)} (A.3) and the surrogate posterior ̂

Π(ξ) 

> n

(dX | D) ∝ Π0(dX) exp {− N̂ R(ξ) 

> n

(X)}. (A.4) Since distances are invariant under Euclidean isometries, the posterior distribution cannot possibly be con-sistent under the Euclidean metric. Instead, we prove consistency in the following "orbit distance" obtained by taking the Frobenius distance ∥A∥F = ( ∑ 

> i,j

A2 

> ij

)1/2 and "modding out" by isometries: 

Definition A.1 (Orbital distance) Let G = {(Q, b) : Q ∈ O (d), b ∈ Rd} act on configurations via 

(Q, b) · xi = Qx i + b. Define the orbit distance 

dG (X, X⋆) = inf 

> (Q,b)∈G

1

√n

∥∥X − (X⋆Q + 1b ⊤)∥∥F = inf 

> Q∈O (d)

1

√n ∥Xc − X⋆c Q∥F . (A.5) Before proving our main result (Theorem 1), we will first establish posterior consistency for the baseline algorithm with parameter ξ = ξ0. For notational simplicity, we drop the algorithmic superscript until Section A.7 and let ̂ ℓij ≡̂ ℓ(ξ0) 

> ij

,̂ Rn ≡̂ R(ξ0) 

> n

, and ̂ Πn ≡̂ Π(ξ0) 

> n

denote the surrogate quantities under the baseline. The key assumption is that the surrogate likelihoods have errors going to 0: 

Assumption A.1 (Small Approximation Error) There exists a deterministic sequence εn ↓ 0 such that, with probability → 1 under the joint law of (X⋆, D),

sup 

> X∈([0 ,1] d)n

max 

> 1≤i<j ≤n

∣∣∣̂ ℓij (X) − ℓM ( ˜Dij , r ij (X)) 

∣∣∣ ≤ εn. (A.6) Note that, on the high-probability event (Equation A.6), the error in the risk is also small: 

sup 

> X∈([0 ,1] d)n

∣∣̂ Rn(X) − Rn(X)∣∣ ≤ εn. (A.7) We also restate the assumptions about the model: 

Assumption A.2 (Nice Data-Generating Process) The data generating process and prior satisfy: (T1) (Random design) x⋆

> 1

, . . . , x⋆n

> i.i.d.

∼ Unif([0 , 1] d).(T2) (Independent pair noise) Conditional on X⋆, the noises {Zij }i<j are i.i.d. N (0 , 1) .(T3) (Prior regularity) The prior Π0 is supported on ([0 , 1] d)n and has density π0 satisfying e−cn ≤

π0(X) ≤ ecn on ([0 , 1] d)n, for some constant c > 0.

For the remainder of Appendix A, all probabilistic statements involving the data-generating law are under-stood under Assumption A.2, unless otherwise stated. Statements about the baseline surrogate posterior are understood in addition under Assumption A.1 or the high-probability event (Equation A.7), as indicated. 

Proposition A.1 (Consistency of the Baseline Algorithm) Fix d ≥ 1, σ > 0, and M ≥ √d. Con-sider Model (Equation 1) where rij = r⋆ij , the exact posterior (Equation A.3) based on ℓM in Equation 6, 

13 and the approximate posterior ̂ Πn (Equation A.4). Suppose Assumptions A.1 and A.2 hold. Then, for every 

δ > 0,̂

Πn

({X : dG (X, X⋆) > δ } ∣∣∣ D

) P

−→ 0. (A.8) 

In particular, the approximate posterior of the baseline algorithm is consistent for the latent configuration up to rigid motion. 

We restate Theorem 1 slightly more formally: 

Theorem A.1 (Theorem 1, Extended) Fix d ≥ 1, σ > 0, M ≥ √d and a parameter space Ξ. Suppose Assumption A.2 holds, and suppose there exists a reference parameter ξ0 ∈ Ξ such that the chain Pξ0 on 

([0 , 1] d)n is uniformly ergodic and ̂ ℓ(ξ0) 

> ij

satisfies Assumption A.1. For each ξ ∈ Ξ, let Pξ be a Markov chain on ([0 , 1] d)n with stationary measure ̂ Π(ξ) 

> n

, and define the mixture kernel P = ∫ 

> Ξ

Pξ μ(dξ ) for a probability measure μ on Ξ. Let ˜Πn be an invariant probability measure of P .Suppose that there exist deterministic constants Cn < ∞ and ρn ∈ (0 , 1) such that 

sup 

> X0∈([0 ,1] d)n

∥∥δX0 P mξ0 −̂ Π(ξ0)

> n

∥∥TV ≤ Cnρmn , m ≥ 0. (A.9) 

Set 

λn = max 

{

0,

⌈ log(1 /C n)log( ρn)

⌉} 

, κn = λn + Cnρλn

> n

1 − ρn

, (A.10) 

and assume that the possibly data-dependent quantity 

∆n = sup 

> ξ∈Ξ

sup 

> X0∈([0 ,1] d)n

∥∥Pξ (X0, ·) − Pξ0 (X0, ·)∥∥TV (A.11) 

satisfies κn∆n

> P

−→ 0.Then for every δ > 0,

˜Πn

({X : dG (X, X⋆) > δ }) P

−→ 0. (A.12) 

A.1 Proof strategy for Proposition A.1 

The basic idea is: 1. Check that the empirical risk Rn concentrates near an associated "population" risk R0 

> n

(Section A.3), 2. Check that the population risk R0

> n

(X) grows quadratically in the orbit (not Euclidean) distance to the truth X⋆, with reasonable constant (Section A.4), 3. Check that the posterior associated with the population risk is consistent (Section A.5). We note that this approach is not the most common approach to analyzing the error of "approximate" or "surrogate" MCMC algorithms. Typically, one aims to have a surrogate that is pointwise close to the target log-likelihood, then use perturbation theory to show that the stationary measure is close to the posterior distribution of interest (e.g., Rastelli et al. (2024) for a typical analysis, applied to a different MCMC algorithm for inferring latent positions). In the present paper, we allow the surrogate to be very far from the log-likelihood pointwise, as long as the error grows only sub-quadratically in the number of points n.We are aware of several other results that prove consistency of MCMC algorithms even as the pointwise error is allowed to grow, though only in the much simpler situation of i.i.d. samples and a finite-dimensional set of parameters to be learned. The influential analysis of SGLD in Teh et al. (2016) is an early example, and this viewpoint is still being refined through works such as Bieringer et al. (2026). We emphasize the following differences between our analysis and most or all previous work: 14 1. Directly analyzing the risk is always more difficult than merely treating an approximate MCMC chain as a perturbation of an ideal chain, where more generic results are available (Mitrophanov, 2005; Alquier et al., 2016; Medina-Aguayo et al., 2016; Rudolf & Schweizer, 2018). 2. The current problem is effectively infinite-dimensional, since the number of latent positions to learn grows with the size of the dataset. For this reason, naive bounds based on, essentially, Taylor expansions and concentration do not work. Compare this to the more classical finite-dimensional analyses of stochastic-gradient MCMC in Teh et al. (2016). 3. The ground truth is only identifiable up to an isometry, so all estimates need to effectively "mod out" by this isometry. 

A.2 A standard matrix inequality 

Lemma 1 Define the linear "double-centering" operator C(A) = JAJ on Rn×n, where J = I − 1 

> n

11 ⊤ is the orthogonal projector onto 1⊥. We have the operator norm bound 

∥C∥ F →F ≤ 1.

Proof 1 Note J⊤ = J, J2 = J and ∥J∥2 = 1 . Using the standard inequality ∥UAV ∥F ≤ ∥ U∥2∥A∥F ∥V∥2

(e.g., the chapter on matrix norms Horn & Johnson (2013)), we obtain for all A that 

∥JAJ ∥F ≤ ∥ J∥22∥A∥F = ∥A∥F ,

so ∥C∥ F →F ≤ 1.

A.3 Uniform concentration of the empirical risk 

Define the conditional (given X⋆) population risk 

R0

> n

(X) = E[Rn(X) | X⋆] = 1

N

∑

> i<j

g(rij (X); r⋆ij 

), (A.13) where, for Z ∼ N (0 , 1) ,

g(r; r⋆) = E

[

ℓM

(TM ([ r⋆ + σZ ]+), r )]

. (A.14) 

Lemma 2 (Uniform concentration) There exist constants C1 = C1(d, σ, M ) > 0, C2 = C2(d, σ, M ) > 0,and c = c(d, σ, M ) > 0 such that, for all 0 < t < C 1,

P

(

sup 

> X∈([0 ,1] d)n

∣∣Rn(X) − R0

> n

(X)∣∣ > t

∣∣∣ X⋆)

≤ 2 exp 

(

C2 nd log 

( C1

t

)

− c N t 2)

.

Proof 2 Fix X. Conditional on X⋆, the random variables 

Yij (X) = ℓM ( ˜Dij , r ij (X)) 

are independent across pairs (i, j ).On 0 < y < M we have |y − r| ≤ M , while at the atoms 

qM,r (0) = Φ( −r/σ ) ≥ Φ( −√d/σ ), qM,r (M ) = 1 − Φ(( M − r)/σ ) ≥ Φ( −M/σ ).

Thus, because r ∈ [0 , √d] and y ∈ [0 , M ], the density qM,r (y) is uniformly bounded above and below by constants 0 < q − ≤ q+ < ∞ depending only on (d, σ, M ). Hence 

Yij (X) ∈ [− log q+, − log q−] (A.15) 15 almost surely. By Equation A.15, writing Wℓ = log( q+/q −), Hoeffding’s inequality gives 

P

(∣∣Rn(X) − R0

> n

(X)∣∣ > t 

∣∣∣ X⋆)

≤ 2 exp 

(

− 2N t 2

W 2

> ℓ

)

. (A.16) 

To make the bound uniform in X, we use a net argument on ([0 , 1] d)n. Equip this space with the sup-norm on rows metric ∥X − X′∥∞ = max i ∥xi − x′

> i

∥. It is well-known (Vershynin, 2025, Corollary 4.2.11) that there exists a universal 0 < C 0 < ∞ so that, for any fixed η > 0, there exists an η-net Nη satisfying 

|N η | ≤

( C0

η

)nd 

. (A.17) 

Next, note that since X, X′ ∈ ([0 , 1] d)n, we have that rij (X) is 2-Lipschitz in ∥·∥ ∞. Also, for each fixed 

y ∈ [0 , M ], the map r 7 → ℓM (y, r ) is C1 on [0 , √d], with derivative 

∂∂r ℓM (y, r ) = 



ϕ(r/σ )

σΦ( −r/σ ) , y = 0 ,r − yσ2 , 0 < y < M, 

− ϕ(( M − r)/σ )

σ(1 − Φ(( M − r)/σ )) , y = M. 

Each of these three branches is uniformly bounded on the relevant compact interval, so there exists Lℓ =

Lℓ(d, σ, M ) < ∞ such that 

|ℓM (y, r ) − ℓM (y, r ′)| ≤ Lℓ|r − r′|, y ∈ [0 , M ], r, r ′ ∈ [0 , √d].

Combining this with the fact that rij (X) is 2-Lipschitz in ∥·∥ ∞, we have 

|ℓM ( ˜Dij , r ij (X)) − ℓM ( ˜Dij , r ij (X′)) | ≤ 2Lℓ ∥X − X′∥∞ . (A.18) 

Averaging over pairs gives 

|Rn(X) − Rn(X′)| ≤ 2Lℓ ∥X − X′∥∞ . (A.19) 

Since R0

> n

(X) is a conditional expectation of Rn(X), we have the same bound: 

|R0

> n

(X) − R0

> n

(X′)| ≤ 2Lℓ ∥X − X′∥∞ . (A.20) 

Pick η = t/ (8 Lℓ). For any X, choose X′ ∈ N η with ∥X − X′∥∞ ≤ η. By the triangle inequality, Equa-tion A.19 and Equation A.20, 

|Rn(X) − R0

> n

(X)| ≤ | Rn(X′) − R0

> n

(X′)| + |Rn(X) − Rn(X′)| + |R0

> n

(X) − R0

> n

(X′)|≤ | Rn(X′) − R0

> n

(X′)| + 4 Lℓη = |Rn(X′) − R0

> n

(X′)| + t

2 . (A.21) 

Therefore, by Equation A.21, a union bound over Nη together with Equation A.16 applied at level t/ 2 gives 

P

(

sup 

> X∈([0 ,1] d)n

|Rn(X) − R0

> n

(X)| > t 

∣∣∣ X⋆)

≤ 2|N η | exp 

(

− N t 2

2W 2

> ℓ

)

.

Substituting Equation A.17 and η = t/ (8 Lℓ) and then enlarging constants gives the conclusion for all suffi-ciently small t.

A.4 A quadratic lower bound for the population risk 

We show R0

> n

(X) is minimized (up to isometry) at X⋆, and furthermore that the risk increases quadratically as you move away from X⋆. More precisely, we will show 

Proposition A.2 There exists cmg = cmg (d, σ, M ) > 0 such that the following holds (uniformly in X) with probability going to 1 as n goes to infinity: 

R0

> n

(X) − R0

> n

(X⋆) ≥ cmg dG (X, X⋆)2, ∀X ∈ ([0 , 1] d)n. (A.22) This is the main result in this section. The proof is deferred to the end, in sub-section A.4.4. 16 A.4.1 Initial quadratic lower bound in the distance 

Lemma 3 There exists csc = csc (σ, M, d ) > 0 such that for all r, r ⋆ ∈ [0 , √d],

g(r; r⋆) − g(r⋆; r⋆) ≥ csc (r − r⋆)2,

where g is defined in Equation A.14. 

Proof 3 For fixed r⋆, let Pr denote the law of ˜D = TM ([ r + σZ ]+), with density qM,r with respect to ν;equivalently, Pr (A) = ∫ 

> A

qM,r (y) ν(dy ) for measurable A ⊆ [0 , M ]. Thus the observation generated at the true distance r⋆ has law Pr⋆ , while evaluating the loss at a candidate distance r uses the density qM,r . Therefore, by the definition of g in Equation A.14, 

g(r; r⋆) − g(r⋆; r⋆) = 

∫

qM,r ⋆ (y) log 

( qM,r ⋆ (y)

qM,r (y)

)

ν(dy ) = KL( Pr⋆ ∥ Pr ). (A.23) 

By Pinsker’s inequality and Equation A.23, 

g(r; r⋆) − g(r⋆; r⋆) ≥ 2∥Pr⋆ − Pr ∥2TV . (A.24) 

Recalling the definition of μM from Equation 5, we have 

|μM (r) − μM (r⋆)| =

∣∣∣∣∣∫ M

> 0

y (qM,r (y) − qM,r ⋆ (y)) ν(dy )

∣∣∣∣∣

≤ M

∫ M

> 0

∣∣qM,r (y) − qM,r ⋆ (y)∣∣ ν(dy )= 2 M ∥Pr − Pr⋆ ∥TV . (A.25) 

Combining Equation A.24 and Equation A.25 gives 

g(r; r⋆) − g(r⋆; r⋆) ≥

(μM (r) − μM (r⋆))2

2M 2 . (A.26) 

For each fixed Z, the map r 7 → TM ([ r + σZ ]+) is piecewise linear with derivative 1{0 < r + σZ < M } for almost every r, so dominated convergence yields 

μ′ 

> M

(r) = P(0 < r + σZ < M ) = Φ 

( M − rσ

)

− Φ

(

− rσ

)

. (A.27) 

Hence, for r ∈ [0 , √d],

μ′ 

> M

(r) = Φ 

( M − rσ

)

+ Φ 

( rσ

)

− 1 ≥ Φ

( M

2σ

)

− 12 =: mM . (A.28) 

We note that mM > 0, since r ∈ [0 , M ] and therefore at least one of r and M − r is at least M/ 2. By the mean value theorem and Equation A.28, 

|μM (r) − μM (r⋆)| ≥ mM |r − r⋆|. (A.29) 

Substituting Equation A.29 into Equation A.26 proves the claim, with csc = m2 

> M

/(2 M 2).

A.4.2 From pairwise distance error to Procrustes error 

Define the average squared distance discrepancy 

∆dist (X, X⋆) = 1

N

∑

> i<j

(rij (X) − r⋆ij 

)2.

We have the straightforward estimate: 17 Lemma 4 (Population risk dominates distance discrepancy) Under the model 1 and loss (Equa-tion 6), 

R0

> n

(X) − R0

> n

(X⋆) ≥ csc ∆dist (X, X⋆),

where csc is as in Lemma 3. 

Proof 4 By Lemma 3, for each pair (i, j ),

g(rij (X); r⋆ij 

) − g(r⋆ij ; r⋆ij 

) ≥ csc 

(rij (X) − r⋆ij 

)2.

Averaging over pairs yields the result. 

Next we relate ∆dist (X, X⋆) to dG (X, X⋆) using a standard Procrustes perturbation bound. Recall that 

Xc = JX and X⋆c = JX ⋆ denote the centered configurations. We will use the following consequence of Theorem 1 Arias-Castro et al. (2020) as our basic Procrustes perturbation bound: with ε2 = ∥∥YY ⊤ − XX ⊤∥∥F , if ∥∥X†∥∥op ε ≤ 1/√2, then 

min  

> Q∈O (d)

∥Y − XQ ∥F ≤ (1 + √2) ∥∥X†∥∥op 

∥∥YY ⊤ − XX ⊤∥∥F .

Note that the theorem in that paper looks slightly different and is quite a bit more complicated; this follows immediately from the second branch in (Arias-Castro et al., 2020, Theorem 1, inequality (5)) after choosing Schatten norm p = 2 , simplifying and rephrasing in our notation. 

Lemma 5 For every α > 0, there exists c2 = c2(α, d ) > 0 such that, if 

smin (X⋆c ) ≥ α√n, (A.30) 

then 

dG (X, X⋆)2 ≤ c2 ∆dist (X, X⋆), ∀X ∈ ([0 , 1] d)n.

Proof 5 We follow Arias-Castro et al. (2020) and work with centered Gram matrices. Denote by D2(X)

the squared-distance matrix with entries D2(X)ij = ∥xi − xj ∥2. With J as above, the centered Gram matrix is 

G(X) = − 12 JD2(X)J = XcX⊤ 

> c

.

We begin by bounding G(X) − G(X⋆) in terms of ∆dist (X, X⋆). By Lemma 1, the Frobenius–Frobenius operator norm of A 7 → JAJ is 1, so 

∥G(X) − G(X⋆)∥F = 12

∥∥J(D2(X) − D2(X⋆))J∥∥F ≤ 12

∥∥D2(X) − D2(X⋆)∥∥F . (A.31) 

For r, r ⋆ ∈ [0 , √d] we have 

|r2 − (r⋆)2| = |r − r⋆| (r + r⋆) ≤ 2√d |r − r⋆|. (A.32) 

Therefore, by Equation A.32, 

∥∥D2(X) − D2(X⋆)∥∥2 

> F

= ∑

> i̸=j

(rij (X)2 − (r⋆ij )2)2

≤ 4d ∑

> i̸=j

(rij (X) − r⋆ij 

)2 (A.33) 

= 8 d ∑

> i<j

(rij (X) − r⋆ij 

)2

= 8 dN ∆dist (X, X⋆). (A.34) 18 Combining Equation A.31 and Equation A.33 gives 

∥G(X) − G(X⋆)∥F ≤ √2dN ∆dist (X, X⋆). (A.35) 

Let 

ε = ∥G(X) − G(X⋆)∥1/2 

> F

. (A.36) 

We now apply (Arias-Castro et al., 2020, Theorem 1) with X = X⋆c and Y = Xc and Schatten norm 

p = 2 (which is equal to the Frobenius norm). In the notation of that theorem, we have YY ⊤ − XX ⊤ =

G(X) − G(X⋆) and hence ε2 = ∥YY ⊤ − XX ⊤∥F . Using the second branch in the statement of (Arias-Castro et al., 2020, Theorem 1, inequality (5)), if 

∥(X⋆c )†∥op ε ≤ 1

√2 , (A.37) 

then 

min  

> Q∈O (d)

∥Xc − X⋆c Q∥F ≤ (1 + √2) ∥(X⋆c )†∥op ∥G(X) − G(X⋆)∥F . (A.38) 

We now give a sufficient condition for Equation A.37. Under Equation A.30, we have ∥(X⋆c )†∥op =1/s min (X⋆c ) ≤ 1/(α√n). Moreover, since N = (n

> 2

) ≤ n2/2, Equations A.35–A.36 imply 

ε ≤ (2dN ∆dist (X, X⋆))1/4 ≤ d1/4n1/2 ∆dist (X, X⋆)1/4. (A.39) 

Hence, by Equation A.30 and Equation A.39, Equation A.37 holds whenever 

∆dist (X, X⋆) ≤ α4

4d . (A.40) 

We now obtain our bound in two cases: when Equation A.40 holds, and when it fails. We begin by assuming it holds. Combining Equation A.38 with Equation A.35 and Equation A.30 gives 

1

√n min  

> Q∈O (d)

∥Xc − X⋆c Q∥F ≤ 1 + √2

αn 

√2dN ∆dist (X, X⋆)

≤ 1 + √2

α

√d ∆dist (X, X⋆). (A.41) 

By the definition of dG in Equation A.5, the left-hand side equals dG (X, X⋆), so squaring Equation A.41 yields 

dG (X, X⋆)2 ≤ (1 + √2) 2

α2 d ∆dist (X, X⋆) whenever Equation A.40 holds. (A.42) 

Finally, we consider the case that Equation A.40 fails. In this case, ∆dist (X, X⋆) ≥ α4/(4 d). Since xi, x⋆i ∈

[0 , 1] d, choosing Q = I and b = 0 in the uncentered definition of the orbit distance gives the trivial bound 

dG (X, X⋆)2 ≤ d. Therefore, 

dG (X, X⋆)2 ≤ d ≤ 4d2

α4 ∆dist (X, X⋆) whenever Equation A.40 fails. (A.43) 

Taking 

c2 = max 

{ (1 + √2) 2

α2 d, 4d2

α4

}

and combining Equation A.42 and Equation A.43 proves the lemma. 

19 A.4.3 Well-conditioning holds with high probability for uniform points 

Lemma 6 Let x⋆iiid 

∼ Unif([0 , 1] d), let X⋆ be the associated matrix of points, and let X⋆c be its centered version. Then there exists c1 = c1(d) > 0 such that 

lim  

> n→∞

P

(

smin (X⋆c ) ≥ c1

√n

)

= 1 .

Proof 6 Let ui = x⋆i − E[x⋆i ] = x⋆i − 12 1d, and let U be the n × d matrix with rows u⊤ 

> i

. Then the rows of U

are i.i.d., mean-zero, bounded random vectors in Rd, and 

X⋆c = JU = U − 1¯u⊤, ¯u = 1

n

> n

∑

> i=1

ui.

We first lower-bound smin (U). Set A = UΣ −1/2 = √12 U, where 

Σ = E[uiu⊤ 

> i

] = 112 Id.

Then the rows of A are isotropic, subgaussian and have subgaussian norm bounded by a constant Kd < ∞

depending only on d. By (Vershynin, 2025, Theorem 4.6.1), there exists an absolute constant 0 < C < ∞ so that for every t > 0,

P[smin (A) ≥ √n − CK 2 

> d

(√d + t)] ≥ 1 − 2e−t2

. (A.44) 

Since A = √12 U, choosing t = √n/ (4 CK 2 

> d

) in Equation A.44 shows that there exist constants cU = cU (d) >

0 and cA = cA(d) > 0 such that, for all sufficiently large n,

P(smin (U) ≥ cU

√n) ≥ 1 − 2e−cAn. (A.45) 

We next control the rank-one correction 1¯u⊤. Because each coordinate of ui is bounded and mean-zero, Hoeffding’s inequality gives, for every a > 0,

P(√n |¯uk| > a ) ≤ 2e−ca 2

, k = 1 , . . . , d, 

for a constant c = c(d) > 0. Therefore, by a union bound, for every b > 0,

P(√n ∥¯u∥ > b ) ≤ 2d exp 

(

− cb 2

d

)

. (A.46) 

Since ∥1¯u⊤∥op = √n ∥¯u∥, substituting b = ( cU /2) √n in Equation A.46 gives 

P

(

∥1¯u⊤∥op > cU

2

√n

)

≤ 2d exp 

(

− cc 2

> U

4d n

)

. (A.47) 

Let 

En = {smin (U) ≥ cU

√n} ∩

{

∥1¯u⊤∥op ≤ cU

2

√n

}

.

By Equation A.45 and Equation A.47, P(En) → 1. On En, since X⋆c = U − 1¯u⊤, Weyl’s inequality for singular values gives 

smin (X⋆c ) = smin (U − 1¯u⊤) ≥ smin (U) − ∥ 1¯u⊤∥op ≥ (cU /2) √n. 

Taking c1 = cU /2 proves the lemma. 

20 A.4.4 Completing the quadratic lower bounds 

Proof 7 (Proof of Proposition A.2) Let c1 = c1(d) be the constant from Lemma 6. We prove Equa-tion A.22 on the high-probability event smin (X⋆c ) ≥ c1

√n, applying Lemma 5 with α = c1.Combine Lemma 4 and Lemma 5: 

R0

> n

(X) − R0

> n

(X⋆) ≥ csc ∆dist (X, X⋆) ≥ csc · 1

c2

dG (X, X⋆)2.

Set cmg = csc /c 2.

Proposition A.3 There exists a constant Cup = Cup (d, σ, M ) > 0 such that, for every X ∈ ([0 , 1] d)n,

R0

> n

(X) − R0

> n

(X⋆) ≤ Cup dG (X, X⋆)2. (A.48) 

Proof 8 Fix X and choose Q ∈ O (d) so that dG (X, X⋆) = n−1/2 ∥Xc − X⋆c Q∥F . Write H = Xc − X⋆c Q,and let hi denote the ith row of H. Then ∥H∥2 

> F

= n d G (X, X⋆)2 and the rows of H sum to 0.For each fixed y ∈ [0 , M ], the map r 7 → ℓM (y, r ) is C2 on [0 , √d], and each of the three branches from Equation A.2 has second derivative uniformly bounded on the relevant compact interval. Hence there exists 

Hℓ = Hℓ(d, σ, M ) < ∞ such that 

sup  

> y∈[0 ,M ]

sup 

> r∈[0 ,√d]

∣∣∂2 

> r

ℓM (y, r )∣∣ ≤ Hℓ. (A.49) 

By Equation A.14, 

g(r; r⋆) = 

∫

ℓM (y, r )qM,r ⋆ (y) ν(dy ).

Using ∂r ℓM (y, r ) = −(∂r qM,r (y)) /q M,r (y), we get 

∂r g(r; r⋆)∣∣r=r⋆ = −

∫

qM,r ⋆ (y) ∂r qM,r (y)|r=r⋆

qM,r ⋆ (y) ν(dy )= −

∫

∂r qM,r (y)∣∣r=r⋆ ν(dy )= −∂r

(∫ 

qM,r (y) ν(dy )

) ∣ ∣∣∣r=r⋆

= 0 ,

because ∫ qM,r (y) ν(dy ) = 1 for every r. Moreover, Equation A.49 implies |∂2 

> r

g(r; r⋆)| ≤ Hℓ for all r, r ⋆ ∈

[0 , √d]. Taylor’s theorem therefore gives 

g(r; r⋆) − g(r⋆; r⋆) ≤ Hℓ

2 (r − r⋆)2, r, r ⋆ ∈ [0 , √d]. (A.50) 

Applying Equation A.50 with r = rij (X) and r⋆ = r⋆ij and then using the fact that centering and rotation do not change pairwise distances, we obtain 

g(rij (X); r⋆ij 

) − g(r⋆ij ; r⋆ij 

) ≤ Hℓ

2

(rij (X) − r⋆ij 

)2 ≤ Hℓ

2 ∥hi − hj ∥2 . (A.51) 

Averaging Equation A.51 over pairs and using the identity ∑ 

> i<j

∥hi − hj ∥2 = n ∥H∥2 

> F

yields 

R0

> n

(X) − R0

> n

(X⋆) ≤ Hℓ

2N

∑

> i<j

∥hi − hj ∥2 = Hℓn ∥H∥2

> F

2N ≤ 2Hℓ dG (X, X⋆)2,

since N = (n

> 2

) ≥ n2/4 for n ≥ 2. This proves Equation A.48 with Cup = 2 Hℓ.

21 A.5 Consistency of the exact posterior 

Proposition A.4 (Consistency of the exact posterior) Suppose Assumption A.2 holds. Then, for ev-ery δ > 0,

Πn

({X : dG (X, X⋆) > δ } ∣∣∣ D

) P

−→ 0.

Proof 9 Fix δ > 0, and define the constant 

a0 = min 

{ 12 ,

√ cmg 

4Cup 

}

∈

(

0, 12

]

, (A.52) 

where cmg is from Proposition A.2 and Cup is from Proposition A.3. Let 

Aδ = {X : dG (X, X⋆) > δ }, Ba0δ = {X : dG (X, X⋆) ≤ a0δ}, ˜Ba0δ = Ba0δ ∩ ([0 , 1] d)n.

Because the integrand is nonnegative, restricting the domain of integration gives 

∫

e−N R n(X) Π0(dX) ≥

∫˜Ba0δ

e−N R n(X) Π0(dX). (A.53) 

Let ηn = sup X∈([0 ,1] d)n |Rn(X) − R0

> n

(X)|. By Lemma 2, ηn → 0 in probability. Let 

Econd  

> n

= {smin (X⋆c ) ≥ c1

√n},

where c1 = c1(d) is the constant from Lemma 6; then P(Econd  

> n

) → 1. Fix γ > 0. On the event {ηn ≤ γ} we have, for all X,

Rn(X) ≥ R0

> n

(X) − γ, Rn(X) ≤ R0

> n

(X) + γ. (A.54) 

Therefore, using Equation A.53 and then Equation A.54, 

Πn(Aδ | D) = 

∫ 

> Aδ

e−N R n(X)Π0(dX)

∫ e−N R n(X)Π0(dX) ≤

∫ 

> Aδ

e−N R n(X)Π0(dX)

∫˜Ba0δ

e−N R n(X)Π0(dX)

≤

∫ 

> Aδ

e−N (R0

> n(X)−γ)

Π0(dX)

∫˜Ba0δ

e−N (R0

> n(X)+ γ)

Π0(dX) = e2N γ ·

∫ 

> Aδ

e−N R 0

> n(X)

Π0(dX)

∫˜Ba0δ

e−N R 0

> n(X)

Π0(dX) .

Next, on Econd  

> n

, Proposition A.2 gives 

R0

> n

(X) ≥ R0

> n

(X⋆) + cmg δ2 for all X ∈ Aδ . (A.55) 

Also, Proposition A.3 and the choice of a0 in Equation A.52 imply 

R0

> n

(X) ≤ R0

> n

(X⋆) + Cup a20δ2 ≤ R0

> n

(X⋆) + 14 cmg δ2 for all X ∈ ˜Ba0δ . (A.56) 

Using Equation A.55 and Equation A.56, 

∫

> Aδ

e−N R 0

> n(X)

Π0(dX) ≤ e−N (R0

> n(X⋆)+ cmg δ2)

Π0([0 , 1] dn ), (A.57) 

and ∫˜Ba0δ

e−N R 0

> n(X)

Π0(dX) ≥ e−N (R0

> n(X⋆)+ cmg δ2/4)

Π0( ˜Ba0δ ). (A.58) 

Combining Equations A.57–A.58 on Econd  

> n

∩ { ηn ≤ γ} yields 

Πn(Aδ | D) ≤ Π0([0 , 1] dn )Π0( ˜Ba0δ ) exp 

(

− N · 34 cmg δ2 + 2 N γ 

)

. (A.59) 22 It remains to lower-bound Π0( ˜Ba0δ ). By Assumption A.2(T3), if π0 denotes the prior density then 

π0(X) ≥ e−cn , X ∈ ([0 , 1] d)n, (A.60) 

for some constant c > 0. Moreover, by the definition 

dG (X, X⋆) = 1

√n min  

> Q∈O (d)

∥Xc − X⋆c Q∥F ,

we have the inclusion {

X ∈ ([0 , 1] d)n : ∥X − X⋆∥F ≤ √n a 0δ

}

⊆ ˜Ba0δ . (A.61) 

Set 

bδ = min 

{

1, a0δ

√d

}

,

and write ∥ · ∥ max for the maximum absolute coordinate over the n × d matrix. Since ∥X − X⋆∥max ≤ a0δ/ √d

implies ∥X − X⋆∥F ≤ √n a 0δ, the box 

Cδ =

{

X ∈ ([0 , 1] d)n : ∥X − X⋆∥max ≤ a0δ/ √d

}

is contained in the ball ˜Ba0δ .Note that each side of this box has length at least bδ , so Vol( Cδ ) ≥ bnd δ . Therefore, by Equation A.60, 

Π0( ˜Ba0δ ) ≥ e−cn · Vol( Cδ ) ≥ e−cn bnd δ . (A.62) 

In particular, 

− log Π 0( ˜Ba0δ ) ≤ cn + nd log 

( 1

bδ

)

, (A.63) 

which is O(n) for each fixed δ > 0.Fix τ = 316 cmg δ2. Since ηn → 0 in probability and P(Econd  

> n

) → 1, the event {ηn ≤ τ } ∩ Econd  

> n

has probability tending to 1. On this event, Equation A.59 with γ = τ and Equation A.63 give 

Πn(Aδ | D) ≤ Π0([0 , 1] dn )Π0( ˜Ba0δ ) exp 

(

− N · 38 cmg δ2)

,

and the right-hand side tends to 0 because N = (n

> 2

) ≍ n2 while Equation A.63 is O(n) for fixed δ. This proves the claim. 

A.6 Proof of Proposition A.1 

Let εn be as in Equation A.6, and define 

Eapp  

> n

=

{

sup 

> X∈([0 ,1] d)n

|̂Rn(X) − Rn(X)| ≤ εn

}

.

By Equation A.7, P(Eapp  

> n

) → 1. On Eapp  

> n

, Equation A.7 gives 

sup 

> X∈([0 ,1] d)n

|̂ Rn(X) − Rn(X)| ≤ εn.

Repeat the proof of Proposition A.4 with ̂ Rn in place of Rn. Everywhere that the previously defined quantity 

ηn appeared, we now have, on Eapp  

> n

,̂

η n = sup 

> X∈([0 ,1] d)n

|̂Rn(X) − R0

> n

(X)| ≤ sup 

> X∈([0 ,1] d)n

|̂ Rn(X) − Rn(X)| + sup 

> X∈([0 ,1] d)n

|Rn(X) − R0

> n

(X)| ≤ εn + ηn.

(A.64) Since P(Eapp  

> n

) → 1, εn → 0 by assumption, and ηn → 0 in probability by Lemma 2, Equation A.64 gives ̂

η n → 0 in probability. The remainder of the argument (risk gap ⇒ posterior concentration) is unchanged. 23 A.7 Proof of Theorem A.1 

Let 

∆mix  

> n

= sup 

> X0∈([0 ,1] d)n

∥∥P (X0, ·) − Pξ0 (X0, ·)∥∥TV .

By convexity of total variation and the definition of P ,

∆mix  

> n

≤

∫

> Ξ

sup 

> X0∈([0 ,1] d)n

∥∥Pξ (X0, ·) − Pξ0 (X0, ·)∥∥TV μ(dξ ) ≤ ∆n. (A.65) By the uniform-ergodic perturbation bound in Alquier et al. (2016) and Equation A.65, 

sup 

> X0∈([0 ,1] d)n

∥∥δX0 P m − δX0 P mξ0

∥∥TV ≤ κn∆mix  

> n

≤ κn∆n, m ≥ 0. (A.66) Integrating Equation A.66 against X0 ∼ ˜Πn and using the invariance of ˜Πn under P gives 

∥∥˜ Πn − ˜ΠnP mξ0

∥∥TV ≤ κn∆n. (A.67) By the triangle inequality and Equation A.67, 

∥∥˜ Πn −̂ Π(ξ0)

> n

∥∥TV ≤ ∥∥˜ Πn − ˜ΠnP mξ0

∥∥TV + ∥∥˜ ΠnP mξ0 −̂ Π(ξ0)

> n

∥∥TV .

Since Pξ0 has invariant distribution ̂ Π(ξ0) 

> n

and is uniformly ergodic, the second term tends to 0 as m → ∞ .Letting m → ∞ yields ∥∥˜ Πn −̂ Π(ξ0)

> n

∥∥TV ≤ κn∆n. (A.68) Now set Aδ = {X : dG (X, X⋆) > δ }. Then Equation A.68 gives 

˜Πn(Aδ ) ≤̂ Π(ξ0) 

> n

(Aδ ) + κn∆n.

The first term goes to 0 in probability by Proposition A.1, and the second does as well by assumption. 24 B Supplementary algorithms 

Algorithm B.1: Quadtree construction 

Input: x1, . . . , xn ∈ R2, y1, . . . , yn ∈ RΩ

Output: quadtree T

Function Insert( i, k )

if xi /∈ C k then return false Update Nk , ¯xk , ¯yk ;

if node k is an empty leaf then 

Store (xi, yi) in node k ;

return true 

if node k is a leaf then 

Subdivide Ck into children c ∈ children (k);

for each stored point j in node k do 

Determine c(j) and call Insert( j, c (j));Mark node k as internal; 

foreach c ∈ children (k) do if Insert( i, c ) then return true 

return false Initialize root node k = 0 with cell C0;

for i = 1 , . . . , n do 

Insert( i, 0);

return T

Algorithm B.2: Local quadtree update (forward) 

Input: quadtree T , current point xi, proposed point x′

> i

Output: locally updated quadtree T

Let Ii = {node k ∈ T : xi ∈ C k } be the set of nodes along the path from root to leaf containing xi;

for node k ∈ I i do 

Cache ¯xk ;Update ¯xk ← ¯xk + (x′

> i−xi)
> Nk

return T

Algorithm B.3: Deterministic evaluation of the BH-BMDS log-likelihood 

Input: x1, . . . , xn ∈ R2; y1, . . . , yn ∈ RΩ; quadtree T

Output: BH-BMDS log-likelihood ˜ℓ(· | T )

Function BH( i, k )

Compute ρik = wk /¯rik ;

if node k is leaf or ρik < θ then return Nk

[ ( ¯Dik −¯rik )2 

> 2σ2

+ log Φ ( ¯rik 

> σ

)]

else return ∑ 

> c∈children (k)

BH (i, c )

Initialize ˜ℓ(· | T ) = −N log(2 πσ 2);

for i = 1 , . . . , n do 

˜ℓ(xi | T ) ← BH (i, 0) ;

˜ℓ(· | T ) ← ˜ℓ(· | T ) + ˜ℓ(xi | T );

return ˜ℓ(· | T )

Algorithm B.4: Stochastic evaluation of the pointwise contribution to the BH-BMDS log-likelihood 

Input: target point xi, quadtree T

Output: ˜ℓ(xi | T )

Function SBH( i, k )

Draw u ∼ Unif (0 , 1) ;Compute p(ρik | θ) (Equation 4); 

if node k is leaf or u < p (ρik | θ) then return Nk

[ ( ¯Dik −¯rik )2 

> 2σ2

+ log Φ ( ¯rik 

> σ

)]

else return ∑ 

> c∈children (k)

SBH (i, c )˜ℓ(xi | T ) ← SBH(i, 0) ;

return ˜ℓ(xi | T )

25 C Additional plots 

Figure C.1: Posterior Procrustes-aligned means across MCMC iterations for 1,000 and 10 ,000 embedded MNIST handwritten digital images under full Bayesian multidimensional scaling (BMDS) and Barnes–Hut Bayesian multidimensional scaling (BH-BMDS) frameworks. 

Figure C.2: Posterior Procrustes-aligned means across MCMC iterations for ThyTau22 and Wild-Type mouse under the Barnes–Hut Bayesian multidimensional scaling (BH-BMDS) framework. High-dimensional embeddings are obtain from a trained MaGNet GNN (Zhou et al., 2025) to decode behavior states (stalled vs. moving) on local field potential recordings. We observe more clear clustering for a Wild-Type than ThyTau22 mouse. 26