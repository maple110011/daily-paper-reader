---
title: Predictive Coding with Bayesian Priors via Proximal Gradients
title_zh: 通过近端梯度实现贝叶斯先验的预测编码
authors: Francesco Bullo
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08374v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 预测编码网络中使用贝叶斯先验的邻近梯度MAP
tldr: 预测编码通常被视为神经科学中的信息处理模型，但缺乏统一的优化理论视角。本文将预测编码重新解释为连续时间近端梯度下降求解正则化最大后验目标，单层问题等价于漏泄发放率网络，其中先验通过近端算子决定非线性激活函数，似然精度控制观测增益。对于层次结构，通过经典变量分裂松弛将深度MAP问题转化为局部和分布式求解器的互连，并替换定向生成链为无向马尔可夫随机场。该工作为预测编码提供了严谨的优化基础，统一了神经回路与贝叶斯推断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08374v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 506, \"height\": 814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08374v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1004, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08374v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1240, \"height\": 289, \"label\": \"Table\"}]"
motivation: 为预测编码建立统一的优化理论框架，揭示其与神经电路和概率推断的内在联系。
method: 将预测编码建模为连续时间近端梯度下降优化正则化MAP目标，单层对应漏泄发放率网络，层次通过变量分裂松弛实现互连的局部求解器。
result: 先验的近端算子决定非线性激活，似然精度控制观测增益；层次结构将生成链转化为无向马尔可夫随机场，每层独立应用激活函数。
conclusion: 预测编码可解释为基于近端梯度的优化算法，为神经计算提供了统一的贝叶斯推断框架。
---

## 摘要
我们将预测编码重新解释为应用于正则化最大后验（MAP）目标的连续时间近端梯度下降。首先研究单层问题，然后研究多层层次结构。对于单层问题，我们证明近端梯度下降正是一个漏放电率网络：膜漏、有效循环矩阵、局部突触驱动和静态非线性均来自同一个优化原理，所得电路与Rao和Ballard提出的电路一致。先验通过其近端算子选择非线性，似然精度设置观测增益。对于层次结构，我们展示了深度MAP问题的经典变量分裂松弛将层次预测编码表示为局部和分布式求解器的互联。在概率建模术语中，这种松弛将有向生成链替换为一个无向马尔可夫随机场，其节点势是各层先验。然后每个层应用自己的激活函数，即其先验的近端算子。

## Abstract
We recast predictive coding as continuous-time proximal gradient descent applied to a regularized maximum-a-posteriori (MAP) objective. We study first a single-level problem and then a multi-level hierarchy. For the single-level problem, we show that proximal gradient descent is precisely a leaky firing-rate network: the membrane leak, the effective recurrent matrix, the local synaptic drive, and the static nonlinearity all follow from one optimization principle, and the resulting circuit is the one proposed by Rao and Ballard. The prior selects the nonlinearity through its proximal operator, and the likelihood precision sets the gain on the observation. For the hierarchy, we show that a classical variable-splitting relaxation of the deep MAP problem yields hierarchical predictive coding as the interconnection of local and distributed solvers. In probabilistic modeling terms, this relaxation replaces the directed generative chain by an undirected Markov random field whose node potentials are the level-wise priors. Each level then applies its own activation function, namely the proximal operator of its prior.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 一、论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：预测编码是神经科学中解释大脑皮层信息处理的重要理论框架，但其与贝叶斯推断、优化算法之间的数学联系尚不清晰。现有工作（如 Rao & Ballard 1999、Friston 2005）将预测编码视为负对数似然的梯度下降，但缺乏对非线性激活函数、膜泄漏和循环矩阵等神经电路特性如何从统一优化原理中产生的系统性解释。
- **核心问题**：如何将预测编码重新解释为连续时间近端梯度下降求解正则化最大后验（MAP）目标，从而揭示以下三者的内在关系：
  1. 大脑假设的贝叶斯先验；
  2. 神经元实现的动力学系统；
  3. 神经元应用的静态激活函数。
- **整体含义**：论文旨在为预测编码提供严谨的优化理论基础，统一神经电路与贝叶斯推断，并推广到层次结构，使得不同层次可以具有不同的激活函数（如 soft-threshold、ReLU、softmax），从而在单一动力学框架内实现从连续特征提取到离散分类的过渡。

## 二、论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将预测编码建模为连续时间近端梯度下降（proximal gradient descent），优化一个正则化的 MAP 能量函数。
- 单层问题中，近端梯度下降自然导出一个漏泄发放率网络（leaky firing-rate network），其中膜泄漏、有效循环矩阵、局部驱动和静态非线性均由优化目标唯一确定。
- 层次问题中，通过变量分裂（variable splitting）松弛深度 MAP 问题，得到层次预测编码作为局部和分布式求解器的互联，等价于一个无向马尔可夫随机场。

### 关键技术细节
1. **单层预测编码**：
   - 定义 MAP 能量：\( E_{\text{MAP}}(x) = \frac{1}{2} \|y - \Phi x\|^2_{\Pi} + R(x) \)，其中 \(y\) 为观测，\(\Phi\) 为字典矩阵，\(\Pi\) 为似然精度，\(R\) 为负对数先验（正则项）。
   - 连续时间近端梯度下降：\( \tau \dot{x} = -x + \text{prox}_R( x + \Phi^\top \Pi \epsilon ) \)，其中 \(\epsilon = y - \Phi x\) 为预测误差。
   - 等价于递归网络形式：\( \tau \dot{x} = -x + \text{prox}_R( W x + \Phi^\top \Pi y ) \)，其中 \(W = I - \Phi^\top \Pi \Phi\) 为有效突触矩阵。
   - 先验 \(R\) 的近端算子恰好等于激活函数（如 Laplace 先验 → soft-threshold，非负先验 → ReLU，箱约束 → 饱和线性，移位熵屏障 → softmax）。

2. **层次预测编码**：
   - 从深度线性生成模型 \( y = \Phi^{(1)} \Phi^{(2)} \cdots \Phi^{(L)} z + \eta \) 出发，引入辅助变量 \(x^{(\ell)}\)，通过变量分裂将硬约束松弛为高斯惩罚，并加入各层先验 \(R^{(\ell)}\)，得到层次 MAP 能量：
     \[
     E_{\text{H-MAP}} = \sum_{\ell=1}^L \frac{1}{2} \|x^{(\ell-1)} - \Phi^{(\ell)} x^{(\ell)}\|^2_{\Pi^{(\ell)}} + \sum_{\ell=1}^L R^{(\ell)}(x^{(\ell)})
     \]
   - 该能量等价于一个链式无向马尔可夫随机场，其中边缘势能为高斯项，节点势能为各层先验。
   - 对各层分别应用近端梯度下降，得到级联动力学：
     \[
     \tau^{(\ell)} \dot{x}^{(\ell)} = -x^{(\ell)} + \text{prox}_{R^{(\ell)}}\left( x^{(\ell)} + u^{(\ell)} \right)
     \]
     其中 \(u^{(\ell)}\) 由下层的预测误差上馈和上层的预测误差反馈构成，实现局部信息传递。
   - 层次结构可以堆叠不同先验（如第一层 Laplace 实现稀疏编码，第二层非负先验实现 ReLU，第三层熵先验实现 softmax 分类）。

### 公式与算法流程（文字说明）
- 单层：给定观测 y，字典 Φ，精度 Π，先验 R，近端梯度动力学（式 11-12）直接给出漏泄神经元的动力学方程。
- 层次：给定多层字典和精度，以及各层先验，层次动力学（式 24）实现分布式求解，每个模块只与相邻模块交换预测误差和预测值。

## 三、实验设计

- **本文不涉及实验**。论文是一篇理论技术报告，主要贡献在于数学推导和框架构建，未使用任何数据集、基准方法或对比实验。文中仅提供了概念性示例（如正性竞争网络、softmax 分类、Hopfield 能量类比），并未进行数值模拟或实证验证。

## 四、资源与算力

- **未提及任何计算资源**。由于是纯理论工作，论文没有报告 GPU 型号、数量、训练时长等算力信息。

## 五、实验数量与充分性

- **实验数量为零**。论文完全依赖于理论分析和数学证明，没有设计任何实验（包括合成数据实验、消融实验等）。
- **充分性**：从实验角度看，论文尚不充分。虽然理论推导是自洽的，但未通过仿真验证动力学是否收敛、收敛速度如何、层次结构的实际性能等。因此无法评估其在实际神经科学或机器学习任务中的有效性。

## 六、论文的主要结论与发现

1. **单层预测编码等价于一个漏泄发放率网络**，其中膜泄漏、循环矩阵、驱动和非线性激活函数均从正则化 MAP 目标的近端梯度下降中自然导出，无需额外假设。
2. **先验直接决定激活函数**：先验的近端算子正是神经元的静态非线性，并给出了一个完整对应表（Laplace → soft-threshold，非负 → ReLU，箱约束 → 饱和线性，熵屏障 → softmax）。
3. **层次预测编码可通过变量分裂松弛深度 MAP 问题得到**，其动力学本质上是分布式的近端梯度下降，每层单独运行，仅与相邻层交换信息，无需全局协调。
4. **层次结构可混合不同先验**，从而在同一动力学中实现从连续稀疏编码到离散分类的过渡，无需切换推理方法。
5. **概率图模型视角**：层次能量对应于无向马尔可夫随机场，其节点势能即为各层先验，马尔可夫性质保证了动力学的局部性。

## 七、优点

1. **统一性与简洁性**：将预测编码、贝叶斯推断、神经动力学和优化算法统一在单一理论框架下，揭示了多个概念的深层联系。
2. **理论严谨性**：严格推导了单层和层次情况下的动力学方程，并给出了关于收敛性的已知参考文献支撑。
3. **模块化与灵活性**：层次结构允许每层选择不同先验和激活函数，可适用于感知到分类的多级处理。
4. **与现有文献的强关联**：明确指出其与 LCA、正性竞争网络、Hopfield 网络等经典模型的关系，便于社区理解。
5. **对神经科学的启发**：为非线性的来源提供了贝叶斯解释，有助于设计更合理的生物可解释模型。

## 八、不足与局限

1. **缺乏实验验证**：论文仅提供理论推导，没有数值模拟或实际应用验证，无法确认动力学在实际问题中的收敛性、速度或鲁棒性。
2. **简化假设**：假设线性高斯似然、各层先验为凸函数（包含指示函数），但实际生物系统中的先验可能非凸或更复杂。
3. **未讨论学习问题**：论文仅关注推理（MAP 估计），未涉及如何学习字典矩阵 \(\Phi^{(\ell)}\) 或精度矩阵 \(\Pi^{(\ell)}\)，而这些在预测编码中通常通过 Hebb 可塑性实现。
4. **变量分裂的近似性**：层次能量（式 21）是原始深度 MAP 问题（式 19）的松弛，仅当精度 \(\Pi^{(\ell)} \to \infty\) 时才等价，有限精度下存在信息损失，且论文未讨论该误差的影响。
5. **未处理时间尺度**：不同层次可能具有不同时间常数 \(\tau^{(\ell)}\)，但论文未分析时间常数差异对整体同步或稳定性的影响。
6. **缺乏与其他方法的定量比较**：未与 VAE、ADMM 训练、标准分类器等在共同基准上进行性能对比，无法评估其实际优势。

（完）
