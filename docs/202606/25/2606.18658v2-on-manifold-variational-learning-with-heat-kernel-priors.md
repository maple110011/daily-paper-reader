---
title: On-Manifold Variational Learning with Heat-Kernel Priors
title_zh: 基于热核先验的流形上变分学习
authors: "Jiarui Xing, Tal Zeevi, Nian Wu, Jian Wang"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18658v2"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 基于热核先验的流形变分学习
tldr: 现有深度潜变量模型用欧氏平均估计高斯混合先验，导致原型偏离数据流形且随子群增多退化。本文提出流形锚定变分框架，基于几何感知EM算法，在热核加权潜图上选取扩散中心度最高的图中心作为原型，确保原型在流形上。引入Dirichlet能量正则化保持几何光滑，并通过不确定性评分实现无标签质量评估。在心脏瘢痕和脑MRI基准上，该方法准确率最高，原型最清晰，且在大子群数下保持稳定。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18658v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1233, \"height\": 628, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18658v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18658v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1280, \"height\": 1033, \"label\": \"Figure\"}]"
motivation: 现有方法用欧氏平均估计高斯混合先验，导致原型偏离数据流形，随子群数增多退化。
method: 提出流形锚定变分框架，基于几何感知EM算法，在热核加权潜图上选取图中心作为原型，并加入Dirichlet能量正则化。
result: 在心脏瘢痕和脑MRI基准上，准确率最高，原型最清晰，且在大子群数下稳定。
conclusion: 流形锚定EM是通用几何工具，可扩展至其他潜变量模型，提升无监督表示学习质量。
---

## 摘要
学习医学影像队列的无监督表示可以揭示临床上有意义的原型，而无需专家标签，这些标签通常带有噪声且无法捕捉真实的病理异质性。然而，现有的深度潜变量模型通过欧几里得平均估计高斯混合先验，产生的原型会偏离弯曲的数据流形，并随着子群体数量的增加而退化。我们提出了一种基于几何感知期望最大化（EM）算法的流形锚定变分框架，其M步将每个子群体原型选为在热核加权潜在图上具有最高扩散中心性的图medoid，确保每个原型保持在流形上。狄利克雷能量正则化器强制潜在空间的几何平滑性，每个子群体的不确定性评分实现了无标签质量评估。流形锚定EM是一种通用几何工具，它扩展了标准EM，并可直接应用于其他潜变量模型。在心脏瘢痕和脑MRI基准测试中，我们的框架在所有比较方法中获得了最高准确性，产生了迄今为止报告的最清晰原型，并且在所有基线退化的较大子群体计数下保持稳定。代码和实现细节见https://github.com/jr-xing/On-Manifold-Variational-Learning-with-Heat-Kernel-Priors。

## Abstract
Learning unsupervised representations of medical imaging cohorts can reveal clinically meaningful prototypes without expert labels, which are often noisy and fail to capture true pathological heterogeneity. However, existing deep latent-variable models estimate Gaussian mixture priors via Euclidean averaging, producing prototypes that drift off the curved data manifold and degenerate as the number of sub-populations grows. We propose a manifold-anchored variational framework built on a geometry-aware Expectation-Maximization (EM) algorithm, whose M-step selects each sub-population prototype as the graph medoid with the highest diffusion centrality on a heat-kernel-weighted latent graph, ensuring that every prototype remains on-manifold. A Dirichlet energy regularizer enforces geometric smoothness of the latent space, and a per-sub-population uncertainty score enables label-free quality assessment. The manifold-anchored EM is a general-purpose geometric tool that extends standard EM and applies readily to other latent-variable models beyond this setting. On cardiac scar and brain MRI benchmarks, our framework attains the highest accuracy among all compared methods, produces the sharpest prototypes reported to date, and remains stable at large sub-population counts where all baselines degenerate. The Code and implementation details are available at https://github.com/jr-xing/On-Manifold-Variational-Learning-with-Heat-Kernel-Priors.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在医学影像的无监督表示学习中，现有深度潜变量模型使用欧氏平均估计高斯混合先验，导致原型（prototype）偏离弯曲的数据流形，随着子群体数量增加而退化为冗余、重叠且低密度的组件。
- **背景**：医学诊断标签不可靠（如心肌瘢痕亚型分界模糊、脑影像诊断受主观性影响），监督方法继承了标签噪声；无监督方法（如VAE-GMM、Diffusion-GMM）的欧氏均值原型不在流形上，产生“离流形漂移”问题，且无法处理拓扑异质性（如心肌有无瘢痕）。
- **整体含义**：作者提出一个将概率潜变量建模与谱流形几何相结合的框架，旨在同时实现无标签表示学习、流形感知原型估计和拓扑异质性群体的生成建模。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：用流形上驻留的图medoid（中心点）代替欧氏均值作为原型，确保原型始终在数据流形上；利用热核构造图的几何结构，并引入狄利克雷能量正则化保持潜在空间平滑性。
- **技术细节**：
  - **热核亲和图**：在潜在空间上构建加权完全图，边权重为热核 `W_ij = exp(-||z_i - z_j||^2 / (4t))`，其中t自适应设为小批量内成对距离的中位数。
  - **狄利克雷能量**：定义 `E(Z) = (1/2) Σ W_ij ||z_i - z_j||^2 = Tr(Z^T L Z)`，作为正则项促进潜在空间几何平滑。
  - **流形锚定EM算法**：
    - E步：计算后验责任 `γ_ik`（同标准EM）。
    - M步：基于硬分配选择图medoid作为原型：`μ*_k = arg max_{z_i ∈ C_k} Σ_{ℓ ∈ C_k} W_iℓ`；协方差矩阵用软责任更新并添加εI确保正定；混合系数也用软责任更新。
  - **变分框架**：编码器输出近似后验 `q_φ(z|x) = N(z; μ_φ, diag(σ²_φ))`，解码器重建图像。训练目标包含三项：重建损失、流形锚定KL散度（每个样本向分配的原型组件正则化）、狄利克雷能量正则化。
  - **不确定性量化**：对每个输入采样M=30个后验样本，计算像素级不确定性 `U_i(p)` 和结构性不确定性 `S_i = M^{-1} Σ DSSIM`，并聚合到每个子群体得到概况。
- **算法流程**：在训练过程中每隔固定间隔使用上述EM步骤更新先验参数（原型、协方差、混合系数）；整个框架端到端联合优化。

## 3. 实验设计：使用了哪些数据集 / 场景，benchmark 是什么，对比了哪些方法

- **数据集**：
  1. **MNIST**（60,000张，10个数字类）：作为受控基准。
  2. **Synthetic Cardiac**（5,000张心脏掩膜，5种形态：健康、心内膜下、透壁、中层、心外膜）。
  3. **OASIS Brain MRI**（185名受试者的T1轴向切片，基于受试者分割防止数据泄漏，共128×128）。
- **Benchmark**：无监督表示学习任务——子群体发现与原型生成；评估指标：ACC、NMI、ARI（有标签数据集）；Silhouette Score（OASIS无标签数据集）。
- **对比方法**：
  - VAE-GMM [15]：变分自编码器+高斯混合先验（标准欧氏均值）。
  - Diffusion-GMM [28]：扩散模型+GMM（同为生成式基线，共享编码器-解码器骨干）。
  - 注：非生成式方法（如DEC、谱聚类）因为无法合成原型和不确定性图，被排除在比较范围之外。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力。若未明确说明，也请指出这一点。

- **文中未明确说明使用的具体GPU型号、数量或训练时长**。仅提到优化器使用Adam，学习率分别为10⁻³（MNIST, Cardiac）和10⁻⁴（OASIS），以及使用了梯度剪裁和L2权重衰减。
- 因此，**算力信息缺失**，可能是作者在最终版本中未提供或省略。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平

- **主要实验**：
  - 三个数据集上的定量比较（图2a）：ACC、NMI、ARI（MNIST和Cardiac），Silhouette Score（OASIS）。
  - 临床相关性分析（图2b）：将发现的子群体与临床诊断分数关联。
  - 原型清晰度与不确定性图可视化（图2c、2d、图3）。
  - OASIS上还报告了每个原型的锐度数值（Sharpness，图3顶部表格）。
- **消融与稳定性**：
  - 组件数K通过肘部法选择，并在邻近K值下验证原型稳定。
  - 不确定性采样M=30且已验证稳定。
  - 结果对多个随机种子取平均。
- **充分性评价**：
  - 覆盖了三个不同领域（手写数字、合成心脏、真实脑MRI）和不同复杂程度。
  - 公平性方面：对比方法均使用相同编码器-解码器骨干和相同K值；但未进行消融实验（如去掉狄利克雷能量正则化、去掉流形锚定EM等）来严格归因各组件贡献。**缺少消融实验是一大不足**。
  - 实验数量可以接受，但若能增加更多真实数据集（如ADNI、多中心数据）和更多基线（如扩散模型的其他变体）会更充分。

## 6. 论文的主要结论与发现

- 提出的流形锚定框架在所有数据集上取得最高准确率和NMI/ARI，尤其在OASIS上Silhouette Score显著高于基线。
- 生成的子群体原型在所有方法中最清晰（sharpest），且与临床诊断分数呈现合理关联（健康原型对应低分数，疾病原型对应高分数）。
- 不确定性图在病理相关区域集中（如瘢痕边界、脑室边界），而基线方法的不确定性图模糊均一。
- 在大子群体数量下（如K接近10），方法保持稳定，而基线退化严重（原型模糊、重叠）。
- 流形锚定EM是通用工具，可扩展至其他潜变量模型。

## 7. 优点：方法或实验设计上有哪些亮点

- **方法创新**：用图medoid代替欧氏均值，几何直观且保证原型在流形上；结合热核亲和图和狄利克雷能量，巧妙将流形学习融入变分框架。
- **通用性**：流形锚定EM不依赖特定模型，论文提到可应用于其他潜变量模型。
- **实用价值**：提供了无标签的子群体不确定性评分，可作为临床质控工具。
- **实验设计**：对比方法均为生成式基线，且共享主干，公平性较好；在OASIS上展示了与临床诊断的关联，增强了医学相关性。
- **效率**：推理时无需重建图，比扩散基线更高效。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏消融实验**：未单独验证狄利克雷能量、流形锚定M-step、热核参数t等组件各自的贡献。
- **算力与训练细节不明**：未报告GPU型号、训练时间、batch size等，影响可复现性。
- **数据集规模偏小**：OASIS仅185个受试者；合成心脏只有5000样本；未在大型多中心医学影像数据（如UK Biobank）上验证。
- **K的选择方法**：仅用肘部法，未探讨模型对K的敏感性（但提到在邻居K下稳定，未展示具体数据）。
- **不确定性量化**：文中声称不确定性高是“校准良好”的体现，但未进行严格的校准性评估（如可靠性图、Q-Q图），也未与重建误差做相关性分析。
- **应用限制**：方法依赖于批内图构造，对大batch size有内存需求（O(B²)），且未考虑非欧几里得数据（如图数据）。
- **风险**：在真实临床中，子群体划分可能无法对应已知病理类别，仍需要专家验证；发现的大群系是否具有临床意义需要更深入验证。

（完）
