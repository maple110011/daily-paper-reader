---
title: Bayesian Inference with Shaped Deep Non-linear MLPs
title_zh: 基于形态化深度非线性多层感知机的贝叶斯推断
authors: "Boris Hanin, Tianze Jiang"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30860v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 深度非线性MLP中的贝叶斯推断，预测后验
tldr: 针对深度非线性MLP在模型参数与数据集规模同时大规模时的贝叶斯推断，本文基于Neural Covariance SDE，分析了输入维度、宽度、层数与样本数均大的情形，提出有效深度LP/N。研究发现，在LP/N的一阶近似下，存在简单准则判定深度是否提升贝叶斯模型证据；且预测后验简化为数据相关核方法，揭示了深度网络贝叶斯推断的底层机制。
source: arxiv
selection_source: fresh_fetch
motivation: 探索大模型和大数据下贝叶斯推断中深度的影响，解决参数与样本量极限非交换导致的先验不确定性。
method: 基于Neural Covariance SDE，分析有效深度LP/N下的贝叶斯后验，涵盖光滑与ReLU激活函数及任意温度。
result: 在LP/N一阶近似下，得到深度提升模型证据的简单准则，且预测后验等价于数据相关核方法。
conclusion: 深度网络贝叶斯推断可简化为核方法，为理解深度学习泛化提供了新视角。
---

## 摘要
深度学习理论的一个核心目标是刻画神经网络在模型规模和训练集规模同时很大时如何做出预测。由于模型参数数量和数据规模发散极限不可交换，因此先验上并不清楚存在哪些极限。在这项工作中，我们通过研究深度非线性MLP在训练样本数($P$)、输入维度($N_0$)、隐藏层宽度($N$)和隐藏层数量($L$)都可以很大的情况下的贝叶斯推断，为这些问题提供了新的视角。我们基于神经协方差SDE（Li等人，2022）来分析在$LP/N\inΘ(1)$区域内的预测后验，该量扮演了有效网络深度的角色。我们的框架涵盖平滑和ReLU激活函数，并适用于任意温度。我们发现，在$LP/N$的一阶近似下，存在一个简单准则，用于判断哪些数据生成过程能从深度中获益，即更大的$LP/N$会增加贝叶斯模型证据。我们还对物理学文献中的一个先前结果给出了新颖的推导：至少在$LP/N$的一阶近似下，贝叶斯预测后验非常简洁，并且简单地等价于一个数据依赖的核方法。

## Abstract
A central aim of deep learning theory is to characterize how neural networks make predictions in the regime of simultaneously large model and training set size. Since the limits of diverging number of model parameters and dataset size do not commute it is not clear a priori what limits exist. In this work, we shed new light on these questions by studying Bayesian inference in deep non-linear MLPs in the regime where the number of training samples ($P$), the input dimension ($N_0$), the hidden layer width ($N$), and the number of hidden layers ($L$) can all be large. We build on the Neural Covariance SDE (Li et al., 2022) to analyze predictive posteriors in the regime where $LP/N\inΘ(1)$, playing the role of an effective network depth. Our framework covers both smooth and ReLU activation functions and applies to arbitrary temperature. We find to first order in $LP/N$ a simple criterion for which data generating processes benefit from depth in the sense that larger $LP/N$ increases the Bayesian model evidence. We also give a novel derivation of a prior result from the physics literature that at least to first order in $LP/N$, the Bayesian predictive posterior is remarkably simple and is simply equivalent to that of a data-dependent kernel method.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

深度学习中当模型参数数量（宽度 $N$、层数 $L$）和训练样本数 $P$ 同时很大时，预测行为如何？由于这些极限不可交换，先验上存在不确定性。本文旨在从贝叶斯推断角度，严格刻画深度非线性多层感知机（MLP）在此类联合大极限下的预测后验和模型证据（贝叶斯证据）。重点关注有效深度 $\tau = LP/N$ 的作用，探究深度何时带来益处、非线性的影响，以及网络是否退化为核方法。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用神经协方差随机微分方程（Neural Covariance SDE, NSDE）描述深度非线性MLP在初始化时的共轭核传播，将贝叶斯推断中的分区函数（partition function）转化为NSDE解的函数期望。然后通过时间尺度变换（$d\tau = P dt$）定义深度时间 $\tau = LP/N$，在 $\tau$ 小的条件下进行一阶扰动展开，得到解析的贝叶斯后验近似。
- **关键技术细节**：
    - 模型：深度MLP，最后输出层为线性层，权重高斯初始化。
    - 贝叶斯设置：平方损失，任意逆温度 $\beta$。
    - **激活函数形态化（shaping）**：对非线性激活函数 $\phi$ 引入形态因子 $s$（如 $\phi_s(x)=s\phi_{\text{base}}(x/s)$ 或 ReLU 形式），使得在 $L,N$ 成比例增长时，非线性的累积效果与线性部分同量级，NSDE有良好极限。
    - NSDE：在 $L,N\to\infty$, $L/N=t$ 固定，且 $s\propto\sqrt{N}$ 条件下，共轭核过程 $\Phi_t$ 收敛到SDE: $d\Phi_t = \alpha_{\text{act}} b(\Phi_t) dt + \Phi_t^{1/2} dB_t \Phi_t^{1/2}$，其中 $b$ 依赖激活函数的非线性特征。
    - **时间尺度化**：$d\tau = P dt$，得到 $d\Phi_\tau = c_{\text{act}} b(\Phi_\tau) d\tau + \frac{1}{\sqrt{P}} \Phi_\tau^{1/2} dB_\tau \Phi_\tau^{1/2}$, $\tau \in [0, LP/N]$。有效深度 $\tau$ 是关键参数。
    - **分区函数**：贝叶斯分区函数 $Z_\beta(x_0,\kappa)$ 转化为关于共轭核的积分（Lemma 4），进而可借助NSDE的期望计算。
    - **小$\tau$扰动展开**：利用Itô引理和Dynkin公式，得到 $\partial_\tau \log Z$ 关于 $\tau=0$ 的显式表达式，等于线性网络项加上非线性核方法项（式(17)），每一项均可解析计算。
    - **贝叶斯证据和后验**：在 $\beta=\infty$ 时，一阶后验仍为高斯，等价于一个数据依赖的核方法，其特征映射在训练数据方向上为ODE特征映射，正交方向有缩放因子（Takeaway 2.2）。
- **公式或算法流程**：文中无传统算法伪码，但逻辑流程为：构造共轭核传播 → NSDE极限 → 时间尺度化 → 小$\tau$展开 → 积分得到分区函数 → 提取后验均值和方差 → 推导证据判据。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

本文为纯理论工作，**未涉及传统数值实验**。作者没有在合成或真实数据集上进行仿真或对比实验。分析基于理论推导和渐进极限，benchmark和对比方法均属于理论层面（如与线性网络 [HZ23]、弱非线性网络 [HZ24] 的结果比较，并复现了物理学文献中核方法等价性的先验结果）。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

**未提及任何算力资源**（GPU、训练时长等）。本文是纯数学理论分析，无需实验验证，故无相关资源需求或说明。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

**无实验**。论文仅提供了理论推导，未进行任何数值实验或消融分析。因此无法评价实验的充分性或公平性。但研究问题的性质决定了理论工作可以无需实验支撑，其充分性主要体现在数学证明的严谨性和推导的完整性。

### 6. 论文的主要结论与发现

- 在 $LP/N\in\Theta(1)$ 的联合极限下，深度非线性MLP的贝叶斯推断可由**神经协方差SDE**精确刻画，分区函数收敛到SDE的期望。
- 在小有效深度 $\tau=LP/N$ 一阶扰动下，贝叶斯证据的导数拆分为线性网络项和核方法项（式(18)）。由此导出**深度有益的条件**：对于光滑激活和ReLU激活，给出了关于训练标签均值、方差和核超参数的显式不等式（Takeaway 3.1 & 3.2）。
- **预测后验在一阶近似下等价于数据依赖的核方法**：后验均值和方差可由一个显式特征映射（ODE特征映射）得到，该特征映射依赖于训练数据，但后验后验仍保持高斯性。
- 恢复了线性网络和无限宽网络（核极限）作为特例的结果，并推广到更一般的激活函数和有限温度。
- 证明了离散MLP与NSDE在生成元意义下的均匀收敛（Theorem 5），保证了理论近似的合理性。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论严谨性**：基于NSDE的收敛结果给出了严格证明，并扩展到贝叶斯推断，弥补了之前物理学水平推导的严谨性不足。
- **统一框架**：同时覆盖线性网络、无限宽网络和有限深度-宽度比情形，且适用于任意温度，提供了宏观视角。
- **解析可解性**：在小深度-时间扰动下，得到了封闭的一阶解析表达式，可直接用于分析模型选择和特征学习。
- **激活函数普适性**：处理了光滑和ReLU两类常见非线性，推导了各自的具体判据。
- **层次清晰**：分别处理线性项和非线性项，并用扰动展开简化，便于理解和扩展。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏数值验证**：作为理论论文未给出任何数值实验或仿真，可能与实际有限尺寸网络的行为存在偏差，尤其是非渐近区域的适用性尚未检验。
- **近似假设较多**：推导依赖于NSDE极限（$N,L\to\infty$）、时间尺度变换（$P$ 大）、小$\tau$一阶展开、以及数据矩阵的共轭核全秩等假设。实际中这些假设未必同时成立，限制了结果的直接实用性。
- **扰动阶数限制**：仅考虑了$LP/N$的一阶项，更高阶项可能对后验或证据产生定性影响，该文未分析。
- **数据协方差简化**：在解释判据时假设 $\Phi_0 \approx \rho I$（正交等范数输入），这在实际高维数据中很少严格成立，结论的鲁棒性未知。
- **未覆盖带偏差网络**：模型假设无偏差项，实际MLP常常包含偏置。
- **缺少与其他贝叶斯近似方法（如变分推断、MCMC）的对比**，无法评估理论结果在近似质量上的优势。

（完）
