---
title: Leveraging tails for adaptation
title_zh: 利用尾部实现自适应
authors: "Sergios Agapiou, Ismaël Castillo, Paul Egels"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20480v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 研究带有重尾先验的贝叶斯后验收缩，展示神经网络的自适应能力
tldr: 非参数贝叶斯后验收缩率受先验尾部影响，系数先验采用p指数尾部（p<1重尾）. 通过让p→0，收缩率改善并在对数因子下完全适应光滑度. 应用于白噪声回归的级数先验和随机设计回归的浅层ReLU网络，过参数化网络可适应任意正则性0≤β≤2. 模拟实验验证了理论预测，为贝叶斯自适应提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20480v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1021, \"height\": 380, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20480v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1405, \"height\": 1323, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20480v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1405, \"height\": 1327, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20480v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1020, \"height\": 762, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20480v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1348, \"height\": 804, \"label\": \"Table\"}]"
motivation: 如何通过先验尾部设计实现非参数贝叶斯后验收缩的自适应，以提升估计效率。
method: 采用p指数尾部先验（p<1），通过极限p→0使收缩率最优，并应用于级数先验和浅层ReLU网络。
result: "p→0时后验收缩率在log因子下完全适应光滑度；浅层ReLU网络可适应任意β∈[0,2]的正则性。"
conclusion: 尾部自适应先验有效改进贝叶斯非参数收缩，理论分析与模拟结果一致。
---

## 摘要
我们考虑非参数设定下贝叶斯后验分布的收缩，其中函数在基或字典上的系数具有 $p$--指数尾部先验，包括拉普拉斯尾部 $(p=1)$ 和更重的尾部 $(p<1)$。研究表明，收缩速率随 $p$ 减小而提高，并且在适当的 $p\to 0$ 条件下，可实现完全适应光滑度（达到对数因子）。作为应用，我们考虑了白噪声回归中的级数先验和随机设计回归中的浅层ReLU神经网络。特别地，我们证明了过参数化的浅层ReLU网络能够适应任意正则性 $0\le β\le 2$。通过模拟研究，我们展示了与理论预测行为的高度实证一致性。

## Abstract
We consider contraction of Bayesian posterior distributions in nonparametric settings where coefficients of a function over a basis or dictionary are given priors with $p$--exponential tails, including Laplace tails $(p=1)$ and heavier tails $(p<1)$. It is shown that contraction rates improve as $p$ decreases and that full adaptation to smoothness, up to logarithmic factors, is obtained in an appropriate $p\to 0$ regime. As applications, we consider both series priors in white noise regression and shallow ReLU neural networks in random design regression. In particular, we show that overparametrised shallow ReLU networks can adapt to any regularity $0\le β\le 2$. Through a simulation study, we show strong empirical agreement with the behavior predicted by our theory.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：非参数统计中，估计器能否自动适应未知的函数光滑度等结构属性（即“自适应”）是一个核心目标。贝叶斯方法通过灵活的先验设计具有天然的自适应能力，但常用的高斯过程先验（GP）需要额外的超参数调节或采样，计算成本高，且在更精细的自适应（如复合结构、非均匀光滑度）方面存在局限。
- **背景**：已有研究表明，重尾先验（如拉普拉斯先验、p指数先验）能比高斯先验获得更快的收缩速率。特别地，Agapiou & Castillo (2024) 发现多项式尾部（如学生-t）先验仅需固定标度即可实现全自适应。本文旨在系统研究 p 指数先验在完整范围 \(0 < p \le 2\) 内的收缩性质，尤其关注 \(p \to 0\) 的极限情况，因为：
  - 理论上，\(p\) 越小收缩速率越快，可能无需超参数采样即可自适应。
  - 实践中，深度神经网络（DNN）中高斯权重输出的边际分布近似为 \(2/L\) 指数（\(L\) = 深度），因此 \(p \to 0\) 可模拟深层网络效应。
- **整体意义**：论文为贝叶斯非参数自适应提供了新途径：通过设计尾部足够重的先验（\(p \to 0\) 或多项式尾部），在不依赖额外超参数估计的情况下实现（准）最优收缩速率，并直接联系到过参数化神经网络的适应性质。

## 2. 方法论

### 核心思想
- 采用 **p指数尾部先验**（密度正比于 \(\exp(-|t|^p/p)\)，\(p>0\)），系数独立取自该分布。随着 \(p\) 减小，尾部变重。
- **关键技术细节**：
  1. **级数先验（白噪声模型）**：函数 \(f\) 在正交基上展开 \(f_k = \sigma_k \zeta_k\)，\(\sigma_k = k^{-1/2-\alpha}\)（多项式衰减），\(\zeta_k\) i.i.d. p指数分布。收缩速率 \(\varepsilon_n\) 依赖 \(p, \alpha, \beta\)（真实光滑度）。当 \(\alpha > \beta\)（过平滑先验）时，速率为 \(n^{-\beta/(2\beta + p(\alpha-\beta)+1)}\)，随 \(p\to 0\) 趋近 minimax 速率 \(n^{-\beta/(2\beta+1)}\)（仅差对数因子）。
  2. **自适应策略（\(p\to 0\) 极限）**：允许不同系数使用不同 \(p_k\)，如 \(p_k \propto 1/\log k\)，使得整体 \(p\) 随频率增加而减小，从而在保持先验过平滑的同时逼近最优速率。理论条件为 (16)-(17) 式，具体实例见 Corollary 1（多项式标度 + \(p_k = \log\log k / \log k\)，或超多项式标度 + \(p_k = c/\log^{1+\gamma} k\)）。
  3. **浅层 ReLU 神经网络先验（随机设计回归）**：网络宽度 \(N_\alpha\) 远大于“oracle”宽度 \(N_\beta\)（过参数化），权重 \(w_k \sim p\)指数分布，标度 \(\sigma_n\) 选择分为“oracle”（依赖 \(\beta\)）和“非 oracle”（独立）。Theorem 4 给出收缩速率，同样随 \(p\) 减小而改善。Theorem 5 采用 \(p_n = 2/\log n\) 和足够小的 \(\sigma_n = n^{-t}\) (\(t>2.5\))，实现几乎 minimax 的自适应速率（仅差 \(\sqrt{\log n}\) 因子）。
  4. **\(\rho\) 温化后验** (\(\rho<1\))：使用分数后验简化技术证明（避免熵估计）。文末提及部分结果可推广到 \(\rho=1\)。

### 主要定理
- **Theorem 1**（级数先验上界）：白噪声模型下，\(p>0\)，\(\alpha,\beta>0\)，后验以速率 \(\varepsilon_n\) 收缩（式 (13)）。
- **Theorem 2**（匹配下界）：\(p\in(0,1]\)，\(\alpha>\beta\)，存在 \(f_0\) 使得后验无法以快于 \(\varepsilon_n\) 的速率收缩（证明针对 \(p<1\)，\(p=1\) 在附录）。
- **Theorem 3**（自适应级数先验）：若 \(p_k\) 和 \(\sigma_k\) 满足 (16)-(17)，则后验以 minimax 速率（对数因子）收缩。
- **Theorem 4 & 5**（神经网络先验）：情形同上，Theorem 5 以 \(p_n=2/\log n\) 实现全自适应（\(\beta\in[0,2]\)）。

## 3. 实验设计

- **场景**：随机设计回归模型 \(y_i = f(x_i) + \xi_i\)，\(x_i\sim U[0,1]\)，\(\xi_i\sim N(0,\sigma^2)\)（\(\sigma=1/4\)）。真实函数 \(f_0\) 具有光滑度 \(\beta=1\)（基于余弦基展开）。
- **样本量**：\(n=400\) 和 \(n=4000\)。
- **对比方法**：
  - **级数先验**（基与真实函数相同）：固定 \(p=2,1,1/2,1/4\) 且 \(\alpha=2\)（过平滑）；变 \(p_k\) 先验（两种方案）；Cauchy 重尾先验（HT(α) 和 OT(γ)）作为性能基准。
  - **浅层神经网络先验**（宽度 \(N_{1/2}\approx \sqrt{n}\) 或 \(n\)）：固定 \(p=2,1,1/2,1/4\) 配合 oracle 或非 oracle 标度；变 \(p_n\) 先验（\(p_n=1/\log n\) 或如 (27) 式）。
- **Benchmark**：无显式外部基准，但内部比较了不同 \(p\) 和不同先验设计，并以 Cauchy 先验（已知自适应）作为最佳执行者参照。

## 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量、训练时长等算力信息。模拟仅涉及一维回归和中等样本量（\(n\leq 4000\)），使用 MCMC（wpCN 算法）采样，计算开销不大。未提及分布式或大规模实验。

## 5. 实验数量与充分性

- **实验数量**：共两大组（级数先验和神经网络先验），每组约 6～8 种先验配置，每种配置在 100 次数据复现下评估两种误差（后验均值的 L2 误差和后验收缩误差），并展示单次运行的后验均值和 95% 可信区间图。总计约 \(2\times 8\times 100=1600\) 次模拟运行（加上不同 n）。
- **充分性**：实验覆盖了论文主要理论预测（较小 \(p\) 性能更好，变 \(p\) 先验最优），与理论一致。但缺乏与其他已有自适应方法（如模型选择、挤压、经验 Bayes）的直接比较；且仅测试了 \(\beta=1\) 的一个真实函数，未全面验证不同 \(\beta\)（如 \(\beta=0.5, 2\)）下的行为。此外，仅一维输入，对高维泛化性未验证。

## 6. 主要结论与发现

- **理论结论**：
  - p指数先验的收缩速率随 \(p\) 减小而改进，在过平滑情形下尤其明显。
  - 通过让 \(p\to 0\)（适当选择 \(p_k\) 或 \(p_n\)），级数先验和神经网络先验均可实现（对数因子意义上的）全自适应，无需超参数采样。
  - 对于浅层 ReLU 网络，过参数化（宽度远大于最优宽度）结合足够重尾的权重先验，可自动适应 \(\beta\in[0,2]\) 的光滑度。
- **模拟结论**：
  - 所有固定 \(p\) 的先验中，\(p\) 越小误差越小；变 \(p\) 先验（\(p_k\) 或 \(p_n\) 递减）性能接近或优于 Cauchy 先验（已知最优）。
  - 神经网络先验中，非 oracle 标度（\(\sigma_n = \varepsilon_n^+/N_\alpha\)）配合小 \(p\) 也能获得近最优性能。
  - 结果与理论预测高度一致。

## 7. 优点

- **理论深度**：完整刻画了 p指数先验从 \(p=2\)（高斯）到 \(p\to 0\)（极重尾）的收缩速率谱，并给出匹配下界，证明速率紧性。
- **创新联系**：将 p指数先验与深层高斯网络输出的边际分布联系起来（\(p=2/L\)），为深层网络的理论分析提供了新视角。
- **自适应机制简洁**：无需分层超先验或超参数估计，仅需先验尾部足够重（\(p\to 0\)）即可实现自适应，简化了贝叶斯非参数方法。
- **实验支撑**：模拟实验验证了理论预测的主要趋势，图表清晰。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了 \(\beta=1\) 和一种真实函数，未验证其他光滑度级别或更复杂函数（如不连续、非均匀光滑）。仅一维输入，高维情况未涉及。
- **缺乏公平比较**：未与标准自适应方法（如 Lepski 方法、经验 Bayes、交叉验证）进行性能对比，无法评估实际增益。
- **数值采样效率**：wpCN 算法对非高斯先验需要更小的步长和更多迭代，计算成本在 \(p\) 很小时增加，但论文未给出具体计算时间比较。
- **理论假设限制**：
  - 主要结果针对 \(\rho<1\) 的温化后验；虽然声称可能推广到 \(\rho=1\)，但未在文中对所有模型证明。
  - 神经网络部分假设隐藏层偏置固定且网格均匀，对随机或学习的偏置情形未讨论。
  - 理论速率含对数因子，实际有限样本下可能不显著。
- **应用限制**：过参数化网络宽度与样本量同阶（如 \(N_0 \approx n\)）导致计算昂贵，按文中 Remark 4 可用较小宽度（如 \(N_{0.5}\)）但只能适应 \(\beta\ge 0.5\)，局限了适用范围。

（完）
