---
title: Function-Space Priors for Bayesian Neural ODEs with Application to Vessel Trajectory Prediction
title_zh: 贝叶斯神经ODE的函数空间先验及其在船舶轨迹预测中的应用
authors: "Jaeyeong Lee, Wonmo Koo, Heeyoung Kim"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06351v1"
tags: ["query:bayes-dl"]
score: 9.0
evidence: 将函数空间先验应用于贝叶斯神经常微分方程以量化不确定性
tldr: 针对AIS数据的不规则采样、缺失报告和复杂动力学导致的船舶轨迹预测挑战，现有贝叶斯神经ODE使用各向同性高斯权重先验无法编码轨迹的平滑性和局部结构。本文在神经ODE的向量场上施加GP核先验，通过正则化项惩罚向量场偏离GP结构，并结合概率多重射击处理长轨迹。该方法在保留不确定度量的同时，有效提升了轨迹预测的精度与校准性，实现了连续时间建模与结构先验的统一。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06351v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 967, \"height\": 636, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06351v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 796, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06351v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 789, \"height\": 402, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06351v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 193, \"label\": \"Table\"}]"
motivation: 贝叶斯神经ODE的权重先验无法编码轨迹的平滑性与局部性，现有函数空间贝叶斯方法不能直接用于神经ODE。
method: 在向量场测量点上施加GP核先验，通过核正则化项增强标准变分目标，并与概率多重射击结合处理长序列。
result: 方法能处理不规则采样和长轨迹，提升预测精度和不确定性校准。
conclusion: 融合函数空间先验与概率多重射击，实现了结构化先验在神经ODE中的有效应用。
---

## 摘要
从自动识别系统（AIS）数据中预测船舶轨迹对于海上态势感知至关重要，但由于不规则采样、缺失报告和复杂动态，该任务仍具有挑战性。除了精确的点预测外，海事应用还需要校准良好的不确定性估计以实现可靠决策。贝叶斯神经常微分方程（ODE）通过对神经向量场参数设置先验，为具有不确定性量化的连续时间轨迹建模提供了原则性框架。然而，常用的各向同性高斯权重先验无法编码船舶动态的信息性结构属性，如平滑性和局部性。现有的函数空间贝叶斯神经网络方法解决了静态映射的这一局限性，但不能直接迁移到神经ODE，因为其中主要关注的是轨迹而非向量场本身。原则上，可以直接在ODE解上放置高斯过程（GP）先验，但这需要通过非线性ODE求解器传播分布，这在解析上是难以处理的。为应对这一挑战，我们采用了一种实用方法，即在有限测量点集合上直接对向量场施加基于GP核的先验。具体而言，我们通过一个基于核的正则化项增强标准权重空间变分目标，该正则化项惩罚向量场偏离GP先验所隐含的结构。为了处理长且不规则的AIS轨迹，我们进一步将此函数空间正则化与概率多点打靶法相结合，该方法在时间片段间解耦推理同时保持全局一致性。

## Abstract
Vessel trajectory prediction from Automatic Identification System (AIS) data is essential for maritime situational awareness, yet it remains challenging due to irregular sampling, missing reports, and complex dynamics. Beyond accurate point forecasts, maritime applications also demand well-calibrated uncertainty estimates for reliable decision-making. Bayesian Neural Ordinary Differential Equations (ODEs) offer a principled framework for continuous-time trajectory modeling with uncertainty quantification by placing a prior over the neural vector field parameters. However, the commonly used isotropic Gaussian weight prior fails to encode informative structural properties of vessel dynamics, such as smoothness and locality. Existing function-space Bayesian neural network methods address this limitation for static mappings, but do not transfer directly to Neural ODEs, where the primary quantity of interest is the trajectory rather than the vector field itself. In principle, one could place a Gaussian process (GP) prior directly over ODE solutions, but this requires propagating distributions through a nonlinear ODE solver, which is analytically intractable. To address this challenge, we adopt a practical approach that imposes a GP-kernel-based prior directly on the vector field evaluated at a finite set of measurement points. Specifically, we augment the standard weight-space variational objective with a kernel-based regularizer that penalizes deviations of the vector field from the structure implied by a GP prior. To handle long and irregular AIS trajectories, we further combine this function-space regularization with probabilistic multiple shooting, which decouples inference across temporal segments while maintaining global consistency.

---

## 论文详细总结（自动生成）

# 论文总结：《贝叶斯神经ODE的函数空间先验及其在船舶轨迹预测中的应用》

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：从AIS数据预测船舶轨迹对海上态势感知至关重要，但面临不规则采样、缺失报告、复杂动力学（如航向变化）等挑战。除点预测外，实际应用还需要校准良好的不确定性估计。
- **现有问题**：贝叶斯神经ODE通过对向量场参数施加权重先验实现不确定性量化，但常用的各向同性高斯权重先验无法编码船舶动态的结构化属性（如平滑性、局部性）。函数空间贝叶斯神经网络（fBNN）可对静态映射施加GP先验，但无法直接迁移到神经ODE，因为主要关注的是轨迹而非向量场本身。直接对轨迹施加GP先验需要通过非线性ODE求解器传播分布，解析上不可行。
- **论文目标**：提出一种实用方法，在神经ODE的向量场上施加基于GP核的函数空间先验，结合概率多点打靶，在保留灵活性同时注入可解释的结构化偏置，提升轨迹预测精度和不确定性校准。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 在贝叶斯神经ODE的标准权重空间变分目标中加入一个基于GP核的向量场正则化项，该项在有限测量点集合上评估，惩罚向量场偏离GP先验所隐含的结构（如平滑性）。这样避免了直接处理轨迹级GP先验的难解性。
  - 结合概率多点打靶法，将长序列分割为多个短片段，在每个片段内独立进行ODE积分，并通过软连续性约束保持全局一致性。

- **关键技术细节**：
  - **状态表示**：AIS观测包括位置（ENU坐标）、SOG、COG（正弦余弦编码），构成5维状态 \( y \in \mathbb{R}^5 \)。
  - **潜在ODE**：连续时间动力系统 \( dx/dt = f_\theta(x,t) \)，\( f_\theta \) 为神经网络参数化的向量场。
  - **概率多点打靶**：将时间区间划分为K个片段，引入可学习的边界状态 \( s_k \)。每个片段内通过ODE积分得到轨迹，并通过软连续性约束 \( p(s_{k+1}|s_k,\theta) = \mathcal{N}(s_{k+1}; \Phi_{\tau_k\to\tau_{k+1}}(s_k;\theta), Q_k) \) 连接相邻片段。
  - **函数空间先验**：选择一组测量点 \( Z = \{z^{(m)}\}_{m=1}^M \)，收集向量场在该点处的评估值 \( F_Z(\theta) \)。施加可分离GP先验：\(\text{vec}(F_Z(\theta)) \sim \mathcal{N}(0, K_Z \otimes I_d)\)，其中核矩阵\( K_Z \)由光滑核（如平方指数或Matérn）计算。对应的正则化项 \( R_{FS}(\theta) = \frac{1}{2} \sum_{j=1}^d f_j^\top K_Z^{-1} f_j + \frac{d}{2} \log |K_Z| + const.\)，直接加入变分目标。
  - **测量点选择**：两种策略：(i) k-means聚类轨迹状态获得代表点；(ii) 机动加权采样（集中关注转向、减速事件区域）。
  - **变分目标**：最大化正则化的变分下界：
    \[
    \mathcal{J}(\phi) = \mathcal{L}_{\text{shoot}}(\phi) - \lambda_{FS} \mathbb{E}_{q_\phi(\theta)}[R_{FS}(\theta)]
    \]
    其中 \(\mathcal{L}_{\text{shoot}}\) 是包含数据对数似然、连续性约束、熵和KL散度的标准射击ELBO。
  - **预测**：从变分后验采样 \( \theta^{(s)} \) 和 \( s_K^{(s)} \)，前向积分得到未来轨迹，蒙特卡洛混合得到预测分布。

- **算法流程**（文字说明）：
  - 阶段1（先验构造）：使用训练集构造测量点集Z和核超参数。
  - 阶段2（每轨迹拟合）：对每条测试轨迹，初始化新模型，仅使用历史观测（5-10点，10-20分钟）优化目标函数，固定步数。
  - 阶段3（预测）：从后验采样并前向积分得到未来10分钟轨迹及不确定性。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：纽约/新泽西港区的真实AIS数据（2024年1月1-7日），经预处理后得到1,589条轨迹（每条至少20个观测点、时长≥15分钟），按船舶身份7:1:2划分训练/验证/测试。状态为5维（x,y,SOG,sin COG,cos COG）。
- **Benchmark**：采用滑动窗口，历史窗口10-20分钟，预测未来10分钟。
- **对比方法**：
  - GPODE（高斯过程向量场，基于稀疏诱导变量）
  - WS-BayesNODE（贝叶斯神经ODE + 标准各向同性高斯权重先验）
  - FS-BayesNODE（k-means）（本文方法，测量点由k-means选择）
  - FS-BayesNODE（maneuver）（本文方法，测量点由机动加权采样选择）
- **评估指标**：ADE（平均位移误差）、FDE（最终位移误差）、NLL（负对数似然）、CRPS（连续等级概率评分），均针对位置坐标（km）。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量或训练时长**。仅提及使用Adam优化器，向量场为单隐含层MLP（32个单元），概率多点打靶设K=1（因为历史窗口短）。函数空间正则化开销为 \( O(M^3 + M d) \)，M通常20-50，可忽略。未提供训练收敛时间或硬件细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 主结果表1：在40条测试轨迹上报告均值与标准差。
  - 定性对比图3：单条轨迹的预测不确定性可视化。
  - 测量点选择消融：比较k-means与机动加权两种策略。
  - 贝叶斯神经ODE变体之间的内部消融（有无函数空间正则化）。
- **充分性与公平性**：
  - **充分性**：覆盖了点预测、概率预测（NLL、CRPS）和可视化，对比了三种代表性基线（GPODE、WS-BayesNODE）及本文变体。消融实验设计合理。
  - **局限性**：仅使用单一AIS数据集（纽约/新泽西港区），未在多个港口或不同环境条件下验证。测试轨迹仅40条，样本量偏小。未与更多现代序列预测方法（如LSTM编码的贝叶斯方法、Transformer等）比较。此外，所有方法采用相同的每轨迹推理协议，但GPODE在此设置下表现显著较差，可能部分由于拟合不充分（每轨迹仅5-10个观测点），但作者未深入讨论该偏差。实验公平性总体较好（相同协议、相同历史/预测长度），但可复现性细节（如种子、超参数选择）未充分给出。

## 6. 主要结论与发现

- FS-BayesNODE（maneuver）在所有指标上最优：ADE 2.66 km, FDE 3.82 km, CRPS 1.41 km，相比GPODE降低误差约40%，相比WS-BayesNODE也有提升。
- 函数空间正则化显著改善了不确定性校准：CRPS（1.41-1.51 vs GPODE 4.57）表明后验更集中；NLL也远低于GPODE（18.9 vs 29.46）。
- 机动加权测量点优于k-means，在动态复杂区域（转向、减速）提供更有效的正则化。
- 定性展示：GPODE的不确定性过早发散，而FS-BayesNODE的不确定性随预测时间平滑增长。

## 7. 优点

- **方法创新性**：首次将函数空间先验引入神经ODE的向量场正则化，避免了轨迹级GP的难解性，提供可解释的结构偏置（如平滑性），且保持神经网络灵活性。
- **实际有效性**：在真实AIS数据上验证了改进，不确定性校准显著优于GPODE，对抗不规则采样和缺失报告具有鲁棒性。
- **计算友好**：概率多点打靶使学习稳定且可扩展，函数空间正则化开销小（M小，核矩阵可预计算）。
- **分析细致**：比较了两种测量点选择策略，揭示了机动加权采样的优势，体现了设计考虑。

## 8. 不足与局限

- **实验覆盖**：仅使用一个港口数据集（New York/New Jersey），未在多个地理区域或不同船舶类型下验证，通用性存疑。测试轨迹仅40条，统计显著性未报告。
- **对比基线**：未与现代序列预测方法（如LSTM-based贝叶斯模型、ODE-RNN、Neural Flow等）比较，仅在贝叶斯ODE框架内对比。
- **GPODE在每轨迹协议下的低性能**：作者未深入分析原因（可能因历史点少导致GP后验过度发散），且未尝试调整GPODE的超参数使其在该协议下公平对比。
- **可复现性**：未提供核函数具体配置、超参数λ_FS选择过程、优化步数等细节，限制了复现能力。
- **应用限制**：假设单个全局向量场无法捕捉船舶间不同动态模式（意图），采用每轨迹推理避免了此问题，但也意味着每轨迹需单独优化，实时性可能受限。未探讨跨轨迹迁移学习或元学习方案。
- **理论局限性**：函数空间正则化仅在有限测量点上生效，本质上是向量场上的先验而不是轨迹级先验，可能无法保证全局轨迹平滑性。作者在Remark 1中提供了广义贝叶斯解释，但并未证明正则化项与理想GP先验之间的理论等价性。

（完）
