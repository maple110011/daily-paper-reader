Title: Deterministic Leave-One-Cluster-Out Cross-Validation for Multilevel Bayesian Structural Equation Models

URL Source: https://arxiv.org/pdf/2609.00670v1

Published Time: Wed, 02 Sep 2026 00:52:05 GMT

Number of Pages: 43

Markdown Content:
DETERMINISTIC LOCO FOR MULTILEVEL SEM 1

Deterministic Leave-One-Cluster-Out Cross-Validation for Multilevel Bayesian Structural Equation Models 

Mohammad Alhyari 1, Haziq Jamil 1,2 , Hans Montcho 1, and Håvard Rue 11King Abdullah University of Science and Technology 

> 2

Universiti Brunei Darussalam 

Author Note 

Mohammad Alhyari https://orcid.org/0009-0009-8770-4660 Haziq Jamil https://orcid.org/0000-0003-3298-1010, Hans Montcho https://orcid.org/0000-0003-2510-2102, Håvard Rue https://orcid.org/0000-0002-0222-1881. Correspondence concerning this article should be addressed to Haziq Jamil, Computer, Electrical and Mathematical Sciences and Engineering (CEMSE) Division, King Abdullah University of Science and Technology, Thuwal, 23955-6900, Kingdom of Saudi Arabia. E-mail: haziq.jamil@kaust.edu.sa. arXiv:2609.00670v1 [stat.ME] 1 Sep 2026 DETERMINISTIC LOCO FOR MULTILEVEL SEM 2

Abstract 

We introduce a closed-form, refit-free procedure for leave-one-cluster-out ( loco )cross-validation in multilevel Gaussian Bayesian structural equation models (SEMs), together with predictive scoring of every nested submodel. Conditional independence of clusters given the parameters expresses the cluster-deleted posterior as a functional of the full posterior. The loco predictive density is then a harmonic mean of the cluster likelihood, computable from a single fit in INLAvaan , the integrated nested Laplace approximation package for Bayesian SEM. We evaluate the harmonic-mean expectation in closed form through a fully exponential Laplace approximation of the reciprocal cluster likelihood under a Gaussian posterior; candidate structural restrictions follow by Gaussian conditioning of the same Laplace summary. The resulting Taylor elpd (expected log predictive density) scores are fully deterministic, requiring neither refitting nor Monte Carlo sampling, so the variance pathology of the naive harmonic-mean estimator does not arise. A direct-sum decomposition of the compound-symmetric cluster covariance makes the cost independent of cluster size, orders of magnitude below brute-force refitting. We validate against brute-force refits and Markov chain Monte Carlo in simulation, and illustrate the procedure on school-safety climate in PISA 2022 and on 16 candidate structures linking personality and well-being in the MIDUS sibling sample. 

Keywords: Bayesian structural equation modelling; cross-validation; integrated nested Laplace approximation; multilevel models; leave-one-cluster-out DETERMINISTIC LOCO FOR MULTILEVEL SEM 3

Deterministic Leave-One-Cluster-Out Cross-Validation for Multilevel Bayesian Structural Equation Models 1 Introduction 

Bayesian structural equation modelling (SEM) has matured into a standard tool in psychometric practice, with implementations spanning the major Markov chain Monte Carlo (MCMC) samplers (Muthén & Asparouhov, 2012) and, more recently, the integrated nested Laplace approximation (INLA) framework (Rue et al., 2009; van Niekerk et al., 2023). This machinery has since been carried into SEMs with normally distributed outcomes (Jamil & Rue, 2026a), where the latent variables are integrated out analytically. Inference begins with a mode-finding step on the model parameters and a Laplace approximation about that mode, which further corrections refine into skewness-adjusted posterior marginals. That work is implemented in the INLAvaan 

package (Jamil & Rue, 2026b), which takes models in familiar lavaan syntax (Rosseel, 2012) and returns accurate posterior summaries in a fraction of the time. With this widespread adoption, the substantive questions tasked to Bayesian SEM have scaled significantly in complexity. Rather than choosing between a small set of theoretically motivated competitors, researchers increasingly navigate large spaces of candidate cross-loadings, structural regressions, and latent hierarchies (Jacobucci et al., 2016; Lu et al., 2016). Structure selection , deciding which paths to include, is variable selection carried out on the latent structure, since a freed path admits a predictor to a structural equation. It is no longer a post-hoc check but a central modelling step, with direct implications for construct validity and theoretical parsimony. The natural Bayesian tool for this selection step is predictive cross-validation. Leave-one-out (LOO) estimates of the expected log predictive density (elpd) are by now standard practice in the Bayesian workflow (Gelman et al., 2026; Vehtari et al., 2017), providing a criterion grounded in out-of-sample generalisation rather than in-sample fit. For SEM in particular, predictive criteria are an attractive alternative to traditional fit indices such as the root mean square error of approximation (RMSEA), the comparative fit index (CFI), and the Tucker–Lewis index (TLI), whose theoretical grounding assumes independent observations and large samples and whose behaviour under hierarchical dependence is at best opaque (Hsu et al., 2015; Ryu & West, 2009). Predictive criteria carry one important nuance in the multilevel case. As Merkle et al. (2019) make explicit, observation-level LOO for clustered data answers a conditional 

prediction question: predicting a new subject within an already-observed cluster, with the cluster’s posterior random effect supplying borrowed strength. The natural target for generalisation across populations is instead the marginal predictive density of an entirely new cluster, with the cluster effect integrated against its prior, the marginal latent distribution, rather than against its data-conditional posterior. The two targets DETERMINISTIC LOCO FOR MULTILEVEL SEM 4differ both numerically and in what they reward. Conditional leave-one-subject-out (loso ) favours models that fit cluster-specific idiosyncrasies, while marginal leave-one-cluster-out ( loco ) favours models that capture stable population-level structure. For structure selection, the goal is generalisation across populations, not interpolation within an observed cluster, and hence loco is the appropriate target. Software practice took some time to adapt, for reasons that were computational rather than conceptual. Deleting a cluster deletes the latent effects attached to it, so the density of the held-out block is not the conditional one a sampler already evaluates but a marginal one, obtained by integrating those effects out (Merkle et al., 2019). Where that integral has no closed form it must be computed for every cluster at every draw, whereas the observation-level score conditions on the latent effects and requires no integral at all, and loso became the default even for multilevel models (see Section 4.1). Analytic marginalisation removes that obstacle. blavaan ’s Stan implementation integrates the latent variables out in closed form for continuous outcomes (Merkle et al., 2021), so a cluster-level marginal likelihood comes as a by-product of the fit. From there the score is routine, since the loo package (Vehtari et al., 2017) requires only that density evaluated at every draw for every cluster, and Pareto-smoothed importance sampling (PSIS) applied to such a matrix returns a loco score from a single fit, with no refits. However, this route requires a full MCMC posterior sample and returns an importance-sampling estimate whose accuracy degrades precisely where a held-out cluster is influential enough to matter for model choice (Merkle, 2026). This paper supplies the required loco target by other means. We show that the 

loco predictive density for a multilevel Gaussian Bayesian SEM admits a closed-form evaluation requiring no refits, no posterior sampling, and no importance weights. The construction rests on three observations: 1. The cluster-level predictive density p(yj | ϑ) of a cluster’s responses on p

indicators, obtained by integrating out the within- and between-cluster latent effects, is a multivariate Gaussian with compound-symmetric covariance. An explicit direct-sum decomposition into within- and between-cluster subspaces reduces all log-determinants, inverses, and quadratic-form evaluations to two p × p

factorisations, at a cost independent of cluster size (Rosseel, 2021). 2. The cluster-deleted posterior satisfies the reweighting identity 

π(ϑ | y−j ) ∝ π(ϑ | y)/p (yj | ϑ), a direct consequence of conditional independence of clusters given ϑ. The loco predictive density then collapses to a multivariate harmonic-mean cluster-level conditional predictive ordinate (CPO; Gelfand et al., 1992) computable from quantities that an encompassing Laplace-approximated fit in INLAvaan already produces. DETERMINISTIC LOCO FOR MULTILEVEL SEM 53. The harmonic-mean expectation itself admits a fully exponential Laplace approximation (Tierney & Kadane, 1986), replacing a variance-pathological Monte Carlo estimator with a deterministic value of zero variance and controlled bias. First- and second-order forms are available, differing in asymptotic accuracy. The same Laplace summary supports structure selection. Candidate single-path restrictions become linear constraints, and the restricted Laplace summary follows from closed-form Gaussian conditioning, a rank-one update when a single path is restricted. Every submodel nested within the encompassing model is therefore scored from the same fit (Section 3.3), at a cost far below refitting each one in turn. The approximations developed here are efficient enough to enumerate a moderately sized structural hypothesis space exhaustively, rather than search it. 

2 Multilevel Structural Equation Models and the Prediction Target 

We work throughout with continuous indicators and Gaussian latent variables, the regime in which the closed-form derivations below hold exactly. A multilevel SEM is assembled from a measurement equation and a structural equation written twice, once for the within-cluster component and once for the between-cluster component. We therefore fix notation first for the single-level model which supplies that pair, and build the two-level version from it. Section 4 describes the loso target as a special case, appropriate for predicting a new subject in an observed cluster. For subjects s = 1 , . . . , N and indicators i = 1 , . . . , p , the measurement model relates a p-vector of observed responses ys ∈ Rp to a q-vector of latent constructs 

ηs ∈ Rq via 

ys = ν + Ληs + ϵs, ϵs ∼ N p(0 , Θ), (1) with intercept ν ∈ Rp, factor loadings Λ ∈ Rp×q, and residual covariance Θ ∈ Rp×p

(typically diagonal). The structural model relates latent constructs to one another and to a constant: 

ηs = α + Bηs + ζs, ζs ∼ N q(0 , Ψ), (2) with α ∈ Rq, B ∈ Rq×q the matrix of structural regression coefficients (the object of structure selection) and Ψ ∈ Rq×q the latent disturbance covariance. We collect the SEM parameters as Rd ∋ ϑ = {ν, Λ, Θ, α, B, Ψ} and write π(ϑ) for their prior. The intercepts ν and α carry priors like any other element of ϑ, and the mean structure is estimated throughout this paper. MCMC implementations do the same and supply intercepts whether or not the user asks for them (Merkle & Rosseel, 2018), which keeps the comparisons of Section 5 like for like. This is a convention, not a restriction. The means may instead be marginalised under a flat prior, in which case each unit is scored by its conditional density given the rest of the sample. That kernel is Gaussian as well, and everything derived below applies to it unchanged. DETERMINISTIC LOCO FOR MULTILEVEL SEM 6Integrating out the latent state ηs yields ys | ϑ ∼ N p

> (

μ(ϑ) , Σ(ϑ))

, the Gaussian reduced form, with 

μ(ϑ) = ν + Λ(I − B)−1α, Σ(ϑ) = Λ(I − B)−1Ψ(I − B)−⊤ Λ⊤ + Θ. (3) This reduced form makes explicit that the structural matrix B acts on the data only through the convolution ( I − B)−1Ψ(I − B)−⊤ , the same operation that, applied once at each level, underlies the cluster reduced form. In the multilevel case, subjects 

s = 1 , . . . , n j are nested in clusters j = 1 , . . . , J , and we adopt the standard within-between decomposition (Muthén, 1994) 

ysj = ν + yb 

> j

+ yw 

> sj

,

in which the within-cluster component yw 

> sj

and the between-cluster component yb 

> j

are each governed by a measurement-and-structural model of the form (1)–(2). At the within level, 

yw 

> sj

= Λwηw 

> sj

+ ϵw 

> sj

, ϵw 

> sj

∼ N p(0 , Θw), ηw 

> sj

= Bwηw 

> sj

+ ζw 

> sj

, ζw 

> sj

∼ N qw (0 , Ψw),

with within-level loadings Λw ∈ Rp×qw , within-level latent state ηw 

> sj

∈ Rqw , structural matrix Bw ∈ Rqw×qw , and disturbance covariance Ψw. The within-level latent state carries no intercept, since within-cluster latent variables are deviations from their cluster means and so have mean zero by construction. At the between level, 

yb 

> j

= Λbηb 

> j

+ ϵb 

> j

, ϵb 

> j

∼ N p(0 , Θb), ηb 

> j

= αb + Bbηb 

> j

+ ζb 

> j

, ζb 

> j

∼ N qb (0 , Ψb), (4) with between-level loadings Λb ∈ Rp×qb , between-level latent state ηb 

> j

∈ Rqb , structural intercept αb ∈ Rqb , structural matrix Bb ∈ Rqb×qb , and disturbance covariance Ψb. As in the single-level case, the matrices Bw and Bb collect the structural regression coefficients. The ϑ parameters are now expanded to include the within- and the between-cluster parameters {ν, Λw, Bw, Ψw, Θw, Λb, αb, Bb, Ψb, Θb}. In a similar reduced-form fashion, the respective corresponding model-implied covariances can be formed as 

Σw(ϑ) = Λw(I − Bw)−1Ψw(I − Bw)−⊤ (Λw)⊤ + Θw,

Σb(ϑ) = Λb(I − Bb)−1Ψb(I − Bb)−⊤ (Λb)⊤ + Θb, (5) both p × p, while the model-implied mean as μ(ϑ) = ν + Λb(I − Bb)−1αb, to which the within level contributes nothing. Stacking the observations within a cluster as 

yj = ( y⊤ 

> 1j

, . . . , y⊤  

> njj

)⊤ ∈ Rnj p, the cluster reduced form derived by Rosseel (2021) is 

yj | ϑ ∼ N nj p

> (

1nj ⊗ μ(ϑ), Vj (ϑ))

, Vj (ϑ) = Inj ⊗ Σw + Jnj ⊗ Σb, (6) DETERMINISTIC LOCO FOR MULTILEVEL SEM 7with Vj (ϑ) ∈ Rnj p×nj p and Jnj = 1nj 1⊤ 

> nj

. The cluster covariance is compound-symmetric in block form, with Σw on the diagonal of each unit’s block and the shared between-cluster effect ηb 

> j

adding Σb to every cross-unit block within the cluster. The structural matrices Bw and Bb are confined entirely to the maps ϑ 7 → Σw

and ϑ 7 → Σb in (5) and to ϑ 7 → μ. The clusters are, moreover, conditionally independent given ϑ,

p(y | ϑ) = 

> J∏
> j=1

p(yj | ϑ), (7) which will underwrite the cluster-deleted posterior identity of Section 3. Evaluated naively, the nj p-dimensional Gaussian (6) costs O(

(nj p)3)

per cluster; but as Rosseel (2021) notes, the compound-symmetric structure of Vj removes the dependence on cluster size, since Vj decomposes over the within- and between-cluster subspaces spanned by the orthogonal projectors Inj − n−1 

> j

Jnj and n−1 

> j

Jnj . Every log-determinant, inverse, and quadratic form then reduces to operations on the two 

p × p matrices Σw and Σw + nj Σb. With ¯ yj = n−1 

> j
> ∑nj
> i=1

yij the cluster mean and 

Sj = ∑nj

> i=1

(yij − ¯yj )( yij − ¯yj )⊤ the within-cluster scatter matrix, the Gaussian log-likelihood splits along the same two subspaces, log p(yj | ϑ) = −12

> [

nj p log(2 π) + log det Vj + Qw 

> j

(ϑ) + Qb 

> j

(ϑ)

> ]

, (8) with log det Vj = ( nj − 1) log det Σw + log det (

Σw + nj Σb

> )

and within- and between-cluster quadratic forms 

Qw 

> j

(ϑ) = tr (

Σ−1w Sj

> )

, Qb 

> j

(ϑ) = nj (¯ yj − μ)⊤(

Σw + nj Σb

> )−1

(¯ yj − μ).

The data enter only through the sufficient statistics (¯ yj , Sj , n j ), formed once before inference, and each subsequent evaluation of the cluster log-likelihood log p(yj | ϑ) then costs O(p3) for two matrix factorisations and O(nj p2) for the quadratic forms, with the cubic dependence on cluster size eliminated. The decomposition presumes that every coordinate varies within clusters, so that Σw is positive definite. A cluster-constant coordinate occupies a null block of Σw and calls instead for the general two-level Gaussian likelihood of Rosseel (2021, Eqs. 21–24), of which (8) is the special case with no between-only coordinates. Exogenous covariates are accommodated by modelling them jointly with the indicators, in the manner of the all-y parameterisation (Bollen, 1989). That is, a covariate xsj is appended to the response vector, so its mean, variance, and covariances become free parameters, and the cluster reduced form (6) and the conditional independence (7) continue to hold with p enlarged to include the covariates. Covariates that vary within clusters enter both Σw and Σb and require nothing new, whereas DETERMINISTIC LOCO FOR MULTILEVEL SEM 8cluster-level covariates are constant within a cluster, contribute no within-cluster variation, and enter only the between covariance Σb, a case handled by the general two-level Gaussian likelihood. The alternative convention of conditioning on the covariates induces a different predictive estimand, set out in Appendix A together with the exact relation between the two. For multilevel SEM, observation-level LOO can be cast as either of two distinct prediction tasks, differing only in the distribution against which the cluster random effect ηb 

> j

is integrated. The conditional (loso ) target predicts a new subject in an observed cluster: ηb 

> j

is integrated against its posterior p(ηb 

> j

| yj, −s, ϑ), so the held-out subject’s own clustermates inform the cluster effect and the prediction borrows their strength, 

p(ysj | yj, −s, y−j ) = 

> ∫∫

p(ysj | ηb 

> j

, ϑ) p(ηb 

> j

| yj, −s, ϑ) dηb 

> j

π(ϑ | y−(sj )) dϑ. (9) The marginal (loco ) target predicts an entirely new cluster: there are no clustermates to estimate ηb 

> j

from, so it is integrated against its prior, the marginal latent distribution 

Nqb

> (

(I − Bb)−1αb, (I − Bb)−1Ψb(I − Bb)−⊤ )

implied by the between-level structural model (4), 

p(yj | y−j ) = 

> ∫∫

p(yj | ηb 

> j

, ϑ) p(ηb 

> j

| ϑ) dηb 

> j

π(ϑ | y−j ) dϑ =

> ∫

p(yj | ϑ) π(ϑ | y−j ) dϑ.

(10) The second equality uses the cluster reduced form (6), in which this latent integral has already been carried out. The choice between the two is not a matter of taste but of which generalisation question is being asked. Because subjects within a cluster are correlated through the shared effect ηb 

> j

, leaving out a single subject leaves that subject’s cluster effect still estimable from the rest of the cluster. loso therefore rewards a model for fitting cluster-specific idiosyncrasy, which does not transfer to a new population. Leaving out an entire cluster removes every observation that identifies ηb 

> j

, so loco 

rewards only population-level structure that generalises across clusters. We therefore take (10) as our target and summarise it by the summed log-predictive score elpd loco =

> J∑
> j=1

log p(yj | y−j ),

with larger values indicating better generalisation to a new cluster. 

3 Leave-One-Cluster-Out From a Single Model Fit 

The loco target (10) is an integral against the cluster-deleted posterior 

π(ϑ | y−j ), and computing that posterior directly by refitting the model for each of the 

J clusters is precisely the computational bottleneck this framework eliminates. The next two subsections remove it, reducing the target to one expectation against the DETERMINISTIC LOCO FOR MULTILEVEL SEM 9full-data posterior and then evaluating that expectation in closed form. The two after them repeat the pattern one level down, for a candidate submodel scored by conditioning the same Laplace summary rather than by its own fit. 

3.1 Exact Cluster-Deletion Identity 

As seen from (7), cluster j enters the full-data likelihood through a single factor, so deleting it reweights the full posterior rather than requiring a fresh fit. The following well-known proposition bypasses these refits entirely, collapsing (10) into a single expectation against the full-data posterior. 

Proposition 1 (loco as a posterior harmonic mean) . If the full-data likelihood factorises at cluster j as p(y | ϑ) = p(yj | ϑ) p(y−j | ϑ), as conditional independence of the clusters (7) guarantees, then for every cluster j the loco predictive density (10) 

satisfies the exact identity 

p(yj | y−j ) = 

> (

Eϑ|y

> [

1/p (yj | ϑ) ] )−1

, (11) 

where the expectation is taken against the full-data posterior π(ϑ | y).Proof. By the assumed factorisation, Bayes’ theorem immediately yields the cluster-deleted posterior as a direct reweighting of the full posterior, 

π(ϑ | y−j ) ∝ π(ϑ | y)

p(yj | ϑ), (12) with normalising constant Zj := Eϑ|y  

> [1
> p(yj|ϑ)
> ]

. Normalising and substituting (12) into the predictive target (10) yields 

p(yj | y−j ) = 

> ∫
>  

p(yj | ϑ) 1

Zj

π(ϑ | y)

>  

p(yj | ϑ) dϑ = 1

Zj

> ∫

π(ϑ | y) dϑ = Z−1 

> j

,

the last step using the fact that π(ϑ | y) integrates to one. This is (11). The right-hand quantity in (11) is the multivariate cluster-level CPO, and Proposition 1 delivers this target in a tractable form. Every cluster-deleted refit is gone; the right-hand side is a functional of the single posterior π(ϑ | y), so one encompassing fit scores all J clusters at once. The reduction is also exact and analytic. For comparison, the leave-group-out construction of Liu et al. (2025) for general latent Gaussian models targets group likelihoods that are typically non-Gaussian, and so retains an inner numerical integration over the latent state. Here, the Gaussian cluster reduced form (6) makes that inner integral closed-form, and Proposition 1 collapses the outer integral over ϑ as well. The loco score for the multilevel Gaussian SEM is therefore both refit-free and quadrature-free. DETERMINISTIC LOCO FOR MULTILEVEL SEM 10 What remains is to evaluate the posterior expectation in (11). Even if posterior samples are readily available, either as a by-product of MCMC or from the Gaussian copula draws that INLAvaan supplies (Jamil & Rue, 2026b), a direct Monte Carlo average over them must be avoided. This is because such an average is the cluster-level counterpart of the harmonic-mean estimator of the marginal likelihood, and inherits its variance pathology. The integrand 1 /p (yj | ϑ) has heavy tails wherever the cluster likelihood is small over a region of non-negligible posterior mass, and the resulting Monte Carlo estimator can have infinite variance (Wolpert & Schmidler, 2012). The next section instead evaluates (11) deterministically, by a Laplace approximation that has zero variance by construction and so sidesteps the pathology entirely, at a cost far below that of stabilising a Monte Carlo estimate. 

3.2 A Deterministic Laplace Approximation of the Cluster CPO 

We turn to the expectation, approximating it by a fully exponential Laplace approximation carried out with the cluster log-likelihood inside the exponent. Let 

L(ϑ) = log π(ϑ | y) and let N (ϑ∗, Ω) be its Gaussian summary, in which ϑ∗ is the centre at which the expansion is taken and Ω−1 := −∇ 2L the curvature at the posterior mode. For each cluster define the score and Hessian at that centre, respectively 

sj := ∇ℓj (ϑ∗) and Hj := ∇2ℓj (ϑ∗), where ℓj (ϑ) := log p(yj | ϑ) is the cluster log-likelihood. Set δ := ϑ − ϑ∗ and d := dim ϑ, and write 

Ij := Eϑ|y

> [

e−ℓj (ϑ) ]

, ˆp(yj | y−j ) := 1 /I j ,

which by Proposition 1 is exact up to the approximation of the single expectation Ij .The following theorem evaluates Ij exactly under a Taylor truncation of ℓj , taken to first order and to second. 

Theorem 2 (Laplace approximation of the cluster CPO) . Assume that the posterior is replaced by its Gaussian summary, π(ϑ | y) = N (ϑ∗, Ω) with Ω ≻ 0. Write ˆI(k) 

> j

for Ij

with ℓj replaced by its kth-order Taylor expansion about ϑ∗, and set 

ˆp(k)(yj | y−j ) := 1 / ˆI(k) 

> j

. Then: (i) First order. ˆI(1)  

> j

is finite for every finite sj , and 

log ˆ p(1) (yj | y−j ) = ℓj (ϑ∗) − 12 s⊤ 

> j

Ωs j . (13) 

(ii) Second order. With Aj := Ω−1 + Hj , ˆI(2)  

> j

is finite if and only if 

Aj ≻ 0 ⇐⇒ λmax 

> (

−Ω1/2Hj Ω1/2)

< 1, (14) DETERMINISTIC LOCO FOR MULTILEVEL SEM 11 

and in that case 

log ˆ p(2) (yj | y−j ) = ℓj (ϑ∗) − 12 s⊤ 

> j

A−1 

> j

sj + 12 log ∣∣∣I + ΩH j

> ∣∣∣

. (15) 

Part (i) is the case Hj = 0 of (ii). 

The proof is given in Appendix B. Under the Gaussian summary, then, the fully exponential Laplace approximation of the cluster CPO is nothing more than a Taylor truncation of ℓj , and we refer to log ˆ p(1) and log ˆ p(2) as the first- and second-order Taylor elpd scores throughout. The second-order form differs from (13) through a single precision update Ω−1 7 → Aj = Ω−1 + Hj and adds a determinant term for the resulting change in normalising volume. These adjustments have a direct deletion interpretation. Removing cluster j subtracts ℓj from the full log posterior, changing its negative Hessian at ϑ∗ to the full-data precision less the observed information −Hj contributed by cluster j, so Aj is interpreted as the approximate leave-j-out precision. Accordingly, 

ϑ∗ − A−1 

> j

sj is one analytic Newton step towards the cluster-deleted mode, i.e., the tilted mode of Tierney and Kadane (1986), with Aj the tilted precision, reached in a single step because of the Gaussian approximation to the posterior. Whereas Tierney and Kadane (1986) remaximise the cluster-deleted log posterior L − ℓj and evaluate its Hessian at the new mode, here the tilted precision Aj is fixed at ϑ∗, so the second-order score is their fully exponential approximation applied to the quadratic surrogates of L

and L − ℓj .Condition (14), Aj ≻ 0, is therefore the existence condition for this local Gaussian representation, as it makes Aj a valid precision matrix and ensures convergence of the Gaussian integral defining the second-order score. Let kji be the eigenvalues of −Ω1/2Hj Ω1/2. Aj ≻ 0 holds exactly when kj := max i kji < 1 for any Hj ,since Aj ≻ 0 is equivalent to I + Ω1/2Hj Ω1/2 ≻ 0, and the determinant adjustment becomes 12 

> ∑
> i

log(1 − kji ), an additional volume penalty whenever the kji are non-negative, as they are when Hj ⪯ 0. The condition kj < 1 also governs the harmonic-mean pathology described earlier. Since Ij is exactly the posterior mean of the deleted-to-full density ratio rj (ϑ) ∝ p(yj | ϑ)−1 in (12), the existence condition just derived is also the condition for that ratio to have finite mean. Both statements in Theorem 2 are exact, so the error in log ˆ p(k) is entirely the error of the Taylor truncation of ℓj . With J → ∞ at nj and p fixed, that error is 

O(J−1) per cluster at first order (the discarded second-order expansion term) and 

O(J−2) at second. Thus, the two orders differ per cluster by 12 tr( −ΩH j ) + O(J−2), the leading term arising from the determinant adjustment and equalling minus the posterior mean of the quadratic Taylor term the first-order truncation discards, the tilt of the quadratic form entering only at O(J−2). Now elpd loco sums J of these, and provided the bounds hold uniformly in j, the summed second-order truncation error is O(J−1)DETERMINISTIC LOCO FOR MULTILEVEL SEM 12 and vanishes as clusters accumulate, whereas the summed first-order error is O(1) and does not. That non-vanishing limit is the sum of the per-cluster gaps, since ∑ 

> j

Hj is the full-data Hessian −I and Ω−1 = I + P, with P := −∇ 2 log π(ϑ∗) the curvature the prior contributes there, 

> ∑
> j
> (

log ˆ p(1) (yj | y−j ) − log ˆ p(2) (yj | y−j )

> )

= 12 tr (

ΩI)

+ O(J−1), (16) the trace being the effective number of parameters peff := tr( ΩI) familiar from the deviance information criterion (DIC; Spiegelhalter et al., 2002) and the widely applicable information criterion (WAIC; Watanabe, 2010). It is the dimension d less a prior shrinkage, in the manner of a ridge trace, since peff = d − tr( ΩP ) ≤ d with equality only under a flat prior. That shrinkage is itself O(J−1), as is the remainder in (16), so the summed gap approaches d/ 2 while differing from it at any finite J. The first-order Taylor elpd therefore overstates elpd loco by a complexity penalty 12 peff , and so comparing candidates of different dimension by the first-order score may be misleading. The same per-cluster kernels also deliver the marginal WAIC, which targets this same leave-one-cluster-out quantity (Merkle et al., 2019) and is ordinarily computed from posterior draws. Under a Gaussian density it needs none, and at first order it reproduces (13) exactly, its penalty being the ploo that the loo package estimates by PSIS (Vehtari et al., 2017). Appendix C gives the derivation. The centre ϑ∗ need not be the posterior mode. Theorem 2 requires only that ℓj

be expanded at the centre of the Gaussian summary, so that δ has mean zero under it. The stationarity condition ∇L(ϑ∗) = 0 is nowhere used, and any centre is admissible. Since Ij is an expectation under the posterior, a centre near the posterior mean is preferable to the mode whenever the posterior is skewed. INLAvaan supplies a variationally mean-corrected centre (Jamil & Rue, 2026a), and it is that point at which we therefore expand, though Ω retains the curvature at the mode. Every remaining ingredient comes from that same fit. The value ℓj (ϑ∗) is closed-form from (8), at O(p3)per cluster and independent of cluster size. The score sj follows by analytic differentiation of ℓj through the matrix calculus of Σw(ϑ), Σb(ϑ) and μ(ϑ), and the Hessian Hj , needed only for the second-order form, by finite differencing that analytic gradient. The total cost is one INLAvaan fit followed by a single post-processing pass, with no refits and no posterior sampling. 

3.3 Submodels as Linear Constraints and the Compatible-Prior Identity 

The Laplace summary N (ϑ∗, Ω) of Section 3.2 supports more than loco scoring of the encompassing model. In the spirit of projection predictive inference (e.g., Piironen et al., 2020), we bypass the prohibitive computational cost of combinatoric refitting by treating our fully parameterised encompassing model as an information-rich DETERMINISTIC LOCO FOR MULTILEVEL SEM 13 reference profile. A candidate structural restriction is then handled as a linear constraint on ϑ. Under a compatibility convention on the submodel priors, the restricted Laplace summary follows from closed-form Gaussian conditioning, and the 

loco score of the restricted model is a one-pass post-processing step. Evaluating a candidate submodel thus costs one conditioning update and one scoring pass, against one full INLAvaan fit for the encompassing model, so a candidate space of realistic size can be enumerated rather than searched. To evaluate structural alternatives efficiently, we operate within a framework where the candidate space consists of candidate submodels nested within a single, unrestricted reference model. Let this encompassing model be parameterised by the full vector ϑ. Any specific candidate structural submodel Mm is then identified with the linear constraint Cmϑ = 0, in which Cm is a selector matrix whose rows pick out the path entries zeroed under Mm. Single-path additions or removals during structure selection thus correspond to the sequential removal or addition of a single row in Cm,respectively. The representation presumes that zeroing the candidate paths leaves the remaining model identified. Restrictions that change identification status (e.g., deleting a loading that leaves a latent factor underdetermined) lie outside its scope and require a separate INLAvaan fit. The constraint acts on ϑ alone, and so composes with any deletion index, including subject-level leave-out. We adopt throughout the compatible-prior convention of Consonni and Veronese (2008), taking each submodel’s prior to be the encompassing prior conditioned on its own defining constraint, 

πm(ϑ) = π(ϑ | Cmϑ = 0) . (17) This is the same condition under which the Savage–Dickey density ratio is valid (Dickey, 1971; Verdinelli & Wasserman, 1995) and that underlies the encompassing-prior approach to constrained model selection (Klugkist & Hoijtink, 2007). Compatibility is automatic in the cases that arise here. INLAvaan places independent priors on the parameters by default, so when π(ϑ) = ∏ 

> k

πk(ϑk) and Cm is a selector matrix, conditioning on Cmϑ = 0 leaves the priors on the retained parameters untouched and (17) holds with πm their encompassing marginal prior. The argument requires only that the constrained coordinates be prior-independent of the retained ones, so a joint prior on a block of parameters, such as one on a latent correlation matrix (Freni-Sterrantino et al., 2025, 2026), is compatible as long as no coordinate inside that block is zeroed. Since structure selection adds and removes individual paths, only selector constraints arise and no separate submodel prior need be elicited. 

Lemma 3. Partition ϑ = ( ϑa, ϑ−a) and consider the single-path restriction ϑa = 0 . Let 

Mm denote the submodel with ϑa fixed at zero, carrying prior πm(ϑ−a). If the DETERMINISTIC LOCO FOR MULTILEVEL SEM 14 

compatible-prior convention πm(ϑ−a) = π(ϑ−a | ϑa = 0) holds, then for every dataset y

πm(ϑ−a | y) = π(ϑ−a | y, ϑ a = 0) . (18) 

The general linear restriction Cmϑ = 0 reduces to this case by an invertible linear reparameterisation mapping Cmϑ onto a subvector of the parameters. 

The identity follows from Bayes’ theorem once the joint prior is factored on the constraint slice, the mass at ϑa = 0 absorbing into the normalising constant. Appendix D gives the argument in full. The posterior equality (18) is used implicitly in those Savage–Dickey derivations to compute Bayes factors, whereas here it does something different, licensing the refit-free construction. Because the loco score of Section 3.2 is a functional of the posterior alone, replacing the submodel refit by conditioning the encompassing posterior introduces no prior-level approximation. Of course, while prior-level compatibility is exact, our operational machinery applies the constraint Cmϑ = 0 directly to the Gaussian Laplace summary N (ϑ∗, Ω) of the reference model. This corresponds to a second-order Taylor approximation of the true constrained mode and curvature. The size of that discrepancy depends on how far the constraint sits from the encompassing centre. Section 5.2 quantifies the dependence and shows the error to be small near the centre, one-sided beyond it, and diagnosable in advance from the Laplace summary itself. 

3.4 Scoring a Restricted Submodel 

Under the Laplace approximation π(ϑ | y) ≈ N (ϑ∗, Ω), conditioning on 

Cmϑ = 0 is Gaussian with moments 

ϑ∗ 

> m

= ϑ∗ − ΩC ⊤

> m
> (

CmΩC ⊤

> m
> )−1

Cm ϑ∗, (19) 

Ωm = Ω − ΩC ⊤

> m
> (

CmΩC ⊤

> m
> )−1

CmΩ. (20) The matrix inverted has dimension equal to the number of constraints, typically far smaller than d. Penny and Ridgway (2013) use the same operation to evaluate Savage–Dickey Bayes factors in nested generalised linear models. For a single-path restriction ϑa = 0, setting Cm = e⊤ 

> a

in (19)–(20) gives the rank-one update 

ϑ∗ 

> m

= ϑ∗ − ϑ∗

> a

Ωaa 

Ω·a, Ωm = Ω − 1Ωaa 

Ω·a(Ω·a)⊤, (21) where Ω·a is the ath column of Ω and Ω aa its ath diagonal entry. The cost is O(d2) per restriction, with no matrix inversion required once Ω is in hand. This is the cheapest case, met whenever two candidate structures differ by a single dropped path. Scoring a submodel by (15) requires the cluster-level score and Hessian at the DETERMINISTIC LOCO FOR MULTILEVEL SEM 15 restricted centre ϑ∗

> m

. Since Ωm is singular on the constraint span, all operations are performed in the dm = d − rank( Cm) unconstrained subspace. Let ϑ∗

> m, free

, Ωm, free ,

sj,m, free , and Hj,m, free denote the corresponding quantities. The score and Hessian are analytic in the model-implied covariances and are obtained once per candidate; the explicit trace formulae are collected in Appendix E. The submodel score is then log ˆ pm(yj | y−j ) ≈ ℓj,m (ϑ∗

> m, free

) − 12 s⊤

> j,m, free

A−1

> j,m

sj,m, free + 12 log ∣∣∣ I + Ωm, free Hj,m, free 

> ∣∣∣

, (22) with Aj,m = Ω−1 

> m, free

+ Hj,m, free , and elpd loco (m) = ∑ 

> j

log ˆ pm(yj | y−j ). 

4 Leave-One-Subject-Out From the Same Model Fit 

Proposition 1 and Theorem 2 use the held-out unit only through (a) a factorisation of the full-data likelihood into the density to be scored and the density of everything else, and (b) the availability of a per-unit log-likelihood with score and Hessian at the mode. Neither property is special to clusters. The deletion index is accordingly a modelling choice, and it is that choice, together with what the held-out density is conditioned on, that fixes the prediction question. Where the held-out block is independent of the rest given ϑ, as a cluster is by (7) and a subject is under independent sampling, the required factorisation is the marginal one already used. Where it is not (e.g., subject inside an observed cluster), the chain rule supplies it, 

p(y | ϑ) = p(ysj | yj, −s, ϑ) p(y−(sj ) | ϑ), and the construction applies with this conditional density instead of the marginal one. Two subject-level scores follow, the conditional target within a cluster and the marginal target for independent data. 

4.1 The Conditional Target: A Subject in an Observed Cluster 

The conditional loso target (9) scores a held-out subject against the clustermates that remain. Its integrand, p(ysj | yj, −s, ϑ), is available in closed form via standard Gaussian conditioning as ysj | yj, −s, ϑ ∼ N p(μsj , Σsj ), with 

μsj (ϑ) = μ + mj Σb(Σw + mj Σb)−1(¯ yj, −s − μ),

Σsj (ϑ) = Σw + Σb − mj Σb(Σw + mj Σb)−1Σb,

where mj = nj − 1 and ¯ yj, −s is the leave-one-out cluster mean. The harmonic-mean identity of Proposition 1 holds with this conditional density replacing p(yj | ϑ), and the Taylor scores in Equations (13)–(15) apply verbatim with ℓsj (ϑ) := log p(ysj | yj, −s, ϑ)and its derivatives replacing ℓj , sj , and Hj . The total conditional score elpd loso is thus computable from the same encompassing model, at a cost proportional to N = ∑ 

> j

nj .Note that while the gap identity (16) holds for any deletion unit, its limit d requires disjoint ones (a requirement met by clusters, but not conditionally scored subjects). Where the goal is interpolation within an observed cluster, i.e., predicting a further student in a school already in the sample, the above conditional target is the DETERMINISTIC LOCO FOR MULTILEVEL SEM 16 right one. For clustered data that is seldom the goal, however. Merkle et al. (2019) make the choice turn on whether a model is meant to predict for the clusters in the data or for new ones, and recommend the marginal, cluster-level criterion whenever it is the latter. The reason is the one given in Section 2, that the held-out subject’s clustermates still identify the cluster effect, so the conditional criterion rewards cluster-specific idiosyncrasy that does not transfer. Regardless of which target a study needs, INLAvaan produces both from the same fit. The type argument of loo() selects between them, "loco" for the cluster-level score and "loso" for the conditional subject-level one. On two-level fits the cluster-level target is the default. 

4.2 The Marginal Target: Independent Data 

The marginal subject-level target is the appropriate one for independent data, where a held-out subject has no clustermates to borrow from. For independent (single-level) data the held-out unit is a single subject s, the observations are independent given ϑ, and the cluster reduced form (6) collapses to the single-level Gaussian (3), ℓs(ϑ) = log Np

> (

ys; μ(ϑ), Σ(ϑ))

, with no compound-symmetric covariance to decompose. Proposition 1 reduces to the familiar observation-level conditional predictive ordinate p(ys | y−s) = (

Eϑ|y[ 1 /p (ys | ϑ) ] )−1

, and the Taylor scores of Theorem 2 apply verbatim with ℓj , sj , Hj replaced by their subject-level counterparts 

ℓs, ss := ∇ℓs(ϑ∗), and Hs := ∇2ℓs(ϑ∗), [first order] log ˆ p(1) (ys | y−s) = ℓs(ϑ∗) − 12 s⊤ 

> s

Ωs s

[second order] log ˆ p(2) (ys | y−s) = ℓs(ϑ∗) − 12 s⊤ 

> s

A−1 

> s

ss + 12 log ∣∣∣I + ΩH s

> ∣∣∣

,

with As = Ω−1 + Hs, the second-order form again requiring As ≻ 0. The conditioning of Section 3.3 applies unchanged as well, so a single encompassing fit scores every candidate submodel at the subject level. The same two requirements are met by other designs. Multigroup data only re-index ϑ, leaving the deletion index and the factorisation over subjects untouched, and jointly modelled exogenous covariates enter the per-unit density as further coordinates (Appendix A). In each case it is enough to identify the density of the unit one means to predict; the score and Hessian machinery then applies unchanged. 

5 Simulation Studies 

We validate the closed-form Taylor elpd scores against brute-force refits to address two questions. First, does the Taylor score recover the gold-standard predictive density, and where they differ, does the discrepancy originate in the truncation itself or in the posterior approximation on which it is built? Second, when a submodel is scored by conditioning the encompassing Laplace summary rather than by its own fit, how far may the constraint sit from the summary centre before the conditioned score degrades? DETERMINISTIC LOCO FOR MULTILEVEL SEM 17 

5.1 Accuracy, Error Decomposition, and Computational Cost 

The study uses p = 6 indicators with intercepts ν = (0 .5, 1.0, 1.5, 0.8, 1.2, 0.6), within-level loadings λw = (1 .0, 0.8, 1.2, 0.9, 1.1, 0.7) on a single factor of variance 

ψw = 1 with residual variances θw = (0 .5, 0.6, 0.4, 0.7, 0.5, 0.6), and a single between-level factor with loadings λb = (1 .0, 1.1, 0.9, 1.2, 0.8, 1.0), variance ψb = 0 .5 and residual variances θb = (0 .20 , 0.15 , 0.25 , 0.10 , 0.20 , 0.15). Between-level loadings differ from within-level ones, so the generating model is deliberately not invariant across levels. These values imply standardised within-level loadings of about 0 .67–0 .89 and indicator intraclass correlations (ICCs) of about 0 .23–0 .38, a well-identified measurement model with a moderately high ICC. The error magnitudes reported below should be read against that design, since what a cluster carries about the between-level parameters scales with its ICC. The theorems of Section 3.2 are stated for a generic deletion unit, so the marginal and conditional targets differ only in the integrand substituted for ℓu. We therefore study two designs, one for each target. Marker-variable scaling is used throughout, and under exact specification, the reported errors purely reflect approximation. For the marginal target ( loco ), we simulate two-level data with distinct within- and between-level one-factor structures, varying the number of clusters 

J ∈ { 30 , 100 , 200 } and cluster sizes nj ∈ { 5, 20 }, so that d = 30. Crossing nj with J

aims to isolate the effect of the cluster count from the total sample size. For the subject-level target ( loso ), we draw unclustered observations ys ∼ N p(ν, Σw) from the within-level measurement model and fit the matching single-level confirmatory factor analysis at N ∈ { 100 , 500 , 1000 }, so that d = 18. Each cell is replicated 100 times. Within a replicate, a single INLAvaan fit supplies the Taylor scores for every unit. We then execute brute-force 

INLAvaan -Laplace and blavaan -MCMC refits on K randomly chosen held-out units (K = 30 clusters for loco , K = 100 subjects for loso , with one MCMC chain of 800 draws after 400 burn-in), aligning and pooling the per-unit quantities across replicates. The two baselines isolate the two approximations at work. The short chains are a concession to scale, as the design calls for 48,000 blavaan refits in total. They still resolve the benchmark finely enough, since each tabulated entry pools 3 ,000 ( loco ) or 10 ,000 ( loso ) of them, and the Gaussian error falls to 0 .000–0 .001 at the larger designs, which appreciable Monte Carlo noise in the benchmark would have kept above zero. All 

loso replicates completed and 96% of loco replicates did, with a further 0 .02% of scored units discarded, in both cases predominantly from divergent MCMC refits. Errors are reported in elpd units. Figure 1 plots the first- and second-order Taylor elpd scores against the MCMC elpd across identical held-out units. The second-order estimate tracks the identity line closely across both arms and all sample sizes. As reported in Table 1, per-cell DETERMINISTIC LOCO FOR MULTILEVEL SEM 18 discrepancies vanish almost entirely once J ≥ 100 ( loco ) or N ≥ 500 ( loso ). The only visible departure occurs at the smallest designs ( J = 30 , n j = 5 for loco ; N = 100 for 

loso ), and strictly in the first-order series, where the linear term alone fails to capture the curvature of the held-out predictive density. The second-order correction reliably eliminates this bias. 1st order 2nd order -1350 -1300 -1250 -1350 -1300 -1250 -1350 -1300 -1250 J = 30 J = 100 J = 200 nj  = 5 nj  = 20 -5100 -5000 -4900 -4800 -4700 -5100 -5000 -4900 -4800 -4700 -5100 -5000 -4900 -4800 -4700 -1350 -1325 -1300 -1275 -1250 -1225 -5100 -5000 -4900 -4800 -4700 

LOCO                 

> N=100 N=500 N=1000 -850 -800 -750 -850 -800 -750 -850 -800 -750 -860 -840 -820 -800 -780 -760 -740 MCMC leave-one-out elpd Re fi t-free Taylor elpd

LOSO 

Figure 1 

First-order (yellow) and second-order (blue) Taylor elpd scores, each from a single fit, against brute-force MCMC-refit elpd over the same held-out units. Each point is one simulated dataset, summing the K refitted units ( K = 30 clusters, K = 100 subjects). The axes are therefore common within each row, since the magnitude of such a sum is set by nj rather than by J. The dashed line indicates exact agreement. 

Figure 1 also appears to show the first-order bias shrinking as J (loco ) or N

(loso ) grows, but Equation (16) dictates a nonzero limit. The explanation is that only 

K units are refitted, and K stays fixed as the design grows. Each panel therefore shows 

K/J (or K/N ) of a total that does not itself shrink, which is why the bias appears to fade. Figure 2 sums over all units instead, and there the first-order error does not vanish but settles on the limit of (16). The plotted total error carries the Gaussian error alongside the truncation error, but that error, the prior shrinkage 12 tr( ΩP ), and the remainder of (16) are all O(J−1), leaving d/ 2 as the asymptote: 24 .8 falls to 15 .8elpd against d/ 2 = 15 for loco and 10 .8 to 9 .2 against d/ 2 = 9 for loso , while the second-order errors appear to diminish. DETERMINISTIC LOCO FOR MULTILEVEL SEM 19 LOCO LOSO 30 100 200 100 200 500 1000 0369010 20 Clusters J / subjects N (log scale) Total elpd error 1st order 2nd order nj = 5 nj = 20 

Figure 2 

Total elpd error over all units against the number of clusters J (loco ) or subjects N

(loso ), estimated as J (or N ) times the mean per-unit error and shown with ±2

sampling standard errors. The dashed line is the asymptotic d/ 2 limit of (16) , which the summed gap approaches to O(J−1).

To isolate the source of any residual discrepancy, we introduce a third quantity: a brute-force INLAvaan refit on the reduced data y−u, with u = j for clusters and u = s

for subjects. This allows us to partition the total error into two distinct components: ̂

elpd u − elpd MCMC   

> u
> ︸︷︷ ︸
> total error

=̂ elpd u − elpd INLA   

> u
> ︸︷︷ ︸
> truncation error

+ elpd INLA  

> u

− elpd MCMC   

> u
> ︸︷︷ ︸
> Gaussian error

.

The truncation error compares the Taylor score against an actual refit under the identical Laplace posterior, isolating the behaviour of the expansion itself. The Gaussian error compares that INLAvaan refit against MCMC, isolating the limits of the Gaussian-posterior approximation, which, by Bernstein–von Mises arguments, should vanish as information accrues. Table 1 details this partition, and two conclusions follow. First, at second order the Taylor truncation contributes negligibly to the overall discrepancy. Its second-order error is small, largest at 0 .061 elpd ( J = 30 , n j = 5), falling to 0 .002 or below for 

loco at J ≥ 200 and to nearly zero for loso at N ≥ 500, and remains well below the first-order error (which reaches 0 .60) throughout, confirming that the curvature correction sharpens the estimate without over-correcting. Because this component compares the Taylor score against a true refit, the residual error against MCMC is not attributable to our expansion machinery. Summed over units, the first-order truncation error runs from 18 .0 down to 15 .6 elpd across the six loco cells and from 9 .9 to 9 .2across the three loso cells, against second-order sums that stay below 1 .9 throughout. Measured directly using 12 

> ∑
> j

tr( −ΩH j ), 12 peff is 8 .6–9 .0 against d/ 2 = 9 for loso and DETERMINISTIC LOCO FOR MULTILEVEL SEM 20 

Table 1 

Per-unit mean error decomposition by design cell (elpd units; 100 replicates per cell). 

1st & 2nd order is the Taylor score against an INLAvaan refit; Gaussian is that refit against a full MCMC refit; Total is their sum. LOO time is the wall-clock cost of the Taylor pass over all units. Monte Carlo standard errors do not exceed 0.002 in the first-and second-order columns and 0.012 in the Gaussian and Total columns. 

Mean per-unit error (elpd) 

N/J nj 1st order 2nd order Gaussian Total LOO time (s) Leave-one-subject-out (LOSO) 100 – 0.099 0.005 0.009 0.014 0.140 500 – 0.019 0.000 0.000 0.000 0.185 1000 – 0.009 0.000 0.000 0.000 0.249 Leave-one-cluster-out (LOCO) 30 5 0.598 0.061 0.229 0.290 0.325 30 20 0.596 0.037 0.093 0.130 0.336 100 5 0.162 0.009 0.025 0.034 0.474 100 20 0.160 0.004 0.007 0.012 0.495 200 5 0.078 0.002 0.008 0.010 0.743 200 20 0.078 0.001 0.001 0.002 0.659 13 .4–14 .9 against d/ 2 = 15 for loco , the shortfall in each case being the prior shrinkage  

> 12

tr( ΩP ) of (16). The gap itself behaves as (16) dictates: it exceeds 12 peff in every cell by the O(J−1) remainder, and this remainder falls from 2 .7 elpd at J = 30 to 0 .5 at 

J = 200 for loco , and from 0 .9 to 0 .1 for loso .Second, the inherited Gaussian posterior approximation error vanishes predictably. For loco the decay is governed by the number of clusters J rather than the total sample size: the error runs 0 .09–0 .23 elpd per cluster at J = 30 and falls to 0.025 or below once J ≥ 100. This aligns with the large-sample behaviour expected of 

loco , where each cluster contributes a single between-level observation. No fit-level quantity we examined predicted the error better than the design itself. The magnitudes are specific to this design, but the pattern is not: the Gaussian error is largest where J

is smallest, which is also where a deleted refit is cheapest to run as a check. The closed-form procedure is orders of magnitude faster than brute-force cross-validation. At the largest design point ( J = 200, nj = 20), one INLAvaan fit plus the closed-form pass over all 200 clusters takes 2 .1 seconds, against 2 .0 seconds per deleted-cluster INLAvaan refit (406 seconds in total, 193 ×) and 24 .1 seconds per 

blavaan -MCMC refit (4 ,814 seconds, 2 ,286 ×). That ratio widens with J, and the reduction in cost entails no loss of accuracy, since the second-order Taylor score agrees with the MCMC benchmark to within 0 .002 elpd per cluster at this design point. DETERMINISTIC LOCO FOR MULTILEVEL SEM 21 

5.2 Accuracy of Conditioned Submodel Scores 

The compatible-prior identity of Section 3.3 is exact, so scoring a submodel by conditioning carries only one further approximation. The constraint is applied to the Gaussian Laplace summary rather than to the posterior itself, and the quality of that local step must depend on how far the constraint sits from the centre. To measure that dependence directly, consider that for a restriction Cmϑ = 0 of rank r, the natural measure of that distance is the Mahalanobis distance from the centre to the constraint surface, 

D2 

> m

:= ( Cmϑ∗)⊤(

CmΩC ⊤

> m
> )−1

(Cmϑ∗), (23) read from the Laplace summary N (ϑ∗, Ω) that (19)–(20) condition. Two readings make 

Dm interpretable. It is the size of the mean shift that conditioning induces, since (19) gives ( ϑ∗ 

> m

− ϑ∗)⊤Ω−1(ϑ∗ 

> m

− ϑ∗) = D2

> m

. Under the encompassing Gaussian D2 

> m

follows a 

χ2 

> r

law, so Pr( χ2 

> r

> D 2

> m

) is the posterior mass lying further from the centre than the constraint itself. For a single dropped path, Dm reduces to |ϑ∗

> a

|/√Ωaa , the number of posterior standard deviations between the fitted coefficient and the zero it is constrained to. For this study, two-level data are generated with p = 12 indicators loading on four three-indicator factors at each level: three latent predictors x1, x 2, x 3 and an outcome y. Standardised within-level loadings cycle through (0 .80 , 0.70 , 0.90) and between-level loadings through (0 .75 , 0.85 , 0.70), indicator intercepts through (0 .5, 1.0, 1.5), and every indicator has an ICC of 0 .20. At the between level the outcome is regressed on all three predictors, yb = β1x1b + β2x2b + β3x3b + ζb, with (β1, β 2, β 3) = (0 .45 , 0.12 , 0), i.e., one strong path, one weak, and one exactly null. The three latent predictors are mutually correlated at ρ, and we take ρ ∈ { 0.35 , 0.80 } to vary how strongly their coefficient estimates are entangled. The candidate set is all 23 = 8 subsets of these β-paths, so restrictions of every rank r ∈ { 1, 2, 3} occur. We use 

J ∈ { 30 , 100 } clusters of size nj = 20, with 150 replicates per cell. For each dataset the encompassing model is fitted once and every candidate is scored twice, first by conditioning the encompassing Laplace summary on Cmϑ = 0, and then by a separate INLAvaan fit of the candidate itself. The two routes share the posterior machinery, so their difference isolates the conditioning step alone. Not every replicate yielded a complete set of candidate scores, and the retained counts are given in Table 2 and the attrition described alongside it. Figure 3 plots the conditioned minus separately fitted loco elpd against Dm, over the 3 ,661 restricted candidate scorings. Sampling variability spreads Dm from zero to 6 .7 posterior standard deviations. The pattern is the same in all four design cells. Near the centre the error is small and symmetric about zero, and beyond two standard deviations it turns systematically negative and grows. DETERMINISTIC LOCO FOR MULTILEVEL SEM 22 32 2121 132 21 21 132 21211 32212113221211 322121 1 3221211322121132212113221211 3221 211 3221 211 32 2121 1 322121 1 322121 1 322 121 1 322121132212113221 211 32 21 21 13221 21 13221211 32 21 21 1 32 21 21 12121132212113221211 3221 211 3221 211 32 21211 32 21 21 1 32 2121 1 3221211 32212113221 2113221211 3221 21132212113221 211 32 2 121132 2121 132212113221 2113221211 32 2121132212113221 211322211322121132212113221211 32 21 21 132 2121132 2121132212113221211 32 21 21 132 2121 132212113221211 32 21211 32 21 21 1322121 1 322121 132212113221 211 3221 211322121 132 21 21 13221 21132212113221 2113221 211 3221 211 32 21 21 13221211 32 2 12113221211322121 13221211 32 21211 322121 132 21 21 13221 1 32 2121 132 21211212113221211322121 13221 2113221 211 32 2121 1 32 2121 13221 211 322121132 21 211 32 21 21 132 21211 3221 211 32212113221211322 1211 32 2121 1 32 21 211 3221 21 1 322121 1322121 1 3221 211 32 2121 132212113221 211322121 132212113221 21 1 3221 2 1 1121 13221 211 322121 132212113212113221 2113221211 3221 2113221211 32212113221 21132 2121 132 21211 32 2121 1 3 2 21 21 1 3221 2113221211 32 2 1211 322121132212113221211322121132 2 1211322121 13221211 322121 1 3221211 322121 132212113221211322 121 13221211 322121 132 2121 1 32 2121 1 3221 211322121 1 32212113221211322121132 2121 1 32 21 21 1 32 2121 1 322121132 2 1211 322121 1 32 21211 32 21211322121 1 322121 1 322121 13221 211322121 1322121 1 322121 1 322121 132 2121 1 3221 211 322121 1322121 1 3221211322121 132 21211 322121 1 3221 211 322121 1322121132 2121 13221211 3221211 3221 2113221211 32 2 121 13221211322121 13221211 3221211 322121132212113221211 322121 1 32212113221211 32212113221211 322 1211 3221211 32 2121 13221211 3221 21132 2 1211 32 2121 1 322121 132 21 211 322121 13221 2113221211 3221211322121132 2121 132 21211322121 1 322 12113221211 32 2121 1322121 132 2121 1 32 21 21 1 3221 211 32 21 21 1 322121 1 3221 211322121132 212113221211 32212113221 2 11322121 132212113221 21132 21211322121132 2121 1 3221211322121132212113221 2113221211 322121132 2121 1 3221 211322121 1322121132 2 121 1 32212113221211 3221 211 3221211 322121 1 322121 132212113221211 32 21 21 132 21211 3221211 32212113221211 322121132 21211 322121 1 32212113221 21132 2121 13221 211 32 21211 322121 1 3221 2113221211 322121 132212113221211 3221 211 32 2121 1322121 1322121132 2121 13221211 322121132 21 21132 21211322121 13221211 3221 21 13221 21 1 322121 13221211 32 21211322121132212113221 21132 21211 32 2121132212113221 2113221211322121 1 322121132 212113221 211 322121 1 321 2113221 21132 21 21132212113221211322121132212113221 211322121 1 3221 21132212113221 21 132 21 211 32 2 1 21132212113221 211322121132212113221211322121132212113221211 3 22121132 2121132 21 21 13221211 322121132 2121 1 3221 21132212113221211 32 21211 32 2121 1 3221 2 11 32 21 21 13221 211 32212112121 13221211 32 2121132212113221211 322121 132 2121322121 1322 121 1322121322121 13221211 3221 21 1 3221 21132 21 2113221 211322121 1322121132212113221 2113221 21132 212113221211 3221 2113221211322121132212113221211322121132 21 2 113221 211322121 132 21211322121 13221 211 32 21 21 1 32 21211 3221 211 322 121 1322121132 21 211 3221 21 132 21 21 1 32 21 21 1322121 1 32 21 21 1 32 212113221211322121 132 2121 1322121 132 21211 32 2121132212113221 211 3221 2113221211322121 1 32 21 21 132212113221211 322121132 2121 1 32 21211322121132 212113221 2113221211322121 1 32212 113221 2113221211 32 21 21 13221 21 132212113221211322121 132 21 21 1 32 21 21 1 3221 2113221 21 1 32 21 21 1 322121 1322121132212113221211 3221211 32 2121 1 3221 21 132212113221211 32 21 21 13221 211 32 21 21 1322121 1 3221 21122121 13221 21 1 322121 13221 211322121132 2121 1 322121 1 32 2121132212113221 2113221 2113221 211322121 132212113221 211 32 21 21 13221 21 1 32 212113221 21 132212113221 21 1 32 21 21 13221211 32 21 21 13221211 322 1 211 3221 21 132212113221 21 13221211322 121 1322121 1 3221211322121 13221 21 1 3221211322121 13221211 32 2121132 2121132212113221211 322121132 21 21 132212113221 21132212113221 21 1322121132212113221 2113221 211 3221211 3221 211 3221211 3221 211322121132212113221211 32 2121 1 32212 1 13221211 3221211322121132 21 2 113221211 3221211 3221 2113221 211322121 132 21 211 322121 1 3221 2 1132 21 21 13221211 3221 211322 121 1 322121 1 3221211 32 21 21 1322121 13221 21132 21 21 13221211J = 100, ρ = 0.35 J = 100, ρ = 0.8 J = 30, ρ = 0.35 J = 30, ρ = 0.8 0 2 4 6 0 2 4 6-15 -10 -5 0-15 -10 -5 0Constraint distance from the mode, Ds Conditioned submodel score error 

Figure 3 

Error of the conditioned submodel score (conditioned minus separately fitted elpd) against the constraint’s Mahalanobis distance Dm from the encompassing summary centre, by design cell. Each point is plotted as the number of paths that candidate restricts. The solid line is the median error in half-standard-deviation bins and the band spans the 10th–90th percentile. 

Three features stand out. First, the one-sidedness sharpens with distance. Pooling the four design cells, the error is negative in 38% of scorings within one standard deviation of the centre, in 59% between one and two, 90% between two and three, 99% between three and four, and in all 225 scorings beyond four. The median error over the same bands runs +0 .04, −0.07, −0.66, −1.26 and −3.58 elpd. A submodel conditioned far from the centre is therefore under-scored rather than over-scored, which rules out the spurious promotion of a heavily restricted candidate, though not its false exclusion. Second, the error is governed by Dm and not by the rank of the restriction. The median absolute error in the Dm ≤ 1 band is 0 .13, 0 .15 and 0 .33 elpd for r = 1 , 2, 3, and in the 

Dm > 4 band 4 .73, 3 .35 and 3 .57, so the distance matters far more than the number of paths being constrained. Third, Dm is read from the encompassing Laplace summary alone, so the candidates at risk are identifiable before any scoring is done. 

Dm is measured in units of Ω, so it places the candidates of a single encompassing fit on a common scale. That scale does not carry across fits, however. Correlation among the latent predictors inflates the posterior standard deviations of the individual coefficients, so at ρ = 0 .80 the constraints sit closer to the centre—a median 

Dm of 1 .55 against 2 .37 at ρ = 0 .35 when J = 100—and yet the error is larger in every distance band, reaching a median absolute 3 .05 elpd against 1 .64. Conditioning DETERMINISTIC LOCO FOR MULTILEVEL SEM 23 

Table 2 

Concordance between conditioned submodel scores and independent INLAvaan fits. Designs are ordered from hardest to easiest (Monte Carlo SEs in parentheses). Reps is the number of replicates, of 150 , with a complete set of candidate scores; the attrition is described in the text. Whole ranking is Kendall’s τ between candidate orderings; best model is the proportion of top-rank matches. Elpd lost is the predictive regret from conditioned selection; in SE units expresses it relative to the paired standard error, so a value below 1 lies within estimation noise. 

Agrees with refitting Size of the disagreement 

J ρ reps whole ranking best model elpd lost in SE units 30 0.80 98 0.57 (0 .03) 0.53 (0 .05) 0.379 (0 .069) 1.17 (0 .10) 30 0.35 125 0.72 (0 .02) 0.71 (0 .04) 0.139 (0 .029) 0.89 (0 .14) 100 0.80 150 0.85 (0 .01) 0.87 (0 .03) 0.041 (0 .019) 0.52 (0 .03) 100 0.35 150 0.95 (0 .01) 0.95 (0 .02) 0.004 (0 .002) 0.27 (0 .02) displaces the summary along the flat ridge that collinearity creates, and a quadratic (Laplace) approximation is least accurate along exactly such directions. These errors matter only insofar as they change what a comparison concludes, so the relevant question is whether the conditioned scores lead to the same decision as refitting. At J = 100 the conditioned scores reproduce the ranking that separate fits produce almost exactly (Table 2), with a Kendall correlation of 0 .95 at ρ = 0 .35 and 0.85 at ρ = 0 .80, and they select the same leading candidate in 95% and 87% of replicates. Where the leaders differ, the disagreement is between near-ties. The 

predictive regret from selecting the highest-scoring candidate, the elpd forfeited relative to the best under separate fits, is 0 .004 and 0 .041. A ∆elpd between two candidates carries a paired standard error, computed from the per-cluster differences in log-CPO contributions in the usual normal approximation (Vehtari et al., 2017). The conditioning error amounts to 0 .27 and 0 .52 of that standard error at J = 100, so it is well inside the noise the comparison already carries. At J = 30 every one of these degrades, and the conditioning error becomes comparable to the paired standard error itself. The real application of Section 7 shows the same behaviour, its near-centre candidates being the least under-scored of the 16. Two limitations should be read alongside these figures. At J = 30 a complete set of candidate scores was obtained in 125 of 150 replicates at ρ = 0 .35 and 98 of 150 at 

ρ = 0 .80. The remainder lost at least one cluster to a non-positive-definite Aj , and did so in the separately fitted references about as often as in the conditioned scores. The retained replicates are thus the better-behaved ones, and the J = 30 summaries should be read as mildly optimistic. The reference itself is also least reliable there: in five of the J = 30 candidate cells a separate fit failed to converge, affecting the conditioned and separately fitted routes identically. DETERMINISTIC LOCO FOR MULTILEVEL SEM 24 

6 Cluster-Level Model Choice in PISA School-Safety Data 

Consider the PISA 2022 student questionnaire (OECD, 2024), restricted to Saudi Arabia, with N = 6 ,125 students in J = 186 schools (sizes 10–42, median 36). Two latent constructs describe the school-safety climate, each measured on both levels: (a) perceived safety (four items—feeling safe to and from school, in classrooms, and elsewhere) and (b) bullying victimisation (the six-item PISA bullying scale). An exploratory fit with the level-2 loadings freely estimated terminated on the boundary of the parameter space, with a near-singular between-school covariance matrix. The ten indicators carry ICCs of only 0 .005–0 .038, so there is enough between-school information to support common loadings but not a freely loaded level-2 measurement model. We therefore constrain the loadings of each construct equal across the two levels. Two student-level covariates, a student’s sense of belonging and socio-economic status (SES), are modelled jointly with the responses, so each splits into a within- and a between-school part and freely covaries with the factors. What we set out to learn is whether their between-school part predicts that school’s safety climate. We answer this by comparing three nested models that share the same variables and within-school structure and differ only in which between-school paths onto perceived safety are freed. The baseline M1 is a pure random intercept, leaving school safety orthogonal to the predictors. M2 frees the school-level belonging-climate path, regressing perceived safety on school-mean belonging, and M3 adds a contextual SES path on top. Each consecutive loco difference then measures how much a school’s belonging climate, or its socio-economic composition, sharpens prediction of safety in a school never seen during fitting. Models are compared by the difference of their summed loco scores, ∆elpd, together with the paired standard error of Section 5.2, here computed from the per-school differences in log-CPO contributions. The Taylor elpd scores in Table 3 come from three single fits, each taking 43 seconds inclusive of the loco pass over all 186 schools. The criterion keeps the belonging path, as freeing the latent regression of belonging to perceived safety ( M2)gives the highest leave-one-school-out elpd, 17 .4 above the random-intercept baseline. A school where students feel they belong is, predictably, a school where students feel safe ,and that link improves out-of-cluster prediction. Adding a contextual SES path on top (M3) does not help, and the elpd falls by 0 .7 well inside its paired standard error, so the two models are not separated on this criterion. School-mean SES carries little school-level signal here (its between-school correlation with safety is only ≈ 0.14), so there is little for the marginal criterion to reward. These loco scores can be checked against the route an applied user would otherwise take. For a two-level model blavaan ’s Stan target returns a log-likelihood with one entry per cluster, the level-2 effects already integrated out (Merkle et al., 2021), so Pareto-smoothed importance sampling applied to that matrix estimates the DETERMINISTIC LOCO FOR MULTILEVEL SEM 25 

Table 3 

loco model comparison on the PISA 2022 school-safety data, Saudi Arabia ( J = 186 

schools, N = 6 ,125 students), by second-order Taylor elpd scores and by MCMC +PSIS. Higher elpd means better prediction of a held-out school. ∆ elpd is the difference from the random-intercept baseline M1, with paired cluster-level standard error from the per-school differences. The best value in each column is set in bold. 

School-level elpd loco ∆ elpd (SE) predictors of safety d Taylor MCMC+PSIS Taylor MCMC+PSIS 

M1 none 58 −61214 .6 −61217 .8 − −M2 + belonging 59 −61197 .2 −61201 .0 17 .4 (6.2) 16 .8 (6.8) 

M3 + belonging, SES 60 −61197 .9 −61198 .0 16 .7 (6.2) 19 .7 (6.6) same elpd loco from a single posterior sample. The cross-level equality constraint is not expressible through blavaan ’s modelling interface, so we impose it in the Stan program it generates, aliasing the free level-2 loading vector to its level-1 counterpart. Every other prior is left at defaults identical to INLAvaan ’s, so that what is compared is two approximations to one estimand rather than two models. Fitting all three models with four chains of 2,000 warm-up and 2,000 sampling iterations, each completing roughly under ten minutes, gives the MCMC+PSIS columns of Table 3. Every sampler converged cleanly, no divergent transitions were reported, and all ̂ R ≤ 1.003. The two routes agree to within 3 .8 elpd throughout, and the paired standard errors are comparable, so the Taylor scores are validated against the established route. The two routes do part company on the SES path, where the Taylor score puts 

M2 ahead of M3 by 0 .7 elpd and the MCMC column leans the other way by 3 .0, and neither margin clears twice its paired SE. In this comparison, however, MCMC+PSIS proves inadequate, as deleting an entire school strains the importance weights regardless of how well the sampler has converged. Figure 4 illustrates this instability. The two methods agree wherever the Pareto index ˆkj —the tail diagnostic attached to each smoothed weight (Vehtari et al., 2024)—is small. Conversely, the largest discrepancies occur among the 7 out of 558 school–model pairs that PSIS flags as unreliable, with the two most extreme cases appearing in the M2 score where ˆkj > 1. The loo_compare() 

function (Sivula et al., 2025) from the R loo package warns as much, and flags the difference as too small for its standard error to be trusted. By contrast, the validity check (14) for the Taylor scores passed (the largest eigenvalue reaches 0.13, against a validity boundary of 1), handing us a clear basis for decision-making. 

7 Predictive Structure Selection in the MIDUS Sibling Sample 

We use the sibling sample of the MIDUS study (Radler, 2014) to ask whether the long-run prospective associations of Extraversion (E) and Openness (O) with psychological well-being (PWB) are best represented as personality preceding PWB, DETERMINISTIC LOCO FOR MULTILEVEL SEM 26 PSIS unreliable 0.0 0.5 1.0 0.00 0.25 0.50 0.70 1.00 1.25 Pareto k ^j (PSIS reliability diagnostic) Per-school elpdLOCO disagreement M1 M2 M3 

Figure 4 

Per-school disagreement between the Taylor elpd loco and its MCMC +PSIS counterpart, against the PSIS reliability diagnostic ˆkj , for all three models. Below ˆkj = 0 .5 the discrepancy is centred on zero. The seven pairs in the shaded region beyond ˆkj = 0 .7,where the importance-sampling estimate is not to be trusted, include the two largest discrepancies, both in the M2 score at ˆkj > 1.

PWB preceding personality, reciprocal prediction, or temporal stability alone. The restriction to E and O is substantive rather than data-driven, as the two traits form the Plasticity metatrait in higher-order models of personality (DeYoung, 2015; Digman, 1997). Furthermore, an earlier analysis of the same three MIDUS waves found that E and O, but not the other Big Five traits, predicted subsequent PWB (Joshanloo, 2023). The waves were collected in 1995–1996, 2004–2006, and 2013–2014, so the two intervals are of approximately equal length. For respondent s in sibling-design family j and wave 

t, let Esjt and Osjt denote the released Midlife Development Inventory (MIDI; Lachman & Weaver, 1997) scale summaries and let rsjt = ( rsj 1t, . . . , r sj 6t)⊤ contain the six Ryff domain scores: autonomy, environmental mastery, personal growth, positive relations, purpose in life, and self-acceptance (Ryff & Keyes, 1995). Stacked over the three waves, the jointly modelled response vector is 

ysj = (

Esj 1, O sj 1, r⊤

> sj 1

, E sj 2, O sj 2, r⊤

> sj 2

, E sj 3, O sj 3, r⊤

> sj 3
> )⊤

∈ R24 ,

and each score is placed on a common Wave-1 reference scale, using the same centring and scaling constants at all three waves. Complete observations give N = 761 respondents in J = 429 families of size one to seven, of which 204 are singletons after longitudinal filtering. In the notation of Section 2, then, p = 24 and nj ∈ { 1, . . . , 7}.Every coordinate is decomposed into a family and a respondent component in DETERMINISTIC LOCO FOR MULTILEVEL SEM 27 the manner of Section 2, with d = 1 , . . . , 6 indexing the Ryff domains: 

Esjt = νEt + Eb 

> jt

+ Ew

> sjt

, Osjt = νOt + Ob 

> jt

+ Ow

> sjt

, rsjdt = νdt + rb 

> jdt

+ rw

> sjdt

,

the intercepts free at every item and wave. Within families, E and O enter as observed summaries with no measurement model of their own, while the six domain components share a single PWB factor ηw 

> sjt

at each wave, rw 

> sjdt

= λw 

> d

ηw 

> sjt

+ ϵw

> sjdt

. Self-acceptance is the marker variable, and the remaining loadings are equal over waves (Joshanloo, 2023), which places ηw 

> sjt

on a common scale and makes its autoregression interpretable. Residual variances are free and their covariances are zero, within and across waves. Collecting the within-family state as hw 

> sjt

= ( Ew

> sjt

, O w

> sjt

, η w

> sjt

)⊤, and writing subscripts outcome-first so that, e.g., βηE denotes Ew 

> sjt

→ ηw

> sj,t +1

, the transition model is 

hw 

> sj,t +1

= Γw(δ)hw 

> sjt

+ ζw

> sj,t +1

, t = 1 , 2, Γw(δ) = 

> 

aE 0 δEη βEη 

0 aO δOη βOη 

δηE βηE δηO βηO aη 

> 

,

with hw 

> sj 1

∼ N 3(0, Ψw1 ) and ζw 

> sjt

∼ N 3(0, Ψw 

> t

) for t = 2 , 3, the three unrestricted diagonal blocks of the within-level disturbance covariance Ψw of Section 2. The diagonal stability paths are always included, while the four personality–PWB paths are selected, switched on and off by the indicators δ. Each coefficient is shared across the two intervals, which the near-equal wave spacing makes reasonable, so one δ controls two displayed arrows. E–O cross-lags are fixed to zero, their contemporaneous association remaining free at every wave through Ψw 

> t

, so that the candidates differ along one substantive axis only. Between families, each of the eight constructs v ∈ { E, O, r 1, . . . , r 6} has one stable component carrying unit loadings on its three wave scores, vb 

> jt

= f b 

> jv

+ eb

> jvt

, with 

f b 

> j

= ( f b 

> jE

, . . . , f b 

> jr 6

)⊤ ∼ N 8(0, Ψb) unrestricted and free wave-specific residual variances 

Θb. This block captures stable family covariance without assigning temporal direction. It carries no between-family PWB factor and no level-2 paths, which keeps the entire structural question at the respondent-within-family level. Order the four path indicators as δ = ( δηE , δ Eη , δ ηO , δ Oη )⊤ ∈ { 0, 1}4. For either personality trait T ∈ { E, O }, the pair ( δηT , δ T η ) distinguishes stability alone (0 , 0), trait-leading (1 , 0), PWB-leading (0 , 1), and reciprocal prediction (1 , 1). The Cartesian product for E and O gives 2 4 = 16 candidates, so the eight optional arrows in Figure 5 represent just four decisions, not eight. The encompassing model has δ = (1 , 1, 1, 1) ⊤,and every other structure is the linear restriction Cδϑ = 0 of Section 3.3 that sets the omitted β coordinates to zero. Everything else—autoregressions, covariances, measurement parameters, intercepts, and the family block—remains free and updates when the restriction is imposed. Since INLAvaan places independent priors on the parameters by default, the compatible-prior convention (17) holds for every Cδ.DETERMINISTIC LOCO FOR MULTILEVEL SEM 28    

> Wave 1
> Ew1
> ηw1
> Ow1
> Wave 2
> Ew2
> ηw2
> Ow2
> Wave 3
> Ew3
> ηw3
> Ow3
> aEaE
> aηaη
> aOaO
> βηE
> βηE
> βEη
> βEη
> βηO
> βηO
> βOη
> βOη

Figure 5 

Compact overview of the encompassing respondent-within-family model. Black stability paths are retained throughout; the coloured β paths are selectively set to zero in the submodels. Each coefficient is shared across the two intervals; measurement indicators for η are omitted, and the within-wave disturbance covariances among E, η, and O are freely estimated in every candidate but not drawn. 

Each family is one cluster, so the cluster reduced form (6) applies with p = 24, and we rank the 16 restrictions by their Taylor elpd loco scores. Whole-family deletion asks which structure best predicts a family unseen during fitting. The conditional target (9) is in any case unavailable here, since 204 of the 429 families are singletons with no clustermates to condition on. The encompassing model converged to a proper solution with 132 free parameters, and the selected structure retained η → E and 

η → O, but omitted E → η and O → η (Figure 6a). Relative to it, adding O → η was worse by 13.8 elpd units (paired SE 2.7), and adding both trait-leading paths, which recovers the encompassing model, worse by 27.0 (SE 3.6). In a separate fit of the selected model the shared unstandardised coefficients were ̂ βEη = 0 .164 (posterior SD 0.035) and ̂ βOη = 0 .146 (SD 0.033). Prediction for a new family therefore supported PWB-leading rather than reciprocal prospective connectivity for both Plasticity traits. Separate fits leave that selection unchanged, with rank agreement over all 16 structures at Kendall’s τ = 0 .88. Conditioned scores were uniformly conservative by an amount that tracked the joint distance (23) of the restriction from the encompassing centre. The three candidates at Dm ≤ 2.11 were under-scored by 2.0 to 4.5 elpd units, the twelve at Dm ≥ 4.40 by 7.3 to 12.5—the far-from-centre degradation of Section 5.2, which only affects structures the data already reject, pushing them further out of contention while leaving the decision intact. A structural hypothesis space can therefore be enumerated by this conditioning strategy rather than narrowed by hand, with every candidate scored on the target the design calls for. In this case, enumerating all 16 structures revises an earlier finding on these waves. Joshanloo’s (2023) within-person model found psychological well-being and both Plasticity traits to be reciprocally related. Scored out of family, the DETERMINISTIC LOCO FOR MULTILEVEL SEM 29 -5.9 (8.1) -20.9 (8.1) +0.2 (6.6) -16.9 (6.4) -18.7 (8.1) -28.5 (8.2) -13.2 (6.8) -24.7 (6.7) +10.6 (7.5) -4.8 (7.4) +27.0 (3.6) +7.9 (2.1) -2.4 (7.0) -12.6 (7.1) +13.2 (1.9) 0.0 (reference) Reciprocal PWB → EE → PWB Stability only Extraversion-PWB pathway Stability only O → PWB PWB → OReciprocal Openness-PWB pathway 

> Conditioned second-order submodel scores

(a) Selected Encompassing -24250 -24230 -24210 Separately  fi tted elpdLOCO -24250 -24230 -24210 Conditioned elpdLOCO 

> Conditioning check across all 16 models

(b)     

> ΔelpdLOCO vs encompassing
> -30 -20 -10 010 20 30

Figure 6 

The 16 model structures, scored by family-loco . (a) Conditioned second-order Taylor 

∆ elpd loco relative to the encompassing model and SE in parentheses, by Extraversion–PWB (rows) and Openness–PWB (columns) pathways; positive values favour the restriction, with the selected structure outlined. (b) The same scores against their separately fitted counterparts, with the equality line dashed; every conditioned value comes from a single encompassing fit. 

trait-leading half of that reciprocity does not pay for itself, and dropping it improves elpd by 27.0 units, so that the leading process for both traits is well-being. That is a predictive ordering and not a causal one, drawn on a family-relative rather than person-relative decomposition and with E and O observed rather than latent. It is nonetheless a distinction this criterion can draw, and one that the cost of refitting has until now kept out of reach. 

8 Discussion 

The marginal, cluster-level predictive density is the correct target for multilevel SEM whenever a structural theory is meant to generalise to new clusters (Merkle et al., 2019), but what has been lacking is an affordable way to compute it, since the existing routes either refit the model once per deleted cluster or reweight a posterior sample by importance weights that fail exactly where a held-out cluster is influential enough to matter. Our work takes a single INLAvaan fit and returns the loco predictive score for the encompassing model and for every submodel obtained by zeroing structural paths, with no refits, no Monte Carlo over the latent state, and no harmonic-mean variance pathology. The loco cost is one Gaussian conditional update and one O(Jd 2)post-processing pass per scored submodel, small relative to the encompassing fit itself. DETERMINISTIC LOCO FOR MULTILEVEL SEM 30 The construction replaces one kind of numerical error with another, without changing what is estimated. Since log p(yj | y−j ) is a fixed functional of the posterior, a Monte Carlo evaluation returns a random value whose error is sampling noise, while the Taylor score returns a deterministic value whose error is that of the truncation itself. That error depends on how far deleting cluster j displaces the posterior, hence on whether any one cluster carries an appreciable share of the information in the sample. In simulation, the truncation error of the second-order Taylor score never exceeds 0 .061 elpd per cluster, and in the two real-data applications the score agrees with MCMC+PSIS wherever PSIS is itself reliable and reproduces the selection that separate refits deliver across an enumerated candidate space. The reach of the construction is delimited by its assumptions. The closed-form cluster likelihood (8) requires continuous indicators and Gaussian latents. The cluster-deleted posterior identity (12) requires only cluster independence given ϑ, and so holds in much broader settings. The Taylor score of Section 3.2 requires only a twice-differentiable per-unit log-likelihood and a Gaussian posterior summary ( ϑ∗, Ω), so it generalises to any model for which such a summary can be formed. In particular, the summary need not come from a Laplace-based optimisation at all. The mean and covariance of a set of MCMC draws estimate exactly such a summary, and a centre near the posterior mean is in fact preferable to the mode (van Niekerk & Rue, 2024), so moment matching the draws and applying (13)–(15) verbatim turns the posterior sample of any Bayesian SEM engine into the same Taylor elpd scores, submodel conditioning and existence check included. For MCMC users this is an alternative route to the cluster-level score that may strain under importance sampling. Comparing the first- and second-order Taylor scores (13) and (15) provides a cheap internal check at no additional cost, their gap being the curvature correction the expansion makes for each cluster. These gaps are small where the truncation has settled, large where it has not, and sum over clusters to 12 peff , so a total materially exceeding 12 

> ∑
> j

tr( −ΩH j ), a quantity the same post-processing pass already returns, signals that the expansion has not converged for the sample as a whole. The existence condition (14) is a second such check. Its failure indicates breakdown of the local quadratic approximation for that deletion, rather than failure of the exact loco 

predictive density. Since Aj is the approximate cluster-deleted precision, Aj̸ ≻ 0 means that the deleted summary is improper, the curvature attributed to cluster j meeting or exceeding the entire posterior precision in some direction of the parameter space. The eigenvalue kj of Section 3.2 measures this leverage, which a well-identified model keeps small. Multilevel SEM are models for fitting two covariance matrices, Σw

to the pooled within-cluster scatter and Σb to the spread of the cluster means. Each cluster contributes its scatter Sj to the former and its mean ¯ yj to the latter, so no parameter element in ϑ belongs to any one cluster, and a cluster’s share of the evidence DETERMINISTIC LOCO FOR MULTILEVEL SEM 31 is roughly nj /N for Σw and 1 /J for Σb. A single cluster can therefore dominate a direction only by being very large, by lying far from the other cluster means, or by being the only cluster whose data carry information about some parameter at all. If a violation happens to occur, it is then a prompt to inspect the data (why does cluster j

contribute so much curvature?) or the specification (is some parameter identified by cluster j alone, such as a path from a covariate that varies in one cluster only?). Methodologically, the submodel scoring of Section 3.3 is a fast surrogate for projection-predictive variable selection (Piironen et al., 2020), but defined directly through the predictive score rather than through a projection onto a reference model. The rank-one update (21) plays a role analogous to projecting onto a constrained submodel, with closed-form Gaussian conditioning in place of a per-candidate Kullback–Leibler optimisation, and no input beyond the summaries the encompassing fit already produces. The conditioning step of Section 3.4 is exact under the Laplace summary of the encompassing posterior and so inherits only that summary’s error. In directions where the posterior is approximately Gaussian the restricted summary is essentially a refit, as borne out by the agreement reported above, whereas in directions where it is highly skewed or multimodal the procedure is best treated as a fast screen, with selected submodels ideally validated by full refits. Because the conditioning error of Section 5.2 is one-sided, that screen errs in one direction only, wrongly excluding a candidate whose restriction sits far from the centre but never spuriously promoting one, and Dm (23) names the candidates at risk before any scoring is done. Three future directions are discussed. First, genuinely non-nested or differently parameterised alternatives, including candidates that change the prior and so leave the compatible-prior convention of Section 3.3, lie outside the conditioning step and require a second fit. But that fit costs seconds, its Taylor scores target the same estimand as the first, and the two candidates would compare directly. Second, the cluster log-likelihood is the only ingredient that changes across designs, so deeper nesting, multigroup data, and incomplete data all enter through the kernel alone, with the deletion identity, the Taylor score, and the conditioning step untouched. Measurement-invariance constraints (Meredith, 1993) are linear restrictions on a group-indexed ϑ, so configural, metric, and scalar specifications could in principle be scored by conditioning a single configural fit. With incomplete data, a full-information kernel (Enders, 2022) scores each unit on the entries it has, valid under missingness at random (Rubin, 1976) provided candidates share the same observed entries. Third, and most interestingly, non-Gaussian indicators preclude the closed form (8), since the latent state cannot be integrated out analytically as in Liu et al. (2025). Yet, the rest of the methodology translates seamlessly. By simply approximating this outer integral—computing ℓj , sj , and Hj through numerical or Laplace integration—the entirety of the downstream pipeline applies exactly as before. DETERMINISTIC LOCO FOR MULTILEVEL SEM 32 

CRediT Author Statement 

Mohammad Alhyari: Conceptualization, Formal analysis, Investigation, Methodology, Project administration, Writing - Original Draft; Haziq Jamil: Data curation, Formal analysis, Funding acquisition, Investigation, Software, Validation, Visualization, Writing - Original Draft, Writing - Review & Editing; Hans Montcho: Conceptualization, Writing - Review & Editing; Håvard Rue: Funding acquisition, Supervision, Validation, Writing - Review & Editing. 

Acknowledgements 

This publication is based upon work supported by the King Abdullah University of Science and Technology (KAUST) Research Funding Office under Award No. URF/1/6921-01-01. 

Data Availability 

The two empirical datasets used in this article are publicly accessible. The MIDUS data (Radler, 2014) are archived in the Midlife in the United States (MIDUS) Series at the Inter-university Consortium for Political and Social Research (ICPSR), and the three waves analysed here are distributed as ICPSR 2760 (MIDUS 1, 1995–1996), ICPSR 4652 (MIDUS 2, 2004–2006), and ICPSR 36346 (MIDUS 3, 2013–2014), available at https://www.icpsr.umich.edu/web/ICPSR/series/203 subject to free registration. The PISA 2022 data are available from the OECD PISA 2022 Database (OECD, 2024). All code required to fully reproduce the results is deposited in an open repository at https://osf.io/7au5h/overview?view_only=965ceb3d57c34fbd96689407373f0ea5, and reported timings are wall-clock on an Apple M4 Pro (14 cores, 24 GB) under R 4.5.2, with the closed-form scoring passes parallelised over 13 cores. DETERMINISTIC LOCO FOR MULTILEVEL SEM 33 

References 

Bollen, K. A. (1989). Structural equations with latent variables (1st ed.). Wiley. https://doi.org/10.1002/9781118619179 Consonni, G., & Veronese, P. (2008). Compatibility of prior specifications across linear models. Statistical Science , 23 (3), 332–353. https://doi.org/10.1214/08-STS258 DeYoung, C. G. (2015). Cybernetic Big Five theory. Journal of Research in Personality ,

56 , 33–58. https://doi.org/10.1016/j.jrp.2014.07.004 Dickey, J. M. (1971). The weighted likelihood ratio, linear hypotheses on normal location parameters. The Annals of Mathematical Statistics , 42 (1), 204–223. https://doi.org/10.1214/aoms/1177693507 Digman, J. M. (1997). Higher-order factors of the Big Five. Journal of Personality and Social Psychology , 73 (6), 1246–1256. https://doi.org/10.1037/0022-3514.73.6.1246 Enders, C. K. (2022). Applied missing data analysis (2nd ed.). Guilford Press. Freni-Sterrantino, A., Rustand, D., van Niekerk, J., Krainski, E., & Rue, H. (2025). A graphical framework for interpretable correlation matrix models for multivariate regression. Statistical Methods & Applications , 34 (3), 409–447. https://doi.org/10.1007/s10260-025-00788-y Freni-Sterrantino, A., van Niekerk, J., Krainski, E. T., Rustand, D., Jamil, H., & Rue, H. (2026). Informative distance-based priors for correlation matrices centred on a target reference. arXiv . https://doi.org/10.48550/arXiv.2607.18926 Gelfand, A. E., Dey, D. K., & Chang, H. (1992). Model determination using predictive distributions with implementation via sampling-based methods. In J. M. Bernardo, J. O. Berger, A. P. Dawid, & A. F. M. Smith (Eds.), Bayesian statistics 4 (pp. 147–167). Oxford University Press. https://doi.org/10.1093/oso/9780198522669.003.0009 Gelman, A., Vehtari, A., McElreath, R., Simpson, D., Margossian, C. C., Yao, Y., Kennedy, L., Gabry, J., Bürkner, P.-C., Modrák, M., & Barajas, V. L. (2026). 

Bayesian workflow . Chapman & Hall/CRC. https://doi.org/10.1201/9781003044024 Hsu, H.-Y., Kwok, O.-M., Lin, J.-H., & Acosta, S. (2015). Detecting misspecified multilevel structural equation models with common fit indices: A Monte Carlo study. Multivariate Behavioral Research , 50 (2), 197–215. https://doi.org/10.1080/00273171.2014.977429 Jacobucci, R., Grimm, K. J., & McArdle, J. J. (2016). Regularized structural equation modeling. Structural Equation Modeling: A Multidisciplinary Journal , 23 (4), 555–566. https://doi.org/10.1080/10705511.2016.1154793 DETERMINISTIC LOCO FOR MULTILEVEL SEM 34 Jamil, H., & Rue, H. (2026a). Approximate Bayesian inference for structural equation models using integrated nested Laplace approximations. arXiv .https://doi.org/10.48550/arXiv.2603.25690 Jamil, H., & Rue, H. (2026b). Implementation and workflows for INLA-based approximate Bayesian structural equation modelling. arXiv .https://doi.org/10.48550/arXiv.2604.00671 Joshanloo, M. (2023). Reciprocal relationships between personality traits and psychological well-being. British Journal of Psychology , 114 (1), 54–69. https://doi.org/10.1111/bjop.12596 Klugkist, I., & Hoijtink, H. (2007). The Bayes factor for inequality and about equality constrained models. Computational Statistics & Data Analysis , 51 (12), 6367–6379. https://doi.org/10.1016/j.csda.2007.01.024 Lachman, M. E., & Weaver, S. L. (1997). The Midlife Development Inventory (MIDI) personality scales: Scale construction and scoring . Brandeis University. https://www.brandeis.edu/psychology/lachman/pdfs/midi-personality-scales.pdf Liu, Z., van Niekerk, J., & Rue, H. (2025). Leave-group-out cross-validation for latent Gaussian models. SORT-Statistics and Operations Research Transactions , 49 (1), 121–146. https://doi.org/10.57645/20.8080.02.25 Lu, Z.-H., Chow, S.-M., & Loken, E. (2016). Bayesian factor analysis as a variable-selection problem: Alternative priors and consequences. Multivariate Behavioral Research , 51 (4), 519–539. https://doi.org/10.1080/00273171.2016.1168279 Meredith, W. (1993). Measurement invariance, factor analysis and factorial invariance. 

Psychometrika , 58 (4), 525–543. https://doi.org/10.1007/BF02294825 Merkle, E. C. (2026). Nuances of information criteria for Bayesian psychometric models. 

Methodology , 22 (3), 195–224. https://doi.org/10.5964/meth.20361 Merkle, E. C., Fitzsimmons, E., Uanhoro, J., & Goodrich, B. (2021). Efficient Bayesian structural equation modeling in Stan. Journal of Statistical Software , 100 (6), 1–22. https://doi.org/10.18637/jss.v100.i06 Merkle, E. C., Furr, D., & Rabe-Hesketh, S. (2019). Bayesian comparison of latent variable models: Conditional versus marginal likelihoods. Psychometrika , 84 (3), 802–829. https://doi.org/10.1007/s11336-019-09679-0 Merkle, E. C., & Rosseel, Y. (2018). blavaan: Bayesian structural equation models via parameter expansion. Journal of Statistical Software , 85 (4), 1–30. https://doi.org/10.18637/jss.v085.i04 Muthén, B. (1994). Multilevel covariance structure analysis. Sociological Methods & Research , 22 (3), 376–398. https://doi.org/10.1177/0049124194022003006 DETERMINISTIC LOCO FOR MULTILEVEL SEM 35 Muthén, B., & Asparouhov, T. (2012). Bayesian structural equation modeling: A more flexible representation of substantive theory. Psychological Methods , 17 (3), 313–335. https://doi.org/10.1037/a0026802 OECD. (2024). PISA 2022 technical report . https://doi.org/10.1787/01820d6d-en Penny, W. D., & Ridgway, G. R. (2013). Efficient posterior probability mapping using Savage–Dickey ratios. PLOS ONE , 8 (3), e59655. https://doi.org/10.1371/journal.pone.0059655 Piironen, J., Paasiniemi, M., & Vehtari, A. (2020). Projective inference in high-dimensional problems: Prediction and feature selection. Electronic Journal of Statistics , 14 (1), 2155–2197. https://doi.org/10.1214/20-EJS1711 Radler, B. T. (2014). The Midlife in the United States (MIDUS) series: A national longitudinal study of health and well-being. Open Health Data , 2, e3. https://doi.org/10.5334/ohd.ai Rosseel, Y. (2012). lavaan: An R package for structural equation modeling. Journal of Statistical Software , 48 (2), 1–36. https://doi.org/10.18637/jss.v048.i02 Rosseel, Y. (2021). Evaluating the observed log-likelihood function in two-level structural equation modeling with missing data: From formulas to R code. 

Psych , 3 (2), 197–232. https://doi.org/10.3390/psych3020017 Rubin, D. B. (1976). Inference and missing data. Biometrika , 63 (3), 581–592. https://doi.org/10.1093/biomet/63.3.581 Rue, H., Martino, S., & Chopin, N. (2009). Approximate Bayesian inference for latent Gaussian models by using integrated nested Laplace approximations. Journal of the Royal Statistical Society: Series B (Statistical Methodology) , 71 (2), 319–392. https://doi.org/10.1111/j.1467-9868.2008.00700.x Ryff, C. D., & Keyes, C. L. M. (1995). The structure of psychological well-being revisited. Journal of Personality and Social Psychology , 69 (4), 719–727. https://doi.org/10.1037/0022-3514.69.4.719 Ryu, E., & West, S. G. (2009). Level-specific evaluation of model fit in multilevel structural equation modeling. Structural Equation Modeling: A Multidisciplinary Journal , 16 (4), 583–601. https://doi.org/10.1080/10705510903203466 Sivula, T., Magnusson, M., Matamoros, A. A., & Vehtari, A. (2025). Uncertainty in Bayesian Leave-One-Out Cross-Validation Based Model Comparison. Bayesian Analysis . https://doi.org/10.1214/25-BA1569 Spiegelhalter, D. J., Best, N. G., Carlin, B. P., & Van Der Linde, A. (2002). Bayesian measures of model complexity and fit. Journal of the Royal Statistical Society Series B: Statistical Methodology , 64 (4), 583–639. https://doi.org/10.1111/1467-9868.00353 DETERMINISTIC LOCO FOR MULTILEVEL SEM 36 Tierney, L., & Kadane, J. B. (1986). Accurate approximations for posterior moments and marginal densities. Journal of the American Statistical Association , 81 (393), 82–86. https://doi.org/10.1080/01621459.1986.10478240 van Niekerk, J., Krainski, E., Rustand, D., & Rue, H. (2023). A new avenue for Bayesian inference with INLA. Computational Statistics & Data Analysis , 181 ,107692. https://doi.org/10.1016/j.csda.2023.107692 van Niekerk, J., & Rue, H. (2024). Low-rank variational Bayes correction to the Laplace method. Journal of Machine Learning Research , 25 (62), 1–25. Vehtari, A., Gelman, A., & Gabry, J. (2017). Practical Bayesian model evaluation using leave-one-out cross-validation and WAIC. Statistics and Computing , 27 (5), 1413–1432. https://doi.org/10.1007/s11222-016-9696-4 Vehtari, A., Simpson, D., Gelman, A., Yao, Y., & Gabry, J. (2024). Pareto smoothed importance sampling. Journal of Machine Learning Research , 25 (72), 1–58. https://jmlr.org/papers/v25/19-556.html Verdinelli, I., & Wasserman, L. (1995). Computing Bayes factors using a generalization of the Savage–Dickey density ratio. Journal of the American Statistical Association , 90 (430), 614–618. https://doi.org/10.1080/01621459.1995.10476554 Watanabe, S. (2010). Asymptotic equivalence of Bayes cross validation and widely applicable information criterion in singular learning theory. Journal of Machine Learning Research , 11 (116), 3571–3594. https://www.jmlr.org/papers/v11/watanabe10a.html Wolpert, R. L., & Schmidler, S. C. (2012). α-stable limit laws for harmonic mean estimators of marginal likelihoods. Statistica Sinica , 22 (3), 1233–1251. https://doi.org/10.5705/ss.2010.221 DETERMINISTIC LOCO FOR MULTILEVEL SEM 37 

Appendix A Joint and Conditional Treatment of Exogenous Covariates 

The main text models exogenous covariates jointly with the indicators. Software offers a second convention, and this appendix details the practical consequences of each choice. Under the joint formulation ( fixed.x = FALSE ) the covariates receive a saturated Gaussian block and are modelled alongside the indicators; under the conditional formulation ( fixed.x = TRUE , the lavaan default) their moments are held at the sample values (¯ x, Sx) and only the outcomes are modelled. The saturated block is variation-independent of the structural parameters, so the two return identical structural estimates, and Figure A1 shows them side by side.                         

> ηxy1y2y3
> γλ1λ2λ3
> ϕψ
> (a) Joint ( fixed.x = FALSE ): scores log p(yj,xj| D −j).
> ηxy1y2y3
> γλ1λ2λ3
> given ψ
> (b) Conditional ( fixed.x = TRUE ): scores log p(yj|xj,D−j).

Figure A1 

The two covariate formulations, for a toy model with one covariate, one latent con-struct, and three indicators. Shading marks the variables to which the model assigns a distribution. Every parameter is common to both panels and estimated identically. The formulations differ only in whether the covariate carries parameters of its own, here the variance ϕ.

The two part company in what they predict. Writing xj for the held-out cluster’s covariates and D−j for all data with cluster j deleted in its entirety, the leave-one-cluster-out estimands are elpd J =

> J∑
> j=1

log p(yj , xj | D −j ), elpd C =

> J∑
> j=1

log p(yj | xj , D−j ),

linked pointwise by the predictive identity log p(yj , xj | D −j ) = log p(yj | xj , D−j ) + log p(xj | D −j ). (A1) The joint estimand scores a new cluster drawn from the population, covariates included; the conditional estimand scores that cluster’s outcomes at a known covariate profile. This is a different distinction from the conditional-versus-marginal treatment of the latent effect in Section 2, where the quantity conditioned upon is unobserved and estimated from the very outcomes being scored, rather than observed and ancillary. DETERMINISTIC LOCO FOR MULTILEVEL SEM 38 Two consequences follow from (A1). Firstly, candidate models carrying the same covariate block share the term log p(xj | D −j ), which cancels pointwise from their paired contrast, so the two formulations return identical elpd differences and identical paired standard errors. Secondly, a joint elpd scores the pair ( yj , xj ) and a conditional one scores yj alone, so the two are never comparable (two joint scores are comparable only when they cover the same variables). Covariate selection under the joint formulation therefore deletes paths rather than variables, whereas under the conditional formulation candidate models may differ in their covariate sets. Under the conditional formulation the covariate moments are data-dependent constants inside the likelihood, frozen at their full-sample values (¯ x, Sx). Deleting cluster j would move them to (¯ x−j , Sx, −j ) and so disturb every remaining factor, rather than simply removing the jth, which is exactly what the reweighting identity of Proposition 1 does not allow. However, the following lemma proves that the conditional density does not involve the covariate moments ( μx, Σxx ) at all, so the values at which they are frozen are immaterial. 

Lemma 4 (Invariance to the frozen block) . Let the covariates enter the structural model through a coefficient matrix Γ, so that η = α + Γx + Bη + ζ. Then the model-implied conditional density p(y | x, ϑ) does not depend on the frozen covariate moments (μx, Σxx ).Proof. Solving the structural equation and substituting into (1), the model-implied moments of the enlarged vector ( y⊤, x⊤)⊤ are 

μy = ν + Λ(I − B)−1(α + Γμx),

Σyx = Λ(I − B)−1ΓΣ xx ,

Σyy = Λ(I − B)−1(

ΓΣ xx Γ⊤ + Ψ)

(I − B)−⊤ Λ⊤ + Θ.

The conditional regression coefficient is Σyx Σ−1 

> xx

= Λ(I − B)−1Γ, in which Σxx has cancelled, so the conditional mean is 

μy + Σyx Σ−1 

> xx

(x − μx) = ν + Λ(I − B)−1(α + Γμx) + Λ(I − B)−1Γ(x − μx)= ν + Λ(I − B)−1(α + Γx ),

in which μx has cancelled as well. The conditional covariance is the Schur complement 

Σyy − Σyx Σ−1 

> xx

Σxy = Λ(I − B)−1(

ΓΣ xx Γ⊤ + Ψ)

(I − B)−⊤ Λ⊤ + Θ

− Λ(I − B)−1ΓΣ xx Γ⊤(I − B)−⊤ Λ⊤,

which reduces to Λ(I − B)−1Ψ(I − B)−⊤ Λ⊤ + Θ, the reduced-form covariance (3) of the outcomes alone. DETERMINISTIC LOCO FOR MULTILEVEL SEM 39 

Table A1 

Which formulation admits which use; joint is fixed.x = FALSE and conditional 

fixed.x = TRUE . On the first row, the case throughout this paper, the two do not merely both apply but agree exactly. A covariate decomposed across levels, or observed incompletely, must be given a distribution; a non-Gaussian one must not.            

> Joint Conditional Comparisons over a common covariate set ✓✓
> Comparisons over differing covariate sets ×✓
> Within- and between-cluster decomposition of a covariate ✓×
> Incompletely observed covariates ✓×
> Non-Gaussian covariates ×✓

In the two-level case the frozen block is the between-level marginal of the covariates, and since each Σl(ϑ) is the level-l instance of the same reduced form, the cancellation applies within each level and hence to the cluster covariance Vj (ϑ)assembled from them. The conditional cluster log-likelihood is therefore the joint one shifted by a ϑ-free constant, leaving sj and Hj unchanged, so the conditional Taylor score inherits exactly the error of the joint one. Everything above transfers to the single-row case ( loso ) of Section 4 on replacing ( yj , xj ) by ( yi, xi). In summary, the two formulations are interchangeable wherever candidate models are compared over a common set of variables, as they are throughout this paper, and are otherwise separated by modelling choice, not scoring, as Table A1 sets out. DETERMINISTIC LOCO FOR MULTILEVEL SEM 40 

Appendix B Proof of the Laplace Approximation of the Cluster CPO 

Proof of Theorem 2. Substituting the second-order expansion of ℓj into Ij and the Gaussian density gives ˆI(2)  

> j

= e−ℓj (ϑ∗)

(2 π)d/ 2|Ω|1/2

> ∫

exp 

> (

−s⊤ 

> j

δ − 12 δ⊤Aj δ

> )

dδ.

Setting Hj = 0 gives ˆI(1)  

> j

with Aj replaced by Ω−1, so the two truncations differ only in which precision matrix enters the Gaussian integral tilted by the linear form −s⊤ 

> j

δ.Diagonalising Aj = U diag( a1, . . . , a d)U⊤ with U orthogonal and substituting z := U⊤δ

factorises the integral into the d scalar integrals ∫ exp (

−tizi − 12 aiz2

> i
> )

dz i with t := U⊤sj ,each finite exactly when ai > 0 and divergent otherwise, whatever the value of ti. The integral therefore converges if and only if Aj ≻ 0, and congruence by Ω1/2 turns this into the symmetric form on the right of (14), since −Ω1/2Hj Ω1/2 is symmetric with the same eigenvalues as −ΩH j . At Hj = 0, Aj = Ω−1 ≻ 0, so the integral is finite for every finite sj .Completing the square, δ⊤Aj δ + 2 s⊤ 

> j

δ = ( δ + A−1 

> j

sj )⊤Aj (δ + A−1 

> j

sj ) − s⊤ 

> j

A−1 

> j

sj ,so the integral evaluates to (2 π)d/ 2|Aj |−1/2 exp ( 12 s⊤ 

> j

A−1 

> j

sj

> )

. The determinant ratio is 

|Aj |−1/2

|Ω|1/2 =

> ∣∣∣

Aj Ω

> ∣∣∣−1/2

=

> ∣∣∣

I + ΩH j

> ∣∣∣−1/2

,

the last equality using Aj Ω = I + Hj Ω and |I + Hj Ω| = |I + ΩH j | (Sylvester’s identity). Hence, under (14), ˆI(2)  

> j

= exp (

−ℓj (ϑ∗) + 12 s⊤ 

> j

A−1 

> j

sj

> )

· | I + ΩH j |−1/2, where the determinant |I + ΩH j | = |Ω| | Aj | is positive, so the log-determinant term is real, and − log ˆI(2)  

> j

gives (15). At Hj = 0, Aj = Ω−1 and |I + ΩH j | = 1, so (15) reduces to (13) exactly, as in the case of (i). DETERMINISTIC LOCO FOR MULTILEVEL SEM 41 

Appendix C Marginal WAIC at First Order 

The marginal WAIC targets the same leave-one-cluster-out quantity as the scores of Section 3.2 (Merkle et al., 2019), and is usually computed from posterior draws. Under a Gaussian density it needs none. Both of its halves—the log pointwise predictive density lppd j := log Eϑ|y

> [

p(yj | ϑ) ]

and the penalty pWAIC ,j := Var ϑ|y

> [

ℓj (ϑ) ]

(Vehtari et al., 2017; Watanabe, 2010)—are expectations of the same expansions of ℓj against the same N (ϑ∗, Ω), and so are functions of ( sj , Hj , Ω) alone. At first order they return the score of Theorem 2(i) exactly. 

Corollary 5 (Marginal WAIC at first order) . Under the Gaussian posterior approximation and the first-order expansion of Theorem 2(i), 

lppd j = ℓj (ϑ∗) + 12 s⊤ 

> j

Ωs j , pWAIC ,j = s⊤ 

> j

Ωs j ,

so that lppd j − pWAIC ,j is the first-order loco score (13) .Proof. Under the first-order expansion, ℓj (ϑ) = ℓj (ϑ∗) + s⊤ 

> j

δ is linear in δ ∼ N (0 , Ω), hence Gaussian with mean ℓj (ϑ∗) and variance s⊤ 

> j

Ωs j . Consequently, lppd j is its cumulant generating function evaluated at one, pWAIC ,j its variance, and their difference yields (13). The resulting penalty 12 pWAIC ,j in (13) is thus a Jensen-style one, falling hardest on clusters whose likelihood is most sensitive to ϑ. Summed over clusters,  

> ∑
> j

pWAIC ,j = tr( Ω ∑ 

> j

sj s⊤ 

> j

), the outer-product form of the information, which agrees with the peff of (16) to the same O(J−1) under correct model specification. This sum is also precisely what the loo package reports as ploo (Vehtari et al., 2017), and in that case, estimated using PSIS. DETERMINISTIC LOCO FOR MULTILEVEL SEM 42 

Appendix D Proof of the Compatible-Prior Identity 

Proof of Lemma 3. By Bayes’ theorem, π(ϑ−a | y, ϑ a = 0) ∝ p(y | 0, ϑ−a) π(0 , ϑ−a). Factor the joint prior on the constraint slice as π(0 , ϑ−a) = πa(0) π(ϑ−a | ϑa = 0). The marginal prior mass πa(0) is constant in ϑ−a and absorbs into the normalising constant. The submodel refit posterior is πm(ϑ−a | y) ∝ p(y | 0, ϑ−a) πm(ϑ−a) with the same likelihood, since “ ϑa fixed at zero” and “path a absent” specify the identical sampling model. The two posteriors coincide iff πm(ϑ−a) ∝ π(ϑ−a | ϑa = 0), which equality of proper densities promotes to the hypothesis. The general case Cmϑ = 0 follows by an invertible reparameterisation whose Jacobian cancels as πa(0) did. DETERMINISTIC LOCO FOR MULTILEVEL SEM 43 

Appendix E Score and Hessian of a Restricted Submodel 

The restricted score (22) requires the free-subspace score sj,m, free and Hessian Hj,m, free of the cluster log-likelihood at the restricted centre ϑ∗

> m

. Taking the mean structure saturated so that structural paths do not enter μ(ϑ), the score and Hessian are analytic in the model-implied covariances: writing Wj,m = Vj (ϑ∗

> m

)−1, G(j,m ) 

> a

= ∂Vj (ϑ∗

> m

)/∂ϑ a,

G(j,m ) 

> ag

= ∂2Vj (ϑ∗

> m

)/∂ϑ a ∂ϑ g, and ˜ yj = yj − 1nj ⊗ μ(ϑ∗

> m

), (sj,m, free )a = −12 tr (

Wj,m G(j,m ) 

> a

(I − Wj,m ˜yj ˜y⊤ 

> j

))

, (E1) (Hj,m, free )ag = −12 tr (

Wj,m G(j,m ) 

> a

Wj,m G(j,m ) 

> g

(2 Wj,m ˜yj ˜y⊤ 

> j

− I))

− 12 tr (

Wj,m G(j,m ) 

> ag

(I − Wj,m ˜yj ˜y⊤ 

> j

))

. (E2) The derivatives G(j,m ) 

> a

, G(j,m ) 

> ag

follow by chain rule from Σw(ϑ), Σb(ϑ) and are obtained once per candidate. Saturation of the mean structure is an assumption of these formulae, not of the construction. It holds for every model fitted in this paper: the mean structure is estimated throughout (Section 2) with free indicator intercepts ν and latent intercepts fixed at zero, the lavaan -family identification default under meanstructure = TRUE 

used by the fits of Sections 6 and 7, so that μ(ϑ) = ν and the structural matrices enter the likelihood only through the covariances. A mean structure through which structural paths do act contributes the standard mean-derivative terms to (E1)–(E2) and changes nothing else in the construction.