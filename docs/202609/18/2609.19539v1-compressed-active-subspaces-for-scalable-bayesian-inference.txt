Title: Compressed Active Subspaces for Scalable Bayesian Inference

URL Source: https://arxiv.org/pdf/2609.19539v1

Published Time: Fri, 18 Sep 2026 00:26:37 GMT

Number of Pages: 5

Markdown Content:
## COMPRESSED ACTIVE SUBSPACES FOR SCALABLE BAYESIAN INFERENCE 

## Thomas Flynn ⋆ Sanket Jantre ⋆ Byung-Jun Yoon ⋆† Kibaek Kim ‡ 

> ⋆

## Computing and Data Sciences Directorate, Brookhaven National Laboratory, Upton, NY, USA  

> †

## Department of Electrical & Computer Engineering, Texas A&M University, College Station, TX, USA  

> ‡

## Mathematics and Computer Science Division, Argonne National Laboratory, Lemont, IL, USA 

ABSTRACT 

Active subspace methods provide a framework for quanti-fying predictive uncertainty in high-dimensional models by identifying and performing inference along parameter direc-tions that have the greatest influence on the model output. However, the construction of active subspaces requires stor-ing many full-dimensional model gradients, which becomes prohibitive as model size increases. We address this limi-tation by proposing Compressed Active Subspaces (CAS) , a scalable approach that first maps the model parameters to a compressed space using a structured isometric embedding and then constructs the active subspace within this reduced parameterization. Our approach substantially reduces the memory required for active subspace construction and en-ables Bayesian inference for large models where standard active subspace methods become impractical. We demon-strate the scalability of CAS on neural networks of increasing size while maintaining predictive performance and robust uncertainty estimates. 

Index Terms — Active subspaces, Bayesian deep learn-ing, random projections, uncertainty quantification (UQ) 

1. INTRODUCTION 

Bayesian inference for deep learning models provides a principled framework for quantifying predictive uncertainty. However, its computational cost grows rapidly with the di-mensionality of the parameter space. Active subspaces offer an efficient way to address this challenge by identifying a low-dimensional linear subspace spanned by parameter di-rections that have the greatest influence on the model output [1]. Recent works show that Bayesian inference restricted to such active subspaces can provide predictive uncertainty estimates comparable to those obtained from full-network Bayesian inference while remaining computationally effi-cient [1]. Leveraging active subspaces has been shown to en-able uncertainty quantification (UQ) for relatively large deep learning models [2] and uncertainty-guided fine-tuning [3]. A key step in the active subspace approach is identify-ing the dominant eigenvectors of the covariance matrix C of model gradients: 

C = E ∇θ fθ (x)∇θ fθ (x)T  . (1) Here, the expectation is taken over the model parameters θ

and inputs x. Following [1], we consider parameter perturba-tions θ ∼ N (θ0, σ 20 I) around a pretrained model’s parame-ters θ0 and sample x ∼ p from the training data distribution. The eigenvectors associated with the largest eigenvalues of C

identify parameter directions along which the model output is most sensitive to these perturbations. In practice, the dominant eigenvectors of C can be approx-imated using a truncated SVD of sampled model gradients. Let θ1, . . . , θ M denote i.i.d. samples from N (θ0, σ 20 I) and let x1, . . . , xM denote samples from the training data distri-bution. We can construct the gradient matrix 

G = [ g1, . . . , gM ] ∈ RN ×M , gi = ∇θ fθi (xi), (2) where N is the number of model parameters. The leading left singular vectors of G provide approximate dominant eigen-vectors for C. While this approach has shown promising re-sults for neural networks [1, 2, 3], its application to large mod-els is limited by the memory required to store G. In particular, each additional sample of the model gradient requires storing another N -dimensional vector, resulting in O(N M ) memory. We address this limitation by first compressing the model parameter space before constructing the active subspace. We propose Compressed Active Subspaces (CAS) , which intro-duces a fixed low-dimensional parameterization of the model using an efficient isometric embedding. Active subspace con-struction is then performed within this compressed parameter space, substantially reducing the memory required to store the sampled model gradients. The resulting active subspace can be mapped back to the original model parameter space for quantifying predictive uncertainty using the Bayesian infer-ence framework of [1]. Our main contributions in this work are as follows: • We propose Compressed Active Subspaces for scal-able active subspace construction in models with high-dimensional parameter spaces. CAS uses a structured isometric embedding to construct active subspaces us-ing compressed model gradients. • We demonstrate the scalability of CAS on neural networks of increasing size and evaluate its predic-tive uncertainty estimates using active subspace-based Bayesian inference. ©This work has been submitted to the IEEE for possible publication. Copyright may be transferred without notice. arXiv:2609.19539v1 [cs.LG] 17 Sep 2026 Related Work. Bayesian inference over low-dimensional subspaces provides an efficient alternative to inference over the entire neural network parameter space. Previous ap-proaches construct low-dimensional parameter subspaces us-ing SGD trajectories or learned regions of the neural network loss landscape [4, 5, 6]. More recently, subspace inference has been combined with parameter-efficient representations to enable Bayesian inference in large language models [7]. Active subspaces originate from the computer model lit-erature, where gradient information is used to identify di-rections in input space that capture the most variability in a model output [8, 9]. Recent works extend this approach to neural network parameter spaces for uncertainty quantifica-tion in deep learning and generative models [1, 2, 3]. Other approaches improve the scalability of Bayesian neural net-works through sparse subnetwork selection [10, 11, 12]. Randomized projections have been used for efficient com-putation in numerical linear algebra and machine learning. In randomized linear algebra, they form the basis of methods for low-rank approximations, randomized SVD, and precondi-tioning [13, 14, 15]. Randomized compression has also been used in distributed machine learning to reduce the commu-nication and storage costs associated with high-dimensional model gradients [16, 17, 18, 19]. Low-rank parameterizations are widely used for parameter-efficient fine-tuning of large neural networks. Low-rank adaptation (LoRA) [20] restricts model updates to low-rank parameterizations while recent methods further reduce the trainable parameters through structured projections and pa-rameter sharing [21]. Bayesian inference and uncertainty quantification have also been studied within LoRA parameter spaces [22, 23, 24]. The projection used in CAS is closely related to the isometric construction proposed in Uni-LORA [21] and to CountSketch matrices [25, 26]. In contrast to the Uni-LoRA construction, we include random signs in the nonzero entries and use the resulting embedding to enable 

compressed active subspace construction. 

2. PRELIMINARIES Bayesian subspace inference. Let D = {(xi, y i)}ni=1 denote the training data and let θ0 ∈ RN denote the pretrained neural network model parameters. Bayesian inference over the full model requires inferring the posterior distribution p(θ|D ),which becomes expensive as N grows. Subspace inference instead restricts parameter variation to a K-dimensional lin-ear subspace around θ0 [1, 4]: 

θ = θ0 + V z , z ∈ RK , (3) where V ∈ RN ×K contains the basis vectors of the subspace. Bayesian inference is then performed over the reduced param-eters z instead of the full model parameters θ. One can use a Gaussian prior p(z) = N (μz , ˜σ2IK ) and obtain posterior predictive distributions via Bayesian model averaging using samples from p(z|D ). Posterior inference over z can be per-

Algorithm 1 Standard Active Subspace Construction  

> 1:

input Model f , weights θ0, # gradient samples M , AS dimension K, perturbation variance σ20 

> 2:

for m = 1 , 2, . . . , M do  

> 3:

Sample xm ∼ p 

> 4:

Sample θm ∼ N (θ0, σ 20 I) 

> 5:

Compute gradient gm ← ∇ θ fθm (xm) 

> 6:

end for  

> 7:

Let G = [ g1, . . . , gM ] ∈ RN ×M 

> 8:

Compute SVD G = U ΣW T 

> 9:

return top K left singular vectors U K = [ u1, . . . , uK ]

formed using standard Markov chain Monte Carlo (MCMC) sampling or variational inference methods as in [1]. 

Active subspace inference. For active subspace inference, the columns of V in (3) are the leading left singular vectors of the sampled gradient matrix G in (2). These directions cor-respond to the dominant eigenvectors of the gradient covari-ance matrix C in (1). Algorithm 1 summarizes the standard active subspace construction. CAS modifies this construction by first mapping the model parameters to a compressed pa-rameter space before computing the active directions. 

3. COMPRESSED ACTIVE SUBSPACES 

In compressed active subspaces, we introduce a fixed isomet-ric embedding P ∈ RN ×R, where R ≪ N . The reduced parameters ϕ ∈ RR are mapped to the full model parameter space as θ = θ0 + P ϕ .

We sample P randomly using the structured construction de-scribed below and keep it fixed throughout active subspace construction. We then perform active subspace construction on the reduced model fθ0+P ϕ (x). This reduces the dimen-sion of each sampled model gradient from N to R.Our choice of P is guided by computational efficiency and storage cost. A general N × R matrix requires O(N R )

storage and would introduce another large matrix for mod-els with high-dimensional parameter spaces. We instead use a normalized CountSketch-like construction [25, 26]. Each model parameter is assigned independently and uniformly to one of R classes. Let Mi ∈ { 1, . . . , R } denote the class as-signed to parameter i and let nj denote the number of param-eters assigned to class j. We additionally sample an indepen-dent random sign si ∈ {− 1, +1 } for each parameter. The entries of P are then 

Pi,j = 1j=Mi

si

√nj

. (4) Since different columns have disjoint support and each col-umn is normalized, the resulting embedding satisfies P T P =

IR. Thus, P preserves the Euclidean geometry of the reduced parameter space when embedded in the full parameter space. 2Algorithm 2 Compressed Active Subspace Construction  

> 1:

input Model f , weights θ0, intermediate dimension R,AS dimension K, # gradient samples M , variance σ20 

> 2:

Sample embedding P ∈ RN ×R according to (4)  

> 3:

Form reduced model bfϕ = fθ0+P ϕ . 

> 4:

U K ← StandardActiveSubspace( bf , 0, M, K, σ 20 ) 

> 5:

return V = P U K

The construction in (4) is closely related to the isomet-ric projection used in Uni-LoRA [21]. The main difference is the random sign si applied to each nonzero entry, as in CountSketch matrices. Since each row of P contains only one nonzero entry, the embedding can be represented using the N class assignments and random signs rather than storing a dense N × R matrix. Likewise, compression and decom-pression using multiplication by P or P T can also be per-formed in O(N ) operations. For CAS, we sample reduced perturbations as ϕm ∼N (0, σ 20 IR) and evaluate the model at θm = θ0 + P ϕ m.The corresponding gradient is 

qm = ∇ϕfθ0+P ϕ m (xm) = P T gm ∈ RR. (5) We therefore construct the compressed gradient matrix 

eG = [ q1, . . . , qM ] ∈ RR×M ,

instead of storing the full gradient matrix G ∈ RN ×M . The leading K left singular vectors of eG define the active direc-tions in the compressed parameter space. These directions can then be mapped back to the original model parameter space through P for Bayesian subspace inference. Let CR denote the gradient covariance matrix in the com-pressed parameter space corresponding to the embedding P .From (5), 

CR = E qq T  = P T CP , (6) Therefore, CAS identifies the dominant active directions within the R-dimensional subspace spanned by P . If U K ∈

RR×K contains the leading eigenvectors of CR, the corre-sponding directions in the original parameter space are V =

P U K . These directions are orthonormal since P T P = IR.

4. EXPERIMENTS 

Our experiments demonstrate the scalability of CAS on neural networks of increasing size across several datasets and evalu-ate its benefit for predictive uncertainty estimation using ac-tive subspace-based Bayesian inference. Except for Gaussian Process Regression, all models are neural networks with a sin-gle hidden layer, where the hidden layer size is varied to test scalability. We use Pyro for Bayesian inference [27] with the No-U-Turn-Sampler (NUTS) throughout. 

4.1. Univariate regression 

We first demonstrate CAS on the univariate regression task in Fig. 1. Data is generated according to y = x+0 .3 sin(2 πx )+ −0.5 0.0 0.5 1.0 1.5             

> X
> 02
> Y−0.5 0.0 0.5 1.0 1.5
> X
> 02
> Y−0.5 0.0 0.5 1.0 1.5
> X
> 02
> Y−0.5 0.0 0.5 1.0 1.5
> X
> 02
> YTrue function Observations Posterior samples Predictive mean

Fig. 1 . Demonstration of CAS on a univariate regression task. Top left: Bayesian inference using standard AS [1] with K =20 . Top right: CAS with an intermediate dimension R = 250 .Bottom left: CAS on a larger model with N ≈ 4.5 × 10 7 and 

R = 10 4. Bottom right: Mean prediction and a ±2 standard deviation band inferred by Gaussian Process Regression. 

0.3 sin(4 πx ) + ϵ, where ϵ ∼ N (0 , σ 2 

> ϵ

) and σϵ = 0 .1. We sample 100 values of x in [−0.2, 1] with observations omit-ted in (0 .2, 0.6) and use M = 500 gradient samples for sub-space construction. We find that CAS closely matches the un-certainty estimates produced by standard AS when both are applied to the same model. In particular, both methods cap-ture increased uncertainty in the region without observations. CAS also scales to the larger model with N ≈ 4.5 × 10 7

parameters and R = 10 4, while continuing to capture mean-ingful predictive uncertainty. 

4.2. UCI regression tasks 

We evaluate CAS on a suite of UCI regression datasets and compare it with standard AS and a random subspace baseline. For the latter, P is used to compress the parameter space and a K-dimensional subspace is sampled from the Haar mea-sure on orthogonal matrices. We denote this method Sketch + Haar. We use K = 16 for all methods. The hidden layer of the neural network model contains 1000 units, resulting in model sizes from approximately N = 7000 to N = 15 ,000 de-pending on the input dimension. For a given input x, we use a Gaussian observation model, and predictive means μθ (x)

and variances σ2 

> θ

(x) are estimated from MCMC samples. For CAS and Sketch + Haar, we use R = 256 .Table 1 reports test root-mean square error (RMSE) and negative log-likelihood (NLL). CAS closely matches standard AS across all datasets despite operating in the compressed pa-rameter space. Compared with Sketch + Haar, CAS provides similar or improved predictive performance, with improve-ments on Boston , Energy , and Yacht datasets. Table 2 shows that CAS also maintains empirical coverage comparable to standard AS and generally close to the nominal 95% level. These results indicate that active subspace construction iden-3Table 1 . Test RMSE / NLL across UCI datasets. Lower is better. Dataset Sketch + Haar Standard AS CAS Airfoil 4.94 ±0.38 / 3.02 ±0.07 4.96 ±0.39 / 3.02 ±0.08 4.93 ±0.39 / 3.02 ±0.08 

Boston 4.66 ±0.88 / 3.00 ±0.13 4.50 ±0.65 / 2.97 ±0.09 4.44 ±0.77 / 2.96 ±0.10 

Concrete 9.90 ±0.63 / 3.72 ±0.05 9.97 ±0.79 / 3.72 ±0.07 9.89 ±0.72 / 3.72 ±0.06 

Energy 2.99 ±0.29 / 2.53 ±0.08 2.88 ±0.23 / 2.50 ±0.06 2.85 ±0.25 / 2.49 ±0.06 

Yacht 9.21 ±2.53 / 3.75 ±0.16 8.53 ±1.67 / 3.70 ±0.08 8.43 ±1.85 / 3.70 ±0.10 

Table 2 . Empirical coverage of nominal 95% predictive in-tervals on UCI datasets. Dataset Sketch + Haar Standard AS CAS Airfoil 0.95 ±0.02 0.95 ±0.02 0.95 ±0.02 

Boston 0.97 ±0.02 0.98 ±0.02 0.98 ±0.02 

Concrete 0.97 ±0.02 0.96 ±0.02 0.96 ±0.02 

Energy 0.91 ±0.04 0.95 ±0.02 0.94 ±0.03 

Yacht 0.96 ±0.03 0.98 ±0.03 0.98 ±0.03 10 0 10 1 10 2 10 3       

> Compression ratio ( N/R)
> 2.4 2.5 2.6 2.7 2.8
> NLL 124816 32
> AS dimension ( K)
> 468
> NLL CAS Standard AS Sketch + Haar

Fig. 2 . Left: NLL as the compression ratio varies from 1.2

(almost no compression) to 1250 with K = 8 fixed. Right: NLL as K varies from 1 to 32 with R = 256 fixed. tifies informative directions within the compressed parameter space compared with selecting a random subspace. Entries in both tables report mean ± standard deviation across five random seeds and data splits. 

4.3. Scaling behavior 

Here, we study the sensitivity of CAS performance to the intermediate dimension R and active subspace dimension 

K. As a representative dataset, we use the UCI Energy 

dataset with a model containing approximately 10 4 parame-ters. Fig. 2 shows that CAS remains competitive with stan-dard AS over a wide range of compression ratios. We also observe that increasing K with R fixed improves the NLL before performance stabilizes at larger active subspace di-mensions. For comparison, at significant compression ratios, the random subspaces used in the Sketch + Haar approach are unable to achieve a competitive NLL. 

4.4. Resource utilization 

CAS constructs the active subspace in a reduced space of dimension R instead of the original model parameter space. 10 1 10 2

> Model size (millions of parameters)
> 10 −1
> 10 0
> 10 1
> Peak GPU memory usage (GB)
> Vanilla gradient descent Standard AS Dense Gaussian + AS CAS GPU capacity (42 GB) OOM

Fig. 3 . Memory usage across model sizes. An × indicates an out-of-memory (OOM) error. We use K = 16 , M = 128 , and 

R = 256 for compressed methods. Baseline GPU memory for a single model gradient is also shown. This allows active subspace construction for significantly larger models, as shown in Fig. 3. Standard AS encounters memory limitations for models above ≈ 3 × 10 7 parameters, while CAS scales to 5 × 10 8 parameters, more than an order of magnitude larger. The memory-efficient structure of P is crucial for this scaling. Using a dense Gaussian embedding results in memory exhaustion at ≈ 3 × 10 7 parameters. 

5. CONCLUSION AND DISCUSSION 

We propose Compressed Active Subspaces (CAS), a scal-able approach for predictive uncertainty estimation in high-dimensional models. CAS uses a structured random embed-ding based on CountSketch matrices to construct the active subspace in a lower-dimensional parameter space. Experi-mentally, CAS provides predictive performance and uncer-tainty estimates comparable to standard AS while substan-tially reducing the dimension required for AS construction. In our experiments, this reduction enables active subspace con-struction for models with up to 5 × 10 8 parameters, where standard AS becomes computationally prohibitive. 

# Acknowledgment 

This work was supported by the U.S. Department of Energy, Office of Science, Advanced Scientific Computing Research, under Contracts DE-SC0012704 and DE-AC02-06CH11357. This research used National Energy Research Scientific Com-puting Center (NERSC) resources under NERSC awards ASCR-ERCAP 35972 and ASCR-ERCAP 34765. 46. REFERENCES 

[1] S. Jantre, N. M. Urban, X. Qian, and B. J. Yoon, “Learn-ing active subspaces for effective and scalable uncer-tainty quantification in deep neural networks,” in IEEE ICASSP , 2024. [2] A. N. M. N. Abeer, S. Jantre, N. M . Urban, and B. J. Yoon, “Leveraging active subspaces to capture epis-temic model uncertainty in deep generative models for molecular design,” in IEEE MLSP , 2024. [3] A. N. M. N. Abeer, S. Jantre, N. M. Urban, and B. J. Yoon, “Enhancing generative molecular design via uncertainty-guided fine-tuning of variational autoen-coders,” Molecular Systems Design & Engineering , vol. 11, no. 2, pp. 154–166, 2026. [4] P. Izmailov, W. J. Maddox, P. Kirichenko, T. Garipov, D. Vetrov, and A. G. Wilson, “Subspace inference for bayesian deep learning,” in UAI , 2020. [5] M. Wortsman, M. C. Horton, C. Guestrin, A. Farhadi, and M. Rastegari, “Learning neural network subspaces,” in ICML , 2021. [6] D. Dold, D. R¨ ugamer, B. Sick, and O. D¨ urr, “Bayesian semi-structured subspace inference,” in AISTATS , 2024. [7] C. Samplawski, A. D. Cobb, M. Acharya, R. Kaur, and S. Jha, “Scalable bayesian low-rank adaptation of large language models via stochastic variational subspace in-ference,” arXiv:2506.21408 , 2025. [8] P. G. Constantine, E. Dow, and Q. Wang, “Active sub-space methods in theory and practice: applications to kriging surfaces,” SIAM Journal on Scientific Comput-ing , vol. 36, no. 4, pp. A1500–A1524, 2014. [9] P. G. Constantine, Active subspaces: Emerging ideas for dimension reduction in parameter studies , SIAM, 2015. [10] S. Jantre, S. Bhattacharya, and T. Maiti, “Layer adap-tive node selection in bayesian neural networks: Statisti-cal guarantees and implementation details,” Neural Net-works , vol. 167, pp. 309–330, 2023. [11] S. Jantre, S. Bhattacharya, and T. Maiti, “Spike-and-slab shrinkage priors for structurally sparse bayesian neural networks,” IEEE TNNLS , vol. 36, no. 6, pp. 11176– 11188, 2024. [12] J. Li, Z. Miao, Q. Qiu, and R. Zhang, “Training bayesian neural networks with sparse subspace variational infer-ence,” in ICLR , 2024. [13] P. G. Martinsson and J. A. Tropp, “Randomized numer-ical linear algebra: Foundations and algorithms,” Acta Numerica , vol. 29, pp. 403–572, 2020. [14] J. X. T. Chen and M. Lopes, “Estimating the Error of Randomized Newton Methods: A Bootstrap Approach,” in ICML , 2020. [15] M. Pilanci and M. J. Wainwright, “Newton Sketch: A Near Linear-Time Optimization Algorithm with Linear-Quadratic Convergence,” SIAM J. Optim. , 2017. [16] T. Flynn, P. Johnstone, and S. Yoo, “Problem-dependent convergence bounds for randomized linear gradient compression,” in Allerton Conference , 2025. [17] T. Vogels, S. P. Karimireddy, and M. Jaggi, “Powersgd: Practical low-rank gradient compression for distributed optimization,” in NeurIPS , 2019. [18] M. Safaryan, F. Hanzely, and P. Richtarik, “Smoothness matrices beat smoothness constants: Better communi-cation compression techniques for distributed optimiza-tion,” NeurIPS , 2021. [19] N. Ivkin, D. Rothchild, E. Ullah, V. Braverman, I. Sto-ica, and R. Arora, “Communication-efficient distributed sgd with sketching,” in NeurIPS , 2019. [20] E. J. Hu, Y. Shen, P. Wallis, Z. Allen-Zhu, Y. Li, S. Wang, L. Wang, and W. Chen, “LoRA: Low-rank adaptation of large language models,” in ICLR , 2022. [21] K. Li, S. Han, Q. Su, W. Li, Z. Cai, and S. Ji, “Uni-loRA: One vector is all you need,” in NeurIPS , 2025. [22] A. Yang, M. Robeyns, X. Wang, and L. Aitchison, “Bayesian low-rank adaptation for large language mod-els,” in ICLR , 2024. [23] A. H. Rahmati, S. Jantre, W. Zhang, Y. Wang, B. J. Yoon, N. M. Urban, and X. Qian, “C-LoRA: Contextual low-rank adaptation for uncertainty estimation in large language models,” in NeurIPS , 2025. [24] S. Jantre, T. Wang, G. Park, K. Chopra, N. Jeon, X. Qian, N. M. Urban, and B. J. Yoon, “Uncertainty-aware adaptation of large language models for protein-protein interaction analysis,” in IEEE EMBC , 2025. [25] M. Charikar, K. Chen, and Martin F. C., “Finding fre-quent items in data streams,” Theoretical Computer Sci-ence , vol. 312, no. 1, pp. 3–15, 2004. [26] K. L. Clarkson and D. P. Woodruff, “Low-rank approx-imation and regression in input sparsity time,” Journal of the ACM , vol. 63, no. 6, pp. 1–45, 2017. [27] E. Bingham, J. P. Chen, et al., “Pyro: deep universal probabilistic programming,” JMLR , vol. 20, no. 1, 2019. 5