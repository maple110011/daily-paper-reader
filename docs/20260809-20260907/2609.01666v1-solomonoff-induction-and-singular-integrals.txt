Title: Solomonoff Induction and Singular Integrals

URL Source: https://arxiv.org/pdf/2609.01666v1

Published Time: Thu, 03 Sep 2026 00:02:28 GMT

Number of Pages: 19

Markdown Content:
# Solomonoff Induction and Singular Integrals 

## Will Troiani ∗ Daniel Murfet †

## September 3, 2026 

Abstract 

The Solomonoff distribution M assigns an a priori probability to a finite binary string z by summing over all programs whose output begins with z, a program of length ℓ weighted by 2−ℓ: the likely strings are those with many short explanations. This is apparently a purely discrete notion of complexity. However, Riemann sums are also countable. We show that the sum defining M contains, suitably reorganised, Riemann sums approximating the Bayesian evidence of any computable statistical model — singular integrals whose asymptotics are governed by Singular Learning Theory and, through it, by invariants of algebraic geometry. Concretely, for every computable Bayesian model we construct a single monotone Turing machine whose induced semimeasure agrees with the evidence Zn up to a uniform multiplicative constant. For a computable Bayesian model satisfying in addition the hypotheses of Watanabe’s free energy asymptotics, it follows that data Xn = X1 · · · Xn drawn i.i.d. from the true distribution satisfies 

− log M (Xn) ≤ nL n(w0) + λ log n − (m − 1) log log n + OP(1) ,

where Ln is the empirical loss, w0 an optimal parameter, λ the learning coefficient, and m its multiplicity. The learning coefficient, a geometric measure of model simplicity, thereby appears within the Solomonoff distribution as the coefficient of 

log n in an upper bound on code length. 

# 1 Introduction 

An approach to universal Artificial General Intelligence (AGI) has been defined in [6, 7] using a formalisation of an agent performing optimally across all computable environments. A key ingredient towards this definition is the Solomonoff prior , which is the mixture, defined with respect to a fixed universal monotone Turing machine U:

ξU := X

> ν

2−K (ν) · ν

> ∗

Resolution. will@resolution.org 

> †

Resolution. murfet@resolution.org 

1arXiv:2609.01666v1 [cs.IT] 1 Sep 2026 where the sum is over all lower-semicomputable semimeasures ν, and K is the Kolmogorov complexity with respect to U.1 Once a method of evaluating the performance of agents has been constructed, the universally intelligent agent AIXI (sometimes spelt AI ξ) is then defined as a maximiser of this valuation; see [7, Chapter 7] for details. A key property of AIXI is that it uses simple reasoning, as motivated by philosophy of science concepts such as Occam’s razor 2 and Epicurus’ principle 3, and formalised by the minimisation of K.It is therefore common to swap out the Solomonoff prior for the Solomonoff distribution 

M , which agrees with ξU up to a multiplicative constant in the case of sequence prediction, and is defined as follows for a finite binary string z:

M (z) = X  

> p:U(p)= z∗

2−ℓ(p), (1) summing over minimal programs p; here U(p) = z∗ means that in the run of U on any input tape extending p, the input head has consumed exactly p at the step at which the last bit of z is written (Definition A.3). That (1) is a reasonable notion of a priori probability is exemplified by the correspondence between machines and semimeasures: every monotone Turing machine T induces a lower-semicomputable semimeasure PT (Definition A.4), where 

PT (z) is the probability that the output of T begins with z when its input tape is filled with uniformly random bits, and conversely every lower-semicomputable semimeasure arises this way up to a bounded multiplicative factor [6, 7]. Under this correspondence, M

dominates each PT with constant 2−ℓ(⟨T ⟩) (Theorem A.7). The minimal programs p in (1) are binary strings which may be of the form ⟨T ⟩p′ where 

⟨T ⟩ is some encoding of a Turing machine T and p′ is the rest of the string, which may be passed as input to T or as extra information. Amongst such strings are two conceptually distinct categories: deterministic programs , specified by the portion of the string ⟨T ⟩ and some input to T contained in p′; and samplers , which read their code from ⟨T ⟩ and then continue reading p′ as random bits used to sample from a predictive distribution. A preference for simplicity has also been considered in a different context, that of 

Bayesian learning . Fix a model class {p(· | w) | w ∈ W }, a family of probability distributions indexed by a parameter w ranging over a space W , together with a prior distribution ϕ on W with density φ. Given data Dn = ( z1, . . . , z n) consisting of n i.i.d. samples, Bayes’ rule updates the prior to the posterior distribution 

p(w | Dn) ∝ φ(w)

> n

Y

> i=1

p(zi | w),

and by Bayesian learning we mean the evolution of this posterior as n increases. Amongst        

> 1Our monotone Turing machines have tape alphabet B={0,1}and no blank symbols, so the contents of the input tape may be read as random bits, to be used for random sampling (Definition A.3). In the context of reinforcement learning, this sum is instead over lower-semicomputable chronological semimeasures, but we will not consider these here.
> 2Among the hypotheses consistent with the observations, prefer the simplest [7, Section 16.1].
> 3Discard no hypothesis consistent with the observations; in the prior, every such hypothesis retains positive weight [7, Section 16.1].

2the samplers appearing in (1) are those implementing Bayesian learning for a fixed computable model class: draw w ∼ ϕ, then sample from p(· | w).When the model class is singular , Singular Learning Theory (SLT) describes the large-n

behaviour of the Bayesian evidence 

Zn := 

Z

> W

φ(w)

> n

Y

> i=1

p(zi | w) dw, 

the normalising constant of the posterior above. Watanabe’s theorems [14, 15] give the asymptotic expansion of the associated free energy (see Appendix B): 

Fn := − log Zn = nL n(w0) + λ log n − (m − 1) log log n + OP(1) ,

where Ln(w) := − 1

> n

Pni=1 log p(zi | w) is the empirical loss , w0 is an optimal parameter, 

λ > 0 is the learning coefficient , and m is its multiplicity. A localisation of λ near a chosen parameter, the Local Learning Coefficient (LLC), has been adopted as a measure of model simplicity [8, 5]: in the regular case the learning coefficient equals half the number of parameters, and so reads as an effective parameter count. This leaves us with two notions of simplicity: minimum description length on the one hand, and singular model simplicity as measured by the learning coefficient on the other. The goal of the present paper is to see how these two notions of simplicity relate to one another. At first sight they belong to different worlds: the sum (1) is indexed by a countable set of discrete objects, while the evidence Zn is an integral over a continuous parameter space, whose asymptotics are controlled by the geometry of the zero locus of the Kullback–Leibler divergence and ultimately by resolution of singularities. The bridge is the observation that Riemann sums are also countable. We show that the sum defining 

M can be reorganised so that within it appear Riemann sums for the evidence integrals of computable Bayesian models. It is no great surprise that Bayesian learning can be approximated by computable procedures, and so ought to contribute terms to the sum (1). There is a challenge, though: to arrive at a meaningful relationship between M (z) on the one hand, with z = z1 · · · zn

and n free to grow, and the learning coefficient on the other, the contribution must come from a single fixed machine serving every n. This is possible, and constitutes the main technical work of the paper. The construction has two stages. First, we define a computable procedure 

BayesSampler (m, ω ) (Definition 2.9), where m is a finite effective description of the statistical model, comprising an integer b > 0, a model class {p(· | w) | w ∈ W }

of probability distributions on the set Bb of bit-strings of length b, and a prior ϕ on 

W , and ω ∈ BN is an infinite string of uniformly random bits. Second, fixing m and hard-wiring it into the procedure yields an ordinary monotone Turing machine Bm with 

Bm(ω) = BayesSampler (m, ω ), whose only input is ω. This one machine is fixed by m

alone, receives neither the sample size nor the data, runs forever, and writes a single infinite string; its induced semimeasure reproduces the Bayesian evidence Zn of the model on every prefix, up to a uniform constant (Theorem 2.15). The relationship is that of a 3Riemann sum to its integral: for every string z = z1 · · · zn consisting of n strings zi ∈ Bb,

PBm (z) = X

> αn

Y

> t=1

p zt | wt(α) ϕ Dj(n),α 

,Zn =

Z

> Wn

Y

> t=1

p zt | w dϕ (w),

where the cells Dj(n),α partition the parameter space W , up to a null set of boundaries, at a resolution growing with n, and wt(α) is the centre of the cube of the current cell at time step t (Lemma 2.14). The estimate relating the two is 

e−π2/6 Zn ≤ PBm (z) ≤ eπ2/6 Zn,

so the induced semimeasure of Bm has exactly the free energy asymptotics of the model (Corollary 2.16). Since Bm is one monotone Turing machine among those contributing to (1), dominance yields the main result (Theorem 2.17): 

− log M (z) ≤ − log Zn + O(1) ,

and when the strings X1, . . . , X n are drawn i.i.d. from a true distribution satisfying the hypotheses of Watanabe’s theorem (Appendix B), writing Xn := X1 · · · Xn,

− log M (Xn) ≤ nL n(w0) + λ log n − (m − 1) log log n + OP(1) .

The two notions of simplicity are thereby related within a single bound: the additive constant, which depends on the model description but on neither n nor the data, pays for the description of the model, while the coefficient of log n is its learning coefficient. Background on the Solomonoff distribution, following [7], is recalled in Appendix A, and the necessary singular learning theory in Appendix B. Related work is discussed in Remark 2.20. 

# 2 An explicit sampler: description length and the learn-ing coefficient 

Fix an observed string z = z1 · · · zn ∈ (Bb)n, with empirical loss Ln and evidence Zn

as in Definition B.13. The string is fixed only for the probability analysis: the sampler constructed below is built from the model description alone and inspects neither z nor n.

## 2.1 Computability assumptions 

The following definition collects the effective assumptions under which this section con-structs the sampler BayesSampler and its specialisations Bm. Fix a binary encoding of the finite objects appearing below: natural numbers, indices (j, α ), rational numbers, strings, and tuples of these. Programs and subroutines receive and return such encodings, and we do not distinguish a finite object from its encoding. 4Definition 2.1. A computable Bayesian model is given by the data: • a compact set of parameters W ⊆ Rd, for some d ∈ Z>0, contained in a rational box 

R,• a statistical model {p(· | w) | w ∈ R} on Bb, the set of bit-strings of length b, for some integer b > 0,• a prior probability measure ϕ on W with density φ,required to satisfy the conditions: 1. there exists a computable L0 > 0 such that for all w, w ′ ∈ R and all s ∈ Bb,

p(s | w) − p(s | w′) ≤ L0∥w − w′∥;

2. there exists a computable η ∈ (0 , 1] such that 

η ≤ p(s | w) ∀ w ∈ R, ∀ s ∈ Bb;

3. the map (r, α ) 7 → ϕ(Dr,α ) is computable; 4. the map (w, s ) 7 → p(s | w) is uniformly computable at rational points: there is a program which, on input a rational w ∈ R, a string s ∈ Bb and l ∈ N, returns a rational bp with |bp − p(s | w)| ≤ 2−l.

Remark 2.2. For (1) it suffices that each map w 7 → p(s | w) is C1 on R (uniformly in 

s ∈ Bb) with a computable uniform bound on ∥∇ wp(s | w)∥.

Definition 2.3. A model description of a computable Bayesian model is a tuple 

m :=  b, T b, ϕ (D·,·), bG, R ,

where: • b is the string length; • Tb is a program witnessing condition (4): on input (w, s, l ), with w ∈ R rational, it returns a rational bp with |bp − p(s | w)| ≤ 2−l;• ϕ(D·,·) is a program computing the function (r, α ) 7 → ϕ(Dr,α ) of condition (3); • bG is a rational number with bG ≥ (L0/η )√d, where L0 and η witness conditions (1) and (2); • R is the list of rational endpoints of the box of Definition 2.1. 5Definition 2.4. For j ∈ N and α ∈ { 0, . . . , 2j − 1}d set 

Cj,α := 

> d

Y

> i=1

[αi2−j , (αi + 1)2 −j ], Dj,α := W ∩ Cj,α .

A cell is a set Dj,α ; it is positive if ϕ(Dj,α ) > 0.

Remark 2.5. Write R = Qdi=1 [ai, a i + si] for the rational box of Definition 2.1 and let 

A : R → [0 , 1] d be the affine map A(w)i = ( wi − ai)/s i, which is computable from the assumptions on R. Transporting the model along A gives the model 

p′(s | w′) := p(s | A−1w′), A−1(w′)i = ai + siw′

> i

,

on the parameter set A(W ) ⊆ [0 , 1] d with prior the pushforward A∗ϕ. Condition (1) holds for p′ with the computable constant L0 · max i si, since 

p′(s | w′

> 1

) − p′(s | w′

> 2

) ≤ L0∥A−1w′ 

> 1

− A−1w′

> 2

∥≤ L0 · max  

> i

si · ∥ w′ 

> 1

− w′

> 2

∥,

and b and conditions (2) and (4) are unchanged. The rational constant bG of Definition 2.3 is replaced by bG · max i si. The cells of the transported model are those of Definition 2.4 for A(W ), namely D′ 

> j,α

= A(W ) ∩ Cj,α , and the map of condition (3) is 

(j, α ) 7 −→ A∗ϕ(D′

> j,α

) = ϕ W ∩ A−1(Cj,α ).

In light of Remark 2.5, we assume from now on that R = [0 , 1] d, so that C0,0 = R,

W ⊆ [0 , 1] d, and D0,0 = W .

Lemma 2.6. Assume conditions (1) and (2) of Definition 2.1. Then for each s ∈ Bb the map w 7 → − log p(s | w) is G-Lipschitz on R, where 

G := L0

η ,

and consequently, for every string z ∈ (Bb)n, the empirical loss Ln of Definition B.13 is 

G-Lipschitz on R.Proof. On [η, 1] the map u 7 → − log u is (1 /η )-Lipschitz. Thus for any s ∈ Bb and any 

w, w ′ ∈ R,

log p(s | w) − log p(s | w′) ≤ 1

η p(s | w) − p(s | w′) ≤ L0

η ∥w − w′∥.

Averaging over the n strings gives the same constant for Ln.

Definition 2.7. Define 

j(t) := min { j ∈ N | bG 2−j ≤ t−2 } for t ≥ 1.

6Definition 2.9 specifies a computable procedure BayesSampler (m, ω ), which receives the model description m deterministically together with an infinite string of bits ω ∈ BN. It calls two subroutines, which we assume exist with the properties of the following definition. 

Definition 2.8. We assume given two subroutines, each consuming input bits sequentially, the decision to halt depending only on the bits consumed so far: • SampleChild( D): given a positive cell D of a dyadic partition of W , replaces D by one of its children D′, chosen with conditional prior mass ϕ(D′)/ϕ (D); it terminates almost surely, and the returned child is almost surely positive. • StringSample( w): given a dyadic point w ∈ R, consumes bits to sample one string from p(· | w); it terminates almost surely. The random choices in SampleChild and StringSample can be implemented by the interval algorithm of Han and Hoshi [3] for sampling a finite distribution from uniformly random bits, using the programs ϕ(D·,·) and Tb of the model description. 

Definition 2.9. BayesSampler is the procedure which, on input a model description 

m = ( b, T b, ϕ (D·,·), bG, R ) together with an infinite string of bits ω ∈ BN, writes an infinite string (concatenated b-bit strings) on the output tape as follows:  

> 1:

D ← D0,0 = W ; j ← 0. 

> 2:

loop over t = 1 , 2, 3, . . .  

> 3:

while j < j (t) do  

> 4:

D ← SampleChild( D); j ← j + 1 . 

> 5:

end while  

> 6:

wt ← the centre of Cj,α , where D = Dj,α . 

> 7:

StringSample( wt). 

> 8:

end loop Example 2.10. Take b = 1 , d = 2 , W = R = [0 , 1] 2, ϕ the uniform prior, and 

p(1 | w) := 4 + w1 + w2

16 , p(0 | w) := 1 − p(1 | w).

Then p(1 | w) ∈ [14 , 38 ] and p(0 | w) ∈ [58 , 34 ], so η = 14 witnesses condition (2), and 

|p(s | w) − p(s | w′)| = 116 (w1 − w′

> 1

) + ( w2 − w′

> 2

) ≤ √216 ∥w − w′∥,

so L0 = √216 witnesses condition (1). The cell masses are ϕ(Dj,α ) = 4 −j , so conditions (3) and (4) hold. Then G = L0/η = √24 , so G√d = 12 ; we take bG = 12 , and 

j(t) = min { j ∈ N | 2j+1 ≥ t2 }, j(1) = 0 , j(2) = 1 , j(3) = 3 , j(4) = 3 .

In a run of BayesSampler (m, ω ): at t = 1 no new child cell is sampled, since j = 0 = j(1) ,and z1 is sampled from p(· | w1) with w1 = ( 12 , 12 ), the centre of C0,0; at t = 2 the loop samples one new child cell, a quadrant D1,α 1 chosen with probability 14 , and samples z2

from p(· | w2); at t = 3 it samples two new child cells in succession, a cell D2,α 2 and then a cell D3,α 3 , each chosen with probability 14 , and samples z3 from p(· | w3). Since j(4) = 3 ,at time step t = 4 it samples z4 from the same p(· | w3). Figure 1 shows this run. 7input tape: ⟨ bB⟩ m 1 0 1 1 0 0 · · · 

interpreter model description bits ω

W = R = [0 , 1] 2

D1,α 1 (t = 2 )

D2,α 2

D3,α 3 (t = 3 )

w1

w2

> w3

a new cell is sampled only when j < j (t)    

> 1 new cell 2 new cells
> t=1 t=2 t=3 t=4

output tape: z1 z2 z3 z4 · · · ∼ p(· | w1) ∼ p(· | w2) ∼ p(· | w3)

Figure 1: The run of Example 2.10, with the input tape segments ⟨ bB⟩ and m of Definition 2.12. Each new cell is chosen from its parent with conditional prior mass 

ϕ(Dj,α )/ϕ (Dj−1,α ′ ), here 14 at every step. 8Definition 2.11. Let m be a model description. Write Bm for the monotone Turing machine such that 

Bm(ω) = BayesSampler (m, ω ), ∀ ω ∈ BN.

Definition 2.12. Fix a prefix-free encoding m 7 → m of model descriptions as binary strings, and write bB for the monotone Turing machine such that, for every model description m,

bB(m ω) = Bm(ω), ∀ ω ∈ BN.

The single machine bB serves every model description, reading m from its input tape before the bits ω. The input tape shown in Figure 1 is that of the universal machine U

computing U(⟨ bB⟩ m ω) = bB(m ω) = Bm(ω), where ⟨ bB⟩ is the encoding of Definition A.5. 

Lemma 2.13. Started from D0,0 = W and iterated, SampleChild produces a nested se-quence of positive cells D(0) ⊇ D(1) ⊇ · · · with P(D(j) = Dj,α ) = ϕ(Dj,α ). The intersection T 

> j

D(j) is a single point w∞ with w∞ ∼ ϕ, and for every j the cell D(j) is ϕ-almost surely the unique cell Dj,α containing w∞.Proof. By induction on j, using that SampleChild selects a child D′ of D with probability 

ϕ(D′)/ϕ (D): P(D(j+1) = Dj+1 ,β ) = P(D(j) = Dj,α ) ϕ(Dj+1 ,β )/ϕ (Dj,α ) = ϕ(Dj+1 ,β ) for the parent α of β (telescoping). The cells are nested with diam( D(j)) ≤ √d 2−j → 0, so T 

> j

D(j) = {w∞}; the law of w∞ is the projective limit of the laws of D(j), namely ϕ. The boundaries of the cubes Cj,α are Lebesgue-null, hence ϕ-null since ϕ has a density, so ϕ-a.s. 

w∞ lies, for each j, in the interior of exactly one cube Cj,α ; D(j) is therefore the unique cell Dj,α containing w∞.For n ≥ 1, each positive cell Dj(n),α and each time step t ≤ n, write 

wt(α) := the centre of Cj(t),α ′ , for the ancestor Dj(t),α ′ of Dj(n),α .

Lemma 2.14. Write PBm for the semimeasure induced by Bm (Definition A.4). For every 

n and every z = z1 · · · zn ∈ (Bb)n,

PBm (z) = X

> αn

Y

> t=1

p zt | wt(α) ϕ Dj(n),α 

, (2) 

summing over the positive cells Dj(n),α .Proof. Let A = {ω ∈ BN | Bm(ω) = z∗} ; by Definition A.4, 

PBm (z) = μ(A).

By the assumed properties of the subroutines (Definition 2.8), Bm writes an infinite string almost surely, and membership of A is determined at time step t = n, at which point cells 

Dj(n),α are being sampled. For a positive cell Dj(n),α and 1 ≤ t ≤ n set 

Eα := ω ∈ BN D(j(n)) (ω) = Dj(n),α ,

9At := ω ∈ BN Bm(ω)(t−1) b+1 · · · Bm(ω)tb = zt ,

where D(j)(ω) is the j-th cell produced by the successive calls to SampleChild in the run of Bm on ω, as in Lemma 2.13. The Eα partition the sample space up to a μ-null set and 

μ(Eα) = ϕ(Dj(n),α ) by Lemma 2.13, so 

μ(A) = X

> α

ϕ(Dj(n),α ) μ(A | Eα).

On Eα the current cell at time step t ≤ n is the ancestor Dj(t),α ′ of Dj(n),α , so the t-th call to StringSample samples the t-th string from p(· | wt(α)) ; by the halting assumption of Definition 2.8, it does so on a tail that is i.i.d. uniform conditional on the bits consumed so far. Hence 

μ(A | Eα) = 

> n

Y

> t=1

μ A1 ∩ · · · ∩ At Eα



μ A1 ∩ · · · ∩ At−1 Eα



=

> n

Y

> t=1

μ At Eα ∩ A1 ∩ · · · ∩ At−1



=

> n

Y

> t=1

p(zt | wt(α)) ,

and (2) follows. 

Theorem 2.15. Let m be a model description and Bm the monotone Turing machine of Definition 2.11. For every n and every z = z1 · · · zn ∈ (Bb)n,

e−π2/6 Zn ≤ PBm (z) ≤ eπ2/6 Zn.

Proof. Let Dj(n),α be a positive cell and w ∈ Dj(n),α . For each t ≤ n, both wt(α) and w

lie in the cube Cj(t),α ′ of the ancestor Dj(t),α ′ of Dj(n),α , so ∥wt(α) − w∥ ≤ √d 2−j(t) and, by Lemma 2.6 and Definition 2.7, 

log p(zt | wt(α)) − log p(zt | w) ≤ G√d 2−j(t) ≤ bG 2−j(t) ≤ t−2.

Summing over t ≤ n and using P 

> t≥1

t−2 = π2/6,

e−π2/6

> n

Y

> t=1

p(zt | w) ≤

> n

Y

> t=1

p(zt | wt(α)) ≤ eπ2/6

> n

Y

> t=1

p(zt | w).

Integrating over w ∈ Dj(n),α against ϕ, summing over the positive cells, which partition W

up to a ϕ-null set, and applying Lemma 2.14 together with Zn = P

> α

R

> Dj(n),α

Qnt=1 p(zt |

w) dϕ (w), gives the two bounds. 

Corollary 2.16. If in addition the hypotheses of Theorem B.12 hold and Xn = X1 · · · Xn

is drawn i.i.d. from the true distribution, then 

− log PBm (Xn) = nL n(w0) + λ log n − (m − 1) log log n + OP(1) .

10 Proof. By Theorem 2.15, log PBm (Xn) + log Zn ≤ π2/6; apply Theorem B.12. 

Theorem 2.17. Let (W, {p(· | w)}w∈W , ϕ ) be a computable Bayesian model (Definition 2.1) with model description m, and let Bm be the monotone Turing machine of Definition 2.11. For every observed string z ∈ (Bb)n,

M (z) ≥ 2−ℓ(⟨Bm⟩) e−π2/6 Zn,

and consequently 

− log M (z) ≤ − log Zn + O(1) ,

the O(1) being independent of n and of the data. If in addition the hypotheses of Theo-rem B.12 hold and Xn = X1 · · · Xn is drawn i.i.d. from the true distribution, then 

− log M (Xn) ≤ nL n(w0) + λ log n − (m − 1) log log n + OP(1) .

Proof. By Theorem 2.15, PBm (z) ≥ e−π2/6Zn. The machine Bm of Definition 2.11 is fixed independently of n and of z, so dominance (Theorem A.7 with v = ⟨Bm⟩) gives 

M (z) ≥ 2−ℓ(⟨Bm⟩)PBm (z) ≥ 2−ℓ(⟨Bm⟩)e−π2/6Zn. Taking − log gives the second inequality with O(1) = ℓ(⟨Bm⟩) log 2 + π2/6, which depends on m (in particular on b) but not on n

or the data. Applying Theorem B.12 to − log Zn yields the third. 

Remark 2.18. For regular models the analogue of Theorem 2.17 with the evidence in place of M is considered in [2, Thm. 2.3] and [6, Thm. 3.74]: under suitable hypotheses including the condition that the Fisher information matrix I(w0) is nonsingular at a point 

w0 ∈ W , for X1, . . . , X n drawn i.i.d. from p(· | w0),

E



log 

Qni=1 p(Xi | w0)

Zn



≤ log 1

φ(w0) + d

2 log n

2π + 12 log det I(w0) + o(1) .

The bound concerns the evidence Zn and not M ; to bound − log M one must exhibit a machine whose semimeasure is comparable to Zn. For continuously parameterized classes this step is discussed in [6, Sec. 3.7.2]; Theorem 2.17 proves it, for singular models. 

Remark 2.19. In [11] a two-part code is constructed: a code word JpK describing a distribution p in the model class is transmitted first, followed by the message xn = x1 · · · xn

encoded with respect to p, written JxnKp, the combined length being ℓ(JpK) + Pni=1 log 1 

> p(xi)

.The redundancy of the code is its excess length over the encoding that uses the data distribution itself: for x1, . . . , x n drawn i.i.d. from q,

Rn := ℓ(JpK) + ℓ(JxnKp) −

> n

X

> i=1

log 1

q(xi) = ℓ(JpK) + 

> n

X

> i=1

log q(xi)

p(xi) .

The main theorem of [11] asserts that the code can be chosen so that, for every realizable data generating distribution q in the model class, 

Rn = λ log n − (m − 1) log log n + OP(1) ,

11 The tree of positive cells. 

D0,0

D1,0 D1,1

D2,0 D2,1 D2,2 D2,3

> ϕ(D1,0)
> ϕ(D0,0)
> ϕ(D1,1)
> ϕ(D0,0)
> ϕ(D2,0)
> ϕ(D1,0)
> ϕ(D2,1)
> ϕ(D1,0)
> ϕ(D2,2)
> ϕ(D1,1)
> ϕ(D2,3)
> ϕ(D1,1)

... ... ... ...

The tree of Hennick–Dellago at threshold t.

D0,0

D1,0 D1,1

D2,0 D2,1 D2,2 D2,3

... ...Figure 2: Left: a directed graphical model presentation of the sequences of nested positive cells that may be taken, along with their probabilities, by the algorithm Bm of Definition 2.11, for dimension d = 1 and compact parameter set W = [0 , 1] . Right: the tree construction of [4, Lem. 5.3], which for t > 0 removes the nodes Dj,α (grey) such that 

{w ∈ W | K(w) < t } ∩ Dj,α = ∅, along with their descendants. with λ the learning coefficient of q for the model and m its multiplicity. This two-part code is the core inspiration for the construction of Bm: our machine is a generic algorithm, one for every computable Bayesian model, which, when its input tape is filled with uniformly random bits, produces the output of a code of this format. 

Remark 2.20. We can represent the sequence of positive cells sampled by Bm as a directed graphical model: the nodes are the positive cells, and each edge is labelled by the probability of its target being sampled. Doing this exhibits a similarity, shown in Figure 2, between our construction and that of [4, Lem. 5.3], whose trees have their branches pruned according to the following rule: for a threshold t > 0, the node Dj,α is removed, together with its descendants, if and only if 

{ w ∈ W | K(w) < t } ∩ Dj,α = ∅,

with K as in Definition B.3; the loss of [4] is the KL-divergence bK, which agrees with K in the realizable case (Remark B.11). Writing Tt for the tree of retained cells and ∂Tt for its set of infinite branches, the map sending a parameter to its branch of nested cells pushes the prior forward to a measure ϕ∗ with ϕ∗(∂Tt) = ϕ(K < t ), and the learning coefficient λ

of the model, Definition B.6, is there shown to be the scaling exponent of this measured boundary as t → 0 [4, Lem. 5.6]: 

λ = lim 

> t→0

log ϕ∗(∂Tt)log t .

12 Acknowledgements 

The majority of this work was carried out while the authors were at Timaeus. This project is funded by the Advanced Research + Invention Agency (ARIA). 

# A The Solomonoff distribution 

Following [7] we recall the elements of algorithmic information theory. 

Definition A.1. Let B := {0, 1}. Write B∗ for finite binary strings and BN for infinite binary strings. For x, y ∈ B∗ ∪ BN, write x ⊑ y if x is a prefix of y.

Definition A.2. For x ∈ B∗, define the cylinder set 

Γx := {ω ∈ BN | x ⊑ ω}.

Let μ denote the uniform product measure on BN, characterised by μ(Γ x) = 2 −ℓ(x), where 

ℓ(x) denotes the length of x.

Definition A.3 (cf. [7, Definition 2.7.1]) . A monotone Turing machine is given by the data: • a state set {1, . . . , k } for some k ≥ 1,• one input tape, one output tape, and j ≥ 1 work tapes, • a transition function δ,required to satisfy the conditions: • all tapes are binary, with no blank symbol, and the work tapes are initialised with zeros, • the input tape is read-only and unidirectional (its head can only move from left to right), • the output tape is write-only and unidirectional. For ω ∈ BN we write T (ω) ∈ B∗ ∪ BN for the output written during the run of T on input tape ω. For v, y ∈ B∗ we write T (v) = y∗ if, in the run on any input ω ⊒ v, the input head has consumed exactly v at the step in which the last bit of y is written. 

Definition A.4. Let T be a monotone Turing machine. For y ∈ B∗, the set of minimal programs [7, Definition 2.7.1] 

VT,y := {p ∈ B∗ | T (p) = y∗} 

is prefix-free, and 

{ω ∈ BN | y ⊑ T (ω)} = G

> p∈V T,y

Γp

13 [7, Section 3.8]. Define the semimeasure induced by T on cylinders by 

PT (Γ y) := μ {ω ∈ BN | y ⊑ T (ω)} = X

> p∈V T,y

2−ℓ(p), (3) the probability that the output of T begins with y when the input tape is filled with uniformly random bits. We abbreviate PT (y) := PT (Γ y).

Definition A.5 (cf. [7, Definition 2.7.2]) . Every monotone Turing machine has a canonical prefix-free encoding T 7 → ⟨ T ⟩ as a binary string. A monotone Turing machine U is 

universal if 

U(⟨T ⟩ω) = T (ω) for every monotone Turing machine T and every ω ∈ BN.Universal machines exist [7, Definition 2.7.2]; we fix one, the reference machine U [7, Remark 2.7.7]. 

Definition A.6 (cf. [7, Definition 3.8.2]) . The Solomonoff distribution (or universal a priori probability) is 

M (Γ y) := X  

> p:U(p)= y∗

2−ℓ(p),

summing over the minimal programs VU,y of Definition A.4. By (3), M (Γ y) = PU (Γ y): the probability that the output of U begins with y on a uniformly random input tape. For 

y ∈ B∗ we abbreviate M (y) := M (Γ y), as in (1). 

Theorem A.7. The universal semimeasure M dominates the contribution of every input prefix [6, 7]: for all v, y ∈ B∗,

M (y) ≥ 2−ℓ(v) μ {ω ∈ BN | y ⊑ U (vω )}.

In particular, taking v = ⟨T ⟩ for a monotone Turing machine T gives M (y) ≥

2−ℓ(⟨T ⟩) PT (y).

# B Singular learning theory (SLT) 

The asymptotics of the free energy associated to the Bayesian evidence are (for singular models) given by the theorems of Watanabe [14, 15]. Since then a range of introductions to and developments of the field have appeared [16, 9, 13, 8, 5, 12, 1, 10]. We give a minimal account here. Throughout, log denotes the natural logarithm and log 2 denotes base-2 logarithm. We work in the binary setting used throughout the paper. Fix: • a dimension d ∈ Z>0 and a compact parameter set W ⊆ Rd,• a statistical model p(x|w) for x ∈ B, w ∈ W ,14 • a true distribution q(x) for x ∈ B,• a prior probability measure ϕ on W with density φ (so dϕ (w) = φ(w) dw ), • an integer n ≥ 1,• data X1, . . . , X n

> i.i.d.

∼ q.

Definition B.1. Define the log density ratio :

f (x, w ) := log q(x)

p(x|w), (x ∈ B, w ∈ W ).

We implicitly assume the usual absolute-continuity condition q(x) > 0 ⇒ p(x|w) > 0 for all w ∈ W so that f is well-defined. 

Definition B.2. Define the population loss and empirical loss :

L(w) := −E log p(X|w), Ln(w) := − 1

n

> n

X

> i=1

log p(Xi|w),

where the expectation is taken under q.Let w0 ∈ W be a minimizer of L(w) (one exists since L is continuous on the compact set W under fundamental condition (I) below) and define the minimizer set 

W0 := {w ∈ W | L(w) = L(w0)}.

Definition B.3. For w ∈ W , define the KL-divergence 

bK(w) := X

> x∈B

q(x) log q(x)

p(x|w) = Ef (X, w ).

Define the (centered) KL function 

K(w) := bK(w) − bK(w0) = L(w) − L(w0) ≥ 0,

so that K(w) = 0 if and only if w ∈ W0.

Definition B.4. Define the empirical KL-divergence and its centered version: 

bKn(w) := 1

n

> n

X

> i=1

f (Xi, w ) = 1

n

> n

X

> i=1

log q(Xi)

p(Xi|w),Kn(w) := bKn(w) − bKn(w0) = Ln(w) − Ln(w0),

so that Kn(w0) = 0 .15 Definition B.5. The partition function (Bayesian evidence) is 

Zn := 

Z

> Wn

Y

> i=1

p(Xi|w) φ(w) dw =

Z

> W

exp   − nL n(w) φ(w) dw. 

Equivalently, 

Zn = exp   − nL n(w0) Z

> W

exp   − nK n(w) φ(w) dw. 

Definition B.6. Define the zeta function for ℜ(z) > 0, where ℜ(z) denotes the real part of z, by 

ζ(z) := 

Z

> W

K(w)z φ(w) dw ;

under fundamental conditions (I) and (II) below it admits a meromorphic continuation to 

C. Let −λ be the pole of ζ with largest real part, and let m be its order. Then λ > 0 is the learning coefficient and m its multiplicity .The learning-theoretic asymptotics depend on (i) analyticity of the log density ratio and (ii) mild integrability and regularity assumptions on (W, φ ).

Definition B.7. Fix s ≥ 2. The tuple (q, p, φ ) satisfies fundamental condition (I) with index s if the following hold. 1. There exists an open set W (C) ⊂ Cd such that (a) W ⊂ W (C) ∩ Rd,(b) the map w 7 → f (·, w ) is an Ls(q)-valued complex-analytic function on W (C),(c) the envelope ¯f (x) := sup w∈W (C) |f (x, w )| satisfies ¯f ∈ Ls(q).2. There exists ε1 > 0 such that, defining 

Q(x) := sup { p(x|w) | w ∈ W, K (w) ≤ ε1 },

we have the integrability condition 

X

> x∈B

¯f (x)2 Q(x) q(x) < ∞.

Remark B.8. On the finite space B, item 2 of fundamental condition (I) follows from item 1(c), since Q ≤ 1 and the sum is finite; we retain it as stated to match the general form of [15]. 

Definition B.9. The tuple (W, φ ) satisfies fundamental condition (II) if: 1. W is a compact semianalytic set of the form 

W = {w ∈ Rd | π1(w) ≥ 0, . . . , π J (w) ≥ 0},

where each πj is real-analytic on some open neighborhood W (R) ⊂ Rd containing W .16 2. The prior density factors as φ(w) = φ1(w)φ2(w), where φ1(w) > 0 is C∞ on W (R)

and φ2(w) ≥ 0 is real-analytic on W (R).

Definition B.10. Define 

f0(x, w ) := log p(x|w0)

p(x|w) = f (x, w ) − f (x, w 0), (x ∈ B, w ∈ W ),

so that Ef0(X, w ) = K(w), where X ∼ q. The model has relatively finite variance if there exists c0 > 0 such that 

Ef0(X, w )2 ≤ c0 K(w) for all w ∈ W. 

Remark B.11. For w ∈ W0 the condition forces Ef0(X, w )2 = 0 , so all optimal parameters induce the same distribution q-almost surely; in particular Definition B.10 does not depend on the choice of w0. In the realizable case q(x) = p(x|w0) we have f0 = f

almost surely and K = bK.

Theorem B.12. Assume the standard SLT hypotheses: fundamental condition (I) with some index s ≥ 2, fundamental condition (II), and relatively finite variance (Defini-tion B.10); assume moreover that φ {w ∈ W | 0 < K (w) ≤ ε} > 0 for every ε > 0, so that the pole defining (λ, m ) exists. Let (λ, m ) be the learning coefficient and multiplicity from Definition B.6. Then the normalized free energy satisfies 

F (0)  

> n

:= − log 

Z

> W

exp   − nK n(w) φ(w) dw = λ log n − (m − 1) log log n + OP(1) .

Consequently, 

− log Zn = nL n(w0) + λ log n − (m − 1) log log n + OP(1) .

Fix an integer b ≥ 1. We identify s ∈ Bb with the integer Pbk=1 sk 2 b−k ∈ { 0, 1, . . . , 2b −

1} of which it is the length-b binary representation; thus Bb is a finite outcome alphabet of 

2b symbols, and p(· | w) is a distribution on it. In the body of the paper the observed data is treated as a string z = z1 · · · zn ∈ (Bb)n (i.e. n i.i.d. samples, each encoded in b bits). Whenever M (z) appears, z is understood as the concatenated binary string z1 · · · zn ∈ Bnb .The case b = 1 recovers the binary setting. The development of this section is stated for binary outcomes x ∈ B but uses only finiteness of the outcome alphabet, and holds verbatim over Bb; we invoke Theorem B.12 in this generality. 

Definition B.13. Fix an observed string z = z1 · · · zn ∈ (Bb)n. Define the empirical loss 

Ln(w) := − 1

n log 

 nY

> i=1

p(zi | w)



, e−nL n(w) =

> n

Y

> i=1

p(zi | w),

and the partition function (Bayesian evidence) 

Zn := 

Z

> W

e−nL n(w) dϕ (w).

For b = 1 , Definition B.13 agrees with Definitions B.2 and B.5. 17 Bibliography 

[1] Zhongtian Chen, Edmund Lau, Jake Mendel, Susan Wei, and Daniel Murfet. Dynam-ical versus Bayesian phase transitions in a toy model of superposition. arXiv preprint arXiv:2310.06301 , 2023. [2] Bertrand S. Clarke and Andrew R. Barron. Information-theoretic asymptotics of Bayes methods. IEEE Transactions on Information Theory , 36(3):453–471, 1990. [3] Te Sun Han and Mamoru Hoshi. Interval algorithm for random number generation. 

IEEE Transactions on Information Theory , 43(2):599–611, 1997. [4] Max Hennick and Matthias Dellago. Learning coefficients, fractals, and trees in parameter space, 2026. Preprint, March 2026, https://openreview.net/forum?id= KUFH0n1BIM .[5] Jesse Hoogland, George Wang, Matthew Farrugia-Roberts, Liam Carroll, Susan Wei, and Daniel Murfet. Loss landscape degeneracy and stagewise development in transformers. Transactions on Machine Learning Research , 2025. [6] Marcus Hutter. Universal Artificial Intelligence: Sequential Decisions based on Algorithmic Probability . Springer, Berlin, 2005. [7] Marcus Hutter, David Quarel, and Elliot Catt. An Introduction to Universal Artificial Intelligence . Chapman & Hall/CRC Artificial Intelligence and Robotics Series. Taylor and Francis, 2024. [8] Edmund Lau, Zach Furman, George Wang, Daniel Murfet, and Susan Wei. The local learning coefficient: A singularity-aware complexity measure. arXiv preprint arXiv:2308.12108 , 2024. [9] Shaowei Lin. Algebraic methods for evaluating integrals in Bayesian statistics . PhD thesis, University of California, Berkeley, 2011. [10] Daniel Murfet and Will Troiani. Programs as singularities, 2025. arXiv:2504.08075. [11] Einar Urdshals, Edmund Lau, Jesse Hoogland, Stan van Wingerden, and Daniel Murfet. Compressibility measures complexity: Minimum description length meets singular learning theory, 2025. arXiv:2510.12077. [12] George Wang, Jesse Hoogland, Stan van Wingerden, Zach Furman, and Daniel Murfet. Differentiation and specialization of attention heads via the refined local learning coefficient. arXiv preprint arXiv:2410.02984 , 2024. [13] Thomas Waring. Geometric perspectives on program synthesis and semantics. Master’s thesis, The University of Melbourne, 2021. [14] Sumio Watanabe. Algebraic Geometry and Statistical Learning Theory . Cambridge University Press, USA, 2009. 18 [15] Sumio Watanabe. Mathematical Theory of Bayesian Statistics . CRC Press, Boca Raton, FL, 2018. [16] Susan Wei, Daniel Murfet, Mingming Gong, Hui Li, Jesse Gell-Redman, and Thomas Quella. Deep Learning is Singular, and That’s Good. IEEE Transactions on Neural Networks and Learning Systems , 2022. 19