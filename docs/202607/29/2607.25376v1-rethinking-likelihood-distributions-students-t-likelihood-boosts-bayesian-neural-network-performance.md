---
title: "Rethinking Likelihood distributions: Student's t Likelihood Boosts Bayesian Neural Network Performance"
title_zh: 重新思考似然分布：学生t似然提升贝叶斯神经网络性能
authors: "Pei-Hsuan Hsia, Lars H. Heyen, Arvid Weyrauch, Markus Goetz, Achim Streit, Sebastian Krumscheid, Charlotte Debus"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25376v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: "提出使用Student's t似然改进BNN的变分推断"
tldr: 贝叶斯神经网络中通常使用高斯似然分布，但可能导致模型误设。本文探索了学生t、偏态等替代似然分布对回归任务的影响。实验表明，学生t似然在多种数据集和网络架构下均优于高斯似然，预测性能提升且有时训练更快。该发现挑战了传统假设，提供了简单易实现的改进方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25376v1/fig-001.webp\", \"caption\": \"\", \"page\": 13, \"index\": 1, \"width\": 1397, \"height\": 1453}]"
motivation: 高斯似然假设在贝叶斯神经网络中可能不成立，导致模型误设，但替代似然分布的影响尚未被充分研究。
method: 在回归任务中，使用标准MLP对比高斯、学生t、偏态等不同似然分布，评估预测性能与训练效率。
result: 学生t似然在多种数据和网络深度宽度下均优于高斯，预测性能提升，部分情况下训练时间缩短。
conclusion: 学生t似然是贝叶斯神经网络中一种简单有效的替代方案，可提升性能且易于实现。
---

## 摘要
在贝叶斯神经网络中，变分推断是以分布方式建模不确定性的广泛采用的框架，其中证据下界（ELBO）作为标准的目标函数。多个分布对ELBO损失有贡献，例如先验分布、近似后验分布和似然分布。通常，这些分布都采用高斯分布近似，因为其易于计算、允许重参数化梯度，并提供训练时的闭式损失。然而，一些工作指出这一假设并不普遍成立，存在模型错误设定的风险。已有研究提出了针对先验的替代分布，但分布选择对似然分布的影响仍未探索。本文旨在弥合这一空白，研究似然分布的替代假设是否可能优于常用的高斯分布。我们在人工和真实数据集的回归任务上，使用标准多层感知机（MLP）比较了几种似然分布假设，例如偏态或重尾分布。我们的发现表明，学生t分布比高斯似然分布具有更好的预测性能，且独立于数据分布和MLP架构（深度和宽度）。在某些情况下，学生t分布还可以缩短训练时间，同时仍易于实现。

## Abstract
In Bayesian neural networks (BNNs), variational inference is a widely adopted framework for modeling uncertainty in a distributional way, with the evidence lower bound (ELBO) serving as the standard objective function. Several distributions contribute to the ELBO loss, such as the prior, approximated posterior, and likelihood distribution. Typically, these distributions are all approximated by a Gaussian distribution, since it is easy to compute, allows for reparameterized gradients, and provides a closed-form loss for training. However, several works have highlighted that this assumption may not generally hold, posing the risk of model misspecification. Alternative distributions have been proposed for the prior specifically, while the effect of distribution choice on the likelihood distribution remains unexplored. In this work, our aim is to close this gap by investigating whether alternative assumptions for the likelihood distribution can outperform the commonly used Gaussian. We compare several likelihood distribution assumptions, such as skewed or heavy-tailed, across regression tasks on both artificial and real-world datasets using standard multilayer perceptrons (MLPs). Our findings demonstrate that Student's t yields better predictive performance than a Gaussian likelihood distribution, independent of the data distribution and MLP architecture (depth and width). In some cases, Student's t can also lead to shorter training times, while still being easy to implement.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 核心问题与整体含义（研究动机和背景）
贝叶斯神经网络（BNN）通过变分推断（VI）学习权重分布，其标准损失函数ELBO包含先验、近似后验和似然分布三项。当前普遍假设所有这些分布均为高斯分布，因为其计算简便、支持重参数化梯度且具有闭式形式。然而，已有研究表明高斯假设可能并不普适，存在模型误设风险——尤其当真实数据噪声并非高斯时，会限制BNN性能。尽管针对先验分布已有替代方案的研究，但似然分布的选择尚未被系统探索。本文的核心动机是调查：在VI框架下，是否存在比高斯似然更优的替代分布，以及这种选择是否依赖于数据噪声分布。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：通过改变ELBO中似然分布的形式（保持先验和近似后验为高斯），比较不同似然假设对BNN预测性能的影响。假设：若似然分布与数据噪声分布匹配则最优；但实验发现并非如此，重尾的学生t分布普遍更优。
- **关键技术细节**：
  - 使用Bayes-by-Backprop训练VI-MLP，优化ELBO。
  - 似然分布的参数化基于前向传播的样本统计量（均值μ̂、方差σ̂、偏度γ̂）：
    - 高斯：μ=μ̂, σ=σ̂
    - 偏态正态：通过样本偏度γ̂近似形状参数α，再还原位置ξ和尺度ω
    - 学生t：固定自由度ν=5，位置μ̂，尺度τ = σ̂·√((ν-2)/ν + ε)
  - 先验与近似后验均为高斯（先验均值0，标准差1）。
- **核心探索**：固定学生t自由度ν的敏感性实验（ν∈{2,5,7,10,15}），最终选择ν=5作为平衡点。

## 3. 实验设计
- **数据集**：
  - 人工单变量数据集：x∈[-5,5]均匀采样，y=x³+ε，噪声ε为高斯、Gamma、学生t（均标准化为均0标准差3），样本量300/3000/30000。
  - 人工多变量数据集：x∈R²⁰，y∈R⁵，通过复杂非线性函数生成，噪声包括高斯、Gamma、学生t、拉普拉斯、对数正态，标准差3和15，样本量10000/50000。
  - 真实世界数据集：UCI Combined Cycle Power Plant（9568样本，4输入1输出）；德国电力需求时间序列（ENTSO-E，用过去8×24小时预测未来2×24小时）。
- **Benchmark**：以高斯似然为基线，比较偏态正态和学生t似然。
- **对比方法**：三种似然分布（高斯、偏态正态、学生t），在多种网络深度（1/2/4/8层）和宽度（4/8/16/32/64/128节点）的MLP上训练。
- **评估指标**：MSE（点预测），CRPS（概率预测），MAE/MAPE（真实数据集），训练时间和Epoch，P-P图校准。

## 4. 资源与算力
- **计算环境**：单个节点，双路Intel Xeon Ice Lake CPU，4×NVIDIA A100-40GB GPU，512GB内存，Red Hat Enterprise Linux 9.4。
- **软件**：Python 3.9，PyTorch 2.6.0，CUDA 12.4，torch-bayesian 0.0.1。
- **训练细节**：每个模型在单个A100-40 GPU上运行，每个配置重复30个随机种子，最多200个epoch，早停。论文未报告总GPU小时数，但基于实验规模（多种架构×数据集×种子×似然选择）估算为中等规模（可能数百GPU小时）。

## 5. 实验数量与充分性
- **实验数量**：非常充分。
  - 人工单变量：3种噪声×3种样本量×4种网络形状×3种似然×30种子 = 3240次独立运行。
  - 人工多变量：5种噪声×2种样本量×2种噪声标准差×4种网络形状×3种似然×30种子 ≈ 7200次运行。
  - 真实数据集：2个数据集×9种架构×3种似然×30种子 = 1620次运行。
  - 另外还做了自由度敏感性实验（5种ν值×30种子）。
- **充分性与公平性**：
  - 在控制噪声分布的合成数据上验证了“匹配假设”是否成立，揭示了学生t的稳健性。
  - 在真实数据上进一步验证，覆盖不同任务（回归和时间序列）。
  - 使用统计t检验（p<0.05/0.01/0.001）判断差异显著性，30个随机种子确保结果可靠性。
  - 网络架构覆盖深度和宽度变化，消融了架构影响。
  - 不足之处：未在更大规模网络（如ResNet/Transformer）或分类任务上验证；未探索学生t自由度的动态学习；未测试其他重尾分布（如广义极值分布）。

## 6. 主要结论与发现
1. **最优似然不必须匹配数据噪声**：即使数据噪声是高斯、Gamma或学生t，学生t似然在大多数情况下（尤其是大样本）显著优于高斯，而偏态正态表现最差。
2. **学生t是稳健默认选择**：在人工和真实数据集上，学生t似然在CRPS和MSE上普遍最优或持平，且有时训练epoch更少、训练更稳定。
3. **对数正态噪声的特殊情况**：当噪声极端右偏（对数正态）时，高斯似然在MSE上略优，但学生t在CRPS上更好（更好的不确定性量化），说明存在均值偏差与覆盖率的权衡。
4. **样本量效应**：小样本（n=300）且噪声为高斯时，高斯似然最佳；但随着样本增大，学生t优势明显。
5. **架构无关**：学生t优势独立于MLP深度和宽度。

## 7. 优点
- **填补空白**：首次系统研究VI-BNN中似然分布选择对性能的影响，挑战了默认高斯假设。
- **实验设计严谨**：同时控制噪声分布、样本量、网络架构，使用大量随机种子和统计检验，结论可靠。
- **实用性强**：学生t似然实现简单（只需修改ELBO中的对数似然项），计算开销低，无需复杂调参，可直接替代高斯。
- **多维度评估**：不仅比较点预测（MSE），还比较概率预测（CRPS）和校准（P-P图），全面反映不确定性质量。
- **公开代码**：匿名代码仓库可复现。

## 8. 不足与局限
- **实验覆盖有限**：仅在MLP上验证，未测试CNN、RNN、Transformer等更现代架构；仅回归任务，未涉及分类或强化学习。
- **偏态正态参数化粗糙**：偏态正态通过样本偏度近似，可能不如直接优化形状参数灵活，导致其表现差（可能为方法局限而非分布本身问题）。
- **学生t自由度固定**：未尝试学习ν或使用贝叶斯方式推断，可能限制灵活性。
- **真实数据噪声未知**：虽然验证了学生t更优，但无法确认其为何优于高斯（是否因为真实噪声重尾或网络隐层激活产生重尾）。
- **未考虑模型校准**：P-P图显示校准相似，但未量化校准指标（如期望校准误差ECC）。
- **可扩展性**：实验规模中等（最大网络深度8层、宽度128），未测试极深或极宽网络。
- **计算资源未详述**：总GPU小时未报告，不利于其他研究者评估成本。

（完）
