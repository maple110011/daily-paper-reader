---
title: A Hardware-oriented Approach for Efficient Bayesian Inference Computation and Deployment
title_zh: 一种面向硬件的用于高效贝叶斯推理计算与部署的方法
authors: "Nikola Pižurica, Matteo Risso, Nikola Milović, Alessio Burrello, Igor Jovančević, Conor Heins, Miguel de Prado"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17855v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 硬件加速变分消息传递算法，专为高效贝叶斯推理设计
tldr: 贝叶斯推理计算成本高，难以部署到边缘设备。本文提出面向硬件的加速方法，通过重组张量收缩的内存布局和引入稀疏表示，优化变分消息传递算法。在NVIDIA Jetson Orin AGX上测试770个POMDP配置，实现2-5倍加速且输出数值不变。该方法配合自动调优器，为资源受限环境下的贝叶斯推理提供了高效实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17855v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 543, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1684, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 1225, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 904, \"height\": 465, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17855v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17855v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 917, \"height\": 525, \"label\": \"Table\"}]"
motivation: 贝叶斯推理计算代价高，阻碍其在资源受限边缘设备上的部署。
method: 通过内存布局重组和稀疏表示优化变分消息传递中的张量收缩，并引入自动调优器选择最佳变体。
result: 在Jetson Orin AGX上测试770个POMDP配置，获得2-2.5倍典型加速，最高5倍，结果数值不变。
conclusion: 该方法实现了高效贝叶斯推理部署，兼顾性能和精度，适用于边缘场景。
---

## 摘要
贝叶斯推理为不确定性推理提供了原则性基础，但其计算成本阻碍了在资源受限的边缘设备上的部署。本文提出了一种面向硬件的方法，用于在商用现成嵌入式GPU上加速离散贝叶斯推理。我们发现，一类广泛的变分消息传递算法的延迟主要由张量收缩主导。我们的方法通过两种互补的合并策略重构这些操作的内存布局，生成更适合高效GPU执行的紧凑、规则形状的基元。然后我们引入可选的稀疏数组表示和张量聚类方案以减少内存占用。我们实例化该方法，并为隐马尔可夫模型（HMM）生成了三种消息传递算法的优化变体，即变分滤波、变分消息传递和边际消息传递。此外，我们还补充了一个基于机器学习的自动调优器，能够根据给定的生成模型规范自动选择性能最佳的算法变体。在NVIDIA Jetson Orin AGX上，针对770个随机采样的真实部分可观察马尔可夫决策过程（POMDP）配置进行基准测试，我们的实现实现了高达5倍的加速，典型增益为2-2.5倍，同时产生与基线实现数值相同的输出。

## Abstract
Bayesian inference provides a principled foundation for reasoning under uncertainty, but its computational cost hinders deployment on resource-constrained edge devices. In this paper, we present a hardware-oriented methodology for accelerating discrete Bayesian inference on commercial off-the-shelf embedded GPUs. We identify that the latency of a broad class of variational message-passing algorithms is dominated by tensor contractions. Our approach restructures the memory layout of these operations using two complementary merging strategies that produce compact, regularly-shaped primitives better suited for efficient GPU execution. We then introduce optional sparse array representations and a tensor-clustering scheme to reduce the memory footprint. We instantiate the methodology and produce optimized variants of three message-passing algorithms for Hidden Markov Models (HMMs), namely variational filtering, variational message passing, and marginal message passing. Furthermore, we complement this with a machine-learning-based autotuner that automatically selects the best-performing algorithmic variant for a given generative model specification. Benchmarked on an NVIDIA Jetson Orin AGX across 770 randomly sampled realistic Partially Observable Markov Decision Process (POMDP) configurations, our implementations achieve speedups of up to 5x, with typical gains of 2-2.5x, while producing numerically identical outputs to the baseline implementations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：贝叶斯推理计算成本高，尤其是变分消息传递算法中的大量张量收缩操作，导致其难以部署在资源受限的边缘设备（如嵌入式 GPU）上。
- **研究动机**：实际应用（机器人、自动驾驶、推荐系统等）需要在边缘设备上实时进行不确定性推理，而现有框架（如 pymdp、RxInfer.jl）依赖通用编译器，缺乏针对嵌入式硬件的深度优化。
- **整体含义**：本文旨在通过硬件导向的内存布局重组和稀疏化技术，加速离散贝叶斯推理在商用嵌入式 GPU（如 NVIDIA Jetson）上的执行，在保持数值精度的前提下显著降低延迟。

### 2. 论文提出的方法论

- **核心思想**：将变分消息传递算法中的张量收缩操作（如 `log(A)·o`）从原本循环、形状不规则的执行方式，转化为紧凑、规则形状的合并操作，以更好地利用 GPU 的 SIMT 并行能力。
- **关键技术细节**：
  - **合并策略一：轴对齐填充合并** —— 将不同形状的输入数组零填充至统一形状后沿批处理轴拼接，再执行一次张量收缩。
  - **合并策略二：块对角合并** —— 将高阶张量的滞后维度压平，构造块对角矩阵，将多次矩阵-向量乘积累合成一次标准矩阵乘法。
  - **内存优化**：
    - **BCOO 稀疏表示**：利用 JAX 的 batched coordinate 稀疏格式减少存储。
    - **张量聚类**：将形状相似的数组分组再合并，避免过多零填充。
  - **算法实例化**：针对隐马尔可夫模型（HMM）的三种推理算法（变分滤波 FPI、变分消息传递 VMP、边际消息传递 MMP）分别实现了上述优化变体。
  - **自动调优器**：基于 XGBoost 分类器，输入 POMDP 配置参数，预测最快算法变体，避免部署前枚举测试。

### 3. 实验设计

- **数据集 / 场景**：随机生成的 POMDP 配置，共 770 个，涵盖 5/10/25/125 种状态因子和观测模态数，维度均匀或偏态分布，依赖关系符合实际规律。
- **Benchmark**：基线为 pymdp 库的原始 CPU 实现（经 JAX 后端可在 GPU 运行，但未定制优化）。
- **对比方法**：本文提出的 9 种优化变体（Hybrid、Sparse Hybrid、Clustered Hybrid、Hybrid-block、Sparse Hybrid-block、Clustered Hybrid-block、End-to-end、Sparse End-to-end、Clustered End-to-end），以及自动调优器选择的变体。

### 4. 资源与算力

- **硬件平台**：单个 NVIDIA Jetson Orin AGX（嵌入式 GPU）。
- **算力信息**：
  - 训练自动调优器（XGBoost/RF）所需时间未明确，但提到原始基准测试每个配置平均约 4 分钟（大模型 20–25 分钟），而自动调优器只需几分之一秒。
  - 实验中未提及具体 GPU 数量或训练总时长，也未给出加速器利用率等细节。

### 5. 实验数量与充分性

- **实验数量**：共 770 组 POMDP 配置，每个配置对 9 种优化变体及基线进行延迟测量，每种算法（FPI/VMP/MMP）独立评估。
- **充分性**：
  - 覆盖了广泛的维度范围和依赖结构，包含均匀/偏态采样，确保性能归因不依赖于特定输入。
  - 对比了合并策略、稀疏表示、聚类、端到端等消融变体，整体设计全面。
  - 自动调优器采用 5 折交叉验证（80% 训练，20% 测试），并进行了超参数随机搜索（60 组）。
- **公平性**：所有优化变体与基线输出数值完全等同，仅比较延迟，避免精度偏差。

### 6. 论文的主要结论与发现

- 本文方法在 NVIDIA Jetson Orin AGX 上对变分滤波（FPI）实现典型加速 1.5 倍左右，对 VMP 和 MMP 实现典型加速 2–2.5 倍，最高达 4.3–4.9 倍。
- **块对角合并 + 聚类** 的混合变体表现最佳，且几乎不会导致性能回退。
- **自动调优器**（XGBoost）的后悔值极低（FPI: 3.15%，VMP: 1.64%，MMP: 1.58%），能自动选取近似最优变体，避免枚举。
- 稀疏 BCOO 表示目前虽未直接加速，但能降低内存占用（平均 14 倍），未来可能受益于更成熟的支持。
- 参数计数与延迟之间存在可接受的 trade-off（增加少于 1 GB VRAM 通常可接受）。

### 7. 优点

- **算法无关性**：优化重点在通用张量收缩基元，可适用于多种消息传递算法。
- **损失无精度**：所有优化变体输出与原始算法数学等价，无需额外近似。
- **自动部署友好**：机器学习自动调优器可在秒级完成配置选择，免去繁琐的手动调优或穷举测试。
- **硬件针对性强**：在真实边缘 GPU 上验证，所提合并与聚类策略契合 GPU 内存访问模式。
- **开源发布**：代码将开源，便于复现和后续扩展。

### 8. 不足与局限

- **B依赖限制**：当前仅支持平凡 B 依赖（每个状态因子仅依赖自身前一时刻），无法处理一般化的转换依赖结构。
- **稀疏矩阵加速有限**：由于 JAX 对 BCOO 稀疏操作支持尚处实验阶段，稀疏变体并未带来明显加速，仅降低了参数计数。
- **平台单一**：所有实验只在 NVIDIA Jetson Orin AGX 上进行，未验证其他嵌入式 GPU、CPU 或 TPU，推广性存疑。
- **离散模型限定**：仅适用于离散状态/观测模型，无法直接扩展到连续或混合模型。
- **内存开销波动**：对于极大规模因子图，合并后的张量可能超出可用显存；聚类虽缓解，但未完全消除风险。

（完）
