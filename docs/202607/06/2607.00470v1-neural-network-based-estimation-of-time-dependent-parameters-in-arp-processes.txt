Title: Neural Network-Based Estimation of Time-Dependent Parameters in AR(p) Processes

URL Source: https://arxiv.org/pdf/2607.00470v1

Published Time: Thu, 02 Jul 2026 00:32:03 GMT

Number of Pages: 26

Markdown Content:
NEURAL NETWORK-BASED ESTIMATION OF TIME-DEPENDENT PARAMETERS IN AR( p) PROCESSES 

AGNIESZKA KOPEĆ, PAWEŁ PRZYBYŁOWICZ, AND MARTYNA WIĄCEK 

Abstract. We investigate a forecasting framework based on a simple discrete-time dy-namic model with coefficients varying in time. The parameters of the model are recovered within a deep learning framework, which makes it possible to retain a transparent paramet-ric structure while simultaneously accounting for complex and nonstationary patterns in the observed phenomenon. Our analysis covers two specifications of the noise process. Besides the standard Gauss-ian setting, we also consider Laplace-distributed noise, which can offer a more adequate description in the presence of heavier tails and sharper local fluctuations. For both cases, we formulate the predictive scheme of the model and analyze the associated uncertainty quantification, including the construction of prediction intervals. The results illustrate that a relatively simple model, when combined with time-dependent parameter estimation, can serve as a mathematically tractable and practically flexible tool for forecasting complex dynamics under different noise assumptions. The general model is stated for TVAR( p), while the prediction-interval formulas and the numerical experiments are developed for the TVAR(1) case. 

Key words: artificial neural networks, time-varying parameters, AR(p) process, quasi-likelihood function, nonstationary time series, parameter estimation 

MSC 2020: 62M10, 68T07, 62F10, 60G25 

1. Introduction 

Time series models with time-varying parameters constitute a fundamental tool for mod-eling nonstationary phenomena encountered in economics, finance, energy markets, and en-vironmental applications. Classical autoregressive models assume constant coefficients and stationary noise, which often proves inadequate in the presence of structural changes, evolv-ing volatility, or regime-dependent dynamics. To overcome these limitations, time-varying autoregressive (TVAR) models have been extensively studied in the literature. A classical and well-established approach to TVAR modeling is based on state-space for-mulations, in which the autoregressive coefficients are treated as latent stochastic processes, typically evolving according to random walk or autoregressive dynamics. This framework un-derlies dynamic linear models and Bayesian filtering techniques, such as the Kalman filter and its extensions, and provides a coherent probabilistic interpretation together with recursive es-timation procedures; see, for example, [2, 10]. While these methods are theoretically sound and interpretable, they rely on relatively simple assumptions on the temporal evolution of parameters, which may limit their ability to capture nonlinear or irregular time variation. An alternative line of research models time-varying parameters as smooth deterministic functions of time. These functions can be estimated using penalized likelihood methods, spline-based techniques, or other smoothness-inducing regularization approaches [5]. Related frame-works include locally stationary models, which assume that the underlying process evolves  

> 1
> arXiv:2607.00470v1 [stat.ML] 1 Jul 2026 NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 2

gradually over time [1]. Although these methods offer greater flexibility than purely stochas-tic parameter evolution, they still require the smoothness structure or functional form to be specified a priori. In parallel, the rapid development of machine learning methods has led to widespread use of neural networks for time series forecasting, including hybrid ARIMA–ANN approaches that combine linear autoregressive structure with nonlinear neural-network components [14]. Deep learning models have demonstrated strong empirical performance, particularly in large-scale and highly nonlinear settings. Recent probabilistic forecasting approaches combine neural networks with likelihood-based objectives in order to learn conditional distributions of future observations rather than point predictions [3, 11, 12]. However, such methods are typically formulated as black-box predictors and do not preserve the explicit autoregressive structure of classical time series models, which limits interpretability and hinders direct statistical inference on model parameters. More recently, hybrid approaches have emerged that combine stochastic modeling with neural networks, using deep learning as a flexible function approximator for unknown or time-dependent model components. In this paradigm, neural networks are not used to replace the underlying stochastic model, but rather to learn its evolving parameters from data. This idea has been successfully applied to Markov models and stochastic differential equations with time-dependent coefficients, where likelihood-based training allows for statistically principled parameter estimation [4]. Such approaches retain the interpretability of classical models while benefiting from the expressive power of neural networks. A closely related contribution is the DeepTVAR model, where an LSTM network is used to generate time-varying autoregressive coefficient matrices and innovation covariance ma-trices in a VAR( p) framework [8]. DeepTVAR also enforces the causality condition on the autoregressive coefficients through the Ansley–Kohn transform and is applied to energy price forecasting. Compared with DeepTVAR, the present work considers a less general, univariate AR( p) setting, but it differs in several important aspects. First, we use a feedforward neural network rather than a recurrent LSTM architecture. Second, our model explicitly includes a time-dependent intercept, or trend component, c(t), together with time-varying autoregres-sive coefficients and a time-varying noise scale. Third, we study both Gaussian and Laplace innovation specifications and derive prediction intervals for the time-varying AR(1) case. Another related approach is the TVPANN-GARCH model of [9], where artificial neural networks are used to model time-varying parameters in a GARCH-type volatility equation. This work is conceptually close to ours in its use of neural networks as flexible approximators of parameter dynamics, but its statistical object is different: it focuses on conditional volatil-ity modeling, whereas our framework concerns autoregressive dynamics with time-dependent mean and noise parameters. Motivated by these developments, we focus on the estimation of time-dependent parame-ters in autoregressive time series models. In contrast to classical state-space TVAR formula-tions, we model the intercept, autoregressive coefficient, and noise scale as deterministic but unknown functions of time, approximated by a feedforward neural network. Parameter esti-mation is carried out by minimizing the negative log-likelihood corresponding to the assumed noise distribution, thereby embedding the neural network directly into a likelihood-based inference framework. As a result, the learned network outputs can be interpreted as time-varying autoregressive parameters, which preserves interpretability while allowing the model to capture complex nonstationary behavior. In addition to the standard Gaussian assumption, we explicitly consider Laplace-distributed noise in order to account for heavier tails and increased robustness to atypical observations. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 3

Heavy-tailed noise models are well known to be important in financial and energy time series, where extreme observations occur frequently [7, 13]. By allowing for alternative likelihoods within the same modeling framework, we enable a direct comparison between Gaussian and non-Gaussian specifications and assess their impact on parameter estimation and forecasting performance. A further contribution of this work is the derivation of recursive formulas for point fore-casts and multi-step-ahead prediction intervals in the time-varying AR(1) case, treated as a special case of the general TVAR( p) framework. Conditional on the estimated parameter trajectories, we provide explicit expressions for forecast means and variances, leading to com-putationally efficient probabilistic forecasts. Unlike generic neural network forecasting models, our approach preserves the autoregressive structure and allows uncertainty quantification to be carried out in a manner analogous to classical time series analysis. The proposed methodology is illustrated through numerical experiments on both synthetic and real-world datasets, including electricity spot prices. The experiments are intended as illustrative evidence that the neural-network-based estimation can recover time-varying pa-rameters in controlled settings and produce interpretable forecasts in nonstationary time se-ries. Overall, the paper contributes to the growing literature on interpretable, likelihood-based neural network models for stochastic time series with time-dependent structure. 2. Model specification and loss function 

2.1. TVAR( p) model with time-varying coefficients. We consider a time-varying au-toregressive process of order p, defined by 

yt = c(t) + 

> p

X

> j=1

Φj (t) yt−j + εt, t ≥ p, p ≥ 1, (1) with initial values 

y0, y 1, . . . , y p−1 ∈ R.

The functions 

c(·), Φ1(·), . . . , Φp(·)

are assumed to be unknown and potentially nonlinear. Depending on the assumed distribution of the noise process, the model also involves an additional time-dependent scale parameter, denoted by σ2(t) in the Gaussian case and by b(t) in the Laplace case. Our goal is to estimate these time-dependent components based on the observed sample 

{˜y0, ˜y1, ˜y2, . . . , ˜yN }.

2.2. Discrete Markov process of order p ≥ 1. Let (Yt)t∈Z be a discrete Markov process of order p. Then, for every t ≥ p and A ∈ B (R), we have 

P (Yt ∈ A | Yt−1, Y t−2, . . . , Y t−p, . . . , Y 0) = P (Yt ∈ A | Yt−1, . . . , Y t−p).

Assuming that the corresponding conditional densities exist, we obtain the density of the non-initial part of the trajectory, 

f(Yp,...,Y N )|(Y0,...,Y p−1)(yp, . . . , y N | y0, . . . , y p−1) = 

> N

Y

> t=p

fYt|(Yt−1,...,Y t−p)

 yt | yt−1, . . . , y t−p

.

The variables Y0, . . . , Y p−1 are treated as fixed initial values in this conditional likelihood. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 4

Model (1) defines a discrete Markov process of order p.Thus, conditional on the block 

(yt−1, . . . , y t−p), the distribution of yt is fully determined by the time-dependent coefficients and the distribution of the noise term. 2.3. Gaussian specification and loss function. Assume that the noise sequence (εt) con-sists of independent random variables such that 

εt ∼ N (0 , σ 2(t)) , t ≥ p, 

where σ2(t) > 0 for all t ≥ p.Then 

yt+p | yt+p−1, . . . , y t ∼ N 

c(t + p) + 

> p

X

> j=1

Φj (t + p) yt+p−j , σ 2(t + p)

 ,

and the corresponding conditional density is 

f yt+p|(yt+p−1,...,y t)

 x | yt+p−1, . . . , y t



= 1

√2π σ (t + p) exp 

−



x − c(t + p) − Ppj=1 Φj (t + p) yt+p−j

2

2 σ2(t + p)

 .

Hence, the joint density of (yp, . . . , y N ) conditional on (y0, . . . , y p−1) is given by 

f(yp,...,y N )|(y0,...,y p−1)(yp, . . . , y N | y0, . . . , y p−1) = 

> N

Y

> t=p

f yt|(yt−1,...,y t−p)

 yt | yt−1, . . . , y t−p

.

This leads to the negative log-likelihood function 

LG(c, Φ, σ 2) = 

> N

X

> t=p

 12 ln  2π σ 2(t) +



˜yt − c(t) − Ppj=1 Φj (t) ˜ yt−j

2

2 σ2(t)

 .

For Gaussian innovations with a fixed or known scale, minimizing this conditional negative log-likelihood reduces to a conditional least-squares criterion, a classical estimation approach for stochastic processes [6]. We denote the time-varying parameters by 

ΘG(t) = c(t), Φ1(t), . . . , Φp(t), σ 2(t).

Following the approach proposed in [4], we approximate the function ΘG(t) using a neural network with weights w ∈ RM . The corresponding empirical loss function is obtained by substituting the network outputs into LG.2.4. Laplace specification and loss function. Assume that the noise sequence (εt) consists of independent random variables such that 

εt ∼ Laplace(0 , b (t)) , t ≥ p, 

where b(t) > 0 for all t ≥ p.Then 

yt+p | yt+p−1, . . . , y t ∼ Laplace 

c(t + p) + 

> p

X

> j=1

Φj (t + p) yt+p−j , b (t + p)

 ,NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 5

and the corresponding conditional density is 

f yt+p|(yt+p−1,...,y t)

 x | yt+p−1, . . . , y t



= 12 b(t + p) exp 

−

x − c(t + p) − Ppj=1 Φj (t + p) yt+p−j

b(t + p)

 .

Hence, the joint density of (yp, . . . , y N ) conditional on (y0, . . . , y p−1) is given by 

f(yp,...,y N )|(y0,...,y p−1)(yp, . . . , y N | y0, . . . , y p−1) = 

> N

Y

> t=p

f yt|(yt−1,...,y t−p)

 yt | yt−1, . . . , y t−p

.

This leads to the negative log-likelihood function 

LL(c, Φ, b ) = 

> N

X

> t=p

ln  2 b(t) +

˜yt − c(t) − Ppj=1 Φj (t) ˜ yt−j

b(t)

 .

We denote the time-varying parameters by 

ΘL(t) = c(t), Φ1(t), . . . , Φp(t), b (t).

Again, these parameter functions are approximated by a feedforward neural network with weights w ∈ RM . The corresponding empirical loss function is obtained by substituting the network outputs into LL.2.5. AR(1) model. For p = 1 , model (1) reduces to the process 

yt = c(t) + Φ( t)yt−1 + εt, t ∈ { 1, 2, 3, . . . },

with the initial condition y0 ∈ R.In the Gaussian case, we assume that 

εt ∼ N (0 , σ 2(t)) , t ≥ 1.

Then 

yt | yt−1 ∼ N  c(t) + Φ( t)yt−1, σ 2(t),

and the corresponding negative log-likelihood function is 

L(c, Φ, σ 2) =  

> N−1

X

> t=0

"

12 ln  2πσ 2(t + 1)  +

 ˜yt+1 − c(t + 1) − Φ( t + 1)˜ yt

2

2σ2(t + 1) 

#

. (2) We denote the time-varying parameters by 

Θ( t) := c(t), Φ( t), σ 2(t),

and approximate this function by a neural network: 

ˆΘ( t; w) := ˆc(t; w), ˆΦ( t; w), ˆσ2(t; w).

Substituting these approximations into Equation (2) yields the empirical loss function 

ˆL(w) =  

> N−1

X

> t=0

 12 ln  2πˆσ2(t + 1; w) +



˜yt+1 − ˆc(t + 1; w) − ˆΦ( t + 1; w)˜ yt

2

2ˆ σ2(t + 1; w)

 .

In the Laplace case, we assume that 

εt ∼ Laplace(0 , b (t)) , t ≥ 1.NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 6

Then 

yt | yt−1 ∼ Laplace  c(t) + Φ( t)yt−1, b (t),

and the corresponding negative log-likelihood function is 

L(c, Φ, b ) =  

> N−1

X

> t=0



ln  2b(t + 1)  + |˜yt+1 − c(t + 1) − Φ( t + 1)˜ yt|

b(t + 1) 



.

In this case, we denote 

Θ( t) = c(t), Φ( t), b (t),

and approximate this function by a neural network: 

ˆΘ( t; w) = ˆc(t; w), ˆΦ( t; w), ˆb(t; w).

Substituting the network outputs into the likelihood yields the empirical loss 

ˆL(w) =  

> N−1

X

> t=0

"

ln  2ˆb(t + 1; w) + |˜yt+1 − ˆc(t + 1; w) − ˆΦ( t + 1; w)˜ yt|

ˆb(t + 1; w)

#

.

2.6. Normal–Laplace model. As an additional extension, one may consider the case where the noise term follows the Normal–Laplace distribution 

εt ∼ NL  0, σ 2(t), b (t).

A random variable X has the Normal–Laplace distribution if 

X = W + Z, 

where W and Z are independent random variables such that 

W ∼ N (μ, σ 2), Z ∼ Laplace(0 , b ).

Thus, both the Gaussian and Laplace distributions may be viewed as special or limiting cases of this specification. Since the corresponding density is more involved, in the present paper we restrict further analysis to the Gaussian and Laplace cases. 3. Predictions and prediction intervals in TVAR(1) models 

In this section, we study point predictions and prediction intervals for the TVAR(1) model, treated as a special case of the general framework introduced in Section 2. We assume that the time-dependent parameters have already been estimated based on the observed sample 

y0, y 1, . . . , y N ,

and we write the corresponding estimates as 

ˆc(t), ˆΦ( t),

together with the estimated scale parameter, denoted by ˆσ(t) in the Gaussian case and by 

ˆb(t) in the Laplace case. The prediction intervals derived in this section are conditional on the information available at the forecasting origin. Since the estimated parameter trajectories are obtained from this information, the intervals account only for the variability induced by future innovations and do not include the additional uncertainty arising from parameter estimation. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 7

3.1. Prediction intervals for the TVAR(1) model with Gaussian noise. We consider the model 

yt = c(t) + Φ( t)yt−1 + σ(t)ηt, t ≥ 1,

where the sequence (ηt)t≥1 consists of independent and identically distributed random vari-ables such that 

ηt ∼ N (0 , 1) .

Given the estimated parameter functions, for t ≥ N + 1 we define the predictive scheme 

ˆyt = ˆ c(t) + ˆΦ( t)ˆ yt−1 + ˆ σ(t)ηt, ˆyN := yN .

Moreover, we define a filtration 

G0 := {∅, Ω},

and for t ≥ 1,

Gt := σ(η1, . . . , η t),

where σ(η1, . . . , η t) denotes the σ-algebra generated by the random variables η1, . . . , η t. Note that for all t ≥ N ,

σ(ˆ yt) ⊂ G t.

We assume that all estimated parameter functions are measurable with respect to the GN .For k ≥ 1, we introduce the notation 

¯yN +k := E(ˆ yN +k | G N ).

Since ηN +k is independent of GN and has zero mean, we obtain recursively 

¯yN +1 = ˆ c(N + 1) + ˆΦ( N + 1) yN ,

and, for k ≥ 2,

¯yN +k = ˆ c(N + k) + ˆΦ( N + k)¯ yN +k−1.

Let 

eN +k := ˆ yN +k − ¯yN +k

denote the prediction error. Then 

eN +1 = ˆ σ(N + 1) ηN +1 ,

and for k ≥ 2,

eN +k = ˆΦ( N + k)eN +k−1 + ˆ σ(N + k)ηN +k.

Hence, conditionally on GN , the random variable eN +k is Gaussian with zero mean. Its conditional variance can be computed recursively as 

s2  

> N+1

:= Var( eN +1 | G N ) = ˆ σ2(N + 1) ,

and, for k ≥ 2,

s2  

> N+k

:= Var( eN +k | G N ) = ˆΦ2(N + k)s2  

> N+k−1

+ ˆ σ2(N + k).

Therefore, 

eN +k | G N ∼ N (0 , s 2 

> N+k

),

and for a confidence level α ∈ (0 , 1) we obtain 

P

 |eN +k|

sN +k

≤ q(G) 

> α

GN



= α, 

where q(G) 

> α

denotes the α-quantile of |Z| for Z ∼ N (0 , 1) , that is, the α-quantile of the standard half-normal distribution. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 8

Thus, the k-step-ahead prediction interval at level α is 

¯P (G) 

> N+k

(α) := 

h

¯yN +k − q(G) 

> α

sN +k, ¯yN +k + q(G) 

> α

sN +k

i

.

3.2. Prediction intervals for the TVAR(1) model with Laplace noise. We recall a ba-sic property of the Laplace distribution that will be used throughout this subsection. 

Remark 1 (Scaling property of the Laplace distribution) . If X ∼ Laplace(0 , 1) , then for any 

β ∈ R and α > 0,

αX + β ∼ Laplace( β, α ).

We now consider the model 

yt = c(t) + Φ( t)yt−1 + b(t)ξt, t ≥ 1,

where the sequence (ξt)t≥1 consists of independent and identically distributed random vari-ables such that 

ξt ∼ Laplace(0 , 1) .

Given the estimated parameter functions, for t ≥ N + 1 we define 

ˆyt = ˆ c(t) + ˆΦ( t)ˆ yt−1 + ˆb(t)ξt, ˆyN := yN .

As before, we define a filtration 

F0 := {∅, Ω},

and for t ≥ 1,

Ft := σ(ξ1, . . . , ξ t).

Then for all t ≥ N ,

σ(ˆ yt) ⊂ F t.

As in the Gaussian case, we assume that all estimated parameter functions are measurable with respect to the FN . We introduce the notation 

¯yN +k := E(ˆ yN +k | F N ), k ≥ 1.

Since ξN +k is independent of FN and has zero mean, we obtain 

¯yN +1 = ˆ c(N + 1) + ˆΦ( N + 1) yN ,

and, for k ≥ 2,

¯yN +k = ˆ c(N + k) + ˆΦ( N + k)¯ yN +k−1.

One-step-ahead prediction. Let 

eN +1 := ˆ yN +1 − ¯yN +1 .

Then 

eN +1 = ˆb(N + 1) ξN +1 ,

and therefore 

eN +1 | F N ∼ Laplace(0 , ˆb(N + 1)) .

We use the following elementary fact. 

Lemma 1. Let X ∼ Laplace(0 , s ) with s > 0. Then, for every q ≥ 0,

P(|X| ≤ q) = 1 − e−q/s .NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 9

Proof. Let F denote the cumulative distribution function of X. Then 

P(|X| ≤ q) = P(−q ≤ X ≤ q) = F (q) − F (−q).

For X ∼ Laplace(0 , s ),

F (x) = 

( 12 ex/s , x ≤ 0,

1 − 12 e−x/s , x > 0.

Hence, 

P(|X| ≤ q) = 



1 − 12 e−q/s 



− 12 e−q/s = 1 − e−q/s .

□

Applying Lemma 1, we obtain 

P |eN +1 |

ˆb(N + 1) ≤ q(L) 

> α

FN

!

= α

if and only if 

1 − e−q(L) 

> α

= α. 

Thus 

q(L) 

> α

= − ln(1 − α) = ln 

 11 − α



,

and the one-step-ahead prediction interval at level α is 

¯P (L) 

> N+1

(α) := 



¯yN +1 − ˆb(N + 1) ln 

 11 − α



, ¯yN +1 + ˆb(N + 1) ln 

 11 − α

 

.

Two-step-ahead prediction. We now consider the two-step-ahead conditional expectation 

¯yN +2 := E(ˆ yN +2 | F N ).

Using the model structure, we obtain 

¯yN +2 = ˆ c(N + 2) + ˆΦ( N + 2)¯ yN +1 .

The corresponding prediction error is 

eN +2 := ˆ yN +2 − ¯yN +2 = ˆΦ( N + 2) eN +1 + ˆb(N + 2) ξN +2 .

Since 

eN +1 = ˆb(N + 1) ξN +1 ,

it follows that 

eN +2 = ˆΦ( N + 2) ˆb(N + 1) ξN +1 + ˆb(N + 2) ξN +2 .

Set 

a := | ˆΦ( N + 2) | ˆb(N + 1) , c := ˆb(N + 2) .

Then, conditionally on FN ,

eN +2 d

= X1 + X2,

where 

X1 ∼ Laplace(0 , a ), X2 ∼ Laplace(0 , c ),

and X1 and X2 are independent. If a̸ = c, the density of eN +2 is given by 

feN +2 (x) = a e −| x|/a − c e −| x|/c 

2( a2 − c2) , x ∈ R.NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 10 

Consequently, for q ≥ 0,

P(|eN +2 | ≤ q | F N ) = 1 − a2e−q/a − c2e−q/c 

a2 − c2 .

Therefore, the radius q(2)  

> α

of the two-step-ahead prediction interval is determined by the equa-tion 

a2e−q(2)   

> α/a

− c2e−q(2)   

> α/c

= (1 − α)( a2 − c2).

If a = c, then 

feN +2 (x) = 14a e−| x|/a 



1 + |x|

a



, x ∈ R,

and, for q ≥ 0,

P(|eN +2 | ≤ q | F N ) = 1 − e−q/a 

1 + q

2a



.

Hence, in this case q(2)  

> α

is determined by 

e−q(2)   

> α/a

1 + q(2) 

> α

2a

!

= 1 − α. 

The detailed derivation of the density of eN +2 , together with the corresponding distribution function and the equation determining the quantile q(2)  

> α

, is given in Appendix A. Thus, the two-step-ahead prediction interval at level α is 

¯P (L) 

> N+2

(α) := 

h

¯yN +2 − q(2)  

> α

, ¯yN +2 + q(2) 

> α

i

.

In contrast to the Gaussian case, the quantity q(2)  

> α

is not given by a universal quantile depending only on α. It depends on the estimated parameters through a = | ˆΦ( N +2) |ˆb(N +1) 

and c = ˆb(N + 2) , and is obtained as the unique solution of the corresponding scalar equation above. 

Multi-step-ahead prediction. Finally, we define the k-step prediction error 

eN +j := byN +j − yN +j , j ∈ { 0, 1, . . . , k }.

We recall the plug-in predictive path defined by 

byN := yN ,

byN +j := bc(N + j) + bΦ( N + j) byN +j−1 + bb(N + j) ξN +j , j ∈ { 1, . . . , k }. (3) and the associated conditional mean (“noise-free”) prediction given by 

yN := yN ,yN +j := EbyN +j | F N

 = bc(N + j) + bΦ( N + j) yN +j−1, j ∈ { 1, . . . , k }. (4) 

Proposition 1 (Exact conditional law of the k-step prediction error under Laplace innova-tions) . Assume the foregoing set-up. Then, conditional on FN :(1) The error process satisfies the recursion 

eN = 0 , eN +j = bΦ( N + j) eN +j−1 + bb(N + j) ξN +j , j ∈ { 1, . . . , k }, (5) 

and admits the explicit expansion 

eN +k =

> k

X

> j=1

aj,k ξN +j , aj,k := bb(N + j)

> k

Y

> m=j+1

bΦ( N + m), (6) 

with the convention that an empty product equals 1.NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 11 

(2) The conditional characteristic function of eN +k is 

φk(t) := Eeite N +k | F N

 =

> k

Y

> j=1

11 + a2

> j,k

t2 , t ∈ R. (7) 

(3) Let sj,k := |aj,k | for j ∈ { 1, . . . , k } and assume that s1,k , . . . , s k,k are pairwise distinct and strictly positive. Define 

Aj,k := Y

> ℓ∈{ 1,...,k }\{ j}

s2

> j,k

s2 

> j,k

− s2

> ℓ,k

. (8) 

Then eN +k has a Lebesgue density given by the finite sum 

fk(x) := ddx P(eN +k ≤ x | F N ) = 12

> k

X

> j=1

Aj,k 

sj,k 

exp 



− |x|

sj,k 



, x ∈ R. (9) 

In particular, the conditional distribution is symmetric about 0 and for x ≥ 0,

Fk(x) := P(eN +k ≤ x | F N ) = 1 − 12

> k

X

> j=1

Aj,k exp 



− xsj,k 



. (10) 

(4) For any confidence level α ∈ (0 , 1) there exists a unique qk,α > 0 such that 

P  |eN +k| ≤ qk,α | F N

 = α. (11) 

Under the distinct-scales assumption from item 3, qk,α is the unique solution of 

> k

X

> j=1

Aj,k exp 



− qk,α 

sj,k 



= 1 − α. (12) 

Consequently, a central α-level prediction interval for yN +k is 

yN +k − qk,α , y N +k + qk,α 

. (13) 

Proof. We prove the four items in turn. Ad 1. From (3) and (4), we have, for j ≥ 1,

eN +j = byN +j − yN +j

=

bc(N + j) + bΦ( N + j)byN +j−1 + bb(N + j)ξN +j



−

bc(N + j) + bΦ( N + j)yN +j−1



= bΦ( N + j)  byN +j−1 − yN +j−1

 + bb(N + j)ξN +j = bΦ( N + j) eN +j−1 + bb(N + j)ξN +j .

Since eN = byN − yN = yN − yN = 0 , this proves (5). We now show (6) by induction on k. For k = 1 , (5) gives eN +1 = bb(N + 1) ξN +1 , which matches (6) with a1,1 = bb(N + 1) . Assume (6) holds for a given k ≥ 1. Then, by (5), 

eN +( k+1) = bΦ( N + k + 1) eN +k + bb(N + k + 1) ξN +k+1 

= bΦ( N + k + 1) 

> k

X

> j=1

aj,k ξN +j + bb(N + k + 1) ξN +k+1 

=

> k

X

> j=1



aj,k bΦ( N + k + 1) 



ξN +j + bb(N + k + 1) ξN +k+1 .NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 12 

Comparing with (6), we see that aj,k +1 = aj,k bΦ( N +k+1) for j ≤ k and ak+1 ,k +1 = bb(N +k+1) .This is exactly the definition aj,k +1 = bb(N + j) Qk+1  

> m=j+1

bΦ( N + m). Thus (6) holds for k + 1 ,completing the induction. Ad 2. By (6) and the conditional independence of ξN +1 , . . . , ξ N +k from FN (and from each other), 

φk(t) = E

h

exp 



it 

> k

X

> j=1

aj,k ξN +j



FN

i

= E

h kY

> j=1

exp  ita j,k ξN +j

 FN

i

=

> k

Y

> j=1

E exp  ita j,k ξN +j

 =

> k

Y

> j=1

11 + a2

> j,k

t2 ,

where in the last step we used Lemma 2 with s = |aj,k |. This proves (7). Ad 3. Set sj,k := |aj,k | and assume s1,k , . . . , s k,k are pairwise distinct and positive. Introduce the auxiliary variable u := t2 ≥ 0. Then (7) becomes 

φk(t) = 

> k

Y

> j=1

11 + s2

> j,k

t2 =

> k

Y

> j=1

11 + s2

> j,k

u .

By Lemma 3 (applied with sj = sj,k ), there exist constants Aj,k given by (8) such that 

φk(t) = 

> k

X

> j=1

Aj,k 

1 + s2

> j,k

t2 . (14) Since φk is integrable (indeed, φk(t) = O(t−2k) as |t| → ∞ ), the conditional density exists and can be obtained by Fourier inversion: 

fk(x) = 12π

Z ∞−∞ 

e−itx φk(t) d t. (15) Insert (14) into (15) and use that the sum is finite to exchange sum and integral: 

fk(x) = 

> k

X

> j=1

Aj,k 

12π

Z ∞−∞ 

e−itx 

1 + s2

> j,k

t2 dt =

> k

X

> j=1

Aj,k gsj,k (x),

where gs is as in Lemma 4. Applying Lemma 4 gives exactly (9). To obtain the CDF for x ≥ 0, integrate (9) from −∞ to x. Using symmetry of each term 

e−| x|/s , it is convenient to first compute the survival function for x ≥ 0:

P(eN +k > x | F N ) = 

Z ∞

> x

fk(u) d u = 12

> k

X

> j=1

Aj,k 

sj,k 

Z ∞

> x

e−u/s j,k du

= 12

> k

X

> j=1

Aj,k 

sj,k 

h

− sj,k e−u/s j,k 

iu=∞ 

> u=x

= 12

> k

X

> j=1

Aj,k e−x/s j,k .

Therefore, for x ≥ 0, Fk(x) = 1 − P(eN +k > x | F N ) which yields (10). Symmetry follows from (6) since each ξN +j is symmetric about 0 and the coefficients aj,k are deterministic given FN . Alternatively, it is immediate from (9), since fk is an even function. Ad 4. Since each scaled Laplace density is strictly positive everywhere and continuous, the density of a sum of independent scaled Laplace variables is the convolution of strictly positive continuous functions and is therefore itself strictly positive and continuous on R. Hence the NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 13 

conditional CDF of eN +k is continuous and strictly increasing. Consequently, for any α ∈ (0 , 1) 

there exists a unique qk,α > 0 satisfying (11). Under the distinct-scales assumption, by symmetry 

P(|eN +k| ≤ q | F N ) = P(−q ≤ eN +k ≤ q | F N ) = Fk(q) − Fk(−q) = 2 Fk(q) − 1,

for all q ≥ 0. Thus (11) is equivalent to Fk(q) = (1 + α)/2. Using (10) with x = q ≥ 0, we obtain 

1 + α

2 = Fk(q) = 1 − 12

> k

X

> j=1

Aj,k e−q/s j,k ,

which rearranges to (12). The prediction interval (13) then follows directly from the definition 

eN +k = byN +k − yN +k. □

4. Numerical experiments 

In this section, we present numerical experiments on both synthetic and real data. Our aim is twofold: to verify whether the proposed neural-network-based procedure can recover time-dependent model parameters in a controlled setting and to illustrate its forecasting behavior under different noise assumptions. In the synthetic case, the true parameter functions are known and can therefore be directly compared with their estimates. In the real-data case, we analyze the recovered parameter trajectories and report one-step-ahead and two-step-ahead prediction errors obtained under Gaussian and Laplace noise. The temporal ordering of the observations is preserved throughout all experiments, and no random shuffling is used. The real-data forecasts are intended as an illustrative comparison for the considered splits, rather than as a comprehensive forecasting benchmark. The notebooks used to generate the final experimental figures are collected in the folder TVAR_NOTEBOOKS_FINAL and are available at 

https://github.com/agkopec/TVAR_NOTEBOOKS_FINAL .4.1. Numerical experiments on synthetic data. We first consider synthetic data gener-ated from the TVAR(1) model under two noise specifications: Gaussian and Laplace. Since the true parameter functions are known in this setting, these experiments provide a direct benchmark for assessing the accuracy of the proposed neural-network-based estimation pro-cedure. In both synthetic experiments we use N = 99 transitions, i.e. observations y0, . . . , y 99 ,with y0 = 7 and random seed 42 . For t = 0 , . . . , 99 , the deterministic parameter functions are 

c(t) = sin 

 t

6π



+ 7 ,ϕ(t) = −0.3



0.5 cos 

 t

4π + 30 



+ 0 .5



+ 0 .2,

and 

b(t) = 



0.5 0.5 + sin 

 t

6π



+ 0 .5

2

.

For the Laplace experiment the innovations satisfy εt ∼ Laplace(0 , b (t)) , whereas for the Gaussian experiment we use εt ∼ N (0 , σ 2(t)) with 

σ2(t) = b(t).

The same functions c(t) and ϕ(t) are used in both noise specifications. For the Laplace noise specification, the final architecture and the training hyperparameters were selected by a grid-search procedure with multistart initialization. The search covered the hidden-layer widths and depth, activation function, learning rate, smoothness penalty, NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 14 

and the optional constraint imposed on the autoregressive coefficient ( |ϕ| < 0.99 ). The final model was selected by minimizing the average raw mean squared reconstruction error over the recovered parameter trajectories. In the final Laplace run, grid search selected a three-layer GELU network with hidden widths (16 , 16 , 16) . By contrast, for the Gaussian noise specification, the final neural network architecture and training hyperparameters were tuned manually, as grid search failed to produce a satisfactory estimate of the parameter σ2. The final Gaussian run therefore uses a manually selected three-layer GELU network with hidden widths (16 , 32 , 16) .Figure 1 presents representative synthetic trajectories generated under Gaussian and Laplace noise. Figure 2 compares the true and estimated parameter functions. In the Gaussian case, we report the recovery of c(t), ϕ(t), and σ2(t), while in the Laplace case, we report the recovery of c(t), ϕ(t), and b(t).The neural network architectures are summarized in Table 1, and the corresponding recon-struction errors are reported in Table 2. Two hyperparameters are omitted from Table 1, as the same values were used in both cases ( ϕ constrained: no, smoothness: 0). In both cases, the selected model is relatively small, which indicates that accurate recovery of the time-dependent coefficients does not require a highly overparameterized network in the present synthetic setting. The numerical results show that the proposed method reconstructs the time-varying autore-gressive coefficient very accurately under both noise assumptions. The largest discrepancies are observed for the intercept and, in the Gaussian case, for the variance trajectory. Over-all, the recovered parameter curves remain close to the true ones, which confirms that the likelihood-based neural-network approach is capable of learning the time-dependent structure of the model in a stable and interpretable way.    

> (a) Gaussian noise (b) Laplace noise

Figure 1. Synthetic trajectories generated from the TVAR(1) model under Gaussian and Laplace noise. Noise Hidden layers Activation Optimizer Learning rate Batch size Epochs Gaussian (16 , 32 , 16) GELU Adam 10 −3 16 2500 Laplace (16 , 16 , 16) GELU AdamW 3 · 10 −3 100 (full) 2500 

Table 1. Architectures and training hyperparameters used in the synthetic experiments. Table 1 presents the architectures used for both noise specifications. In both cases, relatively small feedforward networks with three hidden layers and GELU activation were employed. The NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 15                 

> (a) Gaussian noise – c(t)(b) Gaussian noise – ϕ(t)(c) Gaussian noise – σ2(t)
> (d) Laplace noise – c(t)(e) Laplace noise – ϕ(t)(f) Laplace noise – b(t)

Figure 2. True and estimated time-dependent parameters in the synthetic experiments. Panels (A)–(C) correspond to the Gaussian case, whereas panels (D)–(F) correspond to the Laplace case. Noise MSE( c) MSE( ϕ) MSE( scale ) MSE mean 

Gaussian 0.0320 0.00315 0.0954 0.0435 

Laplace 0.0741 0.00142 0.0252 0.0336 

Table 2. Parameter reconstruction errors in the synthetic experiments. In the Gaussian case, the scale parameter is σ2(t), while in the Laplace case it is b(t).The quantity MSE mean denotes the arithmetic mean of the three parameter-wise MSE values. main differences lie in the hidden-layer widths, the optimizer, the batch size, and the method of selecting the final architecture: grid search for Laplace and manual tuning for Gaussian. Table 2 confirms that the proposed method reconstructs the autoregressive coefficient very accurately in both cases, with MSE( ϕ) of order 10 −3. In the Gaussian case, the dominant con-tribution to the total reconstruction error comes from the variance trajectory σ2(t), whereas in the Laplace case the scale parameter b(t) is recovered much more accurately. As a consequence, the overall average reconstruction error is smaller in the Laplace experiment. 4.2. Numerical experiments on real data. We next consider real data given by electricity spot prices; see Figure 3. The data are taken from the hourly electricity spot-price dataset for Denmark and neighboring countries available at https://www.kaggle.com/datasets/ arashnic/electricity-spot-price . To obtain a univariate daily time series, we fix one price series and retain one observation per day at the same hour throughout the experiment. The resulting observations are ordered chronologically, and the same preprocessing rule is used for all four fitted models. In contrast to the synthetic setting, the true parameter functions are not known; therefore, the numerical study focuses on the interpretability of the recovered parameter trajectories, the quality of trajectories generated by the fitted models, and the resulting illustrative forecasting performance for the selected train-test splits. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 16 

We report results for two training-set sizes: 81 and 995 observations. For each sample size, we estimate the time-dependent intercept c(t), autoregressive coefficient ϕ(t), and the corresponding scale parameter, namely σ2(t) in the Gaussian case and b(t) in the Laplace case. The fitted networks estimate Θ over a wider horizon; however, only the estimates bΘ( N +1) and 

bΘ( N +2) are used for the two-step-ahead recursive forecasting procedure. In order to make the comparison between the two noise specifications transparent, the network architectures were chosen manually and fixed within each sample-size regime based on preliminary experiments. The resulting architectures are summarized in Table 3. Figure 4 shows the two data windows used in the real-data experiments. Figures 5 and 6 display the estimated time-dependent parameter trajectories for the two training-set sizes. In both cases, the estimated coefficients vary substantially over time, which supports the use of a nonstationary autoregressive specification. 

Figure 3. First 5000 records from the dataset used for numerical experiments on real data: energy spot prices in Denmark and neighboring countries (source: 

https://www.kaggle.com/datasets/arashnic/electricity-spot-price ). 

Figure 4. Data windows used in the numerical experiments on real data: the upper panel corresponds to the training window of length 81, while the lower panel corresponds to the training window of length 995. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 17 

Case Hidden layers Activations Optimizer Epochs Gaussian, 81 (20 , 50 , 20) swish / softplus / GELU RMSprop 1000 Laplace, 81 (20 , 50 , 20) swish / softplus / GELU RMSprop 1000 Gaussian, 995 (25 , 50 , 25) swish / softplus / GELU RMSprop 1000 Laplace, 995 (25 , 50 , 25) swish / softplus / GELU RMSprop 1000 

Table 3. Network architectures used in the real-data experiments. The ar-chitectures were selected manually and then kept fixed within each sample-size setting. 

(a) Gaussian noise – c(t) (b) Gaussian noise – ϕ(t) (c) Gaussian noise – σ2(t)

(d) Laplace noise – c(t) (e) Laplace noise – ϕ(t) (f) Laplace noise – b(t)

Figure 5. Estimated time-dependent parameters for the real-data experiment with training set consisting of 81 observations. 

(a) Gaussian noise – c(t) (b) Gaussian noise – ϕ(t) (c) Gaussian noise – σ2(t)

(d) Laplace noise – c(t) (e) Laplace noise – ϕ(t) (f) Laplace noise – b(t)

Figure 6. Estimated time-dependent parameters for the real-data experiment with training set consisting of 995 observations. To assess whether the fitted models reproduce the global behavior of the observed series, we additionally compare the observed trajectory with a trajectory simulated from the estimated time-dependent parameters. Since the longer training window provides a more informative setting, this comparison is reported only for the case of 995 observations; see Figure 7. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 18   

> (a) Gaussian model – actual vs simulated
> (b) Laplace model – actual vs simulated

Figure 7. Observed and simulated trajectories for the real-data experiment with training set consisting of 995 observations. Finally, Table 4 reports the absolute and relative forecast errors for one-step-ahead and two-step-ahead predictions. The relative error for horizon h ∈ { 1, 2} is computed as 

rel .err .N +h = |yN +h − ¯yN +h||yN +h| + 10 −12 .

Case Noise 1-step abs. 1-step rel. 2-step abs. 2-step rel. 81 observations Gaussian 3.35 0.0612 68.62 0.5232 81 observations Laplace 3.18 0.0581 59.75 0.4555 995 observations Gaussian 38.83 0.1874 39.03 0.1794 995 observations Laplace 35.96 0.1735 15.16 0.0697 

Table 4. Absolute and relative forecasting errors for one-step-ahead and two-step-ahead predictions in the real-data experiments. For the particular train–test splits considered here, the results suggest that the choice of the training-window length has a more pronounced effect on forecast accuracy than the assumed noise distribution. The comparison between Gaussian and Laplace noise should therefore be interpreted as illustrative and specific to these splits. A comprehensive forecasting evaluation would require a rolling or expanding-window backtest, together with comparisons against standard baseline models. 5. Key implementation details for the numerical experiments 

In this section, we reproduce the main implementation components used in the numerical experiments. The code follows the likelihood-based formulation introduced in Section 2 and NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 19 

is kept consistent with the publicly available notebooks. The losses below are written for ordered contiguous observations: when evaluated on the full ordered series, they coincide with the conditional Gaussian or Laplace negative log-likelihoods, up to constants. When smaller ordered mini-batches are used, the same formulas are optimized on contiguous sub-blocks and should be understood as a mini-batch approximation to the full conditional likelihood. In particular, the temporal ordering of the observations is preserved throughout the optimization procedure. 5.1. Gaussian loss. In the Gaussian case, the network outputs the triplet 



ˆc(t), ˆϕ(t), ln  ˆσ2(t)

,

and the loss is given by the Gaussian negative log-likelihood, up to an additive constant. The following implementation was used:  

> 1

def gaussian_loss3 ( y_true , y_pred ) :  

> 2

N = tf . shape ( y_true ) [0] - 1 

> 3

y_true_1 = tf . slice ( y_true , [1 , 0] , [N , 1])  

> 4

y_true_2 = tf . slice ( y_true , [0 , 0] , [N , 1])  

> 56

c = tf . slice ( y_pred , [1 , 0] , [N , 1])  

> 7

phi = tf . slice ( y_pred , [1 , 1] , [N , 1])  

> 8

ln_sigma_squared = tf . slice ( y_pred , [1 , 2] , [N , 1])  

> 9

sigma_squared = tf . exp ( ln_sigma_squared )  

> 10 11

a = tf . square ( y_true_1 - c - phi * y_true_2 )  

> 12

b = a / sigma_squared  

> 13 14

loss = tf . reduce_sum ( ln_sigma_squared + b , axis =0)  

> 15

return loss / 2.0 

Listing 1. Gaussian loss used in the synthetic and real-data experiments. For a fully ordered batch, this is precisely the conditional Gaussian likelihood for the TVAR(1) model, written in terms of the network outputs. For smaller ordered mini-batches, it provides the corresponding blockwise approximation. 5.2. Laplace loss. In the Laplace case, the network outputs the triplet 



ˆc(t), ˆϕ(t), ln 

ˆb(t)

 

,

and the loss corresponds to the Laplace negative log-likelihood:  

> 1

def laplace_loss3 ( y_true , y_pred ) :  

> 2

N = tf . shape ( y_true ) [0] - 1 

> 3

y_true_1 = tf . slice ( y_true , [1 , 0] , [N , 1])  

> 4

y_true_2 = tf . slice ( y_true , [0 , 0] , [N , 1])  

> 56

c = tf . slice ( y_pred , [1 , 0] , [N , 1])  

> 7

phi = tf . slice ( y_pred , [1 , 1] , [N , 1])  

> 8

ln_b = tf . slice ( y_pred , [1 , 2] , [N , 1])  

> 9

b = tf . exp ( ln_b )  

> 10 11

a1 = tf . abs ( y_true_1 - c - phi * y_true_2 )  

> 12

a2 = a1 / b         

> 13 NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 20
> 14 loss =tf . reduce_sum ( ln_b +a2 , axis =0)
> 15 return loss

Listing 2. Laplace loss used in the real-data experiments. Hence, the two noise specifications are treated analogously, with the only difference being the assumed conditional distribution of the innovation term. For experiments on real-world data, the TensorFlow implementation shown in Listing 2 is used directly. For synthetic-data experiments, including grid search with multistart initial-ization, we instead use the following PyTorch implementation of the same Laplace negative log-likelihood, with additional numerical stabilization and an optional smoothness regulariza-tion term:                                                                           

> 1def laplace_ar1_loss ( y_true , y_pred , * , start_idx =1 , lam =0.0 , eps =1 e -12) :
> 2y_prev =y_true [ start_idx -1:-1 , 0]
> 3y_curr =y_true [ start_idx : , 0]
> 45c=y_pred [ start_idx : , 0]
> 6phi =y_pred [ start_idx : , 1]
> 7log_b =y_pred [ start_idx : , 2]
> 8b=torch . exp ( log_b ) . clamp_min ( eps )
> 910 resid =y_curr -c-phi *y_prev
> 11 base =torch . sum ( torch . log ( b ) +torch . abs ( resid ) /b )
> 12 13 if lam >0.0:
> 14 diffs =y_pred [ start_idx : , :] -y_pred [ start_idx -1:-1 , :]
> 15 base =base +lam *torch . mean ( diffs **2)
> 16 return base

Listing 3. Laplace loss used in the synthetic-data experiments. 5.3. Network architecture. The parameter functions are approximated by a feedforward neural network with three outputs corresponding to the time-dependent intercept, autoregres-sive coefficient, and scale parameter. A representative implementation is given below:                    

> 1def build_model ( hidden_units , activations ) :
> 2model =Sequential ()
> 3model . add ( Input ( shape =(1 ,) ) )
> 4for h , act in zip ( hidden_units , activations ) :
> 5model . add ( Dense (h , activation = act ) )
> 6model . add ( Dense (3 , activation = ’ linear ’) )
> 7return model

Listing 4. Feedforward neural network architecture for parameterizing the time-dependent coefficients. The final hidden-layer widths and activations were selected separately for each experiment, either by grid search or manual tuning, as described above. 5.4. Recursive forecasting. For the real-data experiments, the fitted parameter trajectories are used directly in recursive forecasting. In the Gaussian case, both the forecast mean and the forecast variance are propagated recursively: NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 21  

> 1

def gauss ian_f oreca st_re cursi ve ( fitted_df , start , horizon , alpha ) :  

> 2

rows = []  

> 3

y_prev = float ( fitted_df . iloc [ start - 1][ " y " ])  

> 4

mean_prev = None  

> 5

var_prev = None  

> 6

q = float ( norm . ppf ((1.0 + alpha ) / 2.0) )  

> 78

for step in range (1 , horizon + 1) :  

> 9

idx = start + step - 1 

> 10

c = float ( fitted_df . iloc [ idx ][ " c_hat " ])  

> 11

phi = float ( fitted_df . iloc [ idx ][ " phi_hat " ])  

> 12

sigma2 = float ( fitted_df . iloc [ idx ][ " sigma2_hat " ])  

> 13 14

if step == 1:  

> 15

mean = c + phi * y_prev  

> 16

var = sigma2  

> 17

else :  

> 18

mean = c + phi * float ( mean_prev )  

> 19

var = phi * phi * float ( var_prev ) + sigma2  

> 20 21

rows . append ({  

> 22

" step " : step ,  

> 23

" forecast_mean " : mean ,  

> 24

" interval_lower " : mean - q * math . sqrt ( var ) ,  

> 25

" interval_upper " : mean + q * math . sqrt ( var ) ,  

> 26

" actual_y " : float ( fitted_df . iloc [ idx ][ " y " ]) ,  

> 27

})  

> 28

mean_prev , var_prev = mean , var  

> 29

return pd . DataFrame ( rows ) 

Listing 5. Recursive Gaussian forecast used in the real-data experiments. In the Laplace case, the forecast mean is propagated recursively as well, whereas the pre-diction interval is constructed using the one-step and two-step formulas derived in Section 3:  

> 1

def la place_forecast_r ecursive ( fitted_df , start , horizon , alpha ) :  

> 2

rows = []  

> 3

y_prev = float ( fitted_df . iloc [ start - 1][ " y " ])  

> 4

means = []  

> 56

for step in range (1 , horizon + 1) :  

> 7

idx = start + step - 1 

> 8

c = float ( fitted_df . iloc [ idx ][ " c_hat " ])  

> 9

phi = float ( fitted_df . iloc [ idx ][ " phi_hat " ])  

> 10

b = float ( fitted_df . iloc [ idx ][ " b_hat " ])  

> 11 12

if step == 1:  

> 13

mean = c + phi * y_prev  

> 14

radius = -b * math . log (1.0 - alpha )  

> 15

else :  

> 16

mean = c + phi * means [ -1]  

> 17

if step == 2:  

> 18

a = abs ( phi ) * float ( fitted_df . iloc [ start ][ " b_hat " ])  

> 19

radius = laplace_quantile_two_step ( a =a , c =b , alpha = alpha ) NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 22  

> 20

else :  

> 21

radius = np . nan  

> 22 23

rows . append ({  

> 24

" step " : step ,  

> 25

" forecast_mean " : mean ,  

> 26

" interval_lower " : mean - radius if np . isfinite ( radius ) else np . nan ,  

> 27

" interval_upper " : mean + radius if np . isfinite ( radius ) else np . nan ,  

> 28

" actual_y " : float ( fitted_df . iloc [ idx ][ " y " ]) ,  

> 29

})  

> 30

means . append ( mean )  

> 31

return pd . DataFrame ( rows ) 

Listing 6. Recursive Laplace forecast used in the real-data experiments. 5.5. Learning schedule and ordered training. In the real-data experiments, the constant learning rate was replaced with a decreasing learning rate, which led to improved results. Moreover, the optimization was performed with shuffle=False , so that every mini-batch contains contiguous observations in chronological order:  

> 1

def make_lr_schedule ( C =1000 , scale =1.0) :  

> 2

def schedule ( epoch ) :  

> 3

return scale / ( epoch + C )  

> 4

return schedule  

> 56

C = 1000 if noise == " laplace " else 100  

> 7

callbacks = [ LearningRateScheduler ( make_lr_schedule ( C =C , scale =1.0) ) ]  

> 89

batch_size = len ( x_train ) if noise == " gaussian " and data_key == " 81 " else 16  

> 10 11

history = model . fit (  

> 12

x_train ,  

> 13

y_train ,  

> 14

batch_size = batch_size ,  

> 15

epochs =1000 ,  

> 16

verbose =0 ,  

> 17

shuffle = False ,  

> 18

callbacks = callbacks  

> 19

)

Listing 7. Training protocol used in the final experiments. When batch_size=len(x_train) , the loss is the full ordered conditional likelihood. When 

batch_size=16 , the same loss is evaluated on ordered contiguous sub-blocks and therefore acts as a mini-batch approximation to the full-series likelihood; transitions crossing mini-batch boundaries are not included in a given mini-batch evaluation. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 23 

6. Conclusion and future work 

We proposed a neural-network-based likelihood framework for estimating time-dependent parameters in autoregressive models. The approach combines the interpretability of a struc-tured stochastic model with the flexibility of data-driven function approximation, allowing relatively simple AR models to capture complex nonstationary behavior. The analysis covered both Gaussian and Laplace noise specifications, together with recursive prediction formulas for the TVAR(1) case. In particular, we derived explicit prediction inter-vals in the Gaussian setting and tractable conditional error distributions in the Laplace setting. For k = 1 , the prediction intervals admit closed-form expressions, whereas for k ≥ 2 they do not admit closed-form solutions and are obtained as the unique root of a one-dimensional nonlinear equation. The numerical results on synthetic and real data indicate that the proposed method is ca-pable of recovering meaningful parameter trajectories and producing interpretable illustrative forecasts under different noise assumptions. Possible directions for future work include considering more general noise families such as the Normal–Laplace distribution, carrying out a broader rolling-window forecasting evaluation with standard baseline models, and studying multi-step prediction for general TVAR( p) mod-els. Another natural direction is the investigation of theoretical properties of the estimation procedure and its extension to multivariate autoregressive models. 

Appendix A. Auxiliary calculations for the two-step-ahead prediction interval in the Laplace case 

In this appendix, we derive the distribution of the two-step-ahead prediction error in the TVAR(1) model with Laplace noise. Let 

eN +2 = ˆΦ( N + 2) ˆb(N + 1) ξN +1 + ˆb(N + 2) ξN +2 ,

where 

ξN +1 , ξ N +2 i.i.d. 

∼ Laplace(0 , 1) .

Set 

a := | ˆΦ( N + 2) |ˆb(N + 1) , c := ˆb(N + 2) .

Then, conditionally on the available information up to time N ,

eN +2 d

= X1 + X2,

where 

X1 ∼ Laplace(0 , a ), X2 ∼ Laplace(0 , c ),

and X1, X 2 are independent. A.1. Characteristic function and density. For a random variable X ∼ Laplace(0 , s ), the characteristic function is 

φX (t) = 11 + s2t2 .

Hence, 

φeN +2 (t) = φX1 (t)φX2 (t) = 1(1 + a2t2)(1 + c2t2) .

Assume first that a̸ = c. We use the decomposition 

1(1 + a2t2)(1 + c2t2) = A

1 + a2t2 + B

1 + c2t2 .NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 24 

Matching coefficients gives 

A = a2

a2 − c2 , B = − c2

a2 − c2 .

Therefore, 

φeN +2 (t) = a2

a2 − c2 · 11 + a2t2 − c2

a2 − c2 · 11 + c2t2 .

Using the inverse Fourier transform and the identity 

F−1

 11 + s2t2



(x) = 12s e−| x|/s ,

we obtain 

feN +2 (x) = a

2( a2 − c2) e−| x|/a − c

2( a2 − c2) e−| x|/c , x ∈ R.

Equivalently, 

feN +2 (x) = a e −| x|/a − c e −| x|/c 

2( a2 − c2) .

A.2. The case a = c. If a = c, we take the limit in the previous expression: 

feN +2 (x) = lim 

> u→a

u e −| x|/u − a e −| x|/a 

2( u2 − a2) .

Writing 

g(u) := u e −| x|/u ,

we have 

g′(u) = e−| x|/u 



1 + |x|

u



.

Hence, 

feN +2 (x) = 14a e−| x|/a 



1 + |x|

a



, x ∈ R.

A.3. Distribution function and quantile equation. Assume first that a̸ = c. Since the density is symmetric, for t ≥ 0 we obtain 

FeN +2 (t) = 1 − a2

2( a2 − c2) e−t/a + c2

2( a2 − c2) e−t/c ,

while for t < 0,

FeN +2 (t) = a2

2( a2 − c2) et/a − c2

2( a2 − c2) et/c .

Thus, for q ≥ 0,

P(|eN +2 | ≤ q) = FeN +2 (q) − FeN +2 (−q) = 1 − a2e−q/a − c2e−q/c 

a2 − c2 .

Therefore, the radius q(2)  

> α

of the two-step-ahead prediction interval is determined by 

a2e−q(2)   

> α/a

− c2e−q(2)   

> α/c

= (1 − α)( a2 − c2).

In the case a = c, the density is 

feN +2 (x) = 14a e−| x|/a 



1 + |x|

a



,

which yields 

P(|eN +2 | ≤ q) = 1 − e−q/a 

1 + q

2a



, q ≥ 0.NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 25 

Hence, q(2)  

> α

satisfies 

e−q(2)   

> α/a

1 + q(2) 

> α

2a

!

= 1 − α. 

Appendix B. Auxiliary lemmas for multi-step prediction intervals under Laplace innovations 

Lemma 2. Let ξ ∼ Laplace(0 , 1) , i.e. fξ(x) = 12 e−| x|. Then, for all t ∈ R,

Eeitξ  = 11 + t2 . (16) 

More generally, if s > 0 and X := sξ ∼ Laplace(0 , s ), then E[eitX ] = 1 /(1 + s2t2).Proof. The characteristic function of the Laplace distribution is given in Kotz et al. [7, Sec-tion 2.1.2, Eq. (2.1.8)]. □

Lemma 3. Let s1, . . . , s k > 0 be pairwise distinct. Define the rational function 

R(u) := 

> k

Y

> j=1

11 + s2 

> j

u , u ∈ C \  − 1/s 21, . . . , −1/s 2 

> k

.

Then 

R(u) = 

> k

X

> j=1

Aj

1 + s2 

> j

u , (17) 

where 

Aj = Y

> ℓ∈{ 1,...,k }\{ j}

s2

> j

s2 

> j

− s2

> ℓ

, j ∈ { 1, . . . , k }, (18) 

and Pkj=1 Aj = 1 .Proof. Since the sj are pairwise distinct, the numbers −1/s 2 

> j

are pairwise distinct and hence 

R(u) has only simple poles at u = −1/s 2 

> j

. Therefore R(u) admits a partial fraction decompo-sition with simple terms of the form Aj /(1 + s2 

> j

u). Uniqueness follows because the functions 

u 7 → 1/(1 + s2 

> j

u) have distinct poles, hence they are linearly independent over C.Multiply both sides of (17) by (1 + s2 

> j

u) and then evaluate at the pole location u = −1/s 2 

> j

.On the left-hand side, 

lim 

> u→− 1/s 2
> j

(1 + s2 

> j

u)R(u) = lim 

> u→− 1/s 2
> j

Y

> ℓ̸=j

11 + s2 

> ℓ

u = Y

> ℓ̸=j

11 − s2 

> ℓ

/s 2

> j

= Y

> ℓ̸=j

s2

> j

s2 

> j

− s2

> ℓ

.

On the right-hand side of (17), we obtain 

lim 

> u→− 1/s 2
> j

(1 + s2 

> j

u)

> k

X

> m=1

Am

1 + s2

> m

u = lim 

> u→− 1/s 2
> j

Aj + X

> m̸=j

Am

1 + s2 

> j

u

1 + s2

> m

u

!

= Aj ,

because for m̸ = j the factor (1 + s2 

> j

u) vanishes at u = −1/s 2 

> j

while the denominator remains nonzero. Equating the two limits yields (18). Set u = 0 in (17). Since R(0) = 1 , we obtain 1 = Pkj=1 Aj . □

The following result is well-known. NEURAL NETWORK ESTIMATION OF TIME-DEPENDENT AR( p) PARAMETERS 26 

Lemma 4 (Inverse Fourier transform of (1 + s2t2)−1). Let s > 0. Define 

gs(x) := 12π

Z ∞−∞ 

e−itx 

1 + s2t2 dt, x ∈ R. (19) 

Then 

gs(x) = 12s exp 



− |x|

s



, x ∈ R. (20) 

In particular, gs is the density of Laplace(0 , s ).

References 

[1] R. Dahlhaus , Fitting time series models to nonstationary processes , The Annals of Statistics, 25 (1997), pp. 1–37. [2] J. Durbin and S. J. Koopman , Time Series Analysis by State Space Methods , Oxford Statistical Science Series, Oxford University Press, Oxford, 2 ed., 2012. [3] O. Dürr, B. Sick, and E. Murina , Probabilistic Deep Learning: With Python, Keras and TensorFlow Probability , Manning Publications, Shelter Island, NY, 2020. [4] A. Kałuża, P. M. Morkisz, B. Mulewicz, P. Przybyłowicz, and M. Wiącek , Deep learning-based estimation of time-dependent parameters in Markov models with application to nonlinear regression and SDEs , Applied Mathematics and Computation, 480 (2024), p. 128906. [5] G. Kitagawa and W. Gersch , Smoothness Priors Analysis of Time Series , vol. 116 of Lecture Notes in Statistics, Springer, New York, 1996. [6] L. A. Klimko and P. I. Nelson , On conditional least squares estimation for stochastic processes , The Annals of Statistics, 6 (1978), pp. 629–642. [7] S. Kotz, T. J. Kozubowski, and K. Podgórski , The Laplace Distribution and Generalizations: A Re-visit with Applications to Communications, Economics, Engineering, and Finance , vol. 183 of Progress in Mathematics, Birkhäuser, Boston, 2001. [8] X. Li and J. Yuan , DeepTVAR: Deep learning for a time-varying VAR model with extension to integrated VAR , International Journal of Forecasting, 40 (2024), pp. 1123–1133. [9] M. Nongni Donfack and A. Dufays , Modeling time-varying parameters using artificial neural net-works: A GARCH illustration , Studies in Nonlinear Dynamics & Econometrics, 25 (2021), pp. 311–343. [10] R. Prado and M. West , Time Series: Modeling, Computation, and Inference , Chapman & Hall/CRC Texts in Statistical Science, CRC Press, Boca Raton, 2010. [11] D. Salinas, V. Flunkert, J. Gasthaus, and T. Januschowski , DeepAR: Probabilistic forecasting with autoregressive recurrent networks , International Journal of Forecasting, 36 (2020), pp. 1181–1191. [12] D. Tran, M. W. Dusenberry, M. van der Wilk, and D. Hafner , Bayesian layers: A module for neural network uncertainty , in Advances in Neural Information Processing Systems 32, 2019, pp. 14660– 14672. [13] R. Weron , Electricity price forecasting: A review of the state-of-the-art with a look into the future ,International Journal of Forecasting, 30 (2014), pp. 1030–1081. [14] G. P. Zhang , Time series forecasting using a hybrid ARIMA and neural network model , Neurocomputing, 50 (2003), pp. 159–175. 

AGH University of Krakow, Faculty of Applied Mathematics, Al. A. Mickiewicza 30, 30-059 Kraków, Poland 

Email address : agkopec@agh.edu.pl, corresponding author 

AGH University of Krakow, Faculty of Applied Mathematics, Al. A. Mickiewicza 30, 30-059 Kraków, Poland 

Email address : pprzybyl@agh.edu.pl 

AGH University of Krakow, Faculty of Applied Mathematics, Al. A. Mickiewicza 30, 30-059 Kraków, Poland 

Email address : martynawiacek@agh.edu.pl