Title: Width-Robust Learnability in Mean-Field Bayesian Neural Networks

URL Source: https://arxiv.org/pdf/2607.05735v1

Published Time: Wed, 08 Jul 2026 00:21:30 GMT

Number of Pages: 36

Markdown Content:
# Width-Robust Learnability in Mean-Field Bayesian Neural Networks 

Dmitry Vaintrob Principles of Intelligence Kaarel Hänni June 2026 

Abstract 

Infinite-width limits are a standard way to reason about neural networks, but it is not automatic that the limiting learner has the same complexity-theoretic inductive bias as large finite networks. We study this question for Bayesian neural networks at the mean-field, or critical feature-learning, scaling. The central quantity is the reduced entropy 

s∞(y, ε ) = lim sup 

> N

− 1

N log π0 

> N

(L ≤ ε),

the intensive prior cost of representing a target function y to population mean-squared error 

ε.Our main result is a width-robust learnability theorem. At fixed depth, a family of Boolean-cube targets is learnable from polynomially many samples at infinite width if and only if it is learnable at polynomial width, if and only if its reduced entropy is polynomially bounded. Equivalently, up to polynomial slack in accuracy, the Bayesian mean-field learner generalizes exactly on the targets that can be represented by polynomial-size networks. The forward direction is proved by a form of subsampling: from the infinitely many hidden neurons in the mean-field solution, one can select polynomially many representa-tives and still preserve the learned function on every input simultaneously. At the critical scaling this subsampling has both an “active” component, which keeps the data-dependent low-dimensional statistics, and a “lazy” component, which resamples the entropy-dominated directions from the prior. Thus the infinite-width mean-field limit gives a clean analytic description of learning without introducing spurious width-dependent generalization power. 

# Contents 

1 Introduction 22 Setup 6

2.1 Model and prior . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 62.2 Learning target and Bayesian MSE conditioning . . . . . . . . . . . . . . . . . . . 7

3 Main results 84 The mean-field limit and its consequences 9

4.1 The infinite-width limit . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 94.2 First consequences of finite reduced entropy . . . . . . . . . . . . . . . . . . . . . 10 4.3 Spectral split . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 1arXiv:2607.05735v1 [stat.ML] 7 Jul 2026 5 Pointwise compression 12 

5.1 The lazy-swap lemma . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 12 5.2 The construction and the compression theorem . . . . . . . . . . . . . . . . . . . 13 

6 Conservation by cloning 14 7 Learnability 15 

7.1 Low reduced entropy implies Gibbs learnability . . . . . . . . . . . . . . . . . . . 15 7.2 The converse: Gibbs learnability implies low reduced entropy . . . . . . . . . . . 18 7.3 Assembly of the equivalence . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 

8 Discussion 19 A Regularity from the entropy budget 21 

A.1 The adaptive-temperature Gibbs law . . . . . . . . . . . . . . . . . . . . . . . . . 21 A.2 Entropy consequences . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 22 A.3 The suffix invariant and the one-step lazy-score bound . . . . . . . . . . . . . . . 23 A.4 The regularity package . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 26 

B Pointwise compression 26 

B.1 The active–lazy split . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 27 B.2 The lazy swap . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 27 B.3 The lazy-Gram bound . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 B.4 Finite sampled features: active reconstruction and fresh chaos . . . . . . . . . . . 28 B.5 Normalized-channel Lipschitz induction . . . . . . . . . . . . . . . . . . . . . . . 29 B.6 One right-to-left step . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 30 B.7 Backward induction . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 31 

C Conservation: the cloning construction 31 

C.1 Construction . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 32 C.2 Deviation propagation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 32 C.3 Entropy and change of measure . . . . . . . . . . . . . . . . . . . . . . . . . . . . 33 

D Mean-field vacua in the full field space 33 

# 1 Introduction 

A wide neural network generalizes far better than its parameter count would suggest, because its prior places more mass on simple, structured functions than on complex ones. Making this precise means identifying the inductive bias of the model: the complexity ordering on target functions induced, through Bayesian conditioning, by the architecture together with the weight prior. For a target y the natural complexity parameter is the prior log-probability that the network fits y to a given accuracy — the harder y is to represent, the smaller this probability and the larger the complexity. We study this complexity, and the learnability it controls, in the overparameterized regime where the induced function-space bias is cleanest. 2γ

## mean field 

γ < 1 γ = 1 γ > 1 

> active /lazy

Figure 1: The spectrum across scalings. The same decaying feature-kernel spectrum, re-coloured by regime. At γ < 1 every direction is lazy (brown) and the learner is a fixed kernel (NTK/NNGP); at the mean-field scaling γ = 1 a few top directions become active (blue) above a lazy bulk; at γ > 1 the lazy bulk falls below the accuracy threshold and only active directions remain. This paper treats the critical scaling γ = 1 .

Which infinite width? Overparameterization is usually idealized by an infinite-width limit, but there are several such limits and they disagree about complexity. In the lazy (NTK/NNGP) scaling the network behaves like a fixed kernel method [10, 12], with the inductive bias of a Gaussian process; such methods provably require super-polynomially many samples on simple structured targets such as parities [7, 1], even though those targets are computed by small networks. The mean-field or critical rich scaling ( γ = 1 ) [14, 4, 2, 23] keeps a different part of the finite network alive. The output is still an average over many hidden neurons, but the distribution of those neurons is allowed to change after seeing data. Thus the infinite-width object is not a frozen kernel, but a law describing the typical learned hidden neuron. This limit is widely expected to recover a more circuit-like complexity ordering. What has been missing is a generalization theory for it — and, just as importantly, an argument that the infinite-width limit says anything about the finite widths one actually uses. 

The reduced entropy. We make the complexity measure explicit. At the mean-field scaling a width-N network has prior probability π0 

> N

(L ≤ ε) of achieving population mean-squared error at most ε on y; this probability decays like e−N s , and the intensive rate 

s∞(y, ε ) = lim sup  

> N→∞



− 1 

> N

log π0 

> N

(L ≤ ε)



is the reduced entropy — a width-independent complexity of y relative to the prior. It is the Bayesian/PAC-Bayes analogue of description length: the number of nats per neuron the prior must spend to put mass near the target. It is finite for every y (memorization gives s∞ = O(2 d)); the content is which targets have polynomial reduced entropy. 

Main result: learnability is width-robust. Our main result is that at γ = 1 the reduced en-tropy governs learnability, and learnability is the same at infinite width and at polynomial width. Informally (Theorem 3.1): for a family of targets (yd)d and fixed depth, the following are equiv-alent — learnable at infinite width from polynomially many samples; learnable at polynomial width from polynomially many samples; computed to o(1) mean-squared error by a polynomial-width network; and s∞(yd, ε ) polynomial in d. In a sentence: a target is learnable at infinite width from polynomial data if and only if it is computed to o(1) error by a polynomial-width network. 

3This can be read as a complexity-theoretic sanity check on the mean-field limit. The infinite-width equations replace a large finite network by a cleaner object: instead of tracking every neuron, one tracks the probability law of a typical learned neuron, or equivalently a system of self-consistent kernels and tilts. The theorem says that this cleaner object has not acquired an artificial computational advantage merely from having infinitely many neurons available. Infinite width changes the analytic description of learning, but not the class of targets learned with polynomial sample complexity. The mechanisms visible in the mean-field equations are therefore not artifacts of an infinite resource: any low-entropy mean-field solution has a polynomial-width realization with the same predictions, up to polynomial slack. 

Generalization is width-robust, and the witness is pointwise. The equivalence is, at heart, a robustness statement: the population-ε generalization behavior of γ = 1 mean-field Bayesian learning is insensitive to width. Exactly the same targets are learnable at polynomial and at infinite width, up to the standard polynomial slack in the accuracy thresholds, width, and sample size (Theorem 3.2). What we prove is stronger than a coincidence of risk values. For any low-entropy infinite-width Gibbs solution achieving ε accuracy, Theorem 3.2 produces a single polynomial-width network agreeing with it at every input simultaneously , sup x |fˆθ(x) − f∞(x)| ≤ δ, and hence matching the target in mean square at the same accuracy, up to the polynomial slack. So the function learned at infinite width is not merely matched in mean square by a finite-width learner — it is reproduced pointwise. The width-robustness of generalization is witnessed by an explicit pointwise agreement between the infinite- and polynomial-width learned functions, rather than inferred from two separately bounded risks. 

Relation to prior work. The result is closest in spirit to recent work showing that overparam-eterized neural networks can have a function-space simplicity bias not visible from parameter counting alone. Buzaglo, Harel, Nacson, Brutzkus, Srebro, and Soudry [3] show that typical random interpolating networks drawn from an apparently flat parameter prior generalize from narrow teachers, because redundancy in the parameterization induces a non-uniform prior over functions. Daniely and Granot’s approximate-description-length framework gives another route to weakly width-dependent or width-independent sample-complexity bounds for norm-controlled neural networks [5, 6]. Our contribution is complementary: rather than bounding a finite class di-rectly, we identify the intensive posterior entropy of an infinite-width mean-field Bayesian learner and prove that low-entropy infinite-width solutions compress to polynomial width. The mean-field side of the paper is connected to the growing literature on rich infinite-width limits and adaptive kernels, including Tensor Programs/ μP [23], self-consistent GP and DMFT descriptions of learned representations [15, 2, 21], multi-scale adaptive-kernel theories [19, 11], and recent work of Ringel and collaborators on sample/width scales and algorithmic capture in rich regimes [18, 8]. The lecture notes [9] give a broad Bayesian/statistical-physics account of the path from Gaussian processes to adaptive learned kernels. We also share language with recent work of Zavatone-Veth, Bordelon, and Pehlevan [24], where subsampling and summary statistics are used to relate complicated learning dynamics to simpler effective descriptions. Our theorem gives a complementary complexity statement for Bayesian fully connected networks on the Boolean cube. Together with the companion analysis of the γ > 1 regime and the standard 

γ < 1 reduction to kernel learning, this informally closes the width-scaling loop for this model class: kernel behavior below the mean-field scale, critical mean-field behavior at γ = 1 , and no 4additional polynomial-sample learnability gained merely by passing to infinite width. 

The engine: subsampling. The forward direction is a version of subsampling. Starting from the infinite-width learned object, we want to keep only polynomially many neurons and still get the same function. If the neurons were ordinary independent samples from a fixed distribution, this would be a routine Monte Carlo statement. The difficulty is that the distribution is itself learned from the training data, and in principle the data could be spread thinly across very many weak directions. The proof shows that, for low reduced entropy solutions, this does not happen in a way that matters for the output. At the critical scaling γ = 1 this subsampling has two parts. The first is an active subsampling step: after projecting to the few directions where the posterior has substantial data-dependent content, we sample actual neurons and preserve their projected behavior. This is the same kind of subsampling that appears in the over-rich regime γ > 1 treated in the companion paper. The second is a lazy step: in the many remaining directions, we do not try to keep the realized hidden coordinates. Instead we replace them by fresh prior noise and prove, uniformly over all inputs, that the output barely changes. The comparison is a short Stein-type argument; see Lemma 5.1. This active/lazy split is not just a technicality. At γ = 1 the variational problem contains three terms of the same order: the empirical loss, the weight prior, and the ordinary entropy of the posterior over neurons. Active subsampling is the part needed to remember the low-dimensional statistics selected by the data and the weight prior. Lazy subsampling is the part needed to keep the entropy term honest: most hidden coordinates are not fixed by the data, but are resampled from the correct prior law. In the over-rich regime γ > 1, the entropy term becomes lower order and the active subsampling picture is more direct. In the lazy/kernel regime 

γ < 1, the weight-prior cost is lower order in the corresponding variational problem, only lazy hidden-layer terms remain, and the learner reduces to kernel learning. This subsampling mechanism drives both directions of the equivalence. Compression (the forward direction) turns a low-entropy infinite-width network into a genuinely small one: keep polynomially many representatives of the active behavior, resample the lazy behavior from the prior, and obtain a network whose width is polynomial in the input dimension d (with the degree set by the depth) that agrees with the original network at every input at once (Theorem 5.4). The converse asks the reverse question — must the reduced entropy be small whenever the target can already be computed by a small network? — and shows that it must. Suppose some network of modest width W, a “teacher”, already reaches the target accuracy. One can imitate it inside a much wider network by cloning : replace each of the teacher’s W neurons by many independent neurons whose weights sit near that neuron’s, up to small noise. Since there are only 

W neurons to imitate, this imitating distribution departs from the random-initialization prior in only a poly( W)-sized way, so it already places appreciable probability on the accurate region — and appreciable prior probability is exactly what small reduced entropy means (Theorem 6.1). Compression and cloning are inverse operations: a small network can be read off from low reduced entropy, and conversely a small network forces the reduced entropy to be low. The generalization bound and its converse then assemble these into the equivalence, with pointwise compression supplying the fixed, finite-dimensional class of functions that the uniform-convergence step relies on. 5λ0      

> active Vlazy C
> N→ ∞
> project to active fresh lazy chaos
> N0= poly

Figure 2: Pointwise compression at γ = 1 . Top: the feature-kernel spectrum splits at the cutoff λ0 into a low-dimensional active space V (dim V ≤ 1/λ 0) and a lazy bulk C (∥C∥op ≤ λ0). 

Bottom: every neuron of the infinite-width learned network carries an active and a lazy part. The construction subsamples a few neurons (red), projects them onto the active directions through the fixed top eigenfunctions {ψk} — reconstructing the active block with no O(1) bias — and then resamples the lazy part as fresh prior chaos N (0 , C ); the lazy-swap lemma (Lemma 5.1) prices the resampling at p√λ0 c⋆ per consumed evaluation. The result is a width-N0 = poly 

network agreeing with f∞ at every input simultaneously. 

Assumptions and scope. The model assumptions are mild: a bounded Lipschitz activation with biases satisfying a quantitative finite-set interpolation property, as holds for standard sig-moidal activations, and teachers measured by width and polynomial weight size. The mean-field limit itself — the convergence of the neuron distribution to a data-dependent law (propagation of chaos) — is the standard object of [14, 22, 16, 21, 20, 11], which we recall and cite rather than re-derive; the regularity properties our construction needs (moment and sensitivity bounds on the conditioned neuron law) are derived from the bounded activation and a finite reduced entropy, not posited. We fix the depth L, and all polynomial bounds carry exponents depending only on L. We do not claim that arbitrary finite training algorithms find the Gibbs posterior, nor that the constants are practical; the result is a width-robust characterization of the Bayesian mean-field inductive bias. This paper treats the infinite-width and polynomial-width regimes; the general finite-width regime and the over-rich scalings γ > 1 are the subject of a companion paper. 

# 2 Setup 

2.1 Model and prior 

We fix an input dimension d and the Boolean cube Bd = {± 1}d with the uniform measure μ;write F = L2(Bd, μ ) and view targets and network functions as elements of F. The target 

y : Bd → [−1, 1] is bounded, and d is fixed within a complexity picture (we never change it when varying the width). We use a fully connected architecture of fixed depth L (L weight matrices, L−1 hidden layers), 6with trainable biases, 1 hidden width N , and a single activation φ. Hidden pre/postactivations are ζℓ, z ℓ ∈ RN with z0 = x, zℓ = φ(ζℓ). We work at the mean-field (Mei–Montanari–Nguyen) exponent γ = 1 : hidden weights carry the standard μP normalization and the readout is an intensive 1/N average, 

ζ1 = W 1x+b1, ζℓ+1 = 1√N W ℓ+1 zℓ +bℓ+1 (1 ≤ ℓ ≤ L−2) , fθ(x) = 1

> NN

X

> i=1

αi φ ζL−1 

> i

(x),

(1) with i.i.d. Gaussian prior weights W 1 

> ij

∼ N (0 , 1/d ), W ℓij ∼ N (0 , 1) , biases bℓi ∼ N (0 , 1) , and readouts αi ∼ N (0 , 1) ; write π0 

> N

for this prior on θ. The exponent γ = 1 is the scaling at which the readout is a genuine population average and the limit is feature-learning rather than kernel (γ < 1 collapses to the NNGP/kernel regime, γ > 1 is a companion paper). Throughout we assume the activation φ is bounded ( |φ| ≤ 1), Lipschitz, and non-polynomial .Boundedness and the Lipschitz bound are what the compression analysis uses; non-polynomiality, together with the biases, is the classical condition under which the architecture is a universal approximator [13], and we invoke it only through the following interpolation bound. Teachers are measured by their width and the size of their weights: the complexity statements quantify over networks whose weights have polynomial size. 

Lemma 2.1 (Memorization) . Under the standing assumption, for any ε′ > 0, any n distinct points x1, . . . , x n ∈ Bd and targets t1, . . . , t n ∈ [−1, 1] , there is a one-hidden-layer network of width O(n) whose weights have size poly( n, d, 1/ε ′) and whose output is within ε′ of ti at each 

xi. Taking n = 2 d gives an ε′-accurate interpolant for every y : Bd → [−1, 1] at width O(2 d) with weights of size poly(2 d, 1/ε ′).Proof sketch. Pick a direction w with the n values w · xi distinct and separated by a gap 

ρ ≥ 1/poly( n, d ) (a generic, e.g. random, w of norm O(√d) works on the cube). Order them and write t(·) as a sum of n scaled increments. Each increment is realized to accuracy 

O(ε′/n ) by a single ridge feature φ(s(w · x + b)) whose transition scale resolves the gap, with 

s = poly(1 /ρ, n/ε ′). The non-polynomial hypothesis with biases is used only to guarantee this finite-set separation/approximation property; exact interpolation at ε′ = 0 for arbitrary non-polynomial Lipschitz activations is not needed anywhere below. The resulting weights are 

poly( n, d, 1/ε ′).

2.2 Learning target and Bayesian MSE conditioning 

The learning target is the population mean-squared error 

L(θ) = Ex∼μ

 fθ(x) − y(x)2.

We study Bayesian MSE conditioning : for an accuracy level ε > 0, the prior conditioned on the MSE ball, 

πε(y, N ) = π0

> N

  · | L(θ) ≤ ε , (2)                                               

> 1Biases are included only for convenience: for depth L≥2they are inessential whenever φis neither even nor odd. The condition is necessary — a bias-free network with even (resp. odd) φhas f(−x) = f(x)(resp.
> f(−x) = −f(x)) on Bd, and so cannot fit an antipodal pair {x, −x}with unrelated targets — and sufficient: projecting onto a generic direction amakes the values a·xjdistinct, and the dilates r7→φ(sr )(s∈R) of a non-polynomial φthat is neither even nor odd are linearly independent on {a·xj}, so suitable scales make
> [φ(sia·xj)] i,j nonsingular and Lemma 2.1 holds with all biases set to zero. We keep them only to streamline the exposition; the reader may safely ignore them.

7and the (negative) reduced entropy at γ = 1 together with its infinite-width limit, 

sN (y, ε ) = − 1 

> N

log π0

> N

 L(θ) ≤ ε, s∞(y, ε ) = lim sup  

> N→∞

sN (y, ε ). (3) Equivalently N s N (y, ε ) = DKL 

 πε(y, N ) ∥ π0

> N

, since restricting and renormalizing to an event of mass p costs relative entropy − log p.The sharp conditioning (2) and its smooth counterpart play distinct roles, and it is worth separating them at the outset. The hard ball (2) serves only to define the complexity measure: its reduced entropy s∞ is the quantity the equivalence is stated in. The object the mean-field formalism actually acts on, and the object that is trained and shown to generalize, is the smooth Gibbs (tempered) posterior πβ ∝ e−βL dπ 0 

> N

, tuned so that its expected loss equals 

ε, with intensive inverse temperature ¯β⋆ = β/N — at finite data its empirical form πβ ∝

e−β bLD dπ 0 

> N

, the learner of §7.1. The smooth tilt is what carries a score for the mean-field equations (Definition 4.2), where the hard indicator carries none; the hard ball enters the learner only through the budget s∞, never as a constraint imposed on it. The single fact we need relating the two is that the required inverse temperature is polynomially bounded (Lemma 4.3). 

# 3 Main results 

We state the results before developing the mean-field formalism they are proved in (§4.1 onward). The first is the headline equivalence; the second is the engine. 

Theorem 3.1 (Width-robust learnability; informal) . Fix depth L and a family (yd), yd : {± 1}d →

[−1, 1] . Under γ = 1 Bayesian MSE learning with a bounded, Lipschitz, non-polynomial activa-tion (with biases), the following are equivalent, where each “polynomial” means a fixed polynomial in d with exponent depending only on L:

(i) (infinite width) there is polynomial data D(d) such that the infinite-width Gibbs posterior 

trained on D random points has population MSE L = o(1) in expectation; 

(ii) (polynomial width) there are polynomial N0(d), D (d) such that the width-N0 Gibbs posterior trained on D random points has population MSE L = o(1) ;

(iii) there is a polynomial-width network, with weights of polynomial size, achieving L = o(1) 

on yd.Equivalently, the reduced entropy s∞(yd, ε ) is polynomially bounded in d. Informally: a target is learnable at infinite width from polynomial data iff it is computed to o(1) mean-squared error by a polynomial-width network. The label informal refers only to the phrasing — the asymptotic family (yd) and the “ L = o(1) ”shorthand. The statement is a genuine theorem: it is the assembly (§7.3) of the quantitative compression and conservation bounds (Theorem 3.2) with the generalization and converse results (Theorems 7.2 and 7.3), all proved below. The “posterior” in (i)–(ii) is the empirical Gibbs posterior π ∝ exp( −N ¯β bLD) π0 

> N

at a polynomial intensive inverse temperature ¯β chosen by the free-energy bounds (Lemmas 4.3 and A.1); the reduced-entropy budget enters only through the complexity side s∞, never as a hard constraint on the learner. 

From the equivalence to two primitives. Both directions of Theorem 3.1 factor through a single pair of quantitative statements about a fixed target at a fixed accuracy. In one direction, 8a small accurate network must make the accurate region easy to reach for the prior — it must force the reduced entropy to be small; we call this conservation (the small network is “cloned” into the wide one without spending much entropy). In the other, small reduced entropy must be realizable by a genuinely small network that still generalizes; we call this compression (the wide accurate network is subsampled down to polynomial width). To run these at finite accuracy we allow a little slack: compression starts from a network accurate to an inner level ε− and returns one accurate to an outer level ε+ > ε −, losing a controlled amount along the way, while conservation runs in the reverse direction. The gap ∆ = min( ε − ε−, ε + − ε) is the room to maneuver, and it is what the polynomial bounds depend on. These are the two primitives; §7.3 chains them with the generalization bound (Theorem 7.2) and its converse (Theorem 7.3) to recover Theorem 3.1. 

Theorem 3.2 (Compression and conservation) . Fix L and accuracy levels ε− < ε < ε + with gap ∆ = min( ε − ε−, ε + − ε). For any y : Bd → [−1, 1] :

(a) (compression) if s∞(y, ε −) ≤ s, there is a network fˆθ of width N0 ≤ poly( s, d, 1/∆) (ex-ponent O(L)) and weights of size poly( s, d, 1/∆) (exponent O(L)) with population MSE 

L(ˆθ) ≤ ε+; moreover fˆθ agrees pointwise with an infinite-width MSE-ε-accurate network, 

sup x∈Bd |fˆθ(x) − f∞(x)| ≤ δ;

(b) (conservation) if some width-W network with weights of polynomial size has L ≤ ε−, then 

s∞(y, ε ) ≤ poly( W, d ).In particular s∞(y, ε ) ≤ poly(2 d) for every y (Lemma 2.1). 

# 4 The mean-field limit and its consequences 

4.1 The infinite-width limit 

As N → ∞ the network admits the standard mean-field description, which we take as a cited input and recall in the two features we use. Existence and structure in the feature-learning regime are developed in [14, 22, 4, 17, 16, 23, 2] and, in the Bayesian/posterior form relevant to (2), in [21, 20, 11]. 

Definition 4.1 (Single-site mean-field description) . In the limit the posterior is described by a tower of single-site laws Pℓ, one per layer, on preactivation fields ζ ∈ F — the function 

x 7 → ζ(x) on Bd, equivalently a vector in R2d

, carrying the scalar readout α at the last hidden layer. Any fixed finite set of neurons is an independent sample from Pℓ (propagation of chaos ), 

> 1
> m

Pmi=1 Ψ( ζi) → EPℓ Ψ for bounded continuous Ψ, so a width-N network is N i.i.d. draws and its output is the single-site average f (x) = EPL−1 [α φ (ζ(x))] . The layers are linked by their feature kernels 

Kℓ(x, x ′) = EPℓ

φ(ζ(x)) φ(ζ(x′)) , Kℓ(x, x ) ≤ 1, tr Kℓ ≤ 1, (4) in the sense that, conditionally on the previous layer, a layer-(ℓ+1) preactivation field is the Gaussian with covariance Kℓ.

Definition 4.2 (Boltzmann tilt) . Conditioning on the data tilts each single-site law by a self-consistent effective potential: the posterior single-site law is a reweighting of the prior single-site law, dP ℓ ∝ e−gℓ dP 0 

> ℓ

, with gℓ fixed by the mean-field equations. At the readout the tilt is generated by the loss, g = β ℓ eff with β the inverse temperature/multiplier conjugate to the accuracy constraint; at hidden layers gℓ is the backpropagated molecular field. We call ∥∇ gℓ∥L2(μ)

the susceptibility of layer ℓ.9In general the infinite-width limit is a mixture of such towers, and the mean-field equations fixing the tilt are stated at the level of a single pure component. Appendix D makes this precise: each component is a mean-field vacuum v, a self-consistent solution of the mean-field equations that supplies the kernels Kℓ and the tilt gℓ above. Every estimate below holds conditionally on a vacuum, with constants depending only on the KL/free-energy budget, and is then integrated over the mixture; we therefore suppress the vacuum and work with a single tower Pℓ.

The Cameron–Martin norm. For a layer kernel Kℓ write ∥w∥2CM = w, K −1 

> ℓ

w for the Cameron–Martin norm of the prior field N (0 , K ℓ) — the reproducing-kernel Hilbert space norm of Kℓ, which weights each direction inversely by its prior variance, so a field has small ∥·∥ CM 

exactly when it is concentrated on the high-variance (top-spectrum) directions of the kernel. Write ex ∈ F for the evaluation functional, ⟨w, e x⟩ = w(x), so that Kℓ(x, x ) = ⟨ex, K ℓex⟩.

4.2 First consequences of finite reduced entropy 

Three quantities the construction needs are bounded here from the standing assumption and a finite reduced entropy: the inverse temperature conjugate to the accuracy constraint, the readout second moment, and the layer susceptibilities (the lazy-coordinate scores). The first makes precise the “polynomial flexibility on the bounds” and is the only place the gap ∆ enters. 

Lemma 4.3 (Multiplier bound) . Fix ε− < ε and suppose s∞(y, ε −) ≤ K. Let F (β) =

− log Eπ0 

> N

e−βL and let β⋆ be the inverse temperature at which the Gibbs posterior πβ ∝ e−βLπ0

> N

has expected loss Eπβ⋆ [L] = ε. Then the intensive multiplier — the β of the readout tilt in Definition 4.2, with the extensive temperature N divided out — obeys 

¯β⋆ := β⋆/N ≤ sN (y, ε −)

ε − ε− 

> N→∞

−−−−→ Kε − ε−

≤ K

∆ .

Proof. For every β ≥ 0, Eπ0 

> N

e−βL ≥ R 

> {L≤ε−}

e−βL dπ 0 

> N

≥ e−βε − π0 

> N

(L ≤ ε−), so taking − log ,

F (β) ≤ βε − +   − log π0 

> N

(L ≤ ε−) = βε − + N s N (y, ε −). (5) On the other hand, with Zβ = Eπ0 

> N

e−βL = e−F (β),

0 ≤ DKL (πβ⋆ ∥π0 

> N

) = Eπβ⋆ [−β⋆L − log Zβ⋆ ] = F (β⋆) − β⋆ Eπβ⋆ [L] = F (β⋆) − β⋆ε, 

i.e. F (β⋆) ≥ β⋆ε. Combining with (5) at β = β⋆, β⋆ε ≤ β⋆ε− + N s N (y, ε −), hence β⋆(ε − ε−) ≤

N s N (y, ε −) and ¯β⋆ ≤ sN (y, ε −)/(ε − ε−); let N → ∞ . No tightness of (5), and no comparison of active and Gibbs entropies, is used. We take K = s∞(y, ε −), the reduced entropy at the inner radius, as the budget for all bounds below; we do not assume any comparability of s∞ between the accuracy levels. Thus ¯β⋆ ≤ K/ ∆.We now use this for the susceptibility. 

Proposition 4.4 (Regularity of a finite-KL Gibbs posterior) . Let πN, ¯β be either the population Gibbs posterior or an empirical Gibbs posterior at intensive inverse temperature ¯β, and suppose  

> 1
> N

DKL (πN, ¯β ∥π0 

> N

) ≤ R, ¯β ≤ R. 

Let Pℓ be any single-site law of the mean-field description (Appendix D). In the infinite-width limit, after removing a posterior subpopulation whose contribution to the downstream suffix output is o(1) in sup norm, the following quantities are bounded by poly( R, d )O(L):

10 (a) (readout moment and cap) the last-layer readout coordinate satisfies EPL−1 [α2] ≤ O(1+ R),and capping |α| at a polynomial threshold changes the output by o(1) ;

(b) (operator norm) the kept incoming weight matrices obey 1√N W ℓ+1 kept op ≤ poly( R, d )O(1) 

with high probability; 

(c) (active coefficients) for every polynomial-dimensional active space Uℓ, the kept fields have polynomial Cameron–Martin coefficients PUℓ K−1/2 

> ℓ

ζ ≤ poly( R, d )O(1) ;

(d) (susceptibility) the backpropagated lazy-score through any already constructed polynomial regular suffix is bounded by 

sup 

> h

Eρh ∥∇ lazy gℓ∥L2(μ) ≤ c⋆ = poly( R, d )O(L).

For empirical loss, the corresponding lazy-swap input is bounded by the empirical lazy-Gram estimate of Lemma B.3 once the empirical Gram top space is included in Uℓ.

This is the body-level form of Proposition A.10 and Proposition A.7. The point is that the regularity assumptions are derived from a Gibbs KL-rate bound, not from support on a hard ball: Lemma A.2 transfers the KL rate to single-site marginals, Gaussian entropy inequalities give moment and operator-norm truncations, and the suffix adjoint invariant bounds the actual contribution of the truncated population to the output layer by layer. Reduced entropy enters only by Lemma 4.3 or the empirical free-energy bound of Lemma A.1, which provide the required polynomial value of R.

4.3 Spectral split 

Fix a layer kernel K = Kℓ and diagonalize it, K = P 

> k

λk ψk ⊗ ψk with λ1 ≥ λ2 ≥ · · · ≥ 0 and 

{ψk} orthonormal in F. For a cutoff λ0 ∈ (0 , 1) split the spectrum into an active (top) part and a lazy (bulk) part, 

V = span {ψk : λk ≥ λ0}, C = K V ⊥ = X 

> λk<λ 0

λk ψk ⊗ ψk.

Because tr K ≤ 1 (4), the active part is low-dimensional, dim V ≤ λ−10 (each retained eigenvalue is ≥ λ0 and they sum to ≤ 1), and ∥C∥op ≤ λ0. The single estimate the lazy-swap rests on is that the lazy block is small uniformly in the input : with ex the evaluation functional, 

C2(x, x ) = X 

> λk<λ 0

λ2 

> k

ψk(x)2 ≤ λ0

X 

> λk<λ 0

λk ψk(x)2 = λ0 C(x, x ) ≤ λ0, (6) using C(x, x ) ≤ K(x, x ) ≤ 1. Equivalently 

C1/2ex L2(μ) = pC(x, x ) ≤ 1 and C1/2 

> op

= pλmax (C) ≤ pλ0, both uniform in x. 

(7) The active part will be reproduced explicitly by the subsampled neurons; the lazy part is what the next lemma swaps for fresh prior noise. The names echo the training regimes: a lazy direction is one left at the prior, behaving as in the lazy (kernel) regime, while an active direction carries data-dependent feature learning. This is the same dichotomy at the level of single directions — at γ < 1 there are no active directions, so every hidden layer is a fresh prior draw and the learner is exactly kernel (NNGP) regression; the content of γ = 1 is that a low-dimensional set of directions turns active while the rest stay lazy, which is what the compression of §5 keeps while resampling the rest from the prior. 11 5 Pointwise compression 

5.1 The lazy-swap lemma 

The lemma compares the tilted lazy law to the prior lazy law against any bounded Lipschitz functional of finitely many input evaluations, with an error governed by the lazy susceptibility (Proposition 4.4) times the lazy scale √λ0 (7). Because the tilt couples the active and lazy coordinates, the statement is conditional on the active coordinates h and uniform in them. It is the mechanism behind “the loss couples only to the top directions”: along the lazy block the tilt is nearly invisible. We prove it by Stein’s method, which needs no sub-Gaussianity, moment matching, or convexity, and which delivers the multi-point version the construction consumes. 

Lemma 5.1 (Lazy swap, multi-point) . Fix the active coordinates h ∈ V and let ρh(dv ) ∝

e−g(h+v)ρ0(dv ) on V ⊥, ρ0 = N (0 , C ), with lazy susceptibility Eρh ∥∇ vg(h + ·)∥L2(μ) ≤ c. Let 

F (v) = G v(x1), . . . , v (xp) depend on the field through p evaluations at arbitrary inputs, with 

G bounded and 1-Lipschitz in each argument. Then 

Eρh F − Eρ0 F ≤ p pλ0 c, 

uniformly in h and in the evaluation points. In particular the one- and two-point cases give 

Eρh φ(a + v(x)) − Eρ0 φ(a + v(x)) ≤ √λ0 c and Eρh [φxφx′ ] − Eρ0 [φxφx′ ] ≤ 2√λ0 c. Applied with c = c⋆ from Proposition 4.4, the error is poly( K, d, 1/∆) O(L)√λ0.Proof. Whitening. Let ˜v = C−1/2v, standard Gaussian under ρ0, and ˜g(˜ v) = g(h + C1/2 ˜v), so 

∇˜v ˜g = C1/2∇vg(h + v) and, by (7), ∥∇ ˜v ˜g∥ ≤ C1/2 

> op

∥∇ vg∥ ≤ √λ0 ∥∇ vg∥ pointwise in ˜v.Each evaluation is a linear form v(xi) = ⟨˜v, b i⟩ with bi = C1/2exi , ∥bi∥ = pC(xi, x i) ≤ 1 (7), so 

˜F (˜ v) = G(⟨˜v, b 1⟩ , . . . , ⟨˜v, b p⟩) has ∇ ˜F ∞ ≤ P 

> i

∥bi∥ ≤ p.

Stein solution. Let Lf = ∆ f − ⟨ ˜v, ∇f ⟩ be the Ornstein–Uhlenbeck generator (invariant law 

ρ0 in the ˜v coordinate), and f solve Lf = ˜F − Eρ0 ˜F , i.e. f = R ∞ 

> 0

(Eρ0 ˜F − Pt ˜F ) dt . Mehler’s formula ∇Pt = e−tPt∇ gives ∥∇ f ∥∞ ≤ ∇ ˜F ∞ ≤ p.

One integration by parts. Under ρh ∝ e−˜gρ0, Gaussian integration by parts gives Eρh [Lf ] = 

Eρh ⟨∇ f, ∇˜g⟩, and since Eρh [Lf ] = Eρh ˜F − Eρ0 ˜F ,

Eρh F − Eρ0 F = Eρh ⟨∇ f, ∇˜g⟩ ≤ ∥∇ f ∥∞ Eρh ∥∇ ˜g∥ ≤ p pλ0 Eρh ∥∇ vg∥ ≤ p pλ0 c. 

The bound uses the score only through its ρh-expectation Eρh ∥∇ vg∥ ≤ c, not a deterministic sup. 

Remark 5.2 (Where the uniformity comes from) . The factor √λ0 is C1/2 

> op

and the per-point 1 is C1/2ex = pC(x, x ) ≤ 1; both are uniform in x by (7), which is the entire reason the conclusion is a sup-norm statement rather than a μ-averaged one. This uniformity is what upgrades MSE accuracy to the pointwise agreement of Theorem 3.2(a). 

Remark 5.3 (What Stein removed) . The comparison reduces to the exact identity EρF −Eρ0 F =

Eρ ⟨∇ f, ∇˜g⟩; the Mehler bound caps ∥∇ f ∥∞ by the test functional’s Lipschitz constant, and √λ0 = C1/2 

> op

supplies the smallness. No sub-Gaussianity, no moment matching, and no convexity of the tilt are used, and the same line gives the two-point (kernel) and general p-point versions at the cost of the factor p — which is exactly what the construction consumes when it reproduces both features and the diagonal of the lazy kernel. 12 5.2 The construction and the compression theorem 

Fix the cutoff λ0 = ( δ/ poly) 2 and subsample N0 = poly( K, d, 1/δ )O(L) neurons from each single-site law Pℓ, together with fresh independent noise; assemble a width-N0 network ˆθ layer by layer. Figure 2 summarizes the construction. The first layer is copied verbatim. At a hidden layer each incoming weight is built in two pieces, matched to the μP forward pass ζℓ+1 = 1√N0 W ˆh, where 

ˆh = φ(ζℓ) is the postactivation feature of the previous layer. 

Active block, via fixed eigenfunctions. Read the kept features against the deterministic top eigenfunctions {ψk}k∈V of Kℓ — not a sample-dependent pseudo-inverse. Since EPℓ [

Dˆh, ψ k

E ˆh] = 

Kℓψk = λkψk lands back in V , the empirical average 1

> N0

P

> i

Dˆhi, ψ k

E ˆhi reproduces the active block PV ζj up to a mean-zero O(poly /√N0) fluctuation, with no O(1) lazy bias — the defining advantage of reading against fixed directions. The weight size is cW Aj 

> 22

→ ∥ PV ζj ∥2CM ≤ dim V +

O(K) ≤ poly (the budget K caps the Cameron–Martin norm on V ). 

Lazy directions, from the prior. On the kept neurons draw a fresh prior Gaussian weight and project it off the active read directions, P 

> i

ηji PV ˆhℓi = 0 — equivalently, keep only its component in the kernel of the dim V -row active map, still a genuine Gaussian weight. The resulting field has zero active component and lazy covariance → Cℓ: the lazy block of each neuron is drawn straight from the prior law N (0 , C ℓ), while the active block is supplied by the reconstruction above. Only r = dim V of the N directions are projected out — a vanishing fraction — so the weight is the prior up to that projection; the projection is exactly what stops the draw from leaking an O(1) , unswappable component into the active block, where (unlike the lazy block, with ∥C∥op ≤ λ0) the swap has no smallness to spend. The lazy-swap lemma then prices the one move that changes the law — replacing the realized lazy block by this fresh chaos — at ≤ p √λ0 c⋆ per consumed p-point evaluation, with c⋆ the sus-ceptibility of Proposition 4.4. Writing eℓ(x) for the layer-ℓ per-input active-reconstruction error, one step contributes the active fluctuation ( poly /√N0, mean-zero), the lazy swap ( poly √λ0 c⋆), and the inherited error read through bounded coefficients and the 1-Lipschitz φ, giving the recursion 

eℓ+1 (x) ≤ poly 

p λ0 + 1√N0 + eℓ(x)



, e1(x) ≤ poly /pN0, (8) uniformly in x, with all Hoeffding events unioned over the 2d inputs, the dim V ≤ poly directions, the O(L) layers and the N0 neurons (an o(1) total once N0 ≥ poly · d). 

Theorem 5.4 (Pointwise compression; proof of Theorem 3.2(a)) . Assume s∞(y, ε −) ≤ s and the standing assumption on φ, with fixed depth L. For every δ ∈ (0 , 1) the construction above returns a width-N0 network ˆθ with N0 ≤ poly( s, d, 1/δ )O(L), whose weights have size Wmax ≤

poly( s, d, 1/δ )O(L) with probability 1 − o(1) (hidden raw weights ≤ poly /√N0 per entry, fresh noise ≤ poly per entry, readout capped at τ ), such that 

P



sup 

> x∈Bd

|fˆθ(x) − f∞(x)| ≤ δ



≥ 1 − o(1) .

Choosing δ ≤ ∆/poly gives population MSE L(ˆθ) ≤ ε+ and N0 ≤ poly( s, d, 1/∆) O(L), which is Theorem 3.2(a). Proof. Set K = s and iterate (8) across the L − 1 hidden layers, adding the readout (a sub-sample average of the capped αi against the reconstructed features, with the same lazy-swap 13 and Hoeffding clauses and the o(1) capping tail of Proposition 4.4(a)). With λ0 = ( δ/ poly) 2,

N0 = poly( s, d, 1/δ )O(L) this gives sup x |fˆθ − f∞| ≤ δ whp; the weight bounds hold on the same events; and since f∞ is MSE-ε-accurate, L(ˆθ) ≤ (√ε + δ)2 ≤ ε+ once δ ≤ ∆/poly . The construc-tion, the per-step bound (concentration of the subsampled features, the conditioned-chaos lazy covariance, and the lazy-swap deployment), and the bounded-differences accounting are carried out in full in Appendix B (Theorem B.14). 

# 6 Conservation by cloning 

Cloning is the cheap converse to compression. A narrow teacher’s function is reproduced by a single-site law that tilts only the teacher’s own (polynomial-dimensional) active directions and leaves every complementary direction at the prior, so the reduced entropy it costs is polynomial in the teacher width — the data occupy a poly( W)-dimensional subspace, and the rest is prior. 

Theorem 6.1 (Conservation; proof of Theorem 3.2(b)) . Let fθ⋆ be a width-W network with population MSE L(θ⋆) ≤ ε− and weights of polynomial size , W ⋆ℓ,i ≤ poly( W, d ) (so the teacher fields have ζ⋆ℓ,i CM ≤ poly( W, d )). Then for all sufficiently large N ,

sN (y, ε ) ≤ poly( W, d ), hence s∞(y, ε ) ≤ poly( W, d ).

In particular, since every y : Bd → [−1, 1] is computed to any fixed accuracy by a width-O(2 d)

network with poly(2 d) weights (Lemma 2.1), s∞(y, ε ) ≤ poly(2 d) for all y.Proof sketch; full argument in Appendix C. Write N = Wm and partition the N neurons at each layer into W clone groups of size m, group b cloning teacher neuron b. The exact clone is the block weight 

W ℓ+1 = 1√m

 W ⋆,ℓ +1 ⊗ 1m×m

 = 1√m



W ⋆

> 11

1 · · · W ⋆

> 1W

1

... ...

W ⋆

> W1

1 · · · W ⋆

> WW

1

 ,

with first-layer weights and readouts cloned likewise ( W 1 

> j

= W ⋆, 1 

> b

, αj = α⋆b for j in group b). On a group-constant previous layer this reproduces the teacher exactly: the μP sums telescope, 

> 1√N

P 

> i

W ℓ+1  

> ji

zi = 1√W

P 

> a

W ⋆ba z⋆a = ζ⋆,ℓ +1  

> b

, and the 1 

> N

readout average becomes the teacher’s  

> 1
> W

average. The block factor 1√m is the only rescaling: it reconciles the wide net’s μP hidden normalization 1√N with the teacher’s 1√W , since √N = √Wm (the readout alone is intensive,  

> 1
> N

→ 1 

> W

), exactly the convention of (1). 

Trial law (a microscopic cylinder). Let πcyl  

> N

be the prior conditioned on the cylinder in which, for each neuron, the W group-mean coordinates 

D

W ℓ+1  

> j, ·

, u a

E

(ua = m−1/21group a), the d first-layer weights, and the readout lie within radius ρ of their cloned targets; the remaining N − W

orthogonal coordinates of each row are left at the prior. The cylinder has positive radius precisely to keep − log π0 

> N

(cyl) finite. 

The free directions are harmless — the data is W-dimensional. The constrained group means span a W-dimensional subspace of each row; on a group-constant input the free orthogonal weights contribute nothing, since P 

> i∈a

W ⊥ 

> ji

= 0 annihilates the group mean. Acting instead 14 on the within-group deviation zℓi − ¯zℓa, they inject noise of size the previous spread. The first-layer tube gives spread σ1 ≤ Lφρ√d, and one layer multiplies it by O(Lφ) (the free weights have O(1) operator norm), so σℓ ≤ poly( d, L φ)Lρ; the group means then track the teacher to poly( W, d, B, L φ)Lρ (Lemma C.1). The clone therefore reproduces the teacher uniformly ,

sup x |fclone (x) − fθ⋆ (x)| ≤ poly( W, d, B )Lρ, in both empirical and population MSE. 

Entropy. Only the d + W + 1 constrained coordinates per neuron cost relative entropy; each is a Gaussian coordinate pinned to a tube of radius ρ about a target of size ≤ poly( W, d ), costing 

log 1 

> ρ

+ O(poly( W, d )) , while the N − W free directions cost zero. As πcyl  

> N

is the prior restricted to this product cylinder, 

DKL 

 πcyl  

> N

∥π0

> N

 = − log π0 

> N

(cyl) = N (d + W + 1) log 1 

> ρ

+ O(poly( W, d )) .

Taking ρ = exp( −poly( W, d, B )) so that poly Lρ ≤ ∆ gives cloned loss ≤ ε− + ∆ ≤ ε with probability ≥ 12 over the free coordinates, and Kclone := 1 

> N

DKL (πcyl  

> N

∥π0 

> N

) = poly( W, d, B ).

Change of measure. With πcyl  

> N

(L ≤ ε) ≥ 12 , the data-processing inequality on {L ≤ ε} gives 

− log π0 

> N

(L ≤ ε) ≤ 2DKL (πcyl  

> N

∥π0 

> N

) + O(1) = N poly( W, d, B ), so sN (y, ε ) ≤ poly( W, d, B ) for all large N and the lim sup is the same. The identical construction with bLD in place of L yields the empirical statement π0 

> N

(bLD ≤ bLD(θ⋆) + η) ≥ e−N poly( W,d,B, log 1 /η ) used in the forward assembly. Finally Lemma 2.1 realizes any y to any fixed accuracy at width W = O(2 d) with poly(2 d)

weights, giving the last claim. Together with Theorem 5.4, the reduced entropy and the minimal polynomial-weight teacher width are polynomially equivalent: s∞(y, ε −) ≤ s yields a teacher of width poly( s) with poly( s)

weights (Theorem 5.4), and a polynomial-weight teacher of width W yields s∞ ≤ poly( W)

(Theorem 6.1). This is the conservation of the reduced entropy across widths that drives the equivalence of Theorem 3.1. 

# 7 Learnability 

7.1 Low reduced entropy implies Gibbs learnability 

Fix the training model: n i.i.d. inputs x1, . . . , x n ∼ μ with labels y(xi) and empirical loss 

bLD(θ) = 1

> n

P

> i

(fθ(xi) − y(xi)) 2. The learner in this section is not the hard empirical ball. It is the adaptive empirical Gibbs posterior 

πDN, ¯β (dθ ) = exp( −N ¯β bLD(θ)) 

ZD( ¯β) π0 

> N

(dθ ), (9) with intensive inverse temperature ¯β. The hard population ball remains the definition of reduced entropy; it is used only to produce polynomial KL/free-energy budgets. The smooth Gibbs posterior is the object that has a differentiable Boltzmann score and hence a mean-field tower. The forward direction rests on placing the learned function in a fixed , data-independent class and applying uniform convergence there. The following isolates that step; its only inputs are the weight bounds the compression of §5 guarantees, and its covering number is independent of 2d.

Lemma 7.1 (Uniform convergence on a bounded-weight class) . Fix depth L and let HN0,B be the class of width-N0 networks (1) with weights bounded as in the construction (hidden entries 

≤ B/ √N0, fresh-noise entries ≤ B, readouts |αi| ≤ B). Then: 

15 (a) every f ∈ H N0,B has ∥f ∥∞ ≤ B, and ℓf (x) = ( f (x) − y(x)) 2 lies in [0 , (B + 1) 2] and is 

2( B + 1) -Lipschitz in ∥f ∥∞;

(b) log N (HN0,B , δ, ∥·∥ ∞) ≤ P log(3Λ B/δ ), with P = O(N0d+LN 20 ) parameters and parameter-to-function sup-norm Lipschitz constant Λ = poly( B, d )O(L); in particular it is ≤ poly( N0, B, d )O(L) log(1 /δ ),

independent of 2d;

(c) for n ≥ poly( N0, B, d, 1/∆) O(L) log(1 /η ) i.i.d. samples, P  sup f ∈H N0,B |bLD(f ) − L(f )| >

∆/4 ≤ η.Proof. (a) |fθ(x)| ≤ 1

> N0

P 

> i

|αi| | φ| ≤ B; with |y| ≤ 1 this gives the range, and |ℓf − ℓg| =

|f − g| | f + g − 2y| ≤ 2( B + 1) ∥f − g∥∞. (b) The map θ 7 → fθ is sup-norm Lipschitz with constant Λ = poly( B, d )O(L): each layer composes the 1-Lipschitz φ with a weight multiply of operator norm ≤ poly( B, d ) over the μP/ 1/N 0 normalizations, and the bound multiplies over L

layers. Gridding the P = O(N0d + LN 20 ) parameters, each in [−B, B ], at scale δ/ Λ yields a sup-norm δ-cover of size (3Λ B/δ )P . (c) For fixed f the losses are i.i.d. in [0 , (B + 1) 2], so Hoeffding gives P(|bLD(f ) − L(f )| > t ) ≤ 2e−2nt 2/(B+1) 4

. Take a sup-norm δ-cover with δ = ∆ /(16( B + 1)) 

and t = ∆ /8; a union bound over the cover gives the claim. 

Theorem 7.2 (Low entropy ⇒ empirical Gibbs learnability) . Suppose s∞(y, ε −) ≤ s and φ

satisfies the standing assumption, with fixed depth L. Then there are 

n, N 0, ¯β ≤ poly( s, d, 1/∆) O(L)

such that the infinite-width empirical Gibbs learner satisfies 

ED Eθ∼πD 

> ∞,¯β

L(θ) ≤ ε+,

and the same conclusion holds for the width-N0 Gibbs posterior πDN0, ¯β , up to an arbitrarily small 

o(1) slack absorbed into ε+ by increasing the polynomial constants. Proof. Step 0: the learner is the smooth empirical Gibbs object. For fixed D and ¯β, (9) is a finite-data mean-field Gibbs measure. Appendix D, Lemma D.1, gives the N → ∞ limit as a mixture over empirical mean-field vacua. We condition on a vacuum v; then there is a deterministic single-site tower P D, v 

> ℓ

, propagation of chaos holds conditionally, and the Boltzmann tilt has the empirical-loss score. The limiting learned function in this vacuum is f D, v(x) = EP D, v

> L−1

[αφ (ζ(x))] .The regularity and truncation package of Appendix A applies uniformly to every vacuum with the same KL-rate bound, so after proving the estimates conditionally we integrate over the vacuum mixture. 

Step 1: the empirical free-energy witness is the population-good teacher. Set γ = ∆ /32 . First use the reduced-entropy hypothesis at the inner radius to form a population Gibbs witness at the intermediate accuracy ε− + γ/ 4: by the multiplier bound, its intensive inverse temperature and KL rate are poly( s, d, 1/∆) . In the population mean-field decomposition, condition on a component/tower whose loss and free-energy budget satisfy the same bounds up to harmless constant factors; equivalently, apply the following compression argument to the Gibbs witness and then fix a good realization. Applying the quantitative pointwise compression theorem (The-orem 5.4, with sup-norm accuracy δ ≤ γ/ poly ) to this fixed population-good tower produces a 

data-independent polynomial teacher f⋆ ∈ H N⋆,B ⋆ such that 

N⋆, B ⋆ ≤ poly( s, d, 1/∆) O(L), L(f⋆) ≤ ε− + γ. 

16 Fix one realization of this teacher on the high-probability compression event. We choose the final sample size large enough that uniform convergence also holds on the teacher class HN⋆,B ⋆ ;then, except on an o(1) event in D,

bLD(f⋆) ≤ ε− + 2 γ. 

Now clone and thicken this fixed teacher, not a memorizer of the sample. The empirical version of Theorem 6.1 applied to bLD gives, for every such dataset and all sufficiently large widths N ,

π0 

> N

(bLD ≤ ε− + 3 γ) ≥ e−N K ⋆ , K⋆ ≤ poly( s, d, 1/∆) O(L).

The empirical Gibbs variational bound (Lemma A.1) therefore yields 

EπDN, ¯β

bLD ≤ ε− + 3 γ + K⋆/ ¯β, 1 

> N

DKL (πDN, ¯β ∥π0 

> N

) ≤ K⋆ + ¯β(ε− + 3 γ). (10) Choose ¯β ≥ K⋆/γ , still bounded by poly( s, d, 1/∆) O(L), so 

EπDN, ¯β

bLD ≤ ε− + 4 γ, 1 

> N

DKL (πDN, ¯β ∥π0 

> N

) ≤ R⋆ := poly( s, d, 1/∆) O(L).

Thus the empirical Gibbs posterior has both low empirical loss and a polynomial KL rate for the right reason: the witness is the low-entropy population teacher supplied by s∞(y, ε −) ≤ s.

Step 2: truncate and compress into a fixed class. The KL-rate bound in (10) feeds Propo-sition A.10 and the explicit kept-set version, Proposition A.7. After discarding a posterior subpopulation whose suffix-output contribution is at most η in sup norm, the empirical Gibbs tower has polynomial readout caps, operator norms, active Cameron–Martin coefficients, and empirical lazy-score bounds. The empirical active block is taken basis-free as 

Uℓ = U pop  

> ℓ

⊕ U emp  

> ℓ

,

where U pop  

> ℓ

is the population spectral top space and U emp  

> ℓ

is the top space of the normalized empirical evaluation covariance 1

> n

P

> a≤n

(K1/2 

> ℓ

exa ) ⊗ (K1/2 

> ℓ

exa ) above cutoff τ . Thus dim Uℓ ≤

λ−10 + τ −1, and the empirical lazy leakage is not asserted to vanish; Appendix B, Lemma B.3, gives instead 

c⊤ bCℓDc ≤ τ B 2

for every empirical-loss score P 

> a

caexa with ∥c∥2 ≤ B2/n . Taking τ, λ 0 inverse-polynomial makes the population and empirical lazy-swap errors ≤ η while keeping dim Uℓ polynomial. The pointwise compression theorem (Theorem 5.4, in the empirical version supplied by Ap-pendix B) then produces, conditionally on each vacuum, a data-dependent proxy hD, v ∈ H N0,B 

with N0, B ≤ poly( s, d, ¯β, 1/∆) O(L) = poly( s, d, 1/∆) O(L) and 

Ev∼ρD EπD   

> ∞,¯β(·| v)

∥fθ − hD, v∥∞ ≤ η. (11) Here HN0,B itself is fixed independently of D; only the point hD, v in the class is random through the sample and the vacuum. 

Step 3: uniform convergence transfers empirical Gibbs loss to population loss. Enlarge 

N0, B if necessary so that HN⋆,B ⋆ ⊆ HN0,B . Now choose n ≥ poly( N0, B, d, 1/∆) O(L) =poly( s, d, 1/∆) O(L) so that Lemma 7.1 gives, with probability 1 − o(1) over D,

sup 

> h∈H N0,B

|bLD(h) − L(h)| ≤ ∆/4. (12) 17 On this event, Lipschitz transfer of the squared loss and (11) first compare the Gibbs function to its compressed proxy, 

EπD 

> ∞,¯β

L(θ) ≤ Ev∼ρD L(hD, v) + poly( B)η, Ev∼ρD bLD(hD, v) ≤ EπD 

> ∞,¯β

bLD(θ) + poly( B)η. 

Since each hD, v lies in the fixed class HN0,B , uniform convergence (12) applies to the proxy rather than to the raw Gibbs draw. Thus 

EπD 

> ∞,¯β

L(θ) ≤ EπD 

> ∞,¯β

bLD(θ) + ∆ /4 + poly( B)η ≤ ε− + 4 γ + ∆ /4 + poly( B)η + o(1) ≤ ε+,

for η ≤ ∆/poly( B), since ε+ − ε− ≥ ∆ after shrinking constants in the intermediate gaps. On the exceptional o(1) fraction of datasets, Proposition A.7 gives uniform integrability of the loss under the Gibbs law, so their contribution is o(1) . This proves the infinite-width claim. 

Step 4: finite width. The same argument applies to the width-N0 Gibbs posterior after the kept-set truncation of Proposition A.7. The empirical free-energy bound (10) is valid at width N0 once N0 is chosen large enough to contain and clone the fixed population teacher 

f⋆. Truncation changes expected outputs, empirical loss, and population loss by at most η; the truncated posterior is supported on HN0,B , so (12) applies directly to its draws. Letting η = o(∆) 

gives EDEπDN0, ¯β

L ≤ ε+.The pointwise compression is exactly what lets a draw from the a priori width-N (or infinite-width) Gibbs posterior be compared to a fixed finite-dimensional class for the union bound. The hard reduced-entropy ball supplies the polynomial budget; it is not itself the learner. 

7.2 The converse: Gibbs learnability implies low reduced entropy 

Theorem 7.3 (Gibbs learnability ⇒ low entropy) . Suppose that for some n and some polyno-mially bounded intensive temperature ¯β ≤ poly( n, d ), the empirical Gibbs learner satisfies 

EDEθ∼πDN, ¯β

L(θ) ≤ ε−

for all sufficiently large N (or in the infinite-width limit). If ε− < ε with a fixed gap, then 

s∞(y, ε ) ≤ poly( n, d ), with polynomial dependence also on ¯β and the gap. Proof. For every dataset D, memorization plus cloning gives an empirical prior-mass lower bound 

π0 

> N

(bLD ≤ ε− + o(1)) ≥ e−N K D , KD ≤ poly( n, d ).

The empirical free-energy bound of Lemma A.1 therefore gives  

> 1
> N

DKL (πDN, ¯β ∥π0 

> N

) ≤ KD + ¯β(ε− + o(1)) ≤ poly( n, d, ¯β) (13) for every D.Choose an intermediate threshold t ∈ (ε−, ε ). By the assumed population performance and Markov’s inequality over the draw of D, the set of datasets for which 

EπDN, ¯β

L ≤ t0 := ε− + t

2

18 has probability at least 1 − ε−/t 0 > 0. Fix such a dataset. Markov’s inequality under the Gibbs law then gives 

πDN, ¯β (L ≤ t) ≥ 1 − t0/t =: q > 0,

where q depends only on the fixed accuracy gap. Apply data processing for the binary partition 

A = {L ≤ t} to Q = πDN, ¯β and P = π0 

> N

: if Q(A) ≥ q, then P (A) ≥ exp( −q−1DKL (Q∥P )−Oq(1)) .Together with (13), 

π0 

> N

(L ≤ t) ≥ exp {− N poly( n, d, ¯β, 1/q ) − Oq(1) }.

Since t < ε , monotonicity gives the same lower bound for π0 

> N

(L ≤ ε). Hence sN (y, ε ) ≤

poly( n, d, ¯β, 1/q ) + o(1) and the limsup gives s∞(y, ε ) ≤ poly( n, d ) for polynomial ¯β, with the fixed-gap constants absorbed into the polynomial. The mechanism is still memorization plus change of measure, but the change of measure now starts from the empirical Gibbs law rather than a hard empirical posterior. 

7.3 Assembly of the equivalence 

Proof of Theorem 3.1. Throughout, the accuracy levels ε− < ε < ε + and 1/∆ are baseline constants, absorbed into the polynomials; “ L = o(1) ” is read at a fixed accuracy and recovered by the usual rounding of a boolean target. We show that (i) and (ii) are each equivalent to 

s∞(yd, ε ) being polynomial in d, which also gives (iii) and the equivalence of (i) and (ii). 

s∞ polynomial ⇒ (i) and (ii). By Theorem 7.2 with s = poly( d), both the infinite-width empirical Gibbs posterior and the width-N0 empirical Gibbs posterior ( N0 = poly( d)) trained on 

n = poly( d) samples have population MSE ≤ ε+ = o(1) in expectation, for polynomial intensive temperature ¯β.

(i) or (ii) ⇒ s∞ polynomial. Gibbs learnability from n = poly( d) samples at polynomial intensive temperature gives, by Theorem 7.3, s∞(yd, ε ) ≤ poly( n, d ) = poly( d).

Equivalence with (iii) and the reduced-entropy characterization. If s∞(yd, ε −) ≤ poly( d) then Theorem 5.4 produces a network of width poly( d) whose weights have polynomial size whp (hidden raw weights ≤ poly /√N0 per entry, fresh noise ≤ poly , readouts capped at τ = poly ), with L ≤ ε+ = o(1) , which is (iii); conversely a width-W polynomial-weight network with L ≤ ε−

gives s∞ ≤ poly( W ) by Theorem 6.1, so (iii) with W = poly( d) returns s∞ ≤ poly( d). Hence all of (i), (ii), (iii) hold exactly when s∞(yd, ε ) is polynomial in d.The pointwise compression (Theorem 5.4) is the engine throughout: it supplies the fixed low-complexity class for the forward generalization (Theorem 7.2) and the explicit polynomial-width teacher in the equivalence with (iii), and — via the sup x agreement of Theorem 3.2(a) — it is what makes the infinite-width and polynomial-width learners agree as functions, not merely in risk. 

# 8 Discussion 

Why γ = 1 is the critical case. The mean-field exponent γ = 1 is the delicate one. At the over-rich scalings γ > 1 the lazy bulk of each feature kernel contributes below the accuracy threshold, so compression may simply discard it and the lazy-swap is unnecessary; at γ = 1 the lazy content is of the same order as ε and must be reproduced in law , which is exactly what 19 the lazy-swap lemma (Lemma 5.1) provides. This is why the γ = 1 result is not a corollary of the γ > 1 one, and why the pointwise (rather than mean-square) reproduction is forced here. The scalings γ > 1, where the argument is softer, are treated alongside the general finite-width theory in a companion paper. 

Finite width. This paper establishes the equivalence between infinite width and polynomial 

width. The only place infinite width is genuinely used is propagation of chaos (Definition 4.1), which makes the subsampled feature averages exact single-site expectations; polynomial width needs no such input because the network is already small and ordinary uniform convergence applies, and cloning transfers in the other direction without it. At general (super-polynomial) finite width these averages fluctuate, and controlling them uniformly over the 2d inputs requires a concentration hypothesis on the single-site law. Establishing that hypothesis is the central analytic task of the companion paper; as N → ∞ it degenerates into chaos and the present results are recovered. 

Relation to circuit complexity. A polynomial-width network is, up to bounded precision, a polynomial-size circuit, so the characterization reads morally as “learnable if and only if computed by a small circuit” — a P/poly -flavored statement for mean-field learning. We resist stating it as a theorem: our reductions cost a factor O(L) in the exponent and do not preserve depth, so the induced complexity statement is coarse, tracking polynomial size but not the depth that distinguishes circuit classes. We therefore offer the circuit reading as motivation for the reduced-entropy measure rather than as a complexity-theoretic equivalence. 

What the pointwise witness buys. The reduced entropy is defined through mean-squared error, which is the robust complexity measure; yet the compression that certifies it is pointwise. This is not a cosmetic strengthening. It is the pointwise agreement that lets a draw from the (a priori width-N , even infinite) posterior be placed in a fixed finite-dimensional class for a union bound, and that makes the infinite-width and polynomial-width learners agree as functions rather than merely in risk — the sense in which the inductive bias, and not just the achievable loss, is conserved across widths. 

# Acknowledgments 

We thank Lauren Greenspan, Lawrence Chan, Zohar Ringel, Noa Rubin, Jake Mendel, and others for helpful discussions. The authors used large language model tools to assist with drafting and editing parts of the paper, including portions of the exposition and lemma statements. The mathematical arguments, references, and final manuscript were reviewed by the authors, who take full responsibility for the content. The first author was supported by Princint (Principles of Intelligence) through funding from grants from the AI Security Institute and Coefficient Giving. 

Notation in the appendices. The appendices develop the construction in a slightly more general, induction-friendly notation than the body; the dictionary is as follows. The active space is written Uℓ (the body’s V of the population spectral split, §4.3, and the enlarged Ueff of §7.1 once the empirical evaluation directions are adjoined). Layer widths are Mℓ (the body’s retained 20 width N0). The intensive multiplier is ¯β (the body’s ¯β⋆), and R denotes the resulting Gibbs KL rate 1 

> N

DKL (π∥π0 

> N

) ≤ K + ¯βε − = poly( K, 1/∆) , with K = s∞(y, ε −) the entropy budget. The per-layer regularity constants are collected as A+, with Lipschitz/adjoint constants Λ, Γ for the suffix; these are what the body summarizes as the susceptibility c⋆ and the readout cap τ . All polynomial bounds carry exponents depending only on the fixed depth L.

# A Regularity from the entropy budget 

This appendix proves that a single scalar — the intensive KL rate of the Gibbs posterior — controls every regularity quantity the compression argument of Appendix B consumes: capped readouts, normalized operator norms, Cameron–Martin active coefficients, and lazy scores. Two principles keep the estimates clean. First, although the width-N posterior is not a product, the prior is, and the chain rule for relative entropy passes the budget to the averaged single-site marginal (Lemma A.2); all per-neuron envelopes then follow from finite-dimensional Gaussian estimates against that marginal. Second — and this is the point of the suffix-invariant formulation below — when we bound the layer-ℓ score we do so through the polynomial suffix already built to its right , never through the raw width-N matrices, so the lazy score inherits the suffix’s polynomial Lipschitz constant directly. Throughout, π denotes a population Gibbs posterior πN, ¯β ∝ e−N ¯βLπ0 

> N

or its empirical ana-logue πDN, ¯β ∝ e−N ¯β bLD π0 

> N

, with intensive inverse temperature ¯β. We write L⋆ for L or bLD as appropriate. 

A.1 The adaptive-temperature Gibbs law 

Lemma A.1 (Multiplier) . Suppose s∞(y, ε −) ≤ K. Then for every ¯β > 0,

lim sup  

> N→∞

EπN, ¯β L ≤ ε− + K

¯β , lim sup   

> N→∞
> 1
> N

DKL 

 πN, ¯β ∥ π0

> N

 ≤ K + ¯βε −.

In particular, taking ¯β = 2 K/ (ε − ε−) gives expected loss ≤ ε− + ( ε − ε−)/2 < ε and KL rate 

≤ K 1 + 2 ε−/(ε − ε−), which is poly( K, 1/∆) since ε − ε− ≥ ∆. The same bounds hold for the empirical posterior with L → bLD, ε− → τ , K → KD whenever π0 

> N

(bLD ≤ τ ) ≥ e−N (KD +o(1)) .Proof. Write ZN ( ¯β) = Eπ0 

> N

e−N ¯βL and ΦN ( ¯β) = − 1 

> N

log ZN ( ¯β). Because s∞(y, ε −) ≤ K, for every δ > 0 and all large N we have π0 

> N

(L ≤ ε−) ≥ e−N (K+δ), hence 

ZN ( ¯β) ≥

Z

> {L≤ε−}

e−N ¯βL dπ 0 

> N

≥ e−N ¯βε − π0 

> N

(L ≤ ε−) ≥ e−N ( ¯βε −+K+δ),

so ΦN ( ¯β) ≤ ¯βε − + K + δ. By the definition of πN, ¯β , 

> 1
> N

DKL 

 πN, ¯β ∥π0

> N

 = EπN, ¯β

h 1 

> N

log dπ N, ¯β

> dπ 0
> N

i

= Φ N ( ¯β) − ¯β EπN, ¯β L ≥ 0.

The first inequality gives EπN, ¯β L ≤ ΦN ( ¯β)/ ¯β ≤ ε− + ( K + δ)/ ¯β; the second, together with 

EπN, ¯β L ≥ 0, gives 1 

> N

DKL ≤ ΦN ( ¯β) ≤ K + ¯βε − + δ. Let δ ↓ 0. The empirical statement is identical, replacing L by bLD and the entropy lower bound by its empirical counterpart. 21 A.2 Entropy consequences 

Lemma A.2 (Single-site KL decomposition) . Let π be any (exchangeable) measure on θ =(θ1, . . . , θ N ) and let the prior factorize, π0 = NNi=1 π0(i). Let π(i) be the ith marginal of π and 

¯ρ = 1

> N

P 

> i

π(i) the averaged single-site marginal (against the common single-site prior ρ0). Then 

DKL 

 ¯ρ ∥ ρ0 ≤ 1

> NN

X

> i=1

DKL 

 π(i) ∥ ρ0 ≤ 1 

> N

DKL 

 π ∥ π0.

In particular, if 1 

> N

DKL (π∥π0) ≤ R then DKL (¯ ρ∥ρ0) ≤ R.Proof. The first inequality is convexity of relative entropy in its first argument. For the second, the chain-rule identity for a product reference measure reads 

DKL 

 π ∥ N 

> i

π0(i)

 = DKL 

 π ∥ N 

> i

π(i)

 + PNi=1 DKL 

 π(i)∥π0(i)

 ≥ PNi=1 DKL 

 π(i)∥ρ0,

since the total-correlation term is nonnegative and π0(i) = ρ0. Divide by N .

Lemma A.3 (Gaussian moment and projection bounds) . Let γ = N (0 , I m) and let q ≪ γ with 

DKL (q∥γ) ≤ R. Then: 

(a) for any unit vector a, Eq ⟨X, a ⟩2 ≤ 4R + 2 log 2 ;

(b) for any subspace U of dimension r, Eq ∥PU X∥2 ≤ 4R + 2 r log 2 ;

(c) for any T > 0, Eq

 ∥PU X∥ 1∥PU X∥>T 

 ≤ (4 R + 2 r log 2) /T ;

(d) if X = ( Xx)x∈Bd is any centered Gaussian field with sup x EX2 

> x

≤ 1, then Eq ∥X∥2 

> ∞

≤

4R + 4 d log 2 + 2 log 2 .The first three bounds hold for γ = N (0 , K ) with ∥ · ∥ read in Cameron–Martin coordinates 

X 7 → K−1/2X; the last bound is in the original field coordinates and uses only K(x, x ) ≤ 1.Proof. The Donsker–Varadhan inequality EqF ≤ DKL (q∥γ) + log Eγ eF applied to F = 14 ⟨X, a ⟩2

gives, using Eγ etZ 2

= (1 − 2t)−1/2 at t = 14 , 14 Eq ⟨X, a ⟩2 ≤ R + 12 log 2 , which is (a). For (b) take 

F = 14 ∥PU X∥2 and Eγ etχ 2 

> r

= (1 − 2t)−r/ 2. Part (c) is E[Y 1Y >T ] ≤ E[Y 2]/T with Y = ∥PU X∥.Whitening reduces the N (0 , K ) case to the standard one. For (d), by the union bound and the one-dimensional Gaussian moment generating function, 

Eγ exp { 14 ∥X∥2

> ∞

} ≤ X

> x∈Bd

Eγ eX2 

> x/4

≤ 2d√2.

Applying Donsker–Varadhan to F = 14 ∥X∥2 

> ∞

gives the stated bound. Thus the full cube maxi-mum costs only O(d) = log |Bd|, not O(2 d).

Lemma A.4 (Operator-norm truncation) . Let W ∈ Rm×n have i.i.d. N (0 , 1) prior P and let Q

be any posterior with DKL (Q∥P ) ≤ κ. There is a universal c > 0 such that for all A ≥ 2,

Q



∥W ∥op > A (√m + √n)



≤ κ + log 2 

c A 2(m + n) .

Consequently, for a hidden matrix W ℓ ∈ RN ×N whose posterior marginal has DKL ≤ N R ,choosing A =  R/ (2 cη )1/2 gives 1√N W ℓ 

> op

≤ 2A = poly( R, 1/η ) outside a posterior event of probability ≤ η. No independence of the posterior rows is used. 

22 Proof. Since ∥W ∥op is 1-Lipschitz in the Frobenius norm, Gaussian concentration gives P (∥W ∥op >

√m + √n + t) ≤ e−t2/2; taking t = ( A − 1)( √m + √n) and using (√m + √n)2 ≥ m + n yields 

P (∥W ∥op > A (√m + √n)) ≤ e−cA 2(m+n) for a universal c. The data-processing (entropy) in-equality Q(E) ≤ (DKL (Q∥P ) + log 2) / log(1 /P (E)) then gives the claim. For the application, the matrix marginal of the posterior has κ ≤ DKL (π∥π0) ≤ N R , m = n = N , so the bound is 

(N R + log 2) /(2 cA 2N ) = O(R/A 2).

Corollary A.5 (Cameron–Martin active coefficients) . Let Uℓ be an active space of dimension 

rℓ at layer ℓ. Under DKL (¯ ρℓ∥ρ0 

> ℓ

) ≤ R, the active coefficient of a posterior single-site field ζ

obeys E¯ρℓ PUℓ K−1/2 

> ℓ

ζ 2

≤ 4R + 2 rℓ log 2 . With the spectral cutoffs of Appendix B, rℓ ≤ poly , so truncating the active coefficient at a polynomial threshold discards single-site mass contributing 

O(η) to the bounded output. Proof. Immediate from Lemma A.3(b),(c) applied to X = K−1/2 

> ℓ

ζ (a standard Gaussian under the prior), with ∥PUℓ X∥ = ∥ζ∥CM -projection. 

A.3 The suffix invariant and the one-step lazy-score bound 

The right-to-left construction of Appendix B maintains, at each stage, a finite suffix Sℓ+1 :

FMℓ+1 → RBd

mapping the surviving layer-(ℓ+1) fields to the output. We measure perturbations of M fields by the normalized channel metric dM (ξ, ξ ′) = sup x

  1

> M

P 

> j≤M

|ξj (x) − ξ′ 

> j

(x)|21/2.

Definition A.6 (Finite regular suffix) . Sℓ+1 is A-regular if: (I1) Mℓ+1 ≤ A; (I2) ∥Sℓ+1 (ξ)∥∞ ≤ A

on the kept fields; (I3) Sℓ+1 is A-Lipschitz for dMℓ+1 ; (I4) for every input x, channel j, and direction u ∈ F , the directional derivative obeys |Dj,u Sℓ+1 (ξ)( x)| ≤ A√Mℓ+1 

∥u∥L2(μ).

Proposition A.7 (Kept-set truncation with suffix contribution) . Assume 1 

> N

DKL (π∥π0 

> N

) ≤ R

and that the suffix to the right of layer ℓ is A-regular. Let Uℓ be any active space of dimension 

r ≤ A and let GT be the event that, for the relevant layer variables, all of the following are at most T : readout/scalar coefficients, PUℓ K−1/2 

> ℓ

ζ , the field sup norm ∥ζ∥∞, and the normalized incoming operator norm. For every η > 0 there is 

T = poly( A, R, d, 1/η )O(1) 

such that replacing all channels outside GT by zero changes the expected suffix output by at most 

η:

Eπ ∥Sℓ+1 (ξ) − Sℓ+1 (ξ1GT )∥∞ ≤ η. (14) 

The same statement holds at the readout layer with SL(( αi, ζ i)i) = M −1 P 

> i

αiφ(ζi), and it holds for empirical Gibbs laws uniformly in the dataset whenever the KL-rate bound is uniform. Proof. The entropy chain rule (Lemma A.2) gives a single-site marginal with KL at most R.Lemma A.3 gives second moments for fixed Gaussian coordinates, active Cameron–Martin projec-tions, and the whole cube maximum. In particular, the field sup norm obeys E ∥ζ∥2 

> ∞

≤ O(R + d)

whenever Kℓ(x, x ) ≤ 1: the union over the 2d inputs costs only log |Bd| = O(d) by the Gaussian-max/Donsker–Varadhan estimate of Lemma A.3(d), not 2d. Lemma A.4 gives the operator-norm tail. Hence, for the channel-size variable 

M (ζ, α, W ) = 1 + |α| + ∥ζ∥∞ + PUℓ K−1/2 

> ℓ

ζ + N −1/2 ∥W ∥op ,

23 we have EπM 2 ≤ poly( A, R, d ) after projecting to the averaged single-site/matrix marginals. Choose T ≥ poly( A, R, d )/η 2 so that Eπ[M 1M >T ] ≤ η/A by Cauchy–Schwarz/Markov. At the readout, boundedness of φ gives directly 

E M −1 X

> i

αiφ(ζi)1GcT

> ∞

≤ E¯ρ|α|1GcT ≤ η. 

For a hidden layer, A-regularity gives 

∥Sℓ+1 (ξ) − Sℓ+1 (ξ1GT )∥∞ ≤ A

 1

M

X

> j≤M

∥ξj ∥2 

> ∞

1GcT (j)

1/2

.

Taking expectation and using exchangeability/Jensen bounds this by A E¯ρ ∥ξ∥2 

> ∞

1GcT

1/2 ≤ η

after increasing T by a polynomial factor. None of the estimates uses independence under the posterior; only marginal KL bounds and exchangeability are used. The corresponding adjoint and inductive closure estimates are spelled out in Lemma A.8. 

Lemma A.8 (Backward truncation closure) . Fix a layer ℓ and suppose that the already-constructed suffix Sℓ+1 is A-regular in the sense of Definition A.6. Let Tℓ denote the layer map sending the layer-ℓ channel population to the layer-(ℓ + 1) field input read by Sℓ+1 , and let T Gℓ be the same map after deleting the channels outside the kept event GT of Proposition A.7. For every η > 0

one may choose 

T = poly( A, R, ¯β, d, 1/η )O(1) 

so that 

E Sℓ+1 (Tℓ) − Sℓ+1 (T Gℓ ) ∞ ≤ η. (15) 

Moreover the enlarged suffix 

SGℓ := Sℓ+1 ◦ T Gℓ

is A+-regular, with A+ = poly( A, R, ¯β, d, 1/η )O(1) . If the layers k > ℓ have already been truncated with errors ηk, then after truncating layer ℓ the total output error is at most η + P 

> k>ℓ

ηk. The same statement holds conditionally on any mean-field vacuum, uniformly over vacua satisfying the same KL/free-energy bound. Proof. The output estimate is exactly the Lipschitz version of Proposition A.7. Writing u = Tℓ

and uG = T Gℓ , the A-Lipschitz property gives 

Sℓ+1 (u) − Sℓ+1 (uG) ∞ ≤ A d(u, u G).

The deleted part of u − uG is the normalized average of the bad layer-ℓ channel fields. The KL-rate bound, Lemma A.3, and Lemma A.4 give polynomial second-moment and tail bounds for precisely these channel fields, including the cube maximum and the retained active Cameron– Martin coefficients. Choosing T as in Proposition A.7 therefore makes Ed(u, u G) ≤ η/A , which proves (15). It remains to check that the invariant closes after composing with the kept layer. On GT ,the activation is bounded and Lipschitz, the incoming normalized operator norm, retained active coefficients, scalar/readout coefficients, and relevant field sup norms are all bounded by T . Hence the map T Gℓ is Lipschitz for the normalized channel metrics, with constant poly( T, d ), and its first adjoint acting on any suffix test function has L2(μ) norm bounded by poly( T, d ) times 24 the adjoint norm of the test function. Composing these two estimates with (I3)–(I4) for Sℓ+1 

gives (I3)–(I4) for SGℓ with constant A+ = poly( A, T, d ). The size and bounded-output clauses (I1)–(I2) obey the same polynomial update because only polynomially many active channels and polynomially many fresh-chaos channels are kept in the compression step. Finally, truncating several layers gives a telescoping sum: replace the rightmost untruncated layer one at a time and apply the preceding output estimate at each step. Conditional-on-vacuum versions are identical, since all estimates use only the common KL/free-energy bound and the deterministic A-regular suffix in that vacuum. 

Proposition A.9 (One-step lazy-score bound) . Let Sℓ+1 be A-regular and let gℓ be the single-site effective potential at layer ℓ induced by the Gibbs loss at intensive temperature ¯β, with output residual |f − y| ≤ A on the kept set. Then the population lazy score satisfies 

Eρh ∥∇ lazy gℓ∥L2(μ) ≤ C ¯β A 2.

For the empirical loss the same magnitude bound holds for the L2(D) score. With the empirical active space chosen as the top eigenspace of the normalized empirical evaluation covariance, the projection of the empirical score onto the lazy block is not zero in general, but its contribution to the lazy-swap input is bounded by Lemma B.3 of Appendix B. Proof. The score is a first variation of the mean-field loss, not an ordinary Lipschitz derivative in the normalized channel metric. Let νℓN = N −1 P 

> j≤N

δζℓj be the layer-ℓ empirical law, and write the population loss, after the suffix to the right has been fixed, as a smooth finite-dimensional functional Φ( νℓN ). Replacing one atom ζj by ζj +tu changes νℓN by (1 /N )( δζj +tu −δζj ). Therefore 

ddt t=0 N ¯β Φ( νℓN ) = ¯β D u

 δΦ

δν ℓ



(ζj ).

This is the cancellation that makes the single-site Gibbs score intensive: it uses the 1/N empirical-measure weight of one atom. It is not obtained from the single-channel Lipschitz bound (I4), which is only O(N −1/2) because it is measured in the normalized chaos metric. The variational derivative above is the usual back-propagated costate. At the readout layer it is explicit: 

Du

 δΦ

δν L−1



(α, ζ ) = 2 α

Z

> Bd

(f − y)( x)φ′(ζ(x)) u(x) dμ (x),

so its L2(μ) norm is ≤ CA 2 on the kept set. For earlier layers, differentiating through the kept suffix gives the same form with (f − y) replaced by the corresponding adjoint/costate field. The A-regularity package and Lemma A.8 bound this costate and the kept operator norms by 

poly( A); after enlarging A once, this gives 

∇ζj gℓ L2(μ) = ¯β ∇ζ (δΦ/δν ℓ)( ζj ) L2(μ) ≤ C ¯βA 2.

Projecting to the lazy block can only decrease the norm, and averaging over the active coordinates therefore yields the displayed population bound. For the empirical loss bLD = 1

> n

P

> a

(f (xa) − ya)2 the same first-variation calculation gives an intensive score of the form P 

> a

caexa , with ∥c∥2 ≤ CA 2/n after absorbing the bounded residual and adjoint constants into A. Thus the score lies in the empirical evaluation span. With the top empirical evaluation eigenspace included in the active block, the residual lazy projection is controlled in quadratic form by Lemma B.3. This is the empirical lazy-score input used in the lazy-swap step. 25 A.4 The regularity package 

Proposition A.10 (Regularity package) . Assume the mean-field tower with propagation of chaos 

(see Appendix D) , the Gibbs KL-rate bound 1 

> N

DKL (π∥π0 

> N

) ≤ R with ¯β ≤ R, and the A-regularity of the suffix already built to the right. For every η > 0 there is A+ = poly( R, ¯β, d, 1/η )O(1) such that, after discarding a posterior sub-population whose contribution to the suffix output is at most 

η in sup-norm, the quantities needed for the layer-ℓ compression step are all ≤ A+:

(i) readout magnitudes and retained scalar coefficients; 

(ii) normalized operator norms 1√N W ℓ 

> kept op

;

(iii) Cameron–Martin active coefficients PUℓ K−1/2 

> ℓ

ζ of retained downstream fields; 

(iv) population lazy scores Eρh ∥∇ lazy gℓ∥, or the empirical lazy-swap input after enlarging Uℓ by the empirical evaluation directions; 

(v) the Lipschitz and adjoint constants (I3)–(I4) of the suffix enlarged by the new layer. Consequently the induction invariant of Definition A.6 is preserved with constant A+, and the next compression step proceeds at polynomial width and weight. Proof. By Lemma A.2 the averaged single-site marginal has DKL ≤ R against the Gaussian single-site prior, so Lemma A.3 bounds the second moment of every fixed finite-dimensional coordinate (readouts, biases, retained scalar coefficients), and Proposition A.7 upgrades these moment bounds to a kept event whose removed population has suffix-output contribution at most 

η, and Lemma A.8 records the backward-induction closure: after the bad channels are deleted, the enlarged suffix remains regular with only a polynomial increase in the invariant. This gives (i) in the sense actually needed by the compression induction, not merely small posterior mass. Item (ii) is Lemma A.4, and (iii) is Corollary A.5. Item (iv) is Proposition A.9. For (v), the newly constructed incoming matrix is W = W G + W A with W Aji = 1√N

P 

> a≤r

bja ⟨hi, q a⟩; the fresh Gaussian part obeys W G 

> op

/pMℓ+1 ≤ A+ by Lemma A.4, while the factored active part obeys W A 

> op

/pMℓ+1 ≤ A+ from r, ∥bj ∥ , | ⟨ hi, q a⟩ | ≤ A+ (Lemma B.9 of Appendix B); the normalized-channel inequality then propagates (I3)–(I4) with a polynomial increase, and the depth is fixed. It remains to record the bounded-differences regularity used to upgrade the lazy-swap and covariance comparison from expectation to high probability. This concentration is not obtained by applying (I4) to the realized lazy chaos: (I4) gives only an M −1/2 

> ℓ+1

single-channel sensitivity in the normalized chaos metric, which would not yield vanishing bounded-differences variance. Instead, after conditioning on the lower-layer features and performing the lazy swap, the suffix depends on the new channels only through M −1 

> ℓ+1

empirical averages: the active-reconstruction averages and the empirical lazy covariance that parametrizes an independent fresh Gaussian field. Hence replacing one constructed channel changes each consumed average by O(A+/M ℓ+1 ),and McDiarmid’s inequality gives the desired high-probability upgrade. This is precisely the mechanism formalized in Lemma B.13 of Appendix B; the right-to-left propagation of the clause is part of that lemma, not a consequence of (I4). 

# B Pointwise compression 

We prove the compression half of the engine: a mean-field network achieving accuracy ε is reproduced, at every input simultaneously, by a polynomial-width network. The construction 26 is right-to-left. With the suffix already replaced by a finite, regular network, we subsample the next layer’s neurons from the single-site law, reconstruct the active coordinates of each surviving downstream field deterministically from finitely many feature averages, and supply the lazy coordinates by fresh prior chaos. The lazy swap (Lemma B.2) certifies that replacing the realized tilted lazy content by prior noise is invisible to the suffix; the normalized-channel Lipschitz induction (§B.5) propagates the layerwise errors at polynomial cost. Every regularity input is supplied by Appendix A. 

B.1 The active–lazy split 

Definition B.1 (Active–lazy split) . Fix a layer and write K = Kℓ, h = φ(ζ). Let q1, . . . , q r ∈ F 

be read directions, ψa = Kq a, Gab = ⟨qa, Kq b⟩ (assumed invertible). The active covariance is KA = P 

> a,b

ψa(G−1)ab ψb and the lazy covariance C = K − KA. Equivalently, with U =span {K1/2qa} in whitened coordinates, KA = K1/2PU K1/2 and C = K1/2PU ⊥ K1/2. We write 

λ0 = ∥C∥op and assume C(x, x ) ≤ 1 (automatic for a spectral split, since K(x, x ) ≤ 1). In this paper the active space is Uℓ = U pop  

> ℓ

⊕ U emp  

> ℓ

in whitened Cameron–Martin coordinates. The population part U pop  

> ℓ

is the top eigenspace of Kℓ above cutoff λ0. For a dataset D = ( xa)a≤n,define the empirical evaluation operator 

EDu =   D

u, K 1/2 

> ℓ

exa

E 

> a≤n

, TD = 1

n E∗

> D

ED = 1

n

X

> a≤n

(K1/2 

> ℓ

exa ) ⊗ (K1/2 

> ℓ

exa ).

The empirical part U emp  

> ℓ

is the top eigenspace of TD above cutoff τ . The population part controls the field-space lazy swap; the empirical part controls the residual coupling of the empirical score (Lemma B.3). Both are polynomial-dimensional: dim U pop  

> ℓ

≤ 1/λ 0 and dim U emp  

> ℓ

≤ 1/τ , since 

tr Kℓ ≤ 1 and tr TD = 1 

> n

tr bKℓn ≤ 1.

B.2 The lazy swap 

Lemma B.2 (Lazy swap) . Fix the active coordinates h and let ρh(dv ) ∝ e−g(h+v) N (0 , C )( dv )

on U ⊥. For F (v) = G(v(x1), . . . , v (xp)) with G bounded and 1-Lipschitz in each argument, 

Eρh F − EN (0 ,C )F ≤ p Eρh C1/2∇vg F ,

uniformly in h and in x1, . . . , x p. In particular: 

(a) (population) the right side is ≤ p√λ0 sup h Eρh ∥∇ vg∥;

(b) (empirical) if ∇vg is the lazy part of an empirical-loss score P 

> a

caexa , then Eρh C1/2∇vg 2 =

c⊤ bCnc, bounded in Lemma B.3. Proof. Whiten: v = C1/2 ˜v, ˜v ∼ N (0 , I ), and ˜g(˜ v) = g(h + C1/2 ˜v), so ∇˜v ˜g = C1/2∇vg. Each evaluation is v(xi) = ˜v, C 1/2exi with C1/2exi 

> 2

= C(xi, x i) ≤ 1, so ˜F (˜ v) := F (C1/2 ˜v)

is 1-Lipschitz in each of p unit-gradient linear forms. Let L = ∆ − ⟨ ˜v, ∇⟩ be the Ornstein– Uhlenbeck generator and solve Lu = ˜F − EN (0 ,I ) ˜F . Mehler’s formula ∇Pt = e−tPt∇ gives 

∥∇ u∥∞ ≤ R ∞ 

> 0

e−t ∇Pt ˜F ∞ dt ≤ ∇ ˜F ∞ ≤ p. Since ρh ∝ e−˜gN (0 , I ), self-adjointness of L for 

N (0 , I ) gives, with ψ = e−˜g (so ρh = ψ N (0 , I )/Z and ∇ψ = −ψ ∇˜g), 

Eρh [Lu] = 1 

> Z

EN (0 ,I )[ψ Lu] = − 1 

> Z

EN (0 ,I ) ⟨∇ ψ, ∇u⟩ = Eρh ⟨∇ ˜v ˜g, ∇u⟩ ,

27 i.e. Eρh ˜F −EN (0 ,I ) ˜F = Eρh ⟨∇ u, ∇˜v ˜g⟩. Hence the left side is ≤ ∥∇ u∥∞ Eρh ∥∇ ˜v ˜g∥ ≤ p Eρh C1/2∇vg .For (a), C1/2∇vg ≤ √λ0 ∥∇ vg∥. For (b), C1/2∇vg 2 = ⟨∇ vg, C ∇vg⟩ = P 

> a,b

cacb C(xa, x b) = 

c⊤ bCnc, using ∇vg = P 

> a

caPU ⊥ exa and CP U ⊥ = C.

B.3 The lazy-Gram bound 

Lemma B.3 (Lazy-Gram operator norm) . Let D = ( xa)a≤n and let ED, T D be the basis-free em-pirical evaluation operator and covariance from Definition B.1. Let U emp contain the eigenspaces of TD with eigenvalue at least τ , and set 

C = K1/2P(U pop ⊕U emp )⊥ K1/2, bCD = [ C(xa, x b)] a,b ≤n.

Then, deterministically, 

bCD op ≤ nτ. 

Consequently, for any empirical-loss score ∇g = P 

> a

caexa with ∥c∥2 ≤ B2/n ,

c⊤ bCDc ≤ τ B 2, so the empirical lazy-swap error is ≤ p√τ B. 

Proof. For u ∈ F in whitened coordinates, EDu = ( u, K 1/2exa )a and TD = n−1E∗

> D

ED. The nonzero eigenvalues of TD are the eigenvalues of n−1EDE∗ 

> D

= n−1 bKD. Removing the top eigenspaces of TD above cutoff τ therefore leaves 

EDP(U emp )⊥ E∗  

> Dop

= n λ r+1 (TD) ≤ nτ. 

Adding the population active space only decreases the projection, so bCD = EDP(U pop ⊕U emp )⊥ E∗

> D

has operator norm at most nτ . The count of empirical directions is r ≤ τ −1 tr TD = τ −1n−1 tr bKD ≤

1/τ . The quadratic-form inequality is Cauchy–Schwarz, and Lemma B.2(b) gives the lazy-swap conclusion. The factor n in bCD op cancels the 1/n in empirical-loss score norms, which is why an inverse-polynomial τ suffices. 

B.4 Finite sampled features: active reconstruction and fresh chaos 

Lemma B.4 (Empirical active reconstruction) . Suppose r ≤ R, G−1 

> op

≤ R, and the read variables are bounded, | ⟨ h, q a⟩ | ≤ R on the relevant support. With probability 1 − o(1) , provided 

N ≥ poly( R, d, 1/η ), for every b ∈ Rr with ∥b∥ ≤ B,

sup 

> x∈Bd
> r

X

> a=1

ba

h 1

> NN

X

> i=1

⟨hi, q a⟩ hi(x) − ψa(x)

i

≤ ηB. 

Hence the active weights W Ai = 1√N

P 

> a

ba ⟨hi, q a⟩ induce, under μP, 1√N

P 

> i

W Ai hi = P 

> a

baψa +

O∞(ηB ).Proof. For fixed a, x the variable ⟨hi, q a⟩ hi(x) is bounded by R (as |hi(x)| ≤ 1) with mean 

(Kq a)( x) = ψa(x). Hoeffding and a union bound over the rD pairs (a, x ) give entrywise error 

≤ η/r once N ≥ CR 2r2 log( rD )/η 2 = poly( R, d, 1/η ); sum against b. The last identity is the 

N −1/2 normalization. 28 For the lazy field, draw ξ = ( ξ1, . . . , ξ N ) ∼ N (0 , P U ⊥ ) (Euclidean projection in RN off the empirical active read vectors) and set vN (x) = 1√N

P 

> i

ξihi(x). Conditional on the features, vN

is centered Gaussian with covariance bC(x, x ′) = 1 

> N

h(x)⊤PU ⊥ h(x′).

Lemma B.5 (Lazy-covariance concentration) . Under the hypotheses of Lemma B.4, with prob-ability 1 − o(1) , sup x,x ′∈Bd | bC(x, x ′) − C(x, x ′)| ≤ η for N ≥ poly( R, d, 1/η ); likewise for any polynomial-size list of consumed entries. Proof. First bK(x, x ′) → K(x, x ′) uniformly over the D2 pairs by Hoeffding (cost O(d) in N ). Writing bC = bK −bk bG−1bk⊤ with bka(x) = 1

> N

P 

> i

hi(x) ⟨hi, q a⟩, Lemma B.4 gives bka → ψa uniformly and bG → G entrywise; since G−1 ≤ R, bG−1 → G−1, so bk bG−1bk⊤ → kG −1k⊤ = KA uniformly, whence bC → K − KA = C.

Remark B.6 (Scaling) . The chaos weights ξi are raw order-one weights: under μP, 1√N

P 

> i

ξihi

has covariance 1

> N

P 

> i

hi ⊗ hi. The deterministic active corrections W Ai are order N −1/2, imple-menting a mean-field average. 

B.5 Normalized-channel Lipschitz induction 

Definition B.7 (Normalized channel metric) . For ξ = ( ξ1, . . . , ξ M ) ∈ F M , dM (ξ, ξ ′) = sup x∈Bd

  1

> M

P 

> j≤M

|ξj (x)−

ξ′ 

> j

(x)|21/2; a suffix S : FM → RBd

is Λ-Lipschitz if ∥S(ξ) − S(ξ′)∥∞ ≤ Λ dM (ξ, ξ ′).

Lemma B.8 (Lipschitz propagation) . For T (ζ)j (x) = 1√N

P 

> i

Wji φ(ζi(x)) , dM (T ζ, T ζ ′) ≤

Lφ  

> ∥W∥op
> √M

dN (ζ, ζ ′). Hence if S is Λ-Lipschitz then S ◦ T is ΛLφ ∥W ∥op /√M -Lipschitz. Proof. Fix x and put ∆h(x) = ( φ(ζi(x)) − φ(ζ′

> i

(x))) i, so ∥∆h(x)∥2 ≤ Lφ

√N dN (ζ, ζ ′). The out-put difference is N −1/2W ∆h(x), so   1

> M

P 

> j

|T (ζ)j (x) − T (ζ′)j (x)|21/2 = 1√M √N ∥W ∆h(x)∥2 ≤

Lφ  

> ∥W∥op
> √M

dN (ζ, ζ ′); take sup x.

Lemma B.9 (Operator bound for constructed weights) . Write a constructed incoming matrix as 

W = W A + W G with W G the fresh Gaussian part and W Aji = 1√N

P 

> a≤r

bja ⟨hi, q a⟩. If ∥bj ∥2 ≤ B

for every downstream j and (⟨hi, q a⟩)a≤r 2 ≤ R√r for every i, then 1√M W A 

> op

≤ BR √r;after standard truncation 1√M W G 

> op

≤ poly(1 , pN/M ) w.h.p. Hence at polynomial width ratios 1√M ∥W ∥op is polynomial. Proof. With Bmat = ( bja )j,a and U = ( ⟨hi, q a⟩)i,a , W A = N −1/2Bmat U⊤. Each row of Bmat 

has norm ≤ B, so ∥Bmat ∥op ≤ B√M ; each row of U has norm ≤ R√r, so ∥U∥op ≤ R√rN .Thus W A 

> op

/√M ≤ (B√M )( R√rN )/(√M √N ) = BR √r. The Gaussian part is a standard rectangular random-matrix bound (Lemma A.4 with κ = 0 ), after truncating the tail of large rows. 

Proposition B.10 (Regularity induction) . If at each step W ℓ 

> op

/pMℓ+1 ≤ Aℓ ≤ poly , then 

Λℓ ≤ Λℓ+1 LφAℓ, so Λℓ ≤ poly O(L) at fixed depth. Proof. Lemma B.8 composed right-to-left. 29 B.6 One right-to-left step 

Definition B.11 (Regular suffix) . S : FM → RBd

is (M, Λ, Γ) -regular if it is Λ-Lipschitz for 

dM , satisfies the adjoint bound (I4) of Definition A.6, and, as a function of its M independent input channels, has influence ≤ Γ/M on each output coordinate. 

Proposition B.12 (One step) . Let S be (M, Λ, Γ) -regular and the regularity package (Proposi-tion A.10) hold at layer ℓ, with kept active coefficients ∥bj ∥ ≤ B. For N ≥ poly( M, Λ, Γ, B, r, R, d, 1/η ),the construction Wji = W Aji + ξji (ξj ∼ N (0 , P U ⊥ ) independent across j) gives, with probability 

1 − o(1) ,

S(bζ1, . . . , bζM ) − S(ζ1, . . . , ζ M ) ∞ ≤ poly( M, Λ, Γ, B, R, r ) η + sw ℓ

 + τtrunc ,

where sw ℓ = √λ0 c⋆ (population) or √τ B (empirical, Lemma B.3). Proof. Three contributions. Active reconstruction: by Lemma B.4, ∥buA,j − uA,j ∥∞ ≤ ηB , so 

dM (buA, u A) ≤ ηB and the Λ-Lipschitz suffix gives output error ≤ ΛηB . Lazy covariance: 

conditional on the features the constructed lazy field is Gaussian with covariance bC = C + O(η)

on the consumed pairs (Lemma B.5); a Gaussian coupling on the poly consumed coordinates, with the Lipschitz suffix, contributes ≤ poly( M, Λ, Γ) η. Lazy swap: comparing tilted lazy fields to independent prior lazy fields N (0 , C ) at fixed active coordinates, Lemma B.2 on the consumed evaluations contributes ≤ poly( M, Λ, Γ) sw ℓ. Add τtrunc . Concentration from expectation to high probability uses the bounded-differences clause and a union bound over Bd (cost √d). 

Lemma B.13 (Bounded-differences propagation) . Let S be (M, Λ, Γ) -regular and build a new layer of width N as in Proposition B.12, with ∥W ∥op /√M ≤ A, kept active coefficients ∥bj ∥ ≤ B

read over r = dim U directions at leverage ≤ R, and lazy cutoff λ0. Then S ◦ T is (N, Λ′, Γ′)-regular with the explicit constants 

Λ′ ≤ ΛLφA, Γ′ ≤ 2 Λ Lφ (1 + BR √r) (1 + λ−1/20 ),

and the reconstructed output concentrates around its conditional mean: for any η > 0, with probability ≥ 1 − η,

(reconstruction ) − E[ · | features ] ∞ ≤ Γ′

q d log 2+log(2 /η )2N .

At fixed depth Λℓ ≤ (LφA)O(L) and Γℓ ≤ poly O(L).Proof. Lipschitz. By Lemma B.8, S ◦ T is ΛLφ(∥W ∥op /√M )-Lipschitz, i.e. Λ′ ≤ ΛLφA.

Influence. The point is that the N new channels enter the reconstructed output only through two 1 

> N

-averages, never through the realized chaos. After the lazy swap (Lemma B.2) the suffix reads (active reconstruction) + (an independent fresh N (0 , bC) field); the swapped noise is drawn independently of the layer’s neurons, so perturbing a channel has no effect through it. The only data-dependent quantities are the active averages bmk(x) = 1

> N

P

> i≤N

Dˆhi, ψ k

E ˆhi(x) (k ≤ r)and the empirical lazy Gram bC(x, x ′) = 1

> N

P

> i

(PU ⊥ ˆhi)( x)( PU ⊥ ˆhi)( x′) that sets the fresh field’s covariance. Each is a 1 

> N

-weighted average of N independent terms bounded by 1 + λ−1/20 (the lazy leverage, P 

> k∈U

ψk(x)2 ≤ λ−10 from the split), so replacing one channel changes it by ≤

2(1 + λ−1/20 )/N in sup-norm. The reconstruction reads the active averages through coefficients 30 of total size ≤ BR √r (Lemma B.9) and the ΛLφ-Lipschitz suffix, so the influence of channel i

on each output coordinate is 

ci ≤ ΛLφ · 2(1 + BR √r)(1 + λ−1/20 )

N = Γ′

N .

Had one instead propagated the realized chaos field — whose column has norm Θ( √M ) — the per-channel influence would be Θ(Λ /√N ) and P 

> i

c2 

> i

= Θ(Λ 2) would not vanish; it is the lazy swap, replacing that field by an independent draw, that removes the dependence and leaves only the 1 

> N

-averages. McDiarmid over the N independent channels gives P 

> i

c2 

> i

≤ Γ′2/N , and a union bound over Bd (cost d log 2 ) yields the stated deviation. Composing the Λ- and Γ-recursions over the fixed L − 1 layers gives the depth-O(L) bounds. 

B.7 Backward induction 

Theorem B.14 (Pointwise compression) . Fix depth L and ∆. Under the mean-field tower (Lemma D.1) and the regularity package (Proposition A.10), with Uℓ = U pop  

> ℓ

⊕ U emp  

> ℓ

at inverse-polynomial cutoffs λ0, τ , there is a network of width and weight size ≤ poly( K, d, 1/∆) O(L) such that, with probability 1 − o(1) over the subsampling and chaos, 

sup 

> x∈Bd

fbθ(x) − f∞(x) ≤ ∆.

The population case takes U emp = {0}; the empirical case includes U emp  

> ℓ

and uses the lazy-swap branch of Lemma B.3. This is the compression half of the engine (Theorem 5.4) .Proof. Initialization. Sample ML−1 readout neurons, cap |αj | ≤ A, and set f (L−1) = 1

> ML−1

P 

> j

αj φ(ζL−1 

> j

).Hoeffding and a union bound over Bd give f (L−1) − f∞ ∞ ≤ O(A√d/ pML−1) + τcap , and the capped readout suffix is (ML−1, O (A), O (A)) -regular. 

Induction. Suppose after constructing layers ℓ + 1 , . . . , L − 1 the suffix is (Mℓ+1 , Λℓ+1 , Γℓ+1 )-regular and approximates f∞ to sup-norm error Eℓ+1 . Apply Proposition B.12 to build layer ℓ;Lemma B.13 preserves the invariant, and 

Eℓ ≤ Eℓ+1 + poly O(L) ηℓ + sw ℓ

 + τtrunc ,ℓ .

At fixed depth, choosing ηℓ, the lazy scale sw ℓ (i.e. λ0, τ ), and τtrunc ,ℓ below ∆/poly O(L), and the widths polynomially large, telescopes to E0 ≤ ∆. Weight bounds: active weights are 

O(poly /√N ) (Lemma B.4), chaos weights are raw O(poly) after truncation (Remark B.6), read-outs are capped. 

# C Conservation: the cloning construction 

This appendix supplies the deviation-propagation bound behind Theorem 6.1 — the step that makes the free within-group weights harmless — together with the entropy accounting. Fix a depth-L, width-W teacher θ⋆ with W ⋆,ℓ b ≤ B and |α⋆b | ≤ B, and write N = Wm with clone groups of size m, group b cloning teacher neuron b.31 C.1 Construction 

For i in group a and j in group b, the cloned hidden weight is W ℓ+1  

> ji

= 1√m W ⋆,ℓ +1  

> ba

, i.e. the block matrix 1√m (W ⋆,ℓ +1 ⊗1m×m); first-layer weights, biases and readouts are cloned within each group (W 1 

> j

= W ⋆, 1 

> b

, bℓj = b⋆,ℓ b , αj = α⋆b ). The clone law πcyl  

> N

is the prior conditioned on the cylinder 

C: for each neuron j in group b and each layer, the W group-mean coordinates 

D

W ℓ+1  

> j, ·

, u a

E

(ua = m−1/21group a), the d first-layer entries, the bias, and the readout lie within ρ of their cloned targets; the N − W within-group orthogonal coordinates of each row are unconstrained (at the prior). Write ζℓb,s for the field of the sth neuron of group b, zℓb,s = φ(ζℓb,s ), group mean ¯zℓb = 1

> m

P 

> s

zℓb,s ,and teacher field ζ⋆,ℓ b , z⋆,ℓ b = φ(ζ⋆,ℓ b ). Define the spread and bias 

σ2 

> ℓ

(x) = 1

> N

X

> b,s

zℓb,s (x) − ¯zℓb (x) 2, δℓ = max  

> b

sup 

> x

¯zℓb (x) − z⋆,ℓ b (x) .

C.2 Deviation propagation 

Lemma C.1 (Spread and bias) . On the cylinder C, with probability 1 − o(1) over the free coordinates, 

σℓ ≤ poly( d, L φ)L ρ, δℓ ≤ poly( W, d, B, L φ)L ρ, 

and consequently sup x |fclone (x) − fθ⋆ (x)| ≤ poly( W, d, B, L φ)Lρ.Proof. Base. For j = ( b, s ), ζ1 

> b,s

= W 1 

> b,s

· x + b1 

> b,s

with W 1

> b,s

, b 1 

> b,s

within ρ of (W ⋆, 1 

> b

, b ⋆, 1 

> b

), so 

|ζ1 

> b,s

− ζ⋆, 1 

> b

| ≤ ρ(√d + 1) on Bd, giving σ1, δ 1 ≤ Lφ(√d + 1) ρ.

Step. Decompose the forward sum over each input group a using zℓa,t = ¯ zℓa + ( zℓa,t − ¯zℓa). The group-mean constraint P 

> t

W ℓ+1 (b,s ),(a,t ) = √m (W ⋆ba + e(b,s ),a ) with |e| ≤ ρ gives, since  

> √m√N

= 1√W ,

ζℓ+1  

> b,s

− ζ⋆,ℓ +1  

> b

= 1√W

X

> a

W ⋆ba δ(a)

> ℓ

| {z }

> bias transport

+ 1√W

X

> a

e(b,s ),a ¯zℓa

| {z }

> ≤ρ√W

+ 1√N

X

> a,t

W ⊥

> (b,s ),(a,t )

 zℓa,t − ¯zℓa

| {z } 

> =: νb,s ,the noise

+( bℓ+1  

> b,s

− b⋆b ),

where δ(a) 

> ℓ

= ¯ zℓa − z⋆,ℓ a and the group-mean part of the row annihilates the deviation (so only the free W ⊥ enters ν). Conditionally, νb,s has mean 0 and variance 1

> N

P

> a,t

(zℓa,t − ¯zℓa)2 = σ2 

> ℓ

(x),independent across (b, s ), and is sub-Gaussian with proxy ≤ 2σℓ(x) since |zℓa,t − ¯zℓa| ≤ 2.

Concentration, per group then global. Within group b the m noises {νb,s }s are i.i.d. mean-zero, so by Bernstein for sub-exponential variables their empirical second moment obeys P | 1

> m

P 

> s

ν2

> b,s

−

σ2 

> ℓ

| > 12 σ2

> ℓ

 ≤ 2e−cm for an absolute c > 0. A union bound over the W groups and the 2d inputs costs log W + d log 2 in the exponent, so once m ≥ C(d + log W ) — automatic as N = Wm → ∞ 

— every group concentrates simultaneously with probability 1 − o(1) . On that event the within-group spread of zℓ+1  

> b,s

= φ(ζℓ+1  

> b,s

) is at most Lφ times the noise std plus the bias tube, and averaging the squared spread over all N neurons gives σℓ+1 ≤ Lφ(2 σℓ + ρ); the group-mean averaging fluctuation ¯νb has size ≤ σℓ/√m → 0 and is absorbed into the bias recursion. Iterating from 

σ1 ≤ Lφ(√d + 1) ρ gives the explicit closed form σℓ ≤ (√d + 1)(2 Lφ)ℓρ, free of W, B .For the bias, |δℓ+1 | ≤ Lφ 1

> m

P 

> s

|ζℓ+1  

> b,s

− ζ⋆,ℓ +1  

> b

|, and the display bounds the right side by 

Lφ

 B δ ℓ +ρ√W+σℓ +ρ (using | 1√W

P 

> a

W ⋆ba δ(a) 

> ℓ

| ≤ 1√W ∥W ⋆b ∥ √W δℓ ≤ Bδ ℓ and 1

> m

P 

> s

|νb,s | ≤ σℓ

32 on the concentration event). Hence δℓ+1 ≤ LφB δ ℓ + Lφ(√W + 1) ρ + Lφσℓ, and substituting 

σℓ ≤ (√d + 1)(2 Lφ)ℓρ and iterating from δ1 ≤ Lφ(√d + 1) ρ gives the explicit closed form 

δℓ ≤ 4ℓ (√W + √d) (2 LφB)ℓρ. The output bound follows from fclone = 1

> W

P 

> b

αz b with αz b =

α⋆b (z⋆,L −1 

> b

+ δ(b)

> L−1

) + O(ρ).

C.3 Entropy and change of measure 

Proof of Theorem 6.1. Entropy. The cylinder C is a product over neurons and, within each neuron, over the d + W + 1 constrained Gaussian coordinates (the rest free). A standard N (0 , 1) 

coordinate pinned to [ t − ρ, t + ρ ] has − log P(·) = log 1 

> ρ

+ 12 t2 + O(1) , and the free coordinates contribute 0. With targets of size ≤ B ≤ poly( W, d ),

DKL 

 πcyl  

> N

∥π0

> N

 = − log π0 

> N

(C) = N (d + W + 1) log 1 

> ρ

+ O(poly( W, d )) .

Loss. By Lemma C.1, sup x |fclone − fθ⋆ | ≤ Θ := poly( W, d, B, L φ)Lρ with probability ≥ 12 on 

C; choosing ρ = exp( −poly( W, d, B )) so that Θ ≤ ∆ gives, in sup-norm and hence in both L

and bLD, L(clone) ≤ (pL(θ⋆) + Θ) 2 ≤ ε− + ∆ ≤ ε. Thus πcyl  

> N

(L ≤ ε) ≥ 12 , and Kclone :=  

> 1
> N

DKL (πcyl  

> N

∥π0 

> N

) = poly( W, d, B ).

Change of measure. The data-processing inequality for the binary partition {L ≤ ε} under 

πcyl  

> N

≪ π0 

> N

gives, using πcyl  

> N

(L ≤ ε) ≥ 12 ,

− log π0 

> N

(L ≤ ε) ≤ 2DKL 

 πcyl  

> N

∥π0

> N

 + O(1) = N poly( W, d, B ),

so sN (y, ε ) ≤ poly( W, d, B ) for all large N and the lim sup agrees. Replacing L by bLD throughout (the sup-norm bound is dataset-independent) gives, for any fixed D and tolerance η with ρ =exp( −poly( W, d, B, log 1 

> η

)) ,

π0

> N

 bLD ≤ bLD(θ⋆) + η ≥ e−N poly( W,d,B, log 1 

> η)

,

the empirical-entropy bound consumed by the forward assembly. Memorization (Lemma 2.1) realizes any y at width O(2 d), giving s∞(y, ε ) ≤ poly(2 d).

# D Mean-field vacua in the full field space 

This appendix records only the formal infinite-width object. It is a fixed-d statement on the full field space 

X = RBd

, dim X = 2 d,

and its constants may depend arbitrarily on 2d. The polynomial active/lazy compression esti-mates used for learnability are proved elsewhere; they are not part of the mean-field existence theorem. For a fixed dataset D and intensive inverse temperature ¯β, let ΠDN, ¯β denote the empirical Gibbs law. A mean-field vacuum is a self-consistent tower 

v = ( νℓ, K ℓ, b ℓ, Γℓ)L−1

> ℓ=1

where νℓ is a one-site law on the layer-ℓ preactivation field (and the readout scalar at the last hidden layer), Kℓ+1 = Eνℓ φ(ζ)φ(ζ)⊤, and the fields are sampled from Gaussian kernel measures 33 tilted by the backward molecular fields. Allowing the readout coordinate to be included in the statistic, the one-site laws have the form 

dν ℓ

> v

(ζ) ∝ exp 

n

−

D

bℓ

> v

, φ (ζ)

E

−

D

φ(ζ), Γℓ

> v

φ(ζ)

Eo 

dN (0 , K ℓ

> v

)( ζ). (16) Equivalently, in coordinates on the support of the Gaussian, the reference measure contributes the quadratic term 12 ζ, (Kℓ

> v

)†ζ , where † denotes the Moore–Penrose inverse; this avoids assuming the kernel is nonsingular. The linear source bℓ vanishes in centered symmetric special cases; keeping it avoids making a spurious symmetry assumption. Equivalently, the vacua are the minimizers of the finite-dimensional mean-field free energy 

FD(v) = X

> ℓ

DKL (νℓ∥νℓ, 0 

> Kℓ

) + ¯β bLD(fv)

after imposing the forward kernel-consistency constraints; the multipliers for these constraints are precisely the backward tilts (bℓ, Γℓ).

Lemma D.1 (Mean-field limit as a mixture of vacua) . Fix d, L, D and ¯β < ∞. Along every width sequence Nj → ∞ there is a subsequence, a probability measure ρD on the set VD of minimizing mean-field vacua, and conditionally chaotic product laws Πv such that 

ΠDNj , ¯β =⇒

Z

> VD

Πv dρ D(v)

for bounded cylinder observables of finitely many neurons and their fields. Conditional on a vacuum v, any fixed finite collection of neurons in each layer is asymptotically i.i.d. with one-site laws νℓ

> v

, the kernels satisfy 

Kℓ+1  

> v

(x, x ′) = Eνℓ 

> v

φ(ζ(x)) φ(ζ(x′)) , Kℓ+1  

> v

(x, x ) ≤ 1,

and the output is 

fv(x) = EνL−1 

> v

αφ (ζ(x)) .

If the minimizer is unique, the mixture is a single deterministic mean-field tower. Proof sketch. This is the standard finite-dimensional mean-field Gibbs variational principle. The empirical measures of the neurons take values in probability measures on the finite-dimensional state space X (augmented by the readout scalar in the last layer). Under the product Gaussian prior, Sanov’s theorem gives the entropy term. The empirical Gibbs factor has the intensive form exp {− N ¯β bLD} and depends continuously on the empirical measures through the finite vector of outputs and the forward kernel recursion. Varadhan’s lemma, or equivalently the Gibbs variational principle plus the standard propagation-of-chaos theorem for exchangeable mean-field Gibbs measures, gives convergence of the free energy to inf v FD(v) and identifies all subsequential limits as mixtures of minimizers. Taking the first variation of FD at a minimizer gives dν ℓ/dν ℓ, 0 

> Kℓ

∝ exp {− δΦD/δν ℓ}, where 

ΦD is the loss plus the kernel-consistency Lagrangian. Since the loss and constraints depend on a layer only through the finite feature statistics φ(ζ) and their second moments, this varia-tional derivative is a linear plus quadratic function of φ(ζ), giving (16). The Gaussian readout coordinate is handled as part of the one-site state; standard finite-dimensional exponential-integrability/truncation approximations justify the same variational formula for the unbounded Gaussian prior. No polynomial-in-d estimate is asserted here. In the body of the paper we freely condition on a vacuum v. All regularity and compression estimates used there are uniform over vacua satisfying the relevant KL/free-energy budget, so after proving them conditionally one integrates over the mixing measure ρD.34 References 

[1] Boaz Barak, Benjamin L. Edelman, Surbhi Goel, Sham Kakade, Eran Malach, and Cyril Zhang. Hidden progress in deep learning: SGD learns parities near the computational limit. In Advances in Neural Information Processing Systems , 2022. [2] Blake Bordelon and Cengiz Pehlevan. Self-consistent dynamical field theory of kernel evo-lution in wide neural networks. volume 2023, 2022. [3] Gon Buzaglo, Itamar Harel, Mor Shpigel Nacson, Alon Brutzkus, Nathan Srebro, and Daniel Soudry. How uniform random weights induce non-uniform bias: Typical interpolating neural networks generalize with narrow teachers. In Proceedings of the 41st International Confer-ence on Machine Learning , volume 235 of Proceedings of Machine Learning Research , pages 5035–5081, 2024. [4] Lénaïc Chizat and Francis R. Bach. On the global convergence of gradient descent for over-parameterized models using optimal transport. In Neural Information Processing Systems ,2018. [5] Amit Daniely and Elad Granot. Generalization bounds for neural networks via approximate description length. In Advances in Neural Information Processing Systems , 2019. [6] Amit Daniely and Elad Granot. On the sample complexity of two-layer networks: Lipschitz vs. element-wise lipschitz activation. In Proceedings of The 35th International Conference on Algorithmic Learning Theory , volume 237 of Proceedings of Machine Learning Research ,pages 505–517, 2024. [7] Amit Daniely and Eran Malach. Learning parities with neural networks. In Advances in Neural Information Processing Systems , 2020. [8] Orit Davidovich and Zohar Ringel. Algorithmic task capture, computational complexity, and inductive bias of infinite transformers. arXiv preprint arXiv:2603.11161 , 2026. [9] Moritz Helias, Javed Lindner, Lars Schutzeichel, and Zohar Ringel. Lecture notes: From gaussian processes to feature learning. arXiv preprint arXiv:2602.12855 , 2026. [10] Arthur Jacot, Franck Gabriel, and Clément Hongler. Neural tangent kernel: convergence and generalization in neural networks. In Advances in Neural Information Processing Systems ,2018. [11] Clarissa Lauditi, Blake Bordelon, and Cengiz Pehlevan. Adaptive kernel predictors from feature-learning infinite limits of neural networks. ArXiv , abs/2502.07998, 2025. [12] Jaehoon Lee, Yasaman Bahri, Roman Novak, Samuel S. Schoenholz, Jeffrey Pennington, and Jascha Sohl-Dickstein. Deep neural networks as Gaussian processes. In International Conference on Learning Representations , 2018. [13] Moshe Leshno, Vladimir Ya. Lin, Allan Pinkus, and Shimon Schocken. Multilayer feed-forward networks with a nonpolynomial activation function can approximate any function. 

Neural Networks , 6(6):861–867, 1993. 35 [14] Song Mei, Andrea Montanari, and Phan-Minh Nguyen. A mean field view of the landscape of two-layer neural networks. Proceedings of the National Academy of Sciences of the United States of America , 115:E7665 – E7671, 2018. [15] Gadi Naveh and Zohar Ringel. A self consistent theory of gaussian processes captures feature learning effects in finite CNNs. arXiv preprint arXiv:2106.04110 , 2021. [16] Phan-Minh Nguyen and Huy Tuan Pham. A rigorous framework for the mean field limit of multilayer neural networks. Mathematical Statistics and Learning , 6(3):201–357, 2023. [17] Grant M. Rotskoff and Eric Vanden-Eijnden. Trainability and accuracy of artificial neural networks: An interacting particle system approach. Communications on Pure and Applied Mathematics , 75, 2018. [18] Noa Rubin, Orit Davidovich, and Zohar Ringel. Mitigating the curse of detail: Scaling arguments for feature learning and sample complexity. arXiv preprint arXiv:2512.04165 ,2025. [19] Noa Rubin, Kirsten Fischer, Javed Lindner, Inbar Seroussi, Zohar Ringel, Michael Kr"amer, and Moritz Helias. From kernels to features: A multi-scale adaptive theory of feature learning. In Proceedings of the 42nd International Conference on Machine Learning , volume 267 of Proceedings of Machine Learning Research , pages 52225–52257, 2025. [20] Noa Rubin, Zohar Ringel, Inbar Seroussi, and Moritz Helias. A unified approach to fea-ture learning in bayesian neural networks. In High-dimensional Learning Dynamics (HiLD) Workshop , 2024. [21] Inbar Seroussi and Zohar Ringel. Separation of scales and a thermodynamic description of feature learning in some cnns. Nature Communications , 14, 2021. [22] Justin A. Sirignano and Konstantinos V. Spiliopoulos. Mean field analysis of neural net-works: A central limit theorem. Stochastic Processes and their Applications , 2018. [23] Greg Yang and J. Edward Hu. Tensor programs iv: Feature learning in infinite-width neural networks. In International Conference on Machine Learning , 2021. [24] Jacob A. Zavatone-Veth, Blake Bordelon, and Cengiz Pehlevan. Summary statistics of learning link changing neural representations to behavior. arXiv preprint arXiv:2504.16920 ,2025. 36