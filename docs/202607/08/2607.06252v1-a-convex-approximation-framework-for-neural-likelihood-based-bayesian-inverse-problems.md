---
title: A Convex Approximation Framework for Neural Likelihood-Based Bayesian Inverse Problems
title_zh: 一种基于神经似然的贝叶斯逆问题的凸逼近框架
authors: "Fabian Schneider, Tapio Helin, Leila Taghizadeh"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06252v1"
tags: ["query:bayes-dl"]
score: 8.0
evidence: 神经似然近似解决贝叶斯逆问题
tldr: 许多科学工程问题中精确建模困难，经典贝叶斯推理受限于高维和计算成本。本文提出神经似然近似框架，通过最小化KL散度学习似然函数，并引入非归一化势函数将归一化融入训练目标，使学习问题成为严格凸优化。理论证明经验最小化器随样本量增长收敛到真实似然，数值实验在去模糊和非线性PDE成像中验证有效性。该工作为贝叶斯逆问题提供了更可靠的理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06252v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1543, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06252v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1541, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06252v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1379, \"height\": 1754, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06252v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1346, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06252v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 994, \"height\": 242, \"label\": \"Table\"}]"
motivation: 传统贝叶斯逆问题方法在高维复杂系统中计算困难，现有似然近似缺乏严格凸性理论保障。
method: 通过最小化KL散度训练似然代理，将归一化常数融入目标，构造非归一化势函数，使学习问题严格凸。
result: 经验最小化器在大样本下一致收敛到真实似然；数值实验在去模糊和非线性PDE成像中表现良好。
conclusion: 提出的凸近似框架提升了神经似然近似的理论严谨性，为高维贝叶斯逆问题提供可靠方法。
---

## 摘要
科学与工程中的许多问题难以准确建模，原因可能是物理机制未知、测量不确定性量化不佳，或者高保真模拟的计算成本过高。这些挑战限制了经典概率推断方法（如马尔可夫链蒙特卡洛）的适用性，尤其是在高维贝叶斯逆问题中。随着科学实验数据的日益丰富，机器学习方法为显式参数建模提供了一种灵活的替代方案。我们研究神经似然近似，其目标是在不了解底层数据生成过程的情况下，直接从数据中学习似然函数。一种常见的方法是通过最小化真实后验与近似后验之间的KL散度来训练似然代理，这等价于最小化期望负对数似然。本文通过缓解限制性模型类别的局限性，改进了神经似然近似的理论基础：我们证明，通过处理未归一化的势函数并将归一化纳入训练目标，所得的学习问题是严格凸的。我们证明，随着样本量的增加，所得数据驱动目标的经验最小化器收敛到真实似然。针对神经似然近似，我们进行了数值实验，涉及一个去模糊问题和一个基于非线性偏微分方程的成像问题。

## Abstract
Many problems in science and engineering are difficult to model accurately, either due to unknown physical mechanisms, poorly quantified measurement uncertainty, or prohibitive computational costs of high-fidelity simulations. These challenges limit the applicability of classical probabilistic inference methods such as Markov chain Monte Carlo, especially in high-dimensional Bayesian inverse problems. As data from scientific experiments become increasingly available, machine learning methods offer a flexible alternative to explicit parametric modelling. We study neural likelihood approximation, where the goal is to learn the likelihood function directly from data without explicit knowledge of the underlying data-generating process. A common approach trains likelihood surrogates by minimizing the Kullback-Leibler divergence between the true posterior and an approximate posterior, which is equivalent to minimizing the expected negative log-likelihood. This work improves the theoretical foundations of neural likelihood approximation by alleviating limitations of restrictive model classes: we show that, by working with un-normalized potentials and folding normalization into the training objective, the resulting learning problem is strictly convex. We show that empirical minimizers of the resulting data-driven objective converge to the true likelihood as the sample size grows. Numerical experiments for the neural likelihood approximation are conducted for a deblurring and a non-linear PDE based imaging problem.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：许多科学和工程问题中，要么物理机制未知、要么测量不确定性难以量化、要么高保真模拟计算成本过高，导致经典贝叶斯推断方法（如MCMC）在高维逆问题中难以应用。神经似然近似通过从数据直接学习似然函数提供了一种灵活的替代方案，但现有方法通常要求似然函数归一化为概率分布（如使用归一化流、自回归流等参数化模型），这限制了函数类的表达能力，且优化问题非凸。
- **研究动机**：本文旨在发展一种理论更健全的神经似然近似框架，通过将归一化过程融入训练目标，使学习问题变为严格凸优化，并证明经验最小化器的统计一致性，从而为高维贝叶斯逆问题提供可靠且高效的工具。

## 2. 论文提出的方法论：核心思想与关键技术细节

- **核心思想**：不直接学习归一化的概率密度，而是学习未归一化的势函数（负对数似然，NLL）\(f(x;y)\)，并通过除以归一化常数 \(Z_f(y)=\int_H e^{-f(x;y)}\mu(dx)\) 得到近似后验 \(\mu^y_f\)。将KL散度最小化目标改写为：
  \[
  \Phi(f) = \mathbb{E}_{\lambda}[f(x,y)] + \mathbb{E}_{\pi}[\log Z_f(y)] + \text{常数}
  \]
  该目标仅依赖于联合分布 \(\lambda\) 和边际 \(\pi\)，无需知道真实似然 \(L\)。
- **关键证明**：
  - **严格凸性**（Theorem 2.8）：利用Hölder不等式证明 \(\Phi\) 在函数类 \(F_\Phi\) 上是严格凸的。传统归一化参数化（如归一化流）的函数集是非凸的，而本文的未归一化势函数类若选定为凸集，则目标凸。
  - **一致性**（Theorem 3.7）：在有限维假设下（\(H=\mathbb{R}^n\)），假设函数类为有界 \(C^1\) 且满足二次上下界，经验目标 \(\Phi_N(f)\) 一致收敛到 \(\Phi(f)\)，从而近似最小化器依概率收敛到真实势函数 \(L\)。证明依赖于括号熵有限（Theorem 3.9）和嵌套蒙特卡洛估计的收敛性（Theorem 3.4）。
- **算法流程（文字说明）**：
  1. 从联合分布 \(\lambda\) 采样 \(N\) 对 \((x_i,y_i)\)；对每个 \(y_i\)，从先验 \(\mu\) 采样 \(M(N)\) 个 \(x_{i,j}\)。
  2. 定义经验目标：\(\Phi_N(f)=\frac{1}{N}\sum_{i=1}^N f(x_i,y_i) + \frac{1}{N}\sum_{i=1}^N \log\left(\frac{1}{M}\sum_{j=1}^M e^{-f(x_{i,j},y_i)}\right)\)。
  3. 在凸函数类 \(F\) 上最小化 \(\Phi_N(f)\)（例如用神经网络参数化 \(f\)，但凸性仅对函数空间成立，对参数空间非凸）。
  4. 得到近似势函数 \(f^*_N\)，用于后验采样（如pCN MCMC）。

## 3. 实验设计

- **场景与数据集**：
  - **去模糊问题**（1D）：
    - 未知量 \(x\in L^2([0,1])\)，先验为高斯过程（平方指数核，\(\sigma=0.1, l=0.1\)）。
    - 前向映射：与高斯核卷积，在50个点采样。观测噪声 \(\epsilon\sim N(0,0.1^2 I)\)。
    - 训练数据：1000个联合样本 \((x,y)\)。
  - **半导体器件掺杂反演**（2D非线性PDE）：
    - 未知量为pn结掺杂浓度 \(C\)，由两个独立Matern高斯场组成（\(\sigma=2, l=20, \nu=0.9\)）。
    - 前向模型：求解静电势方程和连续性方程（有限元，网格尺寸0.1），输出100个边界电流密度测量值。噪声 \(\sigma=0.01\)。
    - 训练数据：10000个样本，PDE求解耗时约5小时。
- **对比方法**：
  - **自由形式近似**：神经网络直接输出 \(f_\theta(x,y)\)（4-5层全连接或卷积+全连接）。
  - **残差近似**：近似前向映射 \(g_\theta(x)\)，并假设已知噪声水平 \(\delta^2\)，似然为 \(\frac{1}{2\delta^2}\|g_\theta(x)-y\|^2\)。
  - **校准残差近似**：同时学习前向映射 \(g_\theta(x)\) 和协方差矩阵 \(\Sigma_\theta\)。
  - **GP回归**（仅去模糊问题）：以真实似然值为目标，学习高斯过程（平方指数核）作为替代。
  - **基准**：真实后验（使用真实NLL）。
- **评价指标**：后验均值、偏差、方差的可视化对比，以及计算时间。

## 4. 资源与算力

- 文中未明确说明GPU型号和数量，但提到了GPU集群用于NLL近似的并行加速。
- **训练数据生成**：半导体问题用MATLAB PDE求解器，10000个样本耗时≈5小时（CPU）。
- **神经网络训练**：30个epoch，每epoch约120秒（GPU）。
- **MCMC采样对比**：
  - 单次PDE求解0.7008秒（CPU）；NLL近似单次0.0042秒（CPU）、0.0016秒（GPU）。
  - 1000次并行评估：PDE 700秒 vs NLL近似 0.0331秒（GPU），加速约20000倍。
- **后验采样总耗时**：NLL近似约<50分钟（CPU）或<10分钟（GPU）；PDE模型约265小时。

## 5. 实验数量与充分性

- **实验数量**：两个问题（一维去模糊、二维半导体反演），每个问题测试了多种近似方法（自由形式、残差、校准残差，去模糊还包含GP回归）。均进行了MCMC后验采样和可视化对比。
- **充分性分析**：
  - **优点**：覆盖了不同复杂度的问题（线性卷积 vs 非线性PDE），对比了多种似然近似策略，提供了计算时间定量比较，验证了理论结果（凸性和加速）。
  - **不足**：
    - 缺乏消融实验：未系统研究训练数据量对精度的影响，未对比不同网络架构或超参数。
    - 缺乏统计量化：后验结果仅通过可视化展示，未给出如可信区间覆盖率、均方误差等定量指标。
    - 仅一个噪声水平：未测试不同噪声强度对校准残差方法的影响。
    - 未对比其他神经似然方法（如归一化流、SNL等），仅对比了GP回归（且GP需要真实似然值，不公平）。
    - 半导体问题中校准残差表现很差，但未深入分析原因（如噪声协方差估计困难或网络容量不足）。

## 6. 论文的主要结论与发现

- **理论贡献**：
  - 证明了对未归一化势函数的最小化KL散度目标是严格凸的（Theorem 2.8）。
  - 建立了经验估计的一致性：在合理假设下，数据驱动的最小化器随样本量收敛到真实势函数（Theorem 3.7）。
- **实验结论**：
  - 神经似然近似可大幅加速贝叶斯逆问题的计算，比PDE模型快200-20000倍，同时保持后验精度。
  - 自由形式和残差近似在已知/未知噪声下均能逼近真实后验；校准残差近似在未知噪声且非线性强时性能下降。
  - 即使仅有1000个训练样本，神经似然近似也比使用10000个点的GP回归更准确。

## 7. 优点

- **理论严谨**：首次将神经似然近似置于凸优化框架下，并证明了统计一致性，为方法提供了严格的数学基础。
- **灵活高效**：无需强制归一化的网络架构（如归一化流），允许使用任意函数类（如标准神经网络），简化了模型设计。
- **实用加速**：在非线性PDE逆问题上实现2-4个数量级的加速，离线训练后可实时后验采样。
- **广泛适用**：仅需要联合数据 \((x,y)\)，无需前向模型封闭表达式或似然评估，适合黑箱仿真场景。

## 8. 不足与局限

- **理论局限**：
  - 一致性证明局限于有限维空间（\(H=\mathbb{R}^n\)），未推广到无穷维函数空间，尽管方法中用到无穷维表述。
  - 嵌套蒙特卡洛估计的收敛率未定量给出，最优内循环样本量 \(M\) 的选择未讨论。
  - 凸性对函数空间成立，但神经网络参数化后优化仍非凸，理论无法保证找到全局最优。
- **实验局限**：
  - 仅测试了两个问题，缺乏高维（如图像级）或更复杂前向模型的验证。
  - 未与当前主流方法（如SNL、归一化流、BayesFlow）进行公平比较。
  - 半导体问题中校准残差表现差，缺乏系统诊断。
  - 未提供不确定性量化指标（如区间覆盖率、校准曲线）来评估后验质量。
- **偏差风险**：假设函数类包含真实似然（Assumption 2），实际可能不满足；先验指数矩条件可能过于严格。
- **应用限制**：需要从联合分布采样的能力，对于无仿真器或只能获取有限真实实验数据的问题可能不适用。

（完）
