---
title: Width-Robust Learnability in Mean-Field Bayesian Neural Networks
title_zh: 平均场贝叶斯神经网络中的宽度鲁棒可学习性
authors: "Dmitry Vaintrob, Kaarel Hänni"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05735v1"
tags: ["query:bayes-dl"]
score: 9.0
evidence: 研究平均场尺度下的贝叶斯神经网络，证明了宽度鲁棒可学习性，涉及BNN训练和推断
tldr: 平均场标度的贝叶斯神经网络中，无限宽度极限被用于分析学习能力，但其归纳偏置是否与有限宽度网络一致尚不明确。本文通过定义缩减熵作为目标函数的先验代价，证明了宽度鲁棒可学习性定理：在固定深度下，布尔立方体目标函数族在无限宽度下多项式样本可学习，当且仅当在多项式宽度下可学习，当且仅当其缩减熵是多项式有界的。该定理表明，平均场贝叶斯学习器只能泛化那些能被多项式大小网络表示的目标，通过子采样方法从无限宽度解中保留数据相关统计量并重采样熵主导方向，实现了宽度鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05735v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1281, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05735v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1162, \"height\": 687, \"label\": \"Figure\"}]"
motivation: 探究无限宽度极限是否引入虚假的宽度依赖泛化能力，以及有限宽度网络与无限宽度极限的学习能力是否等价。
method: 定义缩减熵衡量目标函数的先验代价，通过子采样从无限宽度解中选取多项式个代表神经元，同时保留数据相关统计量和重采样熵主导方向。
result: 证明了宽度鲁棒可学习性定理：固定深度下，布尔立方体目标函数族在无限宽度与多项式宽度下的多项式样本可学习性等价，且等价于缩减熵多项式有界。
conclusion: 平均场贝叶斯学习器的泛化能力完全由多项式大小网络的表示能力决定，无限宽度极限不会引入额外的学习能力。
---

## 摘要
无限宽度极限是推理神经网络的标准方式，但极限学习器是否具有与大型有限网络相同的复杂性理论归纳偏差并非自动成立。我们针对平均场或临界特征学习缩放下的贝叶斯神经网络研究了这一问题。核心量是"约化熵" \[ s_\infty(y,\varepsilon)=\limsup_N -\frac{1}{N}\log π_N^0(L\le \varepsilon) \]，即表示目标函数 $y$ 至总体均方误差 $\varepsilon$ 的集约先验代价。我们的主要结果是宽度鲁棒可学习性定理。在固定深度下，一族布尔立方体目标在无限宽度下可被多项式个样本学习当且仅当它在多项式宽度下可学习，当且仅当它的约化熵是多项式有界的。等价地，在精度的多项式松弛下，贝叶斯平均场学习器恰好泛化到那些可由多项式大小网络表示的目标。正向方向通过一种子采样形式证明：从平均场解中无限多的隐藏神经元中，可以选择多项式个代表，并同时在每个输入上保持学习到的函数。在临界缩放下，这种子采样既有"主动"成分（保留数据相关的低维统计量），又有"惰性"成分（从先验中重新采样熵主导的方向）。因此，无限宽度平均场极限在不引入虚假的宽度相关泛化能力的情况下，提供了学习的清晰解析描述。

## Abstract
Infinite-width limits are a standard way to reason about neural networks, but it is not automatic that the limiting learner has the same complexity-theoretic inductive bias as large finite networks. We study this question for Bayesian neural networks at the mean-field, or critical feature-learning, scaling. The central quantity is the \emph{reduced entropy} \[   s_\infty(y,\varepsilon)=\limsup_N -\frac{1}{N}\log π_N^0(L\le \varepsilon), \] the intensive prior cost of representing a target function $y$ to population mean-squared error $\varepsilon$.   Our main result is a width-robust learnability theorem. At fixed depth, a family of Boolean-cube targets is learnable from polynomially many samples at infinite width if and only if it is learnable at polynomial width, if and only if its reduced entropy is polynomially bounded. Equivalently, up to polynomial slack in accuracy, the Bayesian mean-field learner generalizes exactly on the targets that can be represented by polynomial-size networks.   The forward direction is proved by a form of subsampling: from the infinitely many hidden neurons in the mean-field solution, one can select polynomially many representatives and still preserve the learned function on every input simultaneously. At the critical scaling this subsampling has both an ``active'' component, which keeps the data-dependent low-dimensional statistics, and a ``lazy'' component, which resamples the entropy-dominated directions from the prior. Thus the infinite-width mean-field limit gives a clean analytic description of learning without introducing spurious width-dependent generalization power.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

论文研究的是**平均场（Mean-Field）缩放下贝叶斯神经网络的宽度鲁棒可学习性**。在深度学习中，无限宽度极限（如NTK/NNGP）被广泛用于分析网络行为，但不同缩放机制（惰性γ<1、临界γ=1、过富γ>1）会导致截然不同的归纳偏置。一个关键问题是：**无限宽度极限下的学习者是否具有与有限宽度网络相同的复杂度理论归纳偏差？** 特别是，平均场临界缩放（γ=1）被认为能保持特征学习，但其泛化能力是否依赖于无穷多的神经元？本文通过定义**约化熵**（reduced entropy）作为目标函数的集约先验代价，证明了**宽度鲁棒可学习性定理**，表明在固定深度下，布尔立方体目标族在无限宽度下可被多项式样本学习当且仅当它在多项式宽度下可学习，且等价于约化熵多项式有界。这一结果验证了平均场极限不会引入虚假的宽度依赖泛化能力，提供了对贝叶斯神经网络归纳偏置的清晰解析描述。

### 2. 方法论：核心思想、关键技术细节

**核心思想**：以约化熵作为衡量目标函数复杂度的关键量，通过**子采样**（subsampling）建立无限宽度解与多项式宽度网络之间的桥梁。

**关键技术细节**：
- **约化熵的定义**：  
  \[
  s_\infty(y,\varepsilon)=\limsup_{N\to\infty} -\frac{1}{N}\log\pi_N^0(L\le \varepsilon)
  \]
  表示将目标函数拟合到均方误差ε所需的集约先验代价（每神经元的自然对数概率）。
- **主动/惰性分裂**（Active/Lazy Split）：在临界缩放γ=1下，特征内核谱分为低维主动空间（数据相关统计量）和高维惰性块（先验噪声主导）。压缩过程包括：
  1. **主动子采样**：从无限宽度解中选择多项式个神经元，投影到主动方向上，保留数据依赖的低维统计量。
  2. **惰性交换**：对惰性块，用来自先验的独立高斯噪声替换，并通过**懒惰交换引理**（Lazy-Swap Lemma）证明这种替换对函数输出影响很小（误差界为 \(p\sqrt{\lambda_0}c_\star\)，其中\(\lambda_0\)是惰性块算子范数，\(c_\star\)是敏感性）。
- **压缩定理**：任何约化熵有限的无限宽度解，都能被一个宽度和权重均为多项式大小的网络在逐点意义下近似（sup norm误差可控），从而保持均方误差精度。
- **反向方向**（守恒定理）：若存在一个多项式宽度且权重多项式大小的网络以高精度实现目标，则其约化熵也是多项式有界的（通过克隆构造：将教师网络的每个神经元复制成群，并证明该分布偏离先验的KL散度是多项式的）。

**理论框架**：使用贝叶斯均场极限（传播混沌、吉布斯后验、变分自由能），结合斯坦因方法、Ornstein-Uhlenbeck过程、Mehler公式等分析工具。

### 3. 实验设计

**该论文为纯理论工作，没有设计任何数值实验或数据集实验**。因此：
- **使用的数据集/场景**：无。论文理论对象是定义在布尔立方体 \(\{±1\}^d\) 上的有界函数。
- **基准（Benchmark）**：无对比方法，论文主要提出理论等价性定理。
- **对比的方法**：无。

### 4. 资源与算力

论文**未提及任何算力消耗**（如GPU型号、数量、训练时长）。作为纯理论论文，不涉及大规模数值模拟。

### 5. 实验数量与充分性

论文**没有进行实验**。其充分性体现于严格的数学证明，包括：
- 压缩定理（Theorem 5.4）和克隆定理（Theorem 6.1）的完整推导。
- 从约化熵到吉布斯可学习性的正向方向（Theorem 7.2）和反向方向（Theorem 7.3）。
- 最终组装为宽度鲁棒可学习性定理（Theorem 3.1）。

理论推导覆盖了所有关键步骤，但**未包括任何数值验证**，因此对于实际应用的可信度需要通过后续实验支持。

### 6. 主要结论与发现

1. **宽度鲁棒可学习性**：在固定深度、平均场缩放下，以下条件等价：
   - （i）无限宽度下从多项式样本中学习到目标；
   - （ii）多项式宽度下从多项式样本中学习到目标；
   - （iii）目标可由一个多项式宽度且权重多项式大小的网络实现；
   - （iv）目标的约化熵 \(s_\infty(y,\varepsilon)\) 是多项式有界的。
2. **压缩与守恒互逆**：低约化熵的无限宽度解可压缩为多项式宽度网络（逐点一致逼近），反之，存在多项式宽度教师可推出低约化熵。
3. **临界缩放的本质**：γ=1时惰性块与主动块同阶，需要惰性交换引理精确处理；γ>1时惰性块可忽略，γ<1时退化为核方法。
4. **归纳偏差的宽度不变性**：平均场贝叶斯学习器的泛化能力完全由多项式大小网络的表示能力决定，无限宽度不增加额外可学习目标。

### 7. 优点

- **理论深度与清晰性**：严格定义了约化熵，并证明了其作为复杂度度量的完备性。
- **创新性方法**：主动/惰性分裂及惰性交换引理是新颖的技术贡献，尤其适用于临界缩放。
- **宽度鲁棒性**：证明了无限宽度与多项式宽度在样本复杂度上的等价性，消除了对无限宽度极限可能引入虚假好处的担忧。
- **点态逼近结果**：压缩定理给出逐点一致误差界，比传统均方误差结果更强，使得无限宽度与有限宽度函数在逐点意义上一致。
- **定理闭合性**：正向与反向方向均完整证明，且与理论复杂度（P/poly风格）相联系，提供深度学习的计算复杂性视角。

### 8. 不足与局限

- **无实验验证**：是完全理论论文，缺少数值实验验证定理在实际神经网络训练中的表现，例如实际SGD找到的后验是否接近理论构造。
- **假设较强**：需要激活函数有界、Lipschitz、非多项式；假定布尔立方体均匀分布；固定深度且多项式指数依赖于深度L；先验为高斯独立权重。这些假设在真实场景不一定满足（如ReLU无界，但可通过截断近似；数据分布可能非均匀）。
- **有限宽度推广未完成**：论文只处理了无限宽度和多项式宽度，一般有限宽度（如超多项式）的情况留给了后续工作（companion paper）。
- **复杂度实际常数巨大**：多项式指数虽固定但可能很大（如\(O(L)\)次方），实际多项式阈值可能不切实际。
- **贝叶斯设定限制**：结果针对贝叶斯后验（吉布斯采样），而实际训练常用SGD等优化算法，不一定收敛到吉布斯后验，因此宽度鲁棒性可能不直接适用于常规训练方法。
- **未讨论错误累积**：压缩过程中使用了多次概率界限，虽然概率高，但未给出置信度与样本量之间的显式可靠性权衡。

（完）
