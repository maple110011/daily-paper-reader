Title: Wahkon: A Statistically Principled Deep RKHS Superposition Network

URL Source: https://arxiv.org/pdf/2605.14041v1

Published Time: Fri, 15 May 2026 00:14:59 GMT

Number of Pages: 17

Markdown Content:
# Wahkon: A Statistically Principled Deep RKHS Superposition Network 

Yongkai Chen 

Department of Statistics, Harvard University 

yongkaichen@fas.harvard.edu 

Wenxuan Zhong 

Department of Statistics, University of Georgia 

wenxuan@uga.edu 

Ping Ma 

Department of Statistics, University of Georgia 

pingma@uga.edu 

## Abstract 

Deep learning excels at prediction but often lacks finite-sample guarantees and calibrated uncertainty; RKHS (Reproducing Kernel Hilbert Space)-based methods provide those guarantees but struggle to adapt in high dimensions. We propose Wahkon, a deep RKHS superposition network that unifies Kolmogorov’s super-position principle with RKHS regularization in the smoothing-spline tradition of Wahba. This yields a finite-dimensional deep representer theorem that makes training tractable and provides explicit layerwise complexity control. We show the penalized estimator is exactly the MAP (maximum a posteriori) estimate under a hierarchical Gaussian-process prior, extending the spline/GP duality to deep compositions. Using metric-entropy arguments, we establish minimax-optimal convergence rates under mild smoothness and clarify how depth and width trade off with regularity. Empirically, Wahkon outperforms multilayer perceptrons, Neural Tangent Kernels, and Kolmogorov–Arnold Networks across simulation bench-marks and a single-cell CITE-seq study. By unifying Kolmogorov’s superposition principle with RKHS regularization, Wahkon delivers accuracy, interpretability, and statistical rigor in a single framework. 

## 1 Introduction 

Modern neural networks have transformed prediction across science and engineering (Goodfellow et al., 2016), but their use for formal statistical inference remains challenging. Standard architec-tures typically rely on predetermined activation functions, such as ReLU or sigmoid, together with regularization and stabilization devices such as weight decay, dropout, normalization, and early stopping. While these tools are highly effective for prediction, they do not by themselves provide the function-space complexity control, uncertainty quantification, or interpretability that are central to many statistical applications. In contrast, classical nonparametric methods grounded in reproducing kernel Hilbert spaces (RKHS) offer explicit smoothness control, optimal convergence rates, and a close Bayesian interpretation through Gaussian processes (GPs) (Wahba, 1990; Gu, 2013). Neural tangent kernel (NTK) theory extends this kernel perspective to overparameterized neural networks by showing that, in suitable infinite-width and lazy-training regimes, gradient descent behaves like kernel regression with a deterministic kernel determined by the architecture and initialization (Jacot et al., 2018; Lee et al., 2019). However, classical kernel methods and NTK-based methods typically rely on kernels that are fixed before training or only weakly adaptive. Such kernels can impose a geometry and smoothness structure that may be poorly aligned with high-dimensional, heterogeneous data. Bridging these paradigms calls for an approach that preserves deep learning’s adaptive representation power while restoring RKHS-style complexity control and principled statistical inference. 

> Submitted to STAI-X 2026 (Full Paper Track). Do not distribute.
> arXiv:2605.14041v1 [stat.ME] 13 May 2026

We introduce Wahkon, a method inspired by Kolmogorov’s superposition theorem (Kolmogorov, 1957; Arnold, 1963) and the recent Kolmogorov–Arnold network (KAN) paradigm, which combines deep neural architectures with learnable activation functions. Wahkon adopts a standard deep compositional structure but replaces fixed activations with learned univariate link functions along every edge of the network, each constrained to lie in an RKHS in the sense of Wahba’s smoothing-spline framework. The deep compositional structure enables Wahkon with high representational power and the RKHS-based regularization at the level of individual links provides a hierarchical smoothness control while remaining interpretable. The article makes three main contributions. First, we establish a deep representer theorem for Wahkon, despite the infinite-dimensional function space for each link function and the deep compositional architecture, the estimator admits a finite kernel expansion at every layer. This extends KAN-style architectures to an RKHS setting, replacing fixed B-spline bases and heuristic penalties with principled RKHS regularization, which in turn yields improved statistical convergence guarantees. Second, we show that the penalized Wahkon estimator coincides with the Maximum a posteriori 

with a deep hierarchical Gaussian-process prior on the link functions. This extends the classical spline/GP duality (Wahba, 1978; Rasmussen and Williams, 2006) to the deep network structure, which provides insights into the success of Wahkon. Third, we derive minimax-optimal convergence rates under mild smoothness assumptions and make explicit how depth, width, and smoothness of link functions interact, providing principled guidance for hyperparameter selection. Empirically, Wahkon outperforms multilayer perceptrons (MLPs), NTK, and KANs in synthetic benchmarks. In a CITE-seq application, Wahkon accurately predicts surface protein abundance from RNA (Stoeckius et al., 2017; Stuart et al., 2019). Collectively, these results demonstrate that Wahkon combines the representational power of deep architectures with the rigor and stability of RKHS-based regularization, yielding a flexible yet statistically principled framework for high-dimensional prediction and inference. 

## 2 Background and Motivation 

Kolmogorov’s Superposition Principle Theorem 2.1 (Kolmogorov (1957)) . For any continuous f : [0 , 1] D → R, there exist continuous univariate functions {ϕqk } and {ψq } such that f (x) = P2D+1  

> q=1

ψq

PDk=1 ϕqk (xk)



.

While nonconstructive, this theorem implies that any continuous multivariate function can be ap-proximated by compositions of univariate functions. However, practical implementations prioritize computational efficiency over exact representation, diverging from the theorem’s idealized structure. 

Multilayer Perceptron Networks Multilayer perceptrons (MLPs) approximate a target by com-posing affine maps with a fixed nonlinearity, typically ReLU (Goodfellow et al., 2016). They are universal approximators (e.g., Barron, 1993) and highly effective in practice, but fixed activations limit adaptivity and formal uncertainty. 

Kolmogorov–Arnold Networks Kolmogorov–Arnold Networks (KANs) (Liu et al., 2024) replace fixed activations with learned univariate functions and summation across inputs. This brings depth efficiency and interpretability but lacks explicit statistical regularization and finite-sample guarantees. 

Other related works Spline ANOVA and RKHS methods provide smoothness control, representer structure, and Bayesian interpretation (Wahba, 1990; Gu, 2013; Wahba, 1978). Deep kernel learning (Wilson et al., 2016) bring kernels to multilayer neural network settings. Neural Tangent Kernel (Jacot et al., 2018) and its infinite-width GP correspondence (Lee et al., 2018, 2019) connect deep networks to GPs but use a lazy-training regime without feature learning. A growing body of theoretical work studies unfixed link functions. Horowitz and Mammen (2007) establishes rate-optimal estimation for generalized additive models with unknown link functions. For fully connected deep networks, Schmidt-Hieber (2020) derive minimax-optimal rates over Hölder-smooth link functions and show that depth enables adaptation to compositional structure. Bauer and Kohler (2019) proves that deep networks overcome the curse of dimensionality when the it admits a compositional representation. 23 The Wahkon Network 

3.1 Network Architecture for Multivariate Regression 

Consider the nonparametric regression model Y = η(X) + ϵ, X ∈ RD , E(ϵ | X) = 0 , so that η(x) = E(Y | X = x) is the regression function of interest. A Wahkon network approx-imates η by a composition of L layers. Its architecture is specified by the sequence of layer widths (D0, D 1, . . . , D L), where D0 = D is the input dimension and DL = 1 for scalar-response regression. For a given input x ∈ RD , define the layer outputs recursively. Set x(0) = x,

x(l) = ( x(l)1 , . . . , x (l) 

> Dl

)⊤ ∈ RDl , l = 1 , . . . , L . The lth layer is a map Φ(l) : RDl−1 → RDl ,

x(l) = Φ (l)(x(l−1) ). Each coordinate of Φ(l) is formed by summing univariate transformations of the coordinates from the previous layer: 

x(l) 

> j

=

> Dl−1

X

> k=1

ϕ(l)

> jk



x(l−1) 

> k



, j = 1 , . . . , D l, l = 1 , . . . , L. (1) Here ϕ(l) 

> jk

: R → R is the univariate link function from the kth unit in layer l − 1 to the jth unit in layer l. Unlike conventional neural networks, where nonlinearities are usually fixed in advance, Wahkon learns these link functions from data. We assume each link belongs to a common reproducing kernel Hilbert space HK with reproducing kernel K. The resulting network predictor is the scalar output of the final layer: 

ηWahkon (x) = x(L)1 = Φ (L) ◦ Φ(L−1) ◦ · · · ◦ Φ(1) (x). (2) 

3.2 Penalized Estimation 

Let {(xi, y i)}ni=1 be independent observations with xi ∈ RD and yi ∈ R. For a given collection of link functions, let x(l) 

> i

= ( x(l) 

> i1

, . . . , x (l) 

> iD l

)⊤ ∈ RDl , l = 0 , . . . , L , denote the layer-l output for the 

ith observation with x(0)  

> i

= xi. The fitted value for observation i is x(L) 

> i1

= ηWahkon (xi). We estimate the link functions by minimizing a penalized least-squares, 

L(ηWahkon ) = 

> n

X

> i=1

yi − ηWahkon (xi)2 + n J (ηWahkon ), (3) where the penalty is 

J(ηWahkon ) = 

> L

X

> l=1

λlDlX

> j=1
> Dl−1

X

> k=1

ϕ(l)

> jk
> 2
> HK

, λl > 0. (4) Here λl is the regularization parameter for layer l. Larger λl enforces smoother and smaller link functions, while a smaller λl allows more flexible nonlinear transformations. Unlike weight decay in a standard MLP, this penalty is imposed on the functions ϕ(l) 

> jk

through their RKHS norms. 

3.3 Deep Representer Theorem 

The optimization problem in (3) is infinite-dimensional because each link function ϕ(l) 

> jk

ranges over an RKHS. The following result shows that the minimizer nevertheless has a finite representation. The kernel centers at layer l are the fitted inputs entering that layer, namely {x(l−1)  

> ik

: i = 1 , . . . , n }.

Theorem 3.1 (Deep representer theorem) . Suppose each link function ϕ(l) 

> jk

belongs to HK and that a minimizer of (3) exists. Then, at any minimizer, 

ϕ(l) 

> jk

(t) = 

> n

X

> i=1

c(l) 

> ijk

K



x(l−1)  

> ik

, t 



, j = 1 , . . . , D l, k = 1 , . . . , D l−1, l = 1 , . . . , L, 

for some coefficients c(l) 

> ijk

∈ R. Therefore, the fitted Wahkon network is fully determined by the finite collection of coefficient tensors C = {C(l) : l = 1 , . . . , L }, C(l) ∈ Rn×Dl×Dl−1 , whose (i, j, k )th entry is c(l)

> ijk

.

3For each layer l and input coordinate k of that layer, define the kernel matrix Q(l−1)  

> k

=h

K



x(l−1)  

> ik

, x (l−1) 

> i′k

i ni,i ′=1 

. For l = 1 , this matrix is computed from the observed predictors. For deeper layers, it is computed from the fitted outputs of the previous layer. Let c(l) 

> ·jk

=(c(l)1jk , . . . , c (l)

> njk

)⊤ be the coefficient vector for the link from coordinate k in layer l − 1 to coor-dinate j in layer l. By the reproducing property, ϕ(l)

> jk
> 2
> HK

= c(l)⊤·jk Q(l−1)  

> k

c(l) 

> ·jk

≡ c(l)

> ·jk
> 2
> Q(l−1)
> k

.Consequently, the infinite-dimensional problem in (3) reduces to an optimization over the finite coefficient collection C:

L(C) = y − X(L)(C) 22 + n

> L

X

> l=1

λlDlX

> j=1
> Dl−1

X

> k=1

c(l)

> ·jk
> 2
> Q(l−1)
> k

, (5) where y = ( y1, . . . , y n)⊤ and X(L)(C) =  x(L)11 (C), . . . , x (L) 

> n1

(C)⊤ is the vector of final-layer fitted values. The penalty in (5) is quadratic in the coefficients for fixed layer inputs, while the residual is generally nonlinear because the layer outputs are obtained recursively through the forward map. 

3.4 Profile Objective 

The finite-dimensional objective in (5) can be simplified further by profiling out the final layer. This is useful because, conditional on the outputs of the first L − 1 layers, the last layer is an ordinary kernel ridge regression problem. Let C<L = {C(1) , . . . , C(L−1) } denote the lower-layer coefficients. For fixed C<L , the layer-(L − 1) outputs x(L−1)  

> ik

, i = 1 , . . . , n , k = 1 , . . . , D L−1, are fixed. Define the aggregate final-layer kernel matrix K(L−1) = PDL−1 

> k=1

Q(L−1)  

> k

. For fixed C<L , minimizing (5) over the final-layer coefficients lead to the profile objective 

P(C<L ) = min  

> {c(L)
> ·1k}DL−1
> k=1

L(C) = nλ L y⊤ K(L−1) + nλ LIn

−1y + n

> L−1

X

> l=1

λlDlX

> j=1
> Dl−1

X

> k=1

c(l)

> ·jk
> 2
> Q(l−1)
> k

.

(6) The derivation is provided in the Appendix, Section A. The first term is the residual-plus-penalty value after optimally fitting the last layer; the second term retains the RKHS penalties for the lower-layer link functions. Minimizing P(C<L ) over the lower-layer coefficients is equivalent to minimizing the original objective (5) over all coefficients. When the kernel is differentiable and 

K(L−1) + nλ LIn is nonsingular, the envelope theorem justifies differentiating P with respect to C<L 

without differentiating through the profiled final-layer coefficients. This profiling step improves optimization in practice (Section D, Appendix) because gradient-based updates focus on learning the lower-layer representation, while the final layer is solved exactly at each iteration. The idea is related to deep kernel learning (Wilson et al., 2016), which also learns a representation while solving a kernel model at the output layer. The objectives differ, however: deep kernel learning typically maximizes a Gaussian-process marginal likelihood, whereas Wahkon directly minimizes a penalized least-squares criterion with explicit RKHS penalties on the learned link functions for all layers. 

## 4 Wahkon as a Hierarchical Bayesian Model 

The RKHS penalty in (4) has a natural Bayesian interpretation. In classical smoothing-spline theory, penalized least squares can be viewed as a maximum a posteriori (MAP) estimator under a Gaussian-process prior (Wahba, 1978, 1990). Wahkon extends this correspondence to a deep architecture by assigning Gaussian-process priors to the univariate link functions at each layer. 

4.1 Prior specification 

Recall that the link function from coordinate k in layer l − 1 to coordinate j in layer l is denoted by 

ϕ(l) 

> jk

: R → R, j = 1 , . . . , D l, k = 1 , . . . , D l−1. We assign independent zero-mean Gaussian-process priors 

ϕ(l) 

> jk

∼ GP (0 , τ l K(·, ·)) , l = 1 , . . . , L, (7) 4where τl > 0 is a layer-specific prior variance scale. Larger τl allows more variable link functions in layer l, while smaller τl imposes stronger shrinkage toward zero. For the observed training inputs, define the vector of link evaluations z(l) 

> jk

=



ϕ(l) 

> jk

(x(l−1) 1k ), . . . , ϕ (l) 

> jk

(x(l−1)  

> nk

)

⊤

∈ Rn. Conditional on the layer-(l − 1) outputs, the GP prior implies 

z(l) 

> jk

| X(l−1) ∼ N 



0, τ lQ(l−1) 

> k



, (8) where Q(l−1)  

> k

=

h

K



x(l−1)  

> ik

, x (l−1) 

> i′k

i ni,i ′=1 

. The vectors z(l) 

> jk

are conditionally independent across 

(j, k ), given X(l−1) . The output of the jth unit in layer l is the sum of incoming link evaluations: 

X(l) 

> ·j

= PDl−1 

> k=1

z(l) 

> jk

, j = 1 , . . . , D l. Therefore, conditional on X(l−1) ,

X(l) 

> ·j

| X(l−1) ∼ N 

0, τ lDl−1

X

> k=1

Q(l−1) 

> k

 . (9) This conditional Gaussian representation is the key Bayesian analogue of the forward pass. Marginally across layers, the distribution of X(l) is generally non-Gaussian for l ≥ 2, because the covariance matrices in (9) depend on the random outputs from the previous layer. 

Proposition 4.1 (Marginal moments of hidden neurons) . If the kernel is normalized so that K(t, t ) = 1, then 

E

h

x(l)

> ij

i

= 0 , Var 

h

x(l)

> ij

i

= τlDl−1. (10) 

Remark 1 (Variance scaling) . Equation (10) shows that the conditional prior variance grows linearly with the number of incoming links Dl−1. A variance-preserving choice is therefore τl = τ /D l−1,which gives Var 

h

x(l)

> ij

i

= τ . This scaling is analogous in spirit to variance-preserving initialization rules for neural networks (LeCun et al., 2002), but here it is expressed at the function-prior level. 

4.2 Posterior Distribution and MAP Equivalence 

We now connect the hierarchical GP prior to the penalized estimator in (3) – (4) . Assume the Gaussian regression model yi = ηWahkon (xi) + ϵi, ϵi

> iid

∼ N (0 , σ 2). Under the GP priors in (7) , the negative log-posterior for the collection of link functions is, up to constants, 

12σ2

> n

X

> i=1

yi − ηWAHKON (xi)2 + 12

> L

X

> l=1

1

τlDlX

> j=1
> Dl−1

X

> k=1

ϕ(l)

> jk
> 2
> HK

. (11) Thus the posterior mode is obtained by balancing data fidelity against the RKHS norm of each learned link function. Comparing (11) with the penalized objective (3) –(4) shows that the two criteria agree after the variance–penalty calibration τl = σ2 

> nλ l

, l = 1 , . . . , L. This yields the following equivalence. 

Theorem 4.2 (MAP–penalized equivalence) . Suppose the Gaussian-noise model holds and each link function has the independent GP prior in (7) . If τl = σ2 

> nλ l

, l = 1 , . . . , L, then the maximum a posteriori estimator of the link functions coincides with the minimizer of the penalized least-squares problem (3) –(4) . Equivalently, under the representer parameterization of Theorem 3.1, if 

C∗ = {C(l)∗ : l = 1 , . . . , L }

minimizes the finite-dimensional objective (5) , then the fitted link-evaluation vectors 

z(l)∗ 

> jk

= Q(l−1)  

> k

c(l)∗·jk , j = 1 , . . . , D l, k = 1 , . . . , D l−1, l = 1 , . . . , L, 

give the posterior mode under the hierarchical GP model. 

The theorem extends the classical equivalence between smoothing splines and Gaussian-process MAP estimation (Wahba, 1978, 1990) to the deep compositional setting of Wahkon. The regularization parameters λl can therefore be interpreted as ratios of observation noise variance to layer-specific prior variance. 55 Asymptotic Analysis 

Let bηWahkon be a minimizer of the penalized objective (3) –(4) . For simplicity of exposition, assume that the layer-specific penalties have a common order, 

λl ≍ λn, l = 1 , . . . , L. 

Let FWahkon denote the class of functions represented by the architecture (D0, . . . , D L) under the RKHS-norm constraints stated below. We define the oracle approximation 

η∗ ∈ arg min 

> ˜η∈F Wahkon

Z ˜η(x) − η(x)2 dP X,

and the associated approximation error 

∆2approx =

Z η∗(x) − η(x)2 dP X.

When the target lies in the Wahkon class, this term is zero. More generally, it quantifies the bias induced by using a finite architecture and a chosen RKHS. 

Theorem 5.1 (Convergence rate) . Under the regularity Assumptions E.1–E.3 provided in the Ap-pendix, Section E.5, we have 

Z bηWahkon (x) − η(x)2 dP X = Op



∆2approx + λn + AD n−1λ−1/(2 α)

> n



,

where AD =

QLl=0 Dl

 α+1 −βα

. The three terms in Theorem 5.1 have distinct interpretations. The first is the approximation error of the chosen architecture. The second is the bias introduced by RKHS regularization. The third is the stochastic error, controlled by the entropy of the Wahkon class. Balancing the second and third terms gives λn ≍   AD

> n

 2α 

> 2α+1

. The rate displays the role of the architecture. Increasing width or depth may reduce ∆2approx by enlarging the approximation class, but it can also increase the error through AD . The norm decay parameter β governs this tradeoff. When link norms decrease rapidly with width, the entropy contribution is moderated; when the decay is weak, additional units can increase complexity and slow convergence. Thus the theorem does not simply advocate larger networks; it specifies how architectural growth must be balanced with RKHS regularization to maintain favorable finite-sample behavior. 

## 6 Simulation Studies 

We evaluate Wahkon to (i) verify prior behavior across depth and (ii) assess predictive accuracy on four benchmark functions spanning compositional and nested structures. Full data-generating mechanisms, architectures, and baseline configurations are provided in Appendix C. 

6.1 Prior behavior across layers 

To examine the hierarchical GP prior, we simulated 1,000 draws of hidden-layer outputs in a depth-5

Wahkon with moderate width, using the Gaussian kernel K(x, y ) = exp {− (x − y)2}. As shown in Figure 1, the first layer is approximately Gaussian, as expected from the prior. Deeper layers exhibit significant non-Gaussianity. The prior distribution exhibits two notable characteristics (Figure 1b): 1. Concentration around zero : Approximately 30% of Monte Carlo samples from deeper layers ( l ≥ 3) show squared Mahalanobis distances less than 50, suggesting high probability mass around the prior mean E[X(l) 

> ·,d l

] = 0.2. Heavy-tailed behavior : Approximately 20% of samples demonstrate extreme distances (> 150 ), exceeding the 99.91% quantile of the χ2100 reference distribution. This “spike-and-slab–like” shape indicates automatic shrinkage of many links while preserving the capacity for a subset to remain active. This dual behavior may explain Wahkon’s empirical success in balancing model complexity with generalization performance. 6Figure 1: Prior distribution analysis for hidden layer outputs in a [4 , 4, 4, 4, 4, 4] -Wahkon model. (a) Q-Q plot comparing empirical quantiles of squared Mahalanobis distances (x-axis) against theoret-ical χ2100 quantiles (y-axis). The black line indicates expected behavior under a 100-dimensional multivariate normal distribution. (b) Empirical cumulative distribution function (CDF) of squared Mahalanobis distances. 

Figure 2: Test RMSE (log scale) versus training sample size for four benchmark functions. Curves show means over 100 replications; error bars indicate ± one standard deviation. 

6.2 Function-approximation benchmarks 

We compare Wahkon against three competing methods: KAN (Liu et al., 2024), MLP, and NTK (Jacot et al., 2018). KAN and Wahkon share the same network width (neuron counts per layer) and grid size G = 9 . The MLP maintains the same depth as the Wahkon/KAN architecture, with each hidden layer’s neuron count scaled by √G = 3 to match the parameter count of a KAN layer with grid size G. The NTK is the infinite-width kernel induced by using the same depth as WKN/KAN in each experiment. Full implementation details are provided in Appendix C. We evaluate all methods on four benchmark functions ( f1–f4) that span highly nonlinear structure (f1, D = 3 ), high-dimensional radial oscillation ( f2, D = 10 ), additive composition ( f3, D = 4 ), and nested composition ( f4, D = 6 ). Responses are generated as yi = fk(xi) + 0 .1 εi with 

εi ∼ N (0 , 1) and inputs from Unif[ −1, 1] D . Each experiment is repeated 100 times on randomly generated replicate samples for each setting. The explicit formula of f1–f4 and corresponding model architectures are provided in Appendix C. Fig.2 plots test log(RMSE) evaluated on 1, 000 randomly generated testing data points versus training sample size ∈ [100 , 200 , 400 , 800 , 1600 , 3200] for all four functions. We found that Wahkon attains uniformly lower error than KAN, MLP, and NTK. MLPs underperform despite similar parameter counts, suggesting that learning univariate links within an RKHS is more sample-efficient than employing fixed activations. KANs narrow the gap at larger sample sizes, but the absence of explicit RKHS regularization limits their finite-sample behaviors, leading to slow convergence for small samples compared to Wahkon. NTK performs competitively at small n but plateaus earlier. 

## 7 Application: Predicting Surface Proteins from scRNA-seq 

Single-cell multi-omics promises richer biological insight but remains costly and technically chal-lenging at scale. CITE-seq (Stoeckius et al., 2017) profiles RNA and surface proteins jointly, yet 7Figure 3: Prediction error for 25 surface proteins in human bone marrow CITE-seq. Bars show mean test RMSE over repeated random splits; error bars indicate one standard deviation. antibody panels are expensive, subject to batch effects, and can suffer from dropout. Accurately predicting protein abundances from RNA would lower costs for protein panel design, and reveal RNA–protein relationships (Liu et al., 2024; Ma et al., 2025). We analyzed a human bone marrow mononuclear cell dataset (Stuart et al., 2019) with 30 ,672 

cells, 17 ,009 genes, and 25 surface proteins. RNA counts were library-size normalized and log-transformed, followed by selection of the top 1,000 most variable genes and PCA reduction to 30 

principal components. 25 protein’s abundance, measured by Antibody-derived tag (ADT) counts were transformed using centered log-ratio (CLR) normalization. The resulting 30 -dimensional RNA PCA matrix served as input features, and each of the 25 protein targets was predicted independently. For each protein, we trained a separate predictor using 10% randomly selected cells for training and the remaining for testing. Wahkon used the architecture [30 → 15 → 15 → 15 → 1] with the same training configuration and baseline methods described in Section 6.2. Each method was fit across 20 

random splits to assess stability, and performance was summarized by test RMSE. Figure 3 reports test MSE across the 25 proteins. Wahkon achieved the lowest average error compared with KAN, MLP, and NTK, and was best for the majority of targets with smaller variability across repetitions. Gains were particularly pronounced for CD45 isoforms (CD45RA, CD45RO), which differentiate naïve and memory T cells. 

## 8 Conclusion 

Wahkon unifies Kolmogorov’s compositional view with RKHS regularization to produce a deep model that is simultaneously expressive, and statistically controlled. The architecture replaces fixed activations by learned univariate links, and the hierarchical penalty enforces layerwise smoothness. This foundation supports a Bayesian formulation in which the penalized estimator is the MAP under a hierarchical Gaussian-process prior. Entropy bounds and convergence results clarify how depth and width interact with smoothness, and empirical studies align with these predictions while demonstrating gains over strong baselines, including MLP, KAN, and NTK. There are limits and opportunities. Computational cost grows with sample size and network width, suggesting approximate kernels (Nyström, random features) and low-rank structure as natural acceler-ations. Kernel choice influences performance; data-adaptive or learned kernels and sparsity-promoting priors over links may further improve accuracy and interpretability. By providing a principled bridge between modern representation learning and classical statistical inference, Wahkon offers a template for building deep models that are not only accurate but also reliable and explainable. Another promising direction is uncertainty quantification via the Bayesian equivalence established in Section 4. In practice, full posterior inference over all layers is computationally prohibitive due to the nonlinear coupling between layers. Scalable approximations, such as variational Bayesian inference, offer practical pathways but may underestimate uncertainty. Developing efficient posterior inference methods that propagate uncertainty through the compositional structure remains an important open problem and a natural next step for this framework. 8References 

Arnold, V. I. (1963). On functions of three variables. American Mathematical Society Translations: Series 2 28 , 51–54. Translated from Dokl. Akad. Nauk SSSR 114 (1957), 679–681. Barron, A. R. (1993). Universal approximation bounds for superpositions of a sigmoidal function. 

IEEE Transactions on Information Theory 39 (3), 930–945. Bauer, B. and M. Kohler (2019). On deep learning as a remedy for the curse of dimensionality in nonparametric regression. Edmunds, D. E. and H. Triebel (1996). Function Spaces, Entropy Numbers, Differential Operators .Cambridge University Press. Geer, S. A. (2000). Empirical Processes in M-estimation . Cambridge university press. Goodfellow, I., Y. Bengio, and A. Courville (2016). Deep Learning . MIT Press. Gu, C. (2013). Smoothing Spline ANOVA Models (2nd Ed.) . New York: Springer-Verlag. Horowitz, J. L. and E. Mammen (2007). Rate-optimal estimation for a general class of nonparametric regression models with unknown link functions. Annals of Statistics 35 (6), 2589–2619. Jacot, A., F. Gabriel, and C. Hongler (2018). Neural tangent kernel: Convergence and generalization in neural networks. Advances in Neural Information Processing Systems 31 .Kolmogorov, A. N. (1957). On the representation of continuous functions of several variables by superposition of continuous functions of one variable and addition. Doklady Akademii Nauk SSSR 114 , 953–956. In Russian; English translation in Amer. Math. Soc. Transl. (2) 28:55–59, 1963. LeCun, Y., L. Bottou, G. B. Orr, and K.-R. Müller (2002). Efficient backprop. In Neural networks: Tricks of the trade , pp. 9–50. Springer. Lee, J., Y. Bahri, R. Novak, S. S. Schoenholz, J. Pennington, and J. Sohl-Dickstein (2018). Deep neural networks as gaussian processes. In International Conference on Learning Representations .Lee, J., L. Xiao, S. Schoenholz, Y. Bahri, R. Novak, J. Sohl-Dickstein, and J. Pennington (2019). Wide neural networks of any depth evolve as linear models under gradient descent. Advances in neural information processing systems 32 .Liu, Y., Y. Chen, H. Lu, W. Zhong, G.-C. Yuan, and P. Ma (2024). Orthogonal multimodality integration and clustering in single-cell data. BMC bioinformatics 25 (1), 164. Liu, Z., Y. Wang, S. Vaidya, F. Ruehle, J. Halverson, M. Soljaˇ ci´ c, T. Y. Hou, and M. Tegmark (2024). KAN: Kolmogorov-Arnold networks. arXiv preprint arXiv:2404.19756 .Ma, P., Y. Chen, H. Lu, and W. Zhong (2025). Bisection grover’s search algorithm and its application in analyzing cite-seq data. Journal of the American Statistical Association 120 (549), 52–63. Novak, R., L. Xiao, J. Hron, J. Lee, A. A. Alemi, J. Sohl-Dickstein, and S. S. Schoenholz (2020). Neural tangents: Fast and easy infinite neural networks in python. In International Conference on Learning Representations (ICLR) .Rasmussen, C. E. and C. K. I. Williams (2006). Gaussian Processes for Machine Learning . Cam-bridge, MA: MIT Press. Schmidt-Hieber, J. (2020). Nonparametric regression using deep neural networks with ReLU activation function. The Annals of Statistics 48 (4), 1875–1897. Snoek, J., H. Larochelle, and R. P. Adams (2012). Practical bayesian optimization of machine learning algorithms. Advances in neural information processing systems 25 .Stoeckius, M., C. Hafemeister, W. Stephenson, B. Houck-Loomis, P. K. Chattopadhyay, H. Swerd-low, R. Satija, and P. Smibert (2017). Large-scale simultaneous measurement of epitopes and transcriptomes in single cells. Nature methods 14 (9), 865. 9Stuart, T., A. Butler, P. Hoffman, C. Hafemeister, E. Papalexi, W. M. Mauck, Y. Hao, M. Stoeckius, P. Smibert, and R. Satija (2019). Comprehensive integration of single-cell data. Cell 177 , 1888– 1902.e21. Wahba, G. (1978). Improper priors, spline smoothing and the problem of guarding against model errors in regression. Journal of the Royal Statistical Society Series B: Statistical Methodology 40 (3), 364–372. Wahba, G. (1990). Spline models for observational data . SIAM. Wilson, A. G., Z. Hu, R. Salakhutdinov, and E. P. Xing (2016). Deep kernel learning. In Artificial intelligence and statistics , pp. 370–378. PMLR. 

## Appendix A Derivations of Profile Objective 

By the standard kernel ridge regression identity, the fitted final-layer values are 

bX(L) = K(L−1)  K(L−1) + nλ LIn

−1y,

and the minimum value of the final-layer objective is 

nλ L y⊤ K(L−1) + nλ LIn

−1y

. Therefore, after profiling out the last layer, we obtain the objective 

P(C<L ) = min  

> {c(L)
> ·1k}DL−1
> k=1

L(C) = nλ L y⊤ K(L−1) + nλ LIn

−1y + n

> L−1

X

> l=1

λlDlX

> j=1
> Dl−1

X

> k=1

c(l)

> ·jk
> 2
> Q(l−1)
> k

.

(12) 

## B Implementation Details 

B.1 Kernel and Efficient computation 

The Gaussian kernel is used throughout: 

K(x, y ) = exp 



− (x − y)2

2ℓ2



, ℓ = 0 .5.

Grid-based approximation. The exact representer form uses kernel evaluations at the n layer-specific inputs for each link, which can be costly when n is large. In implementation, we use a fixed grid of G inducing points u1 < · · · < u G for each univariate link. A link is approx-imated by ϕ(l) 

> jk

(t) ≈ PGg=1 a(l) 

> gjk

K(ug , t ), where a(l) 

> gjk

are the grid coefficients. Let R(l−1)  

> k

=

h

K(x(l−1)  

> ik

, u g )

i 

> i=1 ,...,n ;g=1 ,...,G

be the n×G cross-kernel matrix between the layer-(l−1) inputs and the inducing grid. Then evaluations of the link on the training data are approximated by R(l−1)  

> k

a(l)

> ·jk

,reducing the per-link evaluation cost from O(n2) in the full representer expansion to O(nG ). The RKHS norm is approximated by ϕ(l)

> jk
> 2
> HK

≈ a(l)⊤·jk KU U a(l)

> ·jk

, KU U = [ K(ug , u g′ )] Gg,g ′=1 . This grid approximation is used only for computation; the theoretical results above are stated for the exact representer form. Further implementation details are provided in Appendix C. Each univariate link function is parameterized by a kernel expansion on G = 9 equally spaced inducing points that remain fixed during training. The grid-based parameterization reduces kernel matrix construction from O(n2) to O(nG ) per link function. Mini-batch training further limits the per-step cost: at each iteration, a random subset of B = 200 observations is drawn, and the kernel matrices and profile objective are evaluated on this subset. After the mini-batch optimization converges (or early stopping triggers), the last-layer representer coefficients are refitted using the full training set by solving the closed-form expression in (6) . Since the profile objective concentrates out the last layer’s coefficients, the number of parameters updated by the batch gradient descent is reduced by n × DL−1 relative to joint optimization, yielding faster convergence and lower memory usage. 10 B.2 Optimization 

The lower-layer coefficients {C(l)}L−1 

> l=1

are optimized by minimizing the profile objective (6) via the Adam optimizer with learning rate 0.005 , mini-batch size 200 , and a maximum of 500 gradient steps. Early stopping is employed Early stopping is used by reserving 20% of the training data as a validation set. Training stops if the validation loss does not improve by at least 10 −5 for 50 consecutive steps, and the model snapshot with the lowest validation RMSE is retained. 

B.3 Regularization hyperparameter selection 

The profile objective involves layer-specific penalties λ(1) , . . . , λ (L). In practice, we group these into two parts: a common penalty λ(1) = · · · = λ(L−1) = λlower for the feature-learning layers and a separate penalty λ(L) for the last layer. These play distinct roles: λlower controls the smoothness and complexity of the learned feature embedding, while λ(l) governs the bias–variance tradeoff in the final regression layer. The lower-layer penalty is fixed at λlower = n−4/5 

> train

× #links , where #links = PLl=1 Dl−1Dl is the total number of univariate link functions in the network. This scaling follows from the minimax-optimal rate in Theorem 5.1. In practice, we scale λlower by the sum of link functions rather than the product of widths, because we tend to overparameterize the network to obtain stable optimization and predictive performance. An underlying intuition is that the true target function, when expressed in the Wahkon class, typically requires a high intrinsic dimension at only one or a few layers, while the remaining layers have effectively low complexity. The sum-based scaling is more suitable than a worst-case product bound in practice. The last-layer penalty λ(L) is selected via Bayesian optimization with 5-fold cross-validation, as described in Section B.3. The last-layer penalty λ(l) is selected via Bayesian optimization (Snoek et al., 2012) (BO) with 5-fold cross-validated RMSE as the evaluation criterion. We use a Matérn-5/2 GP surrogate with Expected Improvement acquisition, 15 total evaluations ( 5 random initial points followed by 10 BO iterations), and search over λ(l) ∈ [0 .01 s, 3.0 s] where s = n−4/5 × #links . Each BO evaluation trains the full network from scratch with the candidate λ(l) and fixed λlower , ensuring that the lower-layer features are consistent with the regularization being evaluated. 

## C Simulation and Experiment Details 

This section provides the data-generating models, network architectures, and training configurations used in Sections 6 and 7. All methods share the same configuration; the single-cell CITE-seq application differs only in the network widths. 

C.1 Benchmark functions 

Inputs are i.i.d. sampled from Unif[ −1, 1] D and responses are yi = fk(xi)+0 .1 ϵi with ϵi ∼ N (0 , 1) .The four benchmark functions are: (f1) f1(x) = log( x21 +x22 +| tan x3|) + cot   π

> 1+ ex21+sin 6 x2+x23

, D = 3 

(f2) f2(x) = sin  P 10  

> i=1

x2

> i

, D = 10 

(f3) f3(x) = exp  12 [sin( π(x21 +x22)) + sin( π(x23 +x24))] , D = 4 

(f4) f4(x) = exp  sin( π(x21 +x22))  cos( πx 3x4), D = 6 

where f4 has two inactive variables ( x5, x6 are noise). Function Input dim. Wahkon/KAN width 

f1 (nonlinear) 3 [3 → 6 → 6 → 1] 

f2 (radial osc.) 10 [10 → 10 → 10 → 1] 

f3 (compositional) 4 [4 → 4 → 4 → 1] 

f4 (nested comp.) 6 [6 → 6 → 6 → 6 → 1] 

11 Figure 4: Convergence comparison of the profile objective versus the joint objective on benchmark functions f1–f4. Each panel shows training RMSE (left axis) and test RMSE (right axis) as a function of training steps. The dashed vertical line marks the early-stopping snapshot selected by validation performance. The profile objective consistently reaches lower test RMSE in fewer iterations across all four settings. 

C.2 Baselines configuration KAN. Uses the original implementation of Liu et al. (2024) with B-spline basis, grid size G = 9 

(matching Wahkon), LBFGS optimizer (learning rate 1.0 by default), 500 training steps, sparsity penalty tuned via Bayesian optimization (matching Wahkon), and mini-batch training. The network width (neuron counts) matches Wahkon. 

MLP. A standard feedforward network with ReLU activations. The MLP maintains the same depth (number of hidden layers) as the Wahkon/KAN architecture, with each hidden layer’s neuron count scaled by √G = 3 to approximately match the parameter count of a KAN with grid size G = 9 .Trained with Adam (learning rate 0.005 ), mini-batch size 200 , and early stopping (patience 50 ). 

NTK. The infinite-width neural tangent kernel (Jacot et al., 2018) is computed using the 

neural-tangents library (Novak et al., 2020) with a depth-matched architecture and Erf acti-vation. The noise variance σ2 for the NNGP posterior is selected by Bayesian optimization. 

## D Profile vs. Direct Objective: Empirical Comparison 

This section presents empirical evidence comparing the profile objective (6) against the direct optimization in (3) in which all representer coefficients, including the last layer, are updated by gradient descent. We use the same network architecture and hyperparameters for both objectives on each benchmark function, differing only in how the last-layer coefficients are handled. As shown in Fig. 4, the profile objective shows faster convergence across all four functions, typically reaching comparable testing loss in 2–3× fewer steps. Notably, the training curves of the direct objective exhibit periodic oscillatory behavior, visible as recurring spikes in both training and test RMSE throughout optimization. We attribute this to the coupling between the last-layer coefficients and the lower-layer kernel matrices in the gradient descent. When updating the lower-layer parameters, the kernel evaluations at the inducing grid points shift, which in turn changes the optimal last-layer solution. Since the direct objective updates both simultaneously by gradient descent, perturbations in the lower layers displace the current last-layer coefficients from their conditional optimum, causing transient increases in loss until the last-layer’s coefficients converge. The profile objective avoids 12 this pattern entirely as the last-layer coefficients are re-solved analytically at each step. Furthermore, the profile objective leads to a slightly lower error at the early-stopping point selected by validation performance. This improvement may be caused by two factors: (i) the closed-form last-layer solution eliminates optimization noise in the top layer, providing a cleaner gradient signal to the lower layers; and (ii) the reduced parameter count (by n×DL−1 representer coefficients) yields a better-conditioned optimization landscape that is less likely to overfit. 

## E Proofs of Main Results 

Throughout, HK denotes the RKHS with reproducing kernel K. For a layer-l link function, we use the notation ϕ(l) 

> jk

, where j = 1 , . . . , D l indexes the output coordinate and k = 1 , . . . , D l−1 indexes the input coordinate from the previous layer. 

E.1 Proof of Theorem 3.1 

Proof. The proof follows the standard RKHS representer argument, applied recursively through the layers. For the first layer, fix an edge (k → j) and consider ϕ(1)  

> jk

∈ H K. Let 

S(1)  

> k

= span {K (x(0) 1k , ·), . . . , K(x(0)  

> nk

, ·)}.

Every ϕ(1)  

> jk

can be decomposed uniquely as 

ϕ(1)  

> jk

=

> n

X

> i=1

c(1) 

> ijk

K(x(0)  

> ik

, ·) + ρ(1)  

> jk

, ρ(1)  

> jk

⊥ S (1)  

> k

.

By the reproducing property, 

ρ(1)  

> jk

(x(0)  

> ik

) = 

D

ρ(1)  

> jk

, K(x(0)  

> ik

, ·)

E

> HK

= 0 , i = 1 , . . . , n. 

Thus ρ(1)  

> jk

does not change any fitted layer-one output on the training sample, and hence does not change the empirical loss. It only increases the penalty, since 

∥ϕ(1)  

> jk

∥2 

> HK

=

> n

X

> i=1

c(1) 

> ijk

K(x(0)  

> ik

, ·)

> 2
> HK

+ ∥ρ(1)  

> jk

∥2 

> HK

.

Therefore any minimizer must have ρ(1)  

> jk

= 0 .Now suppose that the representer form holds for all layers before l. Then the layer-(l − 1) training outputs {x(l−1)  

> ik

: i = 1 , . . . , n } are fixed once the lower-layer coefficients are fixed. Applying the same orthogonal decomposition argument to ϕ(l) 

> jk

with respect to 

S(l) 

> k

= span {K (x(l−1) 1k , ·), . . . , K(x(l−1)  

> nk

, ·)}

shows that any component orthogonal to S(l) 

> k

does not change the fitted values on the training sample and only increases the RKHS penalty. Hence it must vanish at a minimizer. Therefore, 

ϕ(l) 

> jk

(t) = 

> n

X

> i=1

c(l)

> ijk

K(x(l−1)  

> ik

, t ),

for all j, k, l . This completes the induction. 

E.2 Finite-dimensional form 

Proof. By Theorem 3.1, 

ϕ(l) 

> jk

(t) = 

> n

X

> i=1

c(l)

> ijk

K(x(l−1)  

> ik

, t ).

13 Define the kernel matrix 

Q(l−1)  

> k

=

h

K(x(l−1)  

> ik

, x (l−1)  

> i′k

)

ini,i ′=1 

and the coefficient vector 

c(l) 

> ·jk

= ( c(l)1jk , . . . , c (l)

> njk

)⊤.

Using the reproducing property, 

∥ϕ(l) 

> jk

∥2 

> HK

=

* nX

> i=1

c(l)

> ijk

K(x(l−1)  

> ik

, ·),

> n

X

> i′=1

c(l)

> i′jk

K(x(l−1)  

> i′k

, ·)

+

> HK

=

> n

X

> i=1
> n

X

> i′=1

c(l)

> ijk

c(l)

> i′jk

K(x(l−1)  

> ik

, x (l−1)  

> i′k

)= c(l)⊤·jk Q(l−1)  

> k

c(l)

> ·jk

.

Substituting this identity into the RKHS penalty gives the finite-dimensional criterion in (5) . The penalty is quadratic in the coefficients for fixed layer inputs, while the empirical loss remains generally nonlinear because the layer outputs are obtained recursively through the network. 

E.3 Proof of Proposition 4.1 

Proof. Under the prior 

ϕ(l) 

> jk

∼ GP (0 , τ lK),

and conditional on the layer-(l − 1) outputs, the vector of link evaluations 

z(l) 

> jk

=



ϕ(l) 

> jk

(x(l−1) 1k ), . . . , ϕ (l) 

> jk

(x(l−1)  

> nk

)

⊤

is multivariate normal: 

z(l) 

> jk

| X(l−1) ∼ N 



0, τ lQ(l−1) 

> k



.

The vectors z(l) 

> jk

are conditionally independent across k for fixed j. Since 

X(l) 

> ·j

=

> Dl−1

X

> k=1

z(l) 

> jk

,

the sum of independent Gaussian vectors is Gaussian, yielding 

X(l) 

> ·j

| X(l−1) ∼ N 

0, τ lDl−1

X

> k=1

Q(l−1) 

> k

 .

If K(t, t ) = 1 , then the ith diagonal entry of PDl−1 

> k=1

Q(l−1)  

> k

equals Dl−1, and hence 

Var( x(l) 

> ij

| X(l−1) ) = τlDl−1, E(x(l) 

> ij

| X(l−1) ) = 0 .

Therefore, E

h

x(l)

> ij

i

= E

h

E(x(l) 

> ij

| X(l−1) )

i

= 0, Var 

h

x(l)

> ij

i

= Var 

h

E(x(l) 

> ij

| X(l−1) )

i

+

E

h

Var( x(l) 

> ij

| X(l−1) )

i

= τlDl−1.

E.4 Proof of Theorem 4.2 

Proof. Assume 

yi = ηWahkon (xi) + ϵi, ϵi

> iid

∼ N (0 , σ 2).

The likelihood is proportional to 

exp 

(

− 12σ2

> n

X

> i=1

yi − ηWahkon (xi)2

)

.

14 The independent GP priors on the link functions contribute the function-space penalty 

exp 

− 12

> L

X

> l=1

1

τlDlX

> j=1
> Dl−1

X

> k=1

∥ϕ(l) 

> jk

∥2

> HK

 .

Therefore, up to an additive constant, the negative log-posterior is 

12σ2

> n

X

> i=1

yi − ηWahkon (xi)2 + 12

> L

X

> l=1

1

τlDlX

> j=1
> Dl−1

X

> k=1

∥ϕ(l) 

> jk

∥2 

> HK

.

Multiplying by 2σ2 gives 

> n

X

> i=1

yi − ηWahkon (xi)2 +

> L

X

> l=1

σ2

τlDlX

> j=1
> Dl−1

X

> k=1

∥ϕ(l) 

> jk

∥2 

> HK

.

Thus this objective is identical to (3)–(4) when 

σ2

τl

= nλ l, equivalently τl = σ2

nλ l

.

The MAP estimator therefore coincides with the penalized least-squares estimator. The finite-dimensional coefficient form follows from Theorem 3.1. 

E.5 Preliminary Material for the Proof of Theorem 5.1 Assumptions. We first list the assumptions for deriving the minimax-convergence rate. 

Assumption E.1 (Bounded domain) . The predictor domain ΩX ⊂ RD is bounded; that is, ∥x∥2 ≤

CX for all x ∈ ΩX .

Assumption E.2 (Smooth link functions) . For constants Cϕ > 0 and β > 0, each link function satisfies 

∥ϕ(l) 

> jk

∥HK ≤ CϕD−βl , j = 1 , . . . , D l, k = 1 , . . . , D l−1.

The link functions are also uniformly Lipschitz on the relevant compact domain. 

Assumption E.3 (RKHS entropy) . For each C > 0, the RKHS ball {ϕ : ∥ϕ∥HK ≤ C} satisfies 

log N (δ, {ϕ : ∥ϕ∥HK ≤ C}, ∥ · ∥ ∞) ≤ C2δ−1/α , δ > 0,

for constants C2 > 0 and α > 0.

The regularity conditions outlined in our analysis are mild and broadly applicable. Assumption E.1, which requires predictors to reside within a bounded domain, is a standard assumption in both nonparametric regression and neural network theory (Gu, 2013; Horowitz and Mammen, 2007; Bauer and Kohler, 2019). Assumption E.2 imposes constraints on the Lipschitz continuity and RKHS norm of the activation functions to balance network expressivity with complexity control. Specifically, the RKHS norm bound ∥ϕ(l) 

> dl,d l−1

∥HK ≤ CϕD−βl enforces a decay rate on the norm of activation functions with respect to the number of neurons in the next layer. Assumption E.3 imposes a constraint on the complexity of the function class with the finite norm in RKHS HK. The value of αis determined by the kernel K, and a larger α corresponds to a more constrained function class. For example, for a Sobolev space, α is its order (Edmunds and Triebel, 1996). Under these assumptions, the size of the Wahkon function class can be controlled by an entropy bound. Let 

AD =

> L

Y

> l=0

Dl

!1+ 1  

> α−βα

.

The factor AD summarizes the effect of the architecture on statistical complexity. Width and depth increase the number of links, while the decay D−βl in the RKHS norm moderates this growth. 15 Theorem E.4 (Entropy bound) . Under Assumptions E.1–E.3, 

log N (δ, FWahkon , ∥ · ∥ 2) ≤ CA D δ−1/α ,

where C > 0 is independent of n and δ.

The entropy bound leads to the following convergence result. 

Proof of Theorem E.4. Let 

AD =

> L

Y

> l=0

Dl

!1+ 1  

> α−βα

.

We show that the covering number of the Wahkon class is bounded by CA D δ−1/α .For a single univariate link at layer l, Assumption E.3 and Assumption E.2 imply 

log N



ϵ, {ϕ : ∥ϕ∥HK ≤ CϕD−βl }, ∥ · ∥ ∞



≤ CD −β/α l ϵ−1/α .

Now consider one output coordinate of layer l,

x(l) 

> j

=

> Dl−1

X

> k=1

ϕ(l) 

> jk

(x(l−1)  

> k

).

A δ-cover for the sum can be constructed by using δ/D l−1-covers for each incoming link. Hence, for one output coordinate, the log-covering number is bounded by 

CD l−1D−β/α l

 δDl−1

−1/α 

= CD −β/α l D1+1 /α l−1 δ−1/α .

Since layer l has Dl output coordinates, the log-covering number for the layer map is bounded by 

CD 1−β/α l D1+1 /α l−1 δ−1/α .

The full Wahkon class is obtained by composing these layer maps. By the uniform Lipschitz condition in Assumption E.2, perturbations at an intermediate layer propagate to the output by at most a constant factor depending on the Lipschitz constants and the fixed depth L. These constants can be absorbed into C. Combining the layer-wise covers over l = 1 , . . . , L gives 

log N (δ, FWahkon , ∥ · ∥ 2) ≤ C

> L

Y

> l=0

Dl

!1+ 1  

> α−βα

δ−1/α .

This proves the stated entropy bound. 

E.6 Proof of Theorem 5.1 

Proof. Let η∗ be the best approximation to η in FWahkon , and define 

∆2approx = ∥η∗ − η∥2

> L2(PX)

.

By the triangle inequality, 

∥bηWahkon − η∥2 

> L2(PX)

≤ 2∥bηWahkon − η∗∥2 

> L2(PX)

+ 2∆ 2approx .

It remains to control the estimation error ∥bηWahkon − η∗∥2

> L2(PX)

.The penalized least-squares basic inequality gives 

1

n

> n

X

> i=1

bηWahkon (xi) − η∗(xi)2 + λnJ(bηWahkon ) ≤ empirical process term + λnJ(η∗).

16 Under the assumed boundedness and entropy conditions, standard penalized empirical-process theory for least-squares estimators (see, e.g., Geer, 2000) implies 

∥bηWahkon − η∗∥2 

> L2(PX)

= Op



λnJ(η∗) + AD n−1λ−1/(2 α)

> n



,

where 

AD =

> L

Y

> l=0

Dl

!1+ 1  

> α−βα

.

If the oracle η∗ has bounded penalty, J(η∗) = O(1) , the first term is O(λn). Combining this bound with the approximation term yields 

∥bηWahkon − η∥2 

> L2(PX)

= Op



∆2approx + λn + AD n−1λ−1/(2 α)

> n



,

which proves the theorem. Balancing the last two terms gives 

λn ≍

 AD

n

 2α

> 2α+1

,

and substituting this choice gives the stated optimized rate. 17