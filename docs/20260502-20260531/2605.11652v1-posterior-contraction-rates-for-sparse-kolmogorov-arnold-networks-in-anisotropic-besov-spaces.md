---
title: Posterior Contraction Rates for Sparse Kolmogorov-Arnold Networks in Anisotropic Besov Spaces
title_zh: 各向异性Besov空间中稀疏Kolmogorov-Arnold网络的后验收缩率
authors: "Jeunghun Oh, Kyeongwon Lee, Jaeyong Lee, Lizhen Lin"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11652v1"
tags: ["query:bayes-dl"]
score: 9.0
evidence: 稀疏贝叶斯KAN的后验收缩率分析，涉及贝叶斯神经网络训练与推断
tldr: 本文研究稀疏贝叶斯Kolmogorov-Arnold网络（KAN）在异向Besov空间上的后验收缩率。通过采用spike-and-slab稀疏先验，该模型达到近极小化最优收缩率，且收缩率由函数内在异向光滑性决定。放置超先验可实现自适应。与标准MLP不同，KAN深度可固定，近似复杂度由网络宽度、样条网格和参数稀疏性控制。进一步扩展到复合Besov空间，收缩率取决于层状光滑性和有效维度，避免了维数诅咒。这为贝叶斯神经网络提供了理论支撑，为KAN建立了严谨统计基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11652v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 909, \"label\": \"Figure\"}]"
motivation: 为KAN提供严格的贝叶斯统计基础，证明稀疏先验下的后验收敛速率最优性。
method: 采用spike-and-slab稀疏先验，利用样条边缘函数控制网络宽度、样条网格及参数稀疏性，开发适配稀疏样条架构的理论工具。
result: 在异向Besov空间上实现近极小化最优后验收缩率，通过超先验自适应未知光滑性；在复合Besov空间上依赖层状光滑性和有效维度，避免维数诅咒。
conclusion: 为贝叶斯神经网络提供了理论理解，并为KAN建立了严格的统计基础。
---

## 摘要
我们研究了各向异性Besov空间上稀疏贝叶斯Kolmogorov-Arnold网络（KANs）的后验收缩率，从贝叶斯角度为KANs提供了统计基础。我们证明，配备spike-and-slab类型稀疏先验的稀疏贝叶斯KANs达到了近最优的后验收缩。特别地，收缩率取决于底层函数的内在各向异性光滑性。此外，通过在单个模型大小参数上放置超先验，所得后验适应未知的各向异性光滑性，并仍能达到相应的近最优率。与基于标准稀疏MLP模型的结果相比，我们结果的一个显著特征是KAN深度可以保持固定：由于可学习样条边缘函数的灵活性，所需的逼近复杂度通过网络宽度、样条网格范围和大小以及参数稀疏性来控制。我们的分析开发了针对稀疏样条边缘架构的理论工具，包括贝叶斯KANs的逼近和复杂度界。然后我们扩展到组合Besov空间，并表明收缩率取决于底层组合结构的逐层光滑性和有效维度，从而有效避免了维数灾难。总之，所开发的工具和发现推进了贝叶斯神经网络的理论理解，并为KANs提供了严格的统计基础。

## Abstract
We study posterior contraction rates for sparse Bayesian Kolmogorov-Arnold networks (KANs) over anisotropic Besov spaces, providing a statistical foundation of KANs from a Bayesian point of view. We show that sparse Bayesian KANs equipped with spike-and-slab-type sparsity priors attain the near-minimax posterior contraction. In particular, the contraction rate depends on the intrinsic anisotropic smoothness of the underlying function. Moreover, by placing a hyperprior on a single model-size parameter, the resulting posterior adapts to unknown anisotropic smoothness and still achieves the corresponding near-minimax rate. A distinctive feature of our results, compared with those for standard sparse MLP-based models, is that the KAN depth can be kept fixed: owing to the flexibility of learnable spline edge functions, the required approximation complexity is controlled through the network width, spline-grid range and size, and parameter sparsity. Our analysis develops theoretical tools tailored to sparse spline-edge architectures, including approximation and complexity bounds for Bayesian KANs. We then extend to compositional Besov spaces and show that the contraction rates depend on layerwise smoothness and effective dimension of the underlying compositional structure, thereby effectively avoiding the curse of dimensionality. Together, the developed tools and findings advance the theoretical understanding of Bayesian neural networks and provide rigorous statistical foundations for KANs.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：Kolmogorov-Arnold 网络（KAN）作为一种新兴的神经网络架构，用可学习的样条边缘函数替代固定激活函数，具有更强的灵活性和可解释性。然而，其贝叶斯统计理论基础尚不完善，特别是对异向（anisotropic）光滑函数类的后验收缩性质缺乏严格分析。
- **核心问题**：在异向 Besov 空间（Anisotropic Besov Spaces）中，稀疏贝叶斯 KAN 能否达到近最优的后验收缩率？收缩率如何依赖于函数的内在各向异性光滑性？网络能否自适应未知光滑性？在组合结构下能否缓解维数诅咒？
- **整体含义**：本文从贝叶斯角度为 KAN 提供了严格的统计基础，证明了配备 spike-and-slab 稀疏先验的 KAN 能够以近最优速率收缩到真实函数，其深度可以固定，近似能力来自宽度、网格和稀疏性。这一结果首次将贝叶斯神经网络的最优收缩理论从 ReLU-MLP 扩展到了样条边缘架构，并揭示了 KAN 在适应异向光滑性和避免维数诅咒方面的潜力。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用稀疏贝叶斯 KAN 对异向 Besov 函数进行逼近，通过 spike-and-slab 先验实现参数稀疏性，在固定网络深度下获得与光滑性匹配的收缩率。
- **关键技术细节**：
  1. **KAN 架构**：每层边缘函数由 B-样条展开和 SiLU 激活叠加构成，采用等距固定结点网格。深度固定为 \(L_0 = 3 + 2\lceil\log_2 d\rceil\)，宽度 \(D_n = 2dN_n\)，样条网格区间数 \(G_n = 4H_n - 2m\)，网格范围 \(H_n = \lceil 2B^\star_n\rceil\)，参数稀疏性 \(S_n = \lceil S_0 N_n\rceil\)，其中 \(N_n \asymp n^{1/(2\tilde s+1)}\) 为模型大小参数。
  2. **逼近定理**：对任意异向 Besov 函数 \(f_0\)，存在一个稀疏 KAN 逼近器 \(f^\star_n\)，逼近误差 \(\|f_0 - f^\star_n\|_{L^2} \lesssim N_n^{-\tilde s}\)，且参数满足 \(\|\theta^\star_n\|_\infty \leq B^\star_n = B_0 N_n^\beta\)，\(\|\theta^\star_n\|_0 \leq S_0 N_n\)，其中 \(\beta\) 依赖于光滑性和 \(p\) 值。
  3. **先验设置**：
     - **Spike-and-slab 先验**：给定模型大小参数，先验在恰好 \(S_n\) 个非零坐标上均匀，连续 slab 密度满足下界和重尾条件（如均匀、高斯、拉普拉斯、子威布尔分布）。
     - **Bernoulli 变体**：包含指标变量独立伯努利，包含概率 \(\rho_n\) 满足 \(T_n^{-1}\lesssim \rho_n \ll 1\)。
     - **自适应先验**：对模型大小参数 \(N\) 放置超先验 \(\pi_N(N)\propto e^{-\lambda_N N\log N}\)，其他架构参数通过保守上界覆盖未知光滑性。
  4. **后验收缩率证明**：利用通用后验收缩框架（Ghosal & Van der Vaart），分别验证：
     - **熵条件**：通过稀疏 KAN 的覆盖数界，计算 \(\log N(\epsilon_n, \mathcal{F}_n, L^2) \lesssim n\epsilon_n^2\)。
     - **先验质量**：构造逼近网络，证明其周围参数集上的先验质量满足 \(-\log\Pi(A_n) \lesssim n\epsilon_n^2\)。
     - **筛余概率**：参数超出界的概率指数级小，可被 \(e^{-C n\epsilon_n^2}\) 控制。
  5. **组合 Besov 空间扩展**：通过逐层逼近和误差传播分析（利用 Hölder 嵌入），得到收缩率由内在光滑层 \(\tilde s^*\) 决定，与有效维度而非环境维度 \(d\) 相关。

### 实验设计

- **无实验**：本文为纯理论数学分析，不包含任何实验验证、数据集使用或基准方法对比。所有结果均为理论定理证明。

### 资源与算力

- **未提及**：论文未讨论任何计算资源、GPU 型号、训练时长等。因为是纯理论研究，不需要算力。

### 实验数量与充分性

- **不适用**：论文未进行任何实验，因此不存在实验数量、消融实验或公平性比较的问题。理论证明中的假设（如（A1）–（A4）、（B1）–（B3））均在论文中明确给出，证明过程完整。

### 论文的主要结论与发现

1. **近最优后验收缩率**：在异向 Besov 空间上，稀疏贝叶斯 KAN（spike-and-slab 先验）的后验收缩率为 \(\epsilon_n = n^{-\tilde s/(2\tilde s+1)}(\log n)^{1/2}\)，与已知的极小极大速率仅差一个对数因子。
2. **深度固定、复杂度可控**：KAN 深度可以固定为 \(L_0\)，而近似能力通过宽度 \(D_n\)、样条网格范围 \(H_n\) 和稀疏性 \(S_n\) 控制，无需增加深度或细化网格。
3. **自适应光滑性**：通过在单个模型大小参数 \(N\) 上放置超先验 \(\pi_N(N)\)，后验适应未知的异向光滑性，仍达到近最优率。
4. **组合结构避免维数诅咒**：在组合异向 Besov 空间上，收缩率由内在层 \(\tilde s^*\) 和有效维度 \(t^*\) 决定，与高维环境维度无关，从而缓解了维数灾难。
5. **理论基础推广**：将贝叶斯神经网络的极小极大最优理论从 ReLU-MLP 推广到样条边缘架构（KAN），表明稀疏先验和单一模型大小超先验适用于更丰富的架构。

### 优点

- **理论深度**：首次为 KAN 提供完整的贝叶斯后验收缩率理论，分析了异向光滑性和组合结构下的最优性。
- **方法创新**：提出固定深度 KAN 的显式构造，逼近误差与网络宽度、网格范围、稀疏性参数直接关联，技术工具（覆盖数、先验质量、筛余概率）均针对稀疏样条架构定制。
- **通用性**：覆盖多种 slab 先验（均匀、高斯、拉普拉斯、子威布尔），且扩展到 Bernoulli 先验和自适应先验。
- **可解释性**：揭示了 KAN 与 MLP 的关键差异——深度可固定而复杂度通过样条分辨率控制，解释了 KAN 的灵活性和效率。

### 不足与局限

- **缺乏实验验证**：论文为纯理论分析，未进行数值实验或模拟，无法验证理论率在有限样本下的实际表现。
- **理论假设较强**：需要满足异向 Besov 嵌入条件（\(\tilde s > (1/p - 1/2)_+\)）、样条阶数限制（\(m \ge 2\)）、固定网格区间等，实际应用可能难以完全满足。
- **未考虑计算可行性**：后验推断（MCMC、变分方法）的可行性、收敛速度及计算开销未讨论，理论最优性可能难以在计算上实现。
- **仅考虑特定架构**：假设等宽度、均匀网格、深度固定为有限值，未覆盖更一般的 KAN 结构（如自适应结点、可变深度）。
- **对组合结构的假设**：需要逐层光滑性条件和有效维度约束，实际数据的组合结构可能不满足这些假设。

### 对论文证明技巧的评价

- 论文的证明主要依赖非参数贝叶斯后验收缩的通用框架（Ghosal & Van der Vaart 的经典方法），但针对 KAN 架构进行了细致的适配。特别地，作者构造了逼近器并显式给出了参数绑定的上界，这需要精细的 Besov 空间逼近理论和样条函数分析。覆盖数计算中利用了稀疏参数集的结构，通过球覆盖和组合计数得到与维度无关的对数项，体现了技术的高明。先验概率的下界估计利用了 spike-and-slab 先验的灵活性和连续密度性质，而筛余概率的指数衰减则依赖于参数先验的紧支撑性质。整体证明链条完整，但部分步骤（如定理 3 的证明）涉及大量常数和繁琐的索引处理，可读性有待提高，可能需要读者具有一定的高维逼近理论背景。

### 对论文假设合理性的讨论

- 异向 Besov 空间假设是函数空间分析的标准设定，但实际应用中函数的各向异性光滑性往往是未知的。论文通过自适应先验解决了后者，但异向光滑性指数是否真实存在仍需依赖数据或先验知识。固定网格区间和均匀结点可能限制 KAN 在实际中对高频区域的适应能力，因为最优网格往往需要根据函数变化率自适应调整。另外，假设样条阶数 \(m \ge 2\) 排除了线性样条，这可能是为了避免逼近精度的退化（线性样条无法达到更高的逼近阶）。总体而言，这些假设在理论分析中是合理且常见的，但应用时需要谨慎对待，例如可以通过预分析或交叉验证调整网格参数。

### 论文的贡献与影响

- 该论文填补了贝叶斯 KAN 理论分析的空白，为将 KAN 应用于不确定性量化提供了严格的统计基础。它证明稀疏贝叶斯 KAN 与其他贝叶斯神经网络（如 ReLU-MLP）具有相同的后验收缩最优性，且在某些光滑结构下表现更好（例如，KAN 的深度可固定而 MLP 需要深度适应）。这可能会推动贝叶斯深度学习社区更加关注样条基架构。此外，论文中关于组合假设的分析为处理高维复杂结构提供了一个理论框架，具有启发性，例如在物理建模中函数常可分解为低维子函数的和或乘积。

### 潜在未来工作

- **1）实验验证与模拟**：设计模拟实验，验证后验收缩率是否如理论预期（例如，生成异向 Besov 函数样本，使用 MCMC 或变分推断进行贝叶斯后验估计，并比较均方误差与理论率），同时评估 spike-and-slab 先验在实际采样中的收敛速度与计算效率。
- **2）扩展架构**：考虑自适应结点（如自由结点样条）、可变深度或多尺度样条网格，并分析相应的后验性质。例如，深度自适应可能进一步改善组合结构的逼近效率。
- **3）更紧的常数与对数因子**：优化定理中的对数因子（如 \((\log n)^{1/2}\)），探讨是否能达到确切极小极大速率（即擦除对数因子），或者证明在对数因子下是紧的。
- **4）实际应用场景**：将稀疏贝叶斯 KAN 应用于科学计算图像、偏微分方程求解、物理信息学习等典型场景，评估其在有限样本下的预测精度和不确定性量化表现，并与贝叶斯 MLP、高斯过程等方法对比。
- **5）与其他先验的对比**：研究基于扩散先验、高斯过程先验或泛化贝叶斯框架（如分数型贝叶斯）在 KAN 上的后验收缩性质，考察是否能在更弱的先验假设下保持最优性。

### 对论文写作质量的评价

- 论文结构清晰，先定义数学框架，再陈述主要定理，最后给出证明纲要。定理陈述精确，符号一致，但在证明细节上略显密集，特别是在处理组合 Besov 空间部分（定理4）时，推导较长且依赖多重引理，初学者可能难以跟进。参考文献全面，涵盖了非参数贝叶斯、样条逼近和神经网络理论的相关经典工作。整体而言，写作规范、逻辑严谨，适合有一定统计学习理论基础的读者阅读。

### 总结

- 本文是一篇高质量的理论统计论文，首次为 Kolmogorov-Arnold 网络提供了严格的后验收缩率分析，深入揭示了其异向光滑自适应性和组合结构下的维数鲁棒性。尽管缺乏实验且假设较强，但其理论贡献清晰且具有广泛影响，是贝叶斯深度学习理论的重要进展，为后续研究指明了方向。

（完）
