Title: Robust Surrogate-Based Bayesian Inference via Sampling-Based Adaptive Active Learning (SALE)

URL Source: https://arxiv.org/pdf/2608.00841v1

Published Time: Tue, 04 Aug 2026 00:53:11 GMT

Number of Pages: 74

Markdown Content:
# Robust Surrogate-Based Bayesian Inference via Sampling-Based Adaptive Active Learning (SALE) 

Dayi Li dayi.li@mail.utoronto.ca 

Department of Statistical Sciences, David A. Dunlap Department of Astronomy & Astrophysics University of Toronto 700 University Avenue, 9th Floor Toronto, ON M5G 1Z5, Canada 

Abstract 

Bayesian inference is difficult when likelihood evaluations are expensive and budgets are limited. We propose sampling-based adaptive active learning (SALE), a Gaussian-process (GP) framework for surrogate-based Bayesian inference. SALE uses the expected poste-rior (EP) induced by normalised GP sample paths as a common sequential-design mea-sure: it defines a posterior-guided search region and weights uncertainty reduction (UR). A state-dependent rule allocates evaluations between Bayesian optimisation (BO) for lo-calisation and UR for calibration. For BO, an annealed objective interpolates between the EP and Thompson sampling while regularising the query law against surrogate-path perturbations. For UR, we introduce an ideal EP-weighted rule and a computationally feasible proxy. Under a Bayesian GP framework, we characterise the annealed objective’s stability–bias trade-off through perturbation and Bayesian regret bounds, derive explicit budget-dependent expected total-variation control for the ideal EP-weighted UR, and es-tablish an expected total-variation rate for the implemented proxy. Across analytic bench-marks and simulated likelihoods, SALE reduces total-variation error across all considered settings while avoiding severe failures seen under several external baselines. Econometric and astrophysical examples demonstrate its practical value. 

Keywords: active learning, Bayesian inference, expensive likelihood, Gaussian process, surrogate models 

1 Introduction 

Standard Bayesian inference methods, such as Markov chain Monte Carlo (MCMC), become difficult when the likelihood or log-posterior evaluation is expensive. This occurs when evaluating the likelihood requires a simulator (Pellejero-Iba˜ nez et al., 2020; Zeghal et al., 2025; Lovell et al., 2025), a parameter-dependent normalising constant in the likelihood (Park and Haran, 2018, 2020; Matsubara et al., 2024), repeated integration over latent structure (Bivand et al., 2014; G´ omez-Rubio and Rue, 2018), or the numerical solution of an inverse problem (Lan et al., 2023; Bingham et al., 2024; Kim and Sanz-Alonso, 2025). Standard MCMC may require many more evaluations than the available budget permits. Let f (θ), θ ∈ Ω ⊆ Rd, denote the expensive log-unnormalised posterior. Surrogate-based Bayesian inference learns a cheap approximation to f from a limited evaluation budget, commonly using a Gaussian process (GP; Rasmussen and Williams, 2006) as the surrogate model. Active learning (AL; Settles, 2012) then uses the evolving GP posterior to sequentially select where f is evaluated. Under this framework, inference has been pur- 

> ©2026 Dayi Li. License: CC-BY 4.0, see https://creativecommons.org/licenses/by/4.0/ .arXiv:2608.00841v1 [stat.CO] 1 Aug 2026 Li

sued through direct posterior estimation (Gutmann and Corander, 2016; Kandasamy et al., 2017; Wang and Li, 2018; El Gammal et al., 2023; Li and Zhang, 2026), variational posterior approximation (Acerbi, 2018), and GP-emulated MCMC (J¨ arvenp¨ a¨ a and Corander, 2024). In practice, computational budgets typically permit only one sequential design run, with little opportunity for restarts or problem-specific tuning. The key is that accurate posterior inference generally does not require uniform convergence of the surrogate on Ω, instead it should only evaluate f at inference-relevant regions. Robust finite-budget inference therefore needs to solve two learning problems. Localisation identifies regions carrying appreciable posterior mass. Calibration ensures approximation accuracy throughout those regions. Within direct posterior estimation, one line of work uses Bayesian optimisation (BO; Shahriari et al., 2016) to locate high log-posterior values and the posterior mode (Gutmann and Corander, 2016; Kim and Sanz-Alonso, 2025; Li and Zhang, 2026). These methods can be effective when posterior mass is concentrated around a dominant mode. However, they typically inherit generic BO strategies that treat f as a black-box objective, so they do not exploit the log-density structure of f and may explore inference-irrelevant regions. Standard optimisation guarantees also do not control the resulting surrogate posterior approximation. A second line of work uses uncertainty reduction (UR) for calibration. Posterior-aware UR criteria either weight GP uncertainty by the current surrogate posterior (Kandasamy et al., 2017; Wang and Li, 2018; J¨ arvenp¨ a¨ a et al., 2019, 2021; Llorente et al., 2020; S¨ urer et al., 2024; Villani et al., 2024; Lartaud et al., 2026) or restrict UR to an estimated high-posterior region (Picheny et al., 2010; Bect et al., 2019; Lartaud et al., 2026). However, these strategies provide no common uncertainty-propagated sequential design measure for coordinating localisation and calibration. Tension also exists between theory and finite-budget practice. Existing budget-dependent analyses typically rely on space-filling or pre-scribed random design (Llorente et al., 2020; Helin et al., 2024; Kim and Sanz-Alonso, 2025). Since coverage is controlled independently of the evolving surrogate, convergence rates are tractable, but empirical performance may be suboptimal. Posterior-weighted UR is gener-ally more efficient, but its decision depends on the evolving surrogate, creating a feedback loop that is difficult to control theoretically. To our knowledge, no previous analysis gives an explicit budget-dependent bound on the approximation error of the final surrogate posterior under a sequential posterior-weighted UR design. The exclusive use of BO or UR causes a further limitation. Before localisation, posterior-weighted UR can be misled by a highly uncertain weighting measure and spend evaluations in regions with negligible true posterior mass. BO is therefore needed for localisation. After localisation, UR is needed to ensure calibration in other inference-relevant regions. A reliable method must adapt its allocation between BO and UR as the surrogate evolves. We propose sampling-based adaptive active learning (SALE), an AL framework for GP-surrogate posterior approximation. At iteration t, each GP posterior sample path induces a posterior law on Ω, and averaging these laws gives the expected posterior (EP), denoted by ¯πt. The EP has previously been studied as an uncertainty-propagated posterior estimator and, under the Bayesian GP setup, minimises conditional expected Kullback–Leibler (KL) loss (Reiser et al., 2025; Roberts et al., 2026a,b). SALE instead uses the EP as a common sequential design measure for localisation and calibration. To our knowledge, SALE is the first framework to use the EP as a sequential design law. 

> 2Sampling-Based Adaptive Active Learning

1.1 Contributions 

The main contributions of this paper are as follows. 1. We use the EP as a sequential design measure for AL. Samples from ¯ πt define a posterior-guided search region for BO and supply the candidate set and weighting measure for UR. EP therefore guides both localisation and calibration. 2. We introduce a state-dependent BO–UR allocation rule based on local design reso-lution around the incumbent maximiser. It favours BO while resolution is low and shifts toward UR as it improves. For the allocation rule, we show that UR receives a positive asymptotic fraction of evaluations. 3. For BO, we introduce the annealed objective (AO), which averages the τ -tempered posterior laws induced by GP sample paths. AO equals ¯ πt at τ = 1 and approaches Thompson sampling (TS; Russo et al., 2018) as τ ↓ 0. We show that positive τ -tempering regularises the AO query law against surrogate-path perturbations, while the corresponding TV continuity need not hold for TS. We develop exchange simulated annealing to target AO and derive a Bayesian regret bound quantifying the associated optimisation penalty of AO. 4. We define an ideal EP-weighted UR rule and show that its one-step objective is the expected decrease of a variance functional controlling the conditional expected total-variation (TV) error between the EP and the true posterior. This yields an explicit budget-dependent bound on the expected TV error of the final EP produced from a sequential posterior-weighted UR design. The ideal rule motivates a practical UR proxy, and we establish an expected-TV rate with the same polynomial budget order as the ideal UR rule, up to additional logarithmic factors. For empirical assessment, we focus on the robustness of a method, that is, reliable finite-budget posterior accuracy across different problems. We assess it through joint-TV distance reduction, performance variations across runs, and the frequency of severe failures. We evaluate SALE on analytic benchmarks, simulated likelihoods, and applications in econometrics and astrophysics. Among the methods considered, none is best on every prob-lem in terms of median convergence rate. SALE’s main empirical advantage is robustness: it reduces TV across all settings and avoids several severe failures seen under the considered external baselines. The ablation study identifies EP-guided region restriction as the main stabilising component, with state-dependent allocation, AO, and the practical UR proxy providing further gains. The paper is organised as follows. Section 2 defines the inferential objects and the stand-ing theoretical framework. Section 3 reviews related methods. Section 4 presents SALE and its main theoretical results. Section 5 reports the benchmark and simulation studies, and Section 6 presents the two applications. Section 7 concludes. Appendix A contains core implementation and metric details, while Appendix B contains proofs and auxiliary theoretical results. Extended implementation and reproducibility details and additional empirical results are provided in the Online Appendix. The source code, scripts, data, and result files required to reproduce the numerical results in this paper are archived at https://doi.org/10.5281/zenodo.20319901 . An actively maintained R package is at https://github.com/davidolohowski/SALE .

> 3Li

2 Problem Setup and Theoretical Framework 

Let X = ( x1, . . . , xn) ∈ Xn denote the fixed observed data. Consider a parametric model 

P = {Pθ : θ ∈ Ω}, where Ω ⊂ Rd, and suppose the model admits a likelihood L(θ | X) with respect to a common dominating measure on Xn. Let B(Ω) denote the Borel σ-algebra on Ω. Let p◦ be the prior density with respect to the Lebesgue measure λ on Ω. Define 

f (θ) := log L(θ | X) + log p◦(θ), Z(f ) := 

Z

> Ω

exp {f (u)} λ(d u).

We refer to f as the objective function . The posterior law and its Lebesgue density are 

πX(d θ) = pX(θ)λ(d θ), pX(θ) := exp {f (θ)}/Z(f ).

We consider cases where f (θ) can be evaluated, possibly with noise, but each evaluation is expensive. The goal is to construct a cheap surrogate of f that can be used for subsequent inference by evaluating f at only a small set of design points. For probability laws π and ρ on Ω, write TV( π, ρ ) := sup A∈B(Ω) |π(A) − ρ(A)|. Addi-tionally, write [ n] = {1, . . . , n } for n ∈ N+ and [ n]0 = {0, 1, . . . , n } for n ∈ N.

2.1 GP Surrogate and Sequential Design 

We place a GP prior on f , f ∼ GP (0 , κ ), where κ : Ω × Ω → R is positive definite. Suppose the observations on f are made sequentially, possibly with n0 ≥ 0 initial observations. After 

t active iterations, nt = n0 + t and the design is Dt = {(θi, y i)}nt

> i=1

, with yi = f (θi) + εi,

εi iid 

∼ N (0 , ς 2), and ς2 ≥ 0. Write yt = ( y1, . . . , y nt )⊤, Kt = {κ(θi, θj )}nt

> i,j =1

, kt(θ) = [κ(θ, θ1), . . . , κ (θ, θnt )] ⊤. The GP posterior given Dt has mean, variance, and covariance 

μt(θ) = kt(θ)⊤(Kt + ς2Int )−1yt,σ2 

> t

(θ) = κ(θ, θ) − kt(θ)⊤(Kt + ς2Int )−1kt(θ),κt(θ, θ′) = κ(θ, θ′) − kt(θ)⊤(Kt + ς2Int )−1kt(θ′).

In this paper, we use squared-exponential (SE) kernels with coordinate-specific length scales, yielding automatic relevance determination (Rasmussen and Williams, 2006). The method also applies to sufficiently smooth Mat´ ern kernels. GP-based AL uses the current GP posterior to select the next query θnt+1 through an acquisition rule (Settles, 2012). After observing ynt+1 , ( θnt+1 , y nt+1 ) is added to Dt, the GP posterior is updated, and the process repeats until the evaluation budget is exhausted. For concise theoretical notation, after conditioning on D0 we relabel the active query θnt+1 

as θt+1 ; algorithmic statements retain the original data indexing. Let Ht := σ(Dt) be the information available before the next query is generated. Con-ditional on Ht, write Pt(d g) := P(d g | H t) for the GP posterior law and let ft ∼ Pt denote a sample path drawn from Pt.

2.2 Standing GP Assumptions 

We use the following common framework for theoretical analyses. Assumptions and condi-tions pertaining only to particular components of the analyses are stated in Appendix B. 

> 4Sampling-Based Adaptive Active Learning

Assumption 1. Unless stated otherwise, the following conditions hold. (A1) The domain Ω ⊂ Rd is compact and convex and has nonempty interior. d is fixed. There are constants cΩ, r Ω > 0 such that, with B(θ, r ) = {u : ∥u − θ∥< r },

λ{B(θ, r ) ∩ Ω} ≥ cΩrd, θ ∈ Ω, 0 < r ≤ rΩ.

(A2) f is generated from the GP prior GP (0 , κ ) used by the algorithm, and 

yi = f (θi) + εi, εi iid 

∼ N (0 , ς 2),

where the εi are independent of f and of the algorithmic randomness. The kernel hyperparameters and ς2 are fixed, and GP updating is exact. If ς2 = 0 , redundant observations are removed before GP conditioning. (A3) At iteration t, the next query is measurable with respect to Ht and fresh algorithmic randomness that is independent of f , εi, and earlier algorithmic randomness. Thus the design is non-anticipating. Moreover, conditional on Ht, f, f t iid 

∼ Pt.(A4) For every t, the posterior path ft has a unique maximiser over Ω, Pt-almost surely. (A5) For an open set U ⊃ Ω, κ extends to a covariance kernel on U × U , satisfies Bκ := sup θ∈Ω κ(θ, θ) ∈ (0 , ∞), and the corresponding prior GP on U has a version with 

C1(U ) sample paths a.s. Let d20 (θ, θ′) := Var {f (θ) − f (θ′)} and, for j ∈ [d], let 

d20,j (θ, θ′) := Var {∂j f (θ) − ∂j f (θ′)}. Writing N (ϵ, Ω, d) for the covering number under a pseudometric d, assume 

J0 := 

Z diam(Ω ,d0)0

plog N (ϵ, Ω, d0) d ϵ < ∞,J(j)0 := 

Z diam(Ω ,d0,j )0

q

log N (ϵ, Ω, d0,j ) d ϵ < ∞, j ∈ [d],

and B∂,j := sup θ∈Ω Var {∂j f (θ)} < ∞ for every j. Moreover, for every t, almost surely, μt ∈ C1(U ) and ∂j μt(θ) = E{∂j f (θ) | H t}, ∀θ ∈ Ω, j ∈ [d].

Assumption 1 describes the theoretical Bayesian experiment. In practice, GP hyper-parameters are estimated and several posterior calculations are approximated. The theory may also be read conditional on the initial design D0, with active time t starting after D0.Standard SE kernels satisfy Assumption 1, as do Mat´ ern kernels with smoothness ν > 1. 

2.3 Posterior Laws Induced by the Surrogate Definition 2. For a continuous function g : Ω → R, define 

π(d θ | g) = exp {g(θ)}Z(g) λ(d θ), Z(g) := 

Z

> Ω

exp {g(u)} λ(d u).

The posterior law based on the GP posterior mean is πμt (d θ) := π(d θ | μt). The expected posterior (EP) is 

¯πt(A) := 

Z

π(A | g) Pt(d g), A ∈ B(Ω) .

> 5Li

Equivalently, ¯πt(d θ) = Eft∼Pt {π(d θ | ft)}. Its Lebesgue density is 

¯pt(θ) := d¯ πt

dλ (θ) = 

Z exp {g(θ)}Z(g) Pt(d g) = E{pX(θ) | H t} .

¯πt was studied as an uncertainty-propagated inferential object by Reiser et al. (2025) and Roberts et al. (2026a,b). Its use in SALE is supported by the following result, which is Proposition 1 of Roberts et al. (2026a) applied conditionally on Ht.

Proposition 3. Under Assumption 1, let eπt be any Ht-measurable probability law on Ω.Then, provided the KL loss is well defined, 

E{KL( πX∥eπt) | H t} ≥ E{KL( πX∥¯πt) | H t} a.s. 

Thus, among estimators of πX given Ht, ¯ πt minimises the conditional expected forward-KL loss. Instead of using ¯ πt only as an inferential output, SALE uses it to guide sequential design, and the main calibration theory concerns ¯ πt. To our knowledge, the use of ¯ πt as a common sequential design measure for region restriction, BO, and UR is unexplored. For empirical comparisons, Section 5 uses πμt as a cheap deterministic summary for all methods. ¯πt is also dynamically coherent as a weighting law, as seen in the following proposition. 

Proposition 4. Under Assumption 1, {¯πt}t≥0 is a measure-valued martingale. Equiva-lently, for every bounded measurable h : Ω → R,

Mt(h) := 

Z

> Ω

h(θ) ¯ πt(d θ) = E

Z 

> Ω

h(θ) πX(d θ) Ht



,

is a bounded martingale. Thus, there exists a probability law ¯π∞ such that ¯πt =⇒ ¯π∞ a.s. 

The proof is given in Appendix B.5. Conditional on Ht, the expected value of ¯ πt after the next query is its current value. The realised law can change when a new response is observed, but it has no conditional drift under the ideal Bayesian model. This property underlies the one-step ¯ πt-weighted variance identity in Proposition 13. The martingale property does not assert ¯ π∞ d

= πX; error control of ¯ πt is provided by Theorems 14 and 17. 

3 Related Work 

This section positions SALE within the literature on surrogate-based Bayesian inference by comparing what existing approaches target, how they select evaluations, and the inferential objects and guarantees they provide. 

3.1 Bayesian Optimisation for Surrogate Inference 

Among pointwise BO methods for surrogate inference, Gutmann and Corander (2016) model discrepancies in summary statistics for synthetic likelihoods, while Li and Zhang (2026) apply a GP-upper confidence bound (GP-UCB) to the log-posterior and use the resulting GP to construct a surrogate posterior. These are useful when the main task is locating a small region of high posterior mass. Standard BO regret results, however, do not by themselves control surrogate-posterior error. 

> 6Sampling-Based Adaptive Active Learning

Kim and Sanz-Alonso (2025) augment GP-UCB with random exploration from a fixed external measure P0. Under noiseless evaluations and a reproducing kernel Hilbert space (RKHS) assumption, the random exploration yields expected-Hellinger convergence rates for πμt . However, P0 does not adapt to the evolving surrogate posterior, and its random evaluations need not focus on inference-relevant regions at finite budgets. A distributional BO formulation is given by Oliveira et al. (2021). Their KL-UCB algorithm draws evaluations from an evolving posterior induced by a UCB surface. Under an RKHS assumption, they establish a finite-budget KL bound and show that the minimum KL error attained among the first T UCB-induced posteriors converges to zero. However, this does not establish convergence of the final surrogate posterior, and the KL-minimising iterate cannot be identified without access to the true posterior. 

3.2 Posterior-Focused Uncertainty Reduction 

Kandasamy et al. (2017) model the log-likelihood with a GP and select queries using point-wise uncertainty in the exponentiated GP surrogate. Wang and Li (2018) use a log-GP entropy criterion, while J¨ arvenp¨ a¨ a et al. (2019, 2021) develop expected-loss and variance-based rules for noisy likelihoods, including batch designs. These methods focus evaluations on regions judged important by the current surrogate posterior and are generally more ef-ficient than reducing uncertainty over the full domain Ω. Theoretically, these methods do not establish convergence of the final surrogate posterior. Llorente et al. (2020) sequentially interpolate the unnormalised posterior and select eval-uations using its current interpolant and a diversity term. However, their theoretical control only applies to the pure space-filling specialisation, not the posterior-weighted acquisition. For Bayesian inverse problems, Villani et al. (2024) use the current surrogate posterior to weight an error measure and greedily refine the surrogate. Their analysis bounds surrogate posterior error through this measure but derives no budget-dependent bound. Lartaud et al. (2026) either restrict evaluations to a Mahalanobis region around the estimated posterior mode or use stepwise uncertainty reduction (SUR). They prove almost-sure convergence of a weighted surrogate uncertainty functional to zero under their SUR design. Both inverse-problem methods emulate the forward map under Gaussian observation noise, and do not directly target a generic log-posterior. Related region-restriction and SUR ideas appear in level-set estimation and general sequential design (Picheny et al., 2010; Bect et al., 2019). 

3.3 Variational Surrogate Inference 

Variational Bayesian Monte Carlo (VBMC; Acerbi, 2018, 2019) combines variational in-ference, GP modelling, and active-sampling Bayesian quadrature to return a variational posterior and model-evidence estimate. The aims of VBMC are closely related to those of SALE. However, its design is built around a variational and quadrature objective, instead of direct approximation of the posterior surface. 

3.4 GP-Emulated Markov Chain Monte Carlo 

J¨ arvenp¨ a¨ a and Corander (2024) place a GP on the noisy log-likelihood and use it to ap-proximate the acceptance decisions along a Metropolis–Hastings (MH) path. They request 

> 7

Li 

Algorithm 1 Stochastic surrogate MCMC (ssMCMC) for approximating ¯ πt                                                            

> 1: Input: Sample size S; number of inner Metropolis–Hastings (MH) steps L; GP posterior law Pt; proposal kernel q(θ′|θ); initial state θ0.2: for s= 1 , . . . , S do
> 3: Draw a GP posterior sample path f(s)
> t∼Pt.4: Set θ(0)
> s←θs−1.5: for ℓ= 1 , . . . , L do
> 6: Propose θ′∼q{· | θ(ℓ−1)
> s}.7: Accept or reject θ′using the MH ratio with target π(· | f(s)
> t). 8: If accepted, set θ(ℓ)
> s←θ′; otherwise, set θ(ℓ)
> s←θ(ℓ−1)
> s.9: end for
> 10: Set θs←θ(L)
> s.11: end for
> 12: Output: Sample Xt={θs}Ss=1 approximating ¯ πt.

additional log-likelihood evaluations until the GP-based accept/reject decision is sufficiently reliable. Their design is therefore transition-centred and returns an approximate MCMC sample; its analysis controls individual accept/reject decisions and the evaluations required to resolve them, rather than the accuracy of the resulting MCMC sample or the resulting surrogate posterior. 

3.5 Position of SALE 

SALE’s main distinction is its adaptive design architecture centred on ¯ πt. At iteration t, ¯ πt

is the common design measure for BO and UR; a state-dependent rule allocates the next evaluation between the branches; and AO or the practical UR criterion selects the query. AO exploits the log-density structure by averaging normalised tempered laws induced by GP paths, while an ideal ¯ πt-weighted UR targets posterior calibration. For AO, the theory provides perturbation-stability and Bayesian regret bounds. For UR, the theory provides an explicit budget-dependent bound on the expected TV error of the final EP under the ideal rule and an explicit expected-TV rate for the implemented UR score. 

4 Methods and Theory 

We now develop the architecture of SALE, beginning with the EP-guided localisation. 

4.1 EP-Guided Localisation 

To exploit the information in ¯ πt, one only requires a sample from it. SALE therefore relies on an approximate sample Xt = {θs}Ss=1 from ¯ πt. We generate this sample using the stochastic surrogate MCMC (ssMCMC) motivated by Roberts et al. (2026b). Algorithm 1 summarises the algorithm; implementation details are in Appendix A.1. The first use of Xt is to construct a BO search region Ω t. In practice, Ω t is obtained from Xt by covariance adjustment and marginal tail trimming; see Online Appendix A. For analysis, let R denote the population analogue of this construction and define Ω ◦ 

> t

:= 

R(¯ πt) ⊆ Ω. BO is conducted over Ω t in the implementation and over Ω ◦ 

> t

in analysis. 

8Sampling-Based Adaptive Active Learning 

To reduce computational overhead, Xt and Ω t are refreshed intermittently according to a dynamic schedule in Section 4.5. Let r(t) ≤ t denote the most recent refresh time. Xr(t)

and Ω r(t) are the current working sample and BO search region. Proposition 5 quantifies how localisation under ¯ πt transfers to the true posterior. It follows immediately from the variational characterisation of TV distance. 

Proposition 5. Let Ω◦ 

> t

⊆ Ω be measurable. Suppose that, for some ηt, δ t ∈ [0 , 1] , ¯πt(Ω ◦ 

> t

) ≥

1 − ηt and TV(¯ πt, π X) ≤ δt. Then πX(Ω ◦ 

> t

) ≥ 1 − ηt − δt.

Thus, a region omitting at most ηt posterior mass under ¯ πt omits at most ηt + δt under the true posterior. This makes the connection between localisation and calibration explicit: the UR branch in Section 4.4 targets the TV discrepancy δt.

4.2 State-Dependent BO–UR Allocation 

At each iteration, SALE decides whether the next evaluation is for localisation or calibration. BO is favoured while local design resolution around the incumbent is low; as it improves, UR is favoured to calibrate the surrogate over regions relevant to inference. We formalise this decision through a latent binary state St ∈ { 0, 1}, where St = 0 denotes unresolved value discovery and St = 1 denotes resolution. Fix an integer k ≥ 1, and suppose that the initial design D0 contains at least k +1 distinct input locations. Define 

θ† 

> t

∈ arg max θi: ( θi,y i)∈D t μt(θi), using deterministic tie-breaking. For a GP posterior draw 

ft ∼ Pt, define f + 

> t

:= ft(θ† 

> t

). Given an improvement threshold εimp > 0, define St = 0 if sup θ∈Ω ft(θ) > f + 

> t

+ εimp , and set St = 1 otherwise. The ideal probability for conducting BO at iteration t is therefore 

p⋆t := P(St = 0 | H t) = Pt



sup 

> θ∈Ω

ft(θ) > f + 

> t

+ εimp 



.

Wilson (2024) uses p⋆t to terminate a BO algorithm; SALE instead uses the same event to motivate BO–UR allocation. Computing p⋆t , however, is impractical: as in Wilson (2024), estimating p⋆t requires repeatedly optimising GP posterior draws. Moreover, εimp is on the scale of f and is therefore problem-dependent. Rather than directly estimate p⋆t , SALE uses a cheap proxy for p⋆t . The idea is to monitor local design resolution around θ† 

> t

, treating further improvement as more likely when the design remains sparse around θ† 

> t

and less likely once its neighbourhood is densely sampled. Let Ht = ∇2μt(θ† 

> t

), Ct = M{− (Ht + H⊤ 

> t

)/2}, where M denotes the numerical positive-definite stabilisation map used in computation. For each ( θi, y i) ∈ D t with θi̸ = θ† 

> t

,define the curvature-adjusted distance dt,i = {(θi − θ† 

> t

)⊤Ct(θi − θ† 

> t

)}1/2. Let ∆ loc  

> t,k

be the mean of the k smallest positive dt,i . The initial design D0 ensures that these k distances are available at every t. We set 

bpt,k = tanh 



C∆loc  

> t,k

/a 



∈ [0 , 1] , (1) with C = log(3) /2, so that bpt,k = 1 /2 when ∆ loc  

> t,k

= a. The default for k is k = 5. The parameter a determines how dense the design must be around θ† 

> t

before SALE transitions 

> 9Li

from BO to UR. The curvature adjustment by Ct makes ∆ loc  

> t,k

, and hence a, dimensionless and gives a a stable interpretation across problems. SALE draws Bt ∼ Bernoulli( bpt,k ), conducting BO when Bt = 1 and UR when Bt = 0. More detailed construction of bpt,k is in Appendix A.2. The connection between p⋆t and 

bpt,k is recorded in Online Appendix B. 

4.3 Annealed Objective for Optimisation 

The BO branch of SALE uses an annealed version of ¯ πt.

Definition 6. For g ∈ C(Ω) and τ ∈ (0 , 1] , define 

πτ (d θ | g) = exp {g(θ)/τ }λ(d θ)/Zτ (g), Zτ (g) := 

Z

> Ω

exp {g(u)/τ }λ(d u).

The annealed objective (AO) induced by a Borel probability law Q on C(Ω) is 

Aτ (Q)( A) := 

Z

> C(Ω)

πτ (A | g) Q(d g), A ∈ B(Ω) .

Given Ht and τ ∈ (0 , 1] , AO at iteration t is πτ,t (d θ) := Aτ (Pt)(d θ) = Eft∼Pt {πτ (d θ | ft)} .

At τ = 1, π1,t = ¯ πt. Decreasing τ concentrates each conditional law πτ (· | ft) around high values of ft. Averaging these laws gives an acquisition distribution that is tilted toward high posterior values. The next active query is drawn as θnt+1 ∼ πτ,t .

As τ ↓ 0, AO approaches TS. TS draws ft ∼ Pt and selects θ⋆ft := arg max θ∈Ω ft(θ). Its selection distribution is therefore Law( θ⋆ft | ft ∼ Pt). The following proposition makes this connection precise; the proof is in Appendix B.2. 

Proposition 7. Under Assumption 1, given Ht, πτ,t =⇒ Law( θ⋆ft | ft ∼ Pt) as τ ↓ 0.

The purpose of AO is not, however, to approximate TS by taking τ arbitrarily close to zero. Its practical advantage arises at positive τ . For fixed τ > 0, the map g 7 → πτ (· | g) is stable under perturbations of g, while the argmax map need not be. To see this, equip C(Ω) with the supremum norm and its Borel σ-algebra B{C(Ω) }.Consider path laws Q1, Q2 on C(Ω) with finite first moment in sup-norm: 

Z

> C(Ω)

∥g∥∞ Qj (d g) < ∞, j = 1 , 2. (2) Let Γ( Q1, Q2) be the set of all probability laws γ on C(Ω) × C(Ω) with marginals Q1 and 

Q2, that is, for A, B ∈ B{C(Ω) }, γ{A × C(Ω) } = Q1(A), and γ{C(Ω) × B} = Q2(B). Write 

W1,∞(Q1, Q2) := inf 

> γ∈Γ( Q1,Q2)

Z

∥g1 − g2∥∞ γ(d g1, dg2).

Assumption 1 (A5) ensures Eq. (2) for Pt, almost surely. The following lemma quantifies the stability of the AO law; the proof is in Appendix B.2. 

> 10 Sampling-Based Adaptive Active Learning

Lemma 8. Let g1, g 2 ∈ C(Ω) , ϱ(g1, g 2) := ∥g1 − g2∥∞, and denote pτ,j the Lebesgue density of πτ (· | gj ), j = 1 , 2. For every θ ∈ Ω and τ > 0,

|log pτ, 1(θ) − log pτ, 2(θ)| ≤ 2ϱ(g1, g 2)/τ, TV {πτ (· | g1), π τ (· | g2)} ≤ tanh {ϱ(g1, g 2)/τ } .

Consequently, for any path laws Q1, Q2 satisfying Eq. (2) ,

TV {A τ (Q1), Aτ (Q2)} ≤ W1,∞(Q1, Q2)/τ. 

In particular, if P′ 

> t

is an alternative law to Pt satisfying Eq. (2) and π′ 

> τ,t

:= Aτ (P′

> t

), then TV( πτ,t , π ′

> τ,t

) ≤ W1,∞(Pt, P′

> t

)/τ. (3) By definition of the TV distance, Eq. (3) shows that AO assigns similar next-query probabilities to a region whenever the surrogate path laws can be coupled so that their paths are uniformly close on average. Thus, positive τ stabilises the query distribution even when the maximiser of an individual surrogate path is sensitive to perturbation. No analogous W1,∞-to-TV continuity holds for TS in general. This is particularly rele-vant when the surrogate is highly uncertain since individual GP posterior paths may contain artificially high, narrow spikes: TS switches to such a spike if it is the pathwise maximiser, while positive-τ AO weights both its height and local volume. If the spike is narrow in dif-ferent directions, its local volume shrinks, allowing AO to tolerate a larger spurious height difference before moving its query mass. Online Appendix C gives an explicit illustration of this mechanism and its possible dependence on dimension. Positive τ therefore trades concentration around the sampled-path maximiser for robust-ness to surrogate-law perturbations. The associated optimisation cost of AO is quantified through Bayesian regret (Russo and Van Roy, 2014) in Theorem 10. 

Definition 9. Let θ⋆ be a measurable maximiser of f over Ω, and let D0 = ∅. After T ≥ 1

evaluations, the simple regret is sT = f (θ⋆) − max 0≤t≤T −1 f (θt+1 ). The cumulative regret is 

RT := PT −1 

> t=0

{f (θ⋆) − f (θt+1 )} . The Bayesian regret is RT := E[RT ], where the expectation is over the GP prior, observation noise, and algorithmic randomness. 

Theorem 10. Under Assumption 1, let D0 = ∅, ς2 ≥ 0, and fix τ ∈ (0 , 1] . Fix any υ2 > 0

satisfying υ2 ≥ ς2. Suppose that θt+1 ∼ πτ,t over Ω. Then, for every T ≥ 1,

RT = O

p T γ T (υ) log T



+ O (∆ τT ) , (4) 

where γT (υ) is the GP maximal information gain with regularisation υ2 (Whitehouse et al., 2023) and ∆τT := T τ 1 + log +(1 /τ ) , where log +(x) = max {log x, 0}.

Remark 11. The first term in Eq. (4) is the standard GP-BO complexity term. When 

ς2 > 0, one may take υ2 = ς2; when ς2 = 0, υ2 is an arbitrary fixed analysis parameter. For fixed dimension and any fixed υ2 > 0, the information-gain orders for SE and Mat´ ern kernels with smoothness ν > 1/2 established by Iwazaki (2025, Theorem 7 and Corollary 8) imply that pT γ T (υ) log T = o(T ). The second term is the optimisation penalty induced by a fixed τ > 0, and it is O(T ). Thus Theorem 10 is not a no-regret claim for fixed τ ; together with Lemma 8, it quantifies the trade-off between AO law stability and optimisation bias. 

> 11

Li 

Algorithm 2 Exchange simulated annealing (ESA) for AO 

Require: Design Dt; search domain Ω; GP posterior law Pt; function proposal kernel Qf (ft, df ′ 

> t

) = qf (f ′ 

> t

|

ft) Pt(d f ′ 

> t

); parameter proposal density qθ (θ′ | θ); cooling schedule {τℓ}Lℓ=1 satisfying 1 = τ1 > · · · >τL = τ > 0; iteration counts Lθ , L u, L f ; levelwise sweep counts {sℓ}Lℓ=1 .1: Initialise θ ∈ Ω. 2: Draw ft ∼ Pt.3: for ℓ = 1 , . . . , L do ▷ Temperature τℓ

4: for j = 1 , . . . , s ℓ do 

5: for jθ = 1 , . . . , L θ do ▷ Update θ conditional on ft

6: Propose θ′ ∼ qθ (· | θ). 7: Set 

αθ = min 



1, exp {ft(θ′)/τ ℓ}qθ (θ | θ′)exp {ft(θ)/τ ℓ}qθ (θ′ | θ)



.

8: With probability αθ , set θ ← θ′.9: end for 

10: for jf = 1 , . . . , L f do ▷ Exchange update of ft

11: Propose f ′ 

> t

∼ Qf (ft, ·). 12: Approximately draw u ∼ πτℓ (· | f ′ 

> t

) ∝ exp {f ′ 

> t

(·)/τ ℓ} using Lu MH updates. 13: Set 

αf = min 



1, qf (ft | f ′ 

> t

) exp {f ′ 

> t

(θ)/τ ℓ} exp {ft(u)/τ ℓ}

qf (f ′ 

> t

| ft) exp {ft(θ)/τ ℓ} exp {f ′ 

> t

(u)/τ ℓ}



.

14: With probability αf , set ft ← f ′ 

> t

.15: end for 

16: end for 

17: end for 

18: Set ( eθt, eft) ← (θ, f t). 19: return ( eθt, eft). 

Theorem 10 concerns exact AO sampling over the fixed domain Ω. In SALE, AO is instead restricted to the current BO search region Ω r(t). Unless otherwise stated, we continue to write πτ,t for this restricted acquisition law. Sampling from πτ,t is difficult when τ is small because the target can be highly con-centrated and multimodal. We thus introduce exchange simulated annealing (ESA; Algo-rithm 2), based on simulated annealing (Delahaye et al., 2019). ESA follows a temperature schedule 1 = τ1 > · · · > τ L = τ > 0 and carries its state between successive temperatures. At a fixed τℓ, the ideal ESA kernel targets the joint law Π τℓ,t (d θ, dft) = πτℓ (d θ |

ft) Pt(d ft), whose θ-marginal is πτℓ,t . ESA alternates between an MH update of θ con-ditional on ft and an exchange update of ft. The exchange step introduces an auxiliary draw to cancel the intractable normalising constant Zτℓ (ft) (Murray et al., 2006). With an exact auxiliary draw, the exchange kernel leaves Π τℓ,t invariant. The implemented algo-rithm approximates the auxiliary draw using MH updates and represents GP paths using the random Fourier features (RFF) and Matheron’s representation (Rahimi and Recht, 2007; Wilson et al., 2021). Core implementation details are in Appendix A.3; the proposal kernels, temperature schedule, and numerical settings are in Online Appendix D. Online Appendix H analyses the levelwise convergence for the RFF–Matheron representation. 

12 Sampling-Based Adaptive Active Learning 

4.4 EP-Weighted Calibration 

The UR branch reduces GP uncertainty in regions that are plausible under ¯ πt.

Definition 12. Define the ¯πt-weighted integrated mean squared prediction error by Vt := 

Eu∼¯πt {σ2 

> t

(u)}. The one-step ¯πt-weighted integrated variance-reduction is 

∆t(θ) := 

Z

> Ω

κt(u, θ)2

σ2 

> t

(θ) + ς2 ¯πt(d u), θ ∈ Ω.

When both ς2 = 0 and σ2 

> t

(θ) = 0 , the integrand is defined to be zero. 

Proposition 13. Under Assumption 1, let V (θ) 

> t+1

denote Vt+1 obtained when the next query is fixed at θ ∈ Ω without observing its response. Then E{V (θ) 

> t+1

| H t} = Vt −∆t(θ). Moreover, 

E{TV(¯ πt, π X) | H t} ≤ (Vt/2) 1/2 , and E{TV(¯ πt, π X)} ≤ { E[Vt]/2}1/2 .

The first identity remains exact even though the weighting law changes from ¯ πt to ¯ πt+1 :this follows from the martingale property in Proposition 4 and the response-independent GP variance update. The two claims are proved in Appendix B.4. The ideal UR rule selects θt+1 = arg max θ∈Ω ∆t(θ). Recall Bt from Section 4.2, and let 

N UR  

> T

= PT −1 

> t=0

(1 −Bt) be the number of UR evaluations in T iterations. Theorem 14 achieves finite-budget expected-TV control indexed by realised N UR  

> T

; its proof is in Appendix B.4. 

Theorem 14. Under Assumption 1, suppose that κ is either a Mat´ ern-ν kernel, ν > 0,or an SE kernel, satisfying, respectively, condition (i) or (ii) of Assumption 20. The UR branch selects θt+1 = arg max θ∈Ω ∆t(θ). At non-UR iterations, the query may be selected by any rule satisfying Assumption 1 (A3) . For T ≥ 1 and c > 0, define 

O+(T ) := 

((T + 1) −ν/ (2 ν+d), for a Mat´ ern-ν kernel ,

{1 + log( T + 1) }d/ 2(T + 1) −1/2, for an SE kernel ,

O0 

> c

(T ) := 

((T + 1) −ν/d , for a Mat´ ern-ν kernel ,

exp −c(T + 1) 1/(d+1) , for an SE kernel .

Then, for any fixed ς2 ≥ 0 and every δ, q ∈ (0 , 1) , there exists Cδ,q < ∞, independent of T ,such that 

E{TV(¯ πT , π X)} ≤ 

(Cδ,q O+(T ) + Cδ,q P N UR  

> T

< qT 1/2 + δ, Cδ,q O0 

> cδ,q

(T ) + Cδ,q P N UR  

> T

< qT 1/2 + δ, if ς2 = 0 . (5) 

If, in addition, lim inf T →∞ N UR  

> T

/T > 0 a.s., then, for every δ ∈ (0 , 1) , there exist Cδ < ∞,

cδ > 0, and Tδ < ∞, independent of T , such that, for every T ≥ Tδ,

E{TV(¯ πT , π X)} ≤ 

(

CδO+(T ) + δ, CδO0 

> cδ

(T ) + δ, if ς2 = 0 . (6) 

13 Li 

Corollary 15. Assume the setup and ideal UR rule of Theorem 14. Suppose additionally that Assumption 21 holds, that D0 contains at least k + 1 distinct input locations, and that 

bpt,k is computed as in Section 4.2. Then there exists a random variable ρ⋆ < 1 such that 

sup t≥0 bpt,k ≤ ρ⋆ and lim inf T →∞ N UR  

> T

/T ≥ 1−ρ⋆ > 0 a.s. Consequently, for every δ ∈ (0 , 1) ,there exists Tδ < ∞ such that Eq. (6) holds for all T ≥ Tδ.

Remark 16. On bounded domains with the standard boundary regularity, Santin and Schaback (2016, Theorem 15) gives λλ 

> 0,j

≲ j−(1+2 ν/d ) for a Mat´ ern-ν kernel and λλ 

> 0,j

≲

exp( −cj 1/d ) for an SE kernel, where λλ 

> 0,j

is the eigenvalue of the prior covariance operator in Assumption 20. Thus Assumption 20 (i) holds for Mat´ ern-ν kernels, and Assumption 20 (ii) holds for SE kernels. The Mat´ ern smoothness must, in addition, be large enough for Assumption 1, that is, ν > 1. The additional Assumption 21 and initial-design conditions in Corollary 15 are used only to verify N UR  

> T

; they are not needed for Eq. (5). If N UR  

> T

≡ T ,Eq. (6) hold for every T ≥ 1. Proposition 13 connects sequential design to posterior accuracy: ∆ t controls reduction in Vt, and Vt controls the TV error. The spectral-decay condition then converts repeated ∆t-maximisation into a budget-dependent bound on the final EP’s expected TV error. To our knowledge, Theorem 14 gives the first explicit budget-dependent bound on the expected TV error of the final surrogate posterior under sequential posterior-weighted UR. The ideal ∆ t rule, however, is not used directly in SALE. As mentioned in Section 1, before localisation, ∆ t inherits the uncertainty in ¯ πt and can spend evaluations in inference-irrelevant regions (Seo et al., 2000; Gramacy, 2020). Moreover, evaluating ∆ t requires repeated integration against ¯ πt, covariance calculations, and continuous optimisation over Ω. SALE therefore uses a state-dependent computational proxy motivated by ∆ t.Let Xt = {θs}Ss=1 be an approximate sample from ¯ πt. The Monte Carlo version of ∆ t is 

b∆MC  

> t

(θ) = 

PSs=1 κt(θs, θ)2

S{σ2 

> t

(θ) + ς2} .

When localisation remains unresolved, SALE tempers ¯ πt using St from Section 4.2. For p ∈ [0 , 1], let ρ(p) 

> t

(d θ) ∝ exp {pμ t(θ)}¯πt(d θ). The state-specific laws are ρt, 1(d θ) := 

ρ(0)  

> t

(d θ) = ¯ πt(d θ), and ρt, 0(d θ) := ρ(1)  

> t

(d θ) ∝ exp {μt(θ)}¯πt(d θ). Thus, St = 1 gives pure ¯πt-weighted calibration, while St = 0 tilts the weighting measure toward high GP posterior means. Given p⋆t = P(St = 0 | H t), the Bayes action under posterior expected reverse-KL loss is ρ(p⋆t ) 

> t

; see Appendix B.7 for derivation. Replacing p⋆t by the allocation proxy bpt,k gives the tempered Monte Carlo criterion 

b∆MC  

> t, bpt,k

(θ) = 

PSs=1 w(bpt,k ) 

> t,s

κt(θs, θ)2

σ2 

> t

(θ) + ς2 , w(bpt,k ) 

> t,s

∝ exp {bpt,k μt(θs)},

> S

X

> s=1

w(bpt,k ) 

> t,s

= 1 . (7) 

b∆MC  

> t, bpt,k

is a first-principles bridge from ∆ t to practical implementation. It identifies two ingredients that should be retained in practice: state-tempered posterior weights and GP uncertainty reduction. We convert these ingredients into a cheaper proxy in three steps. First, UR candidates are restricted to θ ∈ U t := unique( Xt). This removes the need for continuous optimisation, while approximately retaining the ¯ πt-weighting in ∆ t.

14 Sampling-Based Adaptive Active Learning 

Second, SALE reuses the most recently refreshed sample Xr(t), where t − r(t) ≤ Tmax ,

for a fixed Tmax , rather than regenerating Xt at every active time t.Third, evaluating all cross-covariance terms in Eq. (7) remains expensive. We therefore replace the full covariance calculation by a local ranking proxy. On the log scale, w(bpt,k )

> t,s

contributes bpt,k μt(θ), while log σt(θ) supplies a local GP-uncertainty ranking. This gives UR t(θ) := bpt,k μt(θ) + log σt(θ). (8) The implemented UR rule is therefore θnt+1 = arg max θ∈U r(t) UR t(θ), Ur(t) = unique {X r(t)}.

Ur(t) restricts UR to the current ¯ πt sample, log σt(θ) ranks local uncertainty, and bpt,k μt(θ)provides a value correction while localisation remains unresolved. Theorem 17 gives a budget-dependent expected-TV rate for the implemented UR score in Eq. (8); the proof is in Appendix B.6. Let r0 = 0 < r1 < r2 < · · · be the refresh times, so that r(t) = max {ri : ri ≤ t}. At ri, Xri = {θri,s }Ss=1 is a sample such that θri,s | H ri ∼ ¯πri ,1 ≤ s ≤ S. Xri is held fixed until the next refresh. Define 

Jt ∈ arg max 

> 1≤s≤S

bpt,k μt(θr(t),s ) + log σt(θr(t),s ) ,

using deterministic tie-breaking and setting log 0 = −∞ . The UR query is θt+1 = θr(t),J t .

Theorem 17. Suppose Assumption 1 holds, except that its fresh-randomness condition (A3) 

is replaced by the persistent-sample condition in Assumption 44, and suppose Assumption 21 holds. Assume that D0 contains at least k + 1 distinct input locations. Fix S ≥ 1 and υ2 > 0

with υ2 ≥ ς2 ≥ 0. Suppose that Pr( Bt = 1 | H t, Xr(t)) = bpt,k and that the UR branch uses the fixed-sample rule above. Then, as T → ∞ ,

E{TV(¯ πT , π X)} =



O



T − ν 

> 2ν+d

{log( T + 1) } 4ν+d

> 2(2 ν+d)



, for a Mat´ ern kernel with ν > 1/2,

O



T −1/2{log( T + 1) } d+1 2



, for an SE kernel .

Remark 18. The Mat´ ern and SE rates follow from the information-gain bounds from Iwazaki (2025, Theorem 7 and Corollary 8; see Appendix B.6). Relative to the ideal-UR benchmark O+(T ) in Theorem 14, these rates have the same polynomial powers of T , but incur logarithmic losses. The Mat´ ern rate acquires the displayed logarithmic factor, while the SE rate has one additional factor plog( T + 1). These losses arise from the maximal-information-gain bound used in the proof. The allocation argument only requires bpt,k to remain uniformly below one. 

4.5 Integrated Workflow of SALE 

Algorithm 3 summarises the implemented SALE workflow. SALE refreshes Xt and Ω t

intermittently, jointly with GP hyperparameter updates. The refresh schedule is controlled by a moving diagnostic of one-step-ahead GP predictive calibration. A refresh is allowed after Tmin iterations since the previous refresh and is forced after Tmax iterations, ensuring a bounded refresh lag. A practical convergence assessment is conducted at refresh times, when the current and previous ssMCMC samples can be compared. SALE stops when bpt,k < p BO and the 

15 Li 

Algorithm 3 Sampling-based adaptive active learning (SALE) 

Require: Objective f ; search domain Ω; nearest-neighbour count k ≥ 1; initial design size n0 ≥ k + 1; sequential evaluation budget T ≥ 1; ssMCMC sample size S; BO stopping threshold pBO ; PSRF tolerance 

εR; refresh limits Tmin < T max .1: Generate a distinct initial space-filling design {θi}n0 

> i=1

⊂ Ω. 2: Evaluate yi = f (θi) + εi, i ∈ [n0], and set D0 = {(θi, y i)}n0

> i=1

.

3: Estimate the GP hyperparameters and fit the GP posterior P0.4: Compute the allocation proxy bp0,k using k-NN curvature adjusted distance. 5: Generate X0 using Algorithm 1, approximately targeting ¯ π0, and construct Ω 0.6: Initialise the refresh interval, set r(0) ← 0, and set bR ← ∞ .7: for t = 0 , . . . , T − 1 do 

8: Set the working sample and BO search region to X wt ← X r(t), Ω wt ← Ωr(t).

9: Draw Bt ∼ Bernoulli( bpt,k ). 10: if Bt = 1 then 

11: Generate θnt+1 from the AO target restricted to Ω wt using ESA; see Algorithm 2. 12: else 

13: Set Uwt ← unique( X wt ). Select θnt+1 ∈ arg max θ∈U wt bpt,k μt(θ) + log σt(θ).

14: end if 

15: Evaluate ynt+1 = f (θnt+1 ) + εnt+1 and set Dt+1 = Dt ∪ { (θnt+1 , y nt+1 )}.

16: Update the GP posterior Pt+1 conditional on Dt+1 .17: Update the refresh interval using the moving calibration diagnostic. 18: if a refresh is triggered then 

19: Set r(t + 1) ← t + 1. 20: Re-estimate the GP hyperparameters and refit Pt+1 .21: Generate Xt+1 using Algorithm 1, approximately targeting ¯ πt+1 .22: Construct Ω t+1 from Xt+1 .23: Recompute the allocation proxy bpt+1 ,k using k-NN curvature adjusted distance. 24: Compute the maximum coordinatewise PSRF bR between Xt+1 and X wt .25: if bpt+1 ,k < p BO and bR < 1 + εR then 

26: break 

27: end if 

28: else 

29: Set r(t + 1) ← r(t). 30: Recompute the allocation proxy bpt+1 ,k .31: end if 

32: end for 

33: return The final design, GP posterior, ssMCMC sample, and BO search region. 

maximum coordinatewise potential scale reduction factor (PSRF; Vehtari et al., 2021), bR,satisfies bR < 1 + εR. pBO and εR are user-defined. The first condition indicates that further BO is no longer favoured; the second indicates that the ¯ πt sample has stabilised. Otherwise, SALE continues until the evaluation budget is exhausted. GP hyperparameter fitting, the predictive-calibration refresh diagnostic, the stopping-rule computation, and all numerical constants are specified in Online Appendix E. 

5 Numerical Experiments 

We use analytic benchmarks and simulated-likelihood examples as controlled settings for evaluating finite-budget surrogate posterior approximation. The examples cover several common posterior geometries and admit reference posteriors. 

16 Sampling-Based Adaptive Active Learning 

As mentioned in Section 1, we assess robustness by TV reduction across problems, run-to-run variability, and the frequency of severe failures under common settings and fixed evaluation budgets. For all assessments in this section, we exclude the initial design D0.

5.1 Analytic Benchmarks 

We consider four analytic log-unnormalised posterior targets on θ := ( θ1, . . . , θ d) ∈ Ω = [−5, 5] d, d ∈ { 4, 6, 8}, with constants omitted. For the Quartic-tail Gaussian and Bimodal Mixture targets, define σj = 0 .30 × 6(j−1) /(d−1) , Dσ = diag( σ1, . . . , σ d), [ R]ij = 0 .5|i−j|.

• Quartic-tail Gaussian. Let z = D−1 

> σ

θ and f (θ) = − 12 z⊤R−1z − λtail 

Pdj=1 sj (z)2,

where sj (z) = τtail softplus {(z2 

> j

− r2)/τ tail }, r = 2, τtail = 0 .35, λtail = 0 .10 , and softplus( x) = log {1 + exp( x)}. The target is approximately Gaussian if the scaled coordinates remain within |zj |≲ r. Beyond this region, the s2 

> j

terms produce rapidly decaying quartic tails. This mimics problems that have scale-separated likelihoods. 

• Neal’s Funnel. The target is f (θ) = −θ21/2 − (d − 1) θ1/2 − exp( −θ1) Pdj=2 θ2 

> j

/2. This is Neal’s funnel truncated to Ω, with θ1 controlling the conditional scale of θ2, . . . , θ d.

• Rosenbrock. The target is f (θ) = − Pd−1 

> j=1

{(θj − 1) 2/100 + ( θ2 

> j

− θj+1 )2}.

• Bimodal Mixture. The target is f (θ) = log [exp {ℓ1(θ)} + exp {ℓ2(θ)}] , where ℓ1 is the Quartic-tail Gaussian log target above and ℓ2(θ) = ηd − z⊤ 

> 1

R−1z1/2, z1 =(0 .85 Dσ)−1(θ − μd), with μd = p8/d 1d, ηd = −0.5 + (3 − d) log(0 .85) . This choice gives approximate component masses of 72% for exp {ℓ1} and 28% for exp {ℓ2} across the dimensions. 

5.1.1 Optimisation Performance 

We first compare AO, TS, and UCB as BO strategies within SALE, keeping all other components and tuning choices fixed. Average BO-branch regret is evaluated on the three unimodal targets. The Bimodal Mixture is instead evaluated by TV distance. Recall Bt from Section 4.2, and let N BO  

> T

= PT −1 

> t=0

Bt be the number of BO evaluations. Excluding the initial design, the average BO-branch regret is ¯RBO  

> T

:= 1

N BO  

> TT−1

X

> t=0

Bt {f (θ⋆) − f (θt+1 )} , N BO  

> T

> 0.

This measures the mean optimisation loss over the BO evaluations by budget T . Normalising by N BO  

> T

removes the effect of differences in the number of BO evaluations. Computational details for the three strategies are given in Online Appendix F. For each target-dimension combination, we use 50 independent replicates. Within each dimension and replicate, the same 20-point Latin hypercube initial design is used across methods and targets. Figure 1 gives the median and the interquartile range (IQR) of ¯RBO  

> T

. It shows a con-sistent ordering across the nine target-dimension settings. After the initial part of the trajectories, AO maintains the lowest median average regret, TS is intermediate, and UCB has the highest regret. At the final budget, AO reduces median average regret by 15%–30% relative to TS and by 40%–53% relative to UCB across all settings. The largest reduction 

> 17

Li 4D Rosenbrock 6D Rosenbrock 8D Rosenbrock                                                   

> 4D Funnel 6D Funnel 8D Funnel
> 4D Quartic 6D Quartic 8D Quartic
> 0100 200 300 400 0100 200 300 400 500 0200 400 600
> 0100 200 300 400 0100 200 300 400 500 0200 400 600
> 0100 200 300 400 0100 200 300 400 500 0200 400 600
> 300
> 1,000
> 3,000
> 30
> 100
> 300
> 300
> 500
> 1,000
> 100
> 300
> 1,000
> 3,000
> 30
> 100
> 300
> 100
> 300
> 1,000
> 100
> 300
> 1,000
> 10
> 30
> 100
> 300
> 10
> 100
> 1,000
> Evaluation budget T
> RBO
> T
> Method AO TS UCB

Figure 1: Average BO-branch regret ¯RBO  

> T

for AO, TS, and UCB. Curves show medians over 50 runs at each evaluation budget; shading shows interquartile ranges. Lower is better. 

relative to TS occurs for the 6D Funnel, while the biggest advantage over UCB is for Rosen-brock. AO also has a lower final upper quartile than both alternatives in every setting, so its advantage extends beyond median performance. These results indicate that AO produces queries with smaller objective gaps on average than UCB and TS on the considered targets. Corresponding simple-regret trajectories are reported in Online Appendix G. AO and TS attain near identical simple regret, indicating that both identify competitive high-value points at similar budgets, while AO makes more consistent BO queries along the full trajectory. 

5.1.2 Inference Performance 

We compare SALE with four full-domain GP-AL baselines under a common experimen-tal framework: GP-UCB, following Gutmann and Corander (2016); Li and Zhang (2026); GP-UCB+, following Kim and Sanz-Alonso (2025); log-GP entropy, following Wang and Li (2018); and log-GP variance, following Kandasamy et al. (2017). We implement these sequential design rules rather than exact reproductions of every paper-specific implemen-

18 Sampling-Based Adaptive Active Learning 4D Bimodal (̂ fBO = 0 .24 +0 .02  

> −0.02 )

6D Bimodal (̂ fBO = 0 .31 +0 .01  

> −0.03 )

8D Bimodal (̂ fBO = 0 .32 +0 .06 

> −0.03 )

4D Rosenbrock (̂ fBO = 0 .32 +0 .02  

> −0.01 )

6D Rosenbrock (̂ fBO = 0 .60 +0 .01  

> −0.03 )

8D Rosenbrock (̂ fBO = 0 .89 +0 .02 

> −0.01 )

4D Funnel (̂ fBO = 0 .37 +0 .02  

> −0.05 )

6D Funnel (̂ fBO = 0 .51 +0 .08  

> −0.03 )

8D Funnel (̂ fBO = 0 .64 +0 .05 

> −0.05 )

4D Quartic (̂ fBO = 0 .30 +0 .01  

> −0.01 )

6D Quartic (̂ fBO = 0 .48 +0 .02  

> −0.02 )

8D Quartic (̂fBO = 0 .65 +0 .03 

> −0.01 )

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0.3 

0.5 

1.0 

0.1 

0.3 

1.0 

0.1 

0.3 

1.0 

0.1 

0.3 

1.0 

0.1 

0.3 

1.0 

0.03 

0.10 

0.30 

1.00 

0.01 

0.10 

1.00 

0.1 

0.3 

1.0 

0.03 

0.10 

0.30 

1.00 

0.01 

0.10 

1.00 

0.001 

0.010 

0.100 

1.000 

0.1 

0.3 

1.0 

Evaluation budget T̂

> TV( πμT , π X)

Method SALE GP-UCB GP-UCB+ Log-GP entropy Log-GP variance 

Figure 2: TV estimates dTV( πμT , π X) versus budget T for SALE and external baselines across benchmarks. Curves show medians over 50 runs; shading shows IQR. Lower is better. 

bfBO gives the median and IQR of BO fractions for SALE over the full budget. For GP-UCB+ on the 6D bimodal target, summaries are computed over 49 runs since one run had numerical failure. 

tation. These baselines are considered because they permit the most direct and controlled comparisons with SALE to ensure a fair assessment. All methods use the same GP surrogate class, initial design, GP-fitting schedule, opti-misation protocol, and evaluation budget. Computational details are given in Online Ap-pendix F; internal comparisons of SALE using AO, TS, and UCB are in Online Appendix G. Although the primary computational budget is the number of objective evaluations, we also report representative wall-clock timing audits in Online Appendix G. At each evaluation budget T , we assess inference quality using TV( πμT , π X). Although the theoretical estimator in Theorem 14 is ¯ πT , accurately estimating TV(¯ πT , π X) would require repeated normalisation and averaging over many GP sample paths at every budget 

19 Li 

and replicate. We therefore use πμT as a cheap deterministic surrogate-posterior summary for all methods. The estimator dTV( πμT , π X) for TV( πμT , π X) is described in Appendix A.4. Figure 2 provides the median and IQR of TV trajectories for all benchmark targets. No method dominates every target in terms of median performance. SALE’s main empirical advantage is robustness: it reduces TV consistently across settings and avoids several severe failures seen under the considered external baselines. On the Quartic target, log-GP entropy has the lowest final median TV in all three dimensions. On Rosenbrock, SALE is strongest in 4D, while log-GP entropy is strongest in 6D and 8D. The highly exploitative behaviour of log-GP entropy is effective for these regular unimodal targets. The remaining external baselines become markedly less effective as the dimension increases. SALE has the lowest median TV on the Funnel target in every dimension, with the clearest separation in 6D and 8D. The anisotropic funnel geometry is difficult to recover under log-GP entropy: the advantage of exploitation in log-GP entropy on the regular targets does not carry over. On the Bimodal Mixture, SALE has the lowest median TV in every dimension. Log-GP entropy occasionally reaches low TV in 6D and 8D but has a much wider IQR, indicating substantial run-to-run variation. The other baselines generally plateau at higher TV. These results are consistent with SALE recovering and calibrating modal mass more reliably. 

5.2 Simulated Examples 

We consider two simulated examples in which the likelihood evaluation requires nontrivial numerical computation. The first is a spatial Mat´ ern Gaussian random-field (GRF) model: 

y(si) | Z, δ ind 

∼ N { Z(si), δ 2}, si ∈ [0 , 1] 2, i ∈ [n],Z(·) ∼ GP { μ, C(·, ·)} ,

C(s, s ′) = κM (s, s ′; σf , ℓ, ν ), s, s ′ ∈ [0 , 1] 2.κM (s, s ′; σf , ℓ, ν ) is the stationary Mat´ ern kernel with signal variance σ2 

> f

, length-scale ℓ, and smoothness parameter ν. n = 200 noisy observations {yi}200  

> i=1

are generated, and we infer 

θ = ( μ, δ, σ f , ℓ, ν ) ∈ R5.The second example is a Lotka–Volterra (LV) inverse problem. Let x(t) > 0 and y(t) > 0denote the prey and predator populations, respectively. The observations are generated by 

Xi ∼ N log x(ti), σ 2 , Yi ∼ N log y(ti), σ 2 ,

dx(t)dt = αx (t) − βx (t)y(t), dy(t)dt = δx (t)y(t) − γy (t),

with x(0) = x0 and y(0) = y0. Observations are simulated for both species at n = 20 time points in [0 .5, 20]. The parameters of interest are θ = ( α, β, γ, δ, x 0, y 0, σ ) ∈ R7.Data-generating values, priors, reparameterisations, and search domains are given in Online Appendix F. Both examples are kept sufficiently small to obtain reference posterior samples by MCMC. SALE and the external baselines use the same experimental protocol as in the analytic benchmarks, with 30 independent replicates per problem and paired initial designs across methods within each replicate; see Online Appendix F. 

> 20

Sampling-Based Adaptive Active Learning GRF-Mat´ ern (̂ fBO = 0 .69 +0 .07                  

> −0.09 )Lotka-Volterra (̂fBO = 0 .75 +0 .12
> −0.04 )
> 0200 400 600 0250 500 750
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> Evaluation budget T̂
> TV( πμT , π X)
> Method SALE GP-UCB GP-UCB+ Log-GP entropy Log-GP variance

Figure 3: TV estimates dTV( πμT , π X) versus budget T for SALE and external baselines across simulated problems. Curves show medians over 30 runs; shading shows IQR. Lower is better. bfBO gives the median and IQR of BO fractions for SALE over the full budget. 

Figure 3 shows that SALE and log-GP entropy are the only methods with substantial median TV reduction under the available budgets. Their final median performance is similar on the GRF–Mat´ ern example, while log-GP entropy has a lower final median on the LV example. SALE, however, has a considerably narrower IQR in both examples. At the final LV budget T = 900, 8 of the 30 log-GP entropy runs have dTV( πμT , π X) > 0.9, compared with 2 of the 30 SALE runs. Internal comparisons among the SALE BO strategies are reported in Online Appendix G. Both examples exhibit strong scale separation: the log-posterior is relatively regular within its main bulk regions but decreases sharply outside them. Log-GP entropy can therefore perform very well once its evaluations concentrate in the appropriate region, as reflected by its competitive lower quartile, but its wide IQR shows that this behaviour is not reliable across initial designs. SALE’s narrower IQR is consistent with its adaptive localisation and ¯ πt-guided restriction of subsequent learning. 

5.3 Ablation Study 

We conduct a targeted ablation study on four representative targets: the 6D Funnel, 8D Bimodal, GRF–Mat´ ern, and LV examples. The study examines the ¯ πt-guided restriction of BO and UR, BO–UR allocation, AO in the BO branch, and the practical UR proxy. We also compare the proxy with the Monte Carlo criterion b∆MC  

> t

.The experimental protocol is otherwise unchanged. Figure 4 reports the full TV trajec-tories. SALE denotes the complete workflow. SALE-TS replaces AO with TS in the BO branch. SALE- b∆MC  

> t

replaces the practical UR proxy by b∆MC  

> t

, with candidates restricted to Xr(t). BO-Only disables the UR branch, whereas UR-Only- b∆MC  

> t

disables the BO branch and applies b∆MC  

> t

over Xr(t). No Restriction keeps the BO search region fixed at Ω and uses a uniform UR candidate set over Ω. 

21 Li GRF-Mat´ ern Lotka-Volterra                         

> 6D Funnel 8D Bimodal
> 0200 400 600 0250 500 750
> 0100 200 300 400 500 0200 400 600
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> Evaluation budget T̂
> TV( πμT , π X)
> Method SALE SALE-TS SALE-̂∆MC
> t
> BO-Only UR-Only-̂∆MC
> tNo Restriction

Figure 4: TV estimates dTV( πμT , π X) versus budget T for SALE and its component ablations. Curves show medians; shading shows IQRs. The 6D Funnel and 8D Bimodal targets use 50 runs, while the GRF–Mat´ ern and LV examples use 30 runs. Lower is better. For No Restriction on GRF–Mat´ ern, summaries use 29 runs as one run had a numerical failure. 

The largest overall deterioration occurs under No Restriction, which has much higher final TV on every target and one numerical failure on the GRF–Mat´ ern example. This ablation jointly removes the ¯ πt-guided BO restriction and UR candidate construction. It therefore supports the importance of their combined localisation role. BO-Only performs considerably worse than SALE on every target, supporting the need for UR calibration after localisation. UR-Only- b∆MC  

> t

eventually achieves competitive final TV on the Funnel and Bimodal targets, but improves later than SALE in Figure 4. On GRF–Mat´ ern, it declines only near the final budget and remains highly variable, while on LV it does not improve. On those harder examples, the contrast between SALE- b∆MC  

> t

and UR-Only- b∆MC  

> t

supports the need for a BO-based localisation phase. Replacing AO by TS does not uniformly worsen the final median, but SALE-TS has a larger upper quartile on all four targets, with the largest differences on the GRF–Mat´ ern and LV examples. Its trajectories in Figure 4 also show later and more variable TV reduction under TS. This complements the optimisation comparison in Section 5.1.1. Finally, SALE- b∆MC  

> t

has a higher median and upper quartile than SALE on all four targets. Thus the practical proxy performs better under the tested budgets even though 

22 Sampling-Based Adaptive Active Learning 

b∆MC  

> t

more directly approximates ∆ t. One plausible explanation is that the state-dependent value term bpt,k μt(θ) remains useful while ¯ πt is not yet a reliable weighting measure. 

6 Applications 

We apply SALE to two expensive-likelihood problems from econometrics and astrophysics. Their computational bottlenecks are distinct: solving a Bellman fixed point and marginal-ising a conditional latent Gaussian model. For each application, we report one SALE run from a 20-point Latin hypercube initial design using the common default settings. 

6.1 Dynamic Discrete-Choice Model for Bus-Engine Replacement 

Dynamic discrete-choice (DDC) models represent sequential decisions as Markov decision processes and characterise optimal decisions through Bellman equations (Rust, 1994; Aguir-regabiria and Mira, 2010). Under full-solution likelihoods, the Bellman fixed point must be computed via dynamic programming (DP) to evaluate the likelihood, which is expensive. Existing Bayesian DDC methods reduce the cost per MCMC iteration by reusing or approximating DP solutions (Imai et al., 2009; Norets, 2009, 2012). More recent work develops specialised Hamiltonian and reversible-jump MCMC (Norets and Shimizu, 2024), or accelerates the DP solver itself (Chen, 2025). We use SALE as a complementary strategy by reducing the number of full likelihood evaluations. Every likelihood value is obtained by solving the original Bellman equations, while approximation is confined to the GP surrogate. The same SALE configuration used in the preceding experiments is applied without modifying the DP solver. We consider the bus-engine replacement problem of Rust (1987), in which a maintenance manager decides each month whether to replace a bus engine. The data contain 8 ,156 monthly transitions from 104 buses, including 60 replacement decisions. For bus i, let 

h ∈ [mi] index its observed transitions, with P 

> i

mi = 8 ,156. Let xih ∈ [K − 1] 0 denote mileage since the most recent replacement, discretised into 500-mile intervals, and define 

zxih = xih /(K − 1). We use K = 800 mileage states. We consider a seven-parameter extension of the Rust model, θ = ( RC , c 1, c 2, c 3, μ, r, ω ).

Here RC is the replacement cost and C(x) = c1zx + c2z2 

> x

+ c3z3 

> x

is the cost of keeping the engine. Let Jih denote the number of mileage-state intervals advanced between consecutive observations. We model Jih using a zero-inflated negative-binomial (NB) distribution with mean μ, size r, and zero-inflation probability ω, allowing both excess unchanged mileage states and overdispersion. The detailed model construction, likelihood function, priors, parameter transformations, and search domains are given in Online Appendix F. Evaluating the full Bellman likelihood requires solving an 800-state Bellman system. One evaluation takes approximately 0 .26 seconds on a PC with an AMD Ryzen 7 9700X 3.8 GHz CPU. We ran SALE for 500 sequential evaluations, for 520 Bellman solves in total. The com-plete run took 15 minutes. At the final budget, the SALE diagnostics were bpT,k = 0 .13 and 

bR = 1 .002. After discarding the first 2 ,000 of 10 ,000 final ssMCMC draws, the parameter-wise effective sample sizes (ESS) ranged from 966 to 2 ,826. The retained ssMCMC draws approximate ¯ πT and are used for the SALE marginals and policy summaries below. To 

> 23

Li 0.0   

> 0.1
> 0.2
> 0.3
> 0.4
> 810 12

RC   

> 0
> 2
> 4
> 6
> 0.0 0.1 0.2 0.3

c1   

> 0
> 3
> 6
> 9
> 0.0 0.1 0.2 0.3

c2  

> 0
> 5
> 10
> 15
> 0.0 0.1 0.2

c3  

> 0
> 4
> 8
> 12
> 6.8 6.9 7.0

μ  

> 0.00
> 0.02
> 0.04
> 70 95 120

r  

> 0
> 50
> 100
> 150
> .030 .0375 .045

ω

> Density

SALE MCMC 

Figure 5: Posterior marginals for the bus-engine replacement model from SALE and refer-ence MCMC. 0.00 

0.05 

0.10 

0.15 

0 1 2 3 4

Mileage ( ×10 5) since replacement 

> Replacement probability

SALE MCMC 

Transitions per bin 100 200 300 400 500 

Figure 6: Fitted bus-engine replacement probability versus bus mileage from SALE and reference MCMC. Curves are the posterior means, while the shaded regions are 50% and 90% credible regions. match the numerical experiments, the reported TV diagnostic is instead computed from the plug-in posterior πμT .For reference, we ran a 40 ,000-iteration random-walk Metropolis chain using the full likelihood. An adaptive Metropolis chain initialised from prior information mixed poorly. We therefore initialised the reference chain at the SALE posterior mode and scaled its 

24 Sampling-Based Adaptive Active Learning 

proposal using the final SALE posterior covariance. The reference run required 40 ,000 Bellman solves, took over three hours, and had a parameter-wise ESS range of 631 to 1,122. Thus, beyond producing a surrogate posterior, SALE supplied the localisation and covariance information needed to make direct full-likelihood MCMC workable. Using the reference MCMC sample, we obtain dTV( πμT , π X) = 0 .102 , with a 95% moving-block bootstrap interval of [0 .098 , 0.107]. The posterior marginals in Figure 5 are almost indistinguishable across all seven parameters. The agreement also extends to the decision-relevant output. Figure 6 shows nearly identical posterior replacement probabilities over the observed mileage range. The SALE summary therefore agrees closely with the SALE-initialised full-likelihood reference chain while using 520 rather than 40 ,000 Bellman solves. This is approximately 77 times fewer full-likelihood evaluations for the SALE run. The example illustrates how SALE can wrap an existing full-solution likelihood without altering the Bellman solver or introducing DDC-specific approximation machinery into the AL method. 

6.2 Detection of Ultra-Diffuse Galaxies 

Ultra-diffuse galaxies (UDGs; van Dokkum et al., 2015) have garnered significant attention in astrophysics as they can help us understand dark matter. However, UDGs are extremely faint and difficult to detect directly in astronomical images (Abraham and van Dokkum, 2014). A complementary strategy is to detect them through spatial clustering of their star clusters (SCs), which trace the underlying gravitational potential and remain visible even when the galaxy itself is faint (Li et al., 2022; van Dokkum et al., 2024; Li et al., 2025c,b). We consider the log-Gaussian Cox process (LGCP; Møller et al., 1998) model of Li et al. (2022). Let X ⊆ S ⊂ R2 be the observed SC point pattern, where S is the image mask. The model is 

X ∼ IPP {Λ( s)} , s ∈ S ,

log Λ( s) = β0 +

> NG

X

> j=1

βj exp 

"

−

 r(s; cj )

Rj

1/n j

#

+ U(s),

U(s) ∼ GP{ 0, κ Mat´ ern (·; σf , ℓ, ν = 1) }.

Here β0 is the log-background SC intensity and the summation terms are S´ ersic intensity profiles (e.g., Li et al., 2025a,c) for the NG visible galaxies. For the j-th visible galaxy, cj

is the known galactic centre, r(s; cj ) is the projected distance from s to cj , and ( Rj , n j ) are the scale and shape parameters for its SC intensity profile. The residual field U(s) captures clustering not explained by the background or visible galaxies. Positive excursions of U(s)therefore provide the UDG-detection signal. Let θ = ( R1, n 1, . . . , R NG , n NG ) denote the nonlinear parameters. Conditional on θ, the model is a latent Gaussian model (LGM) and can be fitted efficiently by integrated nested Laplace approximation (INLA; Rue et al., 2009). Joint inference for θ is harder because its nonlinear dependence moves the model outside the standard LGM class required by INLA. The package inlabru handles this dependence through iterative local linearisation (Bachl et al., 2019), whose accuracy depends on the quality of the linear approximation.              

> 25 Li 0.0
> 0.1
> 0.2
> 10 20 30
> R1
> 0.00
> 0.05
> 0.10
> 10 20 30
> R2
> 0.0
> 0.5
> 1.0
> 1.5
> 0123
> n1
> 0.0
> 0.5
> 1.0
> 1.5
> 01234
> n2
> Density
> inlabru SALE GP-UCB

Figure 7: Posterior marginals of the nonlinear parameters in the UDG example from inlabru ,SALE, and the GP-UCB surrogate method of Li and Zhang (2026). 

A more direct route conditions on θ, performs a full conditional-INLA fit, and uses the resulting marginal log-likelihood ℓINLA (θ) and the corresponding log-unnormalised posterior 

f (θ) to enable posterior sampling (G´ omez-Rubio and Rue, 2018; Berild et al., 2022). This route, however, is expensive: one ℓINLA (θ) evaluation takes approximately 10 seconds on the same computer used for the DDC application in Section 6.1. We use SALE to infer θ

and compare it with the GP-UCB surrogate approach originally proposed for this problem by Li and Zhang (2026). The data are from a Hubble Space Telescope survey of the Perseus Galaxy Cluster (Harris et al., 2020). The image contains 262 detected SCs, two visible galaxies, and two confirmed UDGs. Consequently, θ = ( R1, n 1, R 2, n 2) ∈ R4. The priors follow Li et al. (2022); Li and Zhang (2026), and we restrict the parameters to R1 ∈ [2 , 30], R2 ∈ [2 , 45], n1, n 2 ∈ [0 .05 , 4]. SALE and GP-UCB use the same GP-fitting and experimental settings as in Section 5. Each method receives 100 evaluations of f , including the same 20-point initial Latin hyper-cube design. SALE takes 19 minutes and GP-UCB takes 18 minutes, so the computational cost of both methods is dominated by ℓINLA (θ) evaluations. The final SALE diagnostics are bpT,k = 0 .106 and bR = 1 .0003. Figure 7 compares the posterior marginal densities from SALE, GP-UCB, and inlabru .SALE and inlabru agree closely for several parameters but differ most visibly for R2.GP-UCB shows additional shifts and wider marginals, particularly for R1 and n1.Since a reference posterior is unavailable, we diagnose the SALE and GP-UCB surrogates at 50 held-out parameter values, with 25 drawn from each of the final ssMCMC samples. After weighting the points by normalised exp {f (θ)}, the weighted root mean squared error and mean absolute error of the surrogate mean are 0 .15 and 0 .11 for SALE, compared with 0.77 and 0 .52 for GP-UCB. Normalising exp {f (θ)} and exp {μT (θ)} over the same points gives discrete TV diagnostics of 0 .05 for SALE and 0 .22 for GP-UCB. These diagnostics favour SALE over GP-UCB under the given evaluation budget and validation set. 

> 26

Sampling-Based Adaptive Active Learning WUDG-88       

> WUDG-89
> WUDG-88
> WUDG-89
> inlabru SALE
> 20 40 60 20 40 60
> 20
> 40
> 60
> X (kpc)
> Y (kpc)
> E[exp( U(s))]
> [1.45, 1.50]
> [1.40, 1.45]
> [1.35, 1.40]
> [1.30, 1.35]
> [1.25, 1.30]
> [0.85, 1.25]

Figure 8: Posterior mean of exp {U (s)} from inlabru and SALE for the UDG example. Black points are star clusters; circles mark the two confirmed UDGs, with names shown below. 

Because inlabru does not estimate the same f (θ) as SALE and GP-UCB, we compare ten repeated f (θ) evaluations at the posterior modes returned by SALE and inlabru . f (θ)at the SALE mode is 0 .091 ± 1.3 × 10 −6 log-units higher than the inlabru mode, suggesting that part of the discrepancy in Figure 7 is caused by the linearisation in inlabru .Using the SALE posterior, we propagate uncertainty in θ to U(s) using a 17-point adaptive Gauss–Hermite-type quadrature rule in local eigen-coordinates (Bilodeau et al., 2024); its construction is described in Online Appendix F. Figure 8 shows a stronger residual-intensity signal under SALE near both confirmed UDGs, particularly WUDG-89. The difference is consistent with the posterior of R2: SALE places more mass on smaller values than inlabru , leaving more of the nearby SC intensity to the residual field. Thus, the treatment of the nonlinear parameters affects not only their posterior marginals but also the scientifically relevant UDG-detection map. Together with the held-out diagnostics, this example illustrates the practical value of reliable surrogate construction when each likelihood evaluation requires a separate conditional-INLA fit. 

7 Conclusion 

We introduced SALE, a posterior-aware active learning framework for Bayesian inference with expensive likelihood or posterior evaluations. SALE uses the expected posterior ¯ πt to guide both BO and UR, with a state-dependent allocation rule shifting evaluations from posterior localisation toward calibration. Building on the forward-KL optimality of ¯ πt, the theory quantifies AO’s stability–optimisation trade-off, gives an explicit budget-dependent bound on the expected TV error of the final EP under the ideal ¯ πt-weighted UR rule, and establishes an explicit expected-TV rate for the implemented UR score. Across the benchmarks, simulated examples, and two applications, SALE produced robust posterior approximations under limited evaluation budgets and reduced severe failures relative to the competing methods. These results demonstrate that sequential design with principled ex-ploitation of the structure of posterior distributions can support reliable Bayesian inference when repeated likelihood evaluation is prohibitively expensive. 

27 Li 

Acknowledgments and Disclosure of Funding 

The author thanks Alex Stringer for helpful comments and suggestions. This work was partly supported by a University of Toronto Data Sciences Institute Doctoral Fellowship (2023–2025) and CANSSI Multidisciplinary Doctoral Trainee Program. Computational re-sources were provided by the Digital Research Alliance of Canada. OpenAI’s ChatGPT 5.6 was used for proofreading and assistance with checking mathematical proofs, and OpenAI Codex 5.6 was used for code tidying and optimisation and to automate parts of the numer-ical implementation. All mathematical arguments, references, numerical results, and code were independently verified by the author, who takes full responsibility for the manuscript. 

Appendix A. Core Implementation and Metric Details 

This appendix records the core implementation details used by SALE and the TV estimator for the numerical experiments. Extended numerical settings, problem-specific configura-tions, and secondary empirical results are provided in Online Appendix. 

A.1 Implementation of ssMCMC 

For all examples, ssMCMC uses S = 10 ,000 surrogate-path draws and L = 10 Metropolis– Hastings (MH) updates per draw. The chain is initialised at the incumbent maximiser. The first 1 ,000 outer iterations use a Gaussian random-walk proposal; the remainder use an adaptive Gaussian proposal with covariance as in Roberts and Rosenthal (2009). Surrogate paths are generated with the RFF–Matheron construction in Appendix A.3. 

A.2 Allocation-Proxy Implementation Conventions 

Using the curvature-adjusted distances dt,i defined in Section 4.2, let dt, (1) ≤ dt, (2) ≤ · · · be the ordered positive distances and let I+ index them. The finite-sample convention is ∆loc  

> t,k

= 1

k

> k

X

> j=1

dt, (j)1{|I +|≥ k} + 1

|I +|

X

> i∈I +

dt,i 1{1 ≤ |I +|< k }, ∆loc  

> t,k

= ∞ if |I +|= 0 . (A.1) The allocation probability bpt,k is then defined as in Eq. (1). We use k = 5 and a = 0 .25 throughout. Larger k averages over a broader neighbourhood and generally delays UR, whereas larger a promotes an earlier transition. Under the retained-initial-design condition 

n0 ≥ k + 1 with distinct input locations, at least k positive distances are available at every active iteration, so Eq. (A.1) reduces to the mean of the k smallest positive distances. ∆loc  

> t,k

is a cheap local-spacing diagnostic rather than a calibrated estimate of p⋆t . Alocal-excursion and fill-distance motivation is given in Online Appendix B. 

A.3 Finite-Dimensional ESA Representation 

Algorithm 2 gives the levelwise target, block updates, and exchange acceptance ratio. Here we specify the finite-dimensional path representation and terminal local refinement used in computation. ESA is run on the current working region Ω r(t); as in the main text, πτ,t 

denotes the corresponding restricted AO law. 

> 28

Sampling-Based Adaptive Active Learning 

For a stationary kernel κ, draw ωj ∼ ρ from its spectral distribution and bj ∼ Unif(0 , 2π), independently, and define (Rahimi and Recht, 2007) 

ϕm(θ) = 

r 2κ(0) 

m

h

cos( ω⊤ 

> 1

θ + b1), . . . , cos( ω⊤

> m

θ + bm)

i⊤

.

Let Φt,m = [ ϕm(θ1), . . . , ϕm(θnt )] ⊤, At = ( Kt+ς2Int )−1. Conditional on Ξ m = {(ωj , b j )}mj=1 ,which is held fixed within an ESA run, the decoupled RFF–Matheron path is 

f dec  

> t,m

(θ; w, ε) = ϕm(θ)⊤w + kt(θ)⊤At {yt − Φt,m w − ε} ,

where w ∼ N (0, Im), ε ∼ N (0, ς 2Int ), independently. RFF supplies the prior-path com-ponent, while the Matheron correction retains the exact kernel vector and Gram matrix (Matheron, 1973; Wilson et al., 2021). Writing z = ( w⊤, ε⊤)⊤, the path is a deterministic function of z, so Qf is implemented on this Gaussian latent state and ESA returns ( eθt, ezt). Levelwise convergence of the finite-dimensional construction is established in Online Appendix H. Because bpt,k in Eq. (1) increases with ∆ loc  

> t,k

, the terminal ESA states can remain too dispersed around θ† 

> t

for bpt,k to become sufficiently small under a practical evaluation budget. We therefore apply a budget-limited local refinement to the terminal output. Let ezt =( ew⊤ 

> t

, eε⊤ 

> t

)⊤, eft,m = f dec  

> t,m

(·; ewt, eεt), and set θnt+1 = L eft,m ( eθt), where Lh(θ) denotes local optimisation of h initialised at θ. This moves the proposal towards a local maximiser of the sampled path within the basin selected by ESA, improving the local design resolution and driving bpt,k downward. As a secondary effect, it partially counteracts the optimisation bias associated with a fixed τ , quantified for exact AO in Theorem 10; it does not attempt global optimisation of the terminal path eft,m . The complete proposal kernels, temperature schedule, refinement budget, and numerical settings are reported in Online Appendix D. 

A.4 TV Distance Computation 

For the benchmark and simulated examples, let θ(1) , . . . , θ(M ) ∼ πX be a reference sample, and let πμT = π(· | μT ) be the plug-in law from Definition 2. Since TV( πμT , π X) = 12 EπX



1 − exp {μT (θ) − f (θ)}

EπX [exp {μT (θ) − f (θ)}]



,

define ℓm = μT (θ(m)) − f (θ(m)), ¯ w = M −1 PMm=1 exp( ℓm). The estimator is 

dTV( πμT , π X) = 12M

> M

X

> m=1

1 − exp( ℓm)¯w .

Construction and diagnostics of the reference samples are given in Online Appendix F. 

Appendix B. Proofs 

This appendix records additional assumptions, notations, standard GP results, and proofs of major technical results in the paper. 

29 Li 

B.1 Notation, Useful Results, and Additional Assumptions 

We work under Assumption 1 and use active-time indexing. Unless a result explicitly takes 

D0 = ∅, D0 may be any finite initial design whose input locations are chosen independently of f , the observation noises, and the subsequent algorithmic randomness. Results with an initial design are read conditionally on that design after restarting the active index t.Write Et(·) = E(· | H t), Var t(·) = Var( · | H t). Unless stated otherwise, ∥·∥ is Euclidean and ∥·∥ op is the matrix operator norm. For measurable A ⊆ Rd, set |A|:= λ(A). We retain the Euclidean-ball, covering-number, and diameter notation B(θ, r ), N (ϵ, Ω, d), and diam(Ω , d) from Assumption 1. Let P(Ω) be the Borel probability measures on Ω. For later use, write Gt := ft − μt and M μt := sup θ,θ′∈Ω|μt(θ) − μt(θ′)|.

For AO, retain θ⋆, RT , and RT from Definition 9. Fix a measurable maximiser selector 

s : C(Ω) → Ω, take 

θ⋆ = s(f ), θ⋆ft = s(ft), θAO  

> t+1

| ft ∼ πτ (· | ft),

and write ∆ AO  

> t+1

= Et{f (θ⋆) − f (θAO 

> t+1

)}. Then RT = PT −1 

> t=0

E{∆AO 

> t+1

} by the tower property. For UR, retain N UR  

> T

from Section 4.4 and write IUR  

> t

:= 1 −Bt, so that N UR  

> T

= PT −1 

> t=0

IUR  

> t

.

Posterior-variance contraction gives 0 ≤ σ2 

> t

(θ) ≤ κ(θ, θ) ≤ Bκ. Write 

d2 

> t

(θ, θ′) := Var t{f (θ) − f (θ′)},

for the posterior canonical metric. This is also the conditional canonical metric of Gt.

B.1.1 Standard Gaussian-Process Supremum Bound 

We repeatedly use the following conditional form of Dudley’s entropy bound and the Borell– TIS inequality (Adler and Taylor, 2007, Theorems 1.3.3 and 2.1.1); see also Dudley (1967). 

Lemma 19. Conditionally on a σ-field G, let X = {X(θ) : θ ∈ T } be a centred separable continuous Gaussian process on compact T , with conditional canonical metric 

dX (θ, θ′)2 := Var {X(θ) − X(θ′) | G} .

Suppose that, almost surely, dX (θ, θ′) ≤ d0(θ, θ′), sup θ∈T Var {X(θ) | G} ≤ v2, and 

J(d0) := 

Z diam( T ,d0)0

plog N (ϵ, T , d0) d ϵ < ∞.

Then, writing SX = sup θ∈T |X(θ)|,mG := E[SX | G ] ≤ v + CDJ(d0) a.s. ,

for a universal constant CD < ∞, and for u ≥ 0,

Pr( SX > m G + u | G ) ≤ 2 exp 



− u2

2v2



, a.s. (B.1) 

Hence, for all q ≥ 1, a > 0, E(SqX | G ) ≤ Cq{vq + J(d0)q}, E{exp( aS X ) | G} < ∞ a.s., where Cq < ∞ depends only on q.

The result follows by applying the cited inequalities under the regular conditional Gaus-sian law; the moment conclusions follow by integrating Eq. (B.1). 

> 30

Sampling-Based Adaptive Active Learning 

B.1.2 UR Calibration Assumptions 

Assumption 20. Let Cλ 

> 0

be the prior covariance operator on L2(Ω , λ ), that is, 

(Cλ 

> 0

h)( θ) = 

Z

> Ω

κ(θ, u)h(u) λ(d u),

where κ is the prior covariance kernel. Let λλ 

> 0,1

≥ λλ 

> 0,2

≥ · · · ≥ 0 be its eigenvalues. There exists A0 < ∞ such that at least one of the following conditions holds: 

(i) Polynomial spectral decay: for some ν > 0, λλ 

> 0,j

≤ A0j−(1+2 ν/d ), j ≥ 1.

(ii) Exponential spectral decay: for some a0 > 0, λλ 

> 0,j

≤ A0 exp {− a0j1/d }, j ≥ 1.

The polynomial and exponential conditions correspond, respectively, to the standard Mat´ ern-ν and SE kernels documented in Remark 16. The exponential condition implies the polynomial condition for every finite ν > 0, after changing A0.

B.1.3 Allocation-Proxy Smoothness 

Assumption 21. The prior GP admits a version with a.s. C2(U ) sample paths on an open neighbourhood U ⊃ Ω. For each r, s ∈ [d], the second-derivative process Drs f (θ) = ∂r∂sf (θ)

is a centred continuous GP on Ω, jointly Gaussian with finite collections of function values, with canonical metric d0,rs (θ, θ′)2 := Var {Drs f (θ) − Drs f (θ′)} satisfying 

J(rs )0 := 

Z diam(Ω ,d0,rs )0

q

log N (ε, Ω, d0,rs ) d ε < ∞,

and sup θ∈Ω Var {Drs f (θ)} ≤ B2,rs < ∞.

Assume also that ∂r∂sμt(θ) = Et{Drs f (θ)} a.s., θ ∈ Ω. The curvature matrix used in the allocation proxy has the form Ct = M{− (Ht + H⊤ 

> t

)/2}, where Ht = ∇2μt(θ† 

> t

)

and Ct ≻ 0, and the deterministic map M satisfies ∥M (H)∥op ≤ CM{1 + ∥H∥op } for every symmetric matrix H. Here M abstracts the implementation’s numerical positive-definite stabilisation after negative symmetrisation of the local Hessian, such as a ridge correction or positive-eigenvalue projection. The order statistics defining ∆loc  

> t,k

are computed after excluding zero curvature-adapted distances, equivalently after excluding evaluations at the incumbent location under Ct ≻ 0.

B.2 Proofs of Proposition 7 and Lemma 8 Proof [Proposition 7] Let ¯λ = λ/ |Ω|. By Assumption 1, ¯λ has full support on compact Ω. For Pt-almost every ft, the path is continuous and has the unique maximiser θ⋆ft . Hence the zero-temperature Laplace principle (Hwang, 1980) gives, for every bounded continuous 

g : Ω → R, Z

> Ω

g(θ) πτ (d θ | ft) → g(θ⋆ft ), as τ ↓ 0.

Since the left-hand side is bounded in absolute value by ∥g∥∞, conditional dominated con-vergence yields Z

> Ω

g(θ) πτ,t (d θ) →

Z

g(θ⋆ft ) Pt(d ft), as τ ↓ 0.

31 Li 

This proves the asserted weak convergence. 

Proof [Lemma 8] For simplicity, write ϱ = ϱ(g1, g 2). By Definition 6, the corresponding normalising constants satisfy 

e−ϱ/τ Zτ (g2) ≤ Z τ (g1) ≤ eϱ/τ Zτ (g2).

Thus ∥log( pτ, 1/p τ, 2)∥∞ ≤ 2ϱ/τ. For two probability measures π and ρ with π ≪ ρ, if the Radon-Nikodym derivative satisfies e−a ≤ dπ/ dρ ≤ ea a.e., then TV( π, ρ ) ≤ tanh( a/ 2) .

Indeed, taking B = {dπ/ dρ ≥ 1}, x = π(B), and y = ρ(B), the inequalities x ≤ eay and 1 − y ≤ ea(1 − x) give x − y ≤ (ea − 1) /(ea + 1). Consequently, TV {πτ (· | g1), π τ (· | g2)} ≤ tanh( ϱ/τ ).

Let γ ∈ Γ( Q1, Q2). Since γ has marginals Q1 and Q2, for every A ∈ B(Ω), 

|A τ (Q1)( A) − A τ (Q2)( A)| ≤ 

Z

TV {πτ (· | h1), π τ (· | h2)} γ(d h1, dh2)

≤ 1

τ

Z

∥h1 − h2∥∞ γ(d h1, dh2),

where tanh( u) ≤ u for u ≥ 0 was used. Taking the supremum over A and then the infimum over Γ( Q1, Q2) proves the AO-law bound. Finally, under Assumption 1(A5), posterior covariance contraction and Lemma 19 give 

Et{∥ ft − μt∥∞} < ∞ a.s. Since μt is continuous on compact Ω, Pt therefore has a finite first sup-norm moment almost surely. 

B.3 Proof of Theorem 10 

Throughout this section, the AO query at active time t is generated before yt+1 is observed. Conditional on Ht, θAO  

> t+1

is generated from an independent posterior draw ft ∼ Pt and auxiliary randomness, both conditionally independent of the latent objective f and the fresh observation noise. Thus the AO design is non-anticipative. Conditional on the realised design locations, the usual deterministic-design GP covariance recursion and information-gain identities therefore apply to the adaptive AO trajectory. 

B.3.1 Posterior Regularity 

Lemma 22. Under Assumption 1, for every t ≥ 0, Pt(C1(U )) = 1 a.s. Hence ft and Gt

may be chosen in C1(U ) a.s. Moreover, for each j ∈ [d], Dj Gt | H t is a centred continuous GP on Ω. If dt,j (θ, θ′)2 := Var t{Dj Gt(θ) − Dj Gt(θ′)}, then, almost surely, 

dt,j (θ, θ′) ≤ d0,j (θ, θ′), ∀θ, θ′ ∈ Ω, (B.2) 

and 

Var t{Dj Gt(θ)} ≤ B∂,j , ∀θ ∈ Ω. (B.3) 

> 32

Sampling-Based Adaptive Active Learning 

Proof Since Pr {f ∈ C1(U )} = 1, Pt{C1(U )} = E1{f ∈C1(U )} | H t

 = 1 a.s, Hence ft ∈

C1(U ) a.s., and Gt = ft − μt ∈ C1(U ) a.s. by Assumption 1(A5). The remaining assertions follow from the standard closure of GPs under linear differ-entiation and Gaussian conditioning (Rasmussen and Williams, 2006, Equation (2.24) and Section 9.4). Indeed, the non-anticipation condition in Assumption 1(A3) allows these iden-tities to be applied sequentially along the realised adaptive design. Thus Dj Gt | H t is a centred continuous GP, and its posterior covariance is the corresponding prior covariance minus a positive-semidefinite Gaussian-conditioning term. Applying this covariance order to Dj f (θ) and Dj f (θ) − Dj f (θ′) gives Eqs. (B.2) and (B.3), respectively. 

Lemma 23. Define Lt = 1 + Lip( ft) and L⋆ = 1 + Lip( f ), where 

Lip( g) = sup   

> θ,θ′∈Ω,θ̸=θ′

|g(θ) − g(θ′)|∥θ − θ′∥ .

Under Assumption 1, Lt, L ⋆ < ∞ a.s., and there is a constant CL < ∞ such that 

sup 

> t≥0

E{log +(rΩLt)} = E{log +(rΩL⋆)} ≤ CL. (B.4) 

Proof The C1 sample-path property and compact convexity of Ω imply Lip( g|Ω) ≤ sup 

> θ∈Ω

∥∇ g(θ)∥, g ∈ C1(U ).

Thus Lt, L ⋆ < ∞ a.s. Let Φ( g) = log +[rΩ {1 + Lip( g|Ω)}]. Since ft | H t and f | H t have the same conditional law, 

E{Φ( ft)} = E[Et{Φ( ft)}] = E[Et{Φ( f )}] = E{Φ( f )},

which proves the equality in Eq. (B.4). For finiteness, Lemma 19, applied to Dj f with 

d0 = d0,j and v2 = B∂,j , gives E {sup θ∈Ω|Dj f (θ)|} < ∞, j ∈ [d]. Consequently, 

E{Lip( f )} ≤ 

> d

X

> j=1

E



sup 

> θ∈Ω

|Dj f (θ)|



< ∞.

Since log +(x) ≤ x for x ≥ 0, the common expectation in Eq. (B.4) is finite. 

Lemma 24. Let Sdraw  

> t

= sup θ∈Ω|ft(θ) − μt(θ)|. Under Assumption 1, there exists ¯M < ∞,depending only on J0 and Bκ, such that 

h

E

n

(Sdraw  

> t

)2oi 1/2

≤ ¯M , ∀t ≥ 0.

Proof Condition on Ht. Posterior covariance contraction gives dt ≤ d0, sup θ∈Ω Var t{Gt(θ)} ≤ 

Bκ. The q = 2 conclusion of Lemma 19, with J(d0) = J0, therefore gives 

Et

"

sup 

> θ∈Ω

|Gt(θ)|

2#

≤ C2(J20 + Bκ) a.s. Taking expectations proves the claim with ¯M = {C2(J20 + Bκ)}1/2.

33 Li 

B.3.2 Posterior Identities and Gibbs Softening 

Lemma 25. For every t ≥ 0, Et {f (θ⋆)} = Et{ft(θ⋆ft )}, Et

f (θAO 

> t+1

) = Et

μt(θAO 

> t+1

) .

Proof Conditionally on Ht, f and ft are i.i.d. from Pt. Therefore f {s(f )} = f (θ⋆) and 

ft {s(ft)} = ft(θ⋆ft ) have the same conditional law, proving the first identity. For the second, 

θAO  

> t+1

is generated from ft and auxiliary randomness, and is conditionally independent of f

given Ht. Thus 

Et

f (θAO 

> t+1

) = Et

E f (θAO 

> t+1

) | H t, θAO 

> t+1

 = Et

μt(θAO 

> t+1

) .

Lemma 26. Under Assumption 1, define CΩ = 1+log |Ω|/(cΩrd

> Ω

) . Then, for every t ≥ 0,

ft(θ⋆ft ) − E{ft(θAO 

> t+1

) | H t, f t} ≤ τ CΩ + d log +(rΩLt/τ ) a.s. 

Proof Condition on Ht and ft = g. Let θ⋆g = s(g), g⋆ = g(θ⋆g ), L = 1 + Lip( g), and 

r = rΩ ∧ τ /L . For every ˜θ ∈ B(θ⋆g , r ) ∩ Ω, 

g( ˜θ) ≥ g⋆ − Lip( g)∥ ˜θ − θ⋆g ∥≥ g⋆ − Lr ≥ g⋆ − τ. 

Hence, with Zτ (g) from Definition 6, we have 

Zτ (g) ≥

Z 

> B(θ⋆g,r )∩Ω

exp {g(u)/τ } λ(d u) ≥ exp( g⋆/τ − 1) cΩrd.

Therefore, 

g⋆ − τ log Zτ (g) ≤ τ − τ log cΩ − dτ log r. (B.5) Let pτ,g := d πτ (· | g)/dλ be the Lebesgue density from Definition 6. Therefore, 

τ log Zτ (g) = 

Z

> Ω

g(θ)pτ,g (θ) λ(d θ) + τ H(pτ,g ), H(pτ,g ) := −

Z

> Ω

pτ,g (θ) log pτ,g (θ) λ(d θ).

Moreover, KL( pτ,g ∥uΩ) ≥ 0, where uΩ = |Ω|−11Ω, gives H(pτ,g ) ≤ log |Ω|. Hence 

Z

> Ω

g(θ)pτ,g (θ) λ(d θ) ≥ τ log Zτ (g) − τ log |Ω|.

Combining this inequality with Eq. (B.5) gives 

g⋆ −

Z

> Ω

g(θ)pτ,g (θ) λ(d θ) ≤ τ {1 + log( |Ω|/c Ω) − d log r} .

Finally, since r = rΩ ∧τ /L , − log r ≤ − log rΩ +log +(rΩL/τ ) . Substitution gives the claimed bound. 

Corollary 27. Under Assumption 1, Et[ft(θ⋆ft )−ft(θAO 

> t+1

)] ≤ τ CΩ + d Et

log +(rΩLt/τ )  .

Proof Take conditional expectation in Lemma 26. 

34 Sampling-Based Adaptive Active Learning 

B.3.3 Variance Accumulation 

Fix an auxiliary variance υ2 > 0 satisfying υ2 ≥ ς2. υ2 is used only in the information-gain argument. For a deterministic design θ1: t, write Kt(θ1: t) = {κ(θi, θj )}ti,j =1 , kt(θ; θ1: t) = [κ(θ, θ1), . . . , κ (θ, θt)] ⊤, and define the auxiliary posterior variance 

σ2

> t,υ

(θ; θ1: t) = κ(θ, θ) − kt(θ; θ1: t)⊤{Kt(θ1: t) + υ2It}−1kt(θ; θ1: t).

Define the corresponding regularised maximal information gain by 

γT (υ) = sup  

> θ1: T∈ΩT

12 log det {IT + υ−2KT (θ1: T )}.

Lemma 28. Under Assumption 1 (A1), (A2), and (A5) , fix any υ2 > 0 satisfying υ2 ≥ ς2,and let Cγ,υ = 2 Bκ/log(1 + υ−2Bκ). Then, for every T ≥ 1 and every possibly adaptive query sequence, T −1X

> t=0

σ2 

> t

(θt+1 ) ≤ Cγ,υ γT (υ) a.s. Consequently, PT −1 

> t=0

E[σ2 

> t

(θt+1 )] ≤ Cγ,υ γT (υ).

Proof Fix a realised sequence of query locations θ1: T , where the active index t starts after any initial design D0. Conditioning on the initial observations can only reduce posterior variance. For the active locations alone, replacing ς2 by υ2 ≥ ς2 can only increase posterior variance, by matrix inverse monotonicity. Hence 

σ2 

> t

(θ) ≤ σ2

> t,υ

(θ; θ1: t), t ≥ 0.

When ς2 = 0, this comparison is applied after redundant exact observations have been removed, as in Assumption 1. For any deterministic sequence θ1: T , the sequential determinant identity (Srinivas et al., 2012, Lemma 5.3) gives 12 

> T−1

X

> t=0

log 

(

1 + σ2

> t,υ

(θt+1 ; θ1: t)

υ2

)

= 12 log det {IT + υ−2KT (θ1: T )} ≤ γT (υ).

This identity is algebraic in the realised locations and therefore applies pathwise to the adaptive query sequence. Moreover, 0 ≤ σ2

> t,υ

(θ; θ1: t) ≤ Bκ. Since a/ log(1 + a/υ 2) is increasing, 

a ≤ Bκ

log(1 + Bκ/υ 2) log(1 + a/υ 2), 0 ≤ a ≤ Bκ.

Applying this inequality to the auxiliary variances and using their pathwise domination of the actual variances yields  

> T−1

X

> t=0

σ2 

> t

(θt+1 ) ≤ 2Bκ

log(1 + Bκ/υ 2) γT (υ) = Cγ,υ γT (υ).

Taking expectations proves the second assertion. 

35 Li 

B.3.4 Regret Decomposition and Posterior-Selection Bias 

Define the AO posterior-selection bias bsel  

> t+1

= Et{ft(θAO 

> t+1

) − μt(θAO 

> t+1

)}.

Lemma 29. Under Assumption 1, for every t ≥ 0, ∆AO  

> t+1

= Et{ft(θ⋆ft ) − ft(θAO 

> t+1

)} + bsel 

> t+1

.

Proof By Lemma 25, ∆ AO  

> t+1

= Et{ft(θ⋆ft )}− Et{μt(θAO 

> t+1

)}. Adding and subtracting Et{ft(θAO 

> t+1

)}

gives the result. We now control bsel  

> t+1

by a deterministic covering argument applied to Gt = ft − μt.Recall from Appendix B.1 that dt is the conditional canonical metric of Gt.

Lemma 30. Let eLt = 1 + Lip( Gt), and define fM∂ = d√B∂ + CD

Pdj=1 J(j)0 , B∂ =max 1≤j≤d B∂,j , where CD is the universal constant in Dudley’s entropy bound. Under As-sumption 1, for every u ≥ 0,

Pr 

eLt > 1 + fM∂ + u



≤ 2d exp 



− u2

2d2B∂



. (B.6) 

Proof Condition on Ht and write St,j := sup θ∈Ω|Dj Gt(θ)|. By Lemma 22, the hypotheses of Lemma 19 hold for Dj Gt, with d0 = d0,j and v2 = B∂,j . Hence, for every x ≥ 0, Pr 

n

St,j > pB∂,j + CDJ(j)0 + x Ht

o

≤ 2 exp 



− x2

2B∂



a.s. Since Gt ∈ C1(U ) and Ω is compact and convex, Lip( Gt) ≤ Pdj=1 St,j , while 

> d

X

> j=1

npB∂,j + CDJ(j)0

o

≤ fM∂ .

A union bound with x = u/d , followed by averaging over Ht, proves Eq. (B.6). 

Corollary 31. For T ≥ 1 and ηT ∈ (0 , 1) , define eΛT (ηT ) = 1 + fM∂ + dp2B∂ log(2 dT /η T )

and eGT (ηT ) = TT −1 

> t=0

{eLt ≤ eΛT (ηT )}. Then Pr { eGT (ηT )} ≥ 1 − ηT .

Proof Apply Lemma 30 with u = dp2B∂ log(2 dT /η T ), then union bound over t ∈ [T −1] 0.

Lemma 32. Under Assumption 1, there is a constant C0 < ∞ such that, for all t ≥ 0 and 

θ, θ′ ∈ Ω, dt(θ, θ′) ≤ d0(θ, θ′) ≤ C0∥θ − θ′∥, and |σt(θ) − σt(θ′)|≤ C0∥θ − θ′∥.

Proof The same posterior-variance contraction argument as in Lemma 22, applied to the scalar linear functional f (θ) − f (θ′), gives dt ≤ d0. Let Mj = sup θ∈Ω|Dj f (θ)|,

L∇ = sup θ∈Ω∥∇ f (θ)∥. The q = 2 conclusion of Lemma 19, applied to each Dj f , gives 

E(M 2 

> j

) < ∞. Therefore 

E[L2

> ∇

] ≤ d

> d

X

> j=1

E[M 2 

> j

] < ∞.

36 Sampling-Based Adaptive Active Learning 

Since f ∈ C1(U ) a.s. and Ω is compact convex, |f (θ) − f (θ′)|≤ L∇∥θ − θ′∥ a.s. Squar-ing and taking expectations yields d0(θ, θ′) ≤ C0∥θ − θ′∥, C 0 = {E[L2

> ∇

]}1/2. Finally, for 

θ, θ′ ∈ Ω, let a = σ2 

> t

(θ), b = σ2 

> t

(θ′), c = κt(θ, θ′). Since κt is a covariance kernel, c ≤ √ab 

by Cauchy-Schwarz. Hence |σt(θ) − σt(θ′)|2= a + b − 2√ab ≤ dt(θ, θ′)2, which proves the claim. 

Lemma 33. There exists Ccov < ∞ such that, for every h ∈ (0 , 1] , there is a finite deter-ministic C(h) ⊂ Ω satisfying sup θ∈Ω min u∈C (h)∥θ − u∥≤ h, with |C (h)|≤ Ccov h−d.

Proof Cover a bounded cube containing Ω by lattice cubes of side length h/ √d, and choose one representative point of Ω from each cell intersecting Ω. Fix T ≥ 1, choose ηT , ¯δT ∈ (0 , 1), and set δT = ηT + ¯δT ≤ 1. For t ∈ [T − 1] 0,define ht,T = ( t + 1) −2{1 + eΛT (ηT ) + C0}−1. Let Ct,T = C(ht,T ), and choose a deterministic nearest-grid map [ ·]t,T : Ω → C t,T . Then |C t,T |≤ Ccov {1 + eΛT (ηT ) + C0}d(t + 1) 2d.

Lemma 34. Let ϖt ≥ 1 with P∞ 

> t=0

ϖ−1 

> t

≤ 1, and define βt = 2 log  |C t,T |ϖt/¯δT

 . Let 

ET (¯δT ) = TT −1

> t=0

T 

> θ∈C t,T

{Gt(θ) ≤ √βt σt(θ)}. Then Pr {E T (¯δT )} ≥ 1 − ¯δT .

Proof For fixed t and θ ∈ C t,T , conditionally on Ht, Gt(θ) is centred Gaussian with variance σ2 

> t

(θ). Therefore Pr {Gt(θ) > pβtσt(θ) | H t} ≤ exp( −βt/2) .

Taking expectations and union bounding over θ ∈ C t,T and t ∈ [T − 1] 0,Pr {E T (¯δT )c} ≤  

> T−1

X

> t=0

|C t,T |exp( −βt/2) = ¯δTT −1X

> t=0

ϖ−1 

> t

≤ ¯δT .

The grids Ct,T are proof devices only. They induce the confidence radii βt, but they do not discretise the AO policy or restrict θAO 

> t+1

.

Lemma 35. On FT = eGT (ηT ) ∩ E T (¯δT ), for every t ∈ [T − 1] 0 and θ ∈ Ω,

Gt(θ) ≤ pβt σt(θ) + 1 + √βt

(t + 1) 2 .

Proof On FT ,

Gt(θ) ≤ Gt([ θ]t,T ) + eΛT (ηT )ht,T ≤ pβtσt([ θ]t,T ) + eΛT (ηT )ht,T .

By Lemma 32, σt([ θ]t,T ) ≤ σt(θ)+ C0ht,T . Hence Gt(θ) ≤ √βtσt(θ)+ {eΛT (ηT )+ C0

√βt}ht,T .

The definition of ht,T gives the stated bound. 

37 Li 

Lemma 36. For every t ∈ [T − 1] 0,

E[bsel 

> t+1

] ≤ pβt E{σt(θAO 

> t+1

)} + 1 + √βt

(t + 1) 2 + ¯M pδT .

Proof By Corollary 31 and Lemma 34, Pr( FT ) ≥ 1 − δT . Since bsel  

> t+1

= Et{Gt(θAO 

> t+1

)}, the tower property gives E[bsel 

> t+1

] = E{Gt(θAO 

> t+1

)}, and 

E[bsel 

> t+1

] = E{Gt(θAO 

> t+1

)1FT } + E{Gt(θAO 

> t+1

)1FcT }.

On FT , Lemma 35 holds simultaneously for all θ ∈ Ω; in particular it holds at the random point θAO 

> t+1

. Therefore 

E{Gt(θAO 

> t+1

)1FT } ≤ pβt E{σt(θAO 

> t+1

)1FT } + 1 + √βt

(t + 1) 2

≤ pβt E{σt(θAO 

> t+1

)} + 1 + √βt

(t + 1) 2 .

For the second term, by Cauchy-Schwarz and Lemma 24 

E{Gt(θAO 

> t+1

)1FcT } ≤ E{S draw  

> t

1FcT }≤ [E{(Sdraw  

> t

)2}]1/2 Pr( FcT )1/2 ≤ ¯M pδT .

Combining the two bounds proves the claim. 

B.3.5 Proof of Theorem 10 

Proof In the preceding covering construction, choose ϖt = π2(t+1) 2/6, ηT = ¯δT = T −2/2, 

δT = T −2. By Lemmas 29 and 36 and Corollary 27, the tower property, and Cauchy-Schwarz, 

RT ≤ T τ CΩ + d E log +(rΩL⋆/τ )  +

(

Cγ,υ γT (υ) 

> T−1

X

> t=0

βt

)1/2

+ 

> T−1

X

> t=0

1 + √βt

(t + 1) 2 + T ¯M pδT ,

where Lemma 28 was used for the variance sum. Define 

β⋆T = 2 



log 

 Ccov π2

6¯δT



+ d log {1 + eΛT (ηT ) + C0} + 2( d + 1) log T



.

The grid-cardinality bound gives 

βt ≤ β⋆T , 

> T−1

X

> t=0

βt ≤ T β ⋆T , 

> T−1

X

> t=0

1 + √βt

(t + 1) 2 ≤ π2

6 (1 + pβ⋆T ).

Also, E{log +(rΩL⋆/τ )} ≤ CL + log +(1 /τ ) by Lemma 23. Therefore 

RT ≤ T τ CΩ + d{CL + log +(1 /τ )} + {Cγ,υ β⋆T T γ T (υ)}1/2 + π2

6 (1 + pβ⋆T ) + ¯M . 

38 Sampling-Based Adaptive Active Learning 

Under the selected ηT and ¯δT , eΛT (ηT ) = 1 + fM∂ + dp2B∂ log(4 dT 3), so β⋆T = O(log T ) for fixed d. Substitution gives 

RT = O

np T γ T (υ) log T

o

+ O T τ {1 + log +(1 /τ )} ,

after absorbing the lower-order O(√log T ) + O(1) terms. 

B.4 Proofs of Proposition 13 and Theorem 14 

This section analyses an idealised UR branch that chooses evaluations by the posterior-weighted one-step IMSPE-reduction objective. The calibration rate is controlled through deterministic lower bounds on the realised number of UR evaluations, N UR  

> T

. The argument first controls the spectral envelope of the ¯ πt-weighted covariance operator, then converts realised UR steps into decay of Vt, and finally converts Vt into TV error. Let J − 

> t

= Ht ∨ σ(B0, . . . , B t−1), J + 

> t

= J − 

> t

∨ σ(Bt, θt+1 ). By Assumption 1(A3) and the fresh branch randomisation, 

P(f | J − 

> t

) = P(f | J + 

> t

) = P(f | H t) = Pt .

Thus the past branch labels and current pre-response input contain no information about 

f beyond Ht. Moreover, J − 

> t

⊆ J + 

> t

⊆ J −

> t+1

, so G2t := J − 

> t

, G2t+1 := J + 

> t

is an interlaced filtration. 

B.4.1 Uniform Density and Spectral Envelopes 

Lemma 37. Under the inherited Bayesian continuous-GP setup, let Bπ := sup θ∈Ω pX(θ).

Then Bπ < ∞ a.s. and E[Bπ] < ∞.

Proof Since f is continuous on compact Ω, the normalising constant Z(f ) from Definition 2 satisfies Z(f ) ≥ | Ω|exp {inf u∈Ω f (u)} a.s. Hence 

Bπ = sup 

> θ∈Ω

exp {f (θ)}/Z(f )

≤ | Ω|−1exp 



sup 

> θ∈Ω

f (θ) − inf  

> θ∈Ω

f (θ)



≤ | Ω|−1exp(2 Mf ),

where Mf = sup θ∈Ω|f (θ)|. By Assumption 1(A5) and the linear-exponential moment con-clusion of Lemma 19, applied to the prior process f , E{exp( aM f )} < ∞, a > 0. In par-ticular, E{exp(2 Mf )} < ∞. Therefore E[Bπ] ≤ | Ω|−1E{exp(2 Mf )} < ∞. The almost-sure finiteness of Bπ follows from E{exp(2 Mf )} < ∞.

Lemma 38. Let Bπ,t := Et[Bπ]. Then {Bπ,t }t≥0 is a nonnegative martingale and, for every 

δ ∈ (0 , 1) ,

Pr 



sup 

> t≥0

Bπ,t ≤ E[Bπ]/δ 



≥ 1 − δ. 

> 39

Li 

With EX 

> t

(δ) := {max 0≤s≤t Bπ,s ≤ E[Bπ]/δ } , EX 

> t

(δ) is Ht-measurable, EX

> t+1

(δ) ⊆ E X 

> t

(δ),

Pr {E X 

> t

(δ)} ≥ 1 − δ, and on EX 

> t

(δ),

¯ps(θ) ≤ E[Bπ]/δ, ∀θ ∈ Ω, 0 ≤ s ≤ t. 

Proof By Lemma 37, Bπ is integrable. Therefore Bπ,t = Et[Bπ] is a nonnegative martingale with respect to {H t}t≥0. Doob’s maximal inequality gives, for every finite n,Pr 



max  

> 0≤t≤n

Bπ,t > E[Bπ]/δ 



≤ δ

E[Bπ] E[Bπ,n ] = δ. 

Letting n → ∞ gives the stated horizon-uniform bound. It remains to verify the density domination. For every θ ∈ Ω, the definition of the expected posterior density and the tower property give ¯pt(θ) = Et{pX(θ)} ≤ Et



sup 

> u∈Ω

pX(u)



= Et[Bπ] = Bπ,t .

Thus, on EX 

> t

(δ), ¯ ps(θ) ≤ Bπ,s ≤ E[Bπ]/δ , ∀θ ∈ Ω, 0 ≤ s ≤ t. The measurability and monotonicity of EX 

> t

(δ) are immediate from its definition. 

Lemma 39. Assume Assumption 20. For each t, let Ct be the posterior covariance operator on L2(Ω , ¯πt), that is, 

(Cth)( θ) = 

Z

> Ω

κt(θ, u)h(u) ¯ πt(d u),

and let λt, 1 ≥ λt, 2 ≥ · · · ≥ 0 be its eigenvalues. For every δ ∈ (0 , 1) , on EX 

> t

(δ),

λs,j ≤ Aδj−(1+2 ν/d ), j ≥ 1, 0 ≤ s ≤ t, under polynomial decay ,λs,j ≤ Aδ exp {− a0j1/d }, j ≥ 1, 0 ≤ s ≤ t, under exponential decay ,

respectively, where Aδ = A0 E[Bπ]/δ. 

Proof Fix s ≤ t, and set Mδ = E[Bπ]/δ. On EX 

> t

(δ), ¯ πs has density bounded by Mδ. Let 

Cπ 

> 0,s

be the prior covariance operator on L2(Ω , ¯πs): (Cπ

> 0,s

h)( θ) = 

Z

> Ω

κ(θ, u)h(u) ¯ πs(d u).

We first compare the eigenvalues of Cπ 

> 0,s

with those of Cλ 

> 0

on L2(Ω , λ ). Let Hκ be the reproducing kernel Hilbert space of the prior kernel κ, and let Iλ : Hκ →

L2(Ω , λ ), I¯πs : Hκ → L2(Ω , ¯πs) be the canonical embedding maps. Since 

∥h∥2

> L2(¯ πs)

=

Z

> Ω

h(θ)2 ¯ps(θ)λ(d θ) ≤ Mδ∥h∥2

> L2(Ω ,λ )

,

we have ∥I¯πs h∥L2(¯ πs)≤ M 1/2 

> δ

∥Iλh∥L2(Ω ,λ ) for every h ∈ H κ. Hence 

aj (I¯πs ) ≤ M 1/2 

> δ

aj (Iλ), j ≥ 1,

40 Sampling-Based Adaptive Active Learning 

where aj (·) denotes the j-th approximation number of a compact operator. The nonzero eigenvalues of the covariance operators satisfy λπ 

> 0,s,j

= aj (I¯πs )2, λλ 

> 0,j

= aj (Iλ)2, because 

Cπ 

> 0,s

= I¯πs I∗ 

> ¯πs

and Cλ 

> 0

= IλI∗

> λ

. Therefore λπ 

> 0,s,j

≤ Mδλλ 

> 0,j

.

It remains to pass from the prior covariance operator to the posterior covariance op-erator. Exact GP regression gives posterior covariance domination in the Loewner sense: 0 ⪯ κs ⪯ κ, meaning that for every finite signed measure ρ on Ω, 

Z Z 

> Ω2

κs(θ, u) ρ(d θ)ρ(d u) ≤

Z Z 

> Ω2

κ(θ, u) ρ(d θ)ρ(d u).

Equivalently, for every h ∈ L2(Ω , ¯πs), ⟨h, Csh⟩L2(¯ πs) ≤ ⟨ h, Cπ

> 0,s

h⟩L2(¯ πs). The Courant–Fischer min–max principle for compact positive self-adjoint operators on L2(Ω , ¯πs) then yields λs,j ≤

λπ 

> 0,s,j

for every j ≥ 1. Combining the two bounds gives 

λs,j ≤ Aδj−(1+2 ν/d ) under polynomial decay ,λs,j ≤ Aδ exp {− a0j1/d } under exponential decay .

Since s ≤ t was arbitrary, the claim holds uniformly for all 0 ≤ s ≤ t on EX 

> t

(δ). 

B.4.2 Weighted Variance Decay under Realised UR Steps 

Proof [Proof of the first assertion of Proposition 13] Fix θ ∈ Ω, and use a superscript ( θ)for the post-update quantities in the hypothetical experiment that fixes the next query at 

θ without observing its response. Write ¯ π(θ) 

> t+1

for the corresponding expected posterior. Given Ht, the GP variance update is deterministic: 

σ2,(θ) 

> t+1

(u) = σ2 

> t

(u) − κt(u, θ)2

σ2 

> t

(θ) + ς2 . (B.7) When ς2 = 0 and σ2 

> t

(θ) = 0, the reduction term in Eq. (B.7) is interpreted according to the zero-variance convention in Definition 12, and is therefore zero. For any bounded Ht-measurable h, the tower property and the definition of ¯ πt give 

E

Z 

> Ω

h(u) ¯ π(θ)

> t+1

(d u) Ht



= E

Z 

> Ω

h(u) πX(d u) Ht



=

Z

> Ω

h(u) ¯ πt(d u).

Taking h(u) = σ2,(θ) 

> t+1

(u), which is Ht-measurable and does not depend on the realised response, yields 

E

n

V (θ) 

> t+1

Ht

o

=

Z

> Ω

σ2,(θ) 

> t+1

(u) ¯ πt(d u) = Vt − ∆t(θ). (B.8) Equivalently, if θt+1 is generated before its response and is conditionally independent of 

f given Ht, evaluating Eq. (B.8) at θt+1 gives E[Vt+1 | H t, θt+1 ] = Vt − ∆t(θt+1 ) a.s. 

41 Li 

Lemma 40. Assume Assumption 20. Fix δπ ∈ (0 , 1) and a fixed noise variance ς2 ≥ 0.There exists a deterministic constant c∆,δ π > 0, and, under exponential spectral decay, a deterministic constant CL,δ π ≥ Bκ, such that, for every t ≥ 0, on EX 

> t

(δπ), the following applicable bounds hold. These constants may depend on the fixed ς2, but not on t. Write 

Lδπ (v) := 1 + log( CL,δ π /v ), 0 < v ≤ Bκ, and define expressions with denominator Lδπ (Vt)d

to be zero when Vt = 0 . For the general case of ς2 ≥ 0, take a = 2 . If ς2 = 0 , the noiseless refinement is obtained by taking a = 1 . For either applicable value of a,

sup 

> θ∈Ω

∆t(θ) ≥

(c∆,δ π V a+d/ (2 ν) 

> t

, under polynomial spectral decay ,c∆,δ π V at Lδπ (Vt)−d, under exponential spectral decay .

Proof If Vt = 0, all four claims are trivial. Suppose Vt > 0. Since ¯ πt is a probability measure and σ2 

> t

(v) ≤ Bκ,sup 

> θ∈Ω

∆t(θ) ≥

Z

> Ω

∆t(v) ¯ πt(d v)

≥ 1

Bκ + ς2

Z Z 

> Ω2

κt(u, v)2 ¯πt(d u)¯ πt(d v) = 1

Bκ + ς2

X

> j≥1

λ2 

> t,j

.

(B.9) If ς2 = 0, define the variance-tilted probability measure Qt(d v) := σ2 

> t

(v)¯ πt(d v)/V t. When 

σ2 

> t

(v) = 0, positive semidefiniteness gives κt(u, v) = 0 for every u, so the zero-variance convention in Definition 12 is compatible with the cancellation below. Hence sup 

> θ∈Ω

∆t(θ) ≥

Z

> Ω

∆t(v) Qt(d v) = 1

Vt

Z Z 

> Ω2

κt(u, v)2 ¯πt(d u)¯ πt(d v) = 1

Vt

X

> j≥1

λ2 

> t,j

. (B.10) In either case, 

Vt =

Z

> Ω

κt(u, u) ¯ πt(d u) = X

> j≥1

λt,j .

On EX 

> t

(δπ), Lemma 39 and an integral comparison give constants CA,δ π < ∞ and b0 > 0such that, for m ≥ 1, 

X

> j>m

λt,j ≤

(

CA,δ π m−2ν/d , under polynomial spectral decay ,CA,δ π exp {− b0m1/d }, under exponential spectral decay .

Choose CL,δ π ≥ Bκ and Cm < ∞ sufficiently large, and set 

mt := 

(l 

CmV −d/ (2 ν)

> t

m

, under polynomial spectral decay ,

CmLδπ (Vt)d , under exponential spectral decay .

The constants can be chosen so that P 

> j>m t

λt,j ≤ Vt/2, while 

mt ≤ Cδπ

(

V −d/ (2 ν) 

> t

, under polynomial spectral decay ,Lδπ (Vt)d, under exponential spectral decay .

42 Sampling-Based Adaptive Active Learning 

Therefore, Cauchy’s inequality gives 

X

> j≥1

λ2 

> t,j

≥ 1

mt



> mt

X

> j=1

λt,j 



> 2

≥ cδπ

(

V 2+ d/ (2 ν) 

> t

, under polynomial spectral decay ,V 2 

> t

/L δπ (Vt)d, under exponential spectral decay .

Absorbing all constants as c∆,δ π , and combining the above with Eq. (B.9) proves the two general bounds, while combining it with Eq. (B.10) proves the two noiseless refinements. 

Lemma 41. Assume the Mat´ ern/SE kernel alternative in Theorem 14. Fix δπ ∈ (0 , 1) and a fixed noise variance ς2 ≥ 0. Suppose that whenever IUR  

> t

= 1 , the idealised UR rule chooses 

θt+1 ∈ arg max θ∈Ω ∆t(θ). At non-UR iterations, the selected input may be chosen by any rule taking values in Ω, provided it is generated before observing yt+1 and is conditionally independent of the latent objective f given Ht. Then there exist Cδπ < ∞ and cδπ > 0,possibly depending on the fixed ς2 but independent of T , such that, for every T ≥ 1 and every deterministic nT ∈ [T ]0, the following bounds hold, with the second being the sharper refinement available when ς2 = 0 :

E

n

VT 1EX  

> T(δπ)

o

≤



Cδπ {O+(nT )}2 + Bκ Pr  N UR  

> T

< n T

 ,Cδπ {O0 

> cδπ /2

(nT )}2 + Bκ Pr  N UR  

> T

< n T

 , if ς2 = 0 ,

O+ and O0 

> c

are as defined in Theorem 14. 

Proof Under Mat´ ern-ν kernel, set g(x) = xp, where 

p =

(

2 + d/ (2 ν), for any fixed ς2 ≥ 0,

1 + d/ (2 ν), if ς2 = 0 and the noiseless refinement is used .

Under SE kernel, let Lδπ be as in Lemma 40, set g(0) = 0, and define 

g(x) := xa

Lδπ (x)d , a =

(

2, in the general case ,

1, for the noiseless refinement , 0 < x ≤ Bκ.

In all four cases, g is nonnegative, nondecreasing, and convex on [0 , B κ]. For the SE case, this follows, for x > 0, from 

g′(x) = xa−1Lδπ (x)−d {a + d/L δπ (x)} ,g′′ (x) = xa−2Lδπ (x)−d a(a − 1) + d(2 a − 1) /L δπ (x) + d(d + 1) /L δπ (x)2 > 0,

and the continuous extension at zero. The events EX 

> t

(δπ) are decreasing and Ht-measurable. Let Wt = Vt1EX  

> t(δπ)

. Then Wt ∈ [0 , B κ]. Since EX

> t+1

(δπ) ⊆ E X 

> t

(δπ), the posterior-ignorability identities and Eq. (B.8) imply, for any allowed non-UR input, that E[Wt+1 | J + 

> t

] ≤ Wt on 

{IUR  

> t

= 0 }. On EX 

> t

(δπ), the ideal UR choice, Eq. (B.8) and Lemma 40 give 

E[Vt+1 | J + 

> t

] ≤ Vt − c∆,δ π g(Vt)

43 Li 

on {IUR  

> t

= 1 }. Combining the two cases gives 

E[Wt+1 | J + 

> t

] ≤ Wt − IUR  

> t

c∆,δ π g(Wt). (B.11) Choose c0 ∈ (0 , c ∆,δ π ] so that c0 sup x∈[0 ,B κ] g′(x) ≤ 1, and define ϕ(x) := x − c0g(x), 

x ∈ [0 , B κ]. Then ϕ is nondecreasing and concave on [0 , B κ]. Eq. (B.11) implies that a non-UR step cannot increase Wt in conditional expectation, while a UR step gives the conditional drift Wt − c0g(Wt) = ϕ(Wt). Define X2t = X2t+1 := Wt, t ≥ 0. By Eq. (B.11), {Xk, Gk}k≥0 is a bounded nonnegative supermartingale. At an odd index which is a UR decision, E[X2t+2 | G 2t+1 ] ≤ ϕ(X2t+1 ).

Define the nth UR decision time and its completion time by 

ζUR  

> n

:= inf 

(

t ≥ 0 : 

> t

X

> s=0

IUR  

> s

≥ n

)

, n ≥ 1,ρUR 0 := 0 , ρUR  

> n

:= inf 

(

t ≥ 1 : 

> t−1

X

> s=0

IUR  

> s

≥ n

)

, n ≥ 1.

Thus, on {ζUR  

> n

< ∞} , ρUR  

> n

= ζUR  

> n

+ 1. Under the interlaced filtration, for n ≥ 0, 

SUR  

> n

:= 2 ρUR  

> n

, UUR  

> n+1

:= 2 ζUR  

> n+1

+ 1 ,

are stopping times. On {UUR  

> n+1

< ∞} , SUR  

> n+1

= UUR  

> n+1

+ 1 . Set 

ZUR  

> n

:= XSUR  

> n

1{SUR   

> n<∞}

= WρUR  

> n

1{ρUR  

> n<∞}

,

and eXUUR  

> n+1

:= XUUR  

> n+1

1{UUR 

> n+1 <∞}

. All these variables are set to zero on the corresponding infinite-time events. Because {Xk} is bounded, it is uniformly integrable and has an L1 terminal value X∞.Optional sampling for the possibly infinite stopping times SUR  

> n

≤ UUR  

> n+1

gives 

E

h

XUUR  

> n+1

1{UUR  

> n+1 <∞}

+ X∞1{UUR  

> n+1 =∞}

GSUR 

> n

i

≤ XSUR  

> n

1{SUR   

> n<∞}

+ X∞1{SUR  

> n=∞}

.

On {SUR  

> n

< ∞} , drop the nonnegative terminal-value term on the left. On its complement, 

UUR  

> n+1

= ∞, so both truncated variables below vanish. Consequently, 

E

h eXUUR  

> n+1

GSUR 

> n

i

≤ ZUR  

> n

.

At UUR 

> n+1

, the UR drift and the identity SUR  

> n+1

= UUR  

> n+1

+ 1 give 

E

h

ZUR  

> n+1

GSUR 

> n

i

≤ E

h

ϕ( eXUUR  

> n+1

) GSUR 

> n

i

.

Since ϕ is nondecreasing and concave, 

E

h

ZUR  

> n+1

GSUR 

> n

i

≤ ϕ



E

h eXUUR  

> n+1

GSUR 

> n

i 

≤ ϕ(ZUR  

> n

).

44 Sampling-Based Adaptive Active Learning 

Let zUR  

> n

:= E[ZUR  

> n

]. Taking expectations and using the concavity of ϕ gives 

zUR  

> n+1

≤ E{ϕ(ZUR  

> n

)} ≤ ϕ(zUR  

> n

) = zUR  

> n

− c0g(zUR  

> n

).

Under Mat´ ern-ν kernel, the standard deterministic recursion comparison therefore gives 

zUR  

> n

≤ Cδπ (n + 1) −1/(p−1) =

(

Cδπ (n + 1) −2ν/ (2 ν+d), for any fixed ς2 ≥ 0,Cδπ (n + 1) −2ν/d , for the noiseless refinement .

Under SE kernel, the claim is immediate if some zUR  

> n

= 0. Otherwise, write ℓn = Lδπ (zUR  

> n

). After reducing c0, if necessary, so that c0 max {1, B κ} < 1, consider first the general case. The recursion then implies 

zUR  

> n+1

≤ zUR 

> n

n

1 − c0zUR  

> n

ℓ−dn

o

.

Define h(x) := Lδπ (x)d/x for x ∈ (0 , B κ]. Since h is decreasing and Lδπ (zUR 

> n+1

) ≥ ℓn,

h(zUR 

> n+1

) ≥ ℓdn

zUR  

> n

{1 − c0zUR  

> n

ℓ−dn } ≥ h(zUR  

> n

) + c0,

where the last step uses (1 − u)−1 ≥ 1 + u. Thus h(zUR  

> n

) ≥ h(zUR 0 ) + c0n. Moreover, let 

an := Cδπ {1 + log( n + 1) }d/(n + 1) . For all sufficiently large n, an ≤ Bκ, and 

Lδπ (an) = 1 + log 

 CL,δ π (n + 1) 

Cδπ {1 + log( n + 1) }d



≤ C{1 + log( n + 1) }.

Consequently, 

h(an) = Lδπ (an)d/a n ≤ C−1 

> δπ

Cd(n + 1) ≤ c0n, 

for all sufficiently large n, after enlarging Cδπ if necessary. Monotonicity of h, followed by an enlargement of Cδπ to cover the remaining n, therefore gives 

zUR  

> n

≤ Cδπ

{1 + log( n + 1) }d

n + 1 .

For the noiseless exponential case, the recursion instead gives zUR  

> n+1

≤ zUR  

> n

(1 − c0ℓ−dn ),

and hence 

ℓn+1 ≥ ℓn − log(1 − c0ℓ−dn ) ≥ ℓn + c0ℓ−dn .

Therefore ℓd+1  

> n+1

≥ ℓd+1  

> n

+ ( d + 1) c0, so 

zUR  

> n

≤ Cδπ exp {− cδπ (n + 1) 1/(d+1) }.

For the remainder of the proof, write the applicable bound as 

rδπ (n) := 

(Cδπ {O+(n)}2, for the general bound ,Cδπ {O0 

> cδπ /2

(n)}2, for the noiseless refinement .

45 Li 

Now fix 0 ≤ nT ≤ T , and let AT := {N UR  

> T

≥ nT } = {ρUR  

> nT

≤ T } = {SUR  

> nT

≤ 2T }. Set 

S′ 

> T

:= SUR  

> nT

∧ 2T. Then AT ∈ G S′ 

> T

, so bounded optional sampling gives 

E[WT 1AT ] = E[X2T 1AT ] ≤ E[XS′ 

> T

1AT ]= E[XSUR 

> nT

1AT ] ≤ E[ZUR  

> nT

] ≤ rδπ (nT ).

On AcT , WT ≤ Bκ. Hence E[WT ] ≤ rδπ (nT )+ Bκ Pr  N UR  

> T

< n T

 , which proves the claim. 

B.4.3 From Weighted Variance to TV Calibration 

Lemma 42 (Generalisation of the second assertion of Proposition 13) . Under the inherited ideal Bayesian GP setup, for any Ht-measurable event E,

E{TV(¯ πt, π X)1E } ≤ 

 12 E{Vt1E }

1/2

.

Consequently, E{TV(¯ πt, π X)} ≤ [E{Vt}/2] 1/2 .

Proof Condition on Ht. Let f = μt + G, g = μt + G′, where G and G′ are independent centred posterior GP residuals with common covariance κt. Using Definition 2, write πf := 

π(· | f ) and πg := π(· | g). Conditionally on Ht, πX has the same law as πf , while ¯ πt = Et[πg].

By convexity of TV, Pinsker’s inequality, and Jensen’s inequality, 

Et{TV(¯ πt, π f )} ≤ Et{TV( πg, π f )} ≤ 

 12 Et{KL( πf ∥πg)}

1/2

.

For the KL term, KL( πf ∥πg) = 

Z

> Ω

{f (θ) − g(θ)} πf (d θ) + log Z(g) − log Z(f ).

Taking Et over the conditionally independent pair ( G, G ′), the normalising-constant terms cancel because G and G′ have the same conditional law. The term involving G′ vanishes because G′ is conditionally independent of G and Et[G′(θ)] = 0. Thus, 

Et[KL( πf ∥πg)] = Et

Z 

> Ω

G(θ) π(d θ | μt + G)



, (B.12) where, π(d θ | μt + G) is defined according to Definition 2. It remains to bound Eq. (B.12). Define the log-partition functional Ψ( h) = log 

Z

> Ω

exp {μt(θ) + h(θ)}λ(d θ), h ∈ C(Ω) .

For a deterministic direction a ∈ C(Ω), its first directional derivative is 

DΨh[a] = 

Z

> Ω

a(θ)π(d θ | μt + h).

46 Sampling-Based Adaptive Active Learning 

Hence Z

> Ω

G(θ)π(d θ | μt + G) = DΨG[G].

Let Qn = {Cn, 1, . . . , C n,N n } be finite Borel partitions of Ω with max 1≤i≤Nn diam( Cn,i ) →

0. Choose ξn,i ∈ Cn,i , and set an,i = λ(Cn,i ), Zn,i = G(ξn,i ), μn,i = μt(ξn,i ), Σ n,ij =

κt(ξn,i , ξn,j ). Then Zn = ( Zn, 1, . . . , Z n,N n )⊤ is conditionally Gaussian given Ht, with mean zero and covariance matrix Σ n. Define the finite-dimensional log-partition function Ψn(x) = log 

( NnX

> i=1

an,i exp( μn,i + xi)

)

, x = ( x1, . . . , x Nn ) ∈ RNn ,

and its corresponding softmax weights 

wn,i (x) = an,i exp( μn,i + xi)/

> Nn

X

> ℓ=1

an,ℓ exp( μn,ℓ + xℓ).

Then ∂iΨn(x) = wn,i (x), and ∂ij Ψn(x) = wn,i (x){1(i = j)−wn,j (x)}. The finite-dimensional Gaussian integration-by-parts identity gives 

Et

( NnX

> i=1

Zn,i ∂iΨn(Zn)

)

= Et



> Nn

X

> i,j =1

Σn,ij ∂ij Ψn(Zn)

 .

Equivalently, 

Et

( NnX

> i=1

Zn,i wn,i (Zn)

)

= Et

( NnX

> i=1

κt(ξn,i , ξn,i )wn,i (Zn)

)

− Et



> Nn

X

> i,j =1

κt(ξn,i , ξn,j )wn,i (Zn)wn,j (Zn)

 .

(B.13) This is just Stein’s identity in finite dimensions: E{X⊤∇Ψn(X)} = E[tr {Σn∇2Ψn(X)}]. Let Gn and μt,n be the piecewise-constant approximations of G and μt that take values 

G(ξn,i ) and μt(ξn,i ) on Cn,i , respectively. By continuity of G, μt, and κt on compact Ω, 

Gn → G and μt,n → μt uniformly almost surely, and the corresponding Gibbs laws converge to π(· | μt + G). Moreover, the left-hand side of Eq. (B.13) is dominated by sup θ∈Ω|G(θ)|,which has finite conditional first moment by posterior covariance contraction and Lemma 19; the covariance terms are bounded by Bκ. Dominated convergence therefore yields 

Et{DΨG[G]} = Et

Z 

> Ω

κt(θ, θ)π(d θ | μt + G)



− Et

Z Z 

> Ω2

κt(θ, u)π(d θ | μt + G)π(d u | μt + G)



.

The second term is nonnegative by positive definiteness of κt. Therefore 

Et{KL( πf ∥πg)} = Et{DΨG[G]}≤ Et

Z 

> Ω

κt(θ, θ)π(d θ | μt + G)



=

Z

> Ω

σ2 

> t

(θ)¯ πt(d θ) = Vt.

47 Li 

Thus Et{TV(¯ πt, π X)} ≤ (Vt/2) 1/2. Since E ∈ H t, multiplying by 1E , taking expectations, and applying Cauchy-Schwarz yields 

E{TV(¯ πt, π X)1E } ≤ E

n

(Vt/2) 1/2 1E

o

≤

 12 E[Vt1E ]

1/2

.

Taking E to be the whole sample space gives the second claim. 

Proof [Theorem 14] Fix δ, q ∈ (0 , 1), set δπ = δ/ 2, ET = EX 

> T

(δπ), n T = ⌊qT ⌋. By Lemmas 38 and 42, 

E {TV(¯ πT , π X)} ≤ 

 12 E[VT 1ET ]

1/2

+ δπ.

Since nT + 1 ≥ qT ≥ q(T + 1) /2 and Pr( N UR  

> T

< n T ) ≤ Pr( N UR  

> T

< qT ), applying Lemma 41 and ( x + y)1/2 ≤ x1/2 + y1/2 gives 

E {TV(¯ πT , π X)} ≤ Cδ,q O+(T ) + Cδ,q Pr( N UR  

> T

< qT )1/2 + δ. 

For the SE branch, this also uses 1 + log( nT + 1) ≤ 1 + log( T + 1). If ς2 = 0, the noiseless bound in Lemma 41 similarly gives 

E {TV(¯ πT , π X)} ≤ Cδ,q O0 

> cδ,q

(T ) + Cδ,q Pr( N UR  

> T

< qT )1/2 + δ. 

In the SE case, the fixed rescaling nT + 1 ≥ q(T + 1) /2 and the outer square root are absorbed into cδ,q . Together with the previous general case bound, this proves Eq. (5). For the final assertion, let LUR := lim inf T →∞ N UR  

> T

/T > 0 a.s. Fix δ ∈ (0 , 1), and set ηδ := min 1/4, δ 2/(16 Bκ) . Since LUR > 0 almost surely, there exists a deterministic 

qδ ∈ (0 , 1/2) such that Pr( LUR ≤ 2qδ) ≤ ηδ. On {LUR > 2qδ}, 1{N UR  

> T

< q δT } → 0. Dominated convergence therefore gives lim sup  

> T→∞

Pr( N UR  

> T

< q δT ) ≤ Pr( LUR ≤ 2qδ) ≤ ηδ.

Hence, for all sufficiently large T , Pr( N UR  

> T

< q δT ) ≤ 2ηδ.

Repeat the preceding variance-to-TV argument with posterior-density event level δ/ 2, 

q = qδ, and nT = ⌊qδT ⌋. Before absorbing the count-probability term into Cδ,q , it gives 

E {TV(¯ πT , π X)} ≤ Cδ,q δ O+(T ) + 

 Bκ

2 Pr( N UR  

> T

< q δT )

1/2

+ δ

2 ,

E {TV(¯ πT , π X)} ≤ Cδ,q δ O0

> cδ,q δ

(T ) + 

 Bκ

2 Pr( N UR  

> T

< q δT )

1/2

+ δ

2 , ς2 = 0 .

The definition of ηδ makes each displayed probability term at most δ/ 4 for sufficiently large 

T . Enlarging the constants and the deterministic threshold proves Eq. (6). 

48 Sampling-Based Adaptive Active Learning 

B.5 Proofs of Proposition 4 and Corollary 15 

This section proves the martingale property of the expected posterior and controls the BO–UR allocation proxy. 

B.5.1 Martingale Property of the Expected Posterior 

Proof [Proposition 4] For bounded measurable h, Mt(h) is bounded by ∥h∥∞. Since {H t}t≥0

is increasing, the tower property gives E{Mt+1 (h) | H t} = Mt(h), so Mt(h) is a bounded martingale. Hence Mt(h) converges almost surely and in L1.Let {hm}m≥1 be a countable convergence-determining class in C(Ω). On an event of probability one, Mt(hm) converges for every m. Equip P(Ω) with the topology of weak convergence. Since Ω is compact, P(Ω) is compact under weak convergence. Hence, every subsequence of {¯πt} has a weakly convergent further subsequence. All subsequential limits agree on the class {hm}, hence coincide. Thus ¯ πt ⇒ ¯π∞ almost surely. 

B.5.2 Allocation Proxy Control 

Lemma 43. Suppose Assumption 21 holds and D0 contains at least k + 1 distinct input locations. Let DΩ := sup θ,θ′∈Ω∥θ − θ′∥, S 0 := sup θ∈Ω|f (θ)|, S2 := sup θ∈Ω∥∇ 2f (θ)∥op ,

and define Zj := sup t≥0 E(Sj | H t), j ∈ { 0, 2}, C⋆ := CM(1 + Z2), M ⋆ := 2 Z0. With 

ca := log(3) /(2 a), set ρ⋆ := tanh( caDΩ

√C⋆), Q ⋆ := (1 − ρ⋆) exp( −2ρ⋆M⋆). Then 

sup 

> t≥0

bpt,k ≤ ρ⋆ < 1 a.s. ,

and, for every m > 0, Q⋆ > 0 a.s., E(Q−m⋆ ) < ∞. If Bt is generated from a fresh 

Bernoulli( bpt,k ) draw conditionally on the pre-branch history, then 

lim inf   

> T→∞

N UR  

> T

/T ≥ 1 − ρ⋆ > 0 a.s. 

Proof Let θ0,1, . . . , θ0,k +1 be distinct locations in D0. At most one can equal the incumbent 

θ† 

> t

. Hence at least k have positive curvature-adjusted distance from θ† 

> t

, and each such distance is at most DΩ

p∥Ct∥op . Because ∆ loc  

> t,k

averages the k smallest positive distances, ∆loc  

> t,k

≤ DΩ

p∥Ct∥op .

Applying Lemma 19 with the trivial sigma-field to f and to the finitely many second-derivative processes gives E{exp( bS j )} < ∞, b > 0, j ∈ { 0, 2}, where the assertion for S2

also uses equivalence of matrix norms in fixed dimension. Moreover, sup 

> θ∈Ω

∥∇ 2μt(θ)∥op ≤ E(S2 | H t),

so the growth condition on M gives ∥Ct∥op ≤ C⋆. It follows that bpt,k = tanh( ca∆loc  

> t,k

) ≤ ρ⋆.

The random variables Z0, Z 2 are finite almost surely and have exponential moments of every positive order. Indeed, for b > 0 and r > 1, conditional Jensen’s inequality and Doob’s Lr maximal inequality give 

E{exp( bZ j )} ≤ E



sup 

> t≥0

E{exp( bS j ) | H t}



≤ rr − 1 [E{exp( rbS j )}]1/r < ∞. (B.14) 

49 Li 

Since 

M μt = sup 

> θ,θ′∈Ω

|μt(θ) − μt(θ′)|≤ 2 E(S0 | H t) ≤ M⋆,

the definition of Q⋆ gives a common lower bound for the variance-capture factors used below. Finally, for x ≥ 0, {1 − tanh( x)}−1 ≤ e2x. Thus, for every m > 0, 

Q−m⋆ ≤ exp 



2mc aDΩ

pC⋆ + 2 mM ⋆



,

whose expectation is finite by H¨ older’s inequality and Eq. (B.14); here √C⋆ ≤ √CM(1+ Z2). For the activation claim, let F− 

> t

be the sigma-field available immediately before Bt

is drawn, and set DBt := Bt − bpt,k . Fresh branch randomisation gives E(DBt | F − 

> t

) = 0. With F+ 

> t

:= σ(F− 

> t

, B t), these variables form bounded martingale differences along the natural interlaced pre/post-branch filtration. The martingale strong law therefore yields 

T −1 PT −1 

> t=0

DBt → 0 a.s. Consequently, lim inf  

> T→∞

N UR 

> T

T = lim inf  

> T→∞

(

1 − 1

T 

> T−1

X

> t=0

bpt,k − 1

T 

> T−1

X

> t=0

DBt

)

≥ 1 − ρ⋆ > 0 a.s. 

Proof [Corollary 15] The first two conclusions follow directly from Lemma 43. The final conclusions follow from the last assertion of Theorem 14. 

B.6 Bounded-Lag Fixed-Sample Calibration Assumption 44. There are increasing (Ht)-stopping times r0 = 0 < r1 < r2 < · · · such that r(t) = max {ri : ri ≤ t}, ri+1 − ri ≤ Tmax + 1 for a deterministic integer Tmax < ∞.At each ri, a sample Xri = {θri,s }Ss=1 is generated using auxiliary randomness independent of f , the observation noises, and previous auxiliary draws, with θri,s | H ri ∼ ¯πri for every 

s; dependence among the S sample points is allowed. The realised sample is reused and unchanged for ri ≤ t < ri+1 . At each iteration, the branch, BO, and posterior-path ran-domisations are generated from fresh auxiliary randomness. Conditional on (Ht, Xr(t)), this randomness is independent of f , future observation noises, and all auxiliary randomness used at earlier iterations. 

This construction is ignorable: inductively, Xr(t) and the next query are conditionally independent of f given Ht. After conditioning on the realised query, the response depends on f but not on Xr(t). Thus, for this section, Assumption 44 replaces the fresh-randomness requirement in Assumption 1 (A3) while retaining its non-anticipation conclusion. 

B.6.1 Fixed-Sample Score Calibration 

For each refresh index i ≥ 0, set qi := (1 − bpri,k ) exp {− 2bpri,k M μ 

> ri

}.

Lemma 45. Under the setup of Theorem 17, for every i ≥ 0, Eσ2 

> ri

(θri+1 ) Hri

 ≥ qiVri .

50 Sampling-Based Adaptive Active Learning 

Proof Write t = ri, and let Jσt be a deterministically tie-broken maximiser of σt(θt,s ), 1 ≤ s ≤ S. If the maximum variance is zero, the comparison below is immediate. Otherwise, the definition of Jt gives log σt(θt,J t ) ≥ log σt(θt,J σt ) + bpt,k 

μt(θt,J σt ) − μt(θt,J t )

≥ log σt(θt,J σt ) − bpt,k M μt .

Consequently, 

σ2 

> t

(θt,J t ) ≥ exp( −2bpt,k M μt ) max  

> 1≤s≤S

σ2 

> t

(θt,s ).

Since t is a refresh time, every point in the newly generated sample has conditional marginal ¯πt. Hence 

E



max  

> 1≤s≤S

σ2 

> t

(θt,s ) Ht



≥ 1

S

> S

X

> s=1

Eσ2 

> t

(θt,s ) Ht = Vt.

Conditional on ( Ht, Xt), discard the nonnegative BO-branch contribution and use Pr( Bt =1 | H t, Xt) = bpt,k . Because bpt,k and M μt are Ht-measurable, taking conditional expectation first given ( Ht, Xt) and then given Ht yields 

Eσ2 

> t

(θt+1 ) Ht ≥ (1 − bpt,k ) exp( −2bpt,k M μt )Vt = qiVt.

Proof [Theorem 17] The ignorability argument following Assumption 44 makes the next query conditionally independent of f given Ht. The proof of Proposition 13 therefore gives 

E(Vt+1 | H t) = Vt − E{∆t(θt+1 ) | H t} ≤ Vt. Thus {Vt, Ht}t≥0 is a bounded nonnegative supermartingale. By Lemma 43, qi ≥ (1 − ρ⋆) exp( −2ρ⋆M⋆) = Q⋆ > 0 a.s., and E(Q−1 

> ⋆

) < ∞. Since qi is 

Hri -measurable, Lemma 45 gives 

E(Vri ) ≤ Eq−1 

> i

Eσ2 

> ri

(θri+1 ) Hri

 = Eq−1 

> i

σ2 

> ri

(θri+1 ) .

Let I = IT := ⌈T / (Tmax +1) ⌉. The bounded-gap condition gives ri ≤ i(Tmax +1) ≤ T −1for 0 ≤ i < I. Optional sampling gives 

I E(VT ) ≤

> I− 1

X

> i=0

E(Vri ).

Because the refresh times are distinct and lie in [ T − 1] 0, the pathwise variance-sum bound in Lemma 28 yields 

I E(VT ) ≤ E

(I− 1X

> i=0

σ2 

> ri

(θri+1 )

qi

)

≤ E

(

Q−1 

> ⋆T−1

X

> t=0

σ2 

> t

(θt+1 )

)

≤ Cγ,υ E(Q−1 

> ⋆

)γT (υ).

The second assertion of Proposition 13 therefore gives 

E {TV(¯ πT , π X)} ≤ 

 Cγ,υ E(Q−1 

> ⋆

)γT (υ)2IT

1/2

. (B.15) 

51 Li 

To identify its order, write γκ,D T (v) when the kernel, domain, and regularisation pa-rameter need to be explicit. For either kernel alternative in the theorem, use the standard parameterisation κ(θ, θ′) = s2

> κ

k0{L−1(θ−θ′)}, where sκ > 0, L is nonsingular, and k0 is the corresponding standard isotropic Mat´ ern-ν or SE kernel. Since compact L−1Ω is contained in B(0, R ) for some R < ∞, stationarity, domain monotonicity, and rescaling give 

γκ, Ω 

> T

(υ) = γk0,L−1Ω 

> T

(υ/s κ) ≤ γk0,B(0,R ) 

> T

(υ/s κ).

By Iwazaki (2025, Theorem 7 and Corollary 8), 

γT (υ) = 

(OT d/ (2 ν+d){log( T + 1) }(4 ν+d)/(2 ν+d) , k0 is Mat´ ern-ν, ν > 1/2,

O{log( T + 1) }d+1  , k0 is SE .

Finally, IT ≥ T / (Tmax + 1) . Substitution into Eq. (B.15) proves the claim. 

B.7 Proof of the Reverse-KL Claim Proof The state-specific laws ρt, 0 and ρt, 1 have strictly positive Lebesgue densities qt, 0

and qt, 1. Hence every candidate probability law π with finite risk has a Lebesgue density q.Define 

ℓt(θ) := Et{log qt, St (θ)} , eqt(θ) := exp {ℓt(θ)}

R 

> Ω

exp {ℓt(u)} du .

Then 

Et[KL {ρ∥ρt, St }] = KL( q∥eqt) − log 

Z

> Ω

exp {ℓt(u)} du.

Thus the unique finite-risk minimiser is eqt. Moreover, ℓt(θ) equals log ¯ pt(θ) + p⋆t μt(θ) up to an additive term that does not depend on θ. It follows that eqt(θ) ∝ exp {p⋆t μt(θ)}¯pt(θ),

which is the density of ρ(p⋆t ) 

> t

. Candidate laws singular with respect to λ have infinite risk and therefore cannot improve on this minimiser. 

52 Sampling-Based Adaptive Active Learning 

# Online Appendix 

This online appendix provides additional illustrations, implementation specifications, problem-specific configurations, numerical-comparison protocols, and additional empirical results for the main article. It also gives the auxiliary levelwise convergence analysis for the finite-dimensional RFF–Matheron implementation of ESA. All notation follows the main paper. 

A Construction of the Posterior-Guided Search Region 

This section gives the numerical construction of the adaptive search region Ω t used in the SALE implementation. Let S ≥ 2, and let Xt = {θs}Ss=1 be an approximate expected-posterior sample from ¯ πt. Let ct = S−1 PSs=1 θs and 

bΣt = ( S − 1) −1

> S

X

> s=1

(θs − ct)( θs − ct)⊤.

Write bΣt = UtΛtU ⊤ 

> t

, where Ut is orthogonal and Λt is diagonal with nonnegative entries. Define the covariance-adjustment matrix by Wt = Ut(Λ† 

> t

)1/2U ⊤ 

> t

, where Λ† 

> t

denotes the Moore–Penrose inverse. When bΣt is positive definite, this is the usual whitening matrix 

UtΛ−1/2 

> t

U ⊤ 

> t

. Each sample point is transformed to covariance-adjusted coordinates by zs =(zs, 1, . . . , z s,d )⊤ = Wt(θs − ct), s = 1 , . . . , S .For a trimming level α ∈ (0 , 1), let 

zLt,j = qα/ 2 ({zs,j : s = 1 , . . . , S }) , zUt,j = q1−α/ 2 ({zs,j : s = 1 , . . . , S }) ,

for j = 1 , . . . , d , where qp(·) denotes the empirical p-quantile. Collect these coordinatewise quantiles as zLt = ( zLt, 1, . . . , z Lt,d )⊤ and zUt = ( zUt, 1, . . . , z Ut,d )⊤. The ¯ πt-guided search region is Ωt = θ ∈ Ω : zLt ≤ Wt(θ − ct) ≤ zUt ,

where the inequalities are interpreted componentwise. Smaller α gives a more conservative region, whereas larger α can accelerate BO lo-calisation but risks excluding posterior structure before the surrogate is calibrated. We recommend α ≤ 0.1 and use α = 0 .1 for every numerical example, without example-specific tuning. 

B Local-Excursion Motivation for the Allocation Proxy 

The main article defines p⋆t as a global GP excursion probability. We here link it to local design resolution near θ† 

> t

. For a nonempty compact A ⊆ Ω, define 

p⋆t (A) := Pt



sup 

> θ∈A

ft(θ) > f + 

> t

+ εimp 



.

53 Li 

Thus the global quantity in the main article is p⋆t = p⋆t (Ω) . Let g+ 

> t

(θ) := {ft(θ) − f + 

> t

} − {μt(θ) − μt(θ† 

> t

)}, which is a centred conditional GP because θ† 

> t

is Ht-measurable. Define 

mt(A) := E



sup 

> θ∈A

g+ 

> t

(θ) | H t



,s2 

> t

(A) := sup 

> θ∈A

Var t{g+ 

> t

(θ)} = sup 

> θ∈A

n

σ2 

> t

(θ) + σ2 

> t

(θ† 

> t

) − 2κt(θ, θ† 

> t

)

o

,

and bt(A) := μt(θ† 

> t

) − sup θ∈A μt(θ) + εimp . If the GP posterior has continuous sample paths and st(A) > 0, the conditional Borell–TIS inequality gives 

p⋆t (A) ≤ exp 



− {bt(A) − mt(A)}2+

2s2 

> t

(A)



, {x}+ := max( x, 0) . (B.1) Indeed, the improvement event implies sup θ∈A g+ 

> t

(θ) > b t(A), after which the bound follows from Gaussian concentration. The case st(A) = 0 is immediate. Eq. (B.1) separates the two requirements for resolving a region: μt must rule out an improvement larger than εimp ,and posterior uncertainty in function differences relative to the incumbent must be small. For r > 0, let Bt(r) := {θ ∈ Ω : ∥θ−θ† 

> t

∥Ct ≤ r}, ∥v∥Ct := ( v⊤Ctv)1/2, and define its local fill distance by ht(r) := sup θ∈Bt(r) min θi: ( θi,y i)∈D t ∥θ − θi∥Ct . Taking A = Bt(r) in Eq. (B.1) links the local excursion probability to design resolution around θ† 

> t

. In the noiseless case, 

θ† 

> t

is an evaluated input, so exact GP interpolation gives st(Bt(r)) = sup θ∈Bt(r) σt(θ). Here 

σt is the kernel power function. Under standard kernel and domain regularity conditions, power-function bounds control st(Bt(r)), while corresponding Dudley entropy bounds con-trol mt(Bt(r)), as ht(r) decreases (Dudley, 1967; Wendland, 2004). Local coverage and a common modulus of continuity for μt likewise give bt(Bt(r)) → εimp . Thus, Eq. (B.1) links increasing local resolution to a smaller excursion probability once μt rules out an improve-ment of size εimp . This conditional argument neither asserts that a particular BO policy forces ht(r) → 0 nor treats bpt,k as a calibrated estimate of p⋆t .Computing ht(r) requires a local covering calculation. SALE instead uses the curvature-adjusted nearest-neighbour statistic defined in the main article. Although ∆ loc  

> t,k

is not a fill distance, it provides a cheap local-spacing diagnostic. Moreover, 

p⋆t ≤ p⋆t (Bt(r)) + p⋆t (Ω \ Bt(r)) .

The local proxy addresses only the first term; posterior-guided restriction and continued BO address unresolved value discovery outside the neighbourhood. 

C AO Stabilisation Implication 

Positive τ makes AO respond to both the height and the local volume of a high-valued region. To illustrate this, let Ω = [ −2, 2], ε ∈ (0 , 1), and w ∈ (0 , 1/2), and define 

b(x) = −x2, s±

> ε,w

(x) = ±ε − (x − 1) 2/w 2, g±

> ε,w

(x) = max b(x), s ±

> ε,w

(x) .

The path g− 

> ε,w

has a broad global mode at x = 0 and a narrow secondary mode at x = 1 that is lower by ε. The perturbed path g+ 

> ε,w

raises the narrow mode above the broad mode, 

54 Sampling-Based Adaptive Active Learning 

giving it a unique global maximiser at x = 1. Nevertheless, 

W1,∞



δg− 

> ε,w

, δ g+

> ε,w



= g− 

> ε,w

− g+  

> ε,w ∞

= 2 ε → 0,

whereas the TS selection rule switches from x = 0 to x = 1 for every ε > 0. The broad and narrow modes of g+ 

> ε,w

have heights 0 and ε, respectively, and absolute cur-vatures HB = 2 and HN = 2 /w 2. Their local Laplace contributions to the AO normalising constant consequently satisfy 

MN (τ )/M B (τ ) ≈ (HB /H N )1/2 exp ( ε/τ ) = w exp ( ε/τ ) .

Thus, although TS always selects the maximiser x = 1 at the narrow spike under g+

> ε,w

, the broad basin at x = 0 contributes more AO mass when w exp ( ε/τ ) ≪ 1. The balance occurs near ε = τ log(1 /w ). For fixed τ > 0, a sufficiently narrow spike therefore receives little AO mass even if it is slightly higher. For fixed ( ε, w ), however, the spike dominates as τ ↓ 0, consistently with the TS limit in Proposition 7 in the main text. More generally, if two d-dimensional local modes have heights hB , h N and positive cur-vature matrices HB , H N , their local AO mass ratio is approximately 

 det( HB )det( HN )

1/2

exp {(hN − hB )/τ } .

The determinant term also shows why AO can become increasingly resistant to artificially high, narrow excursions in uncertain surrogate paths. Suppose that a narrow mode has 

r > 1 times the curvature of a broad mode in dn directions and comparable curvature in the remaining directions. Then {det( HB )/det( HN )}1/2 ≈ r−dn/2, so the narrow mode must have a height advantage of hN − hB ≳ dnτ log( r)/2 before its local AO mass matches that of the broad mode. TS, by contrast, switches to the narrow mode as soon as hN > h B ,regardless of its local volume. This distinction is relevant when the surrogate remains highly uncertain. Under a sparse design, an individual GP posterior path may contain a narrow excursion whose height is artificially inflated by posterior uncertainty, rather than reflecting a genuine feature of the objective. As the number of poorly resolved directions or effectively distinct candidate regions increases, there are more opportunities for such a large positive excursion. At the same time, if the excursion is narrow across dn directions, its volume penalty under AO compounds as r−dn/2. AO can therefore withstand an artificial height advantage of order 

dnτ log( r)/2 before moving most of its query mass to the excursion, whereas TS responds immediately once the excursion becomes the sampled-path maximiser. The relevant height advantage is measured relative to the vertical scale of the objective. Indeed, for any a ∈ R and c > 0, πcτ (d x | a + cg ) = πτ (d x | g). Thus, τ rescales with the objective, and the height–volume comparison depends on the dimensionless gap ( hN −hB )/τ ,rather than on the absolute numerical height of the excursion. Highly anisotropic or scale-separated targets with increasing dimension are susceptible to the above mechanism. Their geometry can be difficult to resolve under limited evaluations, making artificially high, narrow excursions more plausible in early GP posterior paths. This provides one possible explanation for the increasing AO-TS regret separation with d

in Figure 1 of the main text, and for the latter, more variable TV reduction of SALE-TS on the GRF–Mat´ ern and Lotka–Volterra examples. 

> 55 Li

D Proposal Kernels and Numerical Settings for ESA 

ESA is initialised at a random point of the current working expected-posterior sample Xr(t).We use m = 1024, L = 40, τℓ = 1 .2−(ℓ−1) , sℓ = 1 , L θ = 20 . For ℓ ≤ 20, Lf = 2 and Lu = 20; for ℓ > 20, Lf = 4 and Lu = 30. Terminal local refinement uses L-BFGS-B with maxit = 100 , initialised at the terminal ESA parameter state. For the proposal distribution of θ given ft, we use a mixture of Metropolis-adjusted Langevin algorithm (Roberts and Tweedie, 1996; Roberts and Rosenthal, 1998) and random-walk proposals: 

qθ(θ′ | θ) = 0 .8qℓ

> MALA

(θ′ | θ) + 0 .2qℓ

> RW

(θ′ | θ),

where 

qℓ

> MALA

(θ′ | θ) = N



θ′; θ + 0.25 2

2 Aθ∇ft(θ), 0.25 2τℓAθ + ξI 



,

and 

qℓ

> RW

(θ′ | θ) = N θ′; θ, τ ℓ Cov( Xr(t)) + ξI .

Here Aθ is a stabilised diagonal preconditioner. It is initialised from diag {Cov( Xr(t))},following the standard use of empirical covariance information in adaptive Metropolis pro-posals (Haario et al., 2001; Roberts and Rosenthal, 2009). After accepted MALA moves, it is updated using clipped coordinatewise secant ratios, a quasi-Newton-style scaling heuris-tic (Nocedal and Wright, 2006), and is reset whenever ft changes. The jitter ξ = 10 −8

prevents numerical degeneracy. The proposal distribution for the auxiliary variable u in the exchange step is the same as qℓ

> RW

.The proposal for ft is specified through the joint latent state z = ( w⊤, ε⊤)⊤ of the decoupled RFF–Matheron path in the main article. We use independent preconditioned Crank–Nicolson updates for its two Gaussian blocks: 

w′ = p1 − ρ2 w + ρzw, ε′ = p1 − ρ2 ε + ρzε,

where zw ∼ N (0, I m), zε ∼ N (0, ς 2Int ), independently, and ρ = 0 .3. These updates are reversible with respect to the product Gaussian reference law of ( w, ε). All numerical settings are fixed across the benchmark, simulation, and applied examples. 

E GP Fitting, Adaptive Refresh, and Stopping 

This section records the common GP-fitting, refresh, and stopping conventions used in the experiments. 

E.1 Initial Design and GP Fitting 

The implementation rescales Ω to [0 , 1] d and uses an automatic relevance determination squared-exponential kernel in every example. At each scheduled refit, the signal variance, length-scales, and nugget 

ϑ = ( σ2 

> f

, ℓ 1, . . . , ℓ d, ς 2),

are estimated by bϑt 

> MAP

∈ arg max ϑ {log p(yt | Θt, ϑ) + log p(ϑ)} ,

> 56 Sampling-Based Adaptive Active Learning

where p(yt | Θt, ϑ) is the GP marginal likelihood and p(ϑ) is the prior for ϑ. Specif-ically, log σf ∼ N (0 , 0.52), log σf ∈ [−10 , 10] , where σf is standardised by the observed response scale; log ℓj ∼ N { log(0 .25 √d), 0.25 2}, ℓ j ≥ 0.01; and ς2 ∼ InvGamma(2 , 10 −3),ς2 ∈ [10 −4, 10 −2]. bϑt 

> MAP

is held fixed until the next refit. Objective evaluations are exact or nearly exact, so ς2 is used operationally as a numerical nugget. 

E.2 Adaptive Refresh Schedule 

The refresh interval is adjusted using a moving calibration diagnostic based on one-step-ahead GP prediction errors. Let Wt denote a moving window of recent queried indices. For 

i ∈ Wt, define the standardised predictive residual 

ri = yi − μi−1(θi)

{σ2

> i−1

(θi) + ς2}1/2 .

The calibration diagnostic monitors whether recent residuals are approximately centred, have approximately unit variance, and do not have excessive large-tail events. Specifically, write ¯rt = 1

|Wt|

X

> i∈Wt

ri, ¯s2 

> t

= 1

|Wt|− 1

X

> i∈Wt

(ri − ¯rt)2, ¯qt = 1

|Wt|

X

> i∈Wt

1{| ri|> z cal }.

The surrogate is treated as poorly calibrated when 

|¯rt|> ϵ mean or |¯s2 

> t

− 1|> ϵ var or ¯qt > ϵ tail .

In the experiments, we set |Wt|= 20, zcal = 2, ϵmean = 0 .25, ϵvar = 0 .5, ϵtail = 0 .2. Arefresh is triggered only after the poor-calibration condition persists for three consecutive checks and the current cooldown interval has elapsed. The cooldown interval is shortened by a shrinkage factor of 0 .91 when the calibration condition is consistently poor, subject to the lower bound Tmin = 20. When the surrogate remains well calibrated, the interval is increased by a growth factor of 1 .15 up to Tmax = 200. 

E.3 Stopping Rule 

At a refresh time, we treat two successive ssMCMC samples as parallel chains, compute a coordinatewise potential scale reduction factor bRj , and set bRmax = max 1≤j≤d bRj . With 

pBO = 0 .1 and εR = 0 .01, SALE stops when bpt,k < p BO and bRmax < 1 + εR, or when the evaluation budget is exhausted. 

F Implementation and Problem-Specific Details 

This section records the problem-specific settings and the protocols used for the supplemen-tary numerical comparisons. 

F.1 Computational Protocol for Numerical Comparisons 

The internal SALE variants differ only in the BO strategy: AO/ESA, TS, or UCB. For TS and UCB, the acquisition function is optimised over Ω r(t) by eight-start L-BFGS-B with 

> 57 Li

maxit = 100 and analytic gradients. The starts are eight distinct random points from 

Xr(t). All remaining SALE components are fixed as specified in the main article and Online Appendices D and E. For the external baselines, acquisition optimisation uses the same eight-start L-BFGS-B routine, with starts drawn from an eight-point Latin hypercube design over Ω. The GP surrogate class and hyperparameter-refitting rule are matched to SALE. The external baselines have the following acquisition rules: 

• GP-UCB; (Gutmann and Corander, 2016; Li and Zhang, 2026): 

θnt+1 = arg max  

> θ∈Ω

μt(θ) + pβtσt(θ), βt = 2 log  n2 

> t

π2|Ω|/6δ , δ = 0 .01 .

• GP-UCB+; (Kim and Sanz-Alonso, 2025): 

θBO  

> nt+1

= arg max  

> θ∈Ω

μt(θ) + pβtσt(θ), βt = 2 log  n2 

> t

π2|Ω|/6δ , δ = 0 .01 ,

θrand  

> nt+1

∼Unif(Ω) .

• Log-GP Entropy; (Wang and Li, 2018): 

θnt+1 = arg max  

> θ∈Ω

μt(θ) + log σt(θ).

• Log-GP Variance; (Kandasamy et al., 2017): 

θnt+1 = arg max  

> θ∈Ω

2μt(θ) + σ2 

> t

(θ) + log[exp {σ2 

> t

(θ)} − 1] .

Note that for GP-UCB+, at each active time t, there are two evaluations based on the GP-UCB query θBO  

> nt+1

and the random exploration query θrand 

> nt+1

. Hence, the total number of iterations for GP-UCB+ is half that of the other methods, while the total number of evaluations is kept equal. Within each replicate, all methods use the same 20-point Latin hypercube initial design. For the analytic benchmarks, a given dimension and replicate also share that design across targets; designs vary across replicates. The UDG comparison likewise uses one common 20-point design for SALE and GP-UCB. 

F.2 Simulation and Applied-Example Details 

We first give the parameterisations, priors, and numerical constructions for the simulated and applied examples. 

F.2.1 GRF–Mat´ ern 

The GRF–Mat´ ern data are generated with μ = 0, σf = 1, ℓ = 0 .12, δ = 0 .10, and ν = 1 .5. The priors are μ ∼ N (0 , 22), log σf ∼ N (log 1 , 0.52), log ℓ ∼ N (log 0 .15 , 0.52), log δ ∼N (log 0 .10 , 0.52), ν ∼ Gamma(3 , 2) , restricted to μ ∈ [−4, 4], σf ∈ [0 .1, 4], ℓ ∈ [0 .01 , 1], 

δ ∈ [0 .01 , 2], ν ∈ [0 .5, 5] .

> 58

Sampling-Based Adaptive Active Learning 

F.2.2 Lotka–Volterra Model 

For inference on the Lotka–Volterra model, we use 

θ = ( mαγ , log β, d αγ , log δ, log x0, log y0, log σ) ∈ R7,

where mαγ = 12 log( αγ ) and dαγ = 12 log( α/γ ), which reduce posterior dependence between α

and γ. The data-generating value on the natural scale is θnat ,0 = (1 , 0.05 , 1.5, 0.075 , 30 , 4, 0.25) .

The prior is placed on log θnat := (log α, log β, log γ, log δ, log x0, log y0, log σ), with log θnat ∼N (log θnat ,0, Σ), Σ = diag(0 .75 2, 1, 0.75 2, 1, 0.75 2, 0.75 2, 0.75 2). The search domain is mαγ ∈

[0 .1, 0.3], log β ∈ [−4, −2] , d αγ ∈ [−0.7, 0.5], log δ ∈ [−3.5, −1.5] , log x0 ∈ [2 .5, 4], log y0 ∈

[0 .5, 2] , log σ ∈ [−2.5, −0.5] .

F.2.3 Bus-Engine Replacement Example 

Let qj (μ, r, ω ), j ∈ [Jmax ]0, denote the probabilities of the discretised mileage increment J.Specifically, q0 = ω + (1 − ω) PNB (0; μ, r ), qj = (1 − ω) PNB (j; μ, r ), j ∈ [Jmax − 1] , and the final category collects the upper tail: qJmax = (1 − ω) PNB (J ≥ Jmax ; μ, r ), J max = 24 . If the engine is kept, mileage accumulates; replacement resets the mileage state before the next increment. The transition probability from state x to x′, under action a ∈ { 0, 1}, is 

Pa, θ(x, x ′) = 

> Jmax

X

> j=0

qj (μ, r, ω )1x′ = min {(1 − a)x + j, K − 1} .

Define u0,θ(x) = −C(x), u1,θ(x) = −RC . For fixed discount factor β = 0 .9999, the choice-specific function Qa, θ and integrated value function Vθ satisfy 

Qa, θ(x) = ua, θ(x) + β

> K−1

X

> x′=0

Pa, θ(x, x ′)Vθ(x′), Vθ(x) = log[exp {Q0,θ(x)} + exp {Q1,θ(x)}] .

The resulting conditional choice probability is 

pθ(a | x) = exp {Qa, θ(x)}

exp {Q0,θ(x)} + exp {Q1,θ(x)} .

The log-likelihood combines the observed choice and transition contributions: 

ℓ(θ) = X

> i,h

{log pθ(aih | xih ) + log Paih ,θ(xih , x i,h +1 )} .

For the prior distribution, we use independent Gaussian priors on the log scale. The parameter vector θ = ( RC , c 1, c 2, c 3, μ, r, ω ) has log θ truncated to log Ω, with mean and standard-deviation m = (log 10 , log 5 , log 2 , 0, log 2 , log 5 , −1) , s = (1 , 1.5, 1.5, 1.5, 1, 1, 2). The search box Ω is RC ∈ [2 , 50], c1 ∈ [0 .01 , 50] , c 2 ∈ [0 .0025 , 20] , c 3 ∈ [0 .0025 , 10], μ ∈

[0 .2, 15] , r ∈ [0 .5, 120] , ω ∈ [3 × 10 −4, 0.95] .

59 Li 

F.2.4 UDG Quadrature-Rule Construction 

To propagate uncertainty in θ to U(s), we use the SALE posterior mode and 2 4 = 16 additional points along sign-combination directions in the local eigen-coordinate system. For each quadrature point of θ, we fit the conditional latent Gaussian model by INLA and average the resulting U(s) using the quadrature weights. A full tensor-product adaptive Gauss–Hermite rule of Bilodeau et al. (2024) requires fitting and storing at least 3 4 = 81 conditional-INLA models, which is memory-prohibitive here. 

F.3 Reference-Posterior Samples for TV Computation 

For each analytic benchmark, we generate 50 ,000 draws with the No-U-Turn Sampler in 

rstan ; after burn-in and thinning, M = 5 ,000 draws are retained. For each simulated example, adaptive Metropolis produces 20 ,000 draws, again reduced to M = 5 ,000. The retained chains have near-unity potential scale reduction factors. 

G Additional Empirical Results 

This section provides the supplementary empirical comparisons and runtime audit. 

G.1 Simple-Regret Comparison 

AO and TS attain similar simple-regret performance in Figure 9, despite AO’s lower average BO-branch regret in the main article. Thus, both methods identify competitive high-value points at broadly similar budgets, while AO makes lower-gap BO queries more consis-tently along the full trajectory. This distinction is consistent with, but not implied by, the perturbation-stability result in the main article. 

G.2 Internal SALE Inference Comparisons 

Figures 10 and 11 report the internal SALE comparisons for the analytic benchmarks and simulated examples, respectively. On the analytic benchmarks, the SALE variants are similar in the easier settings, but AO generally begins reducing TV earlier on the higher-dimensional Funnel and Rosenbrock targets. UCB reduces TV later on these targets, although it can finish slightly lower on the Bimodal examples. The separation is clearer in the simulated examples: AO reduces TV earlier and more consistently, TS transitions later and with wider variability, and UCB makes little progress within the available budgets. These trajectories support AO as the default BO branch for the tested problems while preserving the target-dependent exceptions visible in the benchmarks. 

G.3 Representative Run-Time Audit 

Table 1 reports wall-clock time for log-GP entropy, the closest external competitor in the TV comparisons. Relative to it, SALE-AO is slower for the 4D Quartic target, comparable for 6D, and faster for 8D. Log-GP entropy is 13% and 6% faster in median runtime on 

> 60

Sampling-Based Adaptive Active Learning 4D Rosenbrock 6D Rosenbrock 8D Rosenbrock                                                   

> 4D Funnel 6D Funnel 8D Funnel
> 4D Quartic 6D Quartic 8D Quartic
> 0100 200 300 400 0100 200 300 400 500 0200 400 600
> 0100 200 300 400 0100 200 300 400 500 0200 400 600
> 0100 200 300 400 0100 200 300 400 500 0200 400 600
> -3
> -2
> -1
> 0
> 1
> 2
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> -2
> -1
> 0
> 1
> 2
> -3
> -2
> -1
> 0
> 1
> 2
> -0.5
> 0.0
> 0.5
> 1.0
> -2
> 0
> 2
> -4
> -2
> 0
> -4
> -3
> -2
> -1
> 0
> 1
> -4
> -2
> 0
> Evaluation budget T
> log 10  sT
> Method AO TS UCB

Figure 9: Log simple regret log 10 sT under AO, TS, and UCB for the three unimodal bench-mark targets. Curves show medians over 50 runs; shading shows IQRs. Lower is better. the GRF–Mat´ ern and Lotka–Volterra examples, respectively. SALE-TS is within 5% of SALE-AO on both simulated examples, whereas SALE-UCB is 16% and 24% slower. These are implementation-specific, end-to-end audits rather than language-normalised complexity comparisons. The primary empirical comparison remains posterior accuracy under a fixed budget of objective evaluations. 

61 Li 4D Bimodal 6D Bimodal 8D Bimodal 

4D Rosenbrock 6D Rosenbrock 8D Rosenbrock 

4D Funnel 6D Funnel 8D Funnel 

4D Quartic 6D Quartic 8D Quartic 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0 100 200 300 400 0 100 200 300 400 500 0 200 400 600 

0.3 

0.5 

1.0 

0.1 

0.3 

1.0 

0.3 

0.5 

1.0 

0.3 

0.5 

1.0 

0.1 

0.3 

1.0 

0.03 

0.10 

0.30 

1.00 

0.03 

0.10 

0.30 

1.00 

0.1 

0.3 

1.0 

0.03 

0.10 

0.30 

1.00 

0.01 

0.10 

1.00 

0.001 

0.010 

0.100 

1.000 

0.1 

0.3 

1.0 

Evaluation budget T̂

> TV( πμT , π X)

Method AO TS UCB 

Figure 10: TV estimates dTV( πμT , π X) for SALE-AO, SALE-TS, and SALE-UCB across benchmark targets and dimensions. Curves show medians over 50 runs; shading shows IQRs. Lower is better. 

62 Sampling-Based Adaptive Active Learning GRF-Mat´ ern Lotka-Volterra            

> 0200 400 600 0250 500 750
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> Evaluation budget T̂
> TV( πμT , π X)
> Method AO TS UCB

Figure 11: TV estimates dTV( πμT , π X) for SALE-AO, SALE-TS, and SALE-UCB across simulated examples. Curves show medians over 30 runs; shading shows IQRs. Lower is better. 

Target Method n Total budget T Median [IQR] Rel. median Quartic 4D SALE-AO 50 420 138 .2 [122 .5, 143 .9] 1.00 Log-GP entropy 50 420 74 .4 [69 .5, 81 .9] 0.54 Quartic 6D SALE-AO 50 520 201 .6 [174 .7, 213 .4] 1.00 Log-GP entropy 50 520 205 .9 [186 .7, 216 .3] 1.02 Quartic 8D SALE-AO 50 620 313 .5 [288 .7, 345 .0] 1.00 Log-GP entropy 50 620 462 .3 [411 .5, 497 .9] 1.47 GRF–Mat´ ern SALE-AO 30 720 416 .8 [388 .8, 450 .2] 1.00 SALE-TS 30 720 423 .2 [390 .3, 435 .3] 1.02 SALE-UCB 30 720 484 .5 [465 .8, 505 .0] 1.16 Log-GP entropy 30 720 361 .4 [285 .3, 471 .1] 0.87 Lotka–Volterra SALE-AO 30 920 950 .8 [909 .4, 1010 .0] 1.00 SALE-TS 30 920 1000 .9 [946 .5, 1067 .0] 1.05 SALE-UCB 30 920 1175 .8 [1065 .2, 1266 .7] 1.24 Log-GP entropy 30 920 895 .7 [812 .3, 985 .9] 0.94 

Table 1: End-to-end wall-clock runtime audit. Timings are in seconds and summarised over 

n independent replicates. The relative median is computed within each target, using the default SALE implementation as the reference. 

63 Li 

H Auxiliary RFF–ESA Convergence Results 

This section records the auxiliary convergence analysis for the finite-dimensional RFF– Matheron implementation of ESA. It is not needed for the principal AO regret or UR calibration results in the main article. We adopt the standing GP setup and notation of the main article, fix a realised design Dt, and state the additional conditions used only in this section. 

H.1 RFF and ESA Assumptions Assumption 46. For each fixed realised design, assume Kt + ς2Int is positive definite after redundant exact observations have been removed, and Eρ∥ω∥22< ∞.

Assumption 47. For fixed (t, m, τ ), assume: (E1) W ⊂ Rm+nt is compact, ν(m) 

> t

has full support on W, and h(m) 

> t

is continuous on Ω×W .Write M (m) 

> t

= sup θ∈Ω,z∈W |h(m) 

> t

(θ, z)| < ∞.

(E2) Let ¯λ = λ/λ (Ω) . The parameter proposal has density qθ(θ′ | θ) with respect to ¯λ,and the latent proposal has density qz (z′ | z) with respect to ν(m) 

> t

. There exist finite positive constants qθ, ¯qθ, q z , ¯qz such that 

0 < q θ ≤ qθ(θ′ | θ) ≤ ¯qθ < ∞, ∀θ, θ′ ∈ Ω,

and 

0 < q z ≤ qz (z′ | z) ≤ ¯qz < ∞, ∀z, z′ ∈ W .

(E3) The exchange auxiliary variable is drawn exactly from πτ (· | h(m) 

> t

(·, z′)) , and each ESA sweep contains at least one θ-update and one z-exchange update. 

H.2 Levelwise Convergence of ESA under the RFF–Matheron Representation 

Fix t, condition on Dt, and fix τ > 0. For a stationary kernel κ, draw ωj ∼ ρ from its spectral distribution and bj ∼ Unif(0 , 2π), independently, and define 

ϕm(θ) = 

r 2κ(0) 

m

h

cos( ω⊤ 

> 1

θ + b1), . . . , cos( ω⊤

> m

θ + bm)

i⊤

.

Let Φt,m = [ ϕm(θ1), . . . , ϕm(θnt )] ⊤ and At = ( Kt + ς2Int )−1. Conditional on Ξ m =

{(ωj , b j )}mj=1 , the decoupled RFF–Matheron path is 

f dec  

> t,m

(θ; w, ε) = ϕm(θ)⊤w + kt(θ)⊤At {yt − Φt,m w − ε} ,

where w ∼ N (0, Im) and ε ∼ N (0, ς 2Int ) independently. This is the construction used in Appendix A.3 of the main paper. For brevity, write f dec  

> t,m

= f dec  

> t,m

(·; w, ε) below, and define 

φω,b (θ) = p2κ(0) cos( ω⊤θ + b), κm(θ, θ′) = ϕm(θ)⊤ϕm(θ′).

Set Kt,m = Φt,m Φ⊤

> t,m

, kt,m (θ) = Φt,m ϕm(θ), Gt = Kt + ς2Int , and at(θ) = G−1 

> t

kt(θ).

64 Sampling-Based Adaptive Active Learning 

Conditional on ( Dt, Ξm), f dec  

> t,m

is Gaussian with μt,m (θ) = μt(θ) and covariance 

κt,m (θ, θ′) = κm(θ, θ′) − at(θ)⊤kt,m (θ′) − at(θ′)⊤kt,m (θ)+ at(θ)⊤(Kt,m + ς2Int )at(θ′). (H.1) Let π(m) 

> τ,t

(d θ | Ξm) = E{πτ (d θ | f dec  

> t,m

) | D t, Ξm} denote the corresponding RFF-induced AO law. Define εm = ∥κm − κ∥L∞(Ω ×Ω) . The notation OPr Ξm (·) refers to stochastic order with respect to the RFF draw Ξ m, conditionally on Dt. For a probability measure on Ω × W ,the subscript ( ·)θ denotes its θ-marginal. 

Proposition 48. Under the standing GP setup of the main article and Assumption 46, for fixed (t, Dt, τ ), there exists a finite constant Cτ,t < ∞, depending on τ , Dt, ς2, and the kernel, such that TV 

n

π(m) 

> τ,t

(· | Ξm), π τ,t 

o

≤ Cτ,t εm. Moreover, εm = OPr Ξm (m−1/2),

TV 

n

π(m) 

> τ,t

(· | Ξm), π τ,t 

o

= OPr Ξm (m−1/2).

Proof The uniform RFF kernel approximation εm = OPr Ξm (m−1/2) follows from a standard empirical-process argument for the random-feature class (Sutherland and Schneider, 2015). Indeed, write gθ,θ′ (ω, b ) = φω,b (θ)φω,b (θ′), ( θ, θ′) ∈ Ω × Ω. The class G = {gθ,θ′ : ( θ, θ′) ∈

Ω × Ω} has a bounded envelope because |φω,b (θ)|≤ p2κ(0). Moreover, under the finite spectral second-moment condition in Assumption 46, it has finite L2-entropy integral as a finite-dimensional Lipschitz-parametric class over compact Ω × Ω. Hence G is Pω,b -Donsker, where Pω,b is the law of ( ω, b ), and 

√m sup 

> θ,θ′∈Ω

m−1

> m

X

> j=1

gθ,θ′ (ωj , b j ) − E gθ,θ′ (ω, b ) = OPr Ξm (1) .

Equivalently, εm = OPr Ξm (m−1/2). Let δκ m = κm − κ, δ kt,m (θ) = kt,m (θ) − kt(θ), δKt,m = Kt,m − Kt. Expanding the exact posterior covariance in the same form as Eq. (H.1) gives 

κt,m (θ, θ′)−κt(θ, θ′) = δκ m(θ, θ′)−at(θ)⊤δkt,m (θ′)−at(θ′)⊤δkt,m (θ)+ at(θ)⊤δKt,m at(θ′).

Let Ma,t = sup θ∈Ω∥at(θ)∥2. If nt = 0, set Ma,t = 0. If nt ≥ 1, Assumption 46 ensures that 

Gt = Kt + ς2Int is positive definite. Therefore, 

Ma,t ≤ ∥ G−1 

> t

∥op sup 

> θ∈Ω

∥kt(θ)∥2≤√nt sup θ,u∈Ω|κ(θ, u)|

λmin (Gt) < ∞.

The final inequality follows because nt is finite, λmin (Gt) > 0, and κ is bounded on compact Ω. Moreover, sup 

> θ∈Ω

∥δkt,m (θ)∥2≤ √nt εm, ∥δKt,m ∥op ≤ ntεm.

Consequently, 

∥μt,m − μt∥L∞(Ω) +∥κt,m − κt∥L∞(Ω ×Ω) ≤ CRFF ,t εm,

where CRFF ,t = (1 + √nt Ma,t )2.

65 Li 

It remains to transfer this posterior mean-covariance perturbation to the AO law. For a finite Borel partition Q = {C1, . . . , C N } of Ω, choose ξi ∈ Ci and write the discretised Gibbs mass of a measurable A ⊆ Ω as 

HA, Q(x) = 

PNi=1 λ(A ∩ Ci) exp( xi/τ )

PNi=1 λ(Ci) exp( xi/τ ) , x = ( x1, . . . , x N ) ∈ RN .

The sums of the absolute first and second partial derivatives of HA, Q are bounded by constants depending only on τ , uniformly in A, Q, and N . Indeed, if 

wi(x) = λ(Ci) exp( xi/τ )

PNℓ=1 λ(Cℓ) exp( xℓ/τ ) ,

then 0 ≤ HA, Q ≤ 1 and PNi=1 |∂iHA, Q(x)|≤ 1/τ, PNi,j =1 |∂ij HA, Q(x)|≤ C/τ 2 for a universal constant C < ∞.Let Zm = {f dec  

> t,m

(ξ1), . . . , f dec  

> t,m

(ξN )}⊤ and Z = {ft(ξ1), . . . , f t(ξN )}⊤. Conditionally on (Dt, Ξm), Zm is Gaussian with mean μm and covariance Σm. Conditionally on Dt, Z is Gaussian with mean μ and covariance Σ. The previous display implies max  

> i

|(μm − μ)i|+ max  

> i,j

|(Σm − Σ)ij |≤ CRFF ,t εm.

For s ∈ [0 , 1], let Zs be Gaussian with mean μs = μ + s(μm − μ) and covariance Σs =

Σ + s(Σm − Σ). Gaussian interpolation gives dds E {HA, Q(Zs)} =

> N

X

> i=1

(μm − μ)i E{∂iHA, Q(Zs)} + 12

> N

X

> i,j =1

(Σm − Σ)ij E{∂ij HA, Q(Zs)}.

Using the derivative bounds above yields 

|E {HA, Q(Zm)} − E {HA, Q(Z)}| ≤ Cτ,t εm,

where Cτ,t < ∞ is independent of A, Q, and N .Finally, take a sequence of finite Borel partitions Qn = {Cn, 1, . . . , C n,N n } of Ω such that max 1≤i≤Nn diam( Cn,i ) → 0. For each continuous sample path h, the corresponding piecewise-constant approximations converge uniformly to h on compact Ω. The Gibbs probability of A under the piecewise-constant approximation therefore converges to πτ (A |

h), and the convergence is dominated by one. Applying this to f dec  

> t,m

and ft, then using dominated convergence, gives for every measurable A ⊆ Ω, 

π(m) 

> τ,t

(A | Ξm) − πτ,t (A) ≤ Cτ,t εm.

Since the bound is uniform in A, taking the supremum over measurable A ⊆ Ω proves the total-variation bound. Combining this with the RFF rate proves the final statement. Retain the augmented latent state z = ( w⊤, ε⊤)⊤ ∈ W ⊂ Rm+nt from Appendix A.3, and write 

h(m) 

> t

(θ, z) := f dec  

> t,m

(θ; w, ε).

66 Sampling-Based Adaptive Active Learning 

Let ν(m) 

> t

be the compactly supported reference law on W stipulated in Assumption 47. Define Π(m) 

> τ,t

(d θ, dz) = πτ (d θ | h(m) 

> t

(·, z)) ν(m) 

> t

(d z),

using the AO notation of the main article. Let 

π(m),W 

> τ,t

:= {Π(m) 

> τ,t

}θ, ηm, W := TV 

n

π(m),W 

> τ,t

, π (m) 

> τ,t

(· | Ξm)

o

.

Let K(m) 

> τ,t

denote one sweep of the exact-auxiliary version of the ESA algorithm in the main article, applied to this finite-dimensional state. 

Theorem 49. Under the standing GP setup of the main article and Assumption 47, for every fixed τ > 0 and m ≥ 1, K(m) 

> τ,t

leaves Π(m) 

> τ,t

invariant and is uniformly ergodic. In particular, there exists δτ,t,m > 0, depending on the fixed (t, Dt, Ξm), such that K(m) 

> τ,t

(x, ·) ≥

δτ,t,m Π(m) 

> τ,t

(·), ∀x ∈ Ω × W , and therefore 

sup 

> x∈Ω×W

TV 

n

{K(m) 

> τ,t

}s(x, ·), Π(m)

> τ,t

o

≤ (1 − δτ,t,m )s, s ≥ 1.

Proof The θ-move is an ordinary Metropolis-Hastings update and is reversible with respect to the conditional law πτ (· | h(m) 

> t

(·, z)). For the z-move, consider the augmented proposal obtained by adding ( z′, u), where z′ ∼ qz (· | z)ν(m) 

> t

(d z′), u ∼ πτ (· | h(m) 

> t

(·, z′)) . Swapping 

z and z′ gives exactly the exchange ratio in the ESA algorithm of the main article; the factors Zτ (h(m) 

> t

(·, z)) and Zτ (h(m) 

> t

(·, z′)) cancel. This is the standard exchange argument (Murray et al., 2006). Hence the z-move is reversible with respect to the correct conditional law, and the composition leaves Π (m) 

> τ,t

invariant. It remains to prove uniform ergodicity. Let μ(m)ref := ¯λ ⊗ ν(m) 

> t

. By Assumption 47, 

|h(m) 

> t

(θ, z)|≤ M (m) 

> t

, ∀(θ, z) ∈ Ω × W .

For the θ-move, the conditional density of πτ (· | h(m) 

> t

(·, z)) with respect to ¯λ is bounded above and below by exp {2M (m) 

> t

/τ } and exp {− 2M (m) 

> t

/τ }, respectively. Hence the one-step 

θ-move kernel minorises ¯λ with constant 

εθ,τ,t,m = q2 

> θ

exp {− 4M (m) 

> t

/τ }/¯qθ > 0.

For the exchange move, the one-step exchange kernel minorises ν(m) 

> t

with constant 

εz,τ,t,m = q2 

> z

exp {− 4M (m) 

> t

/τ }/¯qz > 0.

Thus one ESA sweep minorises μ(m)ref with constant ετ,t,m = εθ,τ,t,m εz,τ,t,m .

The density of Π (m) 

> τ,t

with respect to μ(m)ref is bounded above by exp {2M (m) 

> t

/τ }, because 

Zτ (h(m) 

> t

(·, z)) ≥ λ(Ω) exp {− M (m) 

> t

/τ }.

67 Li 

Therefore, μ(m)ref (A) ≥ exp {− 2M (m) 

> t

/τ }Π(m) 

> τ,t

(A) for every measurable A ⊆ Ω×W , and hence 

K(m) 

> τ,t

(x, A ) ≥ δτ,t,m Π(m) 

> τ,t

(A), δτ,t,m = ετ,t,m exp {− 2M (m) 

> t

/τ } > 0.

Doeblin’s condition gives the stated TV convergence (Meyn and Tweedie, 2009). 

Corollary 50. Under the standing GP setup of the main article and Assumptions 46 and 47, for fixed (t, Dt, τ ), any initial state x ∈ Ω × W , and any s ≥ 1,

TV 

n

{K(m) 

> τ,t

}s(x, ·)θ, π τ,t 

o

≤ (1 − δτ,t,m )s + ηm, W + TV 

n

π(m) 

> τ,t

(· | Ξm), π τ,t 

o

.

Consequently, 

TV 

n

{K(m) 

> τ,t

}s(x, ·)θ, π τ,t 

o

≤ (1 − δτ,t,m )s + ηm, W + Cτ,t εm.

Since εm = OPr Ξm (m−1/2), the final term is OPr Ξm (m−1/2).

Proof By the triangle inequality, TV 

n

{K(m) 

> τ,t

}s(x, ·)θ, π τ,t 

o

≤ TV 

n

{K(m) 

> τ,t

}s(x, ·)θ, π (m),W

> τ,t

o

+ TV 

n

π(m),W 

> τ,t

, π (m) 

> τ,t

(· | Ξm)

o

+ TV 

n

π(m) 

> τ,t

(· | Ξm), π τ,t 

o

.

Taking marginals cannot increase total variation distance, so the first term is bounded by (1 − δτ,t,m )s by Theorem 49. The second term is ηm, W by definition. The third term is controlled by Proposition 48. 

Remark 51. The result is levelwise and assumes an exact exchange auxiliary draw. The compact W condition is a proof device for Doeblin minorisation; for a compact truncation of the Gaussian latent law, ηm, W records the corresponding truncation error. The practical noncompact latent state and finite inner chain introduce errors not controlled by this result. 

References 

R. G. Abraham and P. van Dokkum. Ultra–Low Surface Brightness Imaging with the Dragonfly Telephoto Array. PASP , 126(935):55, 2014. L. Acerbi. Variational Bayesian Monte Carlo. In S. Bengio, H. Wallach, H. Larochelle, K. Grauman, N. Cesa-Bianchi, and R. Garnett, editors, NeurIPS 2018 , pages 8213–8223. Curran Associates, Inc., 2018. L. Acerbi. An exploration of acquisition and mean functions in variational Bayesian Monte Carlo. In AABI 2019 , volume 96, pages 1–10, 2019. R. J. Adler and J. E. Taylor. Random Fields and Geometry . Springer, New York, 2007. 

68 Sampling-Based Adaptive Active Learning 

V. Aguirregabiria and P. Mira. Dynamic Discrete Choice Structural Models: A Survey. J. Econom. , 156(1):38–67, 2010. F. E. Bachl, F. Lindgren, D. L. Borchers, and J. B. Illian. inlabru: An R Package for Bayesian Spatial Modelling from Ecological Survey Data. Methods Ecol. Evol. , 10:760– 766, 2019. J. Bect, F. Bachoc, and D. Ginsbourger. A Supermartingale Approach to Gaussian Process Based Sequential Design of Experiments. Bernoulli , 25(4A):2883–2919, 2019. M. O. Berild, S. Martino, V. G´ omez-Rubio, and H. Rue. Importance Sampling with the Integrated Nested Laplace Approximation. J. Comput. Graph. Stat. , 31(4):1225–1237, 2022. B. Bilodeau, A. Stringer, and Y. Tang. Stochastic Convergence Rates and Applications of Adaptive Quadrature in Bayesian Inference. J. Am. Stat. Assoc. , 119(545):690–700, 2024. D. Bingham, T. Butler, and D. Estep. Inverse Problems for Physics-Based Process Models. 

Annu. Rev. Stat. Appl. , 11(1):461–482, 2024. R. S. Bivand, V. G´ omez-Rubio, and H. Rue. Approximate Bayesian Inference for Spatial Econometrics Models. Spat. Stat. , 9:146–165, 2014. E. Chen. Model-Adaptive Approach to Dynamic Discrete Choice Models with Large State Spaces. arXiv preprint arXiv:2501.18746 , 2025. D. Delahaye, S. Chaimatanan, and M. Mongeau. Simulated Annealing: From Basics to Applications. In M. Gendreau and J.-Y. Potvin, editors, Handbook of Metaheuristics ,volume 272 of International Series in Operations Research & Management Science , pages 1–35. Springer, Cham, 3 edition, 2019. R. M. Dudley. The Sizes of Compact Subsets of Hilbert Space and Continuity of Gaussian Processes. J. Funct. Anal. , 1(3):290–330, 1967. J. El Gammal, N. Sch¨ oneberg, J. Torrado, and C. Fidler. Fast and Robust Bayesian Infer-ence Using Gaussian Processes with GPry. JCAP , 2023(10):021, 2023. V. G´ omez-Rubio and H. Rue. Markov Chain Monte Carlo with the Integrated Nested Laplace Approximation. Stat. Comput. , 28:1033–1051, 2018. R. B. Gramacy. Surrogates: Gaussian Process Modeling, Design, and Optimization for the Applied Sciences . Chapman & Hall/CRC, 2020. M. U. Gutmann and J. Corander. Bayesian Optimization for Likelihood-Free Inference of Simulator-Based Statistical Models. J. Mach. Learn. Res. , 17(125):1–47, 2016. H. Haario, E. Saksman, and J. Tamminen. An adaptive Metropolis algorithm. Bernoulli , 7 (2):223–242, 2001. 

> 69 Li

W. E. Harris, R. A. Brown, P. R. Durrell, A. J. Romanowsky, J. Blakeslee, J. Brodie, S. Janssens, T. Lisker, S. Okamoto, and C. Wittmann. The PIPER Survey. I. An Initial Look at the Intergalactic Globular Cluster Population in the Perseus Cluster. ApJ , 890 (2):105, 2020. T. Helin, A. M. Stuart, A. L. Teckentrup, and K. C. Zygalakis. Introduction to Gaussian process regression in Bayesian inverse problems, with new results on experimental design for weighted error measures. In MCQMC 2024 , volume 460 of Springer Proceedings in Mathematics & Statistics , pages 49–79. Springer, 2024. C.-R. Hwang. Laplace’s Method Revisited: Weak Convergence of Probability Measures. 

Ann. Probab. , 8(6):1177–1182, 1980. S. Imai, N. Jain, and A. T. Ching. Bayesian Estimation of Dynamic Discrete Choice Models. 

Econometrica , 77(6):1865–1899, 2009. S. Iwazaki. Improved regret bounds for Gaussian process upper confidence bound in Bayesian optimization. In NeurIPS 2025 , volume 38, 2025. M. J¨ arvenp¨ a¨ a and J. Corander. Approximate Bayesian Inference from Noisy Likelihoods with Gaussian Process Emulated MCMC. J. Mach. Learn. Res. , 25(366):1–55, 2024. M. J¨ arvenp¨ a¨ a, M. U. Gutmann, A. Pleska, A. Vehtari, and P. Marttinen. Efficient Acquisi-tion Rules for Model-Based Approximate Bayesian Computation. Bayesian Anal. , 14(2): 595–622, 2019. M. J¨ arvenp¨ a¨ a, M. U. Gutmann, A. Vehtari, and P. Marttinen. Parallel Gaussian Process Surrogate Bayesian Inference with Noisy Likelihood Evaluations. Bayesian Anal. , 16(1): 147–178, 2021. K. Kandasamy, J. Schneider, and B. P´ oczos. Query Efficient Posterior Estimation in Sci-entific Experiments via Bayesian Active Learning. Artif. Intell. , 243:45–56, 2017. H. Kim and D. Sanz-Alonso. Enhancing Gaussian Process Surrogates for Optimization and Posterior Approximation via Random Exploration. SIAM/ASA J. Uncertain. Quantif. ,13(3):1054–1084, 2025. S. Lan, S. Li, and M. Pasha. Bayesian Spatiotemporal Modeling for Inverse Problems. Stat. Comput. , 33(4):89, 2023. P. Lartaud, P. Humbert, and J. Garnier. Solving Bayesian Inverse Problems Using Gaussian Process Regression with Goal-Oriented Active Learning. Technometrics , 68(1):172–185, 2026. D. Li and Z. Zhang. Bayesian Optimization Sequential Surrogate (BOSS) Algorithm: Fast Bayesian Inference for a Broad Class of Bayesian Hierarchical Models. Comput. Stat. Data Anal. , 213:108253, 2026. 

> 70 Sampling-Based Adaptive Active Learning

D. Li, G. M. Eadie, R. Abraham, P. E. Brown, W. E. Harris, S. R. Janssens, A. J. Ro-manowsky, P. van Dokkum, and S. Danieli. Light from the Darkness: Detecting Ultra-Diffuse Galaxies in the Perseus Cluster through Over-Densities of Globular Clusters with a Log-Gaussian Cox Process. ApJ , 935(1):3, 2022. D. Li, G. M. Eadie, P. E. Brown, W. E. Harris, R. G. Abraham, P. van Dokkum, S. R. Janssens, S. C. Berek, S. Danieli, A. J. Romanowsky, and J. S. Speagle. Discovery of Two Ultra-Diffuse Galaxies with Unusually Bright Globular Cluster Luminosity Functions via a Mark-Dependently Thinned Point Process (MATHPOP). ApJ , 984(2):147, 2025a. D. Li, Q. Liu, G. M. Eadie, R. G. Abraham, F. R. Marleau, W. E. Harris, P. van Dokkum, A. J. Romanowsky, S. Danieli, P. E. Brown, and A. Stringer. Candidate Dark Galaxy-2: Validation and Analysis of an Almost Dark Galaxy in the Perseus Cluster. ApJL , 986 (2):L18, 2025b. D. Li, A. Stringer, P. E. Brown, G. M. Eadie, and R. G. Abraham. Poisson Cluster Process Models for Detecting Ultra-Diffuse Galaxies. Ann. Appl. Stat. , 19(1):261–285, 2025c. F. Llorente, L. Martino, V. Elvira, D. Delgado-G´ omez, and J. L´ opez-Santiago. Adaptive Quadrature Schemes for Bayesian Inference via Active Learning. IEEE Access , 8:208462– 208483, 2020. C. C. Lovell et al. Learning the Universe: Cosmological and Astrophysical Parameter Inference with Galaxy Luminosity Functions and Colours. MNRAS , 544(4):3949–3979, 2025. G. Matheron. The Intrinsic Random Functions and Their Applications. Adv. Appl. Probab. ,5(3):439–468, 1973. T. Matsubara, J. Knoblauch, F.-X. Briol, and C. J. Oates. Generalized Bayesian Inference for Discrete Intractable Likelihood. J. Am. Stat. Assoc. , 119(547):2345–2355, 2024. S. P. Meyn and R. L. Tweedie. Markov Chains and Stochastic Stability . Cambridge Uni-versity Press, 2 edition, 2009. J. Møller, A. R. Syversveen, and R. P. Waagepetersen. Log Gaussian Cox Processes. Scand. J. Stat. , 25(3):451–482, 1998. I. Murray, Z. Ghahramani, and D. J. C. MacKay. MCMC for doubly-intractable distribu-tions. In UAI 2006 , pages 359–366. AUAI Press, 2006. J. Nocedal and S. J. Wright. Numerical Optimization . Springer, 2 edition, 2006. A. Norets. Inference in Dynamic Discrete Choice Models with Serially Correlated Unob-served State Variables. Econometrica , 77(5):1665–1682, 2009. A. Norets. Estimation of Dynamic Discrete Choice Models Using Artificial Neural Network Approximations. Econom. Rev. , 31(1):84–106, 2012. A. Norets and K. Shimizu. Semiparametric Bayesian Estimation of Dynamic Discrete Choice Models. J. Econom. , 238(2):105642, 2024. 

> 71 Li

R. Oliveira, L. Ott, and F. Ramos. No-regret approximate inference via Bayesian optimi-sation. In UAI 2021 , volume 161 of PMLR , pages 2082–2092. PMLR, 2021. J. Park and M. Haran. Bayesian Inference in the Presence of Intractable Normalizing Functions. J. Am. Stat. Assoc. , 113(523):1372–1390, 2018. J. Park and M. Haran. A Function Emulation Approach for Doubly Intractable Distribu-tions. J. Comput. Graph. Stat. , 29(1):66–77, 2020. M. Pellejero-Iba˜ nez, R. E. Angulo, G. Aric` o, M. Zennaro, S. Contreras, and J. St¨ ucker. Cosmological Parameter Estimation via Iterative Emulation of Likelihoods. MNRAS ,499(4):5257–5268, 2020. V. Picheny, D. Ginsbourger, O. Roustant, R. T. Haftka, and N.-H. Kim. Adaptive Designs of Experiments for Accurate Approximation of a Target Region. J. Mech. Des. , 132(7): 071008, 2010. A. Rahimi and B. Recht. Random features for large-scale kernel machines. In NeurIPS 2007 , pages 1177–1184. Curran Associates, Inc., 2007. C. E. Rasmussen and C. K. I. Williams. Gaussian Processes for Machine Learning . MIT Press, Cambridge, MA, 2006. P. Reiser, J. E. Aguilar, A. Guthke, and P.-C. B¨ urkner. Uncertainty Quantification and Propagation in Surrogate-Based Bayesian Inference. Stat. Comput. , 35(3):66, 2025. A. G. Roberts, M. Dietze, and J. H. Huggins. Propagating Surrogate Uncertainty in Bayesian Inverse Problems. arXiv preprint arXiv:2601.03532 , 2026a. A. G. Roberts, M. C. Dietze, and J. H. Huggins. Surrogate-Based Bayesian Inference: Uncertainty Quantification and Active Learning. arXiv preprint arXiv:2603.13646 , 2026b. G. O. Roberts and J. S. Rosenthal. Optimal scaling of discrete approximations to Langevin diffusions. J. R. Stat. Soc. Ser. B , 60(1):255–268, 1998. G. O. Roberts and J. S. Rosenthal. Examples of Adaptive MCMC. J. Comput. Graph. Stat. , 18(2):349–367, 2009. G. O. Roberts and R. L. Tweedie. Exponential convergence of Langevin distributions and their discrete approximations. Bernoulli , 2(4):341–363, 1996. H. Rue, S. Martino, and N. Chopin. Approximate Bayesian Inference for Latent Gaussian Models by Using Integrated Nested Laplace Approximations. J. R. Stat. Soc. Ser. B , 71 (2):319–392, 2009. D. Russo and B. Van Roy. Learning to Optimize via Posterior Sampling. Math. Oper. Res. ,39(4):1221–1243, 2014. D. J. Russo, B. Van Roy, A. Kazerouni, I. Osband, and Z. Wen. A Tutorial on Thompson Sampling. Found. Trends Mach. Learn. , 11(1):1–96, 2018. 

> 72 Sampling-Based Adaptive Active Learning

J. Rust. Optimal Replacement of GMC Bus Engines: An Empirical Model of Harold Zurcher. Econometrica , 55(5):999–1033, 1987. J. Rust. Structural Estimation of Markov Decision Processes. In R. F. Engle and D. L. McFadden, editors, Handbook of Econometrics , volume 4, chapter 51, pages 3081–3143. Elsevier, Amsterdam, 1994. G. Santin and R. Schaback. Approximation of Eigenfunctions in Kernel-Based Spaces. Adv. Comput. Math. , 42(4):973–993, 2016. S. Seo, M. Wallat, T. Graepel, and K. Obermayer. Gaussian process regression: active data selection and test point rejection. In IJCNN 2000 , volume 3, pages 241–246, 2000. B. Settles. Active Learning , volume 6 of Synthesis Lectures on Artificial Intelligence and Machine Learning . Morgan & Claypool Publishers, 2012. B. Shahriari, K. Swersky, Z. Wang, R. P. Adams, and N. de Freitas. Taking the Human Out of the Loop: A Review of Bayesian Optimization. Proc. IEEE , 104(1):148–175, 2016. N. Srinivas, A. Krause, S. M. Kakade, and M. W. Seeger. Information-Theoretic Regret Bounds for Gaussian Process Optimization in the Bandit Setting. IEEE Trans. Inf. Theory , 58(5):3250–3265, 2012. ¨O. S¨ urer, M. Plumlee, and S. M. Wild. Sequential Bayesian Experimental Design for Calibration of Expensive Simulation Models. Technometrics , 66(2):157–171, 2024. D. J. Sutherland and J. Schneider. On the error of random Fourier features. In UAI 2015 ,pages 862–871. AUAI Press, 2015. P. van Dokkum, R. Abraham, A. Merritt, J. Zhang, M. Geha, and C. Conroy. Forty-Seven Milky Way-Sized, Extremely Diffuse Galaxies in the Coma Cluster. ApJL , 798(2):L45, 2015. P. van Dokkum, D. Li, R. Abraham, S. Danieli, G. M. Eadie, W. E. Harris, and A. J. Romanowsky. Deep HST/UVIS Imaging of the Candidate Dark Galaxy CDG-1. RNAAS ,8(5):135, 2024. A. Vehtari, A. Gelman, D. Simpson, B. Carpenter, and P.-C. B¨ urkner. Rank-normalization, folding, and localization: An improved bR for assessing convergence of MCMC (with discussion). Bayesian Anal. , 16(2):667–718, 2021. P. Villani, D. Andr´ es-Arcones, J. F. Unger, and M. Weiser. Posterior Sampling with Adaptive Gaussian Processes in Bayesian Parameter Identification. arXiv preprint arXiv:2411.17858 , 2024. H. Wang and J. Li. Adaptive Gaussian Process Approximation for Bayesian Inference with Expensive Likelihood Functions. Neural Comput. , 30(11):3072–3094, 2018. H. Wendland. Scattered Data Approximation , volume 17. Cambridge University Press, 2004. 

> 73 Li

J. Whitehouse, Z. S. Wu, and A. Ramdas. On the Sublinear Regret of GP-UCB. arXiv preprint arXiv:2307.07539 , 2023. J. T. Wilson. Stopping Bayesian Optimization with Probabilistic Regret Bounds. arXiv preprint arXiv:2402.16811 , 2024. J. T. Wilson, V. Borovitskiy, A. Terenin, P. Mostowsky, and M. P. Deisenroth. Pathwise Conditioning of Gaussian Processes. J. Mach. Learn. Res. , 22(105):1–47, 2021. J. Zeghal, D. Lanzieri, F. Lanusse, A. Boucaud, G. Louppe, E. Aubourg, A. E. Bayer, and T. L. D. E. S. Collaboration. Simulation-Based Inference Benchmark for Weak Lensing Cosmology. A&A , 699:A327, 2025.