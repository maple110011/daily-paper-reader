Title: UCBound-Net: Uncertainty-Guided Boundary-Aware Continual Learning for Domain-Incremental Ultrasound Segmentation

URL Source: https://arxiv.org/pdf/2608.01518v1

Published Time: Tue, 04 Aug 2026 01:37:21 GMT

Number of Pages: 10

Markdown Content:
# UCBound-Net: Uncertainty-Guided Boundary-Aware Continual Learning for Domain-Incremental Ultrasound Segmentation 

Mohammad Amanour Rahman 

Ahsanullah University of Science and Technology, Dhaka, Bangladesh 

amanourrahman609@gmail.com 

Abstract. Continual learning in clinical imaging faces a dual challenge: a model must assimilate knowledge from new anatomical domains while faithfully retaining representations learned from prior tasks—a problem known as catastrophic forgetting. Existing mitigation strategies, including regularization and knowledge distillation, treat all spatial regions equally, ignoring the fact that prediction uncertainty is strongly correlated with the propensity for forgetting. We introduce UCBound-Net , a continual segmentation framework that exploits Monte Carlo (MC) Dropout uncer-tainty as a spatial proxy for forgetting risk. Our method contributes three synergistic components: (i) uncertainty-weighted boundary distillation ,which amplifies the knowledge-transfer signal at high-entropy regions of the frozen teacher; (ii) uncertainty-calibration regularization , which explic-itly penalizes overconfident erroneous predictions; and (iii) uncertainty-guided exemplar selection , a memory buffer that preferentially stores samples whose boundary regions exhibit the highest predictive entropy. Evaluated on a sequential domain-incremental benchmark comprising breast ultrasound (BUSI, Task 1) followed by thyroid ultrasound (TN3K, Task 2), UCBound-Net reduces backward transfer by 43.3% relative to naive fine-tuning ( ∆BWT =−0.098 vs. −0.173 ) while achieving an average Dice Similarity Coefficient (DSC) of 0.755 across both tasks—surpassing both baselines without requiring task-boundary supervision. Our ablation study confirms that each component contributes independently to forget-ting mitigation, yielding a clear design pathway for uncertainty-aware clinical continual learning. 

Keywords: Continual learning · Catastrophic forgetting · Uncertainty quantification · Medical image segmentation · Ultrasound imaging ·

Knowledge distillation 

## 1 Introduction 

Medical imaging AI systems are typically trained under a closed-world assumption: a fixed dataset, a fixed domain, and a fixed deployment target. In clinical practice, however, imaging conditions evolve continuously—new scanner hardware is installed, acquisition protocols are updated, and diagnostic focus shifts across arXiv:2608.01518v1 [cs.CV] 2 Aug 2026 2 M. A. Rahman 

anatomical regions [ 1]. When a deployed model is fine-tuned on a new data stream without protective mechanisms, it suffers catastrophic forgetting [ 2 ]: the rapid, irreversible erosion of performance on previously learned tasks. This is especially dangerous in safety-critical contexts such as ultrasound segmentation, where a model may be sequentially adapted across breast, thyroid, or abdominal imaging without access to stored patient data due to privacy constraints. Existing continual learning (CL) approaches fall into three families. Regular-ization methods [3,4 ] penalize updates to weights identified as important for prior tasks. Distillation methods [ 5, 6] preserve knowledge by enforcing consistency be-tween new and old model outputs. Replay methods [ 7, 8] store a subset of previous data or learned features for interleaved rehearsal. Despite their individual merits, none of these approaches accounts for spatial heterogeneity in forgetting risk: within a single medical image, high-uncertainty boundary pixels are far more vulnerable to forgetting than low-uncertainty interior regions, yet all pixels are treated identically during distillation or replay. A critical observation motivates our work: regions where the model is uncertain about its prediction are precisely the regions most prone to catastrophic forgetting .Boundary regions in ultrasound images, which exhibit speckle noise and low contrast, yield high predictive entropy under MC Dropout. These same regions are most easily overwritten by gradient updates during new-task learning. We hypothesize that selectively amplifying the distillation and replay signals at high-uncertainty spatial locations will yield substantially better forgetting resistance. We make the following contributions: 1. Uncertainty-weighted boundary distillation: a novel Knowledge Dis-tillation (KD) loss in which the pixel-wise transfer weight is derived from the teacher model’s MC Dropout entropy map, concentrating the distillation gradient on high-risk boundary regions. 2. Uncertainty-calibration regularizer: an auxiliary loss that encourages the student’s uncertainty map to be consistent with its own prediction errors, improving calibration across tasks. 3. Uncertainty-guided exemplar buffer: a memory construction strategy that selects training exemplars ranked by predictive boundary entropy, en-suring the most forgetting-prone samples are preferentially preserved. Together, these components form UCBound-Net, which we evaluate on a two-task domain-incremental benchmark using two publicly available ultrasound datasets. UCBound-Net reduces backward transfer by 43.3% over naive fine-tuning and achieves the best calibration (ECE = 0.032) of all compared methods, with a fully ablated analysis confirming the independent contribution of each component. 

## 2 Related Work 

Continual learning for medical image analysis. Continual learning in medi-cal imaging has received growing attention, with studies addressing incremental UCBound-Net: Uncertainty-Guided Continual Ultrasound Segmentation 3

organ segmentation [ 9], multi-site adaptation [ 10 ], and class-incremental classifi-cation [ 11 ]. However, the intersection of uncertainty quantification with continual segmentation in ultrasound has not been explored, representing the gap addressed here. 

Knowledge distillation in continual learning. Learning without Forgetting (LwF) [ 5 ] pioneered the use of soft targets from a frozen teacher to regularize fine-tuning. Subsequent works [ 6 ,12 ] improved upon this by operating in feature space or using pooled distillation objectives. None of these methods incorporates spatial uncertainty to weight the distillation signal, which we identify as a key design omission for segmentation. 

Uncertainty quantification in segmentation. MC Dropout [ 13 ] provides a practical approximation to Bayesian inference for deep networks, enabling the computation of predictive entropy at each spatial location. Uncertainty-aware segmentation has been applied to active learning [14], out-of-distribution detection [ 15 ], and test-time adaptation [ 16 ], but not to continual learning distillation weighting—our primary innovation. 

Rehearsal-based CL. Experience Replay [ 7 ] and its variants store a subset of prior data in a fixed-size memory buffer. Herding [ 17 ] and gradient-based [ 18 ]selection strategies improve buffer quality. We propose a complementary selection criterion based on boundary uncertainty, grounded in the forgetting-risk intuition rather than class-prototype proximity. 

## 3 Methodology 

3.1 Problem Formulation 

We consider a domain-incremental continual learning (DICL) setting with two sequential tasks. Task 1 presents breast ultrasound images D1 = {(x1 

> i

, y1 

> i

)}

(BUSI: benign and malignant lesion segmentation) and Task 2 presents thyroid ultrasound images D2 = {(x2 

> j

, y2 

> j

)} (TN3K: nodule segmentation). After training on D1, access to D1 is removed (privacy constraint). The model must then learn 

D2 while preserving performance on D1. Task identities are unknown at inference time ; no task-boundary oracle is assumed. 

3.2 Network Architecture 

The overall architecture of UCBound-Net is illustrated in Figure 1.UCBound-Net adopts a U-Net decoder [ 19 ] with an EfficientNet-B2 [ 20 ] encoder pre-trained on ImageNet. MC Dropout layers ( p=0.3) are inserted in the decoder bottleneck, enabling stochastic forward passes for uncertainty estimation without additional network parameters. This rate lies within the range commonly adopted for MC Dropout uncertainty estimation [ 13 ] and was confirmed on the Task 1 validation split by comparing p ∈ { 0.1, 0.2, 0.3, 0.4}: p=0.3 gave the best balance between segmentation accuracy and a sufficiently sharp, spatially informative uncertainty map (higher rates degraded DSC without improving boundary localization of U). 4 M. A. Rahman 

Fig. 1: Architecture of UCBound-Net. 

3.3 Uncertainty Estimation via MC Dropout 

Given an input image x, we perform S=10 stochastic forward passes with dropout active to obtain a set of sigmoid probability maps {ps(x)}Ss=1 . The mean prediction and predictive entropy (uncertainty map) are: 

¯p(x) = 1

S

> S

∑

> s=1

ps(x), U(x) = −¯p log ¯ p − (1 − ¯p) log(1 − ¯p). (1) The map U(x) ∈ [0 , ln 2] H×W attains its maximum at decision boundaries and speckle-corrupted regions—precisely the locations most vulnerable to forgetting. 

3.4 UCBound Training Objective 

The total continual training loss during Task 2 is: 

L = Lseg + λd LUCBound + λu Lcalib + λr Lreplay , (2) where λd= 0.5, λu= 0.3, λr = 0.5. These coefficients were chosen via a grid search over {0.1, 0.3, 0.5, 0.7} on the Task 2 validation split, selecting the combination that minimized ∆BWT degradation while keeping Task 2 validation loss within 1% of the unregularized baseline. 

Segmentation loss. Lseg is the standard combination of binary cross-entropy and Dice loss applied to Task 2 predictions. UCBound-Net: Uncertainty-Guided Continual Ultrasound Segmentation 5

Uncertainty-weighted boundary distillation (UCBound loss). Let fθ and 

fθ∗ denote the student (current) and teacher (frozen Task 1) models, respectively. Given Task 2 input x2, the teacher’s uncertainty map U∗ is computed via Eq. (1) with dropout active on fθ∗ . We normalize it to a pixel-wise weight map: 

wij = exp (10 · U ∗

> ij

)∑ 

> i′j′

exp 

(

10 · U ∗

> i′j′

) . (3) The scalar 10 in Eq. (3) acts as a sharpening (temperature-like) factor applied before softmax normalization. Since raw entropy values lie in the narrow range 

U ∈ [0 , ln 2] , using the unscaled values would produce a nearly uniform weight map wij , diluting the intended emphasis on high-uncertainty boundary regions. We found the weighting contrast to be stable for scale values in [5 , 15] , and fixed it at 10 for all experiments. The distillation loss is then: 

LUCBound = ∑

> ij

wij 

(

σ

( fθ (x2)ij 

> τ

)

− σ

( fθ∗ (x2)ij 

> τ

)) 2

, (4) with temperature τ = 4.0 and σ the sigmoid function. Unlike LwF [ 5], which assigns uniform weight to all pixels, Eq. (4) concentrates the distillation gradient at high-entropy boundary regions identified by the teacher. 

Uncertainty-calibration regularizer. To promote calibration, we penalize configurations in which the student is overconfident at erroneous predictions. The student’s own uncertainty map Uθ (Eq. (1) ) is encouraged to be spatially aligned with prediction error: 

Lcalib = −Eij [|σ(fθ )ij − yij | · log( Uθ,ij + ϵ)] . (5) 

3.5 Uncertainty-Guided Exemplar Buffer 

After Task 1 training, we construct a memory buffer M of size |M| =60 using the frozen Task 1 model. Each training sample (x1 

> i

, y1 

> i

) is scored by its mean predictive entropy si = Eij [U∗ 

> ij

], and the top-|M| samples are retained. This uncertainty-guided selection ensures the buffer contains the samples that are most informative about high-risk boundary distributions. During Task 2 training, a mini-batch sampled from M is appended to each update step under Lreplay = Lseg (M),providing gradient signal that counteracts forgetting without storing the entire Task 1 dataset. Storing only 60 samples (11.6% of Task 1 training data) makes this approach both memory-efficient and privacy-compatible. 

## 4 Experiments 

4.1 Datasets and Evaluation Protocol Task 1 — BUSI. The Breast Ultrasound Images dataset [ 21 ] contains 647 images (benign and malignant classes) with pixel-level segmentation masks. We use 517 images for training and 130 for validation (80/20 stratified split). 6 M. A. Rahman 

Table 1: Continual learning results on the BUSI →TN3K sequential benchmark. Task 1 (T1) metrics are measured post-Task 2 training. Task 2 (T2) DSC is on the held-out TN3K test set. ∆BWT ↑: closer to zero indicates less forgetting. Best results in bold .                                         

> Method Task 1 (BUSI) Task 2 (TN3K) ∆BWT ↑Avg DSC ↑
> DSC ↑IoU ↑ECE ↓DSC ↑IoU ↑ECE ↓
> Naive Finetune 0.621 0.527 0.041 0.822 0.727 0.022 −0.173 0.722 LwF [5] 0.639 0.543 0.036 0.813 0.716 0.022 −0.155 0.726
> UCBound-Net (Ours) 0.696 0.600 0.032 0.809 0.713 0.023 −0.098 0.753
> T1-only reference 0.794 0.714 0.014 —————

Task 2 — TN3K. The Thyroid Nodule 3K dataset [ 22 ] provides 2,879 training images (split 85/15 into train/val) and a held-out test set of 614 images. Final Task 2 performance is reported on this unseen test set. 

Sequential protocol. The model is trained on BUSI for 30 epochs, after which Task 1 data is removed. UCBound-Net then trains on TN3K for 30 additional epochs using the full continual objective (Eq. (2) ). Task identity is not provided at any stage. All baselines follow the same sequential schedule. 

Metrics. We report DSC, Intersection-over-Union (IoU), Precision, and Recall for segmentation quality; Expected Calibration Error (ECE) [ 24 ] for probabilistic reliability; and Backward Transfer: 

∆BWT = DSC after  

> 1

− DSC before  

> 1

(6) as the primary forgetting measure. A value of 0 implies no forgetting; more negative values indicate more severe degradation. 

4.2 Implementation Details 

All experiments use PyTorch with a single NVIDIA T4 GPU (16 GB). The encoder is EfficientNet-B2 pre-trained on ImageNet; the decoder follows the standard U-Net architecture from the SMP library [ 23 ]. Task 1 uses AdamW (lr = 1×10 −4, weight decay 10 −4) with cosine annealing. Task 2 uses AdamW (lr = 5×10 −5) with the same scheduler. All images are resized to 256 ×256 with standard ultrasound augmentation (horizontal/vertical flips, random brightness and contrast, Gaussian noise, elastic deformation). Reproducibility is ensured with fixed random seed (42). 

4.3 Comparison with Baselines 

Table 1 presents quantitative results for three methods: Naive Fine-tuning (direct adaptation to Task 2 without forgetting protection), LwF (standard uniform distillation without uncertainty weighting [5]), and UCBound-Net. UCBound-Net: Uncertainty-Guided Continual Ultrasound Segmentation 7

Table 2: Ablation study on the BUSI →TN3K benchmark. Components are added cumulatively. T1 DSC and ∆BWT measure forgetting resistance; T2 DSC measures plasticity. Each component yields a measurable, independent gain.                         

> Configuration T1 DSC ↑T2 DSC ↑Avg DSC ↑∆BWT ↑
> (A) Baseline (no protection) 0.571 0.808 0.690 −0.223 (B) A + Uncertainty Distillation 0.615 0.812 0.714 −0.179 (C) B + Uncertainty Calibration 0.639 0.815 0.727 −0.155 (D) C + Uncertainty Buffer 0.671 0.818 0.745 −0.123 (E) D + Full Synergy (UCBound-Net) 0.689 0.820 0.755 −0.105

UCBound-Net achieves a Task 1 DSC of 0.696 , representing a 43.3% reduction in backward transfer relative to naive fine-tuning ( ∆BWT =−0.098 vs. −0.173 )and a 36.8% improvement over LwF ( −0.155 ). The average DSC across both tasks (0.753 ) surpasses both baselines, exceeding LwF by +4 .3% . Notably, UCBound-Net achieves the best Task 1 calibration ( ECE =0.032 ), confirming that the calibration regularizer (Eq. (5) ) yields well-calibrated predictions even after substantial domain shift. Task 2 DSC remains competitive ( 0.809 ), only marginally below Naive Fine-tuning ( 0.822 ), demonstrating that stronger forgetting resistance does not impair new-task plasticity. These trends are visualised in Fig. 2. The left panel reveals a persistent DSC gap between UCBound-Net and both baselines throughout all 30 Task 2 epochs: the uncertainty-guided distillation signal continuously prevents the degradation seen in competing methods from the very first epoch of Task 2 training. Impor-tantly, the right panel confirms that all three methods achieve similar Task 2 asymptotic performance, ruling out the possibility that UCBound-Net’s forgetting advantage is purchased at the cost of reduced plasticity. 

4.4 Ablation Study 

Table 2 decomposes the contribution of each UCBound-Net component by pro-gressively adding them to the naive baseline. Several trends emerge from the ablation study. Uncertainty-weighted distilla-tion (B) provides the largest individual gain (+4.4% T1 DSC), highlighting the benefit of focusing retention on high-uncertainty regions. Calibration regulariza-tion (C) further improves retention (+2.4%), while the uncertainty-guided replay buffer (D) contributes an additional +3.2%. Combining all components (E) yields the highest average DSC (0.755) and the lowest forgetting, demonstrating their complementary effects. 

4.5 Hyperparameter Sensitivity 

To address concerns about hyperparameter selection, we report the effect of varying the MC Dropout rate p and the loss weights (λd, λ u, λ r ) on Task 1 8 M. A. Rahman 

Fig. 2: Validation DSC during Task 2 training, demonstrating that UCBound-Net reduces forgetting without compromising new-task learning. Table 3: Sensitivity of UCBound-Net to the MC Dropout rate and distillation temperature scale.                

> Setting ∆BWT ↑Avg DSC ↑
> p= 0 .1−0.132 0.741
> p= 0 .2−0.117 0.748
> p= 0 .3(used) −0.098 0.755
> p= 0 .4−0.101 0.749

retention ( ∆BWT ) and average DSC. Table 3 summarizes the results; all other settings follow Sec. 4. 

## 5 Discussion 

Uncertainty as a forgetting proxy. Our central observation is that MC Dropout uncertainty correlates with regions most vulnerable to catastrophic forgetting. In ultrasound images, boundary areas typically exhibit high predictive entropy due to speckle noise and low tissue contrast. During adaptation to a new domain, these regions undergo larger parameter updates and are more susceptible to forgetting. By weighting distillation with uncertainty (Eq. (4) ), UCBound-Net focuses retention on these forgetting-prone locations. To our knowledge, this is the first use of uncertainty as an explicit forgetting signal for continual medical segmentation. 

Plasticity-stability trade-off. Continual learning methods must preserve prior knowledge while remaining adaptable to new tasks. UCBound-Net achieves a favourable balance: Task 2 DSC ( 0.809 ) remains close to Naive Fine-tuning (0.822 ), while Task 1 retention improves by 7.5 absolute DSC points. As shown in Fig. 2, Task 2 learning curves closely match competing methods, indicating that the proposed distillation and replay objectives do not hinder adaptation. UCBound-Net: Uncertainty-Guided Continual Ultrasound Segmentation 9

Calibration and reliability. Unlike conventional CL approaches that optimise only segmentation accuracy, UCBound-Net incorporates calibration through 

Lcalib , achieving the lowest Task 1 ECE ( 0.032 ). In clinical settings, reliable uncertainty estimates are essential because forgotten knowledge should manifest as increased uncertainty rather than overconfident errors. 

Memory efficiency. The uncertainty-guided replay buffer stores only 60 images (11.6requirements compared with large replay-based approaches. This design is attractive for privacy-sensitive clinical environments and could be further extended to feature-level exemplar storage. 

Limitations. The current study evaluates a two-task domain-incremental setting (BUSI →TN3K), which allows precise, controlled measurement of ∆BWT but does not establish stability under longer task sequences. As task count increases, the uncertainty-guided buffer’s fixed capacity ( |M| =60 ) will need to be apportioned across more prior tasks, which may attenuate its per-task effectiveness. Future work should extend UCBound-Net to three or more sequential anatomical domains (e.g., adding abdominal or cardiac ultrasound), investigate adaptive temperature scheduling for uncertainty weighting, and explore extension to 3D ultrasound volumes. 

## 6 Conclusion 

We presented UCBound-Net, a continual medical image segmentation framework that leverages predictive uncertainty as a spatial indicator of catastrophic forget-ting risk. By combining uncertainty-weighted distillation, calibration regulariza-tion, and uncertainty-guided exemplar replay, the proposed method substantially improves knowledge retention on the BUSI →TN3K benchmark, reducing for-getting relative to both Naive Fine-tuning and LwF while achieving the best calibration performance (ECE = 0.032) and a competitive average DSC of 0.753. The framework is lightweight, memory-efficient, and requires no additional model parameters beyond the base U-Net. These results suggest that uncertainty can serve not only as a measure of prediction confidence but also as an effective signal for continual learning, providing a promising direction for adaptive and reliable medical AI systems operating under domain shift. 

## References          

> 1. Perkonigg, M., et al.: Dynamic memory to alleviate catastrophic forgetting in continual learning. Nat. Commun. 12 , 5532 (2021) 2. McCloskey, M., Cohen, N.J.: Catastrophic interference in connectionist networks. Psychol. Learn. Motiv. 24 , 109–165 (1989) 3. Kirkpatrick, J., et al.: Overcoming catastrophic forgetting in neural networks. Proc. Natl. Acad. Sci. 114 (13), 3521–3526 (2017) 4. Zenke, F., Poole, B., Ganguli, S.: Continual learning through synaptic intelligence. In: ICML, pp. 3987–3995 (2017) 5. Li, Z., Hoiem, D.: Learning without forgetting. IEEE Trans. Pattern Anal. Mach. Intell. 40 (12), 2935–2947 (2018)

10 M. A. Rahman 6. Douillard, A., et al.: PODNet: Pooled outputs distillation for small-tasks incremental learning. In: ECCV, pp. 86–102. Springer (2020) 7. Rolnick, D., et al.: Experience replay for continual learning. NeurIPS 32 (2019) 8. Buzzega, P., et al.: Dark experience for general continual learning. NeurIPS 33 ,15920–15930 (2020) 9. Gonzalez, C., et al.: Lifelong nnU-Net: A framework for standardized medical continual learning. arXiv:2306.07191 (2023) 10. Srivastava, A., et al.: SEQUEL: Sequential continual learning for image segmentation. In: MICCAI Workshop (2021) 11. Shenaj, D., et al.: Learning across domains and devices: Style-driven source-free domain adaptation in clustered federated learning. In: WACV, pp. 663–672 (2023) 12. Zhu, F., et al.: Self-sustaining representation expansion for non-exemplar class-incremental learning. In: CVPR, pp. 9286–9295 (2022) 13. Gal, Y., Ghahramani, Z.: Dropout as a Bayesian approximation. In: ICML, pp. 1050–1059 (2016) 14. Houlsby, N., et al.: Bayesian active learning for classification and preference learning. arXiv:1112.5745 (2011) 15. Mukhoti, J., Gal, Y.: Evaluating Bayesian deep learning methods for semantic segmentation. arXiv:1811.12709 (2018) 16. Wang, D., et al.: Tent: Fully test-time adaptation by entropy minimization. In: ICLR (2021) 17. Rebuffi, S.A., et al.: iCaRL: Incremental classifier and representation learning. In: CVPR, pp. 2001–2010 (2017) 18. Chaudhry, A., et al.: Efficient lifelong learning with A-GEM. In: ICLR (2019) 19. Ronneberger, O., Fischer, P., Brox, T.: U-Net: Convolutional networks for biomedical image segmentation. In: MICCAI, pp. 234–241. Springer (2015) 20. Tan, M., Le, Q.: EfficientNet: Rethinking model scaling for convolutional neural networks. In: ICML, pp. 6105–6114 (2019) 21. Al-Dhabyani, W., et al.: Dataset of breast ultrasound images. Data Brief 28 , 104863 (2020) 22. Gong, H., et al.: Multi-task learning for thyroid nodule segmentation with thyroid region prior. In: ISBI, pp. 257–261. IEEE (2021) 23. Iakubovskii, P.: Segmentation models PyTorch. GitHub (2019). https://github. com/qubvel/segmentation_models.pytorch 

24. Guo, C., et al.: On calibration of modern neural networks. In: ICML, pp. 1321–1330 (2017)